// GENERATED FILE. Do not edit directly.
// Source: src/js/messaging.ts
// Build command: npm run build
(() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __typeError = (msg) => {
    throw TypeError(msg);
  };
  var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
  var __require = /* @__PURE__ */ ((x2) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x2, {
    get: (a, b2) => (typeof require !== "undefined" ? require : a)[b2]
  }) : x2)(function(x2) {
    if (typeof require !== "undefined") return require.apply(this, arguments);
    throw Error('Dynamic require of "' + x2 + '" is not supported');
  });
  var __esm = (fn2, res) => function __init() {
    return fn2 && (res = (0, fn2[__getOwnPropNames(fn2)[0]])(fn2 = 0)), res;
  };
  var __export = (target, all) => {
    for (var name2 in all)
      __defProp(target, name2, { get: all[name2], enumerable: true });
  };
  var __copyProps = (to2, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to2, key) && key !== except)
          __defProp(to2, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to2;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));
  var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
  var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
  var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
  var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
  var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
  var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, "access private method"), method);

  // src/lib/csstree/css-tree.js
  function B(e) {
    return e >= 48 && e <= 57;
  }
  function ee(e) {
    return B(e) || e >= 65 && e <= 70 || e >= 97 && e <= 102;
  }
  function kt(e) {
    return e >= 65 && e <= 90;
  }
  function ls(e) {
    return e >= 97 && e <= 122;
  }
  function cs(e) {
    return kt(e) || ls(e);
  }
  function us(e) {
    return e >= 128;
  }
  function yt(e) {
    return cs(e) || us(e) || e === 95;
  }
  function Ne(e) {
    return yt(e) || B(e) || e === 45;
  }
  function ps(e) {
    return e >= 0 && e <= 8 || e === 11 || e >= 14 && e <= 31 || e === 127;
  }
  function Je(e) {
    return e === 10 || e === 13 || e === 12;
  }
  function pe(e) {
    return Je(e) || e === 32 || e === 9;
  }
  function $(e, t) {
    return !(e !== 92 || Je(t) || t === 0);
  }
  function ze(e, t, r) {
    return e === 45 ? yt(t) || t === 45 || $(t, r) : yt(e) ? true : e === 92 ? $(e, t) : false;
  }
  function wt(e, t, r) {
    return e === 43 || e === 45 ? B(t) ? 2 : t === 46 && B(r) ? 3 : 0 : e === 46 ? B(t) ? 2 : 0 : B(e) ? 1 : 0;
  }
  function vt(e) {
    return e === 65279 || e === 65534 ? 1 : 0;
  }
  function Ct(e) {
    return e < 128 ? or[e] : St;
  }
  function Me(e, t) {
    return t < e.length ? e.charCodeAt(t) : 0;
  }
  function At(e, t, r) {
    return r === 13 && Me(e, t + 1) === 10 ? 2 : 1;
  }
  function de(e, t, r) {
    let n = e.charCodeAt(t);
    return kt(n) && (n = n | 32), n === r;
  }
  function ge(e, t, r, n) {
    if (r - t !== n.length || t < 0 || r > e.length) return false;
    for (let o = t; o < r; o++) {
      let i = n.charCodeAt(o - t), s = e.charCodeAt(o);
      if (kt(s) && (s = s | 32), s !== i) return false;
    }
    return true;
  }
  function _o(e, t) {
    for (; t >= 0 && pe(e.charCodeAt(t)); t--) ;
    return t + 1;
  }
  function tt(e, t) {
    for (; t < e.length && pe(e.charCodeAt(t)); t++) ;
    return t;
  }
  function sr(e, t) {
    for (; t < e.length && B(e.charCodeAt(t)); t++) ;
    return t;
  }
  function se(e, t) {
    if (t += 2, ee(Me(e, t - 1))) {
      for (let n = Math.min(e.length, t + 5); t < n && ee(Me(e, t)); t++) ;
      const r = Me(e, t);
      pe(r) && (t += At(e, t, r));
    }
    return t;
  }
  function rt(e, t) {
    for (; t < e.length; t++) {
      const r = e.charCodeAt(t);
      if (!Ne(r)) {
        if ($(r, Me(e, t + 1))) {
          t = se(e, t) - 1;
          continue;
        }
        break;
      }
    }
    return t;
  }
  function Te(e, t) {
    let r = e.charCodeAt(t);
    if ((r === 43 || r === 45) && (r = e.charCodeAt(t += 1)), B(r) && (t = sr(e, t + 1), r = e.charCodeAt(t)), r === 46 && B(e.charCodeAt(t + 1)) && (t += 2, t = sr(e, t)), de(e, t, 101)) {
      let n = 0;
      r = e.charCodeAt(t + 1), (r === 45 || r === 43) && (n = 1, r = e.charCodeAt(t + 2)), B(r) && (t = sr(e, t + 1 + n + 1));
    }
    return t;
  }
  function Tt(e, t) {
    for (; t < e.length; t++) {
      const r = e.charCodeAt(t);
      if (r === 41) {
        t++;
        break;
      }
      $(r, Me(e, t + 1)) && (t = se(e, t));
    }
    return t;
  }
  function Re(e) {
    if (e.length === 1 && !ee(e.charCodeAt(0))) return e[0];
    let t = parseInt(e, 16);
    return (t === 0 || t >= 55296 && t <= 57343 || t > 1114111) && (t = 65533), String.fromCodePoint(t);
  }
  function Be(e = null, t) {
    return e === null || e.length < t ? new Uint32Array(Math.max(t + 1024, 16384)) : e;
  }
  function Ho(e) {
    let t = e.source, r = t.length, n = t.length > 0 ? vt(t.charCodeAt(0)) : 0, o = Be(e.lines, r), i = Be(e.columns, r), s = e.startLine, u = e.startColumn;
    for (let c = n; c < r; c++) {
      const a = t.charCodeAt(c);
      o[c] = s, i[c] = u++, (a === Uo || a === jo || a === ms) && (a === jo && c + 1 < r && t.charCodeAt(c + 1) === Uo && (c++, o[c] = s, i[c] = u), s++, u = 1);
    }
    o[r] = s, i[r] = u, e.lines = o, e.columns = i, e.computed = true;
  }
  function ve(e, t) {
    function r(p) {
      return p < u ? e.charCodeAt(p) : 0;
    }
    function n() {
      if (a = Te(e, a), ze(r(a), r(a + 1), r(a + 2))) {
        l = 12, a = rt(e, a);
        return;
      }
      if (r(a) === 37) {
        l = 11, a++;
        return;
      }
      l = 10;
    }
    function o() {
      const p = a;
      if (a = rt(e, a), ge(e, p, a, "url") && r(a) === 40) {
        if (a = tt(e, a + 1), r(a) === 34 || r(a) === 39) {
          l = 2, a = p + 4;
          return;
        }
        s();
        return;
      }
      if (r(a) === 40) {
        l = 2, a++;
        return;
      }
      l = 1;
    }
    function i(p) {
      for (p || (p = r(a++)), l = 5; a < e.length; a++) {
        const m = e.charCodeAt(a);
        switch (Ct(m)) {
          case p:
            a++;
            return;
          case et:
            if (Je(m)) {
              a += At(e, a, m), l = 6;
              return;
            }
            break;
          case 92:
            if (a === e.length - 1) break;
            const f = r(a + 1);
            Je(f) ? a += At(e, a + 1, f) : $(m, f) && (a = se(e, a) - 1);
            break;
        }
      }
    }
    function s() {
      for (l = 7, a = tt(e, a); a < e.length; a++) {
        const p = e.charCodeAt(a);
        switch (Ct(p)) {
          case 41:
            a++;
            return;
          case et:
            if (a = tt(e, a), r(a) === 41 || a >= e.length) {
              a < e.length && a++;
              return;
            }
            a = Tt(e, a), l = 8;
            return;
          case 34:
          case 39:
          case 40:
          case ar:
            a = Tt(e, a), l = 8;
            return;
          case 92:
            if ($(p, r(a + 1))) {
              a = se(e, a) - 1;
              break;
            }
            a = Tt(e, a), l = 8;
            return;
        }
      }
    }
    e = String(e || "");
    let u = e.length, c = vt(r(0)), a = c, l;
    for (; a < u; ) {
      const p = e.charCodeAt(a);
      switch (Ct(p)) {
        case et:
          l = 13, a = tt(e, a + 1);
          break;
        case 34:
          i();
          break;
        case 35:
          Ne(r(a + 1)) || $(r(a + 1), r(a + 2)) ? (l = 4, a = rt(e, a + 1)) : (l = 9, a++);
          break;
        case 39:
          i();
          break;
        case 40:
          l = 21, a++;
          break;
        case 41:
          l = 22, a++;
          break;
        case 43:
          wt(p, r(a + 1), r(a + 2)) ? n() : (l = 9, a++);
          break;
        case 44:
          l = 18, a++;
          break;
        case 45:
          wt(p, r(a + 1), r(a + 2)) ? n() : r(a + 1) === 45 && r(a + 2) === 62 ? (l = 15, a = a + 3) : ze(p, r(a + 1), r(a + 2)) ? o() : (l = 9, a++);
          break;
        case 46:
          wt(p, r(a + 1), r(a + 2)) ? n() : (l = 9, a++);
          break;
        case 47:
          r(a + 1) === 42 ? (l = 25, a = e.indexOf("*/", a + 2), a = a === -1 ? e.length : a + 2) : (l = 9, a++);
          break;
        case 58:
          l = 16, a++;
          break;
        case 59:
          l = 17, a++;
          break;
        case 60:
          r(a + 1) === 33 && r(a + 2) === 45 && r(a + 3) === 45 ? (l = 14, a = a + 4) : (l = 9, a++);
          break;
        case 64:
          ze(r(a + 1), r(a + 2), r(a + 3)) ? (l = 3, a = rt(e, a + 1)) : (l = 9, a++);
          break;
        case 91:
          l = 19, a++;
          break;
        case 92:
          $(p, r(a + 1)) ? o() : (l = 9, a++);
          break;
        case 93:
          l = 20, a++;
          break;
        case 123:
          l = 23, a++;
          break;
        case 125:
          l = 24, a++;
          break;
        case ir:
          n();
          break;
        case St:
          o();
          break;
        default:
          l = 9, a++;
      }
      t(l, c, c = a);
    }
  }
  function Ee(e, t) {
    const r = Object.create(SyntaxError.prototype), n = new Error();
    return Object.assign(r, { name: e, message: t, get stack() {
      return (n.stack || "").replace(/^(.+\n){1,3}/, `${e}: ${t}
`);
    } });
  }
  function Yo({ source: e, line: t, column: r }, n) {
    function o(l, p) {
      return i.slice(l, p).map((m, f) => `${String(l + f + 1).padStart(c)} |${m}`).join(`
`);
    }
    let i = e.split(/\r\n?|\n|\f/), s = Math.max(1, t - n) - 1, u = Math.min(t + n, i.length + 1), c = Math.max(4, String(u).length) + 1, a = 0;
    r += (Wo.length - 1) * (i[t - 1].substr(0, r - 1).match(/\t/g) || []).length, r > lr && (a = r - qo + 3, r = qo - 2);
    for (let l = s; l <= u; l++) l >= 0 && l < i.length && (i[l] = i[l].replace(/\t/g, Wo), i[l] = (a > 0 && i[l].length > a ? "\u2026" : "") + i[l].substr(a, lr - 2) + (i[l].length > a + lr - 1 ? "\u2026" : ""));
    return [o(s, t), `${new Array(r + c + 2).join("-")}^`, o(t, u)].filter(Boolean).join(`
`);
  }
  function cr(e, t, r, n, o) {
    return Object.assign(Ee("SyntaxError", e), { source: t, offset: r, line: n, column: o, sourceFragment(s) {
      return Yo({ source: t, line: n, column: o }, isNaN(s) ? 0 : s);
    }, get formattedMessage() {
      return `Parse error: ${e}
${Yo({ source: t, line: n, column: o }, 2)}`;
    } });
  }
  function Go(e) {
    let t = this.createList(), r = false, n = { recognizer: e };
    for (; !this.eof; ) {
      switch (this.tokenType) {
        case 25:
          this.next();
          continue;
        case 13:
          r = true, this.next();
          continue;
      }
      const o = e.getNode.call(this, n);
      if (o === void 0) break;
      r && (e.onWhiteSpace && e.onWhiteSpace.call(this, o, t, n), r = false), t.push(o);
    }
    return r && e.onWhiteSpace && e.onWhiteSpace.call(this, null, t, n), t;
  }
  function bs(e) {
    return function() {
      return this[e]();
    };
  }
  function pr(e) {
    const t = /* @__PURE__ */ Object.create(null);
    for (const r in e) {
      const n = e[r], o = n.parse || n;
      o && (t[r] = o);
    }
    return t;
  }
  function xs(e) {
    const t = { context: /* @__PURE__ */ Object.create(null), scope: Object.assign(/* @__PURE__ */ Object.create(null), e.scope), atrule: pr(e.atrule), pseudo: pr(e.pseudo), node: pr(e.node) };
    for (const r in e.parseContext) switch (typeof e.parseContext[r]) {
      case "function":
        t.context[r] = e.parseContext[r];
        break;
      case "string":
        t.context[r] = bs(e.parseContext[r]);
        break;
    }
    return { config: t, ...t, ...t.node };
  }
  function Xo(e) {
    let t = "", r = "<unknown>", n = false, o = Vo, i = false, s = new Et(), u = Object.assign(new nt(), xs(e || {}), { parseAtrulePrelude: true, parseRulePrelude: true, parseValue: true, parseCustomProperty: false, readSequence: Go, consumeUntilBalanceEnd: () => 0, consumeUntilLeftCurlyBracket(a) {
      return a === Ko ? 1 : 0;
    }, consumeUntilLeftCurlyBracketOrSemicolon(a) {
      return a === Ko || a === ur ? 1 : 0;
    }, consumeUntilExclamationMarkOrSemicolon(a) {
      return a === ds || a === ur ? 1 : 0;
    }, consumeUntilSemicolonIncluded(a) {
      return a === ur ? 2 : 0;
    }, createList() {
      return new I();
    }, createSingleNodeList(a) {
      return new I().appendData(a);
    }, getFirstListNode(a) {
      return a && a.first;
    }, getLastListNode(a) {
      return a && a.last;
    }, parseWithFallback(a, l) {
      const p = this.tokenIndex;
      try {
        return a.call(this);
      } catch (m) {
        if (i) throw m;
        const f = l.call(this, p);
        return i = true, o(m, f), i = false, f;
      }
    }, lookupNonWSType(a) {
      let l;
      do
        if (l = this.lookupType(a++), l !== 13) return l;
      while (l !== Qo);
      return Qo;
    }, charCodeAt(a) {
      return a >= 0 && a < t.length ? t.charCodeAt(a) : 0;
    }, substring(a, l) {
      return t.substring(a, l);
    }, substrToCursor(a) {
      return this.source.substring(a, this.tokenStart);
    }, cmpChar(a, l) {
      return de(t, a, l);
    }, cmpStr(a, l, p) {
      return ge(t, a, l, p);
    }, consume(a) {
      const l = this.tokenStart;
      return this.eat(a), this.substrToCursor(l);
    }, consumeFunctionName() {
      const a = t.substring(this.tokenStart, this.tokenEnd - 1);
      return this.eat(2), a;
    }, consumeNumber(a) {
      const l = t.substring(this.tokenStart, Te(t, this.tokenStart));
      return this.eat(a), l;
    }, eat(a) {
      if (this.tokenType !== a) {
        let l = Fe[a].slice(0, -6).replace(/-/g, " ").replace(/^./, (f) => f.toUpperCase()), p = `${/[[\](){}]/.test(l) ? `"${l}"` : l} is expected`, m = this.tokenStart;
        switch (a) {
          case 1:
            this.tokenType === 2 || this.tokenType === 7 ? (m = this.tokenEnd - 1, p = "Identifier is expected but function found") : p = "Identifier is expected";
            break;
          case 4:
            this.isDelim(gs) && (this.next(), m++, p = "Name is expected");
            break;
          case 11:
            this.tokenType === 10 && (m = this.tokenEnd, p = "Percent sign is expected");
            break;
        }
        this.error(p, m);
      }
      this.next();
    }, eatIdent(a) {
      (this.tokenType !== 1 || this.lookupValue(0, a) === false) && this.error(`Identifier "${a}" is expected`), this.next();
    }, eatDelim(a) {
      this.isDelim(a) || this.error(`Delim "${String.fromCharCode(a)}" is expected`), this.next();
    }, getLocation(a, l) {
      return n ? s.getLocationRange(a, l, r) : null;
    }, getLocationFromList(a) {
      if (n) {
        const l = this.getFirstListNode(a), p = this.getLastListNode(a);
        return s.getLocationRange(l !== null ? l.loc.start.offset - s.startOffset : this.tokenStart, p !== null ? p.loc.end.offset - s.startOffset : this.tokenStart, r);
      }
      return null;
    }, error(a, l) {
      const p = typeof l < "u" && l < t.length ? s.getLocation(l) : this.eof ? s.getLocation(_o(t, t.length - 1)) : s.getLocation(this.tokenStart);
      throw new cr(a || "Unexpected input", t, p.offset, p.line, p.column);
    } });
    return Object.assign((a, l) => {
      t = a, l = l || {}, u.setSource(t, ve), s.setSource(t, l.offset, l.line, l.column), r = l.filename || "<unknown>", n = Boolean(l.positions), o = typeof l.onParseError === "function" ? l.onParseError : Vo, i = false, u.parseAtrulePrelude = "parseAtrulePrelude" in l ? Boolean(l.parseAtrulePrelude) : true, u.parseRulePrelude = "parseRulePrelude" in l ? Boolean(l.parseRulePrelude) : true, u.parseValue = "parseValue" in l ? Boolean(l.parseValue) : true, u.parseCustomProperty = "parseCustomProperty" in l ? Boolean(l.parseCustomProperty) : false;
      const { context: p = "default", onComment: m } = l;
      if (!(p in u.context)) throw new Error(`Unknown context \`${p}\``);
      typeof m === "function" && u.forEachToken((P, te, X) => {
        if (P === 25) {
          const S = u.getLocation(te, X), R = ge(t, X - 2, X, "*/") ? t.slice(te + 2, X - 2) : t.slice(te + 2, X);
          m(R, S);
        }
      });
      const f = u.context[p].call(u, l);
      return u.eof || u.error(), f;
    }, { SyntaxError: cr, config: u.config });
  }
  function xi(e) {
    let t = new bi.SourceMapGenerator(), r = { line: 1, column: 0 }, n = { line: 0, column: 0 }, o = { line: 1, column: 0 }, i = { generated: o }, s = 1, u = 0, c = false, a = e.node;
    e.node = function(m) {
      if (m.loc && m.loc.start && gi.has(m.type)) {
        const f = m.loc.start.line, P = m.loc.start.column - 1;
        (n.line !== f || n.column !== P) && (n.line = f, n.column = P, r.line = s, r.column = u, c && (c = false, (r.line !== o.line || r.column !== o.column) && t.addMapping(i)), c = true, t.addMapping({ source: m.loc.source, original: n, generated: r }));
      }
      a.call(this, m), c && gi.has(m.type) && (o.line = s, o.column = u);
    };
    const l = e.emit;
    e.emit = function(m, f, P) {
      for (let te = 0; te < m.length; te++) m.charCodeAt(te) === 10 ? (s++, u = 0) : u++;
      l(m, f, P);
    };
    const p = e.result;
    return e.result = function() {
      return c && t.addMapping(i), { css: p(), map: t };
    }, e;
  }
  function ki(e) {
    const t = new Set(e.map(([r, n]) => xr(r) << 16 | xr(n)));
    return function(r, n, o) {
      const i = xr(n, o), s = o.charCodeAt(0);
      return (s === Bs && n !== 1 && n !== 2 && n !== 15 || s === Fs ? t.has(r << 16 | s << 8) : t.has(r << 16 | i)) && this.emit(" ", 13, true), i;
    };
  }
  function Hs(e, t) {
    if (typeof t === "function") {
      let r = null;
      e.children.forEach((n) => {
        r !== null && t.call(this, r), this.node(n), r = n;
      });
      return;
    }
    e.children.forEach(this.node, this);
  }
  function qs(e) {
    ve(e, (t, r, n) => {
      this.token(t, e.slice(r, n));
    });
  }
  function wi(e) {
    const t = /* @__PURE__ */ new Map();
    for (const r in e.node) {
      const n = e.node[r];
      typeof (n.generate || n) === "function" && t.set(r, n.generate || n);
    }
    return function(r, n) {
      let o = "", i = 0, s = { node(c) {
        if (t.has(c.type)) t.get(c.type).call(u, c);
        else throw new Error(`Unknown node type: ${c.type}`);
      }, tokenBefore: yr, token(c, a) {
        i = this.tokenBefore(i, c, a), this.emit(a, c, false), c === 9 && a.charCodeAt(0) === js && this.emit(`
`, 13, true);
      }, emit(c) {
        o += c;
      }, result() {
        return o;
      } };
      n && (typeof n.decorator === "function" && (s = n.decorator(s)), n.sourceMap && (s = xi(s)), n.mode in Dt && (s.tokenBefore = Dt[n.mode]));
      const u = { node: (c) => s.node(c), children: Hs, token: (c, a) => s.token(c, a), tokenize: qs };
      return s.node(r), s.result();
    };
  }
  function vi(e) {
    return { fromPlainObject(t) {
      return e(t, { enter(r) {
        r.children && !(r.children instanceof I) && (r.children = new I().fromArray(r.children));
      } }), t;
    }, toPlainObject(t) {
      return e(t, { leave(r) {
        r.children && r.children instanceof I && (r.children = r.children.toArray());
      } }), t;
    } };
  }
  function Si(e) {
    return typeof e === "function" ? e : at;
  }
  function Ci(e, t) {
    return function(r, n, o) {
      r.type === t && e.call(this, r, n, o);
    };
  }
  function Ws(e, t) {
    const r = t.structure, n = [];
    for (const o in r) {
      if (kr.call(r, o) === false) continue;
      let i = r[o], s = { name: o, type: false, nullable: false };
      Array.isArray(i) || (i = [i]);
      for (const u of i) u === null ? s.nullable = true : typeof u === "string" ? s.type = "node" : Array.isArray(u) && (s.type = "list");
      s.type && n.push(s);
    }
    return n.length ? { context: t.walkContext, fields: n } : null;
  }
  function Ys(e) {
    const t = {};
    for (const r in e.node) if (kr.call(e.node, r)) {
      const n = e.node[r];
      if (!n.structure) throw new Error(`Missed \`structure\` field in \`${r}\` node type definition`);
      t[r] = Ws(r, n);
    }
    return t;
  }
  function Ai(e, t) {
    const r = e.fields.slice(), n = e.context, o = typeof n === "string";
    return t && r.reverse(), function(i, s, u, c) {
      let a;
      o && (a = s[n], s[n] = i);
      for (const l of r) {
        const p = i[l.name];
        if (!l.nullable || p) {
          if (l.type === "list") {
            if (t ? p.reduceRight(c, false) : p.reduce(c, false)) return true;
          } else if (u(p)) return true;
        }
      }
      o && (s[n] = a);
    };
  }
  function Ti({ StyleSheet: e, Atrule: t, Rule: r, Block: n, DeclarationList: o }) {
    return { Atrule: { StyleSheet: e, Atrule: t, Rule: r, Block: n }, Rule: { StyleSheet: e, Atrule: t, Rule: r, Block: n }, Declaration: { StyleSheet: e, Atrule: t, Rule: r, Block: n, DeclarationList: o } };
  }
  function Ei(e) {
    const t = Ys(e), r = {}, n = {}, o = Symbol("break-walk"), i = Symbol("skip-node");
    for (const a in t) kr.call(t, a) && t[a] !== null && (r[a] = Ai(t[a], false), n[a] = Ai(t[a], true));
    const s = Ti(r), u = Ti(n), c = function(a, l) {
      function p(S, R, ke) {
        const z = m.call(X, S, R, ke);
        return z === o ? true : z === i ? false : !!(P.hasOwnProperty(S.type) && P[S.type](S, X, p, te) || f.call(X, S, R, ke) === o);
      }
      let m = at, f = at, P = r, te = (S, R, ke, z) => S || p(R, ke, z), X = { break: o, skip: i, root: a, stylesheet: null, atrule: null, atrulePrelude: null, rule: null, selector: null, block: null, declaration: null, function: null };
      if (typeof l === "function") m = l;
      else if (l && (m = Si(l.enter), f = Si(l.leave), l.reverse && (P = n), l.visit)) {
        if (s.hasOwnProperty(l.visit)) P = l.reverse ? u[l.visit] : s[l.visit];
        else if (!t.hasOwnProperty(l.visit)) throw new Error(`Bad value \`${l.visit}\` for \`visit\` option (should be: ${Object.keys(t).sort().join(", ")})`);
        m = Ci(m, l.visit), f = Ci(f, l.visit);
      }
      if (m === at && f === at) throw new Error("Neither `enter` nor `leave` walker handler is set or both aren't a function");
      p(a);
    };
    return c.break = o, c.skip = i, c.find = function(a, l) {
      let p = null;
      return c(a, function(m, f, P) {
        if (l.call(this, m, f, P)) return p = m, o;
      }), p;
    }, c.findLast = function(a, l) {
      let p = null;
      return c(a, { reverse: true, enter(m, f, P) {
        if (l.call(this, m, f, P)) return p = m, o;
      } }), p;
    }, c.findAll = function(a, l) {
      const p = [];
      return c(a, function(m, f, P) {
        l.call(this, m, f, P) && p.push(m);
      }), p;
    }, c;
  }
  function Gs(e) {
    return e;
  }
  function Vs(e) {
    const { min: t, max: r, comma: n } = e;
    return t === 0 && r === 0 ? n ? "#?" : "*" : t === 0 && r === 1 ? "?" : t === 1 && r === 0 ? n ? "#" : "+" : t === 1 && r === 1 ? "" : (n ? "#" : "") + (t === r ? `{${t}}` : `{${t},${r !== 0 ? r : ""}}`);
  }
  function Ks(e) {
    switch (e.type) {
      case "Range":
        return ` [${e.min === null ? "-\u221E" : e.min},${e.max === null ? "\u221E" : e.max}]`;
      default:
        throw new Error(`Unknown node type \`${e.type}\``);
    }
  }
  function Qs(e, t, r, n) {
    const o = e.combinator === " " || n ? e.combinator : ` ${e.combinator} `, i = e.terms.map((s) => wr(s, t, r, n)).join(o);
    return e.explicit || r ? (n || i[0] === "," ? "[" : "[ ") + i + (n ? "]" : " ]") : i;
  }
  function wr(e, t, r, n) {
    let o;
    switch (e.type) {
      case "Group":
        o = Qs(e, t, r, n) + (e.disallowEmpty ? "!" : "");
        break;
      case "Multiplier":
        return wr(e.term, t, r, n) + t(Vs(e), e);
      case "Type":
        o = `<${e.name}${e.opts ? t(Ks(e.opts), e.opts) : ""}>`;
        break;
      case "Property":
        o = `<'${e.name}'>`;
        break;
      case "Keyword":
        o = e.name;
        break;
      case "AtKeyword":
        o = `@${e.name}`;
        break;
      case "Function":
        o = `${e.name}(`;
        break;
      case "String":
      case "Token":
        o = e.value;
        break;
      case "Comma":
        o = ",";
        break;
      default:
        throw new Error(`Unknown node type \`${e.type}\``);
    }
    return t(o, e);
  }
  function Pe(e, t) {
    let r = Gs, n = false, o = false;
    return typeof t === "function" ? r = t : t && (n = Boolean(t.forceBraces), o = Boolean(t.compact), typeof t.decorate === "function" && (r = t.decorate)), wr(e, r, n, o);
  }
  function Xs(e, t) {
    let r = e.tokens, n = e.longestMatch, o = n < r.length && r[n].node || null, i = o !== t ? o : null, s = 0, u = 0, c = 0, a = "", l, p;
    for (let m = 0; m < r.length; m++) {
      const f = r[m].value;
      m === n && (u = f.length, s = a.length), i !== null && r[m].node === i && (m <= n ? c++ : c = 0), a += f;
    }
    return n === r.length || c > 1 ? (l = Ot(i || t, "end") || st(Li, a), p = st(l)) : (l = Ot(i, "start") || st(Ot(t, "start") || Li, a.slice(0, s)), p = Ot(i, "end") || st(l, a.substr(s, u))), { css: a, mismatchOffset: s, mismatchLength: u, start: l, end: p };
  }
  function Ot(e, t) {
    const r = e && e.loc && e.loc[t];
    return r ? "line" in r ? st(r) : r : null;
  }
  function st({ offset: e, line: t, column: r }, n) {
    const o = { offset: e, line: t, column: r };
    if (n) {
      const i = n.split(/\n|\r\n?|\f/);
      o.offset += n.length, o.line += i.length - 1, o.column = i.length === 1 ? o.column + n.length : i.pop().length + 1;
    }
    return o;
  }
  function Rt(e, t) {
    return t = t || 0, e.length - t >= 2 && e.charCodeAt(t) === zt && e.charCodeAt(t + 1) === zt;
  }
  function Sr(e, t) {
    if (t = t || 0, e.length - t >= 3 && e.charCodeAt(t) === zt && e.charCodeAt(t + 1) !== zt) {
      const r = e.indexOf("-", t + 2);
      if (r !== -1) return e.substring(t, r + 1);
    }
    return "";
  }
  function $s(e) {
    if (Nt.has(e)) return Nt.get(e);
    let t = e.toLowerCase(), r = Nt.get(t);
    if (r === void 0) {
      const n = Rt(t, 0), o = n ? "" : Sr(t, 0);
      r = Object.freeze({ basename: t.substr(o.length), name: t, prefix: o, vendor: o, custom: n });
    }
    return Nt.set(e, r), r;
  }
  function Zs(e) {
    if (He.has(e)) return He.get(e);
    let t = e, r = e[0];
    r === "/" ? r = e[1] === "/" ? "//" : "/" : r !== "_" && r !== "*" && r !== "$" && r !== "#" && r !== "+" && r !== "&" && (r = "");
    const n = Rt(t, r.length);
    if (!n && (t = t.toLowerCase(), He.has(t))) {
      const u = He.get(t);
      return He.set(e, u), u;
    }
    const o = n ? "" : Sr(t, r.length), i = t.substr(0, r.length + o.length), s = Object.freeze({ basename: t.substr(i.length), name: t.substr(r.length), hack: r, vendor: o, prefix: i, custom: n });
    return He.set(e, s), s;
  }
  function Tr(e, t) {
    return e !== null && e.type === 9 && e.value.charCodeAt(0) === t;
  }
  function lt(e, t, r) {
    for (; e !== null && (e.type === 13 || e.type === 25); ) e = r(++t);
    return t;
  }
  function Se(e, t, r, n) {
    if (!e) return 0;
    const o = e.value.charCodeAt(t);
    if (o === ct || o === he) {
      if (r) return 0;
      t++;
    }
    for (; t < e.value.length; t++) if (!B(e.value.charCodeAt(t))) return 0;
    return n + 1;
  }
  function Ar(e, t, r) {
    let n = false, o = lt(e, t, r);
    if (e = r(o), e === null) return t;
    if (e.type !== 10) if (Tr(e, ct) || Tr(e, he)) {
      if (n = true, o = lt(r(++o), o, r), e = r(o), e === null || e.type !== 10) return 0;
    } else return t;
    if (!n) {
      const i = e.value.charCodeAt(0);
      if (i !== ct && i !== he) return 0;
    }
    return Se(e, n ? 0 : 1, n, o);
  }
  function Er(e, t) {
    let r = 0;
    if (!e) return 0;
    if (e.type === 10) return Se(e, 0, el, r);
    if (e.type === 1 && e.value.charCodeAt(0) === he) {
      if (!de(e.value, 1, Cr)) return 0;
      switch (e.value.length) {
        case 2:
          return Ar(t(++r), r, t);
        case 3:
          return e.value.charCodeAt(2) !== he ? 0 : (r = lt(t(++r), r, t), e = t(r), Se(e, 0, qe, r));
        default:
          return e.value.charCodeAt(2) !== he ? 0 : Se(e, 3, qe, r);
      }
    } else if (e.type === 1 || Tr(e, ct) && t(r + 1).type === 1) {
      if (e.type !== 1 && (e = t(++r)), e === null || !de(e.value, 0, Cr)) return 0;
      switch (e.value.length) {
        case 1:
          return Ar(t(++r), r, t);
        case 2:
          return e.value.charCodeAt(1) !== he ? 0 : (r = lt(t(++r), r, t), e = t(r), Se(e, 0, qe, r));
        default:
          return e.value.charCodeAt(1) !== he ? 0 : Se(e, 2, qe, r);
      }
    } else if (e.type === 12) {
      let n = e.value.charCodeAt(0), o = n === ct || n === he ? 1 : 0, i = o;
      for (; i < e.value.length && B(e.value.charCodeAt(i)); i++) ;
      return i === o || !de(e.value, i, Cr) ? 0 : i + 1 === e.value.length ? Ar(t(++r), r, t) : e.value.charCodeAt(i + 1) !== he ? 0 : i + 2 === e.value.length ? (r = lt(t(++r), r, t), e = t(r), Se(e, 0, qe, r)) : Se(e, i + 2, qe, r);
    }
    return 0;
  }
  function Lr(e, t) {
    return e !== null && e.type === 9 && e.value.charCodeAt(0) === t;
  }
  function nl(e, t) {
    return e.value.charCodeAt(0) === t;
  }
  function ut(e, t, r) {
    let n = 0;
    for (let o = t; o < e.value.length; o++) {
      const i = e.value.charCodeAt(o);
      if (i === Ii && r && n !== 0) return ut(e, t + n + 1, false), 6;
      if (!ee(i) || ++n > 6) return 0;
    }
    return n;
  }
  function Bt(e, t, r) {
    if (!e) return 0;
    for (; Lr(r(t), Di); ) {
      if (++e > 6) return 0;
      t++;
    }
    return t;
  }
  function Pr(e, t) {
    let r = 0;
    if (e === null || e.type !== 1 || !de(e.value, 0, rl) || (e = t(++r), e === null)) return 0;
    if (Lr(e, tl)) return e = t(++r), e === null ? 0 : e.type === 1 ? Bt(ut(e, 0, true), ++r, t) : Lr(e, Di) ? Bt(1, ++r, t) : 0;
    if (e.type === 10) {
      const n = ut(e, 1, true);
      return n === 0 ? 0 : (e = t(++r), e === null ? r : e.type === 12 || e.type === 10 ? !nl(e, Ii) || !ut(e, 1, false) ? 0 : r + 1 : Bt(n, r, t));
    }
    return e.type === 12 ? Bt(ut(e, 1, true), ++r, t) : 0;
  }
  function le(e, t) {
    return t < e.length ? e.charCodeAt(t) : 0;
  }
  function Ni(e, t) {
    return ge(e, 0, e.length, t);
  }
  function zi(e, t) {
    for (let r = 0; r < t.length; r++) if (Ni(e, t[r])) return true;
    return false;
  }
  function Mi(e, t) {
    return t !== e.length - 2 ? false : le(e, t) === 92 && B(le(e, t + 1));
  }
  function _t(e, t, r) {
    if (e && e.type === "Range") {
      const n = Number(r !== void 0 && r !== t.length ? t.substr(0, r) : t);
      if (isNaN(n) || e.min !== null && n < e.min && typeof e.min !== "string" || e.max !== null && n > e.max && typeof e.max !== "string") return true;
    }
    return false;
  }
  function ml(e, t) {
    let r = 0, n = [], o = 0;
    e: do {
      switch (e.type) {
        case 24:
        case 22:
        case 20:
          if (e.type !== r) break e;
          if (r = n.pop(), n.length === 0) {
            o++;
            break e;
          }
          break;
        case 2:
        case 21:
        case 19:
        case 23:
          n.push(r), r = Ir.get(e.type);
          break;
      }
      o++;
    } while (e = t(o));
    return o;
  }
  function ie(e) {
    return function(t, r, n) {
      return t === null ? 0 : t.type === 2 && zi(t.value, ol) ? ml(t, r) : e(t, r, n);
    };
  }
  function N(e) {
    return function(t) {
      return t === null || t.type !== e ? 0 : 1;
    };
  }
  function fl(e) {
    if (e === null || e.type !== 1) return 0;
    const t = e.value.toLowerCase();
    return zi(t, Ft) || Ni(t, "default") ? 0 : 1;
  }
  function dl(e) {
    return e === null || e.type !== 1 || le(e.value, 0) !== 45 || le(e.value, 1) !== 45 ? 0 : 1;
  }
  function gl(e) {
    if (e === null || e.type !== 4) return 0;
    const t = e.value.length;
    if (t !== 4 && t !== 5 && t !== 7 && t !== 9) return 0;
    for (let r = 1; r < t; r++) if (!ee(le(e.value, r))) return 0;
    return 1;
  }
  function bl(e) {
    return e === null || e.type !== 4 || !ze(le(e.value, 1), le(e.value, 2), le(e.value, 3)) ? 0 : 1;
  }
  function xl(e, t) {
    if (!e) return 0;
    let r = 0, n = [], o = 0;
    e: do {
      switch (e.type) {
        case 6:
        case 8:
          break e;
        case 24:
        case 22:
        case 20:
          if (e.type !== r) break e;
          r = n.pop();
          break;
        case 17:
          if (r === 0) break e;
          break;
        case 9:
          if (r === 0 && e.value === "!") break e;
          break;
        case 2:
        case 21:
        case 19:
        case 23:
          n.push(r), r = Ir.get(e.type);
          break;
      }
      o++;
    } while (e = t(o));
    return o;
  }
  function yl(e, t) {
    if (!e) return 0;
    let r = 0, n = [], o = 0;
    e: do {
      switch (e.type) {
        case 6:
        case 8:
          break e;
        case 24:
        case 22:
        case 20:
          if (e.type !== r) break e;
          r = n.pop();
          break;
        case 2:
        case 21:
        case 19:
        case 23:
          n.push(r), r = Ir.get(e.type);
          break;
      }
      o++;
    } while (e = t(o));
    return o;
  }
  function ye(e) {
    return e && (e = new Set(e)), function(t, r, n) {
      if (t === null || t.type !== 12) return 0;
      const o = Te(t.value, 0);
      if (e !== null) {
        const i = t.value.indexOf("\\", o), s = i === -1 || !Mi(t.value, i) ? t.value.substr(o) : t.value.substring(o, i);
        if (e.has(s.toLowerCase()) === false) return 0;
      }
      return _t(n, t.value, o) ? 0 : 1;
    };
  }
  function kl(e, t, r) {
    return e === null || e.type !== 11 || _t(r, e.value, e.value.length - 1) ? 0 : 1;
  }
  function Oi(e) {
    return typeof e !== "function" && (e = function() {
      return 0;
    }), function(t, r, n) {
      return t !== null && t.type === 10 && Number(t.value) === 0 ? 1 : e(t, r, n);
    };
  }
  function wl(e, t, r) {
    if (e === null) return 0;
    const n = Te(e.value, 0);
    return !(n === e.value.length) && !Mi(e.value, n) || _t(r, e.value, n) ? 0 : 1;
  }
  function vl(e, t, r) {
    if (e === null || e.type !== 10) return 0;
    let n = le(e.value, 0) === 43 || le(e.value, 0) === 45 ? 1 : 0;
    for (; n < e.value.length; n++) if (!B(le(e.value, n))) return 0;
    return _t(r, e.value, n) ? 0 : 1;
  }
  function Ut(e, t, r) {
    return Object.assign(Ee("SyntaxError", e), { input: t, offset: r, rawMessage: e, message: `${e}
  ${t}
--${new Array((r || t.length) + 1).join("-")}^` });
  }
  function Wt(e) {
    return e.substringToPos(e.findWsEnd(e.pos));
  }
  function We(e) {
    let t = e.pos;
    for (; t < e.str.length; t++) {
      const r = e.str.charCodeAt(t);
      if (r >= 128 || pt[r] === 0) break;
    }
    return e.pos === t && e.error("Expect a keyword"), e.substringToPos(t);
  }
  function Yt(e) {
    let t = e.pos;
    for (; t < e.str.length; t++) {
      const r = e.str.charCodeAt(t);
      if (r < 48 || r > 57) break;
    }
    return e.pos === t && e.error("Expect a number"), e.substringToPos(t);
  }
  function Ml(e) {
    const t = e.str.indexOf("'", e.pos + 1);
    return t === -1 && (e.pos = e.str.length, e.error("Expect an apostrophe")), e.substringToPos(t + 1);
  }
  function Hi(e) {
    let t = null, r = null;
    return e.eat(qt), t = Yt(e), e.charCode() === Rr ? (e.pos++, e.charCode() !== _i && (r = Yt(e))) : r = t, e.eat(_i), { min: Number(t), max: r ? Number(r) : 0 };
  }
  function Rl(e) {
    let t = null, r = false;
    switch (e.charCode()) {
      case Yi:
        e.pos++, t = { min: 0, max: 0 };
        break;
      case Mr:
        e.pos++, t = { min: 1, max: 0 };
        break;
      case Nr:
        e.pos++, t = { min: 0, max: 1 };
        break;
      case zr:
        e.pos++, r = true, e.charCode() === qt ? t = Hi(e) : e.charCode() === Nr ? (e.pos++, t = { min: 0, max: 0 }) : t = { min: 1, max: 0 };
        break;
      case qt:
        t = Hi(e);
        break;
      default:
        return null;
    }
    return { type: "Multiplier", comma: r, min: t.min, max: t.max, term: null };
  }
  function Ye(e, t) {
    const r = Rl(e);
    return r !== null ? (r.term = t, e.charCode() === zr && e.charCodeAt(e.pos - 1) === Mr ? Ye(e, r) : r) : t;
  }
  function Or(e) {
    const t = e.peek();
    return t === "" ? null : { type: "Token", value: t };
  }
  function Fl(e) {
    let t;
    return e.eat(Fr), e.eat(Ht), t = We(e), e.eat(Ht), e.eat(Gi), Ye(e, { type: "Property", name: t });
  }
  function Bl(e) {
    let t = null, r = null, n = 1;
    return e.eat(Gt), e.charCode() === Fi && (e.peek(), n = -1), n == -1 && e.charCode() === Ui ? e.peek() : (t = n * Number(Yt(e)), pt[e.charCode()] !== 0 && (t += We(e))), Wt(e), e.eat(Rr), Wt(e), e.charCode() === Ui ? e.peek() : (n = 1, e.charCode() === Fi && (e.peek(), n = -1), r = n * Number(Yt(e)), pt[e.charCode()] !== 0 && (r += We(e))), e.eat(Br), { type: "Range", min: t, max: r };
  }
  function _l(e) {
    let t, r = null;
    return e.eat(Fr), t = We(e), e.charCode() === Wi && e.nextCharCode() === Nl && (e.pos += 2, t += "()"), e.charCodeAt(e.findWsEnd(e.pos)) === Gt && (Wt(e), r = Bl(e)), e.eat(Gi), Ye(e, { type: "Type", name: t, opts: r });
  }
  function Ul(e) {
    const t = We(e);
    return e.charCode() === Wi ? (e.pos++, { type: "Function", name: t }) : Ye(e, { type: "Keyword", name: t });
  }
  function jl(e, t) {
    function r(o, i) {
      return { type: "Group", terms: o, combinator: i, disallowEmpty: false, explicit: false };
    }
    let n;
    for (t = Object.keys(t).sort((o, i) => ji[o] - ji[i]); t.length > 0; ) {
      n = t.shift();
      let o = 0, i = 0;
      for (; o < e.length; o++) {
        const s = e[o];
        s.type === "Combinator" && (s.value === n ? (i === -1 && (i = o - 1), e.splice(o, 1), o--) : (i !== -1 && o - i > 1 && (e.splice(i, o - i, r(e.slice(i, o), n)), o = i + 1), i = -1));
      }
      i !== -1 && t.length && e.splice(i, o - i, r(e.slice(i, o), n));
    }
    return n;
  }
  function Vi(e) {
    let t = [], r = {}, n, o = null, i = e.pos;
    for (; n = ql(e); ) n.type !== "Spaces" && (n.type === "Combinator" ? ((o === null || o.type === "Combinator") && (e.pos = i, e.error("Unexpected combinator")), r[n.value] = true) : o !== null && o.type !== "Combinator" && (r[" "] = true, t.push({ type: "Combinator", value: " " })), t.push(n), o = n, i = e.pos);
    return o !== null && o.type === "Combinator" && (e.pos -= i, e.error("Unexpected combinator")), { type: "Group", terms: t, combinator: jl(t, r) || " ", disallowEmpty: false, explicit: false };
  }
  function Hl(e) {
    let t;
    return e.eat(Gt), t = Vi(e), e.eat(Br), t.explicit = true, e.charCode() === qi && (e.pos++, t.disallowEmpty = true), t;
  }
  function ql(e) {
    let t = e.charCode();
    if (t < 128 && pt[t] === 1) return Ul(e);
    switch (t) {
      case Br:
        break;
      case Gt:
        return Ye(e, Hl(e));
      case Fr:
        return e.nextCharCode() === Ht ? Fl(e) : _l(e);
      case Bi:
        return { type: "Combinator", value: e.substringToPos(e.pos + (e.nextCharCode() === Bi ? 2 : 1)) };
      case Ri:
        return e.pos++, e.eat(Ri), { type: "Combinator", value: "&&" };
      case Rr:
        return e.pos++, { type: "Comma" };
      case Ht:
        return Ye(e, { type: "String", value: Ml(e) });
      case Ol:
      case Ll:
      case Pl:
      case Dl:
      case Il:
        return { type: "Spaces", value: Wt(e) };
      case zl:
        return t = e.nextCharCode(), t < 128 && pt[t] === 1 ? (e.pos++, { type: "AtKeyword", name: We(e) }) : Or(e);
      case Yi:
      case Mr:
      case Nr:
      case zr:
      case qi:
        break;
      case qt:
        if (t = e.nextCharCode(), t < 48 || t > 57) return Or(e);
        break;
      default:
        return Or(e);
    }
  }
  function Ge(e) {
    const t = new jt(e), r = Vi(t);
    return t.pos !== e.length && t.error("Unexpected input"), r.terms.length === 1 && r.terms[0].type === "Group" ? r.terms[0] : r;
  }
  function Ki(e) {
    return typeof e === "function" ? e : ht;
  }
  function Vt(e, t, r) {
    function n(s) {
      switch (o.call(r, s), s.type) {
        case "Group":
          s.terms.forEach(n);
          break;
        case "Multiplier":
          n(s.term);
          break;
        case "Type":
        case "Property":
        case "Keyword":
        case "AtKeyword":
        case "Function":
        case "String":
        case "Token":
        case "Comma":
          break;
        default:
          throw new Error(`Unknown type: ${s.type}`);
      }
      i.call(r, s);
    }
    let o = ht, i = ht;
    if (typeof t === "function" ? o = t : t && (o = Ki(t.enter), i = Ki(t.leave)), o === ht && i === ht) throw new Error("Neither `enter` nor `leave` walker handler is set or both aren't a function");
    n(e, r);
  }
  function Yl(e) {
    const t = [];
    return ve(e, (r, n, o) => t.push({ type: r, value: e.slice(n, o), node: null })), t;
  }
  function Xi(e, t) {
    return typeof e === "string" ? Yl(e) : t.generate(e, Wl);
  }
  function Z(e, t, r) {
    return t === C && r === L || e === C && t === C && r === C ? e : (e.type === "If" && e.else === L && t === C && (t = e.then, e = e.match), { type: "If", match: e, then: t, else: r });
  }
  function Zi(e) {
    return e.length > 2 && e.charCodeAt(e.length - 2) === Gl && e.charCodeAt(e.length - 1) === Vl;
  }
  function $i(e) {
    return e.type === "Keyword" || e.type === "AtKeyword" || e.type === "Function" || e.type === "Type" && Zi(e.name);
  }
  function _r(e, t, r) {
    switch (e) {
      case " ": {
        let n = C;
        for (let o = t.length - 1; o >= 0; o--) {
          const i = t[o];
          n = Z(i, n, L);
        }
        return n;
      }
      case "|": {
        let n = L, o = null;
        for (let i = t.length - 1; i >= 0; i--) {
          const s = t[i];
          if ($i(s) && (o === null && i > 0 && $i(t[i - 1]) && (o = /* @__PURE__ */ Object.create(null), n = Z({ type: "Enum", map: o }, C, n)), o !== null)) {
            const u = (Zi(s.name) ? s.name.slice(0, -1) : s.name).toLowerCase();
            if (!(u in o)) {
              o[u] = s;
              continue;
            }
          }
          o = null, n = Z(s, C, n);
        }
        return n;
      }
      case "&&": {
        if (t.length > 5) return { type: "MatchOnce", terms: t, all: true };
        let n = L;
        for (let o = t.length - 1; o >= 0; o--) {
          let i = t[o], s;
          t.length > 1 ? s = _r(e, t.filter((u) => {
            return u !== i;
          }), false) : s = C, n = Z(i, s, n);
        }
        return n;
      }
      case "||": {
        if (t.length > 5) return { type: "MatchOnce", terms: t, all: false };
        let n = r ? C : L;
        for (let o = t.length - 1; o >= 0; o--) {
          let i = t[o], s;
          t.length > 1 ? s = _r(e, t.filter((u) => {
            return u !== i;
          }), true) : s = C, n = Z(i, s, n);
        }
        return n;
      }
    }
  }
  function Kl(e) {
    let t = C, r = Ur(e.term);
    if (e.max === 0) r = Z(r, Kt, L), t = Z(r, null, L), t.then = Z(C, C, t), e.comma && (t.then.else = Z({ type: "Comma", syntax: e }, t, L));
    else for (let n = e.min || 1; n <= e.max; n++) e.comma && t !== C && (t = Z({ type: "Comma", syntax: e }, t, L)), t = Z(r, Z(C, C, t), L);
    if (e.min === 0) t = Z(C, C, t);
    else for (let n = 0; n < e.min - 1; n++) e.comma && t !== C && (t = Z({ type: "Comma", syntax: e }, t, L)), t = Z(r, t, L);
    return t;
  }
  function Ur(e) {
    if (typeof e === "function") return { type: "Generic", fn: e };
    switch (e.type) {
      case "Group": {
        let t = _r(e.combinator, e.terms.map(Ur), false);
        return e.disallowEmpty && (t = Z(t, Kt, L)), t;
      }
      case "Multiplier":
        return Kl(e);
      case "Type":
      case "Property":
        return { type: e.type, name: e.name, syntax: e };
      case "Keyword":
        return { type: e.type, name: e.name.toLowerCase(), syntax: e };
      case "AtKeyword":
        return { type: e.type, name: `@${e.name.toLowerCase()}`, syntax: e };
      case "Function":
        return { type: e.type, name: `${e.name.toLowerCase()}(`, syntax: e };
      case "String":
        return e.value.length === 3 ? { type: "Token", value: e.value.charAt(1), syntax: e } : { type: e.type, value: e.value.substr(1, e.value.length - 2).replace(/\\'/g, "'"), syntax: e };
      case "Token":
        return { type: e.type, value: e.value, syntax: e };
      case "Comma":
        return { type: e.type, syntax: e };
      default:
        throw new Error("Unknown node type:", e.type);
    }
  }
  function Qt(e, t) {
    return typeof e === "string" && (e = Ge(e)), { type: "MatchGraph", match: Ur(e), syntax: t || null, source: e };
  }
  function ec(e) {
    let t = null, r = null, n = e;
    for (; n !== null; ) r = n.prev, n.prev = t, t = n, n = r;
    return t;
  }
  function jr(e, t) {
    if (e.length !== t.length) return false;
    for (let r = 0; r < e.length; r++) {
      let n = t.charCodeAt(r), o = e.charCodeAt(r);
      if (o >= 65 && o <= 90 && (o = o | 32), o !== n) return false;
    }
    return true;
  }
  function tc(e) {
    return e.type !== 9 ? false : e.value !== "?";
  }
  function ra(e) {
    return e === null ? true : e.type === 18 || e.type === 2 || e.type === 21 || e.type === 19 || e.type === 23 || tc(e);
  }
  function na(e) {
    return e === null ? true : e.type === 22 || e.type === 20 || e.type === 24 || e.type === 9 && e.value === "/";
  }
  function rc(e, t, r) {
    function n() {
      do
        R++, S = R < e.length ? e[R] : null;
      while (S !== null && (S.type === 13 || S.type === 25));
    }
    function o(ae) {
      const fe = R + ae;
      return fe < e.length ? e[fe] : null;
    }
    function i(ae, fe) {
      return { nextState: ae, matchStack: z, syntaxStack: p, thenStack: m, tokenIndex: R, prev: fe };
    }
    function s(ae) {
      m = { nextState: ae, matchStack: z, syntaxStack: p, prev: m };
    }
    function u(ae) {
      f = i(ae, f);
    }
    function c() {
      z = { type: Xl, syntax: t.syntax, token: S, prev: z }, n(), P = null, R > ke && (ke = R);
    }
    function a() {
      p = { syntax: t.syntax, opts: t.syntax.opts || p !== null && p.opts || null, prev: p }, z = { type: Hr, syntax: t.syntax, token: z.token, prev: z };
    }
    function l() {
      z.type === Hr ? z = z.prev : z = { type: oa, syntax: p.syntax, token: z.token, prev: z }, p = p.prev;
    }
    let p = null, m = null, f = null, P = null, te = 0, X = null, S = null, R = -1, ke = 0, z = { type: Ql, syntax: null, token: null, prev: null };
    for (n(); X === null && ++te < ta; ) switch (t.type) {
      case "Match":
        if (m === null) {
          if (S !== null && (R !== e.length - 1 || S.value !== "\\0" && S.value !== "\\9")) {
            t = L;
            break;
          }
          X = ea;
          break;
        }
        if (t = m.nextState, t === Kt) if (m.matchStack === z) {
          t = L;
          break;
        } else t = C;
        for (; m.syntaxStack !== p; ) l();
        m = m.prev;
        break;
      case "Mismatch":
        if (P !== null && P !== false) (f === null || R > f.tokenIndex) && (f = P, P = false);
        else if (f === null) {
          X = $l;
          break;
        }
        t = f.nextState, m = f.thenStack, p = f.syntaxStack, z = f.matchStack, R = f.tokenIndex, S = R < e.length ? e[R] : null, f = f.prev;
        break;
      case "MatchGraph":
        t = t.match;
        break;
      case "If":
        t.else !== L && u(t.else), t.then !== C && s(t.then), t = t.match;
        break;
      case "MatchOnce":
        t = { type: "MatchOnceBuffer", syntax: t, index: 0, mask: 0 };
        break;
      case "MatchOnceBuffer": {
        const Q = t.syntax.terms;
        if (t.index === Q.length) {
          if (t.mask === 0 || t.syntax.all) {
            t = L;
            break;
          }
          t = C;
          break;
        }
        if (t.mask === (1 << Q.length) - 1) {
          t = C;
          break;
        }
        for (; t.index < Q.length; t.index++) {
          const J = 1 << t.index;
          if ((t.mask & J) === 0) {
            u(t), s({ type: "AddMatchOnce", syntax: t.syntax, mask: t.mask | J }), t = Q[t.index++];
            break;
          }
        }
        break;
      }
      case "AddMatchOnce":
        t = { type: "MatchOnceBuffer", syntax: t.syntax, index: 0, mask: t.mask };
        break;
      case "Enum":
        if (S !== null) {
          let Q = S.value.toLowerCase();
          if (Q.indexOf("\\") !== -1 && (Q = Q.replace(/\\[09].*$/, "")), Ji.call(t.map, Q)) {
            t = t.map[Q];
            break;
          }
        }
        t = L;
        break;
      case "Generic": {
        const Q = p !== null ? p.opts : null, J = R + Math.floor(t.fn(S, o, Q));
        if (!isNaN(J) && J > R) {
          for (; R < J; ) c();
          t = C;
        } else t = L;
        break;
      }
      case "Type":
      case "Property": {
        const Q = t.type === "Type" ? "types" : "properties", J = Ji.call(r, Q) ? r[Q][t.name] : null;
        if (!J || !J.match) throw new Error(`Bad syntax reference: ${t.type === "Type" ? `<${t.name}>` : `<'${t.name}'>`}`);
        if (P !== false && S !== null && t.type === "Type" && (t.name === "custom-ident" && S.type === 1 || t.name === "length" && S.value === "0")) {
          P === null && (P = i(t, f)), t = L;
          break;
        }
        a(), t = J.match;
        break;
      }
      case "Keyword": {
        const Q = t.name;
        if (S !== null) {
          let J = S.value;
          if (J.indexOf("\\") !== -1 && (J = J.replace(/\\[09].*$/, "")), jr(J, Q)) {
            c(), t = C;
            break;
          }
        }
        t = L;
        break;
      }
      case "AtKeyword":
      case "Function":
        if (S !== null && jr(S.value, t.name)) {
          c(), t = C;
          break;
        }
        t = L;
        break;
      case "Token":
        if (S !== null && S.value === t.value) {
          c(), t = C;
          break;
        }
        t = L;
        break;
      case "Comma":
        S !== null && S.type === 18 ? ra(z.token) ? t = L : (c(), t = na(S) ? L : C) : t = ra(z.token) || na(S) ? C : L;
        break;
      case "String":
        let ae = "", fe = R;
        for (; fe < e.length && ae.length < t.value.length; fe++) ae += e[fe].value;
        if (jr(ae, t.value)) {
          for (; R < fe; ) c();
          t = C;
        } else t = L;
        break;
      default:
        throw new Error(`Unknown node type: ${t.type}`);
    }
    switch (Jl += te, X) {
      case null:
        console.warn(`[csstree-match] BREAK after ${ta} iterations`), X = Zl, z = null;
        break;
      case ea:
        for (; p !== null; ) l();
        break;
      default:
        z = null;
    }
    return { tokens: e, reason: X, iterations: te, match: z, longestMatch: ke };
  }
  function qr(e, t, r) {
    const n = rc(e, t, r || {});
    if (n.match === null) return n;
    let o = n.match, i = n.match = { syntax: t.syntax || null, match: [] }, s = [i];
    for (o = ec(o).prev; o !== null; ) {
      switch (o.type) {
        case Hr:
          i.match.push(i = { syntax: o.syntax, match: [] }), s.push(i);
          break;
        case oa:
          s.pop(), i = s[s.length - 1];
          break;
        default:
          i.match.push({ syntax: o.syntax || null, token: o.token.value, node: o.token.node });
      }
      o = o.prev;
    }
    return n;
  }
  function ia(e) {
    function t(o) {
      return o === null ? false : o.type === "Type" || o.type === "Property" || o.type === "Keyword";
    }
    function r(o) {
      if (Array.isArray(o.match)) {
        for (let i = 0; i < o.match.length; i++) if (r(o.match[i])) return t(o.syntax) && n.unshift(o.syntax), true;
      } else if (o.node === e) return n = t(o.syntax) ? [o.syntax] : [], true;
      return false;
    }
    let n = null;
    return this.matched !== null && r(this.matched), n;
  }
  function nc(e, t) {
    return Wr(this, e, (r) => r.type === "Type" && r.name === t);
  }
  function oc(e, t) {
    return Wr(this, e, (r) => r.type === "Property" && r.name === t);
  }
  function ic(e) {
    return Wr(this, e, (t) => t.type === "Keyword");
  }
  function Wr(e, t, r) {
    const n = ia.call(e, t);
    return n === null ? false : n.some(r);
  }
  function aa(e) {
    return "node" in e ? e.node : aa(e.match[0]);
  }
  function sa(e) {
    return "node" in e ? e.node : sa(e.match[e.match.length - 1]);
  }
  function Gr(e, t, r, n, o) {
    function i(u) {
      if (u.syntax !== null && u.syntax.type === n && u.syntax.name === o) {
        const c = aa(u), a = sa(u);
        e.syntax.walk(t, (l, p, m) => {
          if (l === c) {
            const f = new I();
            do {
              if (f.appendData(p.data), p.data === a) break;
              p = p.next;
            } while (p !== null);
            s.push({ parent: m, nodes: f });
          }
        });
      }
      Array.isArray(u.match) && u.match.forEach(i);
    }
    const s = [];
    return r.matched !== null && i(r.matched), s;
  }
  function Vr(e) {
    return typeof e === "number" && isFinite(e) && Math.floor(e) === e && e >= 0;
  }
  function la(e) {
    return Boolean(e) && Vr(e.offset) && Vr(e.line) && Vr(e.column);
  }
  function ac(e, t) {
    return function(n, o) {
      if (!n || n.constructor !== Object) return o(n, "Type of node should be an Object");
      for (let i in n) {
        let s = true;
        if (mt.call(n, i) !== false) {
          if (i === "type") n.type !== e && o(n, `Wrong node type \`${n.type}\`, expected \`${e}\``);
          else if (i === "loc") {
            if (n.loc === null) continue;
            if (n.loc && n.loc.constructor === Object) if (typeof n.loc.source !== "string") i += ".source";
            else if (!la(n.loc.start)) i += ".start";
            else if (!la(n.loc.end)) i += ".end";
            else continue;
            s = false;
          } else if (t.hasOwnProperty(i)) {
            s = false;
            for (let u = 0; !s && u < t[i].length; u++) {
              const c = t[i][u];
              switch (c) {
                case String:
                  s = typeof n[i] === "string";
                  break;
                case Boolean:
                  s = typeof n[i] === "boolean";
                  break;
                case null:
                  s = n[i] === null;
                  break;
                default:
                  typeof c === "string" ? s = n[i] && n[i].type === c : Array.isArray(c) && (s = n[i] instanceof I);
              }
            }
          } else o(n, `Unknown field \`${i}\` for ${e} node type`);
          s || o(n, `Bad value for \`${e}.${i}\``);
        }
      }
      for (const i in t) mt.call(t, i) && mt.call(n, i) === false && o(n, `Field \`${e}.${i}\` is missed`);
    };
  }
  function sc(e, t) {
    const r = t.structure, n = { type: String, loc: true }, o = { type: `"${e}"` };
    for (const i in r) {
      if (mt.call(r, i) === false) continue;
      const s = [], u = n[i] = Array.isArray(r[i]) ? r[i].slice() : [r[i]];
      for (let c = 0; c < u.length; c++) {
        const a = u[c];
        if (a === String || a === Boolean) s.push(a.name);
        else if (a === null) s.push("null");
        else if (typeof a === "string") s.push(`<${a}>`);
        else if (Array.isArray(a)) s.push("List");
        else throw new Error(`Wrong value \`${a}\` in \`${e}.${i}\` structure definition`);
      }
      o[i] = s.join(" | ");
    }
    return { docs: o, check: ac(e, n) };
  }
  function ca(e) {
    const t = {};
    if (e.node) {
      for (const r in e.node) if (mt.call(e.node, r)) {
        const n = e.node[r];
        if (n.structure) t[r] = sc(r, n);
        else throw new Error(`Missed \`structure\` field in \`${r}\` node type definition`);
      }
    }
    return t;
  }
  function Kr(e, t, r) {
    const n = {};
    for (const o in e) e[o].syntax && (n[o] = r ? e[o].syntax : Pe(e[o].syntax, { compact: t }));
    return n;
  }
  function cc(e, t, r) {
    const n = {};
    for (const [o, i] of Object.entries(e)) n[o] = { prelude: i.prelude && (r ? i.prelude.syntax : Pe(i.prelude.syntax, { compact: t })), descriptors: i.descriptors && Kr(i.descriptors, t, r) };
    return n;
  }
  function uc(e) {
    for (let t = 0; t < e.length; t++) if (e[t].value.toLowerCase() === "var(") return true;
    return false;
  }
  function ce(e, t, r) {
    return { matched: e, iterations: r, error: t, ...Yr };
  }
  function Ve(e, t, r, n) {
    let o = Xi(r, e.syntax), i;
    return uc(o) ? ce(null, new Error("Matching for a tree with var() is not supported")) : (n && (i = qr(o, e.cssWideKeywordsSyntax, e)), (!n || !i.match) && (i = qr(o, t.match, e), !i.match) ? ce(null, new Pi(i.reason, t.syntax, r, i), i.iterations) : ce(i.match, null, i.iterations));
  }
  function Xt(e) {
    return e && e.constructor === Object;
  }
  function ha(e) {
    return Xt(e) ? { ...e } : e;
  }
  function hc(e, t) {
    return Object.assign(e, t);
  }
  function ma(e, t) {
    for (const r in t) Qe.call(t, r) && (Xt(e[r]) ? ma(e[r], t[r]) : e[r] = ha(t[r]));
    return e;
  }
  function ua(e, t) {
    return typeof t === "string" && /^\s*\|/.test(t) ? typeof e === "string" ? e + t : t.replace(/^\s*\|\s*/, "") : t || null;
  }
  function Qr(e, t) {
    if (typeof t === "string") return ua(e, t);
    const r = { ...e };
    for (const n in t) Qe.call(t, n) && (r[n] = ua(Qe.call(e, n) ? e[n] : void 0, t[n]));
    return r;
  }
  function pa(e, t) {
    const r = Qr(e, t);
    return !Xt(r) || Object.keys(r).length ? r : null;
  }
  function ft(e, t, r) {
    for (const n in r) if (Qe.call(r, n) !== false) {
      if (r[n] === true) Qe.call(t, n) && (e[n] = ha(t[n]));
      else if (r[n]) {
        if (typeof r[n] === "function") {
          const o = r[n];
          e[n] = o({}, e[n]), e[n] = o(e[n] || {}, t[n]);
        } else if (Xt(r[n])) {
          const o = {};
          for (const i in e[n]) o[i] = ft({}, e[n][i], r[n]);
          for (const i in t[n]) o[i] = ft(o[i] || {}, t[n][i], r[n]);
          e[n] = o;
        } else if (Array.isArray(r[n])) {
          const o = {}, i = r[n].reduce((s, u) => {
            return s[u] = true, s;
          }, {});
          for (const [s, u] of Object.entries(e[n] || {})) o[s] = {}, u && ft(o[s], u, i);
          for (const s in t[n]) Qe.call(t[n], s) && (o[s] || (o[s] = {}), t[n] && t[n][s] && ft(o[s], t[n][s], i));
          e[n] = o;
        }
      }
    }
    return e;
  }
  function fa(e) {
    const t = Xo(e), r = Ei(e), n = wi(e), { fromPlainObject: o, toPlainObject: i } = vi(r), s = { lexer: null, createLexer: (u) => new Ke(u, s, s.lexer.structure), tokenize: ve, parse: t, generate: n, walk: r, find: r.find, findLast: r.findLast, findAll: r.findAll, fromPlainObject: o, toPlainObject: i, fork(u) {
      const c = $t({}, e);
      return fa(typeof u === "function" ? u(c, Object.assign) : $t(c, u));
    } };
    return s.lexer = new Ke({ generic: true, types: e.types, atrules: e.atrules, properties: e.properties, node: e.node }, s), s;
  }
  function Jt(e, t) {
    let r = this.tokenStart + e, n = this.charCodeAt(r);
    for ((n === me || n === re) && (t && this.error("Number sign is not allowed"), r++); r < this.tokenEnd; r++) B(this.charCodeAt(r)) || this.error("Integer is expected", r);
  }
  function Xe(e) {
    return Jt.call(this, 0, e);
  }
  function Ce(e, t) {
    if (!this.cmpChar(this.tokenStart + e, t)) {
      let r = "";
      switch (t) {
        case Zt:
          r = "N is expected";
          break;
        case re:
          r = "HyphenMinus is expected";
          break;
      }
      this.error(r, this.tokenStart + e);
    }
  }
  function $r() {
    let e = 0, t = 0, r = this.tokenType;
    for (; r === 13 || r === 25; ) r = this.lookupType(++e);
    if (r !== 10) if (this.isDelim(me, e) || this.isDelim(re, e)) {
      t = this.isDelim(me, e) ? me : re;
      do
        r = this.lookupType(++e);
      while (r === 13 || r === 25);
      r !== 10 && (this.skip(e), Xe.call(this, Ie));
    } else return null;
    return e > 0 && this.skip(e), t === 0 && (r = this.charCodeAt(this.tokenStart), r !== me && r !== re && this.error("Number sign is expected")), Xe.call(this, t !== 0), t === re ? `-${this.consume(10)}` : this.consume(10);
  }
  function Zr() {
    let e = this.tokenStart, t = null, r = null;
    if (this.tokenType === 10) Xe.call(this, mc), r = this.consume(10);
    else if (this.tokenType === 1 && this.cmpChar(this.tokenStart, re)) switch (t = "-1", Ce.call(this, 1, Zt), this.tokenEnd - this.tokenStart) {
      case 2:
        this.next(), r = $r.call(this);
        break;
      case 3:
        Ce.call(this, 2, re), this.next(), this.skipSC(), Xe.call(this, Ie), r = `-${this.consume(10)}`;
        break;
      default:
        Ce.call(this, 2, re), Jt.call(this, 3, Ie), this.next(), r = this.substrToCursor(e + 2);
    }
    else if (this.tokenType === 1 || this.isDelim(me) && this.lookupType(1) === 1) {
      let n = 0;
      switch (t = "1", this.isDelim(me) && (n = 1, this.next()), Ce.call(this, 0, Zt), this.tokenEnd - this.tokenStart) {
        case 1:
          this.next(), r = $r.call(this);
          break;
        case 2:
          Ce.call(this, 1, re), this.next(), this.skipSC(), Xe.call(this, Ie), r = `-${this.consume(10)}`;
          break;
        default:
          Ce.call(this, 1, re), Jt.call(this, 2, Ie), this.next(), r = this.substrToCursor(e + n + 1);
      }
    } else if (this.tokenType === 12) {
      let n = this.charCodeAt(this.tokenStart), o = n === me || n === re, i = this.tokenStart + o;
      for (; i < this.tokenEnd && B(this.charCodeAt(i)); i++) ;
      i === this.tokenStart + o && this.error("Integer is expected", this.tokenStart + o), Ce.call(this, i - this.tokenStart, Zt), t = this.substring(e, i), i + 1 === this.tokenEnd ? (this.next(), r = $r.call(this)) : (Ce.call(this, i - this.tokenStart + 1, re), i + 2 === this.tokenEnd ? (this.next(), this.skipSC(), Xe.call(this, Ie), r = `-${this.consume(10)}`) : (Jt.call(this, i - this.tokenStart + 2, Ie), this.next(), r = this.substrToCursor(i + 1)));
    } else this.error();
    return t !== null && t.charCodeAt(0) === me && (t = t.substr(1)), r !== null && r.charCodeAt(0) === me && (r = r.substr(1)), { type: "AnPlusB", loc: this.getLocation(e, this.tokenStart), a: t, b: r };
  }
  function gc(e) {
    if (e.a) {
      const t = e.a === "+1" && "n" || e.a === "1" && "n" || e.a === "-1" && "-n" || `${e.a}n`;
      if (e.b) {
        const r = e.b[0] === "-" || e.b[0] === "+" ? e.b : `+${e.b}`;
        this.tokenize(t + r);
      } else this.tokenize(t);
    } else this.tokenize(e.b);
  }
  function ga(e) {
    return this.Raw(e, this.consumeUntilLeftCurlyBracketOrSemicolon, true);
  }
  function bc() {
    for (let e = 1, t; t = this.lookupType(e); e++) {
      if (t === 24) return true;
      if (t === 23 || t === 3) return false;
    }
    return false;
  }
  function en() {
    let e = this.tokenStart, t, r, n = null, o = null;
    switch (this.eat(3), t = this.substrToCursor(e + 1), r = t.toLowerCase(), this.skipSC(), this.eof === false && this.tokenType !== 23 && this.tokenType !== 17 && (this.parseAtrulePrelude ? n = this.parseWithFallback(this.AtrulePrelude.bind(this, t), ga) : n = ga.call(this, this.tokenIndex), this.skipSC()), this.tokenType) {
      case 17:
        this.next();
        break;
      case 23:
        hasOwnProperty.call(this.atrule, r) && typeof this.atrule[r].block === "function" ? o = this.atrule[r].block.call(this) : o = this.Block(bc.call(this));
        break;
    }
    return { type: "Atrule", loc: this.getLocation(e, this.tokenStart), name: t, prelude: n, block: o };
  }
  function wc(e) {
    this.token(3, `@${e.name}`), e.prelude !== null && this.node(e.prelude), e.block ? this.node(e.block) : this.token(17, ";");
  }
  function rn(e) {
    let t = null;
    return e !== null && (e = e.toLowerCase()), this.skipSC(), hasOwnProperty.call(this.atrule, e) && typeof this.atrule[e].prelude === "function" ? t = this.atrule[e].prelude.call(this) : t = this.readSequence(this.scope.AtrulePrelude), this.skipSC(), this.eof !== true && this.tokenType !== 23 && this.tokenType !== 17 && this.error("Semicolon or block is expected"), { type: "AtrulePrelude", loc: this.getLocationFromList(t), children: t };
  }
  function Ac(e) {
    this.children(e);
  }
  function Pc() {
    this.eof && this.error("Unexpected end of input");
    let e = this.tokenStart, t = false;
    return this.isDelim(ba) ? (t = true, this.next()) : this.isDelim(on) || this.eat(1), this.isDelim(on) ? this.charCodeAt(this.tokenStart + 1) !== er ? (this.next(), this.eat(1)) : t && this.error("Identifier is expected", this.tokenEnd) : t && this.error("Vertical line is expected"), { type: "Identifier", loc: this.getLocation(e, this.tokenStart), name: this.substrToCursor(e) };
  }
  function Ic() {
    const e = this.tokenStart, t = this.charCodeAt(e);
    return t !== er && t !== Lc && t !== Ec && t !== Tc && t !== ba && t !== on && this.error("Attribute selector (=, ~=, ^=, $=, *=, |=) is expected"), this.next(), t !== er && (this.isDelim(er) || this.error("Equal sign is expected"), this.next()), this.substrToCursor(e);
  }
  function an() {
    let e = this.tokenStart, t, r = null, n = null, o = null;
    return this.eat(19), this.skipSC(), t = Pc.call(this), this.skipSC(), this.tokenType !== 20 && (this.tokenType !== 1 && (r = Ic.call(this), this.skipSC(), n = this.tokenType === 5 ? this.String() : this.Identifier(), this.skipSC()), this.tokenType === 1 && (o = this.consume(1), this.skipSC())), this.eat(20), { type: "AttributeSelector", loc: this.getLocation(e, this.tokenStart), name: t, matcher: r, value: n, flags: o };
  }
  function Nc(e) {
    this.token(9, "["), this.node(e.name), e.matcher !== null && (this.tokenize(e.matcher), this.node(e.value)), e.flags !== null && this.token(1, e.flags), this.token(9, "]");
  }
  function ya(e) {
    return this.Raw(e, null, true);
  }
  function zc() {
    return this.parseWithFallback(this.Rule, ya);
  }
  function xa(e) {
    return this.Raw(e, this.consumeUntilSemicolonIncluded, true);
  }
  function Mc() {
    if (this.tokenType === 17) return xa.call(this, this.tokenIndex);
    const e = this.parseWithFallback(this.Declaration, xa);
    return this.tokenType === 17 && this.next(), e;
  }
  function ln(e) {
    const t = e ? Mc : zc, r = this.tokenStart, n = this.createList();
    this.eat(23);
    e: for (; !this.eof; ) switch (this.tokenType) {
      case 24:
        break e;
      case 13:
      case 25:
        this.next();
        break;
      case 3:
        n.push(this.parseWithFallback(this.Atrule, ya));
        break;
      default:
        n.push(t.call(this));
    }
    return this.eof || this.eat(24), { type: "Block", loc: this.getLocation(r, this.tokenStart), children: n };
  }
  function _c(e) {
    this.token(23, "{"), this.children(e, (t) => {
      t.type === "Declaration" && this.token(17, ";");
    }), this.token(24, "}");
  }
  function un(e, t) {
    let r = this.tokenStart, n = null;
    return this.eat(19), n = e.call(this, t), this.eof || this.eat(20), { type: "Brackets", loc: this.getLocation(r, this.tokenStart), children: n };
  }
  function Hc(e) {
    this.token(9, "["), this.children(e), this.token(9, "]");
  }
  function hn() {
    const e = this.tokenStart;
    return this.eat(15), { type: "CDC", loc: this.getLocation(e, this.tokenStart) };
  }
  function Yc() {
    this.token(15, "-->");
  }
  function fn() {
    const e = this.tokenStart;
    return this.eat(14), { type: "CDO", loc: this.getLocation(e, this.tokenStart) };
  }
  function Kc() {
    this.token(14, "<!--");
  }
  function gn() {
    return this.eatDelim(Qc), { type: "ClassSelector", loc: this.getLocation(this.tokenStart - 1, this.tokenEnd), name: this.consume(1) };
  }
  function Zc(e) {
    this.token(9, "."), this.token(1, e.name);
  }
  function xn() {
    let e = this.tokenStart, t;
    switch (this.tokenType) {
      case 13:
        t = " ";
        break;
      case 9:
        switch (this.charCodeAt(this.tokenStart)) {
          case eu:
          case Jc:
          case tu:
            this.next();
            break;
          case ka:
            this.next(), this.eatIdent("deep"), this.eatDelim(ka);
            break;
          default:
            this.error("Combinator is expected");
        }
        t = this.substrToCursor(e);
        break;
    }
    return { type: "Combinator", loc: this.getLocation(e, this.tokenStart), name: t };
  }
  function ou(e) {
    this.tokenize(e.name);
  }
  function kn() {
    let e = this.tokenStart, t = this.tokenEnd;
    return this.eat(25), t - e + 2 >= 2 && this.charCodeAt(t - 2) === iu && this.charCodeAt(t - 1) === au && (t -= 2), { type: "Comment", loc: this.getLocation(e, this.tokenStart), value: this.substring(e + 2, t) };
  }
  function cu(e) {
    this.token(25, `/*${e.value}*/`);
  }
  function du(e) {
    return this.Raw(e, this.consumeUntilExclamationMarkOrSemicolon, true);
  }
  function gu(e) {
    return this.Raw(e, this.consumeUntilExclamationMarkOrSemicolon, false);
  }
  function bu() {
    const e = this.tokenIndex, t = this.Value();
    return t.type !== "Raw" && this.eof === false && this.tokenType !== 17 && this.isDelim(va) === false && this.isBalanceEdge(e) === false && this.error(), t;
  }
  function vn() {
    let e = this.tokenStart, t = this.tokenIndex, r = vu.call(this), n = Rt(r), o = n ? this.parseCustomProperty : this.parseValue, i = n ? gu : du, s = false, u;
    this.skipSC(), this.eat(16);
    const c = this.tokenIndex;
    if (n || this.skipSC(), o ? u = this.parseWithFallback(bu, i) : u = i.call(this, this.tokenIndex), n && u.type === "Value" && u.children.isEmpty) {
      for (let a = c - this.tokenIndex; a <= 0; a++) if (this.lookupType(a) === 13) {
        u.children.appendData({ type: "WhiteSpace", loc: null, value: " " });
        break;
      }
    }
    return this.isDelim(va) && (s = Su.call(this), this.skipSC()), this.eof === false && this.tokenType !== 17 && this.isBalanceEdge(t) === false && this.error(), { type: "Declaration", loc: this.getLocation(e, this.tokenStart), important: s, property: r, value: u };
  }
  function wu(e) {
    this.token(1, e.property), this.token(16, ":"), this.node(e.value), e.important && (this.token(9, "!"), this.token(1, e.important === true ? "important" : e.important));
  }
  function vu() {
    const e = this.tokenStart;
    if (this.tokenType === 9) switch (this.charCodeAt(this.tokenStart)) {
      case mu:
      case pu:
      case fu:
      case uu:
      case hu:
        this.next();
        break;
      case wa:
        this.next(), this.isDelim(wa) && this.next();
        break;
    }
    return this.tokenType === 4 ? this.eat(4) : this.eat(1), this.substrToCursor(e);
  }
  function Su() {
    this.eat(9), this.skipSC();
    const e = this.consume(1);
    return e === "important" ? true : e;
  }
  function Cu(e) {
    return this.Raw(e, this.consumeUntilSemicolonIncluded, true);
  }
  function Cn() {
    const e = this.createList();
    e: for (; !this.eof; ) switch (this.tokenType) {
      case 13:
      case 25:
      case 17:
        this.next();
        break;
      default:
        e.push(this.parseWithFallback(this.Declaration, Cu));
    }
    return { type: "DeclarationList", loc: this.getLocationFromList(e), children: e };
  }
  function Eu(e) {
    this.children(e, (t) => {
      t.type === "Declaration" && this.token(17, ";");
    });
  }
  function Tn() {
    const e = this.tokenStart, t = this.consumeNumber(12);
    return { type: "Dimension", loc: this.getLocation(e, this.tokenStart), value: t, unit: this.substring(e + t.length, this.tokenStart) };
  }
  function Iu(e) {
    this.token(12, e.value + e.unit);
  }
  function Ln(e, t) {
    let r = this.tokenStart, n = this.consumeFunctionName(), o = n.toLowerCase(), i;
    return i = t.hasOwnProperty(o) ? t[o].call(this, t) : e.call(this, t), this.eof || this.eat(22), { type: "Function", loc: this.getLocation(r, this.tokenStart), name: n, children: i };
  }
  function zu(e) {
    this.token(2, `${e.name}(`), this.children(e), this.token(22, ")");
  }
  function In() {
    const e = this.tokenStart;
    return this.eat(4), { type: "Hash", loc: this.getLocation(e, this.tokenStart), value: this.substrToCursor(e + 1) };
  }
  function Bu(e) {
    this.token(4, `#${e.value}`);
  }
  function On() {
    return { type: "Identifier", loc: this.getLocation(this.tokenStart, this.tokenEnd), name: this.consume(1) };
  }
  function ju(e) {
    this.token(1, e.name);
  }
  function zn() {
    const e = this.tokenStart;
    return this.eat(4), { type: "IdSelector", loc: this.getLocation(e, this.tokenStart), name: this.substrToCursor(e + 1) };
  }
  function Wu(e) {
    this.token(9, `#${e.name}`);
  }
  function Rn() {
    let e = this.tokenStart, t, r = null;
    if (this.eat(21), this.skipSC(), t = this.consume(1), this.skipSC(), this.tokenType !== 22) {
      switch (this.eat(16), this.skipSC(), this.tokenType) {
        case 10:
          this.lookupNonWSType(1) === 9 ? r = this.Ratio() : r = this.Number();
          break;
        case 12:
          r = this.Dimension();
          break;
        case 1:
          r = this.Identifier();
          break;
        default:
          this.error("Number, dimension, ratio or identifier is expected");
      }
      this.skipSC();
    }
    return this.eat(22), { type: "MediaFeature", loc: this.getLocation(e, this.tokenStart), name: t, value: r };
  }
  function Vu(e) {
    this.token(21, "("), this.token(1, e.name), e.value !== null && (this.token(16, ":"), this.node(e.value)), this.token(22, ")");
  }
  function Bn() {
    let e = this.createList(), t = null;
    this.skipSC();
    e: for (; !this.eof; ) {
      switch (this.tokenType) {
        case 25:
        case 13:
          this.next();
          continue;
        case 1:
          t = this.Identifier();
          break;
        case 21:
          t = this.MediaFeature();
          break;
        default:
          break e;
      }
      e.push(t);
    }
    return t === null && this.error("Identifier or parenthesis is expected"), { type: "MediaQuery", loc: this.getLocationFromList(e), children: e };
  }
  function Xu(e) {
    this.children(e);
  }
  function Un() {
    const e = this.createList();
    for (this.skipSC(); !this.eof && (e.push(this.MediaQuery()), this.tokenType === 18); ) this.next();
    return { type: "MediaQueryList", loc: this.getLocationFromList(e), children: e };
  }
  function Ju(e) {
    this.children(e, () => this.token(18, ","));
  }
  function Hn() {
    this.skipSC();
    let e = this.tokenStart, t = e, r = null, n;
    return this.lookupValue(0, "odd") || this.lookupValue(0, "even") ? n = this.Identifier() : n = this.AnPlusB(), t = this.tokenStart, this.skipSC(), this.lookupValue(0, "of") && (this.next(), r = this.SelectorList(), t = this.tokenStart), { type: "Nth", loc: this.getLocation(e, t), nth: n, selector: r };
  }
  function rp(e) {
    this.node(e.nth), e.selector !== null && (this.token(1, "of"), this.node(e.selector));
  }
  function Wn() {
    return { type: "Number", loc: this.getLocation(this.tokenStart, this.tokenEnd), value: this.consume(10) };
  }
  function ip(e) {
    this.token(10, e.value);
  }
  function Gn() {
    const e = this.tokenStart;
    return this.next(), { type: "Operator", loc: this.getLocation(e, this.tokenStart), value: this.substrToCursor(e) };
  }
  function lp(e) {
    this.tokenize(e.value);
  }
  function Kn(e, t) {
    let r = this.tokenStart, n = null;
    return this.eat(21), n = e.call(this, t), this.eof || this.eat(22), { type: "Parentheses", loc: this.getLocation(r, this.tokenStart), children: n };
  }
  function pp(e) {
    this.token(21, "("), this.children(e), this.token(22, ")");
  }
  function Xn() {
    return { type: "Percentage", loc: this.getLocation(this.tokenStart, this.tokenEnd), value: this.consumeNumber(11) };
  }
  function fp(e) {
    this.token(11, `${e.value}%`);
  }
  function Zn() {
    let e = this.tokenStart, t = null, r, n;
    return this.eat(16), this.tokenType === 2 ? (r = this.consumeFunctionName(), n = r.toLowerCase(), hasOwnProperty.call(this.pseudo, n) ? (this.skipSC(), t = this.pseudo[n].call(this), this.skipSC()) : (t = this.createList(), t.push(this.Raw(this.tokenIndex, null, false))), this.eat(22)) : r = this.consume(1), { type: "PseudoClassSelector", loc: this.getLocation(e, this.tokenStart), name: r, children: t };
  }
  function xp(e) {
    this.token(16, ":"), e.children === null ? this.token(1, e.name) : (this.token(2, `${e.name}(`), this.children(e), this.token(22, ")"));
  }
  function eo() {
    let e = this.tokenStart, t = null, r, n;
    return this.eat(16), this.eat(16), this.tokenType === 2 ? (r = this.consumeFunctionName(), n = r.toLowerCase(), hasOwnProperty.call(this.pseudo, n) ? (this.skipSC(), t = this.pseudo[n].call(this), this.skipSC()) : (t = this.createList(), t.push(this.Raw(this.tokenIndex, null, false))), this.eat(22)) : r = this.consume(1), { type: "PseudoElementSelector", loc: this.getLocation(e, this.tokenStart), name: r, children: t };
  }
  function vp(e) {
    this.token(16, ":"), this.token(16, ":"), e.children === null ? this.token(1, e.name) : (this.token(2, `${e.name}(`), this.children(e), this.token(22, ")"));
  }
  function Sa() {
    this.skipSC();
    const e = this.consume(10);
    for (let t = 0; t < e.length; t++) {
      const r = e.charCodeAt(t);
      !B(r) && r !== Cp && this.error("Unsigned number is expected", this.tokenStart - e.length + t);
    }
    return Number(e) === 0 && this.error("Zero number is not allowed", this.tokenStart - e.length), e;
  }
  function ro() {
    let e = this.tokenStart, t = Sa.call(this), r;
    return this.skipSC(), this.eatDelim(Sp), r = Sa.call(this), { type: "Ratio", loc: this.getLocation(e, this.tokenStart), left: t, right: r };
  }
  function Ep(e) {
    this.token(10, e.left), this.token(9, "/"), this.token(10, e.right);
  }
  function Lp() {
    return this.tokenIndex > 0 && this.lookupType(-1) === 13 ? this.tokenIndex > 1 ? this.getTokenStart(this.tokenIndex - 1) : this.firstCharOffset : this.tokenStart;
  }
  function oo(e, t, r) {
    let n = this.getTokenStart(e), o;
    return this.skipUntilBalanced(e, t || this.consumeUntilBalanceEnd), r && this.tokenStart > n ? o = Lp.call(this) : o = this.tokenStart, { type: "Raw", loc: this.getLocation(n, o), value: this.substring(n, o) };
  }
  function Dp(e) {
    this.tokenize(e.value);
  }
  function Ca(e) {
    return this.Raw(e, this.consumeUntilLeftCurlyBracket, true);
  }
  function Op() {
    const e = this.SelectorList();
    return e.type !== "Raw" && this.eof === false && this.tokenType !== 23 && this.error(), e;
  }
  function ao() {
    let e = this.tokenIndex, t = this.tokenStart, r, n;
    return this.parseRulePrelude ? r = this.parseWithFallback(Op, Ca) : r = Ca.call(this, e), n = this.Block(true), { type: "Rule", loc: this.getLocation(t, this.tokenStart), prelude: r, block: n };
  }
  function Rp(e) {
    this.node(e.prelude), this.node(e.block);
  }
  function lo() {
    const e = this.readSequence(this.scope.Selector);
    return this.getFirstListNode(e) === null && this.error("Selector is expected"), { type: "Selector", loc: this.getLocationFromList(e), children: e };
  }
  function _p(e) {
    this.children(e);
  }
  function uo() {
    const e = this.createList();
    for (; !this.eof; ) {
      if (e.push(this.Selector()), this.tokenType === 18) {
        this.next();
        continue;
      }
      break;
    }
    return { type: "SelectorList", loc: this.getLocationFromList(e), children: e };
  }
  function qp(e) {
    this.children(e, () => this.token(18, ","));
  }
  function dt(e) {
    let t = e.length, r = e.charCodeAt(0), n = r === Aa || r === Ta ? 1 : 0, o = n === 1 && t > 1 && e.charCodeAt(t - 1) === r ? t - 2 : t - 1, i = "";
    for (let s = n; s <= o; s++) {
      let u = e.charCodeAt(s);
      if (u === ho) {
        if (s === o) {
          s !== t - 1 && (i = e.substr(s + 1));
          break;
        }
        if (u = e.charCodeAt(++s), $(ho, u)) {
          const c = s - 1, a = se(e, c);
          s = a - 1, i += Re(e.substring(c + 1, a));
        } else u === 13 && e.charCodeAt(s + 1) === 10 && s++;
      } else i += e[s];
    }
    return i;
  }
  function mo(e, t) {
    let r = t ? "'" : '"', n = t ? Ta : Aa, o = "", i = false;
    for (let s = 0; s < e.length; s++) {
      const u = e.charCodeAt(s);
      if (u === 0) {
        o += "\uFFFD";
        continue;
      }
      if (u <= 31 || u === 127) {
        o += `\\${u.toString(16)}`, i = true;
        continue;
      }
      u === n || u === ho ? (o += `\\${e.charAt(s)}`, i = false) : (i && (ee(u) || pe(u)) && (o += " "), o += e.charAt(s), i = false);
    }
    return r + o + r;
  }
  function go() {
    return { type: "String", loc: this.getLocation(this.tokenStart, this.tokenEnd), value: dt(this.consume(5)) };
  }
  function Gp(e) {
    this.token(5, mo(e.value));
  }
  function Ea(e) {
    return this.Raw(e, null, false);
  }
  function xo() {
    let e = this.tokenStart, t = this.createList(), r;
    e: for (; !this.eof; ) {
      switch (this.tokenType) {
        case 13:
          this.next();
          continue;
        case 25:
          if (this.charCodeAt(this.tokenStart + 2) !== Vp) {
            this.next();
            continue;
          }
          r = this.Comment();
          break;
        case 14:
          r = this.CDO();
          break;
        case 15:
          r = this.CDC();
          break;
        case 3:
          r = this.parseWithFallback(this.Atrule, Ea);
          break;
        default:
          r = this.parseWithFallback(this.Rule, Ea);
      }
      t.push(r);
    }
    return { type: "StyleSheet", loc: this.getLocation(e, this.tokenStart), children: t };
  }
  function $p(e) {
    this.children(e);
  }
  function ko() {
    this.tokenType !== 1 && this.isDelim(Zp) === false && this.error("Identifier or asterisk is expected"), this.next();
  }
  function wo() {
    const e = this.tokenStart;
    return this.isDelim(La) ? (this.next(), ko.call(this)) : (ko.call(this), this.isDelim(La) && (this.next(), ko.call(this))), { type: "TypeSelector", loc: this.getLocation(e, this.tokenStart), name: this.substrToCursor(e) };
  }
  function th(e) {
    this.tokenize(e.name);
  }
  function gt(e, t) {
    let r = 0;
    for (let n = this.tokenStart + e; n < this.tokenEnd; n++) {
      const o = this.charCodeAt(n);
      if (o === Ia && t && r !== 0) return gt.call(this, e + r + 1, false), -1;
      ee(o) || this.error(t && r !== 0 ? `Hyphen minus${r < 6 ? " or hex digit" : ""} is expected` : r < 6 ? "Hex digit is expected" : "Unexpected input", n), ++r > 6 && this.error("Too many hex digits", n);
    }
    return this.next(), r;
  }
  function tr(e) {
    let t = 0;
    for (; this.isDelim(So); ) ++t > e && this.error("Too many question marks"), this.next();
  }
  function rh(e) {
    this.charCodeAt(this.tokenStart) !== e && this.error(`${e === Pa ? "Plus sign" : "Hyphen minus"} is expected`);
  }
  function nh() {
    let e = 0;
    switch (this.tokenType) {
      case 10:
        if (e = gt.call(this, 1, true), this.isDelim(So)) {
          tr.call(this, 6 - e);
          break;
        }
        if (this.tokenType === 12 || this.tokenType === 10) {
          rh.call(this, Ia), gt.call(this, 1, false);
          break;
        }
        break;
      case 12:
        e = gt.call(this, 1, true), e > 0 && tr.call(this, 6 - e);
        break;
      default:
        if (this.eatDelim(Pa), this.tokenType === 1) {
          e = gt.call(this, 0, true), e > 0 && tr.call(this, 6 - e);
          break;
        }
        if (this.isDelim(So)) {
          this.next(), tr.call(this, 5);
          break;
        }
        this.error("Hex digit or question mark is expected");
    }
  }
  function Co() {
    const e = this.tokenStart;
    return this.eatIdent("u"), nh.call(this), { type: "UnicodeRange", loc: this.getLocation(e, this.tokenStart), value: this.substrToCursor(e) };
  }
  function ah(e) {
    this.tokenize(e.value);
  }
  function Eo(e) {
    let t = e.length, r = 4, n = e.charCodeAt(t - 1) === Da ? t - 2 : t - 1, o = "";
    for (; r < n && pe(e.charCodeAt(r)); ) r++;
    for (; r < n && pe(e.charCodeAt(n)); ) n--;
    for (let i = r; i <= n; i++) {
      let s = e.charCodeAt(i);
      if (s === To) {
        if (i === n) {
          i !== t - 1 && (o = e.substr(i + 1));
          break;
        }
        if (s = e.charCodeAt(++i), $(To, s)) {
          const u = i - 1, c = se(e, u);
          i = c - 1, o += Re(e.substring(u + 1, c));
        } else s === 13 && e.charCodeAt(i + 1) === 10 && i++;
      } else o += e[i];
    }
    return o;
  }
  function Lo(e) {
    let t = "", r = false;
    for (let n = 0; n < e.length; n++) {
      const o = e.charCodeAt(n);
      if (o === 0) {
        t += "\uFFFD";
        continue;
      }
      if (o <= 31 || o === 127) {
        t += `\\${o.toString(16)}`, r = true;
        continue;
      }
      o === sh || o === To || o === lh || o === ch || o === uh || o === Da ? (t += `\\${e.charAt(n)}`, r = false) : (r && ee(o) && (t += " "), t += e.charAt(n), r = false);
    }
    return `url(${t})`;
  }
  function Io() {
    let e = this.tokenStart, t;
    switch (this.tokenType) {
      case 7:
        t = Eo(this.consume(7));
        break;
      case 2:
        this.cmpStr(this.tokenStart, this.tokenEnd, "url(") || this.error("Function name must be `url`"), this.eat(2), this.skipSC(), t = dt(this.consume(5)), this.skipSC(), this.eof || this.eat(22);
        break;
      default:
        this.error("Url or Function is expected");
    }
    return { type: "Url", loc: this.getLocation(e, this.tokenStart), value: t };
  }
  function mh(e) {
    this.token(7, Lo(e.value));
  }
  function Oo() {
    const e = this.tokenStart, t = this.readSequence(this.scope.Value);
    return { type: "Value", loc: this.getLocation(e, this.tokenStart), children: t };
  }
  function gh(e) {
    this.children(e);
  }
  function zo() {
    return this.eat(13), bh;
  }
  function kh(e) {
    this.token(13, e.value);
  }
  function xt(e) {
    switch (this.tokenType) {
      case 4:
        return this.Hash();
      case 18:
        return this.Operator();
      case 21:
        return this.Parentheses(this.readSequence, e.recognizer);
      case 19:
        return this.Brackets(this.readSequence, e.recognizer);
      case 5:
        return this.String();
      case 12:
        return this.Dimension();
      case 11:
        return this.Percentage();
      case 10:
        return this.Number();
      case 2:
        return this.cmpStr(this.tokenStart, this.tokenEnd, "url(") ? this.Url() : this.Function(this.readSequence, e.recognizer);
      case 7:
        return this.Url();
      case 1:
        return this.cmpChar(this.tokenStart, Ah) && this.cmpChar(this.tokenStart + 1, Na) ? this.UnicodeRange() : this.Identifier();
      case 9: {
        const t = this.charCodeAt(this.tokenStart);
        if (t === Ch || t === vh || t === Na || t === Sh) return this.Operator();
        t === wh && this.error("Hex or identifier is expected", this.tokenStart + 1);
        break;
      }
    }
  }
  function Nh(e, t) {
    t.last !== null && t.last.type !== "Combinator" && e !== null && e.type !== "Combinator" && t.push({ type: "Combinator", loc: null, name: " " });
  }
  function zh() {
    switch (this.tokenType) {
      case 19:
        return this.AttributeSelector();
      case 4:
        return this.IdSelector();
      case 16:
        return this.lookupType(1) === 16 ? this.PseudoElementSelector() : this.PseudoClassSelector();
      case 1:
        return this.TypeSelector();
      case 10:
      case 11:
        return this.Percentage();
      case 12:
        this.charCodeAt(this.tokenStart) === Ma && this.error("Identifier is expected", this.tokenStart + 1);
        break;
      case 9: {
        switch (this.charCodeAt(this.tokenStart)) {
          case Lh:
          case Ih:
          case Oh:
          case Ph:
            return this.Combinator();
          case Ma:
            return this.ClassSelector();
          case Eh:
          case Dh:
            return this.TypeSelector();
          case Th:
            return this.IdSelector();
        }
        break;
      }
    }
  }
  function Fa() {
    return this.createSingleNodeList(this.Raw(this.tokenIndex, null, false));
  }
  function Ba() {
    const e = this.createList();
    if (this.skipSC(), e.push(this.Identifier()), this.skipSC(), this.tokenType === 18) {
      e.push(this.Operator());
      const t = this.tokenIndex, r = this.parseCustomProperty ? this.Value(null) : this.Raw(this.tokenIndex, this.consumeUntilExclamationMarkOrSemicolon, false);
      if (r.type === "Value" && r.children.isEmpty) {
        for (let n = t - this.tokenIndex; n <= 0; n++) if (this.lookupType(n) === 13) {
          r.children.appendData({ type: "WhiteSpace", loc: null, value: " " });
          break;
        }
      }
      e.push(r);
    }
    return e;
  }
  function _a(e) {
    return e !== null && e.type === "Operator" && (e.value[e.value.length - 1] === "-" || e.value[e.value.length - 1] === "+");
  }
  function Mh() {
    return this.createSingleNodeList(this.Raw(this.tokenIndex, null, false));
  }
  function Rh() {
    return this.skipSC(), this.tokenType === 1 && this.lookupNonWSType(1) === 16 ? this.createSingleNodeList(this.Declaration()) : Ya.call(this);
  }
  function Ya() {
    let e = this.createList(), t;
    this.skipSC();
    e: for (; !this.eof; ) {
      switch (this.tokenType) {
        case 25:
        case 13:
          this.next();
          continue;
        case 2:
          t = this.Function(Mh, this.scope.AtrulePrelude);
          break;
        case 1:
          t = this.Identifier();
          break;
        case 21:
          t = this.Parentheses(Rh, this.scope.AtrulePrelude);
          break;
        default:
          break e;
      }
      e.push(t);
    }
    return e;
  }
  function Bh(e) {
    let t = e.length - 1, r = "";
    for (let n = 0; n < e.length; n++) {
      let o = e.charCodeAt(n);
      if (o === Ja) {
        if (n === t) break;
        if (o = e.charCodeAt(++n), $(Ja, o)) {
          const i = n - 1, s = se(e, i);
          n = s - 1, r += Re(e.substring(i + 1, s));
        } else o === 13 && e.charCodeAt(n + 1) === 10 && n++;
      } else r += e[n];
    }
    return r;
  }
  function _h(e) {
    let t = "";
    if (e.length === 1 && e.charCodeAt(0) === 45) return "\\-";
    for (let r = 0; r < e.length; r++) {
      const n = e.charCodeAt(r);
      if (n === 0) {
        t += "\uFFFD";
        continue;
      }
      if (n <= 31 || n === 127 || n >= 48 && n <= 57 && (r === 0 || r === 1 && e.charCodeAt(0) === 45)) {
        t += `\\${n.toString(16)} `;
        continue;
      }
      Ne(n) ? t += e.charAt(r) : t += `\\${e.charAt(r)}`;
    }
    return t;
  }
  var ts, nr, rs, ns, os, is, Oe, b, as, ss, Zo, ni, Lt, ui, mi, di, Ze, $e, h, x, D, v, q, Ae, F, Y, g, d, A, y, k, ue, j, O, _, G, U, V, T, w, M, W, E, or, hs, et, ir, St, ar, Fe, Uo, ms, jo, Et, ne, we, fs, nt, _e, I, lr, qo, Wo, Vo, ds, gs, ur, Ko, Qo, bi, gi, Dt, Fs, Bs, xr, yi, _s, Us, yr, js, kr, at, Li, je, Pi, Nt, He, zt, Mt, vr, Ft, ct, he, Cr, qe, el, tl, Ii, Di, rl, ol, Ir, il, al, sl, ll, cl, ul, pl, hl, Dr, Qi, Sl, Cl, Al, Tl, El, jt, Ll, Pl, Il, Dl, Ol, qi, zr, Ri, Ht, Wi, Nl, Yi, Mr, Rr, Fi, Fr, Gi, Nr, zl, Gt, Br, qt, Bi, _i, Ui, pt, ji, ht, Wl, C, L, Kt, Gl, Vl, Ji, Ql, Xl, Hr, oa, ea, $l, Zl, ta, Jl, Yr, mt, lc, Ke, Qe, pc, $t, Xr, da, bt, Jr, me, re, Zt, Ie, mc, fc, dc, tn, xc, yc, kc, nn, vc, Sc, Cc, sn, Tc, ba, er, Ec, on, Lc, Dc, Oc, cn, Rc, Fc, Bc, pn, Uc, jc, mn, qc, Wc, dn, Gc, Vc, bn, Qc, Xc, $c, yn, Jc, ka, eu, tu, ru, nu, wn, iu, au, su, lu, Sn, va, uu, pu, hu, mu, fu, wa, xu, yu, ku, An, Au, Tu, En, Lu, Pu, Pn, Du, Ou, Nu, Dn, Mu, Ru, Fu, Nn, _u, Uu, Mn, Hu, qu, Fn, Yu, Gu, _n, Ku, Qu, jn, $u, Zu, qn, ep, tp, Yn, np, op, Vn, ap, sp, Qn, cp, up, $n, hp, mp, Jn, dp, gp, bp, to, yp, kp, wp, no, Sp, Cp, Ap, Tp, io, Pp, Ip, so, Np, zp, Mp, co, Fp, Bp, po, Up, jp, Hp, bo, fo, ho, Aa, Ta, Wp, Yp, yo, Vp, Kp, Qp, Xp, vo, Zp, La, Jp, eh, Ao, Pa, Ia, So, oh, ih, Do, Po, sh, To, lh, ch, uh, Da, ph, hh, No, fh, dh, Mo, bh, xh, yh, Oa, Ro, wh, vh, Na, Sh, Ch, Ah, za, Th, Eh, Lh, Ph, Ma, Ih, Dh, Oh, Ra, Ua, ja, Ha, qa, Wa, Ga, Va, De, Fh, Ka, rr, Qa, Fo, Xa, $a, Za, es, Ja, ob, ib, ab, sb, lb, cb, ub, pb, hb, mb, fb, db;
  var init_css_tree = __esm({
    "src/lib/csstree/css-tree.js"() {
      ts = Object.create;
      nr = Object.defineProperty;
      rs = Object.getOwnPropertyDescriptor;
      ns = Object.getOwnPropertyNames;
      os = Object.getPrototypeOf;
      is = Object.prototype.hasOwnProperty;
      Oe = (e, t) => () => (t || e((t = { exports: {} }).exports, t), t.exports);
      b = (e, t) => {
        for (const r in t) nr(e, r, { get: t[r], enumerable: true });
      };
      as = (e, t, r, n) => {
        if (t && typeof t === "object" || typeof t === "function") for (const o of ns(t)) !is.call(e, o) && o !== r && nr(e, o, { get: () => t[o], enumerable: !(n = rs(t, o)) || n.enumerable });
        return e;
      };
      ss = (e, t, r) => (r = e != null ? ts(os(e)) : {}, as(t || !e || !e.__esModule ? nr(r, "default", { value: e, enumerable: true }) : r, e));
      Zo = Oe((hr) => {
        const $o = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/".split("");
        hr.encode = function(e) {
          if (0 <= e && e < $o.length) return $o[e];
          throw new TypeError(`Must be between 0 and 63: ${e}`);
        };
        hr.decode = function(e) {
          const t = 65, r = 90, n = 97, o = 122, i = 48, s = 57, u = 43, c = 47, a = 26, l = 52;
          return t <= e && e <= r ? e - t : n <= e && e <= o ? e - n + a : i <= e && e <= s ? e - i + l : e == u ? 62 : e == c ? 63 : -1;
        };
      });
      ni = Oe((fr) => {
        const Jo = Zo(), mr = 5, ei = 1 << mr, ti = ei - 1, ri = ei;
        function ys(e) {
          return e < 0 ? (-e << 1) + 1 : (e << 1) + 0;
        }
        function ks(e) {
          const t = (e & 1) === 1, r = e >> 1;
          return t ? -r : r;
        }
        fr.encode = function(t) {
          let r = "", n, o = ys(t);
          do
            n = o & ti, o >>>= mr, o > 0 && (n |= ri), r += Jo.encode(n);
          while (o > 0);
          return r;
        };
        fr.decode = function(t, r, n) {
          let o = t.length, i = 0, s = 0, u, c;
          do {
            if (r >= o) throw new Error("Expected more digits in base 64 VLQ value.");
            if (c = Jo.decode(t.charCodeAt(r++)), c === -1) throw new Error(`Invalid base64 digit: ${t.charAt(r - 1)}`);
            u = !!(c & ri), c &= ti, i = i + (c << s), s += mr;
          } while (u);
          n.value = ks(i), n.rest = r;
        };
      });
      Lt = Oe((K) => {
        function ws(e, t, r) {
          if (t in e) return e[t];
          if (arguments.length === 3) return r;
          throw new Error(`"${t}" is a required argument.`);
        }
        K.getArg = ws;
        const oi = /^(?:([\w+\-.]+):)?\/\/(?:(\w+:\w+)@)?([\w.-]*)(?::(\d+))?(.*)$/, vs = /^data:.+\,.+$/;
        function ot(e) {
          const t = e.match(oi);
          return t ? { scheme: t[1], auth: t[2], host: t[3], port: t[4], path: t[5] } : null;
        }
        K.urlParse = ot;
        function Ue(e) {
          let t = "";
          return e.scheme && (t += `${e.scheme}:`), t += "//", e.auth && (t += `${e.auth}@`), e.host && (t += e.host), e.port && (t += `:${e.port}`), e.path && (t += e.path), t;
        }
        K.urlGenerate = Ue;
        const Ss = 32;
        function Cs(e) {
          const t = [];
          return function(r) {
            for (let n = 0; n < t.length; n++) if (t[n].input === r) {
              const o = t[0];
              return t[0] = t[n], t[n] = o, t[0].result;
            }
            const i = e(r);
            return t.unshift({ input: r, result: i }), t.length > Ss && t.pop(), i;
          };
        }
        const dr = Cs((t) => {
          let r = t, n = ot(t);
          if (n) {
            if (!n.path) return t;
            r = n.path;
          }
          for (var o = K.isAbsolute(r), i = [], s = 0, u = 0; ; ) if (s = u, u = r.indexOf("/", s), u === -1) {
            i.push(r.slice(s));
            break;
          } else for (i.push(r.slice(s, u)); u < r.length && r[u] === "/"; ) u++;
          for (var c, a = 0, u = i.length - 1; u >= 0; u--) c = i[u], c === "." ? i.splice(u, 1) : c === ".." ? a++ : a > 0 && (c === "" ? (i.splice(u + 1, a), a = 0) : (i.splice(u, 2), a--));
          return r = i.join("/"), r === "" && (r = o ? "/" : "."), n ? (n.path = r, Ue(n)) : r;
        });
        K.normalize = dr;
        function ii(e, t) {
          e === "" && (e = "."), t === "" && (t = ".");
          const r = ot(t), n = ot(e);
          if (n && (e = n.path || "/"), r && !r.scheme) return n && (r.scheme = n.scheme), Ue(r);
          if (r || t.match(vs)) return t;
          if (n && !n.host && !n.path) return n.host = t, Ue(n);
          const o = t.charAt(0) === "/" ? t : dr(`${e.replace(/\/+$/, "")}/${t}`);
          return n ? (n.path = o, Ue(n)) : o;
        }
        K.join = ii;
        K.isAbsolute = function(e) {
          return e.charAt(0) === "/" || oi.test(e);
        };
        function As(e, t) {
          e === "" && (e = "."), e = e.replace(/\/$/, "");
          for (var r = 0; t.indexOf(`${e}/`) !== 0; ) {
            const n = e.lastIndexOf("/");
            if (n < 0 || (e = e.slice(0, n), e.match(/^([^\/]+:\/)?\/*$/))) return t;
            ++r;
          }
          return Array(r + 1).join("../") + t.substr(e.length + 1);
        }
        K.relative = As;
        const ai = (function() {
          const e = /* @__PURE__ */ Object.create(null);
          return !("__proto__" in e);
        })();
        function si(e) {
          return e;
        }
        function Ts(e) {
          return li(e) ? `$${e}` : e;
        }
        K.toSetString = ai ? si : Ts;
        function Es(e) {
          return li(e) ? e.slice(1) : e;
        }
        K.fromSetString = ai ? si : Es;
        function li(e) {
          if (!e) return false;
          const t = e.length;
          if (t < 9 || e.charCodeAt(t - 1) !== 95 || e.charCodeAt(t - 2) !== 95 || e.charCodeAt(t - 3) !== 111 || e.charCodeAt(t - 4) !== 116 || e.charCodeAt(t - 5) !== 111 || e.charCodeAt(t - 6) !== 114 || e.charCodeAt(t - 7) !== 112 || e.charCodeAt(t - 8) !== 95 || e.charCodeAt(t - 9) !== 95) return false;
          for (let r = t - 10; r >= 0; r--) if (e.charCodeAt(r) !== 36) return false;
          return true;
        }
        function Ls(e, t, r) {
          let n = be(e.source, t.source);
          return n !== 0 || (n = e.originalLine - t.originalLine, n !== 0) || (n = e.originalColumn - t.originalColumn, n !== 0 || r) || (n = e.generatedColumn - t.generatedColumn, n !== 0) || (n = e.generatedLine - t.generatedLine, n !== 0) ? n : be(e.name, t.name);
        }
        K.compareByOriginalPositions = Ls;
        function Ps(e, t, r) {
          let n;
          return n = e.originalLine - t.originalLine, n !== 0 || (n = e.originalColumn - t.originalColumn, n !== 0 || r) || (n = e.generatedColumn - t.generatedColumn, n !== 0) || (n = e.generatedLine - t.generatedLine, n !== 0) ? n : be(e.name, t.name);
        }
        K.compareByOriginalPositionsNoSource = Ps;
        function Is(e, t, r) {
          let n = e.generatedLine - t.generatedLine;
          return n !== 0 || (n = e.generatedColumn - t.generatedColumn, n !== 0 || r) || (n = be(e.source, t.source), n !== 0) || (n = e.originalLine - t.originalLine, n !== 0) || (n = e.originalColumn - t.originalColumn, n !== 0) ? n : be(e.name, t.name);
        }
        K.compareByGeneratedPositionsDeflated = Is;
        function Ds(e, t, r) {
          let n = e.generatedColumn - t.generatedColumn;
          return n !== 0 || r || (n = be(e.source, t.source), n !== 0) || (n = e.originalLine - t.originalLine, n !== 0) || (n = e.originalColumn - t.originalColumn, n !== 0) ? n : be(e.name, t.name);
        }
        K.compareByGeneratedPositionsDeflatedNoLine = Ds;
        function be(e, t) {
          return e === t ? 0 : e === null ? 1 : t === null ? -1 : e > t ? 1 : -1;
        }
        function Os(e, t) {
          let r = e.generatedLine - t.generatedLine;
          return r !== 0 || (r = e.generatedColumn - t.generatedColumn, r !== 0) || (r = be(e.source, t.source), r !== 0) || (r = e.originalLine - t.originalLine, r !== 0) || (r = e.originalColumn - t.originalColumn, r !== 0) ? r : be(e.name, t.name);
        }
        K.compareByGeneratedPositionsInflated = Os;
        function Ns(e) {
          return JSON.parse(e.replace(/^\)]}'[^\n]*\n/, ""));
        }
        K.parseSourceMapInput = Ns;
        function zs(e, t, r) {
          if (t = t || "", e && (e[e.length - 1] !== "/" && t[0] !== "/" && (e += "/"), t = e + t), r) {
            const n = ot(r);
            if (!n) throw new Error("sourceMapURL could not be parsed");
            if (n.path) {
              const o = n.path.lastIndexOf("/");
              o >= 0 && (n.path = n.path.substring(0, o + 1));
            }
            t = ii(Ue(n), t);
          }
          return dr(t);
        }
        K.computeSourceURL = zs;
      });
      ui = Oe((ci) => {
        const gr = Lt(), br = Object.prototype.hasOwnProperty, Le = typeof Map < "u";
        function xe() {
          this._array = [], this._set = Le ? /* @__PURE__ */ new Map() : /* @__PURE__ */ Object.create(null);
        }
        xe.fromArray = function(t, r) {
          for (var n = new xe(), o = 0, i = t.length; o < i; o++) n.add(t[o], r);
          return n;
        };
        xe.prototype.size = function() {
          return Le ? this._set.size : Object.getOwnPropertyNames(this._set).length;
        };
        xe.prototype.add = function(t, r) {
          const n = Le ? t : gr.toSetString(t), o = Le ? this.has(t) : br.call(this._set, n), i = this._array.length;
          (!o || r) && this._array.push(t), o || (Le ? this._set.set(t, i) : this._set[n] = i);
        };
        xe.prototype.has = function(t) {
          if (Le) return this._set.has(t);
          const r = gr.toSetString(t);
          return br.call(this._set, r);
        };
        xe.prototype.indexOf = function(t) {
          if (Le) {
            const r = this._set.get(t);
            if (r >= 0) return r;
          } else {
            const n = gr.toSetString(t);
            if (br.call(this._set, n)) return this._set[n];
          }
          throw new Error(`"${t}" is not in the set.`);
        };
        xe.prototype.at = function(t) {
          if (t >= 0 && t < this._array.length) return this._array[t];
          throw new Error(`No element indexed by ${t}`);
        };
        xe.prototype.toArray = function() {
          return this._array.slice();
        };
        ci.ArraySet = xe;
      });
      mi = Oe((hi) => {
        const pi = Lt();
        function Ms(e, t) {
          const r = e.generatedLine, n = t.generatedLine, o = e.generatedColumn, i = t.generatedColumn;
          return n > r || n == r && i >= o || pi.compareByGeneratedPositionsInflated(e, t) <= 0;
        }
        function Pt() {
          this._array = [], this._sorted = true, this._last = { generatedLine: -1, generatedColumn: 0 };
        }
        Pt.prototype.unsortedForEach = function(t, r) {
          this._array.forEach(t, r);
        };
        Pt.prototype.add = function(t) {
          Ms(this._last, t) ? (this._last = t, this._array.push(t)) : (this._sorted = false, this._array.push(t));
        };
        Pt.prototype.toArray = function() {
          return this._sorted || (this._array.sort(pi.compareByGeneratedPositionsInflated), this._sorted = true), this._array;
        };
        hi.MappingList = Pt;
      });
      di = Oe((fi) => {
        const it = ni(), H = Lt(), It = ui().ArraySet, Rs = mi().MappingList;
        function oe(e) {
          e || (e = {}), this._file = H.getArg(e, "file", null), this._sourceRoot = H.getArg(e, "sourceRoot", null), this._skipValidation = H.getArg(e, "skipValidation", false), this._sources = new It(), this._names = new It(), this._mappings = new Rs(), this._sourcesContents = null;
        }
        oe.prototype._version = 3;
        oe.fromSourceMap = function(t) {
          const r = t.sourceRoot, n = new oe({ file: t.file, sourceRoot: r });
          return t.eachMapping((o) => {
            const i = { generated: { line: o.generatedLine, column: o.generatedColumn } };
            o.source != null && (i.source = o.source, r != null && (i.source = H.relative(r, i.source)), i.original = { line: o.originalLine, column: o.originalColumn }, o.name != null && (i.name = o.name)), n.addMapping(i);
          }), t.sources.forEach((o) => {
            let i = o;
            r !== null && (i = H.relative(r, o)), n._sources.has(i) || n._sources.add(i);
            const s = t.sourceContentFor(o);
            s != null && n.setSourceContent(o, s);
          }), n;
        };
        oe.prototype.addMapping = function(t) {
          let r = H.getArg(t, "generated"), n = H.getArg(t, "original", null), o = H.getArg(t, "source", null), i = H.getArg(t, "name", null);
          this._skipValidation || this._validateMapping(r, n, o, i), o != null && (o = String(o), this._sources.has(o) || this._sources.add(o)), i != null && (i = String(i), this._names.has(i) || this._names.add(i)), this._mappings.add({ generatedLine: r.line, generatedColumn: r.column, originalLine: n != null && n.line, originalColumn: n != null && n.column, source: o, name: i });
        };
        oe.prototype.setSourceContent = function(t, r) {
          let n = t;
          this._sourceRoot != null && (n = H.relative(this._sourceRoot, n)), r != null ? (this._sourcesContents || (this._sourcesContents = /* @__PURE__ */ Object.create(null)), this._sourcesContents[H.toSetString(n)] = r) : this._sourcesContents && (delete this._sourcesContents[H.toSetString(n)], Object.keys(this._sourcesContents).length === 0 && (this._sourcesContents = null));
        };
        oe.prototype.applySourceMap = function(t, r, n) {
          let o = r;
          if (r == null) {
            if (t.file == null) throw new Error(`SourceMapGenerator.prototype.applySourceMap requires either an explicit source file, or the source map's "file" property. Both were omitted.`);
            o = t.file;
          }
          const i = this._sourceRoot;
          i != null && (o = H.relative(i, o));
          const s = new It(), u = new It();
          this._mappings.unsortedForEach((c) => {
            if (c.source === o && c.originalLine != null) {
              const a = t.originalPositionFor({ line: c.originalLine, column: c.originalColumn });
              a.source != null && (c.source = a.source, n != null && (c.source = H.join(n, c.source)), i != null && (c.source = H.relative(i, c.source)), c.originalLine = a.line, c.originalColumn = a.column, a.name != null && (c.name = a.name));
            }
            const l = c.source;
            l != null && !s.has(l) && s.add(l);
            const p = c.name;
            p != null && !u.has(p) && u.add(p);
          }, this), this._sources = s, this._names = u, t.sources.forEach(function(c) {
            const a = t.sourceContentFor(c);
            a != null && (n != null && (c = H.join(n, c)), i != null && (c = H.relative(i, c)), this.setSourceContent(c, a));
          }, this);
        };
        oe.prototype._validateMapping = function(t, r, n, o) {
          if (r && typeof r.line !== "number" && typeof r.column !== "number") throw new Error("original.line and original.column are not numbers -- you probably meant to omit the original mapping entirely and only map the generated position. If so, pass null for the original mapping instead of an object with empty or null values.");
          if (!(t && "line" in t && "column" in t && t.line > 0 && t.column >= 0 && !r && !n && !o)) {
            if (t && "line" in t && "column" in t && r && "line" in r && "column" in r && t.line > 0 && t.column >= 0 && r.line > 0 && r.column >= 0 && n) return;
            throw new Error(`Invalid mapping: ${JSON.stringify({ generated: t, source: n, original: r, name: o })}`);
          }
        };
        oe.prototype._serializeMappings = function() {
          for (var t = 0, r = 1, n = 0, o = 0, i = 0, s = 0, u = "", c, a, l, p, m = this._mappings.toArray(), f = 0, P = m.length; f < P; f++) {
            if (a = m[f], c = "", a.generatedLine !== r) for (t = 0; a.generatedLine !== r; ) c += ";", r++;
            else if (f > 0) {
              if (!H.compareByGeneratedPositionsInflated(a, m[f - 1])) continue;
              c += ",";
            }
            c += it.encode(a.generatedColumn - t), t = a.generatedColumn, a.source != null && (p = this._sources.indexOf(a.source), c += it.encode(p - s), s = p, c += it.encode(a.originalLine - 1 - o), o = a.originalLine - 1, c += it.encode(a.originalColumn - n), n = a.originalColumn, a.name != null && (l = this._names.indexOf(a.name), c += it.encode(l - i), i = l)), u += c;
          }
          return u;
        };
        oe.prototype._generateSourcesContent = function(t, r) {
          return t.map(function(n) {
            if (!this._sourcesContents) return null;
            r != null && (n = H.relative(r, n));
            const o = H.toSetString(n);
            return Object.prototype.hasOwnProperty.call(this._sourcesContents, o) ? this._sourcesContents[o] : null;
          }, this);
        };
        oe.prototype.toJSON = function() {
          const t = { version: this._version, sources: this._sources.toArray(), names: this._names.toArray(), mappings: this._serializeMappings() };
          return this._file != null && (t.file = this._file), this._sourceRoot != null && (t.sourceRoot = this._sourceRoot), this._sourcesContents && (t.sourcesContent = this._generateSourcesContent(t.sources, t.sourceRoot)), t;
        };
        oe.prototype.toString = function() {
          return JSON.stringify(this.toJSON());
        };
        fi.SourceMapGenerator = oe;
      });
      Ze = {};
      b(Ze, { AtKeyword: () => D, BadString: () => Ae, BadUrl: () => Y, CDC: () => j, CDO: () => ue, Colon: () => O, Comma: () => G, Comment: () => E, Delim: () => g, Dimension: () => y, EOF: () => $e, Function: () => x, Hash: () => v, Ident: () => h, LeftCurlyBracket: () => M, LeftParenthesis: () => T, LeftSquareBracket: () => U, Number: () => d, Percentage: () => A, RightCurlyBracket: () => W, RightParenthesis: () => w, RightSquareBracket: () => V, Semicolon: () => _, String: () => q, Url: () => F, WhiteSpace: () => k });
      $e = 0;
      h = 1;
      x = 2;
      D = 3;
      v = 4;
      q = 5;
      Ae = 6;
      F = 7;
      Y = 8;
      g = 9;
      d = 10;
      A = 11;
      y = 12;
      k = 13;
      ue = 14;
      j = 15;
      O = 16;
      _ = 17;
      G = 18;
      U = 19;
      V = 20;
      T = 21;
      w = 22;
      M = 23;
      W = 24;
      E = 25;
      or = new Array(128);
      hs = 128;
      et = 130;
      ir = 131;
      St = 132;
      ar = 133;
      for (let e = 0; e < or.length; e++) or[e] = pe(e) && et || B(e) && ir || yt(e) && St || ps(e) && ar || e || hs;
      Fe = ["EOF-token", "ident-token", "function-token", "at-keyword-token", "hash-token", "string-token", "bad-string-token", "url-token", "bad-url-token", "delim-token", "number-token", "percentage-token", "dimension-token", "whitespace-token", "CDO-token", "CDC-token", "colon-token", "semicolon-token", "comma-token", "[-token", "]-token", "(-token", ")-token", "{-token", "}-token"];
      Uo = 10;
      ms = 12;
      jo = 13;
      Et = class {
        constructor() {
          this.lines = null, this.columns = null, this.computed = false;
        }
        setSource(t, r = 0, n = 1, o = 1) {
          this.source = t, this.startOffset = r, this.startLine = n, this.startColumn = o, this.computed = false;
        }
        getLocation(t, r) {
          return this.computed || Ho(this), { source: r, offset: this.startOffset + t, line: this.lines[t], column: this.columns[t] };
        }
        getLocationRange(t, r, n) {
          return this.computed || Ho(this), { source: n, start: { offset: this.startOffset + t, line: this.lines[t], column: this.columns[t] }, end: { offset: this.startOffset + r, line: this.lines[r], column: this.columns[r] } };
        }
      };
      ne = 16777215;
      we = 24;
      fs = /* @__PURE__ */ new Map([[2, 22], [21, 22], [19, 20], [23, 24]]);
      nt = class {
        constructor(t, r) {
          this.setSource(t, r);
        }
        reset() {
          this.eof = false, this.tokenIndex = -1, this.tokenType = 0, this.tokenStart = this.firstCharOffset, this.tokenEnd = this.firstCharOffset;
        }
        setSource(t = "", r = () => {
        }) {
          t = String(t || "");
          let n = t.length, o = Be(this.offsetAndType, t.length + 1), i = Be(this.balance, t.length + 1), s = 0, u = 0, c = 0, a = -1;
          for (this.offsetAndType = null, this.balance = null, r(t, (l, p, m) => {
            switch (l) {
              default:
                i[s] = n;
                break;
              case u: {
                let f = c & ne;
                for (c = i[f], u = c >> we, i[s] = f, i[f++] = s; f < s; f++) i[f] === n && (i[f] = s);
                break;
              }
              case 21:
              case 2:
              case 19:
              case 23:
                i[s] = c, u = fs.get(l), c = u << we | s;
                break;
            }
            o[s++] = l << we | m, a === -1 && (a = p);
          }), o[s] = 0 << we | n, i[s] = n, i[n] = n; c !== 0; ) {
            const l = c & ne;
            c = i[l], i[l] = n;
          }
          this.source = t, this.firstCharOffset = a === -1 ? 0 : a, this.tokenCount = s, this.offsetAndType = o, this.balance = i, this.reset(), this.next();
        }
        lookupType(t) {
          return t += this.tokenIndex, t < this.tokenCount ? this.offsetAndType[t] >> we : 0;
        }
        lookupOffset(t) {
          return t += this.tokenIndex, t < this.tokenCount ? this.offsetAndType[t - 1] & ne : this.source.length;
        }
        lookupValue(t, r) {
          return t += this.tokenIndex, t < this.tokenCount ? ge(this.source, this.offsetAndType[t - 1] & ne, this.offsetAndType[t] & ne, r) : false;
        }
        getTokenStart(t) {
          return t === this.tokenIndex ? this.tokenStart : t > 0 ? t < this.tokenCount ? this.offsetAndType[t - 1] & ne : this.offsetAndType[this.tokenCount] & ne : this.firstCharOffset;
        }
        substrToCursor(t) {
          return this.source.substring(t, this.tokenStart);
        }
        isBalanceEdge(t) {
          return this.balance[this.tokenIndex] < t;
        }
        isDelim(t, r) {
          return r ? this.lookupType(r) === 9 && this.source.charCodeAt(this.lookupOffset(r)) === t : this.tokenType === 9 && this.source.charCodeAt(this.tokenStart) === t;
        }
        skip(t) {
          let r = this.tokenIndex + t;
          r < this.tokenCount ? (this.tokenIndex = r, this.tokenStart = this.offsetAndType[r - 1] & ne, r = this.offsetAndType[r], this.tokenType = r >> we, this.tokenEnd = r & ne) : (this.tokenIndex = this.tokenCount, this.next());
        }
        next() {
          let t = this.tokenIndex + 1;
          t < this.tokenCount ? (this.tokenIndex = t, this.tokenStart = this.tokenEnd, t = this.offsetAndType[t], this.tokenType = t >> we, this.tokenEnd = t & ne) : (this.eof = true, this.tokenIndex = this.tokenCount, this.tokenType = 0, this.tokenStart = this.tokenEnd = this.source.length);
        }
        skipSC() {
          for (; this.tokenType === 13 || this.tokenType === 25; ) this.next();
        }
        skipUntilBalanced(t, r) {
          let n = t, o, i;
          e: for (; n < this.tokenCount; n++) {
            if (o = this.balance[n], o < t) break e;
            switch (i = n > 0 ? this.offsetAndType[n - 1] & ne : this.firstCharOffset, r(this.source.charCodeAt(i))) {
              case 1:
                break e;
              case 2:
                n++;
                break e;
              default:
                this.balance[o] === n && (n = o);
            }
          }
          this.skip(n - this.tokenIndex);
        }
        forEachToken(t) {
          for (let r = 0, n = this.firstCharOffset; r < this.tokenCount; r++) {
            const o = n, i = this.offsetAndType[r], s = i & ne, u = i >> we;
            n = s, t(u, o, s, r);
          }
        }
        dump() {
          const t = new Array(this.tokenCount);
          return this.forEachToken((r, n, o, i) => {
            t[i] = { idx: i, type: Fe[r], chunk: this.source.substring(n, o), balance: this.balance[i] };
          }), t;
        }
      };
      _e = null;
      I = class {
        static createItem(t) {
          return { prev: null, next: null, data: t };
        }
        constructor() {
          this.head = null, this.tail = null, this.cursor = null;
        }
        createItem(t) {
          return I.createItem(t);
        }
        allocateCursor(t, r) {
          let n;
          return _e !== null ? (n = _e, _e = _e.cursor, n.prev = t, n.next = r, n.cursor = this.cursor) : n = { prev: t, next: r, cursor: this.cursor }, this.cursor = n, n;
        }
        releaseCursor() {
          const { cursor: t } = this;
          this.cursor = t.cursor, t.prev = null, t.next = null, t.cursor = _e, _e = t;
        }
        updateCursors(t, r, n, o) {
          let { cursor: i } = this;
          for (; i !== null; ) i.prev === t && (i.prev = r), i.next === n && (i.next = o), i = i.cursor;
        }
        *[Symbol.iterator]() {
          for (let t = this.head; t !== null; t = t.next) yield t.data;
        }
        get size() {
          let t = 0;
          for (let r = this.head; r !== null; r = r.next) t++;
          return t;
        }
        get isEmpty() {
          return this.head === null;
        }
        get first() {
          return this.head && this.head.data;
        }
        get last() {
          return this.tail && this.tail.data;
        }
        fromArray(t) {
          let r = null;
          this.head = null;
          for (const n of t) {
            const o = I.createItem(n);
            r !== null ? r.next = o : this.head = o, o.prev = r, r = o;
          }
          return this.tail = r, this;
        }
        toArray() {
          return [...this];
        }
        toJSON() {
          return [...this];
        }
        forEach(t, r = this) {
          const n = this.allocateCursor(null, this.head);
          for (; n.next !== null; ) {
            const o = n.next;
            n.next = o.next, t.call(r, o.data, o, this);
          }
          this.releaseCursor();
        }
        forEachRight(t, r = this) {
          const n = this.allocateCursor(this.tail, null);
          for (; n.prev !== null; ) {
            const o = n.prev;
            n.prev = o.prev, t.call(r, o.data, o, this);
          }
          this.releaseCursor();
        }
        reduce(t, r, n = this) {
          let o = this.allocateCursor(null, this.head), i = r, s;
          for (; o.next !== null; ) s = o.next, o.next = s.next, i = t.call(n, i, s.data, s, this);
          return this.releaseCursor(), i;
        }
        reduceRight(t, r, n = this) {
          let o = this.allocateCursor(this.tail, null), i = r, s;
          for (; o.prev !== null; ) s = o.prev, o.prev = s.prev, i = t.call(n, i, s.data, s, this);
          return this.releaseCursor(), i;
        }
        some(t, r = this) {
          for (let n = this.head; n !== null; n = n.next) if (t.call(r, n.data, n, this)) return true;
          return false;
        }
        map(t, r = this) {
          const n = new I();
          for (let o = this.head; o !== null; o = o.next) n.appendData(t.call(r, o.data, o, this));
          return n;
        }
        filter(t, r = this) {
          const n = new I();
          for (let o = this.head; o !== null; o = o.next) t.call(r, o.data, o, this) && n.appendData(o.data);
          return n;
        }
        nextUntil(t, r, n = this) {
          if (t === null) return;
          const o = this.allocateCursor(null, t);
          for (; o.next !== null; ) {
            const i = o.next;
            if (o.next = i.next, r.call(n, i.data, i, this)) break;
          }
          this.releaseCursor();
        }
        prevUntil(t, r, n = this) {
          if (t === null) return;
          const o = this.allocateCursor(t, null);
          for (; o.prev !== null; ) {
            const i = o.prev;
            if (o.prev = i.prev, r.call(n, i.data, i, this)) break;
          }
          this.releaseCursor();
        }
        clear() {
          this.head = null, this.tail = null;
        }
        copy() {
          const t = new I();
          for (const r of this) t.appendData(r);
          return t;
        }
        prepend(t) {
          return this.updateCursors(null, t, this.head, t), this.head !== null ? (this.head.prev = t, t.next = this.head) : this.tail = t, this.head = t, this;
        }
        prependData(t) {
          return this.prepend(I.createItem(t));
        }
        append(t) {
          return this.insert(t);
        }
        appendData(t) {
          return this.insert(I.createItem(t));
        }
        insert(t, r = null) {
          if (r !== null) if (this.updateCursors(r.prev, t, r, t), r.prev === null) {
            if (this.head !== r) throw new Error("before doesn't belong to list");
            this.head = t, r.prev = t, t.next = r, this.updateCursors(null, t);
          } else r.prev.next = t, t.prev = r.prev, r.prev = t, t.next = r;
          else this.updateCursors(this.tail, t, null, t), this.tail !== null ? (this.tail.next = t, t.prev = this.tail) : this.head = t, this.tail = t;
          return this;
        }
        insertData(t, r) {
          return this.insert(I.createItem(t), r);
        }
        remove(t) {
          if (this.updateCursors(t, t.prev, t, t.next), t.prev !== null) t.prev.next = t.next;
          else {
            if (this.head !== t) throw new Error("item doesn't belong to list");
            this.head = t.next;
          }
          if (t.next !== null) t.next.prev = t.prev;
          else {
            if (this.tail !== t) throw new Error("item doesn't belong to list");
            this.tail = t.prev;
          }
          return t.prev = null, t.next = null, t;
        }
        push(t) {
          this.insert(I.createItem(t));
        }
        pop() {
          return this.tail !== null ? this.remove(this.tail) : null;
        }
        unshift(t) {
          this.prepend(I.createItem(t));
        }
        shift() {
          return this.head !== null ? this.remove(this.head) : null;
        }
        prependList(t) {
          return this.insertList(t, this.head);
        }
        appendList(t) {
          return this.insertList(t);
        }
        insertList(t, r) {
          return t.head === null ? this : (r != null ? (this.updateCursors(r.prev, t.tail, r, t.head), r.prev !== null ? (r.prev.next = t.head, t.head.prev = r.prev) : this.head = t.head, r.prev = t.tail, t.tail.next = r) : (this.updateCursors(this.tail, t.tail, null, t.head), this.tail !== null ? (this.tail.next = t.head, t.head.prev = this.tail) : this.head = t.head, this.tail = t.tail), t.head = null, t.tail = null, this);
        }
        replace(t, r) {
          "head" in r ? this.insertList(r, t) : this.insert(r, t), this.remove(t);
        }
      };
      lr = 100;
      qo = 60;
      Wo = "    ";
      Vo = () => {
      };
      ds = 33;
      gs = 35;
      ur = 59;
      Ko = 123;
      Qo = 0;
      bi = ss(di(), 1);
      gi = /* @__PURE__ */ new Set(["Atrule", "Selector", "Declaration"]);
      Dt = {};
      b(Dt, { safe: () => yr, spec: () => Us });
      Fs = 43;
      Bs = 45;
      xr = (e, t) => {
        if (e === 9 && (e = t), typeof e === "string") {
          const r = e.charCodeAt(0);
          return r > 127 ? 32768 : r << 8;
        }
        return e;
      };
      yi = [[1, 1], [1, 2], [1, 7], [1, 8], [1, "-"], [1, 10], [1, 11], [1, 12], [1, 15], [1, 21], [3, 1], [3, 2], [3, 7], [3, 8], [3, "-"], [3, 10], [3, 11], [3, 12], [3, 15], [4, 1], [4, 2], [4, 7], [4, 8], [4, "-"], [4, 10], [4, 11], [4, 12], [4, 15], [12, 1], [12, 2], [12, 7], [12, 8], [12, "-"], [12, 10], [12, 11], [12, 12], [12, 15], ["#", 1], ["#", 2], ["#", 7], ["#", 8], ["#", "-"], ["#", 10], ["#", 11], ["#", 12], ["#", 15], ["-", 1], ["-", 2], ["-", 7], ["-", 8], ["-", "-"], ["-", 10], ["-", 11], ["-", 12], ["-", 15], [10, 1], [10, 2], [10, 7], [10, 8], [10, 10], [10, 11], [10, 12], [10, "%"], [10, 15], ["@", 1], ["@", 2], ["@", 7], ["@", 8], ["@", "-"], ["@", 15], [".", 10], [".", 11], [".", 12], ["+", 10], ["+", 11], ["+", 12], ["/", "*"]];
      _s = yi.concat([[1, 4], [12, 4], [4, 4], [3, 21], [3, 5], [3, 16], [11, 11], [11, 12], [11, 2], [11, "-"], [22, 1], [22, 2], [22, 11], [22, 12], [22, 4], [22, "-"]]);
      Us = ki(yi);
      yr = ki(_s);
      js = 92;
      ({ hasOwnProperty: kr } = Object.prototype);
      at = function() {
      };
      Li = { offset: 0, line: 1, column: 1 };
      je = function(e, t) {
        const r = Ee("SyntaxReferenceError", e + (t ? ` \`${t}\`` : ""));
        return r.reference = t, r;
      };
      Pi = function(e, t, r, n) {
        const o = Ee("SyntaxMatchError", e), { css: i, mismatchOffset: s, mismatchLength: u, start: c, end: a } = Xs(n, r);
        return o.rawMessage = e, o.syntax = t ? Pe(t) : "<generic>", o.css = i, o.mismatchOffset = s, o.mismatchLength = u, o.message = `${e}
  syntax: ${o.syntax}
   value: ${i || "<empty string>"}
  --------${new Array(o.mismatchOffset + 1).join("-")}^`, Object.assign(o, c), o.loc = { source: r && r.loc && r.loc.source || "<unknown>", start: c, end: a }, o;
      };
      Nt = /* @__PURE__ */ new Map();
      He = /* @__PURE__ */ new Map();
      zt = 45;
      Mt = $s;
      vr = Zs;
      Ft = ["initial", "inherit", "unset", "revert", "revert-layer"];
      ct = 43;
      he = 45;
      Cr = 110;
      qe = true;
      el = false;
      tl = 43;
      Ii = 45;
      Di = 63;
      rl = 117;
      ol = ["calc(", "-moz-calc(", "-webkit-calc("];
      Ir = /* @__PURE__ */ new Map([[2, 22], [21, 22], [19, 20], [23, 24]]);
      il = ["cm", "mm", "q", "in", "pt", "pc", "px", "em", "rem", "ex", "rex", "cap", "rcap", "ch", "rch", "ic", "ric", "lh", "rlh", "vw", "svw", "lvw", "dvw", "vh", "svh", "lvh", "dvh", "vi", "svi", "lvi", "dvi", "vb", "svb", "lvb", "dvb", "vmin", "svmin", "lvmin", "dvmin", "vmax", "svmax", "lvmax", "dvmax", "cqw", "cqh", "cqi", "cqb", "cqmin", "cqmax"];
      al = ["deg", "grad", "rad", "turn"];
      sl = ["s", "ms"];
      ll = ["hz", "khz"];
      cl = ["dpi", "dpcm", "dppx", "x"];
      ul = ["fr"];
      pl = ["db"];
      hl = ["st"];
      Dr = { "ident-token": N(1), "function-token": N(2), "at-keyword-token": N(3), "hash-token": N(4), "string-token": N(5), "bad-string-token": N(6), "url-token": N(7), "bad-url-token": N(8), "delim-token": N(9), "number-token": N(10), "percentage-token": N(11), "dimension-token": N(12), "whitespace-token": N(13), "CDO-token": N(14), "CDC-token": N(15), "colon-token": N(16), "semicolon-token": N(17), "comma-token": N(18), "[-token": N(19), "]-token": N(20), "(-token": N(21), ")-token": N(22), "{-token": N(23), "}-token": N(24), string: N(5), ident: N(1), "custom-ident": fl, "custom-property-name": dl, "hex-color": gl, "id-selector": bl, "an-plus-b": Er, urange: Pr, "declaration-value": xl, "any-value": yl, dimension: ie(ye(null)), angle: ie(ye(al)), decibel: ie(ye(pl)), frequency: ie(ye(ll)), flex: ie(ye(ul)), length: ie(Oi(ye(il))), resolution: ie(ye(cl)), semitones: ie(ye(hl)), time: ie(ye(sl)), percentage: ie(kl), zero: Oi(), number: ie(wl), integer: ie(vl) };
      Qi = {};
      b(Qi, { SyntaxError: () => Ut, generate: () => Pe, parse: () => Ge, walk: () => Vt });
      Sl = 9;
      Cl = 10;
      Al = 12;
      Tl = 13;
      El = 32;
      jt = class {
        constructor(t) {
          this.str = t, this.pos = 0;
        }
        charCodeAt(t) {
          return t < this.str.length ? this.str.charCodeAt(t) : 0;
        }
        charCode() {
          return this.charCodeAt(this.pos);
        }
        nextCharCode() {
          return this.charCodeAt(this.pos + 1);
        }
        nextNonWsCode(t) {
          return this.charCodeAt(this.findWsEnd(t));
        }
        findWsEnd(t) {
          for (; t < this.str.length; t++) {
            const r = this.str.charCodeAt(t);
            if (r !== Tl && r !== Cl && r !== Al && r !== El && r !== Sl) break;
          }
          return t;
        }
        substringToPos(t) {
          return this.str.substring(this.pos, this.pos = t);
        }
        eat(t) {
          this.charCode() !== t && this.error(`Expect \`${String.fromCharCode(t)}\``), this.pos++;
        }
        peek() {
          return this.pos < this.str.length ? this.str.charAt(this.pos++) : "";
        }
        error(t) {
          throw new Ut(t, this.str, this.pos);
        }
      };
      Ll = 9;
      Pl = 10;
      Il = 12;
      Dl = 13;
      Ol = 32;
      qi = 33;
      zr = 35;
      Ri = 38;
      Ht = 39;
      Wi = 40;
      Nl = 41;
      Yi = 42;
      Mr = 43;
      Rr = 44;
      Fi = 45;
      Fr = 60;
      Gi = 62;
      Nr = 63;
      zl = 64;
      Gt = 91;
      Br = 93;
      qt = 123;
      Bi = 124;
      _i = 125;
      Ui = 8734;
      pt = new Uint8Array(128).map((e, t) => /[a-zA-Z0-9\-]/.test(String.fromCharCode(t)) ? 1 : 0);
      ji = { " ": 1, "&&": 2, "||": 3, "|": 4 };
      ht = function() {
      };
      Wl = { decorator(e) {
        let t = [], r = null;
        return { ...e, node(n) {
          const o = r;
          r = n, e.node.call(this, n), r = o;
        }, emit(n, o, i) {
          t.push({ type: o, value: n, node: i ? null : r });
        }, result() {
          return t;
        } };
      } };
      C = { type: "Match" };
      L = { type: "Mismatch" };
      Kt = { type: "DisallowEmpty" };
      Gl = 40;
      Vl = 41;
      ({ hasOwnProperty: Ji } = Object.prototype);
      Ql = 0;
      Xl = 1;
      Hr = 2;
      oa = 3;
      ea = "Match";
      $l = "Mismatch";
      Zl = "Maximum iteration number exceeded (please fill an issue on https://github.com/csstree/csstree/issues)";
      ta = 15e3;
      Jl = 0;
      Yr = {};
      b(Yr, { getTrace: () => ia, isKeyword: () => ic, isProperty: () => oc, isType: () => nc });
      ({ hasOwnProperty: mt } = Object.prototype);
      lc = Qt(Ft.join(" | "));
      Ke = class {
        constructor(t, r, n) {
          if (this.cssWideKeywordsSyntax = lc, this.syntax = r, this.generic = false, this.atrules = /* @__PURE__ */ Object.create(null), this.properties = /* @__PURE__ */ Object.create(null), this.types = /* @__PURE__ */ Object.create(null), this.structure = n || ca(t), t) {
            if (t.types) for (const o in t.types) this.addType_(o, t.types[o]);
            if (t.generic) {
              this.generic = true;
              for (const o in Dr) this.addType_(o, Dr[o]);
            }
            if (t.atrules) for (const o in t.atrules) this.addAtrule_(o, t.atrules[o]);
            if (t.properties) for (const o in t.properties) this.addProperty_(o, t.properties[o]);
          }
        }
        checkStructure(t) {
          function r(i, s) {
            o.push({ node: i, message: s });
          }
          const n = this.structure, o = [];
          return this.syntax.walk(t, (i) => {
            n.hasOwnProperty(i.type) ? n[i.type].check(i, r) : r(i, `Unknown node type \`${i.type}\``);
          }), o.length ? o : false;
        }
        createDescriptor(t, r, n, o = null) {
          const i = { type: r, name: n }, s = { type: r, name: n, parent: o, serializable: typeof t === "string" || t && typeof t.type === "string", syntax: null, match: null };
          return typeof t === "function" ? s.match = Qt(t, i) : (typeof t === "string" ? Object.defineProperty(s, "syntax", { get() {
            return Object.defineProperty(s, "syntax", { value: Ge(t) }), s.syntax;
          } }) : s.syntax = t, Object.defineProperty(s, "match", { get() {
            return Object.defineProperty(s, "match", { value: Qt(s.syntax, i) }), s.match;
          } })), s;
        }
        addAtrule_(t, r) {
          !r || (this.atrules[t] = { type: "Atrule", name: t, prelude: r.prelude ? this.createDescriptor(r.prelude, "AtrulePrelude", t) : null, descriptors: r.descriptors ? Object.keys(r.descriptors).reduce((n, o) => (n[o] = this.createDescriptor(r.descriptors[o], "AtruleDescriptor", o, t), n), /* @__PURE__ */ Object.create(null)) : null });
        }
        addProperty_(t, r) {
          !r || (this.properties[t] = this.createDescriptor(r, "Property", t));
        }
        addType_(t, r) {
          !r || (this.types[t] = this.createDescriptor(r, "Type", t));
        }
        checkAtruleName(t) {
          if (!this.getAtrule(t)) return new je("Unknown at-rule", `@${t}`);
        }
        checkAtrulePrelude(t, r) {
          const n = this.checkAtruleName(t);
          if (n) return n;
          const o = this.getAtrule(t);
          if (!o.prelude && r) return new SyntaxError(`At-rule \`@${t}\` should not contain a prelude`);
          if (o.prelude && !r && !Ve(this, o.prelude, "", false).matched) return new SyntaxError(`At-rule \`@${t}\` should contain a prelude`);
        }
        checkAtruleDescriptorName(t, r) {
          const n = this.checkAtruleName(t);
          if (n) return n;
          const o = this.getAtrule(t), i = Mt(r);
          if (!o.descriptors) return new SyntaxError(`At-rule \`@${t}\` has no known descriptors`);
          if (!o.descriptors[i.name] && !o.descriptors[i.basename]) return new je("Unknown at-rule descriptor", r);
        }
        checkPropertyName(t) {
          if (!this.getProperty(t)) return new je("Unknown property", t);
        }
        matchAtrulePrelude(t, r) {
          const n = this.checkAtrulePrelude(t, r);
          if (n) return ce(null, n);
          const o = this.getAtrule(t);
          return o.prelude ? Ve(this, o.prelude, r || "", false) : ce(null, null);
        }
        matchAtruleDescriptor(t, r, n) {
          const o = this.checkAtruleDescriptorName(t, r);
          if (o) return ce(null, o);
          const i = this.getAtrule(t), s = Mt(r);
          return Ve(this, i.descriptors[s.name] || i.descriptors[s.basename], n, false);
        }
        matchDeclaration(t) {
          return t.type !== "Declaration" ? ce(null, new Error("Not a Declaration node")) : this.matchProperty(t.property, t.value);
        }
        matchProperty(t, r) {
          if (vr(t).custom) return ce(null, new Error("Lexer matching doesn't applicable for custom properties"));
          const n = this.checkPropertyName(t);
          return n ? ce(null, n) : Ve(this, this.getProperty(t), r, true);
        }
        matchType(t, r) {
          const n = this.getType(t);
          return n ? Ve(this, n, r, false) : ce(null, new je("Unknown type", t));
        }
        match(t, r) {
          return typeof t !== "string" && (!t || !t.type) ? ce(null, new je("Bad syntax")) : ((typeof t === "string" || !t.match) && (t = this.createDescriptor(t, "Type", "anonymous")), Ve(this, t, r, false));
        }
        findValueFragments(t, r, n, o) {
          return Gr(this, r, this.matchProperty(t, r), n, o);
        }
        findDeclarationValueFragments(t, r, n) {
          return Gr(this, t.value, this.matchDeclaration(t), r, n);
        }
        findAllFragments(t, r, n) {
          const o = [];
          return this.syntax.walk(t, { visit: "Declaration", enter: (i) => {
            o.push.apply(o, this.findDeclarationValueFragments(i, r, n));
          } }), o;
        }
        getAtrule(t, r = true) {
          const n = Mt(t);
          return (n.vendor && r ? this.atrules[n.name] || this.atrules[n.basename] : this.atrules[n.name]) || null;
        }
        getAtrulePrelude(t, r = true) {
          const n = this.getAtrule(t, r);
          return n && n.prelude || null;
        }
        getAtruleDescriptor(t, r) {
          return this.atrules.hasOwnProperty(t) && this.atrules.declarators && this.atrules[t].declarators[r] || null;
        }
        getProperty(t, r = true) {
          const n = vr(t);
          return (n.vendor && r ? this.properties[n.name] || this.properties[n.basename] : this.properties[n.name]) || null;
        }
        getType(t) {
          return hasOwnProperty.call(this.types, t) ? this.types[t] : null;
        }
        validate() {
          function t(o, i, s, u) {
            if (s.has(i)) return s.get(i);
            s.set(i, false), u.syntax !== null && Vt(u.syntax, (c) => {
              if (c.type !== "Type" && c.type !== "Property") return;
              const a = c.type === "Type" ? o.types : o.properties, l = c.type === "Type" ? r : n;
              (!hasOwnProperty.call(a, c.name) || t(o, c.name, l, a[c.name])) && s.set(i, true);
            }, this);
          }
          let r = /* @__PURE__ */ new Map(), n = /* @__PURE__ */ new Map();
          for (const o in this.types) t(this, o, r, this.types[o]);
          for (const o in this.properties) t(this, o, n, this.properties[o]);
          return r = [...r.keys()].filter((o) => r.get(o)), n = [...n.keys()].filter((o) => n.get(o)), r.length || n.length ? { types: r, properties: n } : null;
        }
        dump(t, r) {
          return { generic: this.generic, types: Kr(this.types, !r, t), properties: Kr(this.properties, !r, t), atrules: cc(this.atrules, !r, t) };
        }
        toString() {
          return JSON.stringify(this.dump());
        }
      };
      ({ hasOwnProperty: Qe } = Object.prototype);
      pc = { generic: true, types: Qr, atrules: { prelude: pa, descriptors: pa }, properties: Qr, parseContext: hc, scope: ma, atrule: ["parse"], pseudo: ["parse"], node: ["name", "structure", "parse", "generate", "walkContext"] };
      $t = (e, t) => ft(e, t, pc);
      Xr = (e) => fa($t({}, e));
      da = { generic: true, types: { "absolute-size": "xx-small|x-small|small|medium|large|x-large|xx-large|xxx-large", "alpha-value": "<number>|<percentage>", "angle-percentage": "<angle>|<percentage>", "angular-color-hint": "<angle-percentage>", "angular-color-stop": "<color>&&<color-stop-angle>?", "angular-color-stop-list": "[<angular-color-stop> [, <angular-color-hint>]?]# , <angular-color-stop>", "animateable-feature": "scroll-position|contents|<custom-ident>", attachment: "scroll|fixed|local", "attr()": "attr( <attr-name> <type-or-unit>? [, <attr-fallback>]? )", "attr-matcher": "['~'|'|'|'^'|'$'|'*']? '='", "attr-modifier": "i|s", "attribute-selector": "'[' <wq-name> ']'|'[' <wq-name> <attr-matcher> [<string-token>|<ident-token>] <attr-modifier>? ']'", "auto-repeat": "repeat( [auto-fill|auto-fit] , [<line-names>? <fixed-size>]+ <line-names>? )", "auto-track-list": "[<line-names>? [<fixed-size>|<fixed-repeat>]]* <line-names>? <auto-repeat> [<line-names>? [<fixed-size>|<fixed-repeat>]]* <line-names>?", "baseline-position": "[first|last]? baseline", "basic-shape": "<inset()>|<circle()>|<ellipse()>|<polygon()>|<path()>", "bg-image": "none|<image>", "bg-layer": "<bg-image>||<bg-position> [/ <bg-size>]?||<repeat-style>||<attachment>||<box>||<box>", "bg-position": "[[left|center|right|top|bottom|<length-percentage>]|[left|center|right|<length-percentage>] [top|center|bottom|<length-percentage>]|[center|[left|right] <length-percentage>?]&&[center|[top|bottom] <length-percentage>?]]", "bg-size": "[<length-percentage>|auto]{1,2}|cover|contain", "blur()": "blur( <length> )", "blend-mode": "normal|multiply|screen|overlay|darken|lighten|color-dodge|color-burn|hard-light|soft-light|difference|exclusion|hue|saturation|color|luminosity", box: "border-box|padding-box|content-box", "brightness()": "brightness( <number-percentage> )", "calc()": "calc( <calc-sum> )", "calc-sum": "<calc-product> [['+'|'-'] <calc-product>]*", "calc-product": "<calc-value> ['*' <calc-value>|'/' <number>]*", "calc-value": "<number>|<dimension>|<percentage>|( <calc-sum> )", "cf-final-image": "<image>|<color>", "cf-mixing-image": "<percentage>?&&<image>", "circle()": "circle( [<shape-radius>]? [at <position>]? )", "clamp()": "clamp( <calc-sum>#{3} )", "class-selector": "'.' <ident-token>", "clip-source": "<url>", color: "<rgb()>|<rgba()>|<hsl()>|<hsla()>|<hwb()>|<lab()>|<lch()>|<hex-color>|<named-color>|currentcolor|<deprecated-system-color>", "color-stop": "<color-stop-length>|<color-stop-angle>", "color-stop-angle": "<angle-percentage>{1,2}", "color-stop-length": "<length-percentage>{1,2}", "color-stop-list": "[<linear-color-stop> [, <linear-color-hint>]?]# , <linear-color-stop>", combinator: "'>'|'+'|'~'|['||']", "common-lig-values": "[common-ligatures|no-common-ligatures]", "compat-auto": "searchfield|textarea|push-button|slider-horizontal|checkbox|radio|square-button|menulist|listbox|meter|progress-bar|button", "composite-style": "clear|copy|source-over|source-in|source-out|source-atop|destination-over|destination-in|destination-out|destination-atop|xor", "compositing-operator": "add|subtract|intersect|exclude", "compound-selector": "[<type-selector>? <subclass-selector>* [<pseudo-element-selector> <pseudo-class-selector>*]*]!", "compound-selector-list": "<compound-selector>#", "complex-selector": "<compound-selector> [<combinator>? <compound-selector>]*", "complex-selector-list": "<complex-selector>#", "conic-gradient()": "conic-gradient( [from <angle>]? [at <position>]? , <angular-color-stop-list> )", "contextual-alt-values": "[contextual|no-contextual]", "content-distribution": "space-between|space-around|space-evenly|stretch", "content-list": "[<string>|contents|<image>|<counter>|<quote>|<target>|<leader()>|<attr()>]+", "content-position": "center|start|end|flex-start|flex-end", "content-replacement": "<image>", "contrast()": "contrast( [<number-percentage>] )", counter: "<counter()>|<counters()>", "counter()": "counter( <counter-name> , <counter-style>? )", "counter-name": "<custom-ident>", "counter-style": "<counter-style-name>|symbols( )", "counter-style-name": "<custom-ident>", "counters()": "counters( <counter-name> , <string> , <counter-style>? )", "cross-fade()": "cross-fade( <cf-mixing-image> , <cf-final-image>? )", "cubic-bezier-timing-function": "ease|ease-in|ease-out|ease-in-out|cubic-bezier( <number [0,1]> , <number> , <number [0,1]> , <number> )", "deprecated-system-color": "ActiveBorder|ActiveCaption|AppWorkspace|Background|ButtonFace|ButtonHighlight|ButtonShadow|ButtonText|CaptionText|GrayText|Highlight|HighlightText|InactiveBorder|InactiveCaption|InactiveCaptionText|InfoBackground|InfoText|Menu|MenuText|Scrollbar|ThreeDDarkShadow|ThreeDFace|ThreeDHighlight|ThreeDLightShadow|ThreeDShadow|Window|WindowFrame|WindowText", "discretionary-lig-values": "[discretionary-ligatures|no-discretionary-ligatures]", "display-box": "contents|none", "display-inside": "flow|flow-root|table|flex|grid|ruby", "display-internal": "table-row-group|table-header-group|table-footer-group|table-row|table-cell|table-column-group|table-column|table-caption|ruby-base|ruby-text|ruby-base-container|ruby-text-container", "display-legacy": "inline-block|inline-list-item|inline-table|inline-flex|inline-grid", "display-listitem": "<display-outside>?&&[flow|flow-root]?&&list-item", "display-outside": "block|inline|run-in", "drop-shadow()": "drop-shadow( <length>{2,3} <color>? )", "east-asian-variant-values": "[jis78|jis83|jis90|jis04|simplified|traditional]", "east-asian-width-values": "[full-width|proportional-width]", "element()": "element( <custom-ident> , [first|start|last|first-except]? )|element( <id-selector> )", "ellipse()": "ellipse( [<shape-radius>{2}]? [at <position>]? )", "ending-shape": "circle|ellipse", "env()": "env( <custom-ident> , <declaration-value>? )", "explicit-track-list": "[<line-names>? <track-size>]+ <line-names>?", "family-name": "<string>|<custom-ident>+", "feature-tag-value": "<string> [<integer>|on|off]?", "feature-type": "@stylistic|@historical-forms|@styleset|@character-variant|@swash|@ornaments|@annotation", "feature-value-block": "<feature-type> '{' <feature-value-declaration-list> '}'", "feature-value-block-list": "<feature-value-block>+", "feature-value-declaration": "<custom-ident> : <integer>+ ;", "feature-value-declaration-list": "<feature-value-declaration>", "feature-value-name": "<custom-ident>", "fill-rule": "nonzero|evenodd", "filter-function": "<blur()>|<brightness()>|<contrast()>|<drop-shadow()>|<grayscale()>|<hue-rotate()>|<invert()>|<opacity()>|<saturate()>|<sepia()>", "filter-function-list": "[<filter-function>|<url>]+", "final-bg-layer": "<'background-color'>||<bg-image>||<bg-position> [/ <bg-size>]?||<repeat-style>||<attachment>||<box>||<box>", "fit-content()": "fit-content( [<length>|<percentage>] )", "fixed-breadth": "<length-percentage>", "fixed-repeat": "repeat( [<integer [1,\u221E]>] , [<line-names>? <fixed-size>]+ <line-names>? )", "fixed-size": "<fixed-breadth>|minmax( <fixed-breadth> , <track-breadth> )|minmax( <inflexible-breadth> , <fixed-breadth> )", "font-stretch-absolute": "normal|ultra-condensed|extra-condensed|condensed|semi-condensed|semi-expanded|expanded|extra-expanded|ultra-expanded|<percentage>", "font-variant-css21": "[normal|small-caps]", "font-weight-absolute": "normal|bold|<number [1,1000]>", "frequency-percentage": "<frequency>|<percentage>", "general-enclosed": "[<function-token> <any-value> )]|( <ident> <any-value> )", "generic-family": "serif|sans-serif|cursive|fantasy|monospace|-apple-system", "generic-name": "serif|sans-serif|cursive|fantasy|monospace", "geometry-box": "<shape-box>|fill-box|stroke-box|view-box", gradient: "<linear-gradient()>|<repeating-linear-gradient()>|<radial-gradient()>|<repeating-radial-gradient()>|<conic-gradient()>|<repeating-conic-gradient()>|<-legacy-gradient>", "grayscale()": "grayscale( <number-percentage> )", "grid-line": "auto|<custom-ident>|[<integer>&&<custom-ident>?]|[span&&[<integer>||<custom-ident>]]", "historical-lig-values": "[historical-ligatures|no-historical-ligatures]", "hsl()": "hsl( <hue> <percentage> <percentage> [/ <alpha-value>]? )|hsl( <hue> , <percentage> , <percentage> , <alpha-value>? )", "hsla()": "hsla( <hue> <percentage> <percentage> [/ <alpha-value>]? )|hsla( <hue> , <percentage> , <percentage> , <alpha-value>? )", hue: "<number>|<angle>", "hue-rotate()": "hue-rotate( <angle> )", "hwb()": "hwb( [<hue>|none] [<percentage>|none] [<percentage>|none] [/ [<alpha-value>|none]]? )", image: "<url>|<image()>|<image-set()>|<element()>|<paint()>|<cross-fade()>|<gradient>", "image()": "image( <image-tags>? [<image-src>? , <color>?]! )", "image-set()": "image-set( <image-set-option># )", "image-set-option": "[<image>|<string>] [<resolution>||type( <string> )]", "image-src": "<url>|<string>", "image-tags": "ltr|rtl", "inflexible-breadth": "<length>|<percentage>|min-content|max-content|auto", "inset()": "inset( <length-percentage>{1,4} [round <'border-radius'>]? )", "invert()": "invert( <number-percentage> )", "keyframes-name": "<custom-ident>|<string>", "keyframe-block": "<keyframe-selector># { <declaration-list> }", "keyframe-block-list": "<keyframe-block>+", "keyframe-selector": "from|to|<percentage>", "layer()": "layer( <layer-name> )", "layer-name": "<ident> ['.' <ident>]*", "leader()": "leader( <leader-type> )", "leader-type": "dotted|solid|space|<string>", "length-percentage": "<length>|<percentage>", "line-names": "'[' <custom-ident>* ']'", "line-name-list": "[<line-names>|<name-repeat>]+", "line-style": "none|hidden|dotted|dashed|solid|double|groove|ridge|inset|outset", "line-width": "<length>|thin|medium|thick", "linear-color-hint": "<length-percentage>", "linear-color-stop": "<color> <color-stop-length>?", "linear-gradient()": "linear-gradient( [<angle>|to <side-or-corner>]? , <color-stop-list> )", "mask-layer": "<mask-reference>||<position> [/ <bg-size>]?||<repeat-style>||<geometry-box>||[<geometry-box>|no-clip]||<compositing-operator>||<masking-mode>", "mask-position": "[<length-percentage>|left|center|right] [<length-percentage>|top|center|bottom]?", "mask-reference": "none|<image>|<mask-source>", "mask-source": "<url>", "masking-mode": "alpha|luminance|match-source", "matrix()": "matrix( <number>#{6} )", "matrix3d()": "matrix3d( <number>#{16} )", "max()": "max( <calc-sum># )", "media-and": "<media-in-parens> [and <media-in-parens>]+", "media-condition": "<media-not>|<media-and>|<media-or>|<media-in-parens>", "media-condition-without-or": "<media-not>|<media-and>|<media-in-parens>", "media-feature": "( [<mf-plain>|<mf-boolean>|<mf-range>] )", "media-in-parens": "( <media-condition> )|<media-feature>|<general-enclosed>", "media-not": "not <media-in-parens>", "media-or": "<media-in-parens> [or <media-in-parens>]+", "media-query": "<media-condition>|[not|only]? <media-type> [and <media-condition-without-or>]?", "media-query-list": "<media-query>#", "media-type": "<ident>", "mf-boolean": "<mf-name>", "mf-name": "<ident>", "mf-plain": "<mf-name> : <mf-value>", "mf-range": "<mf-name> ['<'|'>']? '='? <mf-value>|<mf-value> ['<'|'>']? '='? <mf-name>|<mf-value> '<' '='? <mf-name> '<' '='? <mf-value>|<mf-value> '>' '='? <mf-name> '>' '='? <mf-value>", "mf-value": "<number>|<dimension>|<ident>|<ratio>", "min()": "min( <calc-sum># )", "minmax()": "minmax( [<length>|<percentage>|min-content|max-content|auto] , [<length>|<percentage>|<flex>|min-content|max-content|auto] )", "name-repeat": "repeat( [<integer [1,\u221E]>|auto-fill] , <line-names>+ )", "named-color": "transparent|aliceblue|antiquewhite|aqua|aquamarine|azure|beige|bisque|black|blanchedalmond|blue|blueviolet|brown|burlywood|cadetblue|chartreuse|chocolate|coral|cornflowerblue|cornsilk|crimson|cyan|darkblue|darkcyan|darkgoldenrod|darkgray|darkgreen|darkgrey|darkkhaki|darkmagenta|darkolivegreen|darkorange|darkorchid|darkred|darksalmon|darkseagreen|darkslateblue|darkslategray|darkslategrey|darkturquoise|darkviolet|deeppink|deepskyblue|dimgray|dimgrey|dodgerblue|firebrick|floralwhite|forestgreen|fuchsia|gainsboro|ghostwhite|gold|goldenrod|gray|green|greenyellow|grey|honeydew|hotpink|indianred|indigo|ivory|khaki|lavender|lavenderblush|lawngreen|lemonchiffon|lightblue|lightcoral|lightcyan|lightgoldenrodyellow|lightgray|lightgreen|lightgrey|lightpink|lightsalmon|lightseagreen|lightskyblue|lightslategray|lightslategrey|lightsteelblue|lightyellow|lime|limegreen|linen|magenta|maroon|mediumaquamarine|mediumblue|mediumorchid|mediumpurple|mediumseagreen|mediumslateblue|mediumspringgreen|mediumturquoise|mediumvioletred|midnightblue|mintcream|mistyrose|moccasin|navajowhite|navy|oldlace|olive|olivedrab|orange|orangered|orchid|palegoldenrod|palegreen|paleturquoise|palevioletred|papayawhip|peachpuff|peru|pink|plum|powderblue|purple|rebeccapurple|red|rosybrown|royalblue|saddlebrown|salmon|sandybrown|seagreen|seashell|sienna|silver|skyblue|slateblue|slategray|slategrey|snow|springgreen|steelblue|tan|teal|thistle|tomato|turquoise|violet|wheat|white|whitesmoke|yellow|yellowgreen|<-non-standard-color>", "namespace-prefix": "<ident>", "ns-prefix": "[<ident-token>|'*']? '|'", "number-percentage": "<number>|<percentage>", "numeric-figure-values": "[lining-nums|oldstyle-nums]", "numeric-fraction-values": "[diagonal-fractions|stacked-fractions]", "numeric-spacing-values": "[proportional-nums|tabular-nums]", nth: "<an-plus-b>|even|odd", "opacity()": "opacity( [<number-percentage>] )", "overflow-position": "unsafe|safe", "outline-radius": "<length>|<percentage>", "page-body": "<declaration>? [; <page-body>]?|<page-margin-box> <page-body>", "page-margin-box": "<page-margin-box-type> '{' <declaration-list> '}'", "page-margin-box-type": "@top-left-corner|@top-left|@top-center|@top-right|@top-right-corner|@bottom-left-corner|@bottom-left|@bottom-center|@bottom-right|@bottom-right-corner|@left-top|@left-middle|@left-bottom|@right-top|@right-middle|@right-bottom", "page-selector-list": "[<page-selector>#]?", "page-selector": "<pseudo-page>+|<ident> <pseudo-page>*", "page-size": "A5|A4|A3|B5|B4|JIS-B5|JIS-B4|letter|legal|ledger", "path()": "path( [<fill-rule> ,]? <string> )", "paint()": "paint( <ident> , <declaration-value>? )", "perspective()": "perspective( <length> )", "polygon()": "polygon( <fill-rule>? , [<length-percentage> <length-percentage>]# )", position: "[[left|center|right]||[top|center|bottom]|[left|center|right|<length-percentage>] [top|center|bottom|<length-percentage>]?|[[left|right] <length-percentage>]&&[[top|bottom] <length-percentage>]]", "pseudo-class-selector": "':' <ident-token>|':' <function-token> <any-value> ')'", "pseudo-element-selector": "':' <pseudo-class-selector>", "pseudo-page": ": [left|right|first|blank]", quote: "open-quote|close-quote|no-open-quote|no-close-quote", "radial-gradient()": "radial-gradient( [<ending-shape>||<size>]? [at <position>]? , <color-stop-list> )", "relative-selector": "<combinator>? <complex-selector>", "relative-selector-list": "<relative-selector>#", "relative-size": "larger|smaller", "repeat-style": "repeat-x|repeat-y|[repeat|space|round|no-repeat]{1,2}", "repeating-conic-gradient()": "repeating-conic-gradient( [from <angle>]? [at <position>]? , <angular-color-stop-list> )", "repeating-linear-gradient()": "repeating-linear-gradient( [<angle>|to <side-or-corner>]? , <color-stop-list> )", "repeating-radial-gradient()": "repeating-radial-gradient( [<ending-shape>||<size>]? [at <position>]? , <color-stop-list> )", "rgb()": "rgb( <percentage>{3} [/ <alpha-value>]? )|rgb( <number>{3} [/ <alpha-value>]? )|rgb( <percentage>#{3} , <alpha-value>? )|rgb( <number>#{3} , <alpha-value>? )", "rgba()": "rgba( <percentage>{3} [/ <alpha-value>]? )|rgba( <number>{3} [/ <alpha-value>]? )|rgba( <percentage>#{3} , <alpha-value>? )|rgba( <number>#{3} , <alpha-value>? )", "rotate()": "rotate( [<angle>|<zero>] )", "rotate3d()": "rotate3d( <number> , <number> , <number> , [<angle>|<zero>] )", "rotateX()": "rotateX( [<angle>|<zero>] )", "rotateY()": "rotateY( [<angle>|<zero>] )", "rotateZ()": "rotateZ( [<angle>|<zero>] )", "saturate()": "saturate( <number-percentage> )", "scale()": "scale( <number> , <number>? )", "scale3d()": "scale3d( <number> , <number> , <number> )", "scaleX()": "scaleX( <number> )", "scaleY()": "scaleY( <number> )", "scaleZ()": "scaleZ( <number> )", "self-position": "center|start|end|self-start|self-end|flex-start|flex-end", "shape-radius": "<length-percentage>|closest-side|farthest-side", "skew()": "skew( [<angle>|<zero>] , [<angle>|<zero>]? )", "skewX()": "skewX( [<angle>|<zero>] )", "skewY()": "skewY( [<angle>|<zero>] )", "sepia()": "sepia( <number-percentage> )", shadow: "inset?&&<length>{2,4}&&<color>?", "shadow-t": "[<length>{2,3}&&<color>?]", shape: "rect( <top> , <right> , <bottom> , <left> )|rect( <top> <right> <bottom> <left> )", "shape-box": "<box>|margin-box", "side-or-corner": "[left|right]||[top|bottom]", "single-animation": "<time>||<easing-function>||<time>||<single-animation-iteration-count>||<single-animation-direction>||<single-animation-fill-mode>||<single-animation-play-state>||[none|<keyframes-name>]", "single-animation-direction": "normal|reverse|alternate|alternate-reverse", "single-animation-fill-mode": "none|forwards|backwards|both", "single-animation-iteration-count": "infinite|<number>", "single-animation-play-state": "running|paused", "single-animation-timeline": "auto|none|<timeline-name>", "single-transition": "[none|<single-transition-property>]||<time>||<easing-function>||<time>", "single-transition-property": "all|<custom-ident>", size: "closest-side|farthest-side|closest-corner|farthest-corner|<length>|<length-percentage>{2}", "step-position": "jump-start|jump-end|jump-none|jump-both|start|end", "step-timing-function": "step-start|step-end|steps( <integer> [, <step-position>]? )", "subclass-selector": "<id-selector>|<class-selector>|<attribute-selector>|<pseudo-class-selector>", "supports-condition": "not <supports-in-parens>|<supports-in-parens> [and <supports-in-parens>]*|<supports-in-parens> [or <supports-in-parens>]*", "supports-in-parens": "( <supports-condition> )|<supports-feature>|<general-enclosed>", "supports-feature": "<supports-decl>|<supports-selector-fn>", "supports-decl": "( <declaration> )", "supports-selector-fn": "selector( <complex-selector> )", symbol: "<string>|<image>|<custom-ident>", target: "<target-counter()>|<target-counters()>|<target-text()>", "target-counter()": "target-counter( [<string>|<url>] , <custom-ident> , <counter-style>? )", "target-counters()": "target-counters( [<string>|<url>] , <custom-ident> , <string> , <counter-style>? )", "target-text()": "target-text( [<string>|<url>] , [content|before|after|first-letter]? )", "time-percentage": "<time>|<percentage>", "timeline-name": "<custom-ident>|<string>", "easing-function": "linear|<cubic-bezier-timing-function>|<step-timing-function>", "track-breadth": "<length-percentage>|<flex>|min-content|max-content|auto", "track-list": "[<line-names>? [<track-size>|<track-repeat>]]+ <line-names>?", "track-repeat": "repeat( [<integer [1,\u221E]>] , [<line-names>? <track-size>]+ <line-names>? )", "track-size": "<track-breadth>|minmax( <inflexible-breadth> , <track-breadth> )|fit-content( [<length>|<percentage>] )", "transform-function": "<matrix()>|<translate()>|<translateX()>|<translateY()>|<scale()>|<scaleX()>|<scaleY()>|<rotate()>|<skew()>|<skewX()>|<skewY()>|<matrix3d()>|<translate3d()>|<translateZ()>|<scale3d()>|<scaleZ()>|<rotate3d()>|<rotateX()>|<rotateY()>|<rotateZ()>|<perspective()>", "transform-list": "<transform-function>+", "translate()": "translate( <length-percentage> , <length-percentage>? )", "translate3d()": "translate3d( <length-percentage> , <length-percentage> , <length> )", "translateX()": "translateX( <length-percentage> )", "translateY()": "translateY( <length-percentage> )", "translateZ()": "translateZ( <length> )", "type-or-unit": "string|color|url|integer|number|length|angle|time|frequency|cap|ch|em|ex|ic|lh|rlh|rem|vb|vi|vw|vh|vmin|vmax|mm|Q|cm|in|pt|pc|px|deg|grad|rad|turn|ms|s|Hz|kHz|%", "type-selector": "<wq-name>|<ns-prefix>? '*'", "var()": "var( <custom-property-name> , <declaration-value>? )", "viewport-length": "auto|<length-percentage>", "visual-box": "content-box|padding-box|border-box", "wq-name": "<ns-prefix>? <ident-token>", "-legacy-gradient": "<-webkit-gradient()>|<-legacy-linear-gradient>|<-legacy-repeating-linear-gradient>|<-legacy-radial-gradient>|<-legacy-repeating-radial-gradient>", "-legacy-linear-gradient": "-moz-linear-gradient( <-legacy-linear-gradient-arguments> )|-webkit-linear-gradient( <-legacy-linear-gradient-arguments> )|-o-linear-gradient( <-legacy-linear-gradient-arguments> )", "-legacy-repeating-linear-gradient": "-moz-repeating-linear-gradient( <-legacy-linear-gradient-arguments> )|-webkit-repeating-linear-gradient( <-legacy-linear-gradient-arguments> )|-o-repeating-linear-gradient( <-legacy-linear-gradient-arguments> )", "-legacy-linear-gradient-arguments": "[<angle>|<side-or-corner>]? , <color-stop-list>", "-legacy-radial-gradient": "-moz-radial-gradient( <-legacy-radial-gradient-arguments> )|-webkit-radial-gradient( <-legacy-radial-gradient-arguments> )|-o-radial-gradient( <-legacy-radial-gradient-arguments> )", "-legacy-repeating-radial-gradient": "-moz-repeating-radial-gradient( <-legacy-radial-gradient-arguments> )|-webkit-repeating-radial-gradient( <-legacy-radial-gradient-arguments> )|-o-repeating-radial-gradient( <-legacy-radial-gradient-arguments> )", "-legacy-radial-gradient-arguments": "[<position> ,]? [[[<-legacy-radial-gradient-shape>||<-legacy-radial-gradient-size>]|[<length>|<percentage>]{2}] ,]? <color-stop-list>", "-legacy-radial-gradient-size": "closest-side|closest-corner|farthest-side|farthest-corner|contain|cover", "-legacy-radial-gradient-shape": "circle|ellipse", "-non-standard-font": "-apple-system-body|-apple-system-headline|-apple-system-subheadline|-apple-system-caption1|-apple-system-caption2|-apple-system-footnote|-apple-system-short-body|-apple-system-short-headline|-apple-system-short-subheadline|-apple-system-short-caption1|-apple-system-short-footnote|-apple-system-tall-body", "-non-standard-color": "-moz-ButtonDefault|-moz-ButtonHoverFace|-moz-ButtonHoverText|-moz-CellHighlight|-moz-CellHighlightText|-moz-Combobox|-moz-ComboboxText|-moz-Dialog|-moz-DialogText|-moz-dragtargetzone|-moz-EvenTreeRow|-moz-Field|-moz-FieldText|-moz-html-CellHighlight|-moz-html-CellHighlightText|-moz-mac-accentdarkestshadow|-moz-mac-accentdarkshadow|-moz-mac-accentface|-moz-mac-accentlightesthighlight|-moz-mac-accentlightshadow|-moz-mac-accentregularhighlight|-moz-mac-accentregularshadow|-moz-mac-chrome-active|-moz-mac-chrome-inactive|-moz-mac-focusring|-moz-mac-menuselect|-moz-mac-menushadow|-moz-mac-menutextselect|-moz-MenuHover|-moz-MenuHoverText|-moz-MenuBarText|-moz-MenuBarHoverText|-moz-nativehyperlinktext|-moz-OddTreeRow|-moz-win-communicationstext|-moz-win-mediatext|-moz-activehyperlinktext|-moz-default-background-color|-moz-default-color|-moz-hyperlinktext|-moz-visitedhyperlinktext|-webkit-activelink|-webkit-focus-ring-color|-webkit-link|-webkit-text", "-non-standard-image-rendering": "optimize-contrast|-moz-crisp-edges|-o-crisp-edges|-webkit-optimize-contrast", "-non-standard-overflow": "-moz-scrollbars-none|-moz-scrollbars-horizontal|-moz-scrollbars-vertical|-moz-hidden-unscrollable", "-non-standard-width": "fill-available|min-intrinsic|intrinsic|-moz-available|-moz-fit-content|-moz-min-content|-moz-max-content|-webkit-min-content|-webkit-max-content", "-webkit-gradient()": "-webkit-gradient( <-webkit-gradient-type> , <-webkit-gradient-point> [, <-webkit-gradient-point>|, <-webkit-gradient-radius> , <-webkit-gradient-point>] [, <-webkit-gradient-radius>]? [, <-webkit-gradient-color-stop>]* )", "-webkit-gradient-color-stop": "from( <color> )|color-stop( [<number-zero-one>|<percentage>] , <color> )|to( <color> )", "-webkit-gradient-point": "[left|center|right|<length-percentage>] [top|center|bottom|<length-percentage>]", "-webkit-gradient-radius": "<length>|<percentage>", "-webkit-gradient-type": "linear|radial", "-webkit-mask-box-repeat": "repeat|stretch|round", "-webkit-mask-clip-style": "border|border-box|padding|padding-box|content|content-box|text", "-ms-filter-function-list": "<-ms-filter-function>+", "-ms-filter-function": "<-ms-filter-function-progid>|<-ms-filter-function-legacy>", "-ms-filter-function-progid": "'progid:' [<ident-token> '.']* [<ident-token>|<function-token> <any-value>? )]", "-ms-filter-function-legacy": "<ident-token>|<function-token> <any-value>? )", "-ms-filter": "<string>", age: "child|young|old", "attr-name": "<wq-name>", "attr-fallback": "<any-value>", "bg-clip": "<box>|border|text", "border-radius": "<length-percentage>{1,2}", bottom: "<length>|auto", "generic-voice": "[<age>? <gender> <integer>?]", gender: "male|female|neutral", "lab()": "lab( [<percentage>|<number>|none] [<percentage>|<number>|none] [<percentage>|<number>|none] [/ [<alpha-value>|none]]? )", "lch()": "lch( [<percentage>|<number>|none] [<percentage>|<number>|none] [<hue>|none] [/ [<alpha-value>|none]]? )", left: "<length>|auto", "mask-image": "<mask-reference>#", paint: "none|<color>|<url> [none|<color>]?|context-fill|context-stroke", ratio: "<number [0,\u221E]> [/ <number [0,\u221E]>]?", "reversed-counter-name": "reversed( <counter-name> )", right: "<length>|auto", "svg-length": "<percentage>|<length>|<number>", "svg-writing-mode": "lr-tb|rl-tb|tb-rl|lr|rl|tb", top: "<length>|auto", "track-group": "'(' [<string>* <track-minmax> <string>*]+ ')' ['[' <positive-integer> ']']?|<track-minmax>", "track-list-v0": "[<string>* <track-group> <string>*]+|none", "track-minmax": "minmax( <track-breadth> , <track-breadth> )|auto|<track-breadth>|fit-content", x: "<number>", y: "<number>", declaration: "<ident-token> : <declaration-value>? ['!' important]?", "declaration-list": "[<declaration>? ';']* <declaration>?", url: "url( <string> <url-modifier>* )|<url-token>", "url-modifier": "<ident>|<function-token> <any-value> )", "number-zero-one": "<number [0,1]>", "number-one-or-greater": "<number [1,\u221E]>", "positive-integer": "<integer [0,\u221E]>", "-non-standard-display": "-ms-inline-flexbox|-ms-grid|-ms-inline-grid|-webkit-flex|-webkit-inline-flex|-webkit-box|-webkit-inline-box|-moz-inline-stack|-moz-box|-moz-inline-box" }, properties: { "--*": "<declaration-value>", "-ms-accelerator": "false|true", "-ms-block-progression": "tb|rl|bt|lr", "-ms-content-zoom-chaining": "none|chained", "-ms-content-zooming": "none|zoom", "-ms-content-zoom-limit": "<'-ms-content-zoom-limit-min'> <'-ms-content-zoom-limit-max'>", "-ms-content-zoom-limit-max": "<percentage>", "-ms-content-zoom-limit-min": "<percentage>", "-ms-content-zoom-snap": "<'-ms-content-zoom-snap-type'>||<'-ms-content-zoom-snap-points'>", "-ms-content-zoom-snap-points": "snapInterval( <percentage> , <percentage> )|snapList( <percentage># )", "-ms-content-zoom-snap-type": "none|proximity|mandatory", "-ms-filter": "<string>", "-ms-flow-from": "[none|<custom-ident>]#", "-ms-flow-into": "[none|<custom-ident>]#", "-ms-grid-columns": "none|<track-list>|<auto-track-list>", "-ms-grid-rows": "none|<track-list>|<auto-track-list>", "-ms-high-contrast-adjust": "auto|none", "-ms-hyphenate-limit-chars": "auto|<integer>{1,3}", "-ms-hyphenate-limit-lines": "no-limit|<integer>", "-ms-hyphenate-limit-zone": "<percentage>|<length>", "-ms-ime-align": "auto|after", "-ms-overflow-style": "auto|none|scrollbar|-ms-autohiding-scrollbar", "-ms-scrollbar-3dlight-color": "<color>", "-ms-scrollbar-arrow-color": "<color>", "-ms-scrollbar-base-color": "<color>", "-ms-scrollbar-darkshadow-color": "<color>", "-ms-scrollbar-face-color": "<color>", "-ms-scrollbar-highlight-color": "<color>", "-ms-scrollbar-shadow-color": "<color>", "-ms-scrollbar-track-color": "<color>", "-ms-scroll-chaining": "chained|none", "-ms-scroll-limit": "<'-ms-scroll-limit-x-min'> <'-ms-scroll-limit-y-min'> <'-ms-scroll-limit-x-max'> <'-ms-scroll-limit-y-max'>", "-ms-scroll-limit-x-max": "auto|<length>", "-ms-scroll-limit-x-min": "<length>", "-ms-scroll-limit-y-max": "auto|<length>", "-ms-scroll-limit-y-min": "<length>", "-ms-scroll-rails": "none|railed", "-ms-scroll-snap-points-x": "snapInterval( <length-percentage> , <length-percentage> )|snapList( <length-percentage># )", "-ms-scroll-snap-points-y": "snapInterval( <length-percentage> , <length-percentage> )|snapList( <length-percentage># )", "-ms-scroll-snap-type": "none|proximity|mandatory", "-ms-scroll-snap-x": "<'-ms-scroll-snap-type'> <'-ms-scroll-snap-points-x'>", "-ms-scroll-snap-y": "<'-ms-scroll-snap-type'> <'-ms-scroll-snap-points-y'>", "-ms-scroll-translation": "none|vertical-to-horizontal", "-ms-text-autospace": "none|ideograph-alpha|ideograph-numeric|ideograph-parenthesis|ideograph-space", "-ms-touch-select": "grippers|none", "-ms-user-select": "none|element|text", "-ms-wrap-flow": "auto|both|start|end|maximum|clear", "-ms-wrap-margin": "<length>", "-ms-wrap-through": "wrap|none", "-moz-appearance": "none|button|button-arrow-down|button-arrow-next|button-arrow-previous|button-arrow-up|button-bevel|button-focus|caret|checkbox|checkbox-container|checkbox-label|checkmenuitem|dualbutton|groupbox|listbox|listitem|menuarrow|menubar|menucheckbox|menuimage|menuitem|menuitemtext|menulist|menulist-button|menulist-text|menulist-textfield|menupopup|menuradio|menuseparator|meterbar|meterchunk|progressbar|progressbar-vertical|progresschunk|progresschunk-vertical|radio|radio-container|radio-label|radiomenuitem|range|range-thumb|resizer|resizerpanel|scale-horizontal|scalethumbend|scalethumb-horizontal|scalethumbstart|scalethumbtick|scalethumb-vertical|scale-vertical|scrollbarbutton-down|scrollbarbutton-left|scrollbarbutton-right|scrollbarbutton-up|scrollbarthumb-horizontal|scrollbarthumb-vertical|scrollbartrack-horizontal|scrollbartrack-vertical|searchfield|separator|sheet|spinner|spinner-downbutton|spinner-textfield|spinner-upbutton|splitter|statusbar|statusbarpanel|tab|tabpanel|tabpanels|tab-scroll-arrow-back|tab-scroll-arrow-forward|textfield|textfield-multiline|toolbar|toolbarbutton|toolbarbutton-dropdown|toolbargripper|toolbox|tooltip|treeheader|treeheadercell|treeheadersortarrow|treeitem|treeline|treetwisty|treetwistyopen|treeview|-moz-mac-unified-toolbar|-moz-win-borderless-glass|-moz-win-browsertabbar-toolbox|-moz-win-communicationstext|-moz-win-communications-toolbox|-moz-win-exclude-glass|-moz-win-glass|-moz-win-mediatext|-moz-win-media-toolbox|-moz-window-button-box|-moz-window-button-box-maximized|-moz-window-button-close|-moz-window-button-maximize|-moz-window-button-minimize|-moz-window-button-restore|-moz-window-frame-bottom|-moz-window-frame-left|-moz-window-frame-right|-moz-window-titlebar|-moz-window-titlebar-maximized", "-moz-binding": "<url>|none", "-moz-border-bottom-colors": "<color>+|none", "-moz-border-left-colors": "<color>+|none", "-moz-border-right-colors": "<color>+|none", "-moz-border-top-colors": "<color>+|none", "-moz-context-properties": "none|[fill|fill-opacity|stroke|stroke-opacity]#", "-moz-float-edge": "border-box|content-box|margin-box|padding-box", "-moz-force-broken-image-icon": "0|1", "-moz-image-region": "<shape>|auto", "-moz-orient": "inline|block|horizontal|vertical", "-moz-outline-radius": "<outline-radius>{1,4} [/ <outline-radius>{1,4}]?", "-moz-outline-radius-bottomleft": "<outline-radius>", "-moz-outline-radius-bottomright": "<outline-radius>", "-moz-outline-radius-topleft": "<outline-radius>", "-moz-outline-radius-topright": "<outline-radius>", "-moz-stack-sizing": "ignore|stretch-to-fit", "-moz-text-blink": "none|blink", "-moz-user-focus": "ignore|normal|select-after|select-before|select-menu|select-same|select-all|none", "-moz-user-input": "auto|none|enabled|disabled", "-moz-user-modify": "read-only|read-write|write-only", "-moz-window-dragging": "drag|no-drag", "-moz-window-shadow": "default|menu|tooltip|sheet|none", "-webkit-appearance": "none|button|button-bevel|caps-lock-indicator|caret|checkbox|default-button|inner-spin-button|listbox|listitem|media-controls-background|media-controls-fullscreen-background|media-current-time-display|media-enter-fullscreen-button|media-exit-fullscreen-button|media-fullscreen-button|media-mute-button|media-overlay-play-button|media-play-button|media-seek-back-button|media-seek-forward-button|media-slider|media-sliderthumb|media-time-remaining-display|media-toggle-closed-captions-button|media-volume-slider|media-volume-slider-container|media-volume-sliderthumb|menulist|menulist-button|menulist-text|menulist-textfield|meter|progress-bar|progress-bar-value|push-button|radio|scrollbarbutton-down|scrollbarbutton-left|scrollbarbutton-right|scrollbarbutton-up|scrollbargripper-horizontal|scrollbargripper-vertical|scrollbarthumb-horizontal|scrollbarthumb-vertical|scrollbartrack-horizontal|scrollbartrack-vertical|searchfield|searchfield-cancel-button|searchfield-decoration|searchfield-results-button|searchfield-results-decoration|slider-horizontal|slider-vertical|sliderthumb-horizontal|sliderthumb-vertical|square-button|textarea|textfield|-apple-pay-button", "-webkit-border-before": "<'border-width'>||<'border-style'>||<color>", "-webkit-border-before-color": "<color>", "-webkit-border-before-style": "<'border-style'>", "-webkit-border-before-width": "<'border-width'>", "-webkit-box-reflect": "[above|below|right|left]? <length>? <image>?", "-webkit-line-clamp": "none|<integer>", "-webkit-mask": "[<mask-reference>||<position> [/ <bg-size>]?||<repeat-style>||[<box>|border|padding|content|text]||[<box>|border|padding|content]]#", "-webkit-mask-attachment": "<attachment>#", "-webkit-mask-clip": "[<box>|border|padding|content|text]#", "-webkit-mask-composite": "<composite-style>#", "-webkit-mask-image": "<mask-reference>#", "-webkit-mask-origin": "[<box>|border|padding|content]#", "-webkit-mask-position": "<position>#", "-webkit-mask-position-x": "[<length-percentage>|left|center|right]#", "-webkit-mask-position-y": "[<length-percentage>|top|center|bottom]#", "-webkit-mask-repeat": "<repeat-style>#", "-webkit-mask-repeat-x": "repeat|no-repeat|space|round", "-webkit-mask-repeat-y": "repeat|no-repeat|space|round", "-webkit-mask-size": "<bg-size>#", "-webkit-overflow-scrolling": "auto|touch", "-webkit-tap-highlight-color": "<color>", "-webkit-text-fill-color": "<color>", "-webkit-text-stroke": "<length>||<color>", "-webkit-text-stroke-color": "<color>", "-webkit-text-stroke-width": "<length>", "-webkit-touch-callout": "default|none", "-webkit-user-modify": "read-only|read-write|read-write-plaintext-only", "accent-color": "auto|<color>", "align-content": "normal|<baseline-position>|<content-distribution>|<overflow-position>? <content-position>", "align-items": "normal|stretch|<baseline-position>|[<overflow-position>? <self-position>]", "align-self": "auto|normal|stretch|<baseline-position>|<overflow-position>? <self-position>", "align-tracks": "[normal|<baseline-position>|<content-distribution>|<overflow-position>? <content-position>]#", all: "initial|inherit|unset|revert|revert-layer", animation: "<single-animation>#", "animation-delay": "<time>#", "animation-direction": "<single-animation-direction>#", "animation-duration": "<time>#", "animation-fill-mode": "<single-animation-fill-mode>#", "animation-iteration-count": "<single-animation-iteration-count>#", "animation-name": "[none|<keyframes-name>]#", "animation-play-state": "<single-animation-play-state>#", "animation-timing-function": "<easing-function>#", "animation-timeline": "<single-animation-timeline>#", appearance: "none|auto|textfield|menulist-button|<compat-auto>", "aspect-ratio": "auto|<ratio>", azimuth: "<angle>|[[left-side|far-left|left|center-left|center|center-right|right|far-right|right-side]||behind]|leftwards|rightwards", "backdrop-filter": "none|<filter-function-list>", "backface-visibility": "visible|hidden", background: "[<bg-layer> ,]* <final-bg-layer>", "background-attachment": "<attachment>#", "background-blend-mode": "<blend-mode>#", "background-clip": "<bg-clip>#", "background-color": "<color>", "background-image": "<bg-image>#", "background-origin": "<box>#", "background-position": "<bg-position>#", "background-position-x": "[center|[[left|right|x-start|x-end]? <length-percentage>?]!]#", "background-position-y": "[center|[[top|bottom|y-start|y-end]? <length-percentage>?]!]#", "background-repeat": "<repeat-style>#", "background-size": "<bg-size>#", "block-overflow": "clip|ellipsis|<string>", "block-size": "<'width'>", border: "<line-width>||<line-style>||<color>", "border-block": "<'border-top-width'>||<'border-top-style'>||<color>", "border-block-color": "<'border-top-color'>{1,2}", "border-block-style": "<'border-top-style'>", "border-block-width": "<'border-top-width'>", "border-block-end": "<'border-top-width'>||<'border-top-style'>||<color>", "border-block-end-color": "<'border-top-color'>", "border-block-end-style": "<'border-top-style'>", "border-block-end-width": "<'border-top-width'>", "border-block-start": "<'border-top-width'>||<'border-top-style'>||<color>", "border-block-start-color": "<'border-top-color'>", "border-block-start-style": "<'border-top-style'>", "border-block-start-width": "<'border-top-width'>", "border-bottom": "<line-width>||<line-style>||<color>", "border-bottom-color": "<'border-top-color'>", "border-bottom-left-radius": "<length-percentage>{1,2}", "border-bottom-right-radius": "<length-percentage>{1,2}", "border-bottom-style": "<line-style>", "border-bottom-width": "<line-width>", "border-collapse": "collapse|separate", "border-color": "<color>{1,4}", "border-end-end-radius": "<length-percentage>{1,2}", "border-end-start-radius": "<length-percentage>{1,2}", "border-image": "<'border-image-source'>||<'border-image-slice'> [/ <'border-image-width'>|/ <'border-image-width'>? / <'border-image-outset'>]?||<'border-image-repeat'>", "border-image-outset": "[<length>|<number>]{1,4}", "border-image-repeat": "[stretch|repeat|round|space]{1,2}", "border-image-slice": "<number-percentage>{1,4}&&fill?", "border-image-source": "none|<image>", "border-image-width": "[<length-percentage>|<number>|auto]{1,4}", "border-inline": "<'border-top-width'>||<'border-top-style'>||<color>", "border-inline-end": "<'border-top-width'>||<'border-top-style'>||<color>", "border-inline-color": "<'border-top-color'>{1,2}", "border-inline-style": "<'border-top-style'>", "border-inline-width": "<'border-top-width'>", "border-inline-end-color": "<'border-top-color'>", "border-inline-end-style": "<'border-top-style'>", "border-inline-end-width": "<'border-top-width'>", "border-inline-start": "<'border-top-width'>||<'border-top-style'>||<color>", "border-inline-start-color": "<'border-top-color'>", "border-inline-start-style": "<'border-top-style'>", "border-inline-start-width": "<'border-top-width'>", "border-left": "<line-width>||<line-style>||<color>", "border-left-color": "<color>", "border-left-style": "<line-style>", "border-left-width": "<line-width>", "border-radius": "<length-percentage>{1,4} [/ <length-percentage>{1,4}]?", "border-right": "<line-width>||<line-style>||<color>", "border-right-color": "<color>", "border-right-style": "<line-style>", "border-right-width": "<line-width>", "border-spacing": "<length> <length>?", "border-start-end-radius": "<length-percentage>{1,2}", "border-start-start-radius": "<length-percentage>{1,2}", "border-style": "<line-style>{1,4}", "border-top": "<line-width>||<line-style>||<color>", "border-top-color": "<color>", "border-top-left-radius": "<length-percentage>{1,2}", "border-top-right-radius": "<length-percentage>{1,2}", "border-top-style": "<line-style>", "border-top-width": "<line-width>", "border-width": "<line-width>{1,4}", bottom: "<length>|<percentage>|auto", "box-align": "start|center|end|baseline|stretch", "box-decoration-break": "slice|clone", "box-direction": "normal|reverse|inherit", "box-flex": "<number>", "box-flex-group": "<integer>", "box-lines": "single|multiple", "box-ordinal-group": "<integer>", "box-orient": "horizontal|vertical|inline-axis|block-axis|inherit", "box-pack": "start|center|end|justify", "box-shadow": "none|<shadow>#", "box-sizing": "content-box|border-box", "break-after": "auto|avoid|always|all|avoid-page|page|left|right|recto|verso|avoid-column|column|avoid-region|region", "break-before": "auto|avoid|always|all|avoid-page|page|left|right|recto|verso|avoid-column|column|avoid-region|region", "break-inside": "auto|avoid|avoid-page|avoid-column|avoid-region", "caption-side": "top|bottom|block-start|block-end|inline-start|inline-end", "caret-color": "auto|<color>", clear: "none|left|right|both|inline-start|inline-end", clip: "<shape>|auto", "clip-path": "<clip-source>|[<basic-shape>||<geometry-box>]|none", color: "<color>", "print-color-adjust": "economy|exact", "color-scheme": "normal|[light|dark|<custom-ident>]+&&only?", "column-count": "<integer>|auto", "column-fill": "auto|balance|balance-all", "column-gap": "normal|<length-percentage>", "column-rule": "<'column-rule-width'>||<'column-rule-style'>||<'column-rule-color'>", "column-rule-color": "<color>", "column-rule-style": "<'border-style'>", "column-rule-width": "<'border-width'>", "column-span": "none|all", "column-width": "<length>|auto", columns: "<'column-width'>||<'column-count'>", contain: "none|strict|content|[size||layout||style||paint]", content: "normal|none|[<content-replacement>|<content-list>] [/ [<string>|<counter>]+]?", "content-visibility": "visible|auto|hidden", "counter-increment": "[<counter-name> <integer>?]+|none", "counter-reset": "[<counter-name> <integer>?|<reversed-counter-name> <integer>?]+|none", "counter-set": "[<counter-name> <integer>?]+|none", cursor: "[[<url> [<x> <y>]? ,]* [auto|default|none|context-menu|help|pointer|progress|wait|cell|crosshair|text|vertical-text|alias|copy|move|no-drop|not-allowed|e-resize|n-resize|ne-resize|nw-resize|s-resize|se-resize|sw-resize|w-resize|ew-resize|ns-resize|nesw-resize|nwse-resize|col-resize|row-resize|all-scroll|zoom-in|zoom-out|grab|grabbing|hand|-webkit-grab|-webkit-grabbing|-webkit-zoom-in|-webkit-zoom-out|-moz-grab|-moz-grabbing|-moz-zoom-in|-moz-zoom-out]]", direction: "ltr|rtl", display: "[<display-outside>||<display-inside>]|<display-listitem>|<display-internal>|<display-box>|<display-legacy>|<-non-standard-display>", "empty-cells": "show|hide", filter: "none|<filter-function-list>|<-ms-filter-function-list>", flex: "none|[<'flex-grow'> <'flex-shrink'>?||<'flex-basis'>]", "flex-basis": "content|<'width'>", "flex-direction": "row|row-reverse|column|column-reverse", "flex-flow": "<'flex-direction'>||<'flex-wrap'>", "flex-grow": "<number>", "flex-shrink": "<number>", "flex-wrap": "nowrap|wrap|wrap-reverse", float: "left|right|none|inline-start|inline-end", font: "[[<'font-style'>||<font-variant-css21>||<'font-weight'>||<'font-stretch'>]? <'font-size'> [/ <'line-height'>]? <'font-family'>]|caption|icon|menu|message-box|small-caption|status-bar", "font-family": "[<family-name>|<generic-family>]#", "font-feature-settings": "normal|<feature-tag-value>#", "font-kerning": "auto|normal|none", "font-language-override": "normal|<string>", "font-optical-sizing": "auto|none", "font-variation-settings": "normal|[<string> <number>]#", "font-size": "<absolute-size>|<relative-size>|<length-percentage>", "font-size-adjust": "none|[ex-height|cap-height|ch-width|ic-width|ic-height]? [from-font|<number>]", "font-smooth": "auto|never|always|<absolute-size>|<length>", "font-stretch": "<font-stretch-absolute>", "font-style": "normal|italic|oblique <angle>?", "font-synthesis": "none|[weight||style||small-caps]", "font-variant": "normal|none|[<common-lig-values>||<discretionary-lig-values>||<historical-lig-values>||<contextual-alt-values>||stylistic( <feature-value-name> )||historical-forms||styleset( <feature-value-name># )||character-variant( <feature-value-name># )||swash( <feature-value-name> )||ornaments( <feature-value-name> )||annotation( <feature-value-name> )||[small-caps|all-small-caps|petite-caps|all-petite-caps|unicase|titling-caps]||<numeric-figure-values>||<numeric-spacing-values>||<numeric-fraction-values>||ordinal||slashed-zero||<east-asian-variant-values>||<east-asian-width-values>||ruby]", "font-variant-alternates": "normal|[stylistic( <feature-value-name> )||historical-forms||styleset( <feature-value-name># )||character-variant( <feature-value-name># )||swash( <feature-value-name> )||ornaments( <feature-value-name> )||annotation( <feature-value-name> )]", "font-variant-caps": "normal|small-caps|all-small-caps|petite-caps|all-petite-caps|unicase|titling-caps", "font-variant-east-asian": "normal|[<east-asian-variant-values>||<east-asian-width-values>||ruby]", "font-variant-ligatures": "normal|none|[<common-lig-values>||<discretionary-lig-values>||<historical-lig-values>||<contextual-alt-values>]", "font-variant-numeric": "normal|[<numeric-figure-values>||<numeric-spacing-values>||<numeric-fraction-values>||ordinal||slashed-zero]", "font-variant-position": "normal|sub|super", "font-weight": "<font-weight-absolute>|bolder|lighter", "forced-color-adjust": "auto|none", gap: "<'row-gap'> <'column-gap'>?", grid: "<'grid-template'>|<'grid-template-rows'> / [auto-flow&&dense?] <'grid-auto-columns'>?|[auto-flow&&dense?] <'grid-auto-rows'>? / <'grid-template-columns'>", "grid-area": "<grid-line> [/ <grid-line>]{0,3}", "grid-auto-columns": "<track-size>+", "grid-auto-flow": "[row|column]||dense", "grid-auto-rows": "<track-size>+", "grid-column": "<grid-line> [/ <grid-line>]?", "grid-column-end": "<grid-line>", "grid-column-gap": "<length-percentage>", "grid-column-start": "<grid-line>", "grid-gap": "<'grid-row-gap'> <'grid-column-gap'>?", "grid-row": "<grid-line> [/ <grid-line>]?", "grid-row-end": "<grid-line>", "grid-row-gap": "<length-percentage>", "grid-row-start": "<grid-line>", "grid-template": "none|[<'grid-template-rows'> / <'grid-template-columns'>]|[<line-names>? <string> <track-size>? <line-names>?]+ [/ <explicit-track-list>]?", "grid-template-areas": "none|<string>+", "grid-template-columns": "none|<track-list>|<auto-track-list>|subgrid <line-name-list>?", "grid-template-rows": "none|<track-list>|<auto-track-list>|subgrid <line-name-list>?", "hanging-punctuation": "none|[first||[force-end|allow-end]||last]", height: "auto|<length>|<percentage>|min-content|max-content|fit-content|fit-content( <length-percentage> )", "hyphenate-character": "auto|<string>", hyphens: "none|manual|auto", "image-orientation": "from-image|<angle>|[<angle>? flip]", "image-rendering": "auto|crisp-edges|pixelated|optimizeSpeed|optimizeQuality|<-non-standard-image-rendering>", "image-resolution": "[from-image||<resolution>]&&snap?", "ime-mode": "auto|normal|active|inactive|disabled", "initial-letter": "normal|[<number> <integer>?]", "initial-letter-align": "[auto|alphabetic|hanging|ideographic]", "inline-size": "<'width'>", "input-security": "auto|none", inset: "<'top'>{1,4}", "inset-block": "<'top'>{1,2}", "inset-block-end": "<'top'>", "inset-block-start": "<'top'>", "inset-inline": "<'top'>{1,2}", "inset-inline-end": "<'top'>", "inset-inline-start": "<'top'>", isolation: "auto|isolate", "justify-content": "normal|<content-distribution>|<overflow-position>? [<content-position>|left|right]", "justify-items": "normal|stretch|<baseline-position>|<overflow-position>? [<self-position>|left|right]|legacy|legacy&&[left|right|center]", "justify-self": "auto|normal|stretch|<baseline-position>|<overflow-position>? [<self-position>|left|right]", "justify-tracks": "[normal|<content-distribution>|<overflow-position>? [<content-position>|left|right]]#", left: "<length>|<percentage>|auto", "letter-spacing": "normal|<length-percentage>", "line-break": "auto|loose|normal|strict|anywhere", "line-clamp": "none|<integer>", "line-height": "normal|<number>|<length>|<percentage>", "line-height-step": "<length>", "list-style": "<'list-style-type'>||<'list-style-position'>||<'list-style-image'>", "list-style-image": "<image>|none", "list-style-position": "inside|outside", "list-style-type": "<counter-style>|<string>|none", margin: "[<length>|<percentage>|auto]{1,4}", "margin-block": "<'margin-left'>{1,2}", "margin-block-end": "<'margin-left'>", "margin-block-start": "<'margin-left'>", "margin-bottom": "<length>|<percentage>|auto", "margin-inline": "<'margin-left'>{1,2}", "margin-inline-end": "<'margin-left'>", "margin-inline-start": "<'margin-left'>", "margin-left": "<length>|<percentage>|auto", "margin-right": "<length>|<percentage>|auto", "margin-top": "<length>|<percentage>|auto", "margin-trim": "none|in-flow|all", mask: "<mask-layer>#", "mask-border": "<'mask-border-source'>||<'mask-border-slice'> [/ <'mask-border-width'>? [/ <'mask-border-outset'>]?]?||<'mask-border-repeat'>||<'mask-border-mode'>", "mask-border-mode": "luminance|alpha", "mask-border-outset": "[<length>|<number>]{1,4}", "mask-border-repeat": "[stretch|repeat|round|space]{1,2}", "mask-border-slice": "<number-percentage>{1,4} fill?", "mask-border-source": "none|<image>", "mask-border-width": "[<length-percentage>|<number>|auto]{1,4}", "mask-clip": "[<geometry-box>|no-clip]#", "mask-composite": "<compositing-operator>#", "mask-image": "<mask-reference>#", "mask-mode": "<masking-mode>#", "mask-origin": "<geometry-box>#", "mask-position": "<position>#", "mask-repeat": "<repeat-style>#", "mask-size": "<bg-size>#", "mask-type": "luminance|alpha", "masonry-auto-flow": "[pack|next]||[definite-first|ordered]", "math-style": "normal|compact", "max-block-size": "<'max-width'>", "max-height": "none|<length-percentage>|min-content|max-content|fit-content|fit-content( <length-percentage> )", "max-inline-size": "<'max-width'>", "max-lines": "none|<integer>", "max-width": "none|<length-percentage>|min-content|max-content|fit-content|fit-content( <length-percentage> )|<-non-standard-width>", "min-block-size": "<'min-width'>", "min-height": "auto|<length>|<percentage>|min-content|max-content|fit-content|fit-content( <length-percentage> )", "min-inline-size": "<'min-width'>", "min-width": "auto|<length>|<percentage>|min-content|max-content|fit-content|fit-content( <length-percentage> )|<-non-standard-width>", "mix-blend-mode": "<blend-mode>|plus-lighter", "object-fit": "fill|contain|cover|none|scale-down", "object-position": "<position>", offset: "[<'offset-position'>? [<'offset-path'> [<'offset-distance'>||<'offset-rotate'>]?]?]! [/ <'offset-anchor'>]?", "offset-anchor": "auto|<position>", "offset-distance": "<length-percentage>", "offset-path": "none|ray( [<angle>&&<size>&&contain?] )|<path()>|<url>|[<basic-shape>||<geometry-box>]", "offset-position": "auto|<position>", "offset-rotate": "[auto|reverse]||<angle>", opacity: "<alpha-value>", order: "<integer>", orphans: "<integer>", outline: "[<'outline-color'>||<'outline-style'>||<'outline-width'>]", "outline-color": "<color>|invert", "outline-offset": "<length>", "outline-style": "auto|<'border-style'>", "outline-width": "<line-width>", overflow: "[visible|hidden|clip|scroll|auto]{1,2}|<-non-standard-overflow>", "overflow-anchor": "auto|none", "overflow-block": "visible|hidden|clip|scroll|auto", "overflow-clip-box": "padding-box|content-box", "overflow-clip-margin": "<visual-box>||<length [0,\u221E]>", "overflow-inline": "visible|hidden|clip|scroll|auto", "overflow-wrap": "normal|break-word|anywhere", "overflow-x": "visible|hidden|clip|scroll|auto", "overflow-y": "visible|hidden|clip|scroll|auto", "overscroll-behavior": "[contain|none|auto]{1,2}", "overscroll-behavior-block": "contain|none|auto", "overscroll-behavior-inline": "contain|none|auto", "overscroll-behavior-x": "contain|none|auto", "overscroll-behavior-y": "contain|none|auto", padding: "[<length>|<percentage>]{1,4}", "padding-block": "<'padding-left'>{1,2}", "padding-block-end": "<'padding-left'>", "padding-block-start": "<'padding-left'>", "padding-bottom": "<length>|<percentage>", "padding-inline": "<'padding-left'>{1,2}", "padding-inline-end": "<'padding-left'>", "padding-inline-start": "<'padding-left'>", "padding-left": "<length>|<percentage>", "padding-right": "<length>|<percentage>", "padding-top": "<length>|<percentage>", "page-break-after": "auto|always|avoid|left|right|recto|verso", "page-break-before": "auto|always|avoid|left|right|recto|verso", "page-break-inside": "auto|avoid", "paint-order": "normal|[fill||stroke||markers]", perspective: "none|<length>", "perspective-origin": "<position>", "place-content": "<'align-content'> <'justify-content'>?", "place-items": "<'align-items'> <'justify-items'>?", "place-self": "<'align-self'> <'justify-self'>?", "pointer-events": "auto|none|visiblePainted|visibleFill|visibleStroke|visible|painted|fill|stroke|all|inherit", position: "static|relative|absolute|sticky|fixed|-webkit-sticky", quotes: "none|auto|[<string> <string>]+", resize: "none|both|horizontal|vertical|block|inline", right: "<length>|<percentage>|auto", rotate: "none|<angle>|[x|y|z|<number>{3}]&&<angle>", "row-gap": "normal|<length-percentage>", "ruby-align": "start|center|space-between|space-around", "ruby-merge": "separate|collapse|auto", "ruby-position": "[alternate||[over|under]]|inter-character", scale: "none|<number>{1,3}", "scrollbar-color": "auto|<color>{2}", "scrollbar-gutter": "auto|stable&&both-edges?", "scrollbar-width": "auto|thin|none", "scroll-behavior": "auto|smooth", "scroll-margin": "<length>{1,4}", "scroll-margin-block": "<length>{1,2}", "scroll-margin-block-start": "<length>", "scroll-margin-block-end": "<length>", "scroll-margin-bottom": "<length>", "scroll-margin-inline": "<length>{1,2}", "scroll-margin-inline-start": "<length>", "scroll-margin-inline-end": "<length>", "scroll-margin-left": "<length>", "scroll-margin-right": "<length>", "scroll-margin-top": "<length>", "scroll-padding": "[auto|<length-percentage>]{1,4}", "scroll-padding-block": "[auto|<length-percentage>]{1,2}", "scroll-padding-block-start": "auto|<length-percentage>", "scroll-padding-block-end": "auto|<length-percentage>", "scroll-padding-bottom": "auto|<length-percentage>", "scroll-padding-inline": "[auto|<length-percentage>]{1,2}", "scroll-padding-inline-start": "auto|<length-percentage>", "scroll-padding-inline-end": "auto|<length-percentage>", "scroll-padding-left": "auto|<length-percentage>", "scroll-padding-right": "auto|<length-percentage>", "scroll-padding-top": "auto|<length-percentage>", "scroll-snap-align": "[none|start|end|center]{1,2}", "scroll-snap-coordinate": "none|<position>#", "scroll-snap-destination": "<position>", "scroll-snap-points-x": "none|repeat( <length-percentage> )", "scroll-snap-points-y": "none|repeat( <length-percentage> )", "scroll-snap-stop": "normal|always", "scroll-snap-type": "none|[x|y|block|inline|both] [mandatory|proximity]?", "scroll-snap-type-x": "none|mandatory|proximity", "scroll-snap-type-y": "none|mandatory|proximity", "shape-image-threshold": "<alpha-value>", "shape-margin": "<length-percentage>", "shape-outside": "none|[<shape-box>||<basic-shape>]|<image>", "tab-size": "<integer>|<length>", "table-layout": "auto|fixed", "text-align": "start|end|left|right|center|justify|match-parent", "text-align-last": "auto|start|end|left|right|center|justify", "text-combine-upright": "none|all|[digits <integer>?]", "text-decoration": "<'text-decoration-line'>||<'text-decoration-style'>||<'text-decoration-color'>||<'text-decoration-thickness'>", "text-decoration-color": "<color>", "text-decoration-line": "none|[underline||overline||line-through||blink]|spelling-error|grammar-error", "text-decoration-skip": "none|[objects||[spaces|[leading-spaces||trailing-spaces]]||edges||box-decoration]", "text-decoration-skip-ink": "auto|all|none", "text-decoration-style": "solid|double|dotted|dashed|wavy", "text-decoration-thickness": "auto|from-font|<length>|<percentage>", "text-emphasis": "<'text-emphasis-style'>||<'text-emphasis-color'>", "text-emphasis-color": "<color>", "text-emphasis-position": "[over|under]&&[right|left]", "text-emphasis-style": "none|[[filled|open]||[dot|circle|double-circle|triangle|sesame]]|<string>", "text-indent": "<length-percentage>&&hanging?&&each-line?", "text-justify": "auto|inter-character|inter-word|none", "text-orientation": "mixed|upright|sideways", "text-overflow": "[clip|ellipsis|<string>]{1,2}", "text-rendering": "auto|optimizeSpeed|optimizeLegibility|geometricPrecision", "text-shadow": "none|<shadow-t>#", "text-size-adjust": "none|auto|<percentage>", "text-transform": "none|capitalize|uppercase|lowercase|full-width|full-size-kana", "text-underline-offset": "auto|<length>|<percentage>", "text-underline-position": "auto|from-font|[under||[left|right]]", top: "<length>|<percentage>|auto", "touch-action": "auto|none|[[pan-x|pan-left|pan-right]||[pan-y|pan-up|pan-down]||pinch-zoom]|manipulation", transform: "none|<transform-list>", "transform-box": "content-box|border-box|fill-box|stroke-box|view-box", "transform-origin": "[<length-percentage>|left|center|right|top|bottom]|[[<length-percentage>|left|center|right]&&[<length-percentage>|top|center|bottom]] <length>?", "transform-style": "flat|preserve-3d", transition: "<single-transition>#", "transition-delay": "<time>#", "transition-duration": "<time>#", "transition-property": "none|<single-transition-property>#", "transition-timing-function": "<easing-function>#", translate: "none|<length-percentage> [<length-percentage> <length>?]?", "unicode-bidi": "normal|embed|isolate|bidi-override|isolate-override|plaintext|-moz-isolate|-moz-isolate-override|-moz-plaintext|-webkit-isolate|-webkit-isolate-override|-webkit-plaintext", "user-select": "auto|text|none|contain|all", "vertical-align": "baseline|sub|super|text-top|text-bottom|middle|top|bottom|<percentage>|<length>", visibility: "visible|hidden|collapse", "white-space": "normal|pre|nowrap|pre-wrap|pre-line|break-spaces", widows: "<integer>", width: "auto|<length>|<percentage>|min-content|max-content|fit-content|fit-content( <length-percentage> )|fill|stretch|intrinsic|-moz-max-content|-webkit-max-content|-moz-fit-content|-webkit-fit-content", "will-change": "auto|<animateable-feature>#", "word-break": "normal|break-all|keep-all|break-word", "word-spacing": "normal|<length>", "word-wrap": "normal|break-word", "writing-mode": "horizontal-tb|vertical-rl|vertical-lr|sideways-rl|sideways-lr|<svg-writing-mode>", "z-index": "auto|<integer>", zoom: "normal|reset|<number>|<percentage>", "-moz-background-clip": "padding|border", "-moz-border-radius-bottomleft": "<'border-bottom-left-radius'>", "-moz-border-radius-bottomright": "<'border-bottom-right-radius'>", "-moz-border-radius-topleft": "<'border-top-left-radius'>", "-moz-border-radius-topright": "<'border-bottom-right-radius'>", "-moz-control-character-visibility": "visible|hidden", "-moz-osx-font-smoothing": "auto|grayscale", "-moz-user-select": "none|text|all|-moz-none", "-ms-flex-align": "start|end|center|baseline|stretch", "-ms-flex-item-align": "auto|start|end|center|baseline|stretch", "-ms-flex-line-pack": "start|end|center|justify|distribute|stretch", "-ms-flex-negative": "<'flex-shrink'>", "-ms-flex-pack": "start|end|center|justify|distribute", "-ms-flex-order": "<integer>", "-ms-flex-positive": "<'flex-grow'>", "-ms-flex-preferred-size": "<'flex-basis'>", "-ms-interpolation-mode": "nearest-neighbor|bicubic", "-ms-grid-column-align": "start|end|center|stretch", "-ms-grid-row-align": "start|end|center|stretch", "-ms-hyphenate-limit-last": "none|always|column|page|spread", "-webkit-background-clip": "[<box>|border|padding|content|text]#", "-webkit-column-break-after": "always|auto|avoid", "-webkit-column-break-before": "always|auto|avoid", "-webkit-column-break-inside": "always|auto|avoid", "-webkit-font-smoothing": "auto|none|antialiased|subpixel-antialiased", "-webkit-mask-box-image": "[<url>|<gradient>|none] [<length-percentage>{4} <-webkit-mask-box-repeat>{2}]?", "-webkit-print-color-adjust": "economy|exact", "-webkit-text-security": "none|circle|disc|square", "-webkit-user-drag": "none|element|auto", "-webkit-user-select": "auto|none|text|all", "alignment-baseline": "auto|baseline|before-edge|text-before-edge|middle|central|after-edge|text-after-edge|ideographic|alphabetic|hanging|mathematical", "baseline-shift": "baseline|sub|super|<svg-length>", behavior: "<url>+", "clip-rule": "nonzero|evenodd", cue: "<'cue-before'> <'cue-after'>?", "cue-after": "<url> <decibel>?|none", "cue-before": "<url> <decibel>?|none", "dominant-baseline": "auto|use-script|no-change|reset-size|ideographic|alphabetic|hanging|mathematical|central|middle|text-after-edge|text-before-edge", fill: "<paint>", "fill-opacity": "<number-zero-one>", "fill-rule": "nonzero|evenodd", "glyph-orientation-horizontal": "<angle>", "glyph-orientation-vertical": "<angle>", kerning: "auto|<svg-length>", marker: "none|<url>", "marker-end": "none|<url>", "marker-mid": "none|<url>", "marker-start": "none|<url>", pause: "<'pause-before'> <'pause-after'>?", "pause-after": "<time>|none|x-weak|weak|medium|strong|x-strong", "pause-before": "<time>|none|x-weak|weak|medium|strong|x-strong", rest: "<'rest-before'> <'rest-after'>?", "rest-after": "<time>|none|x-weak|weak|medium|strong|x-strong", "rest-before": "<time>|none|x-weak|weak|medium|strong|x-strong", "shape-rendering": "auto|optimizeSpeed|crispEdges|geometricPrecision", src: "[<url> [format( <string># )]?|local( <family-name> )]#", speak: "auto|none|normal", "speak-as": "normal|spell-out||digits||[literal-punctuation|no-punctuation]", stroke: "<paint>", "stroke-dasharray": "none|[<svg-length>+]#", "stroke-dashoffset": "<svg-length>", "stroke-linecap": "butt|round|square", "stroke-linejoin": "miter|round|bevel", "stroke-miterlimit": "<number-one-or-greater>", "stroke-opacity": "<number-zero-one>", "stroke-width": "<svg-length>", "text-anchor": "start|middle|end", "unicode-range": "<urange>#", "voice-balance": "<number>|left|center|right|leftwards|rightwards", "voice-duration": "auto|<time>", "voice-family": "[[<family-name>|<generic-voice>] ,]* [<family-name>|<generic-voice>]|preserve", "voice-pitch": "<frequency>&&absolute|[[x-low|low|medium|high|x-high]||[<frequency>|<semitones>|<percentage>]]", "voice-range": "<frequency>&&absolute|[[x-low|low|medium|high|x-high]||[<frequency>|<semitones>|<percentage>]]", "voice-rate": "[normal|x-slow|slow|medium|fast|x-fast]||<percentage>", "voice-stress": "normal|strong|moderate|none|reduced", "voice-volume": "silent|[[x-soft|soft|medium|loud|x-loud]||<decibel>]" }, atrules: { charset: { prelude: "<string>", descriptors: null }, "counter-style": { prelude: "<counter-style-name>", descriptors: { "additive-symbols": "[<integer>&&<symbol>]#", fallback: "<counter-style-name>", negative: "<symbol> <symbol>?", pad: "<integer>&&<symbol>", prefix: "<symbol>", range: "[[<integer>|infinite]{2}]#|auto", "speak-as": "auto|bullets|numbers|words|spell-out|<counter-style-name>", suffix: "<symbol>", symbols: "<symbol>+", system: "cyclic|numeric|alphabetic|symbolic|additive|[fixed <integer>?]|[extends <counter-style-name>]" } }, document: { prelude: "[<url>|url-prefix( <string> )|domain( <string> )|media-document( <string> )|regexp( <string> )]#", descriptors: null }, "font-face": { prelude: null, descriptors: { "ascent-override": "normal|<percentage>", "descent-override": "normal|<percentage>", "font-display": "[auto|block|swap|fallback|optional]", "font-family": "<family-name>", "font-feature-settings": "normal|<feature-tag-value>#", "font-variation-settings": "normal|[<string> <number>]#", "font-stretch": "<font-stretch-absolute>{1,2}", "font-style": "normal|italic|oblique <angle>{0,2}", "font-weight": "<font-weight-absolute>{1,2}", "font-variant": "normal|none|[<common-lig-values>||<discretionary-lig-values>||<historical-lig-values>||<contextual-alt-values>||stylistic( <feature-value-name> )||historical-forms||styleset( <feature-value-name># )||character-variant( <feature-value-name># )||swash( <feature-value-name> )||ornaments( <feature-value-name> )||annotation( <feature-value-name> )||[small-caps|all-small-caps|petite-caps|all-petite-caps|unicase|titling-caps]||<numeric-figure-values>||<numeric-spacing-values>||<numeric-fraction-values>||ordinal||slashed-zero||<east-asian-variant-values>||<east-asian-width-values>||ruby]", "line-gap-override": "normal|<percentage>", "size-adjust": "<percentage>", src: "[<url> [format( <string># )]?|local( <family-name> )]#", "unicode-range": "<urange>#" } }, "font-feature-values": { prelude: "<family-name>#", descriptors: null }, import: { prelude: "[<string>|<url>] [layer|layer( <layer-name> )]? [supports( [<supports-condition>|<declaration>] )]? <media-query-list>?", descriptors: null }, keyframes: { prelude: "<keyframes-name>", descriptors: null }, layer: { prelude: "[<layer-name>#|<layer-name>?]", descriptors: null }, media: { prelude: "<media-query-list>", descriptors: null }, namespace: { prelude: "<namespace-prefix>? [<string>|<url>]", descriptors: null }, page: { prelude: "<page-selector-list>", descriptors: { bleed: "auto|<length>", marks: "none|[crop||cross]", size: "<length>{1,2}|auto|[<page-size>||[portrait|landscape]]" } }, property: { prelude: "<custom-property-name>", descriptors: { syntax: "<string>", inherits: "true|false", "initial-value": "<string>" } }, "scroll-timeline": { prelude: "<timeline-name>", descriptors: null }, supports: { prelude: "<supports-condition>", descriptors: null }, viewport: { prelude: null, descriptors: { height: "<viewport-length>{1,2}", "max-height": "<viewport-length>", "max-width": "<viewport-length>", "max-zoom": "auto|<number>|<percentage>", "min-height": "<viewport-length>", "min-width": "<viewport-length>", "min-zoom": "auto|<number>|<percentage>", orientation: "auto|portrait|landscape", "user-zoom": "zoom|fixed", "viewport-fit": "auto|contain|cover", width: "<viewport-length>{1,2}", zoom: "auto|<number>|<percentage>" } } } };
      bt = {};
      b(bt, { AnPlusB: () => Jr, Atrule: () => tn, AtrulePrelude: () => nn, AttributeSelector: () => sn, Block: () => cn, Brackets: () => pn, CDC: () => mn, CDO: () => dn, ClassSelector: () => bn, Combinator: () => yn, Comment: () => wn, Declaration: () => Sn, DeclarationList: () => An, Dimension: () => En, Function: () => Pn, Hash: () => Dn, IdSelector: () => Mn, Identifier: () => Nn, MediaFeature: () => Fn, MediaQuery: () => _n, MediaQueryList: () => jn, Nth: () => qn, Number: () => Yn, Operator: () => Vn, Parentheses: () => Qn, Percentage: () => $n, PseudoClassSelector: () => Jn, PseudoElementSelector: () => to, Ratio: () => no, Raw: () => io, Rule: () => so, Selector: () => co, SelectorList: () => po, String: () => bo, StyleSheet: () => yo, TypeSelector: () => vo, UnicodeRange: () => Ao, Url: () => Do, Value: () => No, WhiteSpace: () => Mo });
      Jr = {};
      b(Jr, { generate: () => gc, name: () => fc, parse: () => Zr, structure: () => dc });
      me = 43;
      re = 45;
      Zt = 110;
      Ie = true;
      mc = false;
      fc = "AnPlusB";
      dc = { a: [String, null], b: [String, null] };
      tn = {};
      b(tn, { generate: () => wc, name: () => xc, parse: () => en, structure: () => kc, walkContext: () => yc });
      xc = "Atrule";
      yc = "atrule";
      kc = { name: String, prelude: ["AtrulePrelude", "Raw", null], block: ["Block", null] };
      nn = {};
      b(nn, { generate: () => Ac, name: () => vc, parse: () => rn, structure: () => Cc, walkContext: () => Sc });
      vc = "AtrulePrelude";
      Sc = "atrulePrelude";
      Cc = { children: [[]] };
      sn = {};
      b(sn, { generate: () => Nc, name: () => Dc, parse: () => an, structure: () => Oc });
      Tc = 36;
      ba = 42;
      er = 61;
      Ec = 94;
      on = 124;
      Lc = 126;
      Dc = "AttributeSelector";
      Oc = { name: "Identifier", matcher: [String, null], value: ["String", "Identifier", null], flags: [String, null] };
      cn = {};
      b(cn, { generate: () => _c, name: () => Rc, parse: () => ln, structure: () => Bc, walkContext: () => Fc });
      Rc = "Block";
      Fc = "block";
      Bc = { children: [["Atrule", "Rule", "Declaration"]] };
      pn = {};
      b(pn, { generate: () => Hc, name: () => Uc, parse: () => un, structure: () => jc });
      Uc = "Brackets";
      jc = { children: [[]] };
      mn = {};
      b(mn, { generate: () => Yc, name: () => qc, parse: () => hn, structure: () => Wc });
      qc = "CDC";
      Wc = [];
      dn = {};
      b(dn, { generate: () => Kc, name: () => Gc, parse: () => fn, structure: () => Vc });
      Gc = "CDO";
      Vc = [];
      bn = {};
      b(bn, { generate: () => Zc, name: () => Xc, parse: () => gn, structure: () => $c });
      Qc = 46;
      Xc = "ClassSelector";
      $c = { name: String };
      yn = {};
      b(yn, { generate: () => ou, name: () => ru, parse: () => xn, structure: () => nu });
      Jc = 43;
      ka = 47;
      eu = 62;
      tu = 126;
      ru = "Combinator";
      nu = { name: String };
      wn = {};
      b(wn, { generate: () => cu, name: () => su, parse: () => kn, structure: () => lu });
      iu = 42;
      au = 47;
      su = "Comment";
      lu = { value: String };
      Sn = {};
      b(Sn, { generate: () => wu, name: () => xu, parse: () => vn, structure: () => ku, walkContext: () => yu });
      va = 33;
      uu = 35;
      pu = 36;
      hu = 38;
      mu = 42;
      fu = 43;
      wa = 47;
      xu = "Declaration";
      yu = "declaration";
      ku = { important: [Boolean, String], property: String, value: ["Value", "Raw"] };
      An = {};
      b(An, { generate: () => Eu, name: () => Au, parse: () => Cn, structure: () => Tu });
      Au = "DeclarationList";
      Tu = { children: [["Declaration"]] };
      En = {};
      b(En, { generate: () => Iu, name: () => Lu, parse: () => Tn, structure: () => Pu });
      Lu = "Dimension";
      Pu = { value: String, unit: String };
      Pn = {};
      b(Pn, { generate: () => zu, name: () => Du, parse: () => Ln, structure: () => Nu, walkContext: () => Ou });
      Du = "Function";
      Ou = "function";
      Nu = { name: String, children: [[]] };
      Dn = {};
      b(Dn, { generate: () => Bu, name: () => Ru, parse: () => In, structure: () => Fu, xxx: () => Mu });
      Mu = "XXX";
      Ru = "Hash";
      Fu = { value: String };
      Nn = {};
      b(Nn, { generate: () => ju, name: () => _u, parse: () => On, structure: () => Uu });
      _u = "Identifier";
      Uu = { name: String };
      Mn = {};
      b(Mn, { generate: () => Wu, name: () => Hu, parse: () => zn, structure: () => qu });
      Hu = "IdSelector";
      qu = { name: String };
      Fn = {};
      b(Fn, { generate: () => Vu, name: () => Yu, parse: () => Rn, structure: () => Gu });
      Yu = "MediaFeature";
      Gu = { name: String, value: ["Identifier", "Number", "Dimension", "Ratio", null] };
      _n = {};
      b(_n, { generate: () => Xu, name: () => Ku, parse: () => Bn, structure: () => Qu });
      Ku = "MediaQuery";
      Qu = { children: [["Identifier", "MediaFeature", "WhiteSpace"]] };
      jn = {};
      b(jn, { generate: () => Ju, name: () => $u, parse: () => Un, structure: () => Zu });
      $u = "MediaQueryList";
      Zu = { children: [["MediaQuery"]] };
      qn = {};
      b(qn, { generate: () => rp, name: () => ep, parse: () => Hn, structure: () => tp });
      ep = "Nth";
      tp = { nth: ["AnPlusB", "Identifier"], selector: ["SelectorList", null] };
      Yn = {};
      b(Yn, { generate: () => ip, name: () => np, parse: () => Wn, structure: () => op });
      np = "Number";
      op = { value: String };
      Vn = {};
      b(Vn, { generate: () => lp, name: () => ap, parse: () => Gn, structure: () => sp });
      ap = "Operator";
      sp = { value: String };
      Qn = {};
      b(Qn, { generate: () => pp, name: () => cp, parse: () => Kn, structure: () => up });
      cp = "Parentheses";
      up = { children: [[]] };
      $n = {};
      b($n, { generate: () => fp, name: () => hp, parse: () => Xn, structure: () => mp });
      hp = "Percentage";
      mp = { value: String };
      Jn = {};
      b(Jn, { generate: () => xp, name: () => dp, parse: () => Zn, structure: () => bp, walkContext: () => gp });
      dp = "PseudoClassSelector";
      gp = "function";
      bp = { name: String, children: [["Raw"], null] };
      to = {};
      b(to, { generate: () => vp, name: () => yp, parse: () => eo, structure: () => wp, walkContext: () => kp });
      yp = "PseudoElementSelector";
      kp = "function";
      wp = { name: String, children: [["Raw"], null] };
      no = {};
      b(no, { generate: () => Ep, name: () => Ap, parse: () => ro, structure: () => Tp });
      Sp = 47;
      Cp = 46;
      Ap = "Ratio";
      Tp = { left: String, right: String };
      io = {};
      b(io, { generate: () => Dp, name: () => Pp, parse: () => oo, structure: () => Ip });
      Pp = "Raw";
      Ip = { value: String };
      so = {};
      b(so, { generate: () => Rp, name: () => Np, parse: () => ao, structure: () => Mp, walkContext: () => zp });
      Np = "Rule";
      zp = "rule";
      Mp = { prelude: ["SelectorList", "Raw"], block: ["Block"] };
      co = {};
      b(co, { generate: () => _p, name: () => Fp, parse: () => lo, structure: () => Bp });
      Fp = "Selector";
      Bp = { children: [["TypeSelector", "IdSelector", "ClassSelector", "AttributeSelector", "PseudoClassSelector", "PseudoElementSelector", "Combinator", "WhiteSpace"]] };
      po = {};
      b(po, { generate: () => qp, name: () => Up, parse: () => uo, structure: () => Hp, walkContext: () => jp });
      Up = "SelectorList";
      jp = "selector";
      Hp = { children: [["Selector", "Raw"]] };
      bo = {};
      b(bo, { generate: () => Gp, name: () => Wp, parse: () => go, structure: () => Yp });
      fo = {};
      b(fo, { decode: () => dt, encode: () => mo });
      ho = 92;
      Aa = 34;
      Ta = 39;
      Wp = "String";
      Yp = { value: String };
      yo = {};
      b(yo, { generate: () => $p, name: () => Kp, parse: () => xo, structure: () => Xp, walkContext: () => Qp });
      Vp = 33;
      Kp = "StyleSheet";
      Qp = "stylesheet";
      Xp = { children: [["Comment", "CDO", "CDC", "Atrule", "Rule", "Raw"]] };
      vo = {};
      b(vo, { generate: () => th, name: () => Jp, parse: () => wo, structure: () => eh });
      Zp = 42;
      La = 124;
      Jp = "TypeSelector";
      eh = { name: String };
      Ao = {};
      b(Ao, { generate: () => ah, name: () => oh, parse: () => Co, structure: () => ih });
      Pa = 43;
      Ia = 45;
      So = 63;
      oh = "UnicodeRange";
      ih = { value: String };
      Do = {};
      b(Do, { generate: () => mh, name: () => ph, parse: () => Io, structure: () => hh });
      Po = {};
      b(Po, { decode: () => Eo, encode: () => Lo });
      sh = 32;
      To = 92;
      lh = 34;
      ch = 39;
      uh = 40;
      Da = 41;
      ph = "Url";
      hh = { value: String };
      No = {};
      b(No, { generate: () => gh, name: () => fh, parse: () => Oo, structure: () => dh });
      fh = "Value";
      dh = { children: [[]] };
      Mo = {};
      b(Mo, { generate: () => kh, name: () => xh, parse: () => zo, structure: () => yh });
      bh = Object.freeze({ type: "WhiteSpace", loc: null, value: " " });
      xh = "WhiteSpace";
      yh = { value: String };
      Oa = { generic: true, ...da, node: bt };
      Ro = {};
      b(Ro, { AtrulePrelude: () => za, Selector: () => Ra, Value: () => Ua });
      wh = 35;
      vh = 42;
      Na = 43;
      Sh = 45;
      Ch = 47;
      Ah = 117;
      za = { getNode: xt };
      Th = 35;
      Eh = 42;
      Lh = 43;
      Ph = 47;
      Ma = 46;
      Ih = 62;
      Dh = 124;
      Oh = 126;
      Ra = { onWhiteSpace: Nh, getNode: zh };
      Ua = { getNode: xt, onWhiteSpace(e, t) {
        _a(e) && (e.value = ` ${e.value}`), _a(t.last) && (t.last.value += " ");
      }, expression: Fa, var: Ba };
      ja = { parse: { prelude: null, block() {
        return this.Block(true);
      } } };
      Ha = { parse: { prelude() {
        const e = this.createList();
        switch (this.skipSC(), this.tokenType) {
          case 5:
            e.push(this.String());
            break;
          case 7:
          case 2:
            e.push(this.Url());
            break;
          default:
            this.error("String or url() is expected");
        }
        return (this.lookupNonWSType(0) === 1 || this.lookupNonWSType(0) === 21) && e.push(this.MediaQueryList()), e;
      }, block: null } };
      qa = { parse: { prelude() {
        return this.createSingleNodeList(this.MediaQueryList());
      }, block() {
        return this.Block(false);
      } } };
      Wa = { parse: { prelude() {
        return this.createSingleNodeList(this.SelectorList());
      }, block() {
        return this.Block(true);
      } } };
      Ga = { parse: { prelude() {
        const e = Ya.call(this);
        return this.getFirstListNode(e) === null && this.error("Condition is expected"), e;
      }, block() {
        return this.Block(false);
      } } };
      Va = { "font-face": ja, import: Ha, media: qa, page: Wa, supports: Ga };
      De = { parse() {
        return this.createSingleNodeList(this.SelectorList());
      } };
      Fh = { parse() {
        return this.createSingleNodeList(this.Selector());
      } };
      Ka = { parse() {
        return this.createSingleNodeList(this.Identifier());
      } };
      rr = { parse() {
        return this.createSingleNodeList(this.Nth());
      } };
      Qa = { dir: Ka, has: De, lang: Ka, matches: De, is: De, "-moz-any": De, "-webkit-any": De, where: De, not: De, "nth-child": rr, "nth-last-child": rr, "nth-last-of-type": rr, "nth-of-type": rr, slotted: Fh };
      Fo = {};
      b(Fo, { AnPlusB: () => Zr, Atrule: () => en, AtrulePrelude: () => rn, AttributeSelector: () => an, Block: () => ln, Brackets: () => un, CDC: () => hn, CDO: () => fn, ClassSelector: () => gn, Combinator: () => xn, Comment: () => kn, Declaration: () => vn, DeclarationList: () => Cn, Dimension: () => Tn, Function: () => Ln, Hash: () => In, IdSelector: () => zn, Identifier: () => On, MediaFeature: () => Rn, MediaQuery: () => Bn, MediaQueryList: () => Un, Nth: () => Hn, Number: () => Wn, Operator: () => Gn, Parentheses: () => Kn, Percentage: () => Xn, PseudoClassSelector: () => Zn, PseudoElementSelector: () => eo, Ratio: () => ro, Raw: () => oo, Rule: () => ao, Selector: () => lo, SelectorList: () => uo, String: () => go, StyleSheet: () => xo, TypeSelector: () => wo, UnicodeRange: () => Co, Url: () => Io, Value: () => Oo, WhiteSpace: () => zo });
      Xa = { parseContext: { default: "StyleSheet", stylesheet: "StyleSheet", atrule: "Atrule", atrulePrelude(e) {
        return this.AtrulePrelude(e.atrule ? String(e.atrule) : null);
      }, mediaQueryList: "MediaQueryList", mediaQuery: "MediaQuery", rule: "Rule", selectorList: "SelectorList", selector: "Selector", block() {
        return this.Block(true);
      }, declarationList: "DeclarationList", declaration: "Declaration", value: "Value" }, scope: Ro, atrule: Va, pseudo: Qa, node: Fo };
      $a = { node: bt };
      Za = Xr({ ...Oa, ...Xa, ...$a });
      es = {};
      b(es, { decode: () => Bh, encode: () => _h });
      Ja = 92;
      ({ tokenize: ob, parse: ib, generate: ab, lexer: sb, createLexer: lb, walk: cb, find: ub, findLast: pb, findAll: hb, toPlainObject: mb, fromPlainObject: fb, fork: db } = Za);
    }
  });

  // src/js/arglist-parser.ts
  var ArglistParser;
  var init_arglist_parser = __esm({
    "src/js/arglist-parser.ts"() {
      ArglistParser = class {
        constructor(separatorChar = ",", mustQuote = false) {
          this.separatorChar = this.actualSeparatorChar = separatorChar;
          this.separatorCode = this.actualSeparatorCode = separatorChar.charCodeAt(0);
          this.mustQuote = mustQuote;
          this.quoteBeg = 0;
          this.quoteEnd = 0;
          this.argBeg = 0;
          this.argEnd = 0;
          this.separatorBeg = 0;
          this.separatorEnd = 0;
          this.transform = false;
          this.failed = false;
          this.reWhitespaceStart = /^\s+/;
          this.reWhitespaceEnd = /(?:^|\S)(\s+)$/;
          this.reOddTrailingEscape = /(?:^|[^\\])(?:\\\\)*\\$/;
          this.reTrailingEscapeChars = /\\+$/;
        }
        nextArg(pattern, beg = 0) {
          const len = pattern.length;
          this.quoteBeg = beg + this.leftWhitespaceCount(pattern.slice(beg));
          this.failed = false;
          const qc2 = pattern.charCodeAt(this.quoteBeg);
          if (qc2 === 34 || qc2 === 39 || qc2 === 96) {
            this.indexOfNextArgSeparator(pattern, qc2);
            if (this.argEnd !== len) {
              this.quoteEnd = this.argEnd + 1;
              this.separatorBeg = this.separatorEnd = this.quoteEnd;
              this.separatorEnd += this.leftWhitespaceCount(pattern.slice(this.quoteEnd));
              if (this.separatorEnd === len) {
                return this;
              }
              if (pattern.charCodeAt(this.separatorEnd) === this.separatorCode) {
                this.separatorEnd += 1;
                return this;
              }
            }
          }
          this.indexOfNextArgSeparator(pattern, this.separatorCode);
          this.separatorBeg = this.separatorEnd = this.argEnd;
          if (this.separatorBeg < len) {
            this.separatorEnd += 1;
          }
          this.argEnd -= this.rightWhitespaceCount(pattern.slice(0, this.separatorBeg));
          this.quoteEnd = this.argEnd;
          if (this.mustQuote) {
            this.failed = true;
          }
          return this;
        }
        normalizeArg(s, char = "") {
          if (char === "") {
            char = this.actualSeparatorChar;
          }
          let out = "";
          let pos = 0;
          while ((pos = s.lastIndexOf(char)) !== -1) {
            out = s.slice(pos) + out;
            s = s.slice(0, pos);
            const match = this.reTrailingEscapeChars.exec(s);
            if (match === null) {
              continue;
            }
            const tail = (match[0].length & 1) !== 0 ? match[0].slice(0, -1) : match[0];
            out = tail + out;
            s = s.slice(0, -match[0].length);
          }
          if (out === "") {
            return s;
          }
          return s + out;
        }
        leftWhitespaceCount(s) {
          const match = this.reWhitespaceStart.exec(s);
          return match === null ? 0 : match[0].length;
        }
        rightWhitespaceCount(s) {
          const match = this.reWhitespaceEnd.exec(s);
          return match === null ? 0 : match[1].length;
        }
        indexOfNextArgSeparator(pattern, separatorCode) {
          this.argBeg = this.argEnd = separatorCode !== this.separatorCode ? this.quoteBeg + 1 : this.quoteBeg;
          this.transform = false;
          if (separatorCode !== this.actualSeparatorCode) {
            this.actualSeparatorCode = separatorCode;
            this.actualSeparatorChar = String.fromCharCode(separatorCode);
          }
          while (this.argEnd < pattern.length) {
            const pos = pattern.indexOf(this.actualSeparatorChar, this.argEnd);
            if (pos === -1) {
              return this.argEnd = pattern.length;
            }
            if (this.reOddTrailingEscape.test(pattern.slice(0, pos)) === false) {
              return this.argEnd = pos;
            }
            this.transform = true;
            this.argEnd = pos + 1;
          }
        }
      };
    }
  });

  // src/js/jsonpath.ts
  var _UNDEFINED, _ROOT, _CURRENT, _CHILDREN, _DESCENDANTS, _reUnquotedIdentifier, _reExpr, _reIndice, _root, _compiled, _JSONPath_instances, compile_fn, evaluate_fn, getMatches_fn, getMatchesFromAll_fn, getMatchesFromKeys_fn, getMatchesFromExpr_fn, normalizeKey_fn, getDescendants_fn, consumeIdentifier_fn, consumeUnquotedIdentifier_fn, untilChar_fn, compileExpr_fn, resolvePath_fn, evaluateExpr_fn, modifyVal_fn, _JSONPath, JSONPath;
  var init_jsonpath = __esm({
    "src/js/jsonpath.ts"() {
      _JSONPath = class _JSONPath {
        constructor() {
          __privateAdd(this, _JSONPath_instances);
          __privateAdd(this, _UNDEFINED, 0);
          __privateAdd(this, _ROOT, 1);
          __privateAdd(this, _CURRENT, 2);
          __privateAdd(this, _CHILDREN, 3);
          __privateAdd(this, _DESCENDANTS, 4);
          __privateAdd(this, _reUnquotedIdentifier, /^[A-Za-z_][\w]*|^\*/);
          __privateAdd(this, _reExpr, /^([!=^$*]=|[<>]=?)(.+?)\]/);
          __privateAdd(this, _reIndice, /^-?\d+/);
          __privateAdd(this, _root);
          __privateAdd(this, _compiled);
        }
        static create(query) {
          const jsonp = new _JSONPath();
          jsonp.compile(query);
          return jsonp;
        }
        static toJSON(obj, stringifier, ...args) {
          return (stringifier || JSON.stringify)(obj, ...args).replace(/\//g, "\\/");
        }
        get value() {
          return __privateGet(this, _compiled) && __privateGet(this, _compiled).rval;
        }
        set value(v2) {
          if (__privateGet(this, _compiled) === void 0) {
            return;
          }
          __privateGet(this, _compiled).rval = v2;
        }
        get valid() {
          return __privateGet(this, _compiled) !== void 0;
        }
        compile(query) {
          __privateSet(this, _compiled, void 0);
          const r = __privateMethod(this, _JSONPath_instances, compile_fn).call(this, query, 0);
          if (r === void 0) {
            return;
          }
          if (r.i !== query.length) {
            let val;
            if (query.startsWith("=", r.i)) {
              if (/^=repl\(.+\)$/.test(query.slice(r.i))) {
                r.modify = "repl";
                val = query.slice(r.i + 6, -1);
              } else {
                val = query.slice(r.i + 1);
              }
            } else if (query.startsWith("+=", r.i)) {
              r.modify = "+";
              val = query.slice(r.i + 2);
            }
            try {
              r.rval = JSON.parse(val);
            } catch (e) {
              console.warn("[uBR] jsonpath: JSON.parse val failed", e);
              return;
            }
          }
          __privateSet(this, _compiled, r);
        }
        evaluate(root) {
          if (this.valid === false) {
            return [];
          }
          __privateSet(this, _root, root);
          const paths = __privateMethod(this, _JSONPath_instances, evaluate_fn).call(this, __privateGet(this, _compiled).steps, []);
          __privateSet(this, _root, null);
          return paths;
        }
        apply(root) {
          if (this.valid === false) {
            return;
          }
          const { rval } = __privateGet(this, _compiled);
          __privateSet(this, _root, { "$": root });
          const paths = __privateMethod(this, _JSONPath_instances, evaluate_fn).call(this, __privateGet(this, _compiled).steps, []);
          let i = paths.length;
          if (i === 0) {
            __privateSet(this, _root, null);
            return;
          }
          while (i--) {
            const { obj, key } = __privateMethod(this, _JSONPath_instances, resolvePath_fn).call(this, paths[i]);
            if (rval !== void 0) {
              __privateMethod(this, _JSONPath_instances, modifyVal_fn).call(this, obj, key);
            } else if (Array.isArray(obj) && typeof key === "number") {
              obj.splice(key, 1);
            } else {
              delete obj[key];
            }
          }
          const result = __privateGet(this, _root)["$"] ?? null;
          __privateSet(this, _root, null);
          return result;
        }
        dump() {
          return JSON.stringify(__privateGet(this, _compiled));
        }
        toJSON(obj, ...args) {
          return _JSONPath.toJSON(obj, null, ...args);
        }
        get [Symbol.toStringTag]() {
          return "JSONPath";
        }
      };
      _UNDEFINED = new WeakMap();
      _ROOT = new WeakMap();
      _CURRENT = new WeakMap();
      _CHILDREN = new WeakMap();
      _DESCENDANTS = new WeakMap();
      _reUnquotedIdentifier = new WeakMap();
      _reExpr = new WeakMap();
      _reIndice = new WeakMap();
      _root = new WeakMap();
      _compiled = new WeakMap();
      _JSONPath_instances = new WeakSet();
      compile_fn = function(query, i) {
        if (query.length === 0) {
          return;
        }
        const steps = [];
        let c = query.charCodeAt(i);
        if (c === 36) {
          steps.push({ mv: __privateGet(this, _ROOT) });
          i += 1;
        } else if (c === 64) {
          steps.push({ mv: __privateGet(this, _CURRENT) });
          i += 1;
        } else {
          steps.push({ mv: i === 0 ? __privateGet(this, _ROOT) : __privateGet(this, _CURRENT) });
        }
        let mv = __privateGet(this, _UNDEFINED);
        for (; ; ) {
          if (i === query.length) {
            break;
          }
          c = query.charCodeAt(i);
          if (c === 32) {
            i += 1;
            continue;
          }
          if (c === 46) {
            if (mv !== __privateGet(this, _UNDEFINED)) {
              return;
            }
            if (query.startsWith("..", i)) {
              mv = __privateGet(this, _DESCENDANTS);
              i += 2;
            } else {
              mv = __privateGet(this, _CHILDREN);
              i += 1;
            }
            continue;
          }
          if (c !== 91) {
            if (mv === __privateGet(this, _UNDEFINED)) {
              const step = steps.at(-1);
              if (step === void 0) {
                return;
              }
              i = __privateMethod(this, _JSONPath_instances, compileExpr_fn).call(this, query, step, i);
              break;
            }
            const s = __privateMethod(this, _JSONPath_instances, consumeUnquotedIdentifier_fn).call(this, query, i);
            if (s === void 0) {
              return;
            }
            steps.push({ mv, k: s });
            i += s.length;
            mv = __privateGet(this, _UNDEFINED);
            continue;
          }
          if (query.startsWith("[?", i)) {
            const not = query.charCodeAt(i + 2) === 33;
            const j2 = i + 2 + (not ? 1 : 0);
            const r2 = __privateMethod(this, _JSONPath_instances, compile_fn).call(this, query, j2);
            if (r2 === void 0) {
              return;
            }
            if (query.startsWith("]", r2.i) === false) {
              return;
            }
            if (not) {
              r2.steps.at(-1).not = true;
            }
            steps.push({ mv: mv || __privateGet(this, _CHILDREN), steps: r2.steps });
            i = r2.i + 1;
            mv = __privateGet(this, _UNDEFINED);
            continue;
          }
          if (query.startsWith("[*]", i)) {
            mv || (mv = __privateGet(this, _CHILDREN));
            steps.push({ mv, k: "*" });
            i += 3;
            mv = __privateGet(this, _UNDEFINED);
            continue;
          }
          const r = __privateMethod(this, _JSONPath_instances, consumeIdentifier_fn).call(this, query, i + 1);
          if (r === void 0) {
            return;
          }
          mv || (mv = __privateGet(this, _CHILDREN));
          steps.push({ mv, k: r.s });
          i = r.i + 1;
          mv = __privateGet(this, _UNDEFINED);
        }
        if (steps.length === 0) {
          return;
        }
        if (mv !== __privateGet(this, _UNDEFINED)) {
          return;
        }
        return { steps, i };
      };
      evaluate_fn = function(steps, pathin) {
        let resultset = [];
        if (Array.isArray(steps) === false) {
          return resultset;
        }
        for (const step of steps) {
          switch (step.mv) {
            case __privateGet(this, _ROOT):
              resultset = [["$"]];
              break;
            case __privateGet(this, _CURRENT):
              resultset = [pathin];
              break;
            case __privateGet(this, _CHILDREN):
            case __privateGet(this, _DESCENDANTS):
              resultset = __privateMethod(this, _JSONPath_instances, getMatches_fn).call(this, resultset, step);
              break;
            default:
              break;
          }
        }
        return resultset;
      };
      getMatches_fn = function(listin, step) {
        const listout = [];
        for (const pathin of listin) {
          const { value: owner } = __privateMethod(this, _JSONPath_instances, resolvePath_fn).call(this, pathin);
          if (step.k === "*") {
            __privateMethod(this, _JSONPath_instances, getMatchesFromAll_fn).call(this, pathin, step, owner, listout);
          } else if (step.k !== void 0) {
            __privateMethod(this, _JSONPath_instances, getMatchesFromKeys_fn).call(this, pathin, step, owner, listout);
          } else if (step.steps) {
            __privateMethod(this, _JSONPath_instances, getMatchesFromExpr_fn).call(this, pathin, step, owner, listout);
          }
        }
        return listout;
      };
      getMatchesFromAll_fn = function(pathin, step, owner, out) {
        const recursive = step.mv === __privateGet(this, _DESCENDANTS);
        for (const { path } of __privateMethod(this, _JSONPath_instances, getDescendants_fn).call(this, owner, recursive)) {
          out.push([...pathin, ...path]);
        }
      };
      getMatchesFromKeys_fn = function(pathin, step, owner, out) {
        const kk = Array.isArray(step.k) ? step.k : [step.k];
        for (const k2 of kk) {
          const normalized = __privateMethod(this, _JSONPath_instances, evaluateExpr_fn).call(this, step, owner, k2);
          if (normalized === void 0) {
            continue;
          }
          out.push([...pathin, normalized]);
        }
        if (step.mv !== __privateGet(this, _DESCENDANTS)) {
          return;
        }
        for (const { obj, key, path } of __privateMethod(this, _JSONPath_instances, getDescendants_fn).call(this, owner, true)) {
          for (const k2 of kk) {
            const normalized = __privateMethod(this, _JSONPath_instances, evaluateExpr_fn).call(this, step, obj[key], k2);
            if (normalized === void 0) {
              continue;
            }
            out.push([...pathin, ...path, normalized]);
          }
        }
      };
      getMatchesFromExpr_fn = function(pathin, step, owner, out) {
        const recursive = step.mv === __privateGet(this, _DESCENDANTS);
        if (Array.isArray(owner) === false) {
          const r = __privateMethod(this, _JSONPath_instances, evaluate_fn).call(this, step.steps, pathin);
          if (r.length !== 0) {
            out.push(pathin);
          }
          if (recursive !== true) {
            return;
          }
        }
        for (const { obj, key, path } of __privateMethod(this, _JSONPath_instances, getDescendants_fn).call(this, owner, recursive)) {
          if (Array.isArray(obj[key])) {
            continue;
          }
          const q2 = [...pathin, ...path];
          const r = __privateMethod(this, _JSONPath_instances, evaluate_fn).call(this, step.steps, q2);
          if (r.length === 0) {
            continue;
          }
          out.push(q2);
        }
      };
      normalizeKey_fn = function(owner, key) {
        if (typeof key === "number") {
          if (Array.isArray(owner)) {
            return key >= 0 ? key : owner.length + key;
          }
        }
        return key;
      };
      getDescendants_fn = function(v2, recursive) {
        const iterator = {
          next() {
            const n = this.stack.length;
            if (n === 0) {
              this.value = void 0;
              this.done = true;
              return this;
            }
            const details = this.stack[n - 1];
            const entry = details.keys.next();
            if (entry.done) {
              this.stack.pop();
              this.path.pop();
              return this.next();
            }
            this.path[n - 1] = entry.value;
            this.value = {
              obj: details.obj,
              key: entry.value,
              path: this.path.slice()
            };
            const v3 = this.value.obj[this.value.key];
            if (recursive) {
              if (Array.isArray(v3)) {
                this.stack.push({ obj: v3, keys: v3.keys() });
              } else if (typeof v3 === "object" && v3 !== null) {
                this.stack.push({ obj: v3, keys: Object.keys(v3).values() });
              }
            }
            return this;
          },
          path: [],
          value: void 0,
          done: false,
          stack: [],
          [Symbol.iterator]() {
            return this;
          }
        };
        if (Array.isArray(v2)) {
          iterator.stack.push({ obj: v2, keys: v2.keys() });
        } else if (typeof v2 === "object" && v2 !== null) {
          iterator.stack.push({ obj: v2, keys: Object.keys(v2).values() });
        }
        return iterator;
      };
      consumeIdentifier_fn = function(query, i) {
        const keys = [];
        for (; ; ) {
          const c0 = query.charCodeAt(i);
          if (c0 === 93) {
            break;
          }
          if (c0 === 44) {
            i += 1;
            continue;
          }
          if (c0 === 39) {
            const r = __privateMethod(this, _JSONPath_instances, untilChar_fn).call(this, query, 39, i + 1);
            if (r === void 0) {
              return;
            }
            keys.push(r.s);
            i = r.i;
            continue;
          }
          if (c0 === 45 || c0 >= 48 && c0 <= 57) {
            const match = __privateGet(this, _reIndice).exec(query.slice(i));
            if (match === null) {
              return;
            }
            const indice = parseInt(query.slice(i), 10);
            keys.push(indice);
            i += match[0].length;
            continue;
          }
          const s = __privateMethod(this, _JSONPath_instances, consumeUnquotedIdentifier_fn).call(this, query, i);
          if (s === void 0) {
            return;
          }
          keys.push(s);
          i += s.length;
        }
        return { s: keys.length === 1 ? keys[0] : keys, i };
      };
      consumeUnquotedIdentifier_fn = function(query, i) {
        const match = __privateGet(this, _reUnquotedIdentifier).exec(query.slice(i));
        if (match === null) {
          return;
        }
        return match[0];
      };
      untilChar_fn = function(query, targetCharCode, i) {
        const len = query.length;
        const parts = [];
        let beg = i, end = i;
        for (; ; ) {
          if (end === len) {
            return;
          }
          const c = query.charCodeAt(end);
          if (c === targetCharCode) {
            parts.push(query.slice(beg, end));
            end += 1;
            break;
          }
          if (c === 92 && end + 1 < len) {
            const d2 = query.charCodeAt(end + 1);
            if (d2 === targetCharCode) {
              parts.push(query.slice(beg, end));
              end += 1;
              beg = end;
            }
          }
          end += 1;
        }
        return { s: parts.join(""), i: end };
      };
      compileExpr_fn = function(query, step, i) {
        if (query.startsWith("=/", i)) {
          const r = __privateMethod(this, _JSONPath_instances, untilChar_fn).call(this, query, 47, i + 2);
          if (r === void 0) {
            return i;
          }
          const match2 = /^[i]/.exec(query.slice(r.i));
          try {
            step.rval = new RegExp(r.s, match2 && match2[0] || void 0);
          } catch (e) {
            console.warn("[uBR] jsonpath: RegExp compile failed", e);
            return i;
          }
          step.op = "re";
          if (match2) {
            r.i += match2[0].length;
          }
          return r.i;
        }
        const match = __privateGet(this, _reExpr).exec(query.slice(i));
        if (match === null) {
          return i;
        }
        try {
          step.rval = JSON.parse(match[2]);
          step.op = match[1];
        } catch (e) {
          console.warn("[uBR] jsonpath: JSON.parse expr failed", e);
        }
        return i + match[1].length + match[2].length;
      };
      resolvePath_fn = function(path) {
        if (path.length === 0) {
          return { value: __privateGet(this, _root) };
        }
        const key = path.at(-1);
        let obj = __privateGet(this, _root);
        for (let i = 0, n = path.length - 1; i < n; i++) {
          obj = obj[path[i]];
        }
        return { obj, key, value: obj[key] };
      };
      evaluateExpr_fn = function(step, owner, key) {
        if (owner === void 0 || owner === null) {
          return;
        }
        if (typeof key === "number") {
          if (Array.isArray(owner) === false) {
            return;
          }
        }
        const k2 = __privateMethod(this, _JSONPath_instances, normalizeKey_fn).call(this, owner, key);
        const hasOwn = Object.hasOwn(owner, k2);
        if (step.op !== void 0 && hasOwn === false) {
          return;
        }
        const target = step.not !== true;
        const v2 = owner[k2];
        let outcome = false;
        switch (step.op) {
          case "==":
            outcome = v2 === step.rval === target;
            break;
          case "!=":
            outcome = v2 !== step.rval === target;
            break;
          case "<":
            outcome = v2 < step.rval === target;
            break;
          case "<=":
            outcome = v2 <= step.rval === target;
            break;
          case ">":
            outcome = v2 > step.rval === target;
            break;
          case ">=":
            outcome = v2 >= step.rval === target;
            break;
          case "^=":
            outcome = `${v2}`.startsWith(step.rval) === target;
            break;
          case "$=":
            outcome = `${v2}`.endsWith(step.rval) === target;
            break;
          case "*=":
            outcome = `${v2}`.includes(step.rval) === target;
            break;
          case "re":
            outcome = step.rval.test(`${v2}`);
            break;
          default:
            outcome = hasOwn === target;
            break;
        }
        if (outcome) {
          return k2;
        }
      };
      modifyVal_fn = function(obj, key) {
        let { modify, rval } = __privateGet(this, _compiled);
        if (typeof rval === "string") {
          rval = rval.replace("${now}", `${Date.now()}`);
        }
        switch (modify) {
          case void 0:
            obj[key] = rval;
            break;
          case "+": {
            if (rval instanceof Object === false) {
              return;
            }
            const lval = obj[key];
            if (lval instanceof Object === false) {
              return;
            }
            if (Array.isArray(lval)) {
              return;
            }
            for (const [k2, v2] of Object.entries(rval)) {
              lval[k2] = v2;
            }
            break;
          }
          case "repl": {
            const lval = obj[key];
            if (typeof lval !== "string") {
              return;
            }
            if (__privateGet(this, _compiled).re === void 0) {
              __privateGet(this, _compiled).re = null;
              try {
                __privateGet(this, _compiled).re = rval.regex !== void 0 ? new RegExp(rval.regex, rval.flags) : new RegExp(rval.pattern.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"));
              } catch (e) {
                console.warn("[uBR] jsonpath: repl RegExp failed", e);
              }
            }
            if (__privateGet(this, _compiled).re === null) {
              return;
            }
            obj[key] = lval.replace(__privateGet(this, _compiled).re, rval.replacement);
            break;
          }
          default:
            break;
        }
      };
      JSONPath = _JSONPath;
    }
  });

  // src/js/static-filtering-parser.ts
  function parseRedirectValue(arg) {
    let token = arg.trim();
    let priority = 0;
    const asDataURI = token.charCodeAt(0) === 37;
    if (asDataURI) {
      token = token.slice(1);
    }
    const match = /:-?\d+$/.exec(token);
    if (match !== null) {
      priority = parseInt(token.slice(match.index + 1), 10);
      token = token.slice(0, match.index);
    }
    return { token, priority, asDataURI };
  }
  function parseQueryPruneValue(arg) {
    let s = arg.trim();
    if (s === "") {
      return { all: true };
    }
    const out = {};
    out.not = s.charCodeAt(0) === 126;
    if (out.not) {
      s = s.slice(1);
    }
    const match = /^\/(.+)\/(i)?$/.exec(s);
    if (match !== null) {
      try {
        out.re = new RegExp(match[1], match[2] || "");
      } catch (e) {
        console.warn("[uBR] static-filtering-parser: RegExp pattern compilation failed", e);
        out.bad = true;
      }
      return out;
    }
    if (s.startsWith("|")) {
      try {
        out.re = new RegExp(`^${s.slice(1)}`, "i");
      } catch (e) {
        console.warn("[uBR] static-filtering-parser: RegExp legacy pattern compilation failed", e);
        out.bad = true;
      }
      return out;
    }
    if (s.includes("|")) {
      out.bad = true;
      return out;
    }
    out.name = s;
    return out;
  }
  function parseHeaderValue(arg) {
    let s = arg.trim();
    const out = {};
    let pos = s.indexOf(":");
    if (pos === -1) {
      pos = s.length;
    }
    out.name = s.slice(0, pos).toLowerCase();
    out.bad = out.name === "";
    s = s.slice(pos + 1);
    out.not = s.charCodeAt(0) === 126;
    if (out.not) {
      s = s.slice(1);
    }
    out.value = s;
    if (s === "") {
      return out;
    }
    const match = /^\/(.+)\/(i)?$/.exec(s);
    out.isRegex = match !== null;
    if (out.isRegex) {
      out.reStr = match[1];
      out.reFlags = match[2] || "";
      try {
        new RegExp(out.reStr, out.reFlags);
      } catch {
        out.bad = true;
      }
      return out;
    }
    out.reFlags = "i";
    if (/[*?]/.test(s) === false) {
      out.reStr = escapeForRegex(s);
      return out;
    }
    const reConstruct = /(?<!\\)[*?]/g;
    const reParts = [];
    let beg = 0;
    for (; ; ) {
      const match2 = reConstruct.exec(s);
      if (match2 === null) {
        break;
      }
      reParts.push(
        escapeForRegex(s.slice(beg, match2.index)),
        match2[0] === "*" ? ".*" : ".?"
      );
      beg = reConstruct.lastIndex;
    }
    reParts.push(escapeForRegex(s.slice(beg)));
    out.reStr = reParts.join("");
    return out;
  }
  function parseReplaceByRegexValue(s) {
    if (s.charCodeAt(0) !== 47) {
      return;
    }
    const parser = new ArglistParser("/");
    parser.nextArg(s, 1);
    let pattern = s.slice(parser.argBeg, parser.argEnd);
    if (parser.transform) {
      pattern = parser.normalizeArg(pattern);
    }
    if (pattern !== "") {
      pattern = parser.normalizeArg(pattern, "$");
      pattern = parser.normalizeArg(pattern, ",");
    }
    parser.nextArg(s, parser.separatorEnd);
    let replacement = s.slice(parser.argBeg, parser.argEnd);
    if (parser.separatorEnd === parser.separatorBeg) {
      return;
    }
    if (parser.transform) {
      replacement = parser.normalizeArg(replacement);
    }
    replacement = parser.normalizeArg(replacement, "$");
    replacement = parser.normalizeArg(replacement, ",");
    const flags = s.slice(parser.separatorEnd);
    if (pattern === "") {
      return { flags, replacement };
    }
    try {
      return { re: new RegExp(pattern, flags), replacement };
    } catch (e) {
      console.warn("[uBR] static-filtering-parser: RegExp failed", e);
    }
  }
  function parseReplaceValue(s) {
    if (s.startsWith("/")) {
      const r = parseReplaceByRegexValue(s);
      if (r) {
        if (r.re === void 0) {
          return;
        }
        r.type = "text";
      }
      return r;
    }
    const pos = s.indexOf(":");
    if (pos === -1) {
      return;
    }
    const type = s.slice(0, pos);
    if (type === "json" || type === "jsonl") {
      const query = s.slice(pos + 1);
      const jsonp = JSONPath.create(query);
      if (jsonp.valid === false) {
        return;
      }
      return { type, jsonp };
    }
  }
  var iota2, AST_TYPE_NONE, AST_TYPE_UNKNOWN, AST_TYPE_COMMENT, AST_TYPE_NETWORK, AST_TYPE_EXTENDED, AST_TYPE_NETWORK_PATTERN_ANY, AST_TYPE_NETWORK_PATTERN_HOSTNAME, AST_TYPE_NETWORK_PATTERN_PLAIN, AST_TYPE_NETWORK_PATTERN_REGEX, AST_TYPE_NETWORK_PATTERN_GENERIC, AST_TYPE_NETWORK_PATTERN_BAD, AST_TYPE_EXTENDED_COSMETIC, AST_TYPE_EXTENDED_SCRIPTLET, AST_TYPE_EXTENDED_HTML, AST_TYPE_EXTENDED_RESPONSEHEADER, AST_TYPE_COMMENT_PREPARSER, AST_FLAG_UNSUPPORTED, AST_FLAG_IGNORE, AST_FLAG_HAS_ERROR, AST_FLAG_IS_EXCEPTION, AST_FLAG_EXT_STRONG, AST_FLAG_EXT_STYLE, AST_FLAG_EXT_SCRIPTLET_ADG, AST_FLAG_NET_PATTERN_LEFT_HNANCHOR, AST_FLAG_NET_PATTERN_RIGHT_PATHANCHOR, AST_FLAG_NET_PATTERN_LEFT_ANCHOR, AST_FLAG_NET_PATTERN_RIGHT_ANCHOR, AST_FLAG_HAS_OPTIONS, AST_ERROR_NONE, AST_ERROR_REGEX, AST_ERROR_PATTERN, AST_ERROR_DOMAIN_NAME, AST_ERROR_OPTION_DUPLICATE, AST_ERROR_OPTION_UNKNOWN, AST_ERROR_OPTION_BADVALUE, AST_ERROR_OPTION_EXCLUDED, AST_ERROR_IF_TOKEN_UNKNOWN, AST_ERROR_UNTRUSTED_SOURCE, NODE_RIGHT_INDEX, NOOP_NODE_SIZE, NODE_TYPE_INDEX, NODE_DOWN_INDEX, NODE_BEG_INDEX, NODE_END_INDEX, NODE_FLAGS_INDEX, NODE_TRANSFORM_INDEX, FULL_NODE_SIZE, NODE_TYPE_NOOP, NODE_TYPE_LINE_RAW, NODE_TYPE_LINE_BODY, NODE_TYPE_WHITESPACE, NODE_TYPE_COMMENT, NODE_TYPE_IGNORE, NODE_TYPE_EXT_RAW, NODE_TYPE_EXT_OPTIONS_ANCHOR, NODE_TYPE_EXT_OPTIONS, NODE_TYPE_EXT_DECORATION, NODE_TYPE_EXT_PATTERN_RAW, NODE_TYPE_EXT_PATTERN_COSMETIC, NODE_TYPE_EXT_PATTERN_HTML, NODE_TYPE_EXT_PATTERN_RESPONSEHEADER, NODE_TYPE_EXT_PATTERN_SCRIPTLET, NODE_TYPE_EXT_PATTERN_SCRIPTLET_TOKEN, NODE_TYPE_EXT_PATTERN_SCRIPTLET_ARGS, NODE_TYPE_EXT_PATTERN_SCRIPTLET_ARG, NODE_TYPE_NET_RAW, NODE_TYPE_NET_EXCEPTION, NODE_TYPE_NET_PATTERN_RAW, NODE_TYPE_NET_PATTERN, NODE_TYPE_NET_PATTERN_PART, NODE_TYPE_NET_PATTERN_PART_SPECIAL, NODE_TYPE_NET_PATTERN_PART_UNICODE, NODE_TYPE_NET_PATTERN_LEFT_HNANCHOR, NODE_TYPE_NET_PATTERN_LEFT_ANCHOR, NODE_TYPE_NET_PATTERN_RIGHT_ANCHOR, NODE_TYPE_NET_OPTIONS_ANCHOR, NODE_TYPE_NET_OPTIONS, NODE_TYPE_NET_OPTION_SEPARATOR, NODE_TYPE_NET_OPTION_SENTINEL, NODE_TYPE_NET_OPTION_RAW, NODE_TYPE_NET_OPTION_NAME_NOT, NODE_TYPE_NET_OPTION_NAME_UNKNOWN, NODE_TYPE_NET_OPTION_NAME_1P, NODE_TYPE_NET_OPTION_NAME_STRICT1P, NODE_TYPE_NET_OPTION_NAME_3P, NODE_TYPE_NET_OPTION_NAME_STRICT3P, NODE_TYPE_NET_OPTION_NAME_ALL, NODE_TYPE_NET_OPTION_NAME_BADFILTER, NODE_TYPE_NET_OPTION_NAME_CNAME, NODE_TYPE_NET_OPTION_NAME_CSP, NODE_TYPE_NET_OPTION_NAME_CSS, NODE_TYPE_NET_OPTION_NAME_DENYALLOW, NODE_TYPE_NET_OPTION_NAME_DOC, NODE_TYPE_NET_OPTION_NAME_EHIDE, NODE_TYPE_NET_OPTION_NAME_EMPTY, NODE_TYPE_NET_OPTION_NAME_FONT, NODE_TYPE_NET_OPTION_NAME_FRAME, NODE_TYPE_NET_OPTION_NAME_FROM, NODE_TYPE_NET_OPTION_NAME_GENERICBLOCK, NODE_TYPE_NET_OPTION_NAME_GHIDE, NODE_TYPE_NET_OPTION_NAME_RESPONSEHEADER, NODE_TYPE_NET_OPTION_NAME_IMAGE, NODE_TYPE_NET_OPTION_NAME_IMPORTANT, NODE_TYPE_NET_OPTION_NAME_INLINEFONT, NODE_TYPE_NET_OPTION_NAME_INLINESCRIPT, NODE_TYPE_NET_OPTION_NAME_IPADDRESS, NODE_TYPE_NET_OPTION_NAME_MATCHCASE, NODE_TYPE_NET_OPTION_NAME_MEDIA, NODE_TYPE_NET_OPTION_NAME_METHOD, NODE_TYPE_NET_OPTION_NAME_MP4, NODE_TYPE_NET_OPTION_NAME_NOOP, NODE_TYPE_NET_OPTION_NAME_OBJECT, NODE_TYPE_NET_OPTION_NAME_OTHER, NODE_TYPE_NET_OPTION_NAME_PERMISSIONS, NODE_TYPE_NET_OPTION_NAME_PING, NODE_TYPE_NET_OPTION_NAME_POPUNDER, NODE_TYPE_NET_OPTION_NAME_POPUP, NODE_TYPE_NET_OPTION_NAME_REASON, NODE_TYPE_NET_OPTION_NAME_REDIRECT, NODE_TYPE_NET_OPTION_NAME_REDIRECTRULE, NODE_TYPE_NET_OPTION_NAME_REMOVEPARAM, NODE_TYPE_NET_OPTION_NAME_REPLACE, NODE_TYPE_NET_OPTION_NAME_REQUESTHEADER, NODE_TYPE_NET_OPTION_NAME_SCRIPT, NODE_TYPE_NET_OPTION_NAME_SHIDE, NODE_TYPE_NET_OPTION_NAME_TO, NODE_TYPE_NET_OPTION_NAME_URLSKIP, NODE_TYPE_NET_OPTION_NAME_URLTRANSFORM, NODE_TYPE_NET_OPTION_NAME_XHR, NODE_TYPE_NET_OPTION_NAME_WEBRTC, NODE_TYPE_NET_OPTION_NAME_WEBSOCKET, NODE_TYPE_NET_OPTION_ASSIGN, NODE_TYPE_NET_OPTION_QUOTE, NODE_TYPE_NET_OPTION_VALUE, NODE_TYPE_OPTION_VALUE_DOMAIN_LIST, NODE_TYPE_OPTION_VALUE_DOMAIN_RAW, NODE_TYPE_OPTION_VALUE_NOT, NODE_TYPE_OPTION_VALUE_DOMAIN, NODE_TYPE_OPTION_VALUE_SEPARATOR, NODE_TYPE_PREPARSE_DIRECTIVE, NODE_TYPE_PREPARSE_DIRECTIVE_VALUE, NODE_TYPE_PREPARSE_DIRECTIVE_IF, NODE_TYPE_PREPARSE_DIRECTIVE_IF_VALUE, NODE_TYPE_COMMENT_URL, NODE_TYPE_COUNT, NODE_FLAG_IGNORE, NODE_FLAG_ERROR, NODE_FLAG_IS_NEGATED, NODE_FLAG_OPTION_HAS_VALUE, NODE_FLAG_PATTERN_UNTOKENIZABLE, nodeTypeFromOptionName, nodeNameFromNodeType, DOMAIN_CAN_USE_WILDCARD, DOMAIN_CAN_USE_ENTITY, DOMAIN_CAN_USE_SINGLE_WILDCARD, DOMAIN_CAN_BE_NEGATED, DOMAIN_CAN_BE_REGEX, DOMAIN_CAN_BE_ANCESTOR, DOMAIN_CAN_HAVE_PATH, DOMAIN_FROM_FROMTO_LIST, DOMAIN_FROM_DENYALLOW_LIST, DOMAIN_FROM_EXT_LIST, astTemplates, removableHTTPHeaders, exCharCodeAt, escapeForRegex, AstWalker, DomainListIterator, AstFilterParser, netOptionTokenDescriptors, ExtSelectorCompiler, utils;
  var init_static_filtering_parser = __esm({
    "src/js/static-filtering-parser.ts"() {
      init_css_tree();
      init_arglist_parser();
      init_jsonpath();
      iota2 = 0;
      iota2 = 0;
      AST_TYPE_NONE = iota2++;
      AST_TYPE_UNKNOWN = iota2++;
      AST_TYPE_COMMENT = iota2++;
      AST_TYPE_NETWORK = iota2++;
      AST_TYPE_EXTENDED = iota2++;
      iota2 = 0;
      AST_TYPE_NETWORK_PATTERN_ANY = iota2++;
      AST_TYPE_NETWORK_PATTERN_HOSTNAME = iota2++;
      AST_TYPE_NETWORK_PATTERN_PLAIN = iota2++;
      AST_TYPE_NETWORK_PATTERN_REGEX = iota2++;
      AST_TYPE_NETWORK_PATTERN_GENERIC = iota2++;
      AST_TYPE_NETWORK_PATTERN_BAD = iota2++;
      AST_TYPE_EXTENDED_COSMETIC = iota2++;
      AST_TYPE_EXTENDED_SCRIPTLET = iota2++;
      AST_TYPE_EXTENDED_HTML = iota2++;
      AST_TYPE_EXTENDED_RESPONSEHEADER = iota2++;
      AST_TYPE_COMMENT_PREPARSER = iota2++;
      iota2 = 0;
      AST_FLAG_UNSUPPORTED = 1 << iota2++;
      AST_FLAG_IGNORE = 1 << iota2++;
      AST_FLAG_HAS_ERROR = 1 << iota2++;
      AST_FLAG_IS_EXCEPTION = 1 << iota2++;
      AST_FLAG_EXT_STRONG = 1 << iota2++;
      AST_FLAG_EXT_STYLE = 1 << iota2++;
      AST_FLAG_EXT_SCRIPTLET_ADG = 1 << iota2++;
      AST_FLAG_NET_PATTERN_LEFT_HNANCHOR = 1 << iota2++;
      AST_FLAG_NET_PATTERN_RIGHT_PATHANCHOR = 1 << iota2++;
      AST_FLAG_NET_PATTERN_LEFT_ANCHOR = 1 << iota2++;
      AST_FLAG_NET_PATTERN_RIGHT_ANCHOR = 1 << iota2++;
      AST_FLAG_HAS_OPTIONS = 1 << iota2++;
      iota2 = 0;
      AST_ERROR_NONE = 1 << iota2++;
      AST_ERROR_REGEX = 1 << iota2++;
      AST_ERROR_PATTERN = 1 << iota2++;
      AST_ERROR_DOMAIN_NAME = 1 << iota2++;
      AST_ERROR_OPTION_DUPLICATE = 1 << iota2++;
      AST_ERROR_OPTION_UNKNOWN = 1 << iota2++;
      AST_ERROR_OPTION_BADVALUE = 1 << iota2++;
      AST_ERROR_OPTION_EXCLUDED = 1 << iota2++;
      AST_ERROR_IF_TOKEN_UNKNOWN = 1 << iota2++;
      AST_ERROR_UNTRUSTED_SOURCE = 1 << iota2++;
      iota2 = 0;
      NODE_RIGHT_INDEX = iota2++;
      NOOP_NODE_SIZE = iota2;
      NODE_TYPE_INDEX = iota2++;
      NODE_DOWN_INDEX = iota2++;
      NODE_BEG_INDEX = iota2++;
      NODE_END_INDEX = iota2++;
      NODE_FLAGS_INDEX = iota2++;
      NODE_TRANSFORM_INDEX = iota2++;
      FULL_NODE_SIZE = iota2;
      iota2 = 0;
      NODE_TYPE_NOOP = iota2++;
      NODE_TYPE_LINE_RAW = iota2++;
      NODE_TYPE_LINE_BODY = iota2++;
      NODE_TYPE_WHITESPACE = iota2++;
      NODE_TYPE_COMMENT = iota2++;
      NODE_TYPE_IGNORE = iota2++;
      NODE_TYPE_EXT_RAW = iota2++;
      NODE_TYPE_EXT_OPTIONS_ANCHOR = iota2++;
      NODE_TYPE_EXT_OPTIONS = iota2++;
      NODE_TYPE_EXT_DECORATION = iota2++;
      NODE_TYPE_EXT_PATTERN_RAW = iota2++;
      NODE_TYPE_EXT_PATTERN_COSMETIC = iota2++;
      NODE_TYPE_EXT_PATTERN_HTML = iota2++;
      NODE_TYPE_EXT_PATTERN_RESPONSEHEADER = iota2++;
      NODE_TYPE_EXT_PATTERN_SCRIPTLET = iota2++;
      NODE_TYPE_EXT_PATTERN_SCRIPTLET_TOKEN = iota2++;
      NODE_TYPE_EXT_PATTERN_SCRIPTLET_ARGS = iota2++;
      NODE_TYPE_EXT_PATTERN_SCRIPTLET_ARG = iota2++;
      NODE_TYPE_NET_RAW = iota2++;
      NODE_TYPE_NET_EXCEPTION = iota2++;
      NODE_TYPE_NET_PATTERN_RAW = iota2++;
      NODE_TYPE_NET_PATTERN = iota2++;
      NODE_TYPE_NET_PATTERN_PART = iota2++;
      NODE_TYPE_NET_PATTERN_PART_SPECIAL = iota2++;
      NODE_TYPE_NET_PATTERN_PART_UNICODE = iota2++;
      NODE_TYPE_NET_PATTERN_LEFT_HNANCHOR = iota2++;
      NODE_TYPE_NET_PATTERN_LEFT_ANCHOR = iota2++;
      NODE_TYPE_NET_PATTERN_RIGHT_ANCHOR = iota2++;
      NODE_TYPE_NET_OPTIONS_ANCHOR = iota2++;
      NODE_TYPE_NET_OPTIONS = iota2++;
      NODE_TYPE_NET_OPTION_SEPARATOR = iota2++;
      NODE_TYPE_NET_OPTION_SENTINEL = iota2++;
      NODE_TYPE_NET_OPTION_RAW = iota2++;
      NODE_TYPE_NET_OPTION_NAME_NOT = iota2++;
      NODE_TYPE_NET_OPTION_NAME_UNKNOWN = iota2++;
      NODE_TYPE_NET_OPTION_NAME_1P = iota2++;
      NODE_TYPE_NET_OPTION_NAME_STRICT1P = iota2++;
      NODE_TYPE_NET_OPTION_NAME_3P = iota2++;
      NODE_TYPE_NET_OPTION_NAME_STRICT3P = iota2++;
      NODE_TYPE_NET_OPTION_NAME_ALL = iota2++;
      NODE_TYPE_NET_OPTION_NAME_BADFILTER = iota2++;
      NODE_TYPE_NET_OPTION_NAME_CNAME = iota2++;
      NODE_TYPE_NET_OPTION_NAME_CSP = iota2++;
      NODE_TYPE_NET_OPTION_NAME_CSS = iota2++;
      NODE_TYPE_NET_OPTION_NAME_DENYALLOW = iota2++;
      NODE_TYPE_NET_OPTION_NAME_DOC = iota2++;
      NODE_TYPE_NET_OPTION_NAME_EHIDE = iota2++;
      NODE_TYPE_NET_OPTION_NAME_EMPTY = iota2++;
      NODE_TYPE_NET_OPTION_NAME_FONT = iota2++;
      NODE_TYPE_NET_OPTION_NAME_FRAME = iota2++;
      NODE_TYPE_NET_OPTION_NAME_FROM = iota2++;
      NODE_TYPE_NET_OPTION_NAME_GENERICBLOCK = iota2++;
      NODE_TYPE_NET_OPTION_NAME_GHIDE = iota2++;
      NODE_TYPE_NET_OPTION_NAME_RESPONSEHEADER = iota2++;
      NODE_TYPE_NET_OPTION_NAME_IMAGE = iota2++;
      NODE_TYPE_NET_OPTION_NAME_IMPORTANT = iota2++;
      NODE_TYPE_NET_OPTION_NAME_INLINEFONT = iota2++;
      NODE_TYPE_NET_OPTION_NAME_INLINESCRIPT = iota2++;
      NODE_TYPE_NET_OPTION_NAME_IPADDRESS = iota2++;
      NODE_TYPE_NET_OPTION_NAME_MATCHCASE = iota2++;
      NODE_TYPE_NET_OPTION_NAME_MEDIA = iota2++;
      NODE_TYPE_NET_OPTION_NAME_METHOD = iota2++;
      NODE_TYPE_NET_OPTION_NAME_MP4 = iota2++;
      NODE_TYPE_NET_OPTION_NAME_NOOP = iota2++;
      NODE_TYPE_NET_OPTION_NAME_OBJECT = iota2++;
      NODE_TYPE_NET_OPTION_NAME_OTHER = iota2++;
      NODE_TYPE_NET_OPTION_NAME_PERMISSIONS = iota2++;
      NODE_TYPE_NET_OPTION_NAME_PING = iota2++;
      NODE_TYPE_NET_OPTION_NAME_POPUNDER = iota2++;
      NODE_TYPE_NET_OPTION_NAME_POPUP = iota2++;
      NODE_TYPE_NET_OPTION_NAME_REASON = iota2++;
      NODE_TYPE_NET_OPTION_NAME_REDIRECT = iota2++;
      NODE_TYPE_NET_OPTION_NAME_REDIRECTRULE = iota2++;
      NODE_TYPE_NET_OPTION_NAME_REMOVEPARAM = iota2++;
      NODE_TYPE_NET_OPTION_NAME_REPLACE = iota2++;
      NODE_TYPE_NET_OPTION_NAME_REQUESTHEADER = iota2++;
      NODE_TYPE_NET_OPTION_NAME_SCRIPT = iota2++;
      NODE_TYPE_NET_OPTION_NAME_SHIDE = iota2++;
      NODE_TYPE_NET_OPTION_NAME_TO = iota2++;
      NODE_TYPE_NET_OPTION_NAME_URLSKIP = iota2++;
      NODE_TYPE_NET_OPTION_NAME_URLTRANSFORM = iota2++;
      NODE_TYPE_NET_OPTION_NAME_XHR = iota2++;
      NODE_TYPE_NET_OPTION_NAME_WEBRTC = iota2++;
      NODE_TYPE_NET_OPTION_NAME_WEBSOCKET = iota2++;
      NODE_TYPE_NET_OPTION_ASSIGN = iota2++;
      NODE_TYPE_NET_OPTION_QUOTE = iota2++;
      NODE_TYPE_NET_OPTION_VALUE = iota2++;
      NODE_TYPE_OPTION_VALUE_DOMAIN_LIST = iota2++;
      NODE_TYPE_OPTION_VALUE_DOMAIN_RAW = iota2++;
      NODE_TYPE_OPTION_VALUE_NOT = iota2++;
      NODE_TYPE_OPTION_VALUE_DOMAIN = iota2++;
      NODE_TYPE_OPTION_VALUE_SEPARATOR = iota2++;
      NODE_TYPE_PREPARSE_DIRECTIVE = iota2++;
      NODE_TYPE_PREPARSE_DIRECTIVE_VALUE = iota2++;
      NODE_TYPE_PREPARSE_DIRECTIVE_IF = iota2++;
      NODE_TYPE_PREPARSE_DIRECTIVE_IF_VALUE = iota2++;
      NODE_TYPE_COMMENT_URL = iota2++;
      NODE_TYPE_COUNT = iota2;
      iota2 = 0;
      NODE_FLAG_IGNORE = 1 << iota2++;
      NODE_FLAG_ERROR = 1 << iota2++;
      NODE_FLAG_IS_NEGATED = 1 << iota2++;
      NODE_FLAG_OPTION_HAS_VALUE = 1 << iota2++;
      NODE_FLAG_PATTERN_UNTOKENIZABLE = 1 << iota2++;
      nodeTypeFromOptionName = /* @__PURE__ */ new Map([
        ["", NODE_TYPE_NET_OPTION_NAME_UNKNOWN],
        ["1p", NODE_TYPE_NET_OPTION_NAME_1P],
        /* synonym */
        ["first-party", NODE_TYPE_NET_OPTION_NAME_1P],
        ["strict1p", NODE_TYPE_NET_OPTION_NAME_STRICT1P],
        /* synonym */
        ["strict-first-party", NODE_TYPE_NET_OPTION_NAME_STRICT1P],
        ["3p", NODE_TYPE_NET_OPTION_NAME_3P],
        /* synonym */
        ["third-party", NODE_TYPE_NET_OPTION_NAME_3P],
        ["strict3p", NODE_TYPE_NET_OPTION_NAME_STRICT3P],
        /* synonym */
        ["strict-third-party", NODE_TYPE_NET_OPTION_NAME_STRICT3P],
        ["all", NODE_TYPE_NET_OPTION_NAME_ALL],
        ["badfilter", NODE_TYPE_NET_OPTION_NAME_BADFILTER],
        ["cname", NODE_TYPE_NET_OPTION_NAME_CNAME],
        ["csp", NODE_TYPE_NET_OPTION_NAME_CSP],
        ["css", NODE_TYPE_NET_OPTION_NAME_CSS],
        /* synonym */
        ["stylesheet", NODE_TYPE_NET_OPTION_NAME_CSS],
        ["denyallow", NODE_TYPE_NET_OPTION_NAME_DENYALLOW],
        ["doc", NODE_TYPE_NET_OPTION_NAME_DOC],
        /* synonym */
        ["document", NODE_TYPE_NET_OPTION_NAME_DOC],
        ["ehide", NODE_TYPE_NET_OPTION_NAME_EHIDE],
        /* synonym */
        ["elemhide", NODE_TYPE_NET_OPTION_NAME_EHIDE],
        ["empty", NODE_TYPE_NET_OPTION_NAME_EMPTY],
        ["font", NODE_TYPE_NET_OPTION_NAME_FONT],
        ["frame", NODE_TYPE_NET_OPTION_NAME_FRAME],
        /* synonym */
        ["subdocument", NODE_TYPE_NET_OPTION_NAME_FRAME],
        ["from", NODE_TYPE_NET_OPTION_NAME_FROM],
        /* synonym */
        ["domain", NODE_TYPE_NET_OPTION_NAME_FROM],
        ["genericblock", NODE_TYPE_NET_OPTION_NAME_GENERICBLOCK],
        ["ghide", NODE_TYPE_NET_OPTION_NAME_GHIDE],
        /* synonym */
        ["generichide", NODE_TYPE_NET_OPTION_NAME_GHIDE],
        ["image", NODE_TYPE_NET_OPTION_NAME_IMAGE],
        ["important", NODE_TYPE_NET_OPTION_NAME_IMPORTANT],
        ["inline-font", NODE_TYPE_NET_OPTION_NAME_INLINEFONT],
        ["inline-script", NODE_TYPE_NET_OPTION_NAME_INLINESCRIPT],
        ["ipaddress", NODE_TYPE_NET_OPTION_NAME_IPADDRESS],
        ["match-case", NODE_TYPE_NET_OPTION_NAME_MATCHCASE],
        ["media", NODE_TYPE_NET_OPTION_NAME_MEDIA],
        ["method", NODE_TYPE_NET_OPTION_NAME_METHOD],
        ["mp4", NODE_TYPE_NET_OPTION_NAME_MP4],
        ["_", NODE_TYPE_NET_OPTION_NAME_NOOP],
        ["object", NODE_TYPE_NET_OPTION_NAME_OBJECT],
        /* synonym */
        ["object-subrequest", NODE_TYPE_NET_OPTION_NAME_OBJECT],
        ["other", NODE_TYPE_NET_OPTION_NAME_OTHER],
        ["permissions", NODE_TYPE_NET_OPTION_NAME_PERMISSIONS],
        ["ping", NODE_TYPE_NET_OPTION_NAME_PING],
        /* synonym */
        ["beacon", NODE_TYPE_NET_OPTION_NAME_PING],
        ["popunder", NODE_TYPE_NET_OPTION_NAME_POPUNDER],
        ["popup", NODE_TYPE_NET_OPTION_NAME_POPUP],
        ["reason", NODE_TYPE_NET_OPTION_NAME_REASON],
        ["redirect", NODE_TYPE_NET_OPTION_NAME_REDIRECT],
        /* synonym */
        ["rewrite", NODE_TYPE_NET_OPTION_NAME_REDIRECT],
        ["redirect-rule", NODE_TYPE_NET_OPTION_NAME_REDIRECTRULE],
        ["removeparam", NODE_TYPE_NET_OPTION_NAME_REMOVEPARAM],
        ["replace", NODE_TYPE_NET_OPTION_NAME_REPLACE],
        /* synonym */
        ["queryprune", NODE_TYPE_NET_OPTION_NAME_REMOVEPARAM],
        ["requestheader", NODE_TYPE_NET_OPTION_NAME_REQUESTHEADER],
        ["responseheader", NODE_TYPE_NET_OPTION_NAME_RESPONSEHEADER],
        /* synonym */
        ["header", NODE_TYPE_NET_OPTION_NAME_RESPONSEHEADER],
        ["script", NODE_TYPE_NET_OPTION_NAME_SCRIPT],
        ["shide", NODE_TYPE_NET_OPTION_NAME_SHIDE],
        /* synonym */
        ["specifichide", NODE_TYPE_NET_OPTION_NAME_SHIDE],
        ["to", NODE_TYPE_NET_OPTION_NAME_TO],
        ["urlskip", NODE_TYPE_NET_OPTION_NAME_URLSKIP],
        ["uritransform", NODE_TYPE_NET_OPTION_NAME_URLTRANSFORM],
        ["xhr", NODE_TYPE_NET_OPTION_NAME_XHR],
        /* synonym */
        ["xmlhttprequest", NODE_TYPE_NET_OPTION_NAME_XHR],
        ["webrtc", NODE_TYPE_NET_OPTION_NAME_WEBRTC],
        ["websocket", NODE_TYPE_NET_OPTION_NAME_WEBSOCKET]
      ]);
      nodeNameFromNodeType = /* @__PURE__ */ new Map([
        [NODE_TYPE_NOOP, "noop"],
        [NODE_TYPE_LINE_RAW, "lineRaw"],
        [NODE_TYPE_LINE_BODY, "lineBody"],
        [NODE_TYPE_WHITESPACE, "whitespace"],
        [NODE_TYPE_COMMENT, "comment"],
        [NODE_TYPE_IGNORE, "ignore"],
        [NODE_TYPE_EXT_RAW, "extRaw"],
        [NODE_TYPE_EXT_OPTIONS_ANCHOR, "extOptionsAnchor"],
        [NODE_TYPE_EXT_OPTIONS, "extOptions"],
        [NODE_TYPE_EXT_DECORATION, "extDecoration"],
        [NODE_TYPE_EXT_PATTERN_RAW, "extPatternRaw"],
        [NODE_TYPE_EXT_PATTERN_COSMETIC, "extPatternCosmetic"],
        [NODE_TYPE_EXT_PATTERN_HTML, "extPatternHtml"],
        [NODE_TYPE_EXT_PATTERN_RESPONSEHEADER, "extPatternResponseheader"],
        [NODE_TYPE_EXT_PATTERN_SCRIPTLET, "extPatternScriptlet"],
        [NODE_TYPE_EXT_PATTERN_SCRIPTLET_TOKEN, "extPatternScriptletToken"],
        [NODE_TYPE_EXT_PATTERN_SCRIPTLET_ARGS, "extPatternScriptletArgs"],
        [NODE_TYPE_EXT_PATTERN_SCRIPTLET_ARG, "extPatternScriptletArg"],
        [NODE_TYPE_NET_RAW, "netRaw"],
        [NODE_TYPE_NET_EXCEPTION, "netException"],
        [NODE_TYPE_NET_PATTERN_RAW, "netPatternRaw"],
        [NODE_TYPE_NET_PATTERN, "netPattern"],
        [NODE_TYPE_NET_PATTERN_PART, "netPatternPart"],
        [NODE_TYPE_NET_PATTERN_PART_SPECIAL, "netPatternPartSpecial"],
        [NODE_TYPE_NET_PATTERN_PART_UNICODE, "netPatternPartUnicode"],
        [NODE_TYPE_NET_PATTERN_LEFT_HNANCHOR, "netPatternLeftHnanchor"],
        [NODE_TYPE_NET_PATTERN_LEFT_ANCHOR, "netPatternLeftAnchor"],
        [NODE_TYPE_NET_PATTERN_RIGHT_ANCHOR, "netPatternRightAnchor"],
        [NODE_TYPE_NET_OPTIONS_ANCHOR, "netOptionsAnchor"],
        [NODE_TYPE_NET_OPTIONS, "netOptions"],
        [NODE_TYPE_NET_OPTION_RAW, "netOptionRaw"],
        [NODE_TYPE_NET_OPTION_SEPARATOR, "netOptionSeparator"],
        [NODE_TYPE_NET_OPTION_SENTINEL, "netOptionSentinel"],
        [NODE_TYPE_NET_OPTION_NAME_NOT, "netOptionNameNot"],
        [NODE_TYPE_NET_OPTION_ASSIGN, "netOptionAssign"],
        [NODE_TYPE_NET_OPTION_VALUE, "netOptionValue"],
        [NODE_TYPE_OPTION_VALUE_DOMAIN_LIST, "netOptionValueDomainList"],
        [NODE_TYPE_OPTION_VALUE_DOMAIN_RAW, "netOptionValueDomainRaw"],
        [NODE_TYPE_OPTION_VALUE_NOT, "netOptionValueNot"],
        [NODE_TYPE_OPTION_VALUE_DOMAIN, "netOptionValueDomain"],
        [NODE_TYPE_OPTION_VALUE_SEPARATOR, "netOptionsValueSeparator"]
      ]);
      {
        for (const [name2, type] of nodeTypeFromOptionName) {
          nodeNameFromNodeType.set(type, name2);
        }
      }
      DOMAIN_CAN_USE_WILDCARD = 1;
      DOMAIN_CAN_USE_ENTITY = 2;
      DOMAIN_CAN_USE_SINGLE_WILDCARD = 4;
      DOMAIN_CAN_BE_NEGATED = 8;
      DOMAIN_CAN_BE_REGEX = 16;
      DOMAIN_CAN_BE_ANCESTOR = 32;
      DOMAIN_CAN_HAVE_PATH = 64;
      DOMAIN_FROM_FROMTO_LIST = DOMAIN_CAN_USE_ENTITY | DOMAIN_CAN_BE_NEGATED | DOMAIN_CAN_BE_REGEX;
      DOMAIN_FROM_DENYALLOW_LIST = 0;
      DOMAIN_FROM_EXT_LIST = DOMAIN_CAN_USE_ENTITY | DOMAIN_CAN_USE_SINGLE_WILDCARD | DOMAIN_CAN_BE_NEGATED | DOMAIN_CAN_BE_REGEX | DOMAIN_CAN_BE_ANCESTOR | DOMAIN_CAN_HAVE_PATH;
      astTemplates = {
        // ||example.com^
        netHnAnchoredHostnameAscii: {
          flags: AST_FLAG_NET_PATTERN_LEFT_HNANCHOR | AST_FLAG_NET_PATTERN_RIGHT_PATHANCHOR,
          type: NODE_TYPE_LINE_BODY,
          beg: 0,
          end: 0,
          children: [{
            type: NODE_TYPE_NET_RAW,
            beg: 0,
            end: 0,
            children: [{
              type: NODE_TYPE_NET_PATTERN_RAW,
              beg: 0,
              end: 0,
              register: true,
              children: [{
                type: NODE_TYPE_NET_PATTERN_LEFT_HNANCHOR,
                beg: 0,
                end: 2
              }, {
                type: NODE_TYPE_NET_PATTERN,
                beg: 2,
                end: -1,
                register: true
              }, {
                type: NODE_TYPE_NET_PATTERN_PART_SPECIAL,
                beg: -1,
                end: 0
              }]
            }]
          }]
        },
        // ||example.com^$third-party
        net3pHnAnchoredHostnameAscii: {
          flags: AST_FLAG_NET_PATTERN_LEFT_HNANCHOR | AST_FLAG_NET_PATTERN_RIGHT_PATHANCHOR | AST_FLAG_HAS_OPTIONS,
          type: NODE_TYPE_LINE_BODY,
          beg: 0,
          end: 0,
          children: [{
            type: NODE_TYPE_NET_RAW,
            beg: 0,
            end: 0,
            children: [{
              type: NODE_TYPE_NET_PATTERN_RAW,
              beg: 0,
              end: 0,
              register: true,
              children: [{
                type: NODE_TYPE_NET_PATTERN_LEFT_HNANCHOR,
                beg: 0,
                end: 2
              }, {
                type: NODE_TYPE_NET_PATTERN,
                beg: 2,
                end: -13,
                register: true
              }, {
                type: NODE_TYPE_NET_PATTERN_PART_SPECIAL,
                beg: -13,
                end: -12
              }]
            }, {
              type: NODE_TYPE_NET_OPTIONS_ANCHOR,
              beg: -12,
              end: -11
            }, {
              type: NODE_TYPE_NET_OPTIONS,
              beg: -11,
              end: 0,
              register: true,
              children: [{
                type: NODE_TYPE_NET_OPTION_RAW,
                beg: 0,
                end: 0,
                children: [{
                  type: NODE_TYPE_NET_OPTION_NAME_3P,
                  beg: 0,
                  end: 0,
                  register: true
                }]
              }]
            }]
          }]
        },
        // ||example.com/path/to/resource
        netHnAnchoredPlainAscii: {
          flags: AST_FLAG_NET_PATTERN_LEFT_HNANCHOR,
          type: NODE_TYPE_LINE_BODY,
          beg: 0,
          end: 0,
          children: [{
            type: NODE_TYPE_NET_RAW,
            beg: 0,
            end: 0,
            children: [{
              type: NODE_TYPE_NET_PATTERN_RAW,
              beg: 0,
              end: 0,
              register: true,
              children: [{
                type: NODE_TYPE_NET_PATTERN_LEFT_HNANCHOR,
                beg: 0,
                end: 2
              }, {
                type: NODE_TYPE_NET_PATTERN,
                beg: 2,
                end: 0,
                register: true
              }]
            }]
          }]
        },
        // example.com
        // -resource.
        netPlainAscii: {
          type: NODE_TYPE_LINE_BODY,
          beg: 0,
          end: 0,
          children: [{
            type: NODE_TYPE_NET_RAW,
            beg: 0,
            end: 0,
            children: [{
              type: NODE_TYPE_NET_PATTERN_RAW,
              beg: 0,
              end: 0,
              register: true,
              children: [{
                type: NODE_TYPE_NET_PATTERN,
                beg: 0,
                end: 0,
                register: true
              }]
            }]
          }]
        },
        // 127.0.0.1 example.com
        netHosts1: {
          type: NODE_TYPE_LINE_BODY,
          beg: 0,
          end: 0,
          children: [{
            type: NODE_TYPE_NET_RAW,
            beg: 0,
            end: 0,
            children: [{
              type: NODE_TYPE_NET_PATTERN_RAW,
              beg: 0,
              end: 0,
              register: true,
              children: [{
                type: NODE_TYPE_IGNORE,
                beg: 0,
                end: 10
              }, {
                type: NODE_TYPE_NET_PATTERN,
                beg: 10,
                end: 0,
                register: true
              }]
            }]
          }]
        },
        // 0.0.0.0 example.com
        netHosts2: {
          type: NODE_TYPE_LINE_BODY,
          beg: 0,
          end: 0,
          children: [{
            type: NODE_TYPE_NET_RAW,
            beg: 0,
            end: 0,
            children: [{
              type: NODE_TYPE_NET_PATTERN_RAW,
              beg: 0,
              end: 0,
              register: true,
              children: [{
                type: NODE_TYPE_IGNORE,
                beg: 0,
                end: 8
              }, {
                type: NODE_TYPE_NET_PATTERN,
                beg: 8,
                end: 0,
                register: true
              }]
            }]
          }]
        },
        // ##.ads-container
        extPlainGenericSelector: {
          type: NODE_TYPE_LINE_BODY,
          beg: 0,
          end: 0,
          children: [{
            type: NODE_TYPE_EXT_RAW,
            beg: 0,
            end: 0,
            children: [{
              type: NODE_TYPE_EXT_OPTIONS_ANCHOR,
              beg: 0,
              end: 2,
              register: true
            }, {
              type: NODE_TYPE_EXT_PATTERN_RAW,
              beg: 2,
              end: 0,
              register: true,
              children: [{
                type: NODE_TYPE_EXT_PATTERN_COSMETIC,
                beg: 0,
                end: 0
              }]
            }]
          }]
        }
      };
      removableHTTPHeaders = /* @__PURE__ */ new Set([
        "location",
        "refresh",
        "report-to",
        "set-cookie"
      ]);
      exCharCodeAt = (s, i) => {
        const pos = i >= 0 ? i : s.length + i;
        return pos >= 0 ? s.charCodeAt(pos) : -1;
      };
      escapeForRegex = (s) => s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
      AstWalker = class {
        constructor(parser, from = 0) {
          this.parser = parser;
          this.stack = [];
          this.reset(from);
        }
        get depth() {
          return this.stackPtr;
        }
        reset(from = 0) {
          this.nodes = this.parser.nodes;
          this.stackPtr = 0;
          return this.current = from || this.parser.rootNode;
        }
        next() {
          const current = this.current;
          if (current === 0) {
            return 0;
          }
          const down = this.nodes[current + NODE_DOWN_INDEX];
          if (down !== 0) {
            this.stack[this.stackPtr++] = this.current;
            return this.current = down;
          }
          const right = this.nodes[current + NODE_RIGHT_INDEX];
          if (right !== 0 && this.stackPtr !== 0) {
            return this.current = right;
          }
          while (this.stackPtr !== 0) {
            const parent = this.stack[--this.stackPtr];
            const right2 = this.nodes[parent + NODE_RIGHT_INDEX];
            if (right2 !== 0) {
              return this.current = right2;
            }
          }
          return this.current = 0;
        }
        right() {
          const current = this.current;
          if (current === 0) {
            return 0;
          }
          const right = this.nodes[current + NODE_RIGHT_INDEX];
          if (right !== 0 && this.stackPtr !== 0) {
            return this.current = right;
          }
          while (this.stackPtr !== 0) {
            const parent = this.stack[--this.stackPtr];
            const right2 = this.nodes[parent + NODE_RIGHT_INDEX];
            if (right2 !== 0) {
              return this.current = right2;
            }
          }
          return this.current = 0;
        }
        until(which) {
          let node = this.next();
          while (node !== 0) {
            if (this.nodes[node + NODE_TYPE_INDEX] === which) {
              return node;
            }
            node = this.next();
          }
          return 0;
        }
        canGoDown() {
          return this.nodes[this.current + NODE_DOWN_INDEX] !== 0;
        }
        dispose() {
          this.parser.walkerJunkyard.push(this);
        }
      };
      DomainListIterator = class {
        constructor(parser, root) {
          this.parser = parser;
          this.walker = parser.getWalker();
          this.value = void 0;
          this.item = { hn: "", not: false, bad: false };
          this.reuse(root);
        }
        next() {
          if (this.done) {
            return this.value;
          }
          let node = this.walker.current;
          let ready = false;
          while (node !== 0) {
            switch (this.parser.getNodeType(node)) {
              case NODE_TYPE_OPTION_VALUE_DOMAIN_RAW:
                this.item.hn = "";
                this.item.not = false;
                this.item.bad = this.parser.getNodeFlags(node, NODE_FLAG_ERROR) !== 0;
                break;
              case NODE_TYPE_OPTION_VALUE_NOT:
                this.item.not = true;
                break;
              case NODE_TYPE_OPTION_VALUE_DOMAIN:
                this.item.hn = this.parser.getNodeTransform(node);
                this.value = this.item;
                ready = true;
                break;
              default:
                break;
            }
            node = this.walker.next();
            if (ready) {
              return this;
            }
          }
          return this.stop();
        }
        reuse(root) {
          this.walker.reset(root);
          this.done = false;
          return this;
        }
        stop() {
          this.done = true;
          this.value = void 0;
          this.parser.domainListIteratorJunkyard.push(this);
          return this;
        }
        [Symbol.iterator]() {
          return this;
        }
      };
      AstFilterParser = class {
        constructor(options = {}) {
          this.raw = "";
          this.rawEnd = 0;
          this.nodes = new Uint32Array(16384);
          this.nodePoolPtr = FULL_NODE_SIZE;
          this.nodePoolEnd = this.nodes.length;
          this.astTransforms = [null];
          this.astTransformPtr = 1;
          this.rootNode = 0;
          this.astType = AST_TYPE_NONE;
          this.astTypeFlavor = AST_TYPE_NONE;
          this.astFlags = 0;
          this.astError = 0;
          this.nodeTypeRegister = [];
          this.nodeTypeRegisterPtr = 0;
          this.nodeTypeLookupTable = new Uint32Array(NODE_TYPE_COUNT);
          this.punycoder = new URL("https://ublock0.invalid/");
          this.domainListIteratorJunkyard = [];
          this.walkerJunkyard = [];
          this.hasWhitespace = false;
          this.hasUnicode = false;
          this.hasUppercase = false;
          this.options = options;
          this.interactive = options.interactive || false;
          this.badTypes = new Set(options.badTypes || []);
          this.maxTokenLength = options.maxTokenLength || 7;
          this.result = { exception: false, raw: "", compiled: "", error: void 0 };
          this.selectorCompiler = new ExtSelectorCompiler(options);
          this.reWhitespaceStart = /^\s+/;
          this.reWhitespaceEnd = /(?:^|\S)(\s+)$/;
          this.reCommentLine = /^(?:!|#\s|####|\[adblock)/i;
          this.reExtAnchor = /(#@?(?:\$\?|\$|%|\?)?#).{1,2}/;
          this.reInlineComment = /(?:\s+#).*?$/;
          this.reNetException = /^@@/;
          this.reNetAnchor = /(?:)\$[^,\w~]/;
          this.reHnAnchoredPlainAscii = /^\|\|[0-9a-z%&,\-./:;=?_]+$/;
          this.reHnAnchoredHostnameAscii = /^\|\|(?:[\da-z][\da-z_-]*\.)*[\da-z_-]*[\da-z]\^$/;
          this.reHnAnchoredHostnameUnicode = /^\|\|(?:[\p{L}\p{N}][\p{L}\p{N}\u{2d}]*\.)*[\p{L}\p{N}\u{2d}]*[\p{L}\p{N}]\^$/u;
          this.reHn3pAnchoredHostnameAscii = /^\|\|(?:[\da-z][\da-z_-]*\.)*[\da-z_-]*[\da-z]\^\$third-party$/;
          this.rePlainAscii = /^[0-9a-z%&\-./:;=?_]{2,}$/;
          this.reNetHosts1 = /^127\.0\.0\.1 (?:[\da-z][\da-z_-]*\.)+[\da-z-]*[a-z]$/;
          this.reNetHosts2 = /^0\.0\.0\.0 (?:[\da-z][\da-z_-]*\.)+[\da-z-]*[a-z]$/;
          this.rePlainGenericCosmetic = /^##[.#][A-Za-z_][\w-]*$/;
          this.reHostnameAscii = /^(?:[\da-z][\da-z_-]*\.)*[\da-z][\da-z-]*[\da-z]$/;
          this.rePlainEntity = /^(?:[\da-z][\da-z_-]*\.)+\*$/;
          this.reHostsSink = /^[\w%.:[\]-]+\s+/;
          this.reHostsRedirect = /(?:0\.0\.0\.0|broadcasthost|local|localhost(?:\.localdomain)?|ip6-\w+)(?:[^\w.-]|$)/;
          this.reNetOptionComma = /,(?:~?[13a-z-]+(?:=.*?)?|_+)(?:,|$)/;
          this.rePointlessLeftAnchor = /^\|\|?\*+/;
          this.rePointlessLeadingWildcards = /^(\*+)[^%0-9A-Za-z\u{a0}-\u{10FFFF}]/u;
          this.rePointlessTrailingSeparator = /\*(\^\**)$/;
          this.rePointlessTrailingWildcards = /(?:[^%0-9A-Za-z]|[%0-9A-Za-z]{7,})(\*+)$/;
          this.reHasWhitespaceChar = /\s/;
          this.reHasUppercaseChar = /[A-Z]/;
          this.reHasUnicodeChar = /[^\x00-\x7F]/;
          this.reUnicodeChars = /\P{ASCII}/gu;
          this.reBadHostnameChars = /[\x00-\x24\x26-\x29\x2b\x2c\x2f\x3b-\x40\x5c\x5e\x60\x7b-\x7f]/;
          this.reIsEntity = /^[^*]+\.\*$/;
          this.rePreparseDirectiveIf = /^!#if /;
          this.rePreparseDirectiveAny = /^!#(?:else|endif|if |include )/;
          this.reURL = /\bhttps?:\/\/\S+/;
          this.reHasPatternSpecialChars = /[*^]/;
          this.rePatternAllSpecialChars = /[*^]+|[^\x00-\x7f]+/g;
          this.reHasInvalidChar = /[\x00-\x1F\x7F-\x9F\xAD\u061C\u200B-\u200F\u2028\u2029\uFEFF\uFFF9-\uFFFC]/;
          this.reHostnamePatternPart = /^[^\x00-\x24\x26-\x29\x2B\x2C\x2F\x3A-\x40\x5B-\x5E\x60\x7B-\x7F]+/;
          this.reHostnameLabel = /[^.]+/g;
          this.reResponseheaderPattern = /^\^responseheader\(.*\)$/;
          this.rePatternScriptletJsonArgs = /^\{.*\}$/;
          this.reBadCSP = /(?:^|[;,])\s*report-(?:to|uri)\b/i;
          this.reBadPP = /(?:^|[;,])\s*report-to\b/i;
          this.reNetOption = /^(~?)([134a-z_-]+)(=?)/;
          this.reNoopOption = /^_+$/;
          this.reAdvancedDomainSyntax = /^([^>]+?)(>>)?(\/.*)?$/;
          this.netOptionValueParser = new ArglistParser(",");
          this.scriptletArgListParser = new ArglistParser(",");
          this.domainRegexValueParser = new ArglistParser("/");
          this.reNetOptionTokens = new RegExp(
            `^~?(${Array.from(netOptionTokenDescriptors.keys()).map((s) => escapeForRegex(s)).join("|")})\\b`
          );
        }
        finish() {
          this.selectorCompiler.finish();
        }
        parse(raw) {
          this.raw = raw;
          this.rawEnd = raw.length;
          this.nodePoolPtr = FULL_NODE_SIZE;
          this.nodeTypeRegisterPtr = 0;
          this.astTransformPtr = 1;
          this.astType = AST_TYPE_NONE;
          this.astTypeFlavor = AST_TYPE_NONE;
          this.astFlags = 0;
          this.astError = 0;
          this.rootNode = this.allocTypedNode(NODE_TYPE_LINE_RAW, 0, this.rawEnd);
          if (this.rawEnd === 0) {
            return;
          }
          const c1st = this.raw.charCodeAt(0);
          const clast = exCharCodeAt(this.raw, -1);
          if (c1st === 124) {
            if (clast === 94 && this.reHnAnchoredHostnameAscii.test(this.raw)) {
              this.astType = AST_TYPE_NETWORK;
              this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_HOSTNAME;
              const node = this.astFromTemplate(
                this.rootNode,
                astTemplates.netHnAnchoredHostnameAscii
              );
              this.linkDown(this.rootNode, node);
              return;
            }
            if (this.raw.endsWith("$third-party") && this.reHn3pAnchoredHostnameAscii.test(this.raw)) {
              this.astType = AST_TYPE_NETWORK;
              this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_HOSTNAME;
              const node = this.astFromTemplate(
                this.rootNode,
                astTemplates.net3pHnAnchoredHostnameAscii
              );
              this.linkDown(this.rootNode, node);
              return;
            }
            if (this.reHnAnchoredPlainAscii.test(this.raw)) {
              this.astType = AST_TYPE_NETWORK;
              this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_PLAIN;
              const node = this.astFromTemplate(
                this.rootNode,
                astTemplates.netHnAnchoredPlainAscii
              );
              this.linkDown(this.rootNode, node);
              return;
            }
          } else if (c1st === 35) {
            if (this.rePlainGenericCosmetic.test(this.raw)) {
              this.astType = AST_TYPE_EXTENDED;
              this.astTypeFlavor = AST_TYPE_EXTENDED_COSMETIC;
              const node = this.astFromTemplate(
                this.rootNode,
                astTemplates.extPlainGenericSelector
              );
              this.linkDown(this.rootNode, node);
              this.result.exception = false;
              this.result.raw = this.raw.slice(2);
              this.result.compiled = this.raw.slice(2);
              return;
            }
          } else if (c1st === 49) {
            if (this.reNetHosts1.test(this.raw)) {
              this.astType = AST_TYPE_NETWORK;
              this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_HOSTNAME;
              const node = this.astFromTemplate(
                this.rootNode,
                astTemplates.netHosts1
              );
              this.linkDown(this.rootNode, node);
              return;
            }
          } else if (c1st === 48) {
            if (this.reNetHosts2.test(this.raw)) {
              this.astType = AST_TYPE_NETWORK;
              this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_HOSTNAME;
              const node = this.astFromTemplate(
                this.rootNode,
                astTemplates.netHosts2
              );
              this.linkDown(this.rootNode, node);
              return;
            }
          } else if ((c1st !== 47 || clast !== 47) && this.rePlainAscii.test(this.raw)) {
            this.astType = AST_TYPE_NETWORK;
            this.astTypeFlavor = this.reHostnameAscii.test(this.raw) ? AST_TYPE_NETWORK_PATTERN_HOSTNAME : AST_TYPE_NETWORK_PATTERN_PLAIN;
            const node = this.astFromTemplate(
              this.rootNode,
              astTemplates.netPlainAscii
            );
            this.linkDown(this.rootNode, node);
            return;
          }
          this.hasWhitespace = this.reHasWhitespaceChar.test(raw);
          this.linkDown(this.rootNode, this.parseRaw(this.rootNode));
        }
        astFromTemplate(parent, template) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          const beg = template.beg + (template.beg >= 0 ? parentBeg : parentEnd);
          const end = template.end + (template.end <= 0 ? parentEnd : parentBeg);
          const node = this.allocTypedNode(template.type, beg, end);
          if (template.register) {
            this.addNodeToRegister(template.type, node);
          }
          if (template.flags) {
            this.addFlags(template.flags);
          }
          if (template.nodeFlags) {
            this.addNodeFlags(node, template.nodeFlags);
          }
          const children = template.children;
          if (children === void 0) {
            return node;
          }
          const head = this.astFromTemplate(node, children[0]);
          this.linkDown(node, head);
          const n = children.length;
          if (n === 1) {
            return node;
          }
          let prev = head;
          for (let i = 1; i < n; i++) {
            prev = this.linkRight(prev, this.astFromTemplate(node, children[i]));
          }
          return node;
        }
        getType() {
          return this.astType;
        }
        isComment() {
          return this.astType === AST_TYPE_COMMENT;
        }
        isFilter() {
          return this.isNetworkFilter() || this.isExtendedFilter();
        }
        isNetworkFilter() {
          return this.astType === AST_TYPE_NETWORK;
        }
        isExtendedFilter() {
          return this.astType === AST_TYPE_EXTENDED;
        }
        isCosmeticFilter() {
          return this.astType === AST_TYPE_EXTENDED && this.astTypeFlavor === AST_TYPE_EXTENDED_COSMETIC;
        }
        isScriptletFilter() {
          return this.astType === AST_TYPE_EXTENDED && this.astTypeFlavor === AST_TYPE_EXTENDED_SCRIPTLET;
        }
        isHtmlFilter() {
          return this.astType === AST_TYPE_EXTENDED && this.astTypeFlavor === AST_TYPE_EXTENDED_HTML;
        }
        isResponseheaderFilter() {
          return this.astType === AST_TYPE_EXTENDED && this.astTypeFlavor === AST_TYPE_EXTENDED_RESPONSEHEADER;
        }
        getFlags(flags = 4294967295) {
          return this.astFlags & flags;
        }
        addFlags(flags) {
          this.astFlags |= flags;
        }
        parseRaw(parent) {
          const head = this.allocHeadNode();
          let prev = head, next = 0;
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          const l1 = this.hasWhitespace ? this.leftWhitespaceCount(this.getNodeString(parent)) : 0;
          if (l1 !== 0) {
            next = this.allocTypedNode(
              NODE_TYPE_WHITESPACE,
              parentBeg,
              parentBeg + l1
            );
            prev = this.linkRight(prev, next);
            if (l1 === parentEnd) {
              return this.throwHeadNode(head);
            }
          }
          const r0 = this.hasWhitespace ? parentEnd - this.rightWhitespaceCount(this.getNodeString(parent)) : parentEnd;
          if (r0 !== l1) {
            next = this.allocTypedNode(
              NODE_TYPE_LINE_BODY,
              parentBeg + l1,
              parentBeg + r0
            );
            this.linkDown(next, this.parseFilter(next));
            prev = this.linkRight(prev, next);
          }
          if (r0 !== parentEnd) {
            next = this.allocTypedNode(
              NODE_TYPE_WHITESPACE,
              parentBeg + r0,
              parentEnd
            );
            this.linkRight(prev, next);
          }
          return this.throwHeadNode(head);
        }
        parseFilter(parent) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          const parentStr = this.getNodeString(parent);
          if (this.reCommentLine.test(parentStr)) {
            const head2 = this.allocTypedNode(NODE_TYPE_COMMENT, parentBeg, parentEnd);
            this.astType = AST_TYPE_COMMENT;
            if (this.interactive) {
              this.linkDown(head2, this.parseComment(head2));
            }
            return head2;
          }
          if (this.reExtAnchor.test(parentStr)) {
            const match = this.reExtAnchor.exec(parentStr);
            const matchLen = match[1].length;
            const head2 = this.allocTypedNode(NODE_TYPE_EXT_RAW, parentBeg, parentEnd);
            this.linkDown(head2, this.parseExt(head2, parentBeg + match.index, matchLen));
            return head2;
          } else if (parentStr.charCodeAt(0) === 35) {
            const head2 = this.allocTypedNode(NODE_TYPE_COMMENT, parentBeg, parentEnd);
            this.astType = AST_TYPE_COMMENT;
            return head2;
          }
          this.hasUppercase = this.reHasUppercaseChar.test(parentStr);
          this.hasUnicode = this.reHasUnicodeChar.test(parentStr);
          this.astType = AST_TYPE_NETWORK;
          let tail = 0, tailStart = parentEnd;
          if (this.hasWhitespace && this.reInlineComment.test(parentStr)) {
            const match = this.reInlineComment.exec(parentStr);
            tailStart = parentBeg + match.index;
            tail = this.allocTypedNode(NODE_TYPE_COMMENT, tailStart, parentEnd);
          }
          const head = this.allocTypedNode(NODE_TYPE_NET_RAW, parentBeg, tailStart);
          if (this.linkDown(head, this.parseNet(head)) === 0) {
            this.astType = AST_TYPE_UNKNOWN;
            this.addFlags(AST_FLAG_UNSUPPORTED | AST_FLAG_HAS_ERROR);
          }
          if (tail !== 0) {
            this.linkRight(head, tail);
          }
          return head;
        }
        parseComment(parent) {
          const parentStr = this.getNodeString(parent);
          if (this.rePreparseDirectiveAny.test(parentStr)) {
            this.astTypeFlavor = AST_TYPE_COMMENT_PREPARSER;
            return this.parsePreparseDirective(parent, parentStr);
          }
          if (this.reURL.test(parentStr) === false) {
            return 0;
          }
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          const match = this.reURL.exec(parentStr);
          const urlBeg = parentBeg + match.index;
          const urlEnd = urlBeg + match[0].length;
          const head = this.allocTypedNode(NODE_TYPE_COMMENT, parentBeg, urlBeg);
          let next = this.allocTypedNode(NODE_TYPE_COMMENT_URL, urlBeg, urlEnd);
          const prev = this.linkRight(head, next);
          if (urlEnd !== parentEnd) {
            next = this.allocTypedNode(NODE_TYPE_COMMENT, urlEnd, parentEnd);
            this.linkRight(prev, next);
          }
          return head;
        }
        parsePreparseDirective(parent, s) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          const match = this.rePreparseDirectiveAny.exec(s);
          const directiveEnd = parentBeg + match[0].length;
          const head = this.allocTypedNode(
            NODE_TYPE_PREPARSE_DIRECTIVE,
            parentBeg,
            directiveEnd
          );
          if (directiveEnd !== parentEnd) {
            const type = s.startsWith("!#if ") ? NODE_TYPE_PREPARSE_DIRECTIVE_IF_VALUE : NODE_TYPE_PREPARSE_DIRECTIVE_VALUE;
            const next = this.allocTypedNode(type, directiveEnd, parentEnd);
            this.addNodeToRegister(type, next);
            this.linkRight(head, next);
            if (type === NODE_TYPE_PREPARSE_DIRECTIVE_IF_VALUE) {
              const rawToken = this.getNodeString(next).trim();
              if (utils.preparser.evaluateExpr(rawToken) === void 0) {
                this.addNodeFlags(next, NODE_FLAG_ERROR);
                this.addFlags(AST_FLAG_HAS_ERROR);
                this.astError = AST_ERROR_IF_TOKEN_UNKNOWN;
              }
            }
          }
          return head;
        }
        // Very common, look into fast-tracking such plain pattern:
        // /^[^!#\$\*\^][^#\$\*\^]*[^\$\*\|]$/
        parseNet(parent) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          const parentStr = this.getNodeString(parent);
          const head = this.allocHeadNode();
          let patternBeg = parentBeg;
          let prev = head, next = 0, tail = 0;
          if (this.reNetException.test(parentStr)) {
            this.addFlags(AST_FLAG_IS_EXCEPTION);
            next = this.allocTypedNode(NODE_TYPE_NET_EXCEPTION, parentBeg, parentBeg + 2);
            prev = this.linkRight(prev, next);
            patternBeg += 2;
          }
          let anchorBeg = this.indexOfNetAnchor(parentStr);
          if (anchorBeg === -1) {
            return 0;
          }
          anchorBeg += parentBeg;
          if (anchorBeg !== parentEnd) {
            tail = this.allocTypedNode(
              NODE_TYPE_NET_OPTIONS_ANCHOR,
              anchorBeg,
              anchorBeg + 1
            );
            next = this.allocTypedNode(
              NODE_TYPE_NET_OPTIONS,
              anchorBeg + 1,
              parentEnd
            );
            this.addFlags(AST_FLAG_HAS_OPTIONS);
            this.addNodeToRegister(NODE_TYPE_NET_OPTIONS, next);
            this.linkDown(next, this.parseNetOptions(next));
            this.linkRight(tail, next);
          }
          next = this.allocTypedNode(
            NODE_TYPE_NET_PATTERN_RAW,
            patternBeg,
            anchorBeg
          );
          this.addNodeToRegister(NODE_TYPE_NET_PATTERN_RAW, next);
          this.linkDown(next, this.parseNetPattern(next));
          prev = this.linkRight(prev, next);
          if (tail !== 0) {
            this.linkRight(prev, tail);
          }
          if (this.astType === AST_TYPE_NETWORK) {
            this.validateNet();
          }
          return this.throwHeadNode(head);
        }
        validateNet() {
          const isException = this.isException();
          let bad = false, realBad = false;
          let abstractTypeCount = 0;
          let behaviorTypeCount = 0;
          let docTypeCount = 0;
          let modifierType = 0;
          let requestTypeCount = 0;
          let unredirectableTypeCount = 0;
          let isBadfilter = false;
          for (let i = 0, n = this.nodeTypeRegisterPtr; i < n; i++) {
            const type = this.nodeTypeRegister[i];
            const targetNode = this.nodeTypeLookupTable[type];
            if (targetNode === 0) {
              continue;
            }
            if (this.badTypes.has(type)) {
              this.addNodeFlags(NODE_FLAG_ERROR);
              this.addFlags(AST_FLAG_HAS_ERROR);
              this.astError = AST_ERROR_OPTION_EXCLUDED;
            }
            const flags = this.getNodeFlags(targetNode);
            if ((flags & NODE_FLAG_ERROR) !== 0) {
              continue;
            }
            const isNegated = (flags & NODE_FLAG_IS_NEGATED) !== 0;
            const hasValue = (flags & NODE_FLAG_OPTION_HAS_VALUE) !== 0;
            bad = false;
            realBad = false;
            switch (type) {
              case NODE_TYPE_NET_OPTION_NAME_ALL:
                realBad = isNegated || hasValue || modifierType !== 0;
                break;
              case NODE_TYPE_NET_OPTION_NAME_1P:
              case NODE_TYPE_NET_OPTION_NAME_3P:
                realBad = hasValue;
                break;
              case NODE_TYPE_NET_OPTION_NAME_BADFILTER:
                isBadfilter = true;
              /* falls through */
              case NODE_TYPE_NET_OPTION_NAME_NOOP:
                realBad = isNegated || hasValue;
                break;
              case NODE_TYPE_NET_OPTION_NAME_CSS:
              case NODE_TYPE_NET_OPTION_NAME_FONT:
              case NODE_TYPE_NET_OPTION_NAME_IMAGE:
              case NODE_TYPE_NET_OPTION_NAME_MEDIA:
              case NODE_TYPE_NET_OPTION_NAME_OTHER:
              case NODE_TYPE_NET_OPTION_NAME_SCRIPT:
              case NODE_TYPE_NET_OPTION_NAME_XHR:
                realBad = hasValue;
                if (realBad) {
                  break;
                }
                requestTypeCount += 1;
                break;
              case NODE_TYPE_NET_OPTION_NAME_CNAME:
                realBad = isException === false || isNegated || hasValue;
                if (realBad) {
                  break;
                }
                modifierType = type;
                break;
              case NODE_TYPE_NET_OPTION_NAME_CSP:
                realBad = (hasValue || isException) === false || modifierType !== 0 || this.reBadCSP.test(
                  this.getNetOptionValue(NODE_TYPE_NET_OPTION_NAME_CSP)
                );
                if (realBad) {
                  break;
                }
                modifierType = type;
                break;
              case NODE_TYPE_NET_OPTION_NAME_DENYALLOW:
                realBad = isNegated || hasValue === false || this.getBranchFromType(NODE_TYPE_NET_OPTION_NAME_FROM) === 0;
                break;
              case NODE_TYPE_NET_OPTION_NAME_DOC:
              case NODE_TYPE_NET_OPTION_NAME_FRAME:
              case NODE_TYPE_NET_OPTION_NAME_OBJECT:
                realBad = hasValue;
                if (realBad) {
                  break;
                }
                docTypeCount += 1;
                break;
              case NODE_TYPE_NET_OPTION_NAME_EHIDE:
              case NODE_TYPE_NET_OPTION_NAME_GHIDE:
              case NODE_TYPE_NET_OPTION_NAME_SHIDE:
                realBad = isNegated || hasValue || modifierType !== 0;
                if (realBad) {
                  break;
                }
                behaviorTypeCount += 1;
                unredirectableTypeCount += 1;
                break;
              case NODE_TYPE_NET_OPTION_NAME_EMPTY:
              case NODE_TYPE_NET_OPTION_NAME_MP4:
                realBad = isNegated || hasValue || modifierType !== 0;
                if (realBad) {
                  break;
                }
                modifierType = type;
                break;
              case NODE_TYPE_NET_OPTION_NAME_FROM:
              case NODE_TYPE_NET_OPTION_NAME_METHOD:
              case NODE_TYPE_NET_OPTION_NAME_TO:
                realBad = isNegated || hasValue === false;
                break;
              case NODE_TYPE_NET_OPTION_NAME_GENERICBLOCK:
                bad = true;
                realBad = isException === false || isNegated || hasValue;
                break;
              case NODE_TYPE_NET_OPTION_NAME_IMPORTANT:
                realBad = isException || isNegated || hasValue;
                break;
              case NODE_TYPE_NET_OPTION_NAME_INLINEFONT:
              case NODE_TYPE_NET_OPTION_NAME_INLINESCRIPT:
                realBad = hasValue;
                if (realBad) {
                  break;
                }
                modifierType = type;
                unredirectableTypeCount += 1;
                break;
              case NODE_TYPE_NET_OPTION_NAME_IPADDRESS: {
                const value = this.getNetOptionValue(NODE_TYPE_NET_OPTION_NAME_IPADDRESS);
                if (/^\/.+\/$/.test(value)) {
                  try {
                    void new RegExp(value);
                  } catch {
                    realBad = true;
                  }
                }
                break;
              }
              case NODE_TYPE_NET_OPTION_NAME_MATCHCASE:
                realBad = this.isRegexPattern() === false;
                break;
              case NODE_TYPE_NET_OPTION_NAME_PERMISSIONS:
                realBad = modifierType !== 0 || (hasValue || isException) === false || this.reBadPP.test(
                  this.getNetOptionValue(NODE_TYPE_NET_OPTION_NAME_PERMISSIONS)
                );
                if (realBad) {
                  break;
                }
                modifierType = type;
                break;
              case NODE_TYPE_NET_OPTION_NAME_PING:
              case NODE_TYPE_NET_OPTION_NAME_WEBSOCKET:
                realBad = hasValue;
                if (realBad) {
                  break;
                }
                requestTypeCount += 1;
                if ((flags & NODE_FLAG_IS_NEGATED) === 0) {
                  unredirectableTypeCount += 1;
                }
                break;
              case NODE_TYPE_NET_OPTION_NAME_POPUNDER:
              case NODE_TYPE_NET_OPTION_NAME_POPUP:
                realBad = hasValue;
                if (realBad) {
                  break;
                }
                abstractTypeCount += 1;
                unredirectableTypeCount += 1;
                break;
              case NODE_TYPE_NET_OPTION_NAME_REASON:
                realBad = hasValue === false;
                break;
              case NODE_TYPE_NET_OPTION_NAME_REDIRECT:
              case NODE_TYPE_NET_OPTION_NAME_REDIRECTRULE:
              case NODE_TYPE_NET_OPTION_NAME_REPLACE:
              case NODE_TYPE_NET_OPTION_NAME_URLSKIP:
              case NODE_TYPE_NET_OPTION_NAME_URLTRANSFORM:
                realBad = isNegated || (isException || hasValue) === false || modifierType !== 0;
                if (realBad) {
                  break;
                }
                modifierType = type;
                break;
              case NODE_TYPE_NET_OPTION_NAME_REMOVEPARAM:
                realBad = isNegated || modifierType !== 0;
                if (realBad) {
                  break;
                }
                modifierType = type;
                break;
              case NODE_TYPE_NET_OPTION_NAME_REQUESTHEADER:
              case NODE_TYPE_NET_OPTION_NAME_RESPONSEHEADER:
                realBad = isNegated || hasValue === false;
                break;
              case NODE_TYPE_NET_OPTION_NAME_STRICT1P:
              case NODE_TYPE_NET_OPTION_NAME_STRICT3P:
                realBad = isNegated || hasValue;
                break;
              case NODE_TYPE_NET_OPTION_NAME_UNKNOWN:
                this.astError = AST_ERROR_OPTION_UNKNOWN;
                realBad = true;
                break;
              case NODE_TYPE_NET_OPTION_NAME_WEBRTC:
                realBad = true;
                break;
              case NODE_TYPE_NET_PATTERN_RAW:
                realBad = this.hasOptions() === false && this.getNetPattern().length <= 1;
                break;
              default:
                break;
            }
            if (bad || realBad) {
              this.addNodeFlags(targetNode, NODE_FLAG_ERROR);
            }
            if (realBad) {
              this.addFlags(AST_FLAG_HAS_ERROR);
            }
          }
          switch (modifierType) {
            case NODE_TYPE_NET_OPTION_NAME_CNAME:
              realBad = abstractTypeCount || behaviorTypeCount || requestTypeCount;
              break;
            case NODE_TYPE_NET_OPTION_NAME_CSP:
            case NODE_TYPE_NET_OPTION_NAME_PERMISSIONS:
              realBad = abstractTypeCount || behaviorTypeCount || requestTypeCount;
              break;
            case NODE_TYPE_NET_OPTION_NAME_INLINEFONT:
            case NODE_TYPE_NET_OPTION_NAME_INLINESCRIPT:
              realBad = behaviorTypeCount;
              break;
            case NODE_TYPE_NET_OPTION_NAME_EMPTY:
              realBad = abstractTypeCount || behaviorTypeCount;
              break;
            case NODE_TYPE_NET_OPTION_NAME_MEDIA:
            case NODE_TYPE_NET_OPTION_NAME_MP4:
              realBad = abstractTypeCount || behaviorTypeCount || docTypeCount || requestTypeCount;
              break;
            case NODE_TYPE_NET_OPTION_NAME_REDIRECT:
            case NODE_TYPE_NET_OPTION_NAME_REDIRECTRULE: {
              realBad = abstractTypeCount || behaviorTypeCount || unredirectableTypeCount;
              break;
            }
            case NODE_TYPE_NET_OPTION_NAME_REPLACE: {
              realBad = abstractTypeCount || behaviorTypeCount || unredirectableTypeCount;
              if (realBad) {
                break;
              }
              if (isException || isBadfilter) {
                break;
              }
              if (this.options.trustedSource !== true) {
                this.astError = AST_ERROR_UNTRUSTED_SOURCE;
                realBad = true;
                break;
              }
              const value = this.getNetOptionValue(NODE_TYPE_NET_OPTION_NAME_REPLACE);
              if (parseReplaceValue(value) === void 0) {
                this.astError = AST_ERROR_OPTION_BADVALUE;
                realBad = true;
              }
              break;
            }
            case NODE_TYPE_NET_OPTION_NAME_URLSKIP: {
              realBad = abstractTypeCount || behaviorTypeCount || unredirectableTypeCount;
              if (realBad) {
                break;
              }
              if (isException || isBadfilter) {
                break;
              }
              if (this.options.trustedSource !== true) {
                this.astError = AST_ERROR_UNTRUSTED_SOURCE;
                realBad = true;
                break;
              }
              const value = this.getNetOptionValue(NODE_TYPE_NET_OPTION_NAME_URLSKIP);
              if (value.length < 2) {
                this.astError = AST_ERROR_OPTION_BADVALUE;
                realBad = true;
              }
              break;
            }
            case NODE_TYPE_NET_OPTION_NAME_URLTRANSFORM: {
              realBad = abstractTypeCount || behaviorTypeCount || unredirectableTypeCount;
              if (realBad) {
                break;
              }
              if (isException || isBadfilter) {
                break;
              }
              if (this.options.trustedSource !== true) {
                this.astError = AST_ERROR_UNTRUSTED_SOURCE;
                realBad = true;
                break;
              }
              const value = this.getNetOptionValue(NODE_TYPE_NET_OPTION_NAME_URLTRANSFORM);
              if (value !== "" && parseReplaceByRegexValue(value) === void 0) {
                this.astError = AST_ERROR_OPTION_BADVALUE;
                realBad = true;
              }
              break;
            }
            case NODE_TYPE_NET_OPTION_NAME_REMOVEPARAM:
              realBad = abstractTypeCount || behaviorTypeCount;
              break;
            default:
              break;
          }
          if (realBad) {
            const targetNode = this.getBranchFromType(modifierType);
            this.addNodeFlags(targetNode, NODE_FLAG_ERROR);
            this.addFlags(AST_FLAG_HAS_ERROR);
          }
        }
        indexOfNetAnchor(s) {
          const end = s.length;
          if (end === 0) {
            return end;
          }
          let j2 = s.lastIndexOf("$");
          if (j2 === -1) {
            return end;
          }
          if (j2 + 1 === end) {
            return end;
          }
          for (; ; ) {
            const before = s.charAt(j2 - 1);
            if (before === "$") {
              return -1;
            }
            if (this.reNetOptionTokens.test(s.slice(j2 + 1))) {
              return j2;
            }
            if (j2 === 0) {
              break;
            }
            j2 = s.lastIndexOf("$", j2 - 1);
            if (j2 === -1) {
              break;
            }
          }
          return end;
        }
        parseNetPattern(parent) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          if (parentEnd === parentBeg) {
            this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_ANY;
            const node = this.allocTypedNode(
              NODE_TYPE_NET_PATTERN,
              parentBeg,
              parentEnd
            );
            this.addNodeToRegister(NODE_TYPE_NET_PATTERN, node);
            this.setNodeTransform(node, "*");
            return node;
          }
          const head = this.allocHeadNode();
          let prev = head, next = 0, tail = 0;
          let pattern = this.getNodeString(parent);
          const hasWildcard = pattern.includes("*");
          const c1st = pattern.charCodeAt(0);
          const c2nd = pattern.charCodeAt(1) || 0;
          const clast = exCharCodeAt(pattern, -1);
          if (hasWildcard === false && c1st === 124 && c2nd === 124 && clast === 94 && this.isAdblockHostnamePattern(pattern)) {
            this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_HOSTNAME;
            this.addFlags(
              AST_FLAG_NET_PATTERN_LEFT_HNANCHOR | AST_FLAG_NET_PATTERN_RIGHT_PATHANCHOR
            );
            next = this.allocTypedNode(
              NODE_TYPE_NET_PATTERN_LEFT_HNANCHOR,
              parentBeg,
              parentBeg + 2
            );
            prev = this.linkRight(prev, next);
            next = this.allocTypedNode(
              NODE_TYPE_NET_PATTERN,
              parentBeg + 2,
              parentEnd - 1
            );
            pattern = pattern.slice(2, -1);
            const normal2 = this.hasUnicode ? this.normalizeHostnameValue(pattern) : pattern;
            if (normal2 !== void 0 && normal2 !== pattern) {
              this.setNodeTransform(next, normal2);
            }
            this.addNodeToRegister(NODE_TYPE_NET_PATTERN, next);
            prev = this.linkRight(prev, next);
            next = this.allocTypedNode(
              NODE_TYPE_NET_PATTERN_PART_SPECIAL,
              parentEnd - 1,
              parentEnd
            );
            this.linkRight(prev, next);
            return this.throwHeadNode(head);
          }
          let patternBeg = parentBeg;
          let patternEnd = parentEnd;
          if (this.hasWhitespace && this.isException() === false && this.hasOptions() === false && this.reHostsSink.test(pattern)) {
            const match = this.reHostsSink.exec(pattern);
            patternBeg += match[0].length;
            pattern = pattern.slice(patternBeg);
            next = this.allocTypedNode(NODE_TYPE_IGNORE, parentBeg, patternBeg);
            prev = this.linkRight(prev, next);
            if (this.reHostsRedirect.test(pattern) || this.reHostnameAscii.test(pattern) === false) {
              this.astType = AST_TYPE_NONE;
              this.addFlags(AST_FLAG_IGNORE);
              next = this.allocTypedNode(NODE_TYPE_IGNORE, patternBeg, parentEnd);
              prev = this.linkRight(prev, next);
              return this.throwHeadNode(head);
            }
            this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_HOSTNAME;
            this.addFlags(
              AST_FLAG_NET_PATTERN_LEFT_HNANCHOR | AST_FLAG_NET_PATTERN_RIGHT_PATHANCHOR
            );
            next = this.allocTypedNode(
              NODE_TYPE_NET_PATTERN,
              patternBeg,
              parentEnd
            );
            this.addNodeToRegister(NODE_TYPE_NET_PATTERN, next);
            this.linkRight(prev, next);
            return this.throwHeadNode(head);
          }
          if (c1st === 47 && clast === 47 && pattern.length > 2) {
            this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_REGEX;
            const normal2 = this.normalizeRegexPattern(pattern);
            next = this.allocTypedNode(
              NODE_TYPE_NET_PATTERN,
              patternBeg,
              patternEnd
            );
            this.addNodeToRegister(NODE_TYPE_NET_PATTERN, next);
            if (normal2 !== "") {
              if (normal2 !== pattern) {
                this.setNodeTransform(next, normal2);
              }
            } else {
              this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_BAD;
              this.astError = AST_ERROR_REGEX;
              this.addFlags(AST_FLAG_HAS_ERROR);
              this.addNodeFlags(next, NODE_FLAG_ERROR);
            }
            this.linkRight(prev, next);
            return this.throwHeadNode(head);
          }
          if (c1st === 124) {
            if (c2nd === 124) {
              const type = this.isTokenCharCode(pattern.charCodeAt(2) || 0) ? NODE_TYPE_NET_PATTERN_LEFT_HNANCHOR : NODE_TYPE_IGNORE;
              next = this.allocTypedNode(type, patternBeg, patternBeg + 2);
              if (type === NODE_TYPE_NET_PATTERN_LEFT_HNANCHOR) {
                this.addFlags(AST_FLAG_NET_PATTERN_LEFT_HNANCHOR);
              }
              patternBeg += 2;
              pattern = pattern.slice(2);
            } else {
              const type = this.isTokenCharCode(c2nd) ? NODE_TYPE_NET_PATTERN_LEFT_ANCHOR : NODE_TYPE_IGNORE;
              next = this.allocTypedNode(type, patternBeg, patternBeg + 1);
              if (type === NODE_TYPE_NET_PATTERN_LEFT_ANCHOR) {
                this.addFlags(AST_FLAG_NET_PATTERN_LEFT_ANCHOR);
              }
              patternBeg += 1;
              pattern = pattern.slice(1);
            }
            prev = this.linkRight(prev, next);
            if (patternBeg === patternEnd) {
              this.addNodeFlags(next, NODE_FLAG_IGNORE);
            }
          }
          if (exCharCodeAt(pattern, -1) === 124) {
            const type = exCharCodeAt(pattern, -2) !== 42 ? NODE_TYPE_NET_PATTERN_RIGHT_ANCHOR : NODE_TYPE_IGNORE;
            tail = this.allocTypedNode(type, patternEnd - 1, patternEnd);
            if (type === NODE_TYPE_NET_PATTERN_RIGHT_ANCHOR) {
              this.addFlags(AST_FLAG_NET_PATTERN_RIGHT_ANCHOR);
            }
            patternEnd -= 1;
            pattern = pattern.slice(0, -1);
            if (patternEnd === patternBeg) {
              this.addNodeFlags(tail, NODE_FLAG_IGNORE);
            }
          }
          if (hasWildcard && this.rePointlessLeadingWildcards.test(pattern)) {
            const match = this.rePointlessLeadingWildcards.exec(pattern);
            const ignoreLen = match[1].length;
            next = this.allocTypedNode(
              NODE_TYPE_IGNORE,
              patternBeg,
              patternBeg + ignoreLen
            );
            prev = this.linkRight(prev, next);
            patternBeg += ignoreLen;
            pattern = pattern.slice(ignoreLen);
          }
          if (this.rePointlessTrailingSeparator.test(pattern)) {
            const match = this.rePointlessTrailingSeparator.exec(pattern);
            const ignoreLen = match[1].length;
            next = this.allocTypedNode(
              NODE_TYPE_IGNORE,
              patternEnd - ignoreLen,
              patternEnd
            );
            patternEnd -= ignoreLen;
            pattern = pattern.slice(0, -ignoreLen);
            if (tail !== 0) {
              this.linkRight(next, tail);
            }
            tail = next;
          }
          if (hasWildcard && this.rePointlessTrailingWildcards.test(pattern)) {
            const match = this.rePointlessTrailingWildcards.exec(pattern);
            const ignoreLen = match[1].length;
            const needWildcard = pattern.charCodeAt(0) === 47 && exCharCodeAt(pattern, -ignoreLen - 1) === 47;
            const goodWildcardBeg = patternEnd - ignoreLen;
            const badWildcardBeg = goodWildcardBeg + (needWildcard ? 1 : 0);
            if (badWildcardBeg !== patternEnd) {
              next = this.allocTypedNode(
                NODE_TYPE_IGNORE,
                badWildcardBeg,
                patternEnd
              );
              if (tail !== 0) {
                this.linkRight(next, tail);
              }
              tail = next;
            }
            if (goodWildcardBeg !== badWildcardBeg) {
              next = this.allocTypedNode(
                NODE_TYPE_NET_PATTERN_PART_SPECIAL,
                goodWildcardBeg,
                badWildcardBeg
              );
              if (tail !== 0) {
                this.linkRight(next, tail);
              }
              tail = next;
            }
            patternEnd -= ignoreLen;
            pattern = pattern.slice(0, -ignoreLen);
          }
          const patternHasWhitespace = this.hasWhitespace && this.reHasWhitespaceChar.test(pattern);
          const needNormalization = this.needPatternNormalization(pattern);
          const normal = needNormalization ? this.normalizePattern(pattern) : pattern;
          next = this.allocTypedNode(NODE_TYPE_NET_PATTERN, patternBeg, patternEnd);
          if (patternHasWhitespace || normal === void 0) {
            this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_BAD;
            this.addFlags(AST_FLAG_HAS_ERROR);
            this.astError = AST_ERROR_PATTERN;
            this.addNodeFlags(next, NODE_FLAG_ERROR);
          } else if (normal === "*") {
            this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_ANY;
          } else if (this.reHostnameAscii.test(normal)) {
            this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_HOSTNAME;
          } else if (this.reHasPatternSpecialChars.test(normal)) {
            this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_GENERIC;
          } else {
            this.astTypeFlavor = AST_TYPE_NETWORK_PATTERN_PLAIN;
          }
          this.addNodeToRegister(NODE_TYPE_NET_PATTERN, next);
          if (needNormalization && normal !== void 0) {
            this.setNodeTransform(next, normal);
          }
          if (this.interactive) {
            this.linkDown(next, this.parsePatternParts(next, pattern));
          }
          prev = this.linkRight(prev, next);
          if (tail !== 0) {
            this.linkRight(prev, tail);
          }
          return this.throwHeadNode(head);
        }
        isAdblockHostnamePattern(pattern) {
          if (this.hasUnicode) {
            return this.reHnAnchoredHostnameUnicode.test(pattern);
          }
          return this.reHnAnchoredHostnameAscii.test(pattern);
        }
        parsePatternParts(parent, pattern) {
          if (pattern.length === 0) {
            return 0;
          }
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const matches = pattern.matchAll(this.rePatternAllSpecialChars);
          const head = this.allocHeadNode();
          let prev = head, next = 0;
          let plainPartBeg = 0;
          for (const match of matches) {
            const plainPartEnd = match.index;
            if (plainPartEnd !== plainPartBeg) {
              next = this.allocTypedNode(
                NODE_TYPE_NET_PATTERN_PART,
                parentBeg + plainPartBeg,
                parentBeg + plainPartEnd
              );
              prev = this.linkRight(prev, next);
            }
            plainPartBeg = plainPartEnd + match[0].length;
            const type = match[0].charCodeAt(0) < 128 ? NODE_TYPE_NET_PATTERN_PART_SPECIAL : NODE_TYPE_NET_PATTERN_PART_UNICODE;
            next = this.allocTypedNode(
              type,
              parentBeg + plainPartEnd,
              parentBeg + plainPartBeg
            );
            prev = this.linkRight(prev, next);
          }
          if (plainPartBeg !== pattern.length) {
            next = this.allocTypedNode(
              NODE_TYPE_NET_PATTERN_PART,
              parentBeg + plainPartBeg,
              parentBeg + pattern.length
            );
            this.linkRight(prev, next);
          }
          return this.throwHeadNode(head);
        }
        // https://github.com/uBlockOrigin/uBlock-issues/issues/1118#issuecomment-650730158
        //   Be ready to deal with non-punycode-able Unicode characters.
        // https://github.com/uBlockOrigin/uBlock-issues/issues/772
        //   Encode Unicode characters beyond the hostname part.
        // Prepend with '*' character to prevent the browser API from refusing to
        // punycode -- this occurs when the extracted label starts with a dash.
        needPatternNormalization(pattern) {
          return pattern.length === 0 || this.hasUppercase || this.hasUnicode;
        }
        normalizePattern(pattern) {
          if (pattern.length === 0) {
            return "*";
          }
          if (this.reHasInvalidChar.test(pattern)) {
            return;
          }
          let normal = pattern.toLowerCase();
          if (this.hasUnicode === false) {
            return normal;
          }
          if (this.reHostnamePatternPart.test(normal)) {
            const match = this.reHostnamePatternPart.exec(normal);
            const hn2 = match[0].replace(this.reHostnameLabel, (s) => {
              if (this.reHasUnicodeChar.test(s) === false) {
                return s;
              }
              if (s.charCodeAt(0) === 45) {
                s = `*${s}`;
              }
              return this.normalizeHostnameValue(s, DOMAIN_CAN_USE_WILDCARD) || s;
            });
            normal = hn2 + normal.slice(match.index + match[0].length);
          }
          if (this.reHasUnicodeChar.test(normal) === false) {
            return normal;
          }
          try {
            normal = normal.replace(
              this.reUnicodeChars,
              (s) => encodeURIComponent(s).toLowerCase()
            );
          } catch (e) {
            console.warn("[uBR] static-filtering-parser: regex replacement failed", e);
            return;
          }
          return normal;
        }
        getNetPattern() {
          const node = this.nodeTypeLookupTable[NODE_TYPE_NET_PATTERN];
          return this.getNodeTransform(node);
        }
        isAnyPattern() {
          return this.astTypeFlavor === AST_TYPE_NETWORK_PATTERN_ANY;
        }
        isHostnamePattern() {
          return this.astTypeFlavor === AST_TYPE_NETWORK_PATTERN_HOSTNAME;
        }
        isRegexPattern() {
          return this.astTypeFlavor === AST_TYPE_NETWORK_PATTERN_REGEX;
        }
        isPlainPattern() {
          return this.astTypeFlavor === AST_TYPE_NETWORK_PATTERN_PLAIN;
        }
        isGenericPattern() {
          return this.astTypeFlavor === AST_TYPE_NETWORK_PATTERN_GENERIC;
        }
        isBadPattern() {
          return this.astTypeFlavor === AST_TYPE_NETWORK_PATTERN_BAD;
        }
        parseNetOptions(parent) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          if (parentEnd === parentBeg) {
            return 0;
          }
          const s = this.getNodeString(parent);
          const optionsEnd = s.length;
          const parseDetails = { node: 0, len: 0 };
          const head = this.allocHeadNode();
          let prev = head, next = 0;
          let optionBeg = 0, optionEnd = 0;
          while (optionBeg !== optionsEnd) {
            next = this.allocTypedNode(
              NODE_TYPE_NET_OPTION_RAW,
              parentBeg + optionBeg,
              parentBeg + optionsEnd
              // open ended
            );
            this.parseNetOption(next, parseDetails);
            optionEnd += parseDetails.len;
            this.nodes[next + NODE_END_INDEX] = parentBeg + optionEnd;
            this.linkDown(next, parseDetails.node);
            prev = this.linkRight(prev, next);
            if (optionEnd === optionsEnd) {
              break;
            }
            optionBeg = optionEnd + 1;
            next = this.allocTypedNode(
              NODE_TYPE_NET_OPTION_SEPARATOR,
              parentBeg + optionEnd,
              parentBeg + optionBeg
            );
            if (parseDetails.len === 0 || optionBeg === optionsEnd) {
              this.addNodeFlags(next, NODE_FLAG_ERROR);
              this.addFlags(AST_FLAG_HAS_ERROR);
            }
            prev = this.linkRight(prev, next);
            optionEnd = optionBeg;
          }
          this.linkRight(
            prev,
            this.allocSentinelNode(NODE_TYPE_NET_OPTION_SENTINEL, parentEnd)
          );
          return this.throwHeadNode(head);
        }
        parseNetOption(parent, parseDetails) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const s = this.getNodeString(parent);
          const match = this.reNetOption.exec(s) || [];
          if (match.length === 0) {
            this.addNodeFlags(parent, NODE_FLAG_ERROR);
            this.addFlags(AST_FLAG_HAS_ERROR);
            this.astError = AST_ERROR_OPTION_UNKNOWN;
            parseDetails.node = 0;
            parseDetails.len = s.length;
            return;
          }
          const head = this.allocHeadNode();
          let prev = head, next = 0;
          const matchEnd = match && match[0].length || 0;
          const negated = match[1] === "~";
          if (negated) {
            this.addNodeFlags(parent, NODE_FLAG_IS_NEGATED);
            next = this.allocTypedNode(
              NODE_TYPE_NET_OPTION_NAME_NOT,
              parentBeg,
              parentBeg + 1
            );
            prev = this.linkRight(prev, next);
          }
          const nameBeg = negated ? 1 : 0;
          const assigned = match[3] === "=";
          const nameEnd = matchEnd - (assigned ? 1 : 0);
          const name2 = match[2] || "";
          let nodeOptionType = nodeTypeFromOptionName.get(name2);
          if (nodeOptionType === void 0) {
            nodeOptionType = this.reNoopOption.test(name2) ? NODE_TYPE_NET_OPTION_NAME_NOOP : NODE_TYPE_NET_OPTION_NAME_UNKNOWN;
          }
          next = this.allocTypedNode(
            nodeOptionType,
            parentBeg + nameBeg,
            parentBeg + nameEnd
          );
          if (nodeOptionType !== NODE_TYPE_NET_OPTION_NAME_NOOP && this.getBranchFromType(nodeOptionType) !== 0) {
            this.addNodeFlags(parent, NODE_FLAG_ERROR);
            this.addFlags(AST_FLAG_HAS_ERROR);
            this.astError = AST_ERROR_OPTION_DUPLICATE;
          } else {
            this.addNodeToRegister(nodeOptionType, parent);
          }
          prev = this.linkRight(prev, next);
          if (assigned === false) {
            parseDetails.node = this.throwHeadNode(head);
            parseDetails.len = matchEnd;
            return;
          }
          next = this.allocTypedNode(
            NODE_TYPE_NET_OPTION_ASSIGN,
            parentBeg + matchEnd - 1,
            parentBeg + matchEnd
          );
          prev = this.linkRight(prev, next);
          this.addNodeFlags(parent, NODE_FLAG_OPTION_HAS_VALUE);
          const details = this.netOptionValueParser.nextArg(s, matchEnd);
          if (details.quoteBeg !== details.argBeg) {
            next = this.allocTypedNode(
              NODE_TYPE_NET_OPTION_QUOTE,
              parentBeg + details.quoteBeg,
              parentBeg + details.argBeg
            );
            prev = this.linkRight(prev, next);
          } else {
            const argEnd = this.endOfNetOption(s, matchEnd);
            if (argEnd !== details.argEnd) {
              details.argEnd = details.quoteEnd = argEnd;
            }
          }
          next = this.allocTypedNode(
            NODE_TYPE_NET_OPTION_VALUE,
            parentBeg + details.argBeg,
            parentBeg + details.argEnd
          );
          if (details.argBeg === details.argEnd) {
            this.addNodeFlags(parent, NODE_FLAG_ERROR);
            this.addFlags(AST_FLAG_HAS_ERROR);
            this.astError = AST_ERROR_OPTION_BADVALUE;
          } else if (details.transform) {
            const arg = s.slice(details.argBeg, details.argEnd);
            this.setNodeTransform(next, this.netOptionValueParser.normalizeArg(arg));
          }
          switch (nodeOptionType) {
            case NODE_TYPE_NET_OPTION_NAME_DENYALLOW:
              this.linkDown(next, this.parseDomainList(next, "|"), DOMAIN_FROM_DENYALLOW_LIST);
              break;
            case NODE_TYPE_NET_OPTION_NAME_FROM:
            case NODE_TYPE_NET_OPTION_NAME_TO:
              this.linkDown(next, this.parseDomainList(next, "|", DOMAIN_FROM_FROMTO_LIST));
              break;
            default:
              break;
          }
          prev = this.linkRight(prev, next);
          if (details.quoteEnd !== details.argEnd) {
            next = this.allocTypedNode(
              NODE_TYPE_NET_OPTION_QUOTE,
              parentBeg + details.argEnd,
              parentBeg + details.quoteEnd
            );
            this.linkRight(prev, next);
          }
          parseDetails.node = this.throwHeadNode(head);
          parseDetails.len = details.quoteEnd;
        }
        endOfNetOption(s, beg) {
          const match = this.reNetOptionComma.exec(s.slice(beg));
          return match !== null ? beg + match.index : s.length;
        }
        getNetOptionValue(type) {
          if (this.nodeTypeRegister.includes(type) === false) {
            return "";
          }
          const optionNode = this.nodeTypeLookupTable[type];
          if (optionNode === 0) {
            return "";
          }
          const valueNode = this.findDescendantByType(optionNode, NODE_TYPE_NET_OPTION_VALUE);
          if (valueNode === 0) {
            return "";
          }
          return this.getNodeTransform(valueNode);
        }
        parseDomainList(parent, separator, mode = 0) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          const containerNode = this.allocTypedNode(
            NODE_TYPE_OPTION_VALUE_DOMAIN_LIST,
            parentBeg,
            parentEnd
          );
          if (parentEnd === parentBeg) {
            return containerNode;
          }
          const separatorCode = separator.charCodeAt(0);
          const parseDetails = { separator, mode, node: 0, len: 0 };
          const listNode = this.allocHeadNode();
          let prev = listNode;
          let domainNode = 0;
          let separatorNode = 0;
          const s = this.getNodeString(parent);
          const listEnd = s.length;
          let beg = 0, end = 0;
          while (beg < listEnd) {
            const next = this.allocTypedNode(
              NODE_TYPE_OPTION_VALUE_DOMAIN_RAW,
              parentBeg + beg,
              parentBeg + listEnd
              // open ended
            );
            this.parseDomain(next, parseDetails);
            end = beg + parseDetails.len;
            const badSeparator = end < listEnd && s.charCodeAt(end) !== separatorCode;
            if (badSeparator) {
              end = s.indexOf(separator, end);
              if (end === -1) {
                end = listEnd;
              }
            }
            this.nodes[next + NODE_END_INDEX] = parentBeg + end;
            if (end !== beg) {
              domainNode = next;
              this.linkDown(domainNode, parseDetails.node);
              prev = this.linkRight(prev, domainNode);
              if (badSeparator) {
                this.addNodeFlags(domainNode, NODE_FLAG_ERROR);
                this.addFlags(AST_FLAG_HAS_ERROR);
              }
            } else {
              domainNode = 0;
              if (separatorNode !== 0) {
                this.addNodeFlags(separatorNode, NODE_FLAG_ERROR);
                this.addFlags(AST_FLAG_HAS_ERROR);
              }
            }
            if (s.charCodeAt(end) === separatorCode) {
              beg = end;
              end += 1;
              separatorNode = this.allocTypedNode(
                NODE_TYPE_OPTION_VALUE_SEPARATOR,
                parentBeg + beg,
                parentBeg + end
              );
              prev = this.linkRight(prev, separatorNode);
              if (domainNode === 0) {
                this.addNodeFlags(separatorNode, NODE_FLAG_ERROR);
                this.addFlags(AST_FLAG_HAS_ERROR);
              }
            } else {
              separatorNode = 0;
            }
            beg = end;
          }
          if (separatorNode !== 0) {
            this.addNodeFlags(separatorNode, NODE_FLAG_ERROR);
            this.addFlags(AST_FLAG_HAS_ERROR);
          }
          this.linkDown(containerNode, this.throwHeadNode(listNode));
          return containerNode;
        }
        parseDomain(parent, parseDetails) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          const not = this.charCodeAt(parentBeg) === 126;
          let head = 0, next = 0;
          let beg = parentBeg;
          if (not) {
            this.addNodeFlags(parent, NODE_FLAG_IS_NEGATED);
            head = this.allocTypedNode(NODE_TYPE_OPTION_VALUE_NOT, beg, beg + 1);
            if ((parseDetails.mode & DOMAIN_CAN_BE_NEGATED) === 0) {
              this.addNodeFlags(parent, NODE_FLAG_ERROR);
            }
            beg += 1;
          }
          const c0 = this.charCodeAt(beg);
          let end = beg;
          let isRegex = false;
          if (c0 === 47) {
            this.domainRegexValueParser.nextArg(this.raw, beg + 1);
            end = this.domainRegexValueParser.separatorEnd;
            isRegex = true;
          } else if (c0 === 91 && this.startsWith("[$domain=/", beg)) {
            end = this.indexOf("/]", beg + 10, parentEnd);
            if (end !== -1) {
              end += 2;
            }
            isRegex = true;
          } else {
            end = this.indexOf(parseDetails.separator, end, parentEnd);
          }
          if (end === -1) {
            end = parentEnd;
          }
          if (beg !== end) {
            next = this.allocTypedNode(NODE_TYPE_OPTION_VALUE_DOMAIN, beg, end);
            const hn2 = this.normalizeDomainValue(next, isRegex, parseDetails.mode);
            if (hn2 !== void 0) {
              if (hn2 !== "") {
                this.setNodeTransform(next, hn2);
              } else {
                this.addNodeFlags(parent, NODE_FLAG_ERROR);
                this.addFlags(AST_FLAG_HAS_ERROR);
                this.astError = AST_ERROR_DOMAIN_NAME;
              }
            }
            if (head === 0) {
              head = next;
            } else {
              this.linkRight(head, next);
            }
          } else {
            this.addNodeFlags(parent, NODE_FLAG_ERROR);
            this.addFlags(AST_FLAG_HAS_ERROR);
          }
          parseDetails.node = head;
          parseDetails.len = end - parentBeg;
        }
        normalizeDomainValue(node, isRegex, modeBits) {
          const raw = this.getNodeString(node);
          if (isRegex) {
            if ((modeBits & DOMAIN_CAN_BE_REGEX) === 0) {
              return "";
            }
            return this.normalizeDomainRegexValue(raw);
          }
          const r1 = this.normalizeHostnameValue(raw, modeBits);
          if (r1 === void 0) {
            return;
          }
          if (r1 !== "") {
            return r1;
          }
          const match = this.reAdvancedDomainSyntax.exec(raw);
          if (match === null) {
            return "";
          }
          ;
          const isAncestor = match[2] !== void 0;
          if (isAncestor && (modeBits & DOMAIN_CAN_BE_ANCESTOR) === 0) {
            return "";
          }
          const hasPath = match[3] !== void 0;
          if (hasPath && (modeBits & DOMAIN_CAN_HAVE_PATH) === 0) {
            return "";
          }
          if (isAncestor && hasPath) {
            return "";
          }
          const r2 = this.normalizeHostnameValue(match[1], modeBits);
          if (r2 === void 0) {
            return;
          }
          if (r2 === "") {
            return "";
          }
          return `${r2}${match[2] ?? ""}${match[3] ?? ""}`;
        }
        normalizeDomainRegexValue(before) {
          const regex = before.startsWith("[$domain=/") ? `${before.slice(9, -1)}` : before;
          const source = this.normalizeRegexPattern(regex.replace(/\\\|/g, "|"));
          if (source === "") {
            return "";
          }
          const after = `/${source}/`;
          if (after === before) {
            return;
          }
          return after;
        }
        parseExt(parent, anchorBeg, anchorLen) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          const head = this.allocHeadNode();
          let prev = head, next = 0;
          this.astType = AST_TYPE_EXTENDED;
          this.addFlags(this.extFlagsFromAnchor(anchorBeg));
          if (anchorBeg > parentBeg) {
            next = this.allocTypedNode(
              NODE_TYPE_EXT_OPTIONS,
              parentBeg,
              anchorBeg
            );
            this.addFlags(AST_FLAG_HAS_OPTIONS);
            this.addNodeToRegister(NODE_TYPE_EXT_OPTIONS, next);
            const down2 = this.parseDomainList(next, ",", DOMAIN_FROM_EXT_LIST);
            this.linkDown(next, down2);
            prev = this.linkRight(prev, next);
          }
          next = this.allocTypedNode(
            NODE_TYPE_EXT_OPTIONS_ANCHOR,
            anchorBeg,
            anchorBeg + anchorLen
          );
          this.addNodeToRegister(NODE_TYPE_EXT_OPTIONS_ANCHOR, next);
          prev = this.linkRight(prev, next);
          next = this.allocTypedNode(
            NODE_TYPE_EXT_PATTERN_RAW,
            anchorBeg + anchorLen,
            parentEnd
          );
          this.addNodeToRegister(NODE_TYPE_EXT_PATTERN_RAW, next);
          const down = this.parseExtPattern(next);
          if (down !== 0) {
            this.linkDown(next, down);
          } else {
            this.addNodeFlags(next, NODE_FLAG_ERROR);
            this.addFlags(AST_FLAG_HAS_ERROR);
          }
          this.linkRight(prev, next);
          this.validateExt();
          return this.throwHeadNode(head);
        }
        extFlagsFromAnchor(anchorBeg) {
          let c = this.charCodeAt(anchorBeg + 1);
          if (c === 35) {
            return 0;
          }
          if (c === 37) {
            return AST_FLAG_EXT_SCRIPTLET_ADG;
          }
          if (c === 63) {
            return AST_FLAG_EXT_STRONG;
          }
          if (c === 36) {
            c = this.charCodeAt(anchorBeg + 2);
            if (c === 35) {
              return AST_FLAG_EXT_STYLE;
            }
            if (c === 63) {
              return AST_FLAG_EXT_STYLE | AST_FLAG_EXT_STRONG;
            }
          }
          if (c === 64) {
            return AST_FLAG_IS_EXCEPTION | this.extFlagsFromAnchor(anchorBeg + 1);
          }
          return AST_FLAG_UNSUPPORTED | AST_FLAG_HAS_ERROR;
        }
        validateExt() {
          const isException = this.isException();
          let realBad = false;
          for (let i = 0, n = this.nodeTypeRegisterPtr; i < n; i++) {
            const type = this.nodeTypeRegister[i];
            const targetNode = this.nodeTypeLookupTable[type];
            if (targetNode === 0) {
              continue;
            }
            const flags = this.getNodeFlags(targetNode);
            if ((flags & NODE_FLAG_ERROR) !== 0) {
              continue;
            }
            realBad = false;
            switch (type) {
              case NODE_TYPE_EXT_PATTERN_RESPONSEHEADER: {
                const pattern = this.getNodeString(targetNode);
                realBad = pattern !== "" && removableHTTPHeaders.has(pattern) === false || pattern === "" && isException === false;
                break;
              }
              case NODE_TYPE_EXT_PATTERN_SCRIPTLET_TOKEN: {
                if (this.interactive !== true) {
                  break;
                }
                if (isException) {
                  break;
                }
                const { trustedSource, trustedScriptletTokens } = this.options;
                if (trustedScriptletTokens instanceof Set === false) {
                  break;
                }
                const token = this.getNodeString(targetNode);
                if (trustedScriptletTokens.has(token) && trustedSource !== true) {
                  this.astError = AST_ERROR_UNTRUSTED_SOURCE;
                  realBad = true;
                }
                break;
              }
              default:
                break;
            }
            if (realBad) {
              this.addNodeFlags(targetNode, NODE_FLAG_ERROR);
              this.addFlags(AST_FLAG_HAS_ERROR);
            }
          }
        }
        parseExtPattern(parent) {
          const c = this.charCodeAt(this.nodes[parent + NODE_BEG_INDEX]);
          if (c === 43) {
            const s = this.getNodeString(parent);
            if (/^\+js\(.*\)$/.exec(s) !== null) {
              this.astTypeFlavor = AST_TYPE_EXTENDED_SCRIPTLET;
              return this.parseExtPatternScriptlet(parent);
            }
          }
          if (this.getFlags(AST_FLAG_EXT_SCRIPTLET_ADG)) {
            const s = this.getNodeString(parent);
            if (/^\/\/scriptlet\(.*\)$/.exec(s) !== null) {
              this.astTypeFlavor = AST_TYPE_EXTENDED_SCRIPTLET;
              return this.parseExtPatternScriptlet(parent);
            }
            return 0;
          }
          if (c === 94) {
            const s = this.getNodeString(parent);
            if (this.reResponseheaderPattern.test(s)) {
              this.astTypeFlavor = AST_TYPE_EXTENDED_RESPONSEHEADER;
              return this.parseExtPatternResponseheader(parent);
            }
            this.astTypeFlavor = AST_TYPE_EXTENDED_HTML;
            return this.parseExtPatternHtml(parent);
          }
          this.astTypeFlavor = AST_TYPE_EXTENDED_COSMETIC;
          return this.parseExtPatternCosmetic(parent);
        }
        parseExtPatternScriptlet(parent) {
          const beg = this.nodes[parent + NODE_BEG_INDEX];
          const end = this.nodes[parent + NODE_END_INDEX];
          const s = this.getNodeString(parent);
          const rawArg0 = beg + (s.startsWith("+js") ? 4 : 12);
          const rawArg1 = end - 1;
          const head = this.allocTypedNode(NODE_TYPE_EXT_DECORATION, beg, rawArg0);
          let prev = head, next = 0;
          next = this.allocTypedNode(NODE_TYPE_EXT_PATTERN_SCRIPTLET, rawArg0, rawArg1);
          this.addNodeToRegister(NODE_TYPE_EXT_PATTERN_SCRIPTLET, next);
          this.linkDown(next, this.parseExtPatternScriptletArgs(next));
          prev = this.linkRight(prev, next);
          next = this.allocTypedNode(NODE_TYPE_EXT_DECORATION, rawArg1, end);
          this.linkRight(prev, next);
          return head;
        }
        parseExtPatternScriptletArgs(parent) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          if (parentEnd === parentBeg) {
            return 0;
          }
          const head = this.allocHeadNode();
          let prev = head, next = 0;
          const s = this.getNodeString(parent);
          const argsEnd = s.length;
          this.scriptletArgListParser.mustQuote = this.getFlags(AST_FLAG_EXT_SCRIPTLET_ADG) !== 0;
          const details = this.scriptletArgListParser.nextArg(s, 0);
          if (details.argBeg > 0) {
            next = this.allocTypedNode(
              NODE_TYPE_EXT_DECORATION,
              parentBeg,
              parentBeg + details.argBeg
            );
            prev = this.linkRight(prev, next);
          }
          const token = s.slice(details.argBeg, details.argEnd);
          const tokenEnd = details.argEnd - (token.endsWith(".js") ? 3 : 0);
          next = this.allocTypedNode(
            NODE_TYPE_EXT_PATTERN_SCRIPTLET_TOKEN,
            parentBeg + details.argBeg,
            parentBeg + tokenEnd
          );
          this.addNodeToRegister(NODE_TYPE_EXT_PATTERN_SCRIPTLET_TOKEN, next);
          if (details.failed) {
            this.addNodeFlags(next, NODE_FLAG_ERROR);
            this.addFlags(AST_FLAG_HAS_ERROR);
          }
          prev = this.linkRight(prev, next);
          if (tokenEnd < details.argEnd) {
            next = this.allocTypedNode(
              NODE_TYPE_IGNORE,
              parentBeg + tokenEnd,
              parentBeg + details.argEnd
            );
            prev = this.linkRight(prev, next);
          }
          if (details.quoteEnd < argsEnd) {
            next = this.allocTypedNode(
              NODE_TYPE_EXT_DECORATION,
              parentBeg + details.argEnd,
              parentBeg + details.separatorEnd
            );
            prev = this.linkRight(prev, next);
          }
          next = this.allocTypedNode(
            NODE_TYPE_EXT_PATTERN_SCRIPTLET_ARGS,
            parentBeg + details.separatorEnd,
            parentBeg + argsEnd
          );
          this.linkDown(next, this.parseExtPatternScriptletArglist(next));
          this.linkRight(prev, next);
          return this.throwHeadNode(head);
        }
        parseExtPatternScriptletArglist(parent) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          if (parentEnd === parentBeg) {
            return 0;
          }
          const s = this.getNodeString(parent);
          const argsEnd = s.length;
          const head = this.allocHeadNode();
          let prev = head, next = 0;
          let decorationBeg = 0;
          let i = 0;
          for (; ; ) {
            const details = this.scriptletArgListParser.nextArg(s, i);
            if (decorationBeg < details.argBeg) {
              next = this.allocTypedNode(
                NODE_TYPE_EXT_DECORATION,
                parentBeg + decorationBeg,
                parentBeg + details.argBeg
              );
              prev = this.linkRight(prev, next);
            }
            if (i === argsEnd) {
              break;
            }
            next = this.allocTypedNode(
              NODE_TYPE_EXT_PATTERN_SCRIPTLET_ARG,
              parentBeg + details.argBeg,
              parentBeg + details.argEnd
            );
            if (details.transform) {
              const arg = s.slice(details.argBeg, details.argEnd);
              this.setNodeTransform(
                next,
                this.scriptletArgListParser.normalizeArg(arg)
              );
            }
            prev = this.linkRight(prev, next);
            if (details.failed) {
              this.addNodeFlags(next, NODE_FLAG_ERROR);
              this.addFlags(AST_FLAG_HAS_ERROR);
            }
            decorationBeg = details.argEnd;
            i = details.separatorEnd;
          }
          return this.throwHeadNode(head);
        }
        getScriptletArgs() {
          const args = [];
          if (this.isScriptletFilter() === false) {
            return args;
          }
          const root = this.getBranchFromType(NODE_TYPE_EXT_PATTERN_SCRIPTLET);
          const walker = this.getWalker(root);
          for (let node = walker.next(); node !== 0; node = walker.next()) {
            switch (this.getNodeType(node)) {
              case NODE_TYPE_EXT_PATTERN_SCRIPTLET_TOKEN:
              case NODE_TYPE_EXT_PATTERN_SCRIPTLET_ARG:
                args.push(this.getNodeTransform(node));
                break;
              default:
                break;
            }
          }
          walker.dispose();
          return args;
        }
        parseExtPatternResponseheader(parent) {
          const beg = this.nodes[parent + NODE_BEG_INDEX];
          const end = this.nodes[parent + NODE_END_INDEX];
          const s = this.getNodeString(parent);
          const rawArg0 = beg + 16;
          const rawArg1 = end - 1;
          const head = this.allocTypedNode(NODE_TYPE_EXT_DECORATION, beg, rawArg0);
          let prev = head, next = 0;
          const trimmedArg0 = rawArg0 + this.leftWhitespaceCount(s);
          const trimmedArg1 = rawArg1 - this.rightWhitespaceCount(s);
          if (trimmedArg0 !== rawArg0) {
            next = this.allocTypedNode(NODE_TYPE_WHITESPACE, rawArg0, trimmedArg0);
            prev = this.linkRight(prev, next);
          }
          next = this.allocTypedNode(NODE_TYPE_EXT_PATTERN_RESPONSEHEADER, rawArg0, rawArg1);
          this.addNodeToRegister(NODE_TYPE_EXT_PATTERN_RESPONSEHEADER, next);
          if (rawArg1 === rawArg0 && this.isException() === false) {
            this.addNodeFlags(parent, NODE_FLAG_ERROR);
            this.addFlags(AST_FLAG_HAS_ERROR);
          }
          prev = this.linkRight(prev, next);
          if (trimmedArg1 !== rawArg1) {
            next = this.allocTypedNode(NODE_TYPE_WHITESPACE, trimmedArg1, rawArg1);
            prev = this.linkRight(prev, next);
          }
          next = this.allocTypedNode(NODE_TYPE_EXT_DECORATION, rawArg1, end);
          this.linkRight(prev, next);
          return head;
        }
        getResponseheaderName() {
          if (this.isResponseheaderFilter() === false) {
            return "";
          }
          const root = this.getBranchFromType(NODE_TYPE_EXT_PATTERN_RESPONSEHEADER);
          return this.getNodeString(root);
        }
        parseExtPatternHtml(parent) {
          const beg = this.nodes[parent + NODE_BEG_INDEX];
          const end = this.nodes[parent + NODE_END_INDEX];
          const head = this.allocTypedNode(NODE_TYPE_EXT_DECORATION, beg, beg + 1);
          let prev = head, next = 0;
          next = this.allocTypedNode(NODE_TYPE_EXT_PATTERN_HTML, beg + 1, end);
          this.linkRight(prev, next);
          if ((this.hasOptions() || this.isException()) === false) {
            this.addNodeFlags(parent, NODE_FLAG_ERROR);
            this.addFlags(AST_FLAG_HAS_ERROR);
            return head;
          }
          this.result.exception = this.isException();
          this.result.raw = this.getNodeString(next);
          this.result.compiled = void 0;
          const success = this.selectorCompiler.compile(
            this.result.raw,
            this.result,
            {
              asProcedural: this.getFlags(AST_FLAG_EXT_STRONG) !== 0
            }
          );
          if (success !== true) {
            this.addNodeFlags(next, NODE_FLAG_ERROR);
            this.addFlags(AST_FLAG_HAS_ERROR);
          }
          return head;
        }
        parseExtPatternCosmetic(parent) {
          const parentBeg = this.nodes[parent + NODE_BEG_INDEX];
          const parentEnd = this.nodes[parent + NODE_END_INDEX];
          const head = this.allocTypedNode(
            NODE_TYPE_EXT_PATTERN_COSMETIC,
            parentBeg,
            parentEnd
          );
          this.result.exception = this.isException();
          this.result.raw = this.getNodeString(head);
          this.result.compiled = void 0;
          const success = this.selectorCompiler.compile(
            this.result.raw,
            this.result,
            {
              asProcedural: this.getFlags(AST_FLAG_EXT_STRONG) !== 0,
              adgStyleSyntax: this.getFlags(AST_FLAG_EXT_STYLE) !== 0
            }
          );
          if (success !== true) {
            this.addNodeFlags(head, NODE_FLAG_ERROR);
            this.addFlags(AST_FLAG_HAS_ERROR);
          }
          return head;
        }
        hasError() {
          return (this.astFlags & AST_FLAG_HAS_ERROR) !== 0;
        }
        isUnsupported() {
          return (this.astFlags & AST_FLAG_UNSUPPORTED) !== 0;
        }
        hasOptions() {
          return (this.astFlags & AST_FLAG_HAS_OPTIONS) !== 0;
        }
        isNegatedOption(type) {
          const node = this.nodeTypeLookupTable[type];
          const flags = this.nodes[node + NODE_FLAGS_INDEX];
          return (flags & NODE_FLAG_IS_NEGATED) !== 0;
        }
        isException() {
          return (this.astFlags & AST_FLAG_IS_EXCEPTION) !== 0;
        }
        isLeftHnAnchored() {
          return (this.astFlags & AST_FLAG_NET_PATTERN_LEFT_HNANCHOR) !== 0;
        }
        isLeftAnchored() {
          return (this.astFlags & AST_FLAG_NET_PATTERN_LEFT_ANCHOR) !== 0;
        }
        isRightAnchored() {
          return (this.astFlags & AST_FLAG_NET_PATTERN_RIGHT_ANCHOR) !== 0;
        }
        linkRight(prev, next) {
          return this.nodes[prev + NODE_RIGHT_INDEX] = next;
        }
        linkDown(node, down) {
          return this.nodes[node + NODE_DOWN_INDEX] = down;
        }
        makeChain(nodes) {
          for (let i = 1; i < nodes.length; i++) {
            this.nodes[nodes[i - 1] + NODE_RIGHT_INDEX] = nodes[i];
          }
          return nodes[0];
        }
        allocHeadNode() {
          const node = this.nodePoolPtr;
          this.nodePoolPtr += NOOP_NODE_SIZE;
          if (this.nodePoolPtr > this.nodePoolEnd) {
            this.growNodePool(this.nodePoolPtr);
          }
          this.nodes[node + NODE_RIGHT_INDEX] = 0;
          return node;
        }
        throwHeadNode(head) {
          return this.nodes[head + NODE_RIGHT_INDEX];
        }
        allocTypedNode(type, beg, end) {
          const node = this.nodePoolPtr;
          this.nodePoolPtr += FULL_NODE_SIZE;
          if (this.nodePoolPtr > this.nodePoolEnd) {
            this.growNodePool(this.nodePoolPtr);
          }
          this.nodes[node + NODE_RIGHT_INDEX] = 0;
          this.nodes[node + NODE_TYPE_INDEX] = type;
          this.nodes[node + NODE_DOWN_INDEX] = 0;
          this.nodes[node + NODE_BEG_INDEX] = beg;
          this.nodes[node + NODE_END_INDEX] = end;
          this.nodes[node + NODE_TRANSFORM_INDEX] = 0;
          this.nodes[node + NODE_FLAGS_INDEX] = 0;
          return node;
        }
        allocSentinelNode(type, beg) {
          return this.allocTypedNode(type, beg, beg);
        }
        growNodePool(min) {
          const oldSize = this.nodes.length;
          const newSize = min + 16383 & ~16383;
          if (newSize === oldSize) {
            return;
          }
          const newArray = new Uint32Array(newSize);
          newArray.set(this.nodes);
          this.nodes = newArray;
          this.nodePoolEnd = newSize;
        }
        getNodeTypes() {
          return this.nodeTypeRegister.slice(0, this.nodeTypeRegisterPtr);
        }
        getNodeType(node) {
          return node !== 0 ? this.nodes[node + NODE_TYPE_INDEX] : 0;
        }
        getNodeFlags(node, flags = 4294967295) {
          return this.nodes[node + NODE_FLAGS_INDEX] & flags;
        }
        setNodeFlags(node, flags) {
          this.nodes[node + NODE_FLAGS_INDEX] = flags;
        }
        addNodeFlags(node, flags) {
          if (node === 0) {
            return;
          }
          this.nodes[node + NODE_FLAGS_INDEX] |= flags;
        }
        removeNodeFlags(node, flags) {
          this.nodes[node + NODE_FLAGS_INDEX] &= ~flags;
        }
        addNodeToRegister(type, node) {
          this.nodeTypeRegister[this.nodeTypeRegisterPtr++] = type;
          this.nodeTypeLookupTable[type] = node;
        }
        getBranchFromType(type) {
          const ptr = this.nodeTypeRegisterPtr;
          if (ptr === 0) {
            return 0;
          }
          return this.nodeTypeRegister.lastIndexOf(type, ptr - 1) !== -1 ? this.nodeTypeLookupTable[type] : 0;
        }
        nodeIsEmptyString(node) {
          return this.nodes[node + NODE_END_INDEX] === this.nodes[node + NODE_BEG_INDEX];
        }
        getNodeString(node) {
          const beg = this.nodes[node + NODE_BEG_INDEX];
          const end = this.nodes[node + NODE_END_INDEX];
          if (end === beg) {
            return "";
          }
          if (beg === 0 && end === this.rawEnd) {
            return this.raw;
          }
          return this.raw.slice(beg, end);
        }
        getNodeStringBeg(node) {
          return this.nodes[node + NODE_BEG_INDEX];
        }
        getNodeStringEnd(node) {
          return this.nodes[node + NODE_END_INDEX];
        }
        getNodeStringLen(node) {
          if (node === 0) {
            return "";
          }
          return this.nodes[node + NODE_END_INDEX] - this.nodes[node + NODE_BEG_INDEX];
        }
        isNodeTransformed(node) {
          return this.nodes[node + NODE_TRANSFORM_INDEX] !== 0;
        }
        getNodeTransform(node) {
          if (node === 0) {
            return "";
          }
          const slot = this.nodes[node + NODE_TRANSFORM_INDEX];
          return slot !== 0 ? this.astTransforms[slot] : this.getNodeString(node);
        }
        setNodeTransform(node, value) {
          const slot = this.astTransformPtr++;
          this.astTransforms[slot] = value;
          this.nodes[node + NODE_TRANSFORM_INDEX] = slot;
        }
        getTypeString(type) {
          const node = this.getBranchFromType(type);
          if (node === 0) {
            return;
          }
          return this.getNodeString(node);
        }
        leftWhitespaceCount(s) {
          const match = this.reWhitespaceStart.exec(s);
          return match === null ? 0 : match[0].length;
        }
        rightWhitespaceCount(s) {
          const match = this.reWhitespaceEnd.exec(s);
          return match === null ? 0 : match[1].length;
        }
        nextCommaInCommaSeparatedListString(s, start) {
          const n = s.length;
          if (n === 0) {
            return -1;
          }
          const ilastchar = n - 1;
          let i = start;
          while (i < n) {
            const c = s.charCodeAt(i);
            if (c === 44) {
              return i + 1;
            }
            if (c === 92) {
              if (i < ilastchar) {
                i += 1;
              }
            }
          }
          return -1;
        }
        endOfLiteralRegex(s, start) {
          const n = s.length;
          if (n === 0) {
            return -1;
          }
          const ilastchar = n - 1;
          let i = start + 1;
          while (i < n) {
            const c = s.charCodeAt(i);
            if (c === 47) {
              return i + 1;
            }
            if (c === 92) {
              if (i < ilastchar) {
                i += 1;
              }
            }
            i += 1;
          }
          return -1;
        }
        charCodeAt(pos) {
          return pos < this.rawEnd ? this.raw.charCodeAt(pos) : -1;
        }
        indexOf(needle, beg, end = 0) {
          const haystack = end === 0 ? this.raw : this.raw.slice(0, end);
          return haystack.indexOf(needle, beg);
        }
        startsWith(s, pos) {
          return pos < this.rawEnd && this.raw.startsWith(s, pos);
        }
        isTokenCharCode(c) {
          return c === 37 || c >= 48 && c <= 57 || c >= 65 && c <= 90 || c >= 97 && c <= 122;
        }
        // Ultimately, let the browser API do the hostname normalization, after
        // making some other trivial checks.
        //
        // returns:
        //   undefined: no normalization needed, use original hostname
        //   empty string: hostname is invalid
        //   non-empty string: normalized hostname
        normalizeHostnameValue(s, modeBits = 0) {
          if (this.reHostnameAscii.test(s)) {
            return;
          }
          if (this.reBadHostnameChars.test(s)) {
            return "";
          }
          let hn2 = s;
          const hasWildcard = hn2.includes("*");
          if (hasWildcard) {
            if (modeBits === 0) {
              return "";
            }
            if (hn2.length === 1) {
              if ((modeBits & DOMAIN_CAN_USE_SINGLE_WILDCARD) === 0) {
                return "";
              }
              return;
            }
            if ((modeBits & DOMAIN_CAN_USE_ENTITY) !== 0) {
              if (this.rePlainEntity.test(hn2)) {
                return;
              }
              if (this.reIsEntity.test(hn2) === false) {
                return "";
              }
            } else if ((modeBits & DOMAIN_CAN_USE_WILDCARD) === 0) {
              return "";
            }
            hn2 = hn2.replace(/\*/g, "__asterisk__");
          }
          this.punycoder.hostname = "_";
          try {
            this.punycoder.hostname = hn2;
            hn2 = this.punycoder.hostname;
          } catch (e) {
            console.warn("[uBR] static-filtering-parser: punycode hostname conversion failed", e);
            return "";
          }
          if (hn2 === "_" || hn2 === "") {
            return "";
          }
          if (hasWildcard) {
            hn2 = this.punycoder.hostname.replace(/__asterisk__/g, "*");
          }
          if ((modeBits & DOMAIN_CAN_USE_WILDCARD) === 0 && (hn2.charCodeAt(0) === 46 || exCharCodeAt(hn2, -1) === 46)) {
            return "";
          }
          return hn2;
        }
        normalizeRegexPattern(s) {
          try {
            const source = /^\/.+\/$/.test(s) ? s.slice(1, -1) : s;
            const regex = new RegExp(source);
            return regex.source;
          } catch (ex) {
            console.warn("[uBR] static-filtering-parser: normalizeRegexPattern failed", ex);
            this.normalizeRegexPattern.message = ex.toString();
          }
          return "";
        }
        getDomainListIterator(root) {
          const iter = this.domainListIteratorJunkyard.length !== 0 ? this.domainListIteratorJunkyard.pop().reuse(root) : new DomainListIterator(this, root);
          return root !== 0 ? iter : iter.stop();
        }
        getNetFilterFromOptionIterator() {
          return this.getDomainListIterator(
            this.getBranchFromType(NODE_TYPE_NET_OPTION_NAME_FROM)
          );
        }
        getNetFilterToOptionIterator() {
          return this.getDomainListIterator(
            this.getBranchFromType(NODE_TYPE_NET_OPTION_NAME_TO)
          );
        }
        getNetFilterDenyallowOptionIterator() {
          return this.getDomainListIterator(
            this.getBranchFromType(NODE_TYPE_NET_OPTION_NAME_DENYALLOW)
          );
        }
        getExtFilterDomainIterator() {
          return this.getDomainListIterator(
            this.getBranchFromType(NODE_TYPE_EXT_OPTIONS)
          );
        }
        getWalker(from) {
          if (this.walkerJunkyard.length === 0) {
            return new AstWalker(this, from);
          }
          const walker = this.walkerJunkyard.pop();
          walker.reset(from);
          return walker;
        }
        findDescendantByType(from, type) {
          const walker = this.getWalker(from);
          let node = walker.next();
          while (node !== 0) {
            if (this.getNodeType(node) === type) {
              return node;
            }
            node = walker.next();
          }
          return 0;
        }
        dump() {
          if (this.astType === AST_TYPE_COMMENT) {
            return;
          }
          const walker = this.getWalker();
          for (let node = walker.reset(); node !== 0; node = walker.next()) {
            const type = this.nodes[node + NODE_TYPE_INDEX];
            const value = this.getNodeString(node);
            const name2 = nodeNameFromNodeType.get(type) || `${type}`;
            const bits = this.getNodeFlags(node).toString(2).padStart(4, "0");
            const indent2 = "  ".repeat(walker.depth);
            console.log(`${indent2}type=${name2} "${value}" 0b${bits}`);
            if (this.isNodeTransformed(node)) {
              console.log(`${indent2}    transform="${this.getNodeTransform(node)}"`);
            }
          }
        }
      };
      netOptionTokenDescriptors = /* @__PURE__ */ new Map([
        ["1p", { canNegate: true }],
        /* synonym */
        ["first-party", { canNegate: true }],
        ["strict1p", {}],
        /* synonym */
        ["strict-first-party", {}],
        ["3p", { canNegate: true }],
        /* synonym */
        ["third-party", { canNegate: true }],
        ["strict3p", {}],
        /* synonym */
        ["strict-third-party", {}],
        ["all", {}],
        ["badfilter", {}],
        ["cname", { allowOnly: true }],
        ["csp", { mustAssign: true }],
        ["css", { canNegate: true }],
        /* synonym */
        ["stylesheet", { canNegate: true }],
        ["denyallow", { mustAssign: true }],
        ["doc", { canNegate: true }],
        /* synonym */
        ["document", { canNegate: true }],
        ["ehide", {}],
        /* synonym */
        ["elemhide", {}],
        ["empty", { blockOnly: true }],
        ["frame", { canNegate: true }],
        /* synonym */
        ["subdocument", { canNegate: true }],
        ["from", { mustAssign: true }],
        /* synonym */
        ["domain", { mustAssign: true }],
        ["font", { canNegate: true }],
        ["genericblock", {}],
        ["ghide", {}],
        /* synonym */
        ["generichide", {}],
        ["image", { canNegate: true }],
        ["important", { blockOnly: true }],
        ["inline-font", { canNegate: true }],
        ["inline-script", { canNegate: true }],
        ["ipaddress", { mustAssign: true }],
        ["match-case", {}],
        ["media", { canNegate: true }],
        ["method", { mustAssign: true }],
        ["mp4", { blockOnly: true }],
        ["_", {}],
        ["object", { canNegate: true }],
        /* synonym */
        ["object-subrequest", { canNegate: true }],
        ["other", { canNegate: true }],
        ["permissions", { mustAssign: true }],
        ["ping", { canNegate: true }],
        /* synonym */
        ["beacon", { canNegate: true }],
        ["popunder", {}],
        ["popup", { canNegate: true }],
        ["reason", { mustAssign: true }],
        ["redirect", { mustAssign: true }],
        /* synonym */
        ["rewrite", { mustAssign: true }],
        ["redirect-rule", { mustAssign: true }],
        ["removeparam", {}],
        /* synonym */
        ["queryprune", {}],
        ["replace", { mustAssign: true }],
        ["requestheader", { mustAssign: true }],
        ["responseheader", { mustAssign: true }],
        /* synonym */
        ["header", { mustAssign: true }],
        ["script", { canNegate: true }],
        ["shide", {}],
        /* synonym */
        ["specifichide", {}],
        ["to", { mustAssign: true }],
        ["urlskip", { mustAssign: true }],
        ["uritransform", { mustAssign: true }],
        ["xhr", { canNegate: true }],
        /* synonym */
        ["xmlhttprequest", { canNegate: true }],
        ["webrtc", {}],
        ["websocket", { canNegate: true }]
      ]);
      ExtSelectorCompiler = class {
        constructor(instanceOptions = {}) {
          this.reParseRegexLiteral = /^\/(.+)\/([imu]+)?$/;
          const cssIdentifier = "[A-Za-z_][\\w-]*";
          const cssClassOrId = `[.#]${cssIdentifier}`;
          const cssAttribute = `\\[${cssIdentifier}(?:[*^$]?="[^"\\]\\\\\\x09-\\x0D]+")?\\]`;
          const cssSimple = `(?:${cssIdentifier}(?:${cssClassOrId})*(?:${cssAttribute})*|${cssClassOrId}(?:${cssClassOrId})*(?:${cssAttribute})*|${cssAttribute}(?:${cssAttribute})*)`;
          const cssCombinator = "(?: | [+>~] )";
          this.reCommonSelector = new RegExp(
            `^${cssSimple}(?:${cssCombinator}${cssSimple})*$`
          );
          this.reEatBackslashes = /\\([()])/g;
          this.knownPseudoClasses = /* @__PURE__ */ new Set([
            "active",
            "any-link",
            "autofill",
            "blank",
            "checked",
            "current",
            "default",
            "defined",
            "dir",
            "disabled",
            "empty",
            "enabled",
            "first",
            "first-child",
            "first-of-type",
            "fullscreen",
            "future",
            "focus",
            "focus-visible",
            "focus-within",
            "has",
            "host",
            "host-context",
            "hover",
            "indeterminate",
            "in-range",
            "invalid",
            "is",
            "lang",
            "last-child",
            "last-of-type",
            "left",
            "link",
            "local-link",
            "modal",
            "not",
            "nth-child",
            "nth-col",
            "nth-last-child",
            "nth-last-col",
            "nth-last-of-type",
            "nth-of-type",
            "only-child",
            "only-of-type",
            "optional",
            "out-of-range",
            "past",
            "picture-in-picture",
            "placeholder-shown",
            "paused",
            "playing",
            "read-only",
            "read-write",
            "required",
            "right",
            "root",
            "scope",
            "state",
            "target",
            "target-within",
            "user-invalid",
            "valid",
            "visited",
            "where"
          ]);
          this.knownPseudoClassesWithArgs = /* @__PURE__ */ new Set([
            "dir",
            "has",
            "host-context",
            "is",
            "lang",
            "not",
            "nth-child",
            "nth-col",
            "nth-last-child",
            "nth-last-col",
            "nth-last-of-type",
            "nth-of-type",
            "state",
            "where"
          ]);
          this.knownPseudoElements = /* @__PURE__ */ new Set([
            "after",
            "backdrop",
            "before",
            "cue",
            "cue-region",
            "first-letter",
            "first-line",
            "file-selector-button",
            "grammar-error",
            "marker",
            "part",
            "placeholder",
            "selection",
            "slotted",
            "spelling-error",
            "target-text"
          ]);
          this.knownPseudoElementsWithArgs = /* @__PURE__ */ new Set([
            "part",
            "slotted"
          ]);
          this.normalizedOperators = /* @__PURE__ */ new Map([
            ["-abp-has", "has"],
            ["-abp-contains", "has-text"],
            ["contains", "has-text"],
            ["nth-ancestor", "upward"],
            ["watch-attrs", "watch-attr"]
          ]);
          this.actionOperators = /* @__PURE__ */ new Set([
            ":remove",
            ":style"
          ]);
          this.proceduralOperatorNames = /* @__PURE__ */ new Set([
            "has-text",
            "if",
            "if-not",
            "matches-attr",
            "matches-css",
            "matches-css-after",
            "matches-css-before",
            "matches-media",
            "matches-path",
            "matches-prop",
            "min-text-length",
            "others",
            "shadow",
            "upward",
            "watch-attr",
            "xpath"
          ]);
          this.maybeProceduralOperatorNames = /* @__PURE__ */ new Set([
            "has",
            "not"
          ]);
          this.proceduralActionNames = /* @__PURE__ */ new Set([
            "remove",
            "remove-attr",
            "remove-class",
            "style"
          ]);
          this.normalizedExtendedSyntaxOperators = /* @__PURE__ */ new Map([
            ["contains", "has-text"],
            ["has", "has"]
          ]);
          this.reIsRelativeSelector = /^\s*[+>~]/;
          this.reExtendedSyntax = /\[-(?:abp|ext)-[a-z-]+=(['"])(?:.+?)(?:\1)\]/;
          this.reExtendedSyntaxReplacer = /\[-(?:abp|ext)-([a-z-]+)=(['"])(.+?)\2\]/g;
          this.abpProceduralOpReplacer = /:-abp-(?:[a-z]+)\(/g;
          this.nativeCssHas = instanceOptions.nativeCssHas === true;
          this.reInvalidIdentifier = /^\d/;
          this.error = void 0;
        }
        // CSSTree library holds onto last string parsed, and this is problematic
        // when the string is a slice of a huge parent string (typically a whole
        // filter list), it causes the huge parent string to stay in memory.
        // Asking CSSTree to parse an empty string resolves this issue.
        finish() {
          ib("");
        }
        compile(raw, out, compileOptions = {}) {
          this.asProcedural = compileOptions.asProcedural === true;
          if (this.isStyleInjectionFilter(raw)) {
            const translated = this.translateStyleInjectionFilter(raw);
            if (translated === void 0) {
              return false;
            }
            raw = translated;
          } else if (compileOptions.adgStyleSyntax === true) {
            return false;
          }
          if (this.asProcedural) {
            if (this.reExtendedSyntax.test(raw)) {
              raw = raw.replace(this.reExtendedSyntaxReplacer, (a, a1, a2, a3) => {
                const op2 = this.normalizedExtendedSyntaxOperators.get(a1);
                if (op2 === void 0) {
                  return a;
                }
                return `:${op2}(${a3})`;
              });
            } else {
              let asProcedural = false;
              raw = raw.replace(this.abpProceduralOpReplacer, (match) => {
                if (match === ":-abp-contains(") {
                  return ":has-text(";
                }
                if (match === ":-abp-has(") {
                  return ":has(";
                }
                asProcedural = true;
                return match;
              });
              this.asProcedural = asProcedural;
            }
          }
          if (this.reIsRelativeSelector.test(raw)) {
            return false;
          }
          if (this.reCommonSelector.test(raw)) {
            out.compiled = raw;
            return true;
          }
          this.error = void 0;
          out.compiled = this.compileSelector(raw);
          if (out.compiled === void 0) {
            out.error = this.error;
            return false;
          }
          if (out.compiled instanceof Object) {
            out.compiled.raw = raw;
            out.compiled = JSON.stringify(out.compiled);
          }
          return true;
        }
        compileSelector(raw) {
          const parts = this.astFromRaw(raw, "selectorList");
          if (parts === void 0) {
            return;
          }
          if (this.astHasType(parts, "Error")) {
            return;
          }
          if (this.astHasType(parts, "Selector") === false) {
            return;
          }
          if (this.astIsValidSelectorList(parts) === false) {
            return;
          }
          if (this.astHasType(parts, "ProceduralSelector")) {
            if (this.astHasType(parts, "PseudoElementSelector")) {
              return;
            }
          } else if (this.astHasType(parts, "ActionSelector") === false) {
            return this.astSerialize(parts);
          }
          const r = this.astCompile(parts);
          if (this.isCssable(r)) {
            r.cssable = true;
          }
          return r;
        }
        isCssable(r) {
          if (r instanceof Object === false) {
            return false;
          }
          if (Array.isArray(r.action) && r.action[0] !== "style") {
            return false;
          }
          if (Array.isArray(r.tasks) === false) {
            return true;
          }
          if (r.tasks[0][0] === "matches-media") {
            if (r.tasks.length === 1) {
              return true;
            }
            if (r.tasks.length === 2) {
              if (r.selector !== "") {
                return false;
              }
              if (r.tasks[1][0] === "spath") {
                return true;
              }
            }
          }
          return false;
        }
        astFromRaw(raw, type) {
          let ast;
          try {
            ast = ib(raw, {
              context: type,
              parseValue: false
            });
          } catch (reason) {
            const lines = [reason.message];
            const extra = reason.sourceFragment().split("\n");
            if (extra.length !== 0) {
              lines.push("");
            }
            const match = /^[^|]+\|/.exec(extra[0]);
            const beg = match !== null ? match[0].length : 0;
            lines.push(...extra.map((a) => a.slice(beg)));
            this.error = lines.join("\n");
            return;
          }
          const parts = [];
          this.astFlatten(ast, parts);
          return parts;
        }
        astFlatten(data, out) {
          const head = data.children && data.children.head;
          let args;
          switch (data.type) {
            case "AttributeSelector":
            case "ClassSelector":
            case "Combinator":
            case "IdSelector":
            case "MediaFeature":
            case "Nth":
            case "Raw":
            case "TypeSelector":
              out.push({ data });
              break;
            case "Declaration":
              if (data.value) {
                this.astFlatten(data.value, args = []);
              }
              out.push({ data, args });
              args = void 0;
              break;
            case "DeclarationList":
            case "Identifier":
            case "MediaQueryList":
            case "Selector":
            case "SelectorList":
              args = out;
              out.push({ data });
              break;
            case "MediaQuery":
            case "PseudoClassSelector":
            case "PseudoElementSelector":
              if (head) {
                args = [];
              }
              out.push({ data, args });
              break;
            case "Value":
              args = out;
              break;
            default:
              break;
          }
          if (head) {
            if (args) {
              this.astFlatten(head.data, args);
            }
            let next = head.next;
            while (next) {
              this.astFlatten(next.data, args);
              next = next.next;
            }
          }
          if (data.type !== "PseudoClassSelector") {
            return;
          }
          if (data.name.startsWith("-abp-") && this.asProcedural === false) {
            this.error = `${data.name} requires '#?#' separator syntax`;
            return;
          }
          data.name = this.normalizedOperators.get(data.name) || data.name;
          if (this.proceduralOperatorNames.has(data.name)) {
            data.type = "ProceduralSelector";
          } else if (this.proceduralActionNames.has(data.name)) {
            data.type = "ActionSelector";
          } else if (data.name.startsWith("-abp-")) {
            data.type = "Error";
            this.error = `${data.name} is not supported`;
            return;
          }
          if (this.maybeProceduralOperatorNames.has(data.name) === false) {
            return;
          }
          if (this.astHasType(args, "ActionSelector")) {
            data.type = "Error";
            this.error = "invalid use of action operator";
            return;
          }
          if (this.astHasType(args, "ProceduralSelector")) {
            data.type = "ProceduralSelector";
            return;
          }
          switch (data.name) {
            case "has":
              if (this.asProcedural || this.nativeCssHas !== true || this.astHasName(args, "has")) {
                data.type = "ProceduralSelector";
              } else if (this.astHasType(args, "PseudoElementSelector")) {
                data.type = "Error";
              }
              break;
            case "not": {
              if (this.astHasType(args, "Combinator", 0) === false) {
                break;
              }
              if (this.astIsValidSelectorList(args) !== true) {
                data.type = "Error";
              }
              break;
            }
            default:
              break;
          }
        }
        // https://github.com/uBlockOrigin/uBlock-issues/issues/2300
        //   Unquoted attribute values are parsed as Identifier instead of String.
        // https://github.com/uBlockOrigin/uBlock-issues/issues/3127
        //   Escape [\t\n\v\f\r]
        astSerializePart(part) {
          const out = [];
          const { data } = part;
          switch (data.type) {
            case "AttributeSelector": {
              const name2 = data.name.name;
              if (this.reInvalidIdentifier.test(name2)) {
                return;
              }
              if (data.matcher === null) {
                out.push(`[${name2}]`);
                break;
              }
              let value = data.value.value;
              if (typeof value !== "string") {
                value = data.value.name;
              }
              if (/["\\]/.test(value)) {
                value = value.replace(/["\\]/g, "\\$&");
              }
              if (/[\x09-\x0D]/.test(value)) {
                value = value.replace(
                  /[\x09-\x0D]/g,
                  (s) => `\\${s.charCodeAt(0).toString(16).toUpperCase()} `
                );
              }
              let flags = "";
              if (typeof data.flags === "string") {
                if (/^(is?|si?)$/.test(data.flags) === false) {
                  return;
                }
                flags = ` ${data.flags}`;
              }
              out.push(`[${name2}${data.matcher}"${value}"${flags}]`);
              break;
            }
            case "ClassSelector":
              if (this.reInvalidIdentifier.test(data.name)) {
                return;
              }
              out.push(`.${data.name}`);
              break;
            case "Combinator":
              out.push(data.name);
              break;
            case "Identifier":
              if (this.reInvalidIdentifier.test(data.name)) {
                return;
              }
              out.push(data.name);
              break;
            case "IdSelector":
              if (this.reInvalidIdentifier.test(data.name)) {
                return;
              }
              out.push(`#${data.name}`);
              break;
            case "Nth": {
              if (data.selector !== null) {
                return;
              }
              if (data.nth.type === "AnPlusB") {
                const a = parseInt(data.nth.a, 10) || null;
                const b2 = parseInt(data.nth.b, 10) || null;
                if (a !== null) {
                  out.push(`${a}n`);
                  if (b2 === null) {
                    break;
                  }
                  if (b2 < 0) {
                    out.push(`${b2}`);
                  } else {
                    out.push(`+${b2}`);
                  }
                } else if (b2 !== null) {
                  out.push(`${b2}`);
                }
              } else if (data.nth.type === "Identifier") {
                out.push(data.nth.name);
              }
              break;
            }
            case "PseudoElementSelector": {
              const hasArgs = Array.isArray(part.args);
              if (data.name.charCodeAt(0) !== 45) {
                if (this.knownPseudoElements.has(data.name) === false) {
                  return;
                }
                if (this.knownPseudoElementsWithArgs.has(data.name) && hasArgs === false) {
                  return;
                }
              }
              out.push(`::${data.name}`);
              if (hasArgs) {
                const arg = this.astSerialize(part.args);
                if (typeof arg !== "string") {
                  return;
                }
                out.push(`(${arg})`);
              }
              break;
            }
            case "PseudoClassSelector": {
              const hasArgs = Array.isArray(part.args);
              if (data.name.charCodeAt(0) !== 45) {
                if (this.knownPseudoClasses.has(data.name) === false) {
                  return;
                }
                if (this.knownPseudoClassesWithArgs.has(data.name) && hasArgs === false) {
                  return;
                }
              }
              out.push(`:${data.name}`);
              if (hasArgs) {
                const arg = this.astSerialize(part.args);
                if (typeof arg !== "string") {
                  return;
                }
                out.push(`(${arg.trim()})`);
              }
              break;
            }
            case "Raw":
              out.push(data.value);
              break;
            case "TypeSelector":
              if (this.reInvalidIdentifier.test(data.name)) {
                return;
              }
              out.push(data.name);
              break;
            default:
              break;
          }
          return out.join("");
        }
        astSerialize(parts, plainCSS = true) {
          const out = [];
          for (const part of parts) {
            const { data } = part;
            switch (data.type) {
              case "AttributeSelector":
              case "ClassSelector":
              case "Identifier":
              case "IdSelector":
              case "Nth":
              case "PseudoClassSelector":
              case "PseudoElementSelector": {
                const s = this.astSerializePart(part);
                if (s === void 0) {
                  return;
                }
                out.push(s);
                break;
              }
              case "Combinator": {
                const s = this.astSerializePart(part);
                if (s === void 0) {
                  return;
                }
                if (out.length !== 0) {
                  out.push(" ");
                }
                if (s !== " ") {
                  out.push(s, " ");
                }
                break;
              }
              case "TypeSelector": {
                const s = this.astSerializePart(part);
                if (s === void 0) {
                  return;
                }
                if (s === "*" && out.length !== 0) {
                  const before = out[out.length - 1];
                  if (before.endsWith(" ") === false) {
                    return;
                  }
                }
                out.push(s);
                break;
              }
              case "Raw":
                if (plainCSS) {
                  return;
                }
                out.push(this.astSerializePart(part));
                break;
              case "Selector":
                if (out.length !== 0) {
                  out.push(", ");
                }
                break;
              case "SelectorList":
                break;
              default:
                return;
            }
          }
          return out.join("");
        }
        astCompile(parts, details = {}) {
          if (Array.isArray(parts) === false) {
            return;
          }
          if (parts.length === 0) {
            return;
          }
          if (parts[0].data.type !== "SelectorList") {
            return;
          }
          const out = { selector: "" };
          const prelude = [];
          const tasks = [];
          let startOfSelector = true;
          for (const part of parts) {
            if (out.action !== void 0) {
              return;
            }
            const { data } = part;
            switch (data.type) {
              case "ActionSelector": {
                if (details.noaction) {
                  return;
                }
                if (prelude.length !== 0) {
                  if (tasks.length === 0) {
                    out.selector = prelude.join("");
                  } else {
                    tasks.push(this.createSpathTask(prelude.join("")));
                  }
                  prelude.length = 0;
                }
                const args = this.compileArgumentAst(data.name, part.args);
                if (args === void 0) {
                  return;
                }
                out.action = [data.name, args];
                break;
              }
              case "AttributeSelector":
              case "ClassSelector":
              case "IdSelector":
              case "PseudoClassSelector":
              case "PseudoElementSelector":
              case "TypeSelector": {
                const s = this.astSerializePart(part);
                if (s === void 0) {
                  return;
                }
                prelude.push(s);
                startOfSelector = false;
                break;
              }
              case "Combinator": {
                const s = this.astSerializePart(part);
                if (s === void 0) {
                  return;
                }
                if (startOfSelector === false || prelude.length !== 0) {
                  prelude.push(" ");
                }
                if (s !== " ") {
                  prelude.push(s, " ");
                }
                startOfSelector = false;
                break;
              }
              case "ProceduralSelector": {
                if (prelude.length !== 0) {
                  let spath = prelude.join("");
                  prelude.length = 0;
                  if (spath.endsWith(" ")) {
                    spath += "*";
                  }
                  if (tasks.length === 0) {
                    out.selector = spath;
                  } else {
                    tasks.push(this.createSpathTask(spath));
                  }
                }
                const args = this.compileArgumentAst(data.name, part.args);
                if (args === void 0) {
                  return;
                }
                tasks.push([data.name, args]);
                startOfSelector = false;
                break;
              }
              case "Selector":
                if (prelude.length !== 0) {
                  prelude.push(", ");
                }
                startOfSelector = true;
                break;
              case "SelectorList":
                startOfSelector = true;
                break;
              default:
                return;
            }
          }
          if (tasks.length === 0 && out.action === void 0) {
            if (prelude.length === 0) {
              return;
            }
            return prelude.join("").trim();
          }
          if (prelude.length !== 0) {
            tasks.push(this.createSpathTask(prelude.join("")));
          }
          if (tasks.length !== 0) {
            out.tasks = tasks;
          }
          return out;
        }
        astHasType(parts, type, depth = 2147483647) {
          if (Array.isArray(parts) === false) {
            return false;
          }
          for (const part of parts) {
            if (part.data.type === type) {
              return true;
            }
            if (Array.isArray(part.args) && depth !== 0 && this.astHasType(part.args, type, depth - 1)) {
              return true;
            }
          }
          return false;
        }
        astHasName(parts, name2) {
          if (Array.isArray(parts) === false) {
            return false;
          }
          for (const part of parts) {
            if (part.data.name === name2) {
              return true;
            }
            if (Array.isArray(part.args) && this.astHasName(part.args, name2)) {
              return true;
            }
          }
          return false;
        }
        astSelectorsFromSelectorList(args) {
          if (Array.isArray(args) === false) {
            return;
          }
          if (args.length < 3) {
            return;
          }
          if (args[0].data instanceof Object === false) {
            return;
          }
          if (args[0].data.type !== "SelectorList") {
            return;
          }
          if (args[1].data instanceof Object === false) {
            return;
          }
          if (args[1].data.type !== "Selector") {
            return;
          }
          const out = [];
          let beg = 1, end = 0, i = 2;
          for (; ; ) {
            if (i < args.length) {
              const type = args[i].data instanceof Object && args[i].data.type;
              if (type === "Selector") {
                end = i;
              }
            } else {
              end = args.length;
            }
            if (end !== 0) {
              const components = args.slice(beg + 1, end);
              if (components.length === 0) {
                return;
              }
              out.push(components);
              if (end === args.length) {
                break;
              }
              beg = end;
              end = 0;
            }
            if (i === args.length) {
              break;
            }
            i += 1;
          }
          return out;
        }
        astIsValidSelector(components) {
          const len = components.length;
          if (len === 0) {
            return false;
          }
          if (components[0].data.type === "Combinator") {
            return false;
          }
          if (len === 1) {
            return true;
          }
          if (components[len - 1].data.type === "Combinator") {
            return false;
          }
          return true;
        }
        astIsValidSelectorList(args) {
          const selectors = this.astSelectorsFromSelectorList(args);
          if (Array.isArray(selectors) === false || selectors.length === 0) {
            return false;
          }
          for (const selector of selectors) {
            if (this.astIsValidSelector(selector) !== true) {
              return false;
            }
          }
          return true;
        }
        isStyleInjectionFilter(selector) {
          const len = selector.length;
          return len !== 0 && selector.charCodeAt(len - 1) === 125;
        }
        translateStyleInjectionFilter(raw) {
          const matches = /^(.+)\s*\{([^}]+)\}$/.exec(raw);
          if (matches === null) {
            return;
          }
          const selector = matches[1].trim();
          const style = matches[2].trim();
          if (/^\s*remove:\s*true[; ]*$/.test(style)) {
            return `${selector}:remove()`;
          }
          return /display\s*:\s*none\s*!important;?$/.test(style) ? selector : `${selector}:style(${style})`;
        }
        createSpathTask(selector) {
          return ["spath", selector];
        }
        compileArgumentAst(operator, parts) {
          switch (operator) {
            case "has": {
              let r = this.astCompile(parts, { noaction: true });
              if (typeof r === "string") {
                r = { selector: r.replace(/^\s*:scope\s*/, "") };
              }
              return r;
            }
            case "not": {
              return this.astCompile(parts, { noaction: true });
            }
            default:
              break;
          }
          if (Array.isArray(parts) === false || parts.length === 0) {
            return;
          }
          const arg = this.astSerialize(parts, false);
          if (arg === void 0) {
            return;
          }
          switch (operator) {
            case "has-text":
              return this.compileText(arg);
            case "if":
              return this.compileSelector(arg);
            case "if-not":
              return this.compileSelector(arg);
            case "matches-attr":
            case "matches-prop":
              return this.compileMatchAttrArgument(arg);
            case "matches-css":
              return this.compileCSSDeclaration(arg);
            case "matches-css-after":
              return this.compileCSSDeclaration(`after, ${arg}`);
            case "matches-css-before":
              return this.compileCSSDeclaration(`before, ${arg}`);
            case "matches-media":
              return this.compileMediaQuery(arg);
            case "matches-path":
              return this.compileText(arg);
            case "min-text-length":
              return this.compileInteger(arg);
            case "others":
              return this.compileNoArgument(arg);
            case "remove":
              return this.compileNoArgument(arg);
            case "remove-attr":
              return this.compileText(arg);
            case "remove-class":
              return this.compileText(arg);
            case "shadow":
              return this.compileSelector(arg);
            case "style":
              return this.compileStyleProperties(arg);
            case "upward":
              return this.compileUpwardArgument(arg);
            case "watch-attr":
              return this.compileAttrList(arg);
            case "xpath":
              return this.compileXpathExpression(arg);
            default:
              break;
          }
        }
        isBadRegex(s) {
          try {
            void new RegExp(s);
          } catch (ex) {
            console.warn("[uBR] static-filtering-parser: isBadRegex check failed", ex);
            this.isBadRegex.message = ex.toString();
            return true;
          }
          return false;
        }
        unquoteString(s) {
          const end = s.length;
          if (end === 0) {
            return { s: "", end };
          }
          if (/^['"]/.test(s) === false) {
            return { s, i: end };
          }
          const quote = s.charCodeAt(0);
          const out = [];
          let i = 1, c = 0;
          for (; ; ) {
            c = s.charCodeAt(i);
            if (c === quote) {
              i += 1;
              break;
            }
            if (c === 92) {
              i += 1;
              if (i === end) {
                break;
              }
              c = s.charCodeAt(i);
              if (c !== 92 && c !== quote) {
                out.push(92);
              }
            }
            out.push(c);
            i += 1;
            if (i === end) {
              break;
            }
          }
          return { s: String.fromCharCode(...out), i };
        }
        compileMatchAttrArgument(s) {
          if (s === "") {
            return;
          }
          let attr = "", value = "";
          let r = this.unquoteString(s);
          if (r.i === s.length) {
            const pos = r.s.indexOf("=");
            if (pos === -1) {
              attr = r.s;
            } else {
              attr = r.s.slice(0, pos);
              value = r.s.slice(pos + 1);
            }
          } else {
            attr = r.s;
            if (s.charCodeAt(r.i) !== 61) {
              return;
            }
            value = s.slice(r.i + 1);
          }
          if (attr === "") {
            return;
          }
          if (value.length !== 0) {
            r = this.unquoteString(value);
            if (r.i !== value.length) {
              return;
            }
            value = r.s;
          }
          return { attr, value };
        }
        // Remove potentially present quotes before processing.
        compileText(s) {
          if (s === "") {
            this.error = "argument missing";
            return;
          }
          const r = this.unquoteString(s);
          if (r.i !== s.length) {
            return;
          }
          return r.s;
        }
        compileCSSDeclaration(s) {
          let pseudo;
          {
            const match2 = /^[a-z-]+,/.exec(s);
            if (match2 !== null) {
              pseudo = match2[0].slice(0, -1);
              s = s.slice(match2[0].length).trim();
            }
          }
          const pos = s.indexOf(":");
          if (pos === -1) {
            return;
          }
          const name2 = s.slice(0, pos).trim();
          const value = s.slice(pos + 1).trim();
          const match = this.reParseRegexLiteral.exec(value);
          let regexDetails;
          if (match !== null) {
            regexDetails = match[1];
            if (this.isBadRegex(regexDetails)) {
              return;
            }
            if (match[2]) {
              regexDetails = [regexDetails, match[2]];
            }
          } else {
            regexDetails = `^${escapeForRegex(value)}$`;
          }
          return { name: name2, pseudo, value: regexDetails };
        }
        compileInteger(s, min = 0, max = 2147483647) {
          if (/^\d+$/.test(s) === false) {
            return;
          }
          const n = parseInt(s, 10);
          if (n < min || n >= max) {
            return;
          }
          return n;
        }
        compileMediaQuery(s) {
          const parts = this.astFromRaw(s, "mediaQueryList");
          if (parts === void 0) {
            return;
          }
          if (this.astHasType(parts, "Raw")) {
            return;
          }
          if (this.astHasType(parts, "MediaQuery") === false) {
            return;
          }
          return s;
        }
        compileUpwardArgument(s) {
          const i = this.compileInteger(s, 1, 256);
          if (i !== void 0) {
            return i;
          }
          return this.compilePlainSelector(s);
        }
        compilePlainSelector(s) {
          const parts = this.astFromRaw(s, "selectorList");
          if (this.astIsValidSelectorList(parts) !== true) {
            return;
          }
          if (this.astHasType(parts, "ProceduralSelector")) {
            return;
          }
          if (this.astHasType(parts, "ActionSelector")) {
            return;
          }
          if (this.astHasType(parts, "Error")) {
            return;
          }
          return s;
        }
        compileNoArgument(s) {
          if (s === "") {
            return s;
          }
        }
        // https://github.com/uBlockOrigin/uBlock-issues/issues/668
        // https://github.com/uBlockOrigin/uBlock-issues/issues/1693
        // https://github.com/uBlockOrigin/uBlock-issues/issues/1811
        //   Forbid instances of:
        //   - `image-set(`
        //   - `url(`
        //   - any instance of `//`
        //   - backslashes `\`
        //   - opening comment `/*`
        compileStyleProperties(s) {
          if (/image-set\(|url\(|\/\s*\/|\\|\/\*/i.test(s)) {
            return;
          }
          const parts = this.astFromRaw(s, "declarationList");
          if (parts === void 0) {
            return;
          }
          if (this.astHasType(parts, "Declaration") === false) {
            return;
          }
          return s;
        }
        compileAttrList(s) {
          if (s === "") {
            return s;
          }
          const attrs = s.split(/\s*,\s*/);
          const out = [];
          for (const attr of attrs) {
            if (attr !== "") {
              out.push(attr);
            }
          }
          return out;
        }
        compileXpathExpression(s) {
          const r = this.unquoteString(s);
          if (r.i !== s.length) {
            return;
          }
          const doc = globalThis.document;
          if (doc instanceof Object === false) {
            return r.s;
          }
          try {
            const expr = doc.createExpression(r.s, null);
            expr.evaluate(doc, XPathResult.ANY_UNORDERED_NODE_TYPE);
          } catch (e) {
            console.warn("[uBR] static-filtering-parser: XPath evaluation failed", e);
            return;
          }
          return r.s;
        }
      };
      utils = /* @__PURE__ */ (() => {
        const preparserTokens = /* @__PURE__ */ new Map([
          ["ext_ublock", "ublock"],
          ["ext_ubol", "ubol"],
          ["ext_devbuild", "devbuild"],
          ["env_chromium", "chromium"],
          ["env_edge", "edge"],
          ["env_firefox", "firefox"],
          ["env_legacy", "legacy"],
          ["env_mobile", "mobile"],
          ["env_mv3", "mv3"],
          ["env_safari", "safari"],
          ["cap_html_filtering", "html_filtering"],
          ["cap_user_stylesheet", "user_stylesheet"],
          ["cap_ipaddress", "ipaddress"],
          ["false", "false"],
          // Hoping ABP-only list maintainers can at least make use of it to
          // help non-ABP content blockers better deal with filters benefiting
          // only ABP.
          ["ext_abp", "false"],
          // Compatibility with other blockers
          // https://adguard.com/kb/general/ad-filtering/create-own-filters/#conditions-directive
          ["adguard", "adguard"],
          ["adguard_app_android", "false"],
          ["adguard_app_cli", "false"],
          ["adguard_app_ios", "false"],
          ["adguard_app_mac", "false"],
          ["adguard_app_windows", "false"],
          ["adguard_ext_android_cb", "false"],
          ["adguard_ext_chromium", "chromium"],
          ["adguard_ext_chromium_mv3", "mv3"],
          ["adguard_ext_edge", "edge"],
          ["adguard_ext_firefox", "firefox"],
          ["adguard_ext_opera", "chromium"],
          ["adguard_ext_safari", "false"]
        ]);
        const toURL = (url) => {
          try {
            return new URL(url.trim());
          } catch (e) {
            console.warn("[uBR] static-filtering-parser: URL parse failed", e);
          }
        };
        class preparser {
          static evaluateExprToken(token, env = []) {
            const not = token.charCodeAt(0) === 33;
            if (not) {
              token = token.slice(1);
            }
            let state = preparserTokens.get(token);
            if (state === void 0) {
              if (token.startsWith("cap_") === false) {
                return;
              }
              state = "false";
            }
            return state === "false" && not || env.includes(state) !== not;
          }
          static evaluateExpr(expr, env = []) {
            if (expr.startsWith("(") && expr.endsWith(")")) {
              expr = expr.slice(1, -1);
            }
            const matches = Array.from(expr.matchAll(/(?:(?:&&|\|\|)\s+)?\S+/g));
            if (matches.length === 0) {
              return;
            }
            if (matches[0][0].startsWith("|") || matches[0][0].startsWith("&")) {
              return;
            }
            let result = this.evaluateExprToken(matches[0][0], env);
            for (let i = 1; i < matches.length; i++) {
              const parts = matches[i][0].split(/ +/);
              if (parts.length !== 2) {
                return;
              }
              const state = this.evaluateExprToken(parts[1], env);
              if (state === void 0) {
                return;
              }
              if (parts[0] === "||") {
                result = result || state;
              } else if (parts[0] === "&&") {
                result = result && state;
              } else {
                return;
              }
            }
            return result;
          }
          // This method returns an array of indices, corresponding to position in
          // the content string which should alternatively be parsed and discarded.
          static splitter(content, env = []) {
            const reIf = /^!#(if|else|endif)\b([^\n]*)(?:[\n\r]+|$)/gm;
            const stack = [];
            const parts = [0];
            let discard = false;
            const shouldDiscard = () => stack.some((v2) => v2.known && v2.discard);
            const begif = (details) => {
              if (discard === false && details.known && details.discard) {
                parts.push(details.pos);
                discard = true;
              }
              stack.push(details);
            };
            const endif = (match) => {
              stack.pop();
              const stopDiscard = shouldDiscard() === false;
              if (discard && stopDiscard) {
                parts.push(match.index + match[0].length);
                discard = false;
              }
            };
            for (; ; ) {
              const match = reIf.exec(content);
              if (match === null) {
                break;
              }
              switch (match[1]) {
                case "if": {
                  const result = this.evaluateExpr(match[2].trim(), env);
                  begif({
                    known: result !== void 0,
                    discard: result === false,
                    pos: match.index
                  });
                  break;
                }
                case "else": {
                  if (stack.length === 0) {
                    break;
                  }
                  const details = stack[stack.length - 1];
                  endif(match);
                  details.discard = details.discard === false;
                  details.pos = match.index;
                  begif(details);
                  break;
                }
                case "endif": {
                  endif(match);
                  break;
                }
                default:
                  break;
              }
            }
            parts.push(content.length);
            return parts;
          }
          static expandIncludes(parts, env = []) {
            const out = [];
            const reInclude = /^!#include +(\S+)[^\n\r]*(?:[\n\r]+|$)/gm;
            for (const part of parts) {
              if (typeof part === "string") {
                out.push(part);
                continue;
              }
              if (part instanceof Object === false) {
                continue;
              }
              const content = part.content;
              const slices = this.splitter(content, env);
              for (let i = 0, n = slices.length - 1; i < n; i++) {
                const slice = content.slice(slices[i + 0], slices[i + 1]);
                if ((i & 1) !== 0) {
                  out.push(slice);
                  continue;
                }
                let lastIndex = 0;
                for (; ; ) {
                  const match = reInclude.exec(slice);
                  if (match === null) {
                    break;
                  }
                  if (toURL(match[1]) !== void 0) {
                    continue;
                  }
                  if (match[1].indexOf("..") !== -1) {
                    continue;
                  }
                  const pos = part.url.lastIndexOf("/");
                  if (pos === -1) {
                    continue;
                  }
                  const subURL = part.url.slice(0, pos + 1) + match[1].trim();
                  out.push(
                    slice.slice(lastIndex, match.index + match[0].length),
                    `! >>>>>>>> ${subURL}
`,
                    { url: subURL },
                    `! <<<<<<<< ${subURL}
`
                  );
                  lastIndex = reInclude.lastIndex;
                }
                out.push(lastIndex === 0 ? slice : slice.slice(lastIndex));
              }
            }
            return out;
          }
          static prune(content, env) {
            const parts = this.splitter(content, env);
            const out = [];
            for (let i = 0, n = parts.length - 1; i < n; i += 2) {
              const beg = parts[i + 0];
              const end = parts[i + 1];
              out.push(content.slice(beg, end));
            }
            return out.join("\n");
          }
          static getHints() {
            const out = [];
            const vals = /* @__PURE__ */ new Set();
            for (const [key, val] of preparserTokens) {
              if (vals.has(val)) {
                continue;
              }
              vals.add(val);
              out.push(key);
            }
            return out;
          }
          static getTokens(env) {
            const out = /* @__PURE__ */ new Map();
            for (const [key, val] of preparserTokens) {
              out.set(key, val !== "false" && env.includes(val));
            }
            return Array.from(out);
          }
        }
        return {
          preparser
        };
      })();
    }
  });

  // src/lib/publicsuffixlist/publicsuffixlist.js
  var publicsuffixlist_default;
  var init_publicsuffixlist = __esm({
    "src/lib/publicsuffixlist/publicsuffixlist.js"() {
      publicsuffixlist_default = /* @__PURE__ */ (function() {
        const HOSTNAME_SLOT = 0;
        const LABEL_INDICES_SLOT = 256;
        const RULES_PTR_SLOT = 100;
        const SUFFIX_NOT_FOUND_SLOT = 399;
        const CHARDATA_PTR_SLOT = 101;
        const EMPTY_STRING = "";
        const SELFIE_MAGIC = 3;
        let wasmMemory;
        let pslBuffer32;
        let pslBuffer8;
        let pslByteLength = 0;
        let hostnameArg = EMPTY_STRING;
        const fireChangedEvent = function() {
          if (typeof window !== "object") {
            return;
          }
          if (window instanceof Object === false) {
            return;
          }
          if (window.dispatchEvent instanceof Function === false) {
            return;
          }
          if (window.CustomEvent instanceof Function === false) {
            return;
          }
          window.dispatchEvent(new CustomEvent("publicSuffixListChanged"));
        };
        const allocateBuffers = function(byteLength) {
          pslByteLength = byteLength + 3 & ~3;
          if (pslBuffer32 !== void 0 && pslBuffer32.byteLength >= pslByteLength) {
            return;
          }
          if (wasmMemory !== void 0) {
            const newPageCount = pslByteLength + 65535 >>> 16;
            const curPageCount = wasmMemory.buffer.byteLength >>> 16;
            const delta = newPageCount - curPageCount;
            if (delta > 0) {
              wasmMemory.grow(delta);
              pslBuffer32 = new Uint32Array(wasmMemory.buffer);
              pslBuffer8 = new Uint8Array(wasmMemory.buffer);
            }
          } else {
            pslBuffer8 = new Uint8Array(pslByteLength);
            pslBuffer32 = new Uint32Array(pslBuffer8.buffer);
          }
          hostnameArg = EMPTY_STRING;
          pslBuffer8[LABEL_INDICES_SLOT] = 0;
        };
        const parse = function(text, toAscii) {
          const rootRule = {
            l: EMPTY_STRING,
            // l => label
            f: 0,
            // f => flags
            c: void 0
            // c => children
          };
          {
            const compareLabels = function(a, b2) {
              const n = a.length;
              let d2 = n - b2.length;
              if (d2 !== 0) {
                return d2;
              }
              for (let i = 0; i < n; i++) {
                d2 = a.charCodeAt(i) - b2.charCodeAt(i);
                if (d2 !== 0) {
                  return d2;
                }
              }
              return 0;
            };
            const addToTree = function(rule, exception) {
              let node = rootRule;
              let end = rule.length;
              while (end > 0) {
                const beg = rule.lastIndexOf(".", end - 1);
                const label = rule.slice(beg + 1, end);
                end = beg;
                if (Array.isArray(node.c) === false) {
                  const child = { l: label, f: 0, c: void 0 };
                  node.c = [child];
                  node = child;
                  continue;
                }
                let left = 0;
                let right = node.c.length;
                while (left < right) {
                  const i = left + right >>> 1;
                  const d2 = compareLabels(label, node.c[i].l);
                  if (d2 < 0) {
                    right = i;
                    if (right === left) {
                      const child = {
                        l: label,
                        f: 0,
                        c: void 0
                      };
                      node.c.splice(left, 0, child);
                      node = child;
                      break;
                    }
                    continue;
                  }
                  if (d2 > 0) {
                    left = i + 1;
                    if (left === right) {
                      const child = {
                        l: label,
                        f: 0,
                        c: void 0
                      };
                      node.c.splice(right, 0, child);
                      node = child;
                      break;
                    }
                    continue;
                  }
                  node = node.c[i];
                  break;
                }
              }
              node.f |= 1;
              if (exception) {
                node.f |= 2;
              }
            };
            addToTree("*", false);
            const mustPunycode = /[^a-z0-9.-]/;
            const textEnd = text.length;
            let lineBeg = 0;
            while (lineBeg < textEnd) {
              let lineEnd = text.indexOf("\n", lineBeg);
              if (lineEnd === -1) {
                lineEnd = text.indexOf("\r", lineBeg);
                if (lineEnd === -1) {
                  lineEnd = textEnd;
                }
              }
              let line = text.slice(lineBeg, lineEnd).trim();
              lineBeg = lineEnd + 1;
              const pos = line.indexOf("//");
              if (pos !== -1) {
                line = line.slice(0, pos);
              }
              line = line.trim();
              if (line.length === 0) {
                continue;
              }
              const exception = line.charCodeAt(0) === 33;
              if (exception) {
                line = line.slice(1);
              }
              if (mustPunycode.test(line)) {
                line = toAscii(line.toLowerCase());
              }
              addToTree(line, exception);
            }
          }
          {
            const labelToOffsetMap = /* @__PURE__ */ new Map();
            const treeData = [];
            const charData = [];
            const allocate = function(n) {
              const ibuf = treeData.length;
              for (let i = 0; i < n; i++) {
                treeData.push(0);
              }
              return ibuf;
            };
            const storeNode = function(ibuf, node) {
              const nChars = node.l.length;
              const nChildren = node.c !== void 0 ? node.c.length : 0;
              treeData[ibuf + 0] = nChildren << 16 | node.f << 8 | nChars;
              if (nChars <= 4) {
                let v2 = 0;
                if (nChars > 0) {
                  v2 |= node.l.charCodeAt(0);
                  if (nChars > 1) {
                    v2 |= node.l.charCodeAt(1) << 8;
                    if (nChars > 2) {
                      v2 |= node.l.charCodeAt(2) << 16;
                      if (nChars > 3) {
                        v2 |= node.l.charCodeAt(3) << 24;
                      }
                    }
                  }
                }
                treeData[ibuf + 1] = v2;
              } else {
                let offset = labelToOffsetMap.get(node.l);
                if (offset === void 0) {
                  offset = charData.length;
                  for (let i = 0; i < nChars; i++) {
                    charData.push(node.l.charCodeAt(i));
                  }
                  labelToOffsetMap.set(node.l, offset);
                }
                treeData[ibuf + 1] = offset;
              }
              if (Array.isArray(node.c) === false) {
                treeData[ibuf + 2] = 0;
                return;
              }
              const iarray = allocate(nChildren * 3);
              treeData[ibuf + 2] = iarray;
              for (let i = 0; i < nChildren; i++) {
                storeNode(iarray + i * 3, node.c[i]);
              }
            };
            allocate(512 >> 2);
            const iRootRule = allocate(3);
            storeNode(iRootRule, rootRule);
            treeData[RULES_PTR_SLOT] = iRootRule;
            const iCharData = treeData.length << 2;
            treeData[CHARDATA_PTR_SLOT] = iCharData;
            const byteLength = (treeData.length << 2) + (charData.length + 3 & ~3);
            allocateBuffers(byteLength);
            pslBuffer32.set(treeData);
            pslBuffer8.set(charData, treeData.length << 2);
          }
          fireChangedEvent();
        };
        const setHostnameArg = function(hostname) {
          const buf = pslBuffer8;
          if (hostname === hostnameArg) {
            return buf[LABEL_INDICES_SLOT];
          }
          if (hostname === null || hostname.length === 0) {
            hostnameArg = EMPTY_STRING;
            return buf[LABEL_INDICES_SLOT] = 0;
          }
          hostname = hostname.toLowerCase();
          hostnameArg = hostname;
          let n = hostname.length;
          if (n > 255) {
            n = 255;
          }
          buf[LABEL_INDICES_SLOT] = n;
          let i = n;
          let j2 = LABEL_INDICES_SLOT + 1;
          while (i--) {
            const c = hostname.charCodeAt(i);
            if (c === 46) {
              buf[j2 + 0] = i + 1;
              buf[j2 + 1] = i;
              j2 += 2;
            }
            buf[i] = c;
          }
          buf[j2] = 0;
          return n;
        };
        const getPublicSuffixPosJS = function() {
          const buf8 = pslBuffer8;
          const buf32 = pslBuffer32;
          const iCharData = buf32[CHARDATA_PTR_SLOT];
          let iNode = pslBuffer32[RULES_PTR_SLOT];
          let cursorPos = -1;
          let iLabel = LABEL_INDICES_SLOT;
          for (; ; ) {
            const labelBeg = buf8[iLabel + 1];
            const labelLen = buf8[iLabel + 0] - labelBeg;
            let r = buf32[iNode + 0] >>> 16;
            if (r === 0) {
              break;
            }
            const iCandidates = buf32[iNode + 2];
            let l = 0;
            let iFound = 0;
            while (l < r) {
              const iCandidate = l + r >>> 1;
              const iCandidateNode = iCandidates + iCandidate + (iCandidate << 1);
              const candidateLen = buf32[iCandidateNode + 0] & 255;
              let d2 = labelLen - candidateLen;
              if (d2 === 0) {
                const iCandidateChar = candidateLen <= 4 ? iCandidateNode + 1 << 2 : iCharData + buf32[iCandidateNode + 1];
                for (let i = 0; i < labelLen; i++) {
                  d2 = buf8[labelBeg + i] - buf8[iCandidateChar + i];
                  if (d2 !== 0) {
                    break;
                  }
                }
              }
              if (d2 < 0) {
                r = iCandidate;
              } else if (d2 > 0) {
                l = iCandidate + 1;
              } else {
                iFound = iCandidateNode;
                break;
              }
            }
            if (iFound === 0) {
              if (buf32[iCandidates + 1] !== 42) {
                break;
              }
              buf8[SUFFIX_NOT_FOUND_SLOT] = 1;
              iFound = iCandidates;
            }
            iNode = iFound;
            if ((buf32[iNode + 0] & 512) !== 0) {
              if (iLabel > LABEL_INDICES_SLOT) {
                return iLabel - 2;
              }
              break;
            }
            if ((buf32[iNode + 0] & 256) !== 0) {
              cursorPos = iLabel;
            }
            if (labelBeg === 0) {
              break;
            }
            iLabel += 2;
          }
          return cursorPos;
        };
        let getPublicSuffixPosWASM;
        let getPublicSuffixPos = getPublicSuffixPosJS;
        const getPublicSuffix = function(hostname) {
          if (pslBuffer32 === void 0) {
            return EMPTY_STRING;
          }
          const hostnameLen = setHostnameArg(hostname);
          const buf8 = pslBuffer8;
          if (hostnameLen === 0 || buf8[0] === 46) {
            return EMPTY_STRING;
          }
          const cursorPos = getPublicSuffixPos();
          if (cursorPos === -1) {
            return EMPTY_STRING;
          }
          const beg = buf8[cursorPos + 1];
          return beg === 0 ? hostnameArg : hostnameArg.slice(beg);
        };
        const getDomain = function(hostname) {
          if (pslBuffer32 === void 0) {
            return EMPTY_STRING;
          }
          const hostnameLen = setHostnameArg(hostname);
          const buf8 = pslBuffer8;
          if (hostnameLen === 0 || buf8[0] === 46) {
            return EMPTY_STRING;
          }
          const cursorPos = getPublicSuffixPos();
          if (cursorPos === -1 || buf8[cursorPos + 1] === 0) {
            return EMPTY_STRING;
          }
          const beg = buf8[cursorPos + 3];
          return beg === 0 ? hostnameArg : hostnameArg.slice(beg);
        };
        const suffixInPSL = function(hostname) {
          if (pslBuffer32 === void 0) {
            return false;
          }
          const hostnameLen = setHostnameArg(hostname);
          const buf8 = pslBuffer8;
          if (hostnameLen === 0 || buf8[0] === 46) {
            return false;
          }
          buf8[SUFFIX_NOT_FOUND_SLOT] = 0;
          const cursorPos = getPublicSuffixPos();
          return cursorPos !== -1 && buf8[cursorPos + 1] === 0 && buf8[SUFFIX_NOT_FOUND_SLOT] !== 1;
        };
        const toSelfie = function(encoder) {
          if (pslBuffer8 === void 0) {
            return "";
          }
          if (encoder instanceof Object) {
            const bufferStr = encoder.encode(pslBuffer8.buffer, pslByteLength);
            return `${SELFIE_MAGIC}	${bufferStr}`;
          }
          return {
            magic: SELFIE_MAGIC,
            buf32: pslBuffer32.subarray(0, pslByteLength >> 2)
          };
        };
        const fromSelfie = function(selfie, decoder) {
          let byteLength = 0;
          if (typeof selfie === "string" && selfie.length !== 0 && decoder instanceof Object) {
            const pos = selfie.indexOf("	");
            if (pos === -1 || selfie.slice(0, pos) !== `${SELFIE_MAGIC}`) {
              return false;
            }
            const bufferStr = selfie.slice(pos + 1);
            byteLength = decoder.decodeSize(bufferStr);
            if (byteLength === 0) {
              return false;
            }
            allocateBuffers(byteLength);
            decoder.decode(bufferStr, pslBuffer8.buffer);
          } else if (selfie instanceof Object && selfie.magic === SELFIE_MAGIC && selfie.buf32 instanceof Uint32Array) {
            byteLength = selfie.buf32.length << 2;
            allocateBuffers(byteLength);
            pslBuffer32.set(selfie.buf32);
          } else {
            return false;
          }
          hostnameArg = EMPTY_STRING;
          pslBuffer8[LABEL_INDICES_SLOT] = 0;
          fireChangedEvent();
          return true;
        };
        const enableWASM = /* @__PURE__ */ (() => {
          let wasmPromise;
          const getWasmInstance = async function(wasmModuleFetcher, path) {
            if (typeof WebAssembly !== "object") {
              return false;
            }
            const uint32s = new Uint32Array(1);
            const uint8s = new Uint8Array(uint32s.buffer);
            uint32s[0] = 1;
            if (uint8s[0] !== 1) {
              return false;
            }
            try {
              const module = await wasmModuleFetcher(`${path}publicsuffixlist`);
              if (module instanceof WebAssembly.Module === false) {
                return false;
              }
              const pageCount = pslBuffer8 !== void 0 ? pslBuffer8.byteLength + 65535 >>> 16 : 1;
              const memory = new WebAssembly.Memory({ initial: pageCount });
              const instance = await WebAssembly.instantiate(module, {
                imports: { memory }
              });
              if (instance instanceof WebAssembly.Instance === false) {
                return false;
              }
              const curPageCount = memory.buffer.byteLength >>> 16;
              const newPageCount = pslBuffer8 !== void 0 ? pslBuffer8.byteLength + 65535 >>> 16 : 0;
              if (newPageCount > curPageCount) {
                memory.grow(newPageCount - curPageCount);
              }
              if (pslBuffer32 !== void 0) {
                const buf8 = new Uint8Array(memory.buffer);
                const buf32 = new Uint32Array(memory.buffer);
                buf32.set(pslBuffer32);
                pslBuffer8 = buf8;
                pslBuffer32 = buf32;
              }
              wasmMemory = memory;
              getPublicSuffixPosWASM = instance.exports.getPublicSuffixPos;
              getPublicSuffixPos = getPublicSuffixPosWASM;
              return true;
            } catch (reason) {
              console.info(reason);
            }
            return false;
          };
          return async function(wasmModuleFetcher, path) {
            if (getPublicSuffixPosWASM instanceof Function) {
              return true;
            }
            if (wasmPromise instanceof Promise === false) {
              wasmPromise = getWasmInstance(wasmModuleFetcher, path);
            }
            return wasmPromise;
          };
        })();
        const disableWASM = function() {
          if (getPublicSuffixPosWASM instanceof Function) {
            getPublicSuffixPos = getPublicSuffixPosJS;
            getPublicSuffixPosWASM = void 0;
          }
          if (wasmMemory === void 0) {
            return;
          }
          if (pslBuffer32 !== void 0) {
            const buf8 = new Uint8Array(pslByteLength);
            const buf32 = new Uint32Array(buf8.buffer);
            buf32.set(pslBuffer32);
            pslBuffer8 = buf8;
            pslBuffer32 = buf32;
          }
          wasmMemory = void 0;
        };
        return {
          version: "2.0",
          parse,
          getDomain,
          suffixInPSL,
          getPublicSuffix,
          toSelfie,
          fromSelfie,
          disableWASM,
          enableWASM
        };
      })();
    }
  });

  // src/lib/punycode.js
  var punycode_default;
  var init_punycode = __esm({
    "src/lib/punycode.js"() {
      punycode_default = (function() {
        let punycode, maxInt = 2147483647, base = 36, tMin = 1, tMax = 26, skew = 38, damp = 700, initialBias = 72, initialN = 128, delimiter = "-", regexPunycode = /^xn--/, regexNonASCII = /[^\x20-\x7E]/, regexSeparators = /[\x2E\u3002\uFF0E\uFF61]/g, errors = {
          "overflow": "Overflow: input needs wider integers to process",
          "not-basic": "Illegal input >= 0x80 (not a basic code point)",
          "invalid-input": "Invalid input"
        }, baseMinusTMin = base - tMin, floor = Math.floor, stringFromCharCode = String.fromCharCode, key;
        function error(type) {
          throw new RangeError(errors[type]);
        }
        function map(array, fn2) {
          let length = array.length;
          const result = [];
          while (length--) {
            result[length] = fn2(array[length]);
          }
          return result;
        }
        function mapDomain(string, fn2) {
          const parts = string.split("@");
          let result = "";
          if (parts.length > 1) {
            result = `${parts[0]}@`;
            string = parts[1];
          }
          string = string.replace(regexSeparators, ".");
          const labels = string.split(".");
          const encoded = map(labels, fn2).join(".");
          return result + encoded;
        }
        function ucs2decode(string) {
          let output = [], counter = 0, length = string.length, value, extra;
          while (counter < length) {
            value = string.charCodeAt(counter++);
            if (value >= 55296 && value <= 56319 && counter < length) {
              extra = string.charCodeAt(counter++);
              if ((extra & 64512) == 56320) {
                output.push(((value & 1023) << 10) + (extra & 1023) + 65536);
              } else {
                output.push(value);
                counter--;
              }
            } else {
              output.push(value);
            }
          }
          return output;
        }
        function ucs2encode(array) {
          return map(array, (value) => {
            let output = "";
            if (value > 65535) {
              value -= 65536;
              output += stringFromCharCode(value >>> 10 & 1023 | 55296);
              value = 56320 | value & 1023;
            }
            output += stringFromCharCode(value);
            return output;
          }).join("");
        }
        function basicToDigit(codePoint) {
          if (codePoint - 48 < 10) {
            return codePoint - 22;
          }
          if (codePoint - 65 < 26) {
            return codePoint - 65;
          }
          if (codePoint - 97 < 26) {
            return codePoint - 97;
          }
          return base;
        }
        function digitToBasic(digit, flag) {
          return digit + 22 + 75 * (digit < 26) - ((flag != 0) << 5);
        }
        function adapt(delta, numPoints, firstTime) {
          let k2 = 0;
          delta = firstTime ? floor(delta / damp) : delta >> 1;
          delta += floor(delta / numPoints);
          for (; delta > baseMinusTMin * tMax >> 1; k2 += base) {
            delta = floor(delta / baseMinusTMin);
          }
          return floor(k2 + (baseMinusTMin + 1) * delta / (delta + skew));
        }
        function decode(input) {
          let output = [], inputLength = input.length, out, i = 0, n = initialN, bias = initialBias, basic, j2, index, oldi, w2, k2, digit, t, baseMinusT;
          basic = input.lastIndexOf(delimiter);
          if (basic < 0) {
            basic = 0;
          }
          for (j2 = 0; j2 < basic; ++j2) {
            if (input.charCodeAt(j2) >= 128) {
              error("not-basic");
            }
            output.push(input.charCodeAt(j2));
          }
          for (index = basic > 0 ? basic + 1 : 0; index < inputLength; ) {
            for (oldi = i, w2 = 1, k2 = base; ; k2 += base) {
              if (index >= inputLength) {
                error("invalid-input");
              }
              digit = basicToDigit(input.charCodeAt(index++));
              if (digit >= base || digit > floor((maxInt - i) / w2)) {
                error("overflow");
              }
              i += digit * w2;
              t = k2 <= bias ? tMin : k2 >= bias + tMax ? tMax : k2 - bias;
              if (digit < t) {
                break;
              }
              baseMinusT = base - t;
              if (w2 > floor(maxInt / baseMinusT)) {
                error("overflow");
              }
              w2 *= baseMinusT;
            }
            out = output.length + 1;
            bias = adapt(i - oldi, out, oldi == 0);
            if (floor(i / out) > maxInt - n) {
              error("overflow");
            }
            n += floor(i / out);
            i %= out;
            output.splice(i++, 0, n);
          }
          return ucs2encode(output);
        }
        function encode(input) {
          let n, delta, handledCPCount, basicLength, bias, j2, m, q2, k2, t, currentValue, output = [], inputLength, handledCPCountPlusOne, baseMinusT, qMinusT;
          input = ucs2decode(input);
          inputLength = input.length;
          n = initialN;
          delta = 0;
          bias = initialBias;
          for (j2 = 0; j2 < inputLength; ++j2) {
            currentValue = input[j2];
            if (currentValue < 128) {
              output.push(stringFromCharCode(currentValue));
            }
          }
          handledCPCount = basicLength = output.length;
          if (basicLength) {
            output.push(delimiter);
          }
          while (handledCPCount < inputLength) {
            for (m = maxInt, j2 = 0; j2 < inputLength; ++j2) {
              currentValue = input[j2];
              if (currentValue >= n && currentValue < m) {
                m = currentValue;
              }
            }
            handledCPCountPlusOne = handledCPCount + 1;
            if (m - n > floor((maxInt - delta) / handledCPCountPlusOne)) {
              error("overflow");
            }
            delta += (m - n) * handledCPCountPlusOne;
            n = m;
            for (j2 = 0; j2 < inputLength; ++j2) {
              currentValue = input[j2];
              if (currentValue < n && ++delta > maxInt) {
                error("overflow");
              }
              if (currentValue == n) {
                for (q2 = delta, k2 = base; ; k2 += base) {
                  t = k2 <= bias ? tMin : k2 >= bias + tMax ? tMax : k2 - bias;
                  if (q2 < t) {
                    break;
                  }
                  qMinusT = q2 - t;
                  baseMinusT = base - t;
                  output.push(
                    stringFromCharCode(digitToBasic(t + qMinusT % baseMinusT, 0))
                  );
                  q2 = floor(qMinusT / baseMinusT);
                }
                output.push(stringFromCharCode(digitToBasic(q2, 0)));
                bias = adapt(delta, handledCPCountPlusOne, handledCPCount == basicLength);
                delta = 0;
                ++handledCPCount;
              }
            }
            ++delta;
            ++n;
          }
          return output.join("");
        }
        function toUnicode(input) {
          return mapDomain(input, (string) => {
            return regexPunycode.test(string) ? decode(string.slice(4).toLowerCase()) : string;
          });
        }
        function toASCII(input) {
          return mapDomain(input, (string) => {
            return regexNonASCII.test(string) ? `xn--${encode(string)}` : string;
          });
        }
        punycode = {
          /**
           * A string representing the current Punycode.js version number.
           * @memberOf punycode
           * @type String
           */
          "version": "1.3.2",
          /**
           * An object of methods to convert from JavaScript's internal character
           * representation (UCS-2) to Unicode code points, and back.
           * @see <https://mathiasbynens.be/notes/javascript-encoding>
           * @memberOf punycode
           * @type Object
           */
          "ucs2": {
            "decode": ucs2decode,
            "encode": ucs2encode
          },
          "decode": decode,
          "encode": encode,
          "toASCII": toASCII,
          "toUnicode": toUnicode
        };
        return punycode;
      })();
    }
  });

  // src/js/uri-utils.ts
  function domainFromHostname(hostname) {
    return reIPAddressNaive.test(hostname) ? hostname : publicsuffixlist_default.getDomain(hostname);
  }
  function domainFromURI(uri) {
    if (!uri) {
      return "";
    }
    return domainFromHostname(hostnameFromURI(uri));
  }
  function entityFromDomain(domain) {
    const pos = domain.indexOf(".");
    return pos !== -1 ? `${domain.slice(0, pos)}.*` : "";
  }
  function entityFromHostname(hostname, domain) {
    if (domain === void 0) {
      domain = domainFromHostname(hostname);
    }
    const entity = entityFromDomain(domain);
    if (entity === "") {
      return "";
    }
    return `${hostname.slice(0, -domain.length)}${entity}`;
  }
  function hostnameFromURI(uri) {
    let match = reHostnameFromCommonURL.exec(uri);
    if (match !== null) {
      return match[0].slice(8, -1);
    }
    match = reAuthorityFromURI.exec(uri);
    if (match === null) {
      return "";
    }
    const authority = match[1].slice(2);
    if (reHostFromNakedAuthority.test(authority)) {
      return authority.toLowerCase();
    }
    match = reHostFromAuthority.exec(authority);
    if (match === null) {
      match = reIPv6FromAuthority.exec(authority);
      if (match === null) {
        return "";
      }
    }
    let hostname = match[1];
    while (hostname.endsWith(".")) {
      hostname = hostname.slice(0, -1);
    }
    if (reMustNormalizeHostname.test(hostname)) {
      hostname = punycode_default.toASCII(hostname.toLowerCase());
    }
    return hostname;
  }
  function hostnameFromNetworkURL(url) {
    const matches = reHostnameFromNetworkURL.exec(url);
    return matches !== null ? matches[1] : "";
  }
  function originFromURI(uri) {
    let match = reHostnameFromCommonURL.exec(uri);
    if (match !== null) {
      return match[0].slice(0, -1);
    }
    match = reOriginFromURI.exec(uri);
    return match !== null ? match[0].toLowerCase() : "";
  }
  function isNetworkURI(uri) {
    return reNetworkURI.test(uri);
  }
  function toBroaderHostname(hostname) {
    const pos = hostname.indexOf(".");
    if (pos !== -1) {
      return hostname.slice(pos + 1);
    }
    return hostname !== "*" && hostname !== "" ? "*" : "";
  }
  function toBroaderIPv4Address(ipaddress) {
    if (ipaddress === "*" || ipaddress === "") {
      return "";
    }
    const pos = ipaddress.lastIndexOf(".");
    if (pos === -1) {
      return "*";
    }
    return ipaddress.slice(0, pos);
  }
  function toBroaderIPv6Address(ipaddress) {
    return ipaddress !== "*" && ipaddress !== "" ? "*" : "";
  }
  function decomposeHostname(hostname, out) {
    if (out.length !== 0 && out[0] === hostname) {
      return out;
    }
    let broadenFn;
    if (reHostnameVeryCoarse.test(hostname) === false) {
      if (reIPv4VeryCoarse.test(hostname)) {
        broadenFn = toBroaderIPv4Address;
      } else if (hostname.startsWith("[")) {
        broadenFn = toBroaderIPv6Address;
      }
    }
    if (broadenFn === void 0) {
      broadenFn = toBroaderHostname;
    }
    out[0] = hostname;
    let i = 1;
    for (; ; ) {
      hostname = broadenFn(hostname);
      if (hostname === "") {
        break;
      }
      out[i++] = hostname;
    }
    out.length = i;
    return out;
  }
  var reHostnameFromCommonURL, reAuthorityFromURI, reHostFromNakedAuthority, reHostFromAuthority, reIPv6FromAuthority, reMustNormalizeHostname, reOriginFromURI, reHostnameFromNetworkURL, reIPAddressNaive, reNetworkURI, reIPv4VeryCoarse, reHostnameVeryCoarse;
  var init_uri_utils = __esm({
    "src/js/uri-utils.ts"() {
      init_publicsuffixlist();
      init_punycode();
      reHostnameFromCommonURL = /^https:\/\/[0-9a-z._-]+[0-9a-z]\//;
      reAuthorityFromURI = /^(?:[^:/?#]+:)?(\/\/[^/?#]+)/;
      reHostFromNakedAuthority = /^[0-9a-z._-]+[0-9a-z]$/i;
      reHostFromAuthority = /^(?:[^@]*@)?([^:]+)(?::\d*)?$/;
      reIPv6FromAuthority = /^(?:[^@]*@)?(\[[0-9a-f:]+\])(?::\d*)?$/i;
      reMustNormalizeHostname = /[^0-9a-z._-]/;
      reOriginFromURI = /^[^:/?#]+:\/\/[^/?#]+/;
      reHostnameFromNetworkURL = /^(?:http|ws|ftp)s?:\/\/([0-9a-z_][0-9a-z._-]*[0-9a-z])(?::\d+)?\//;
      reIPAddressNaive = /^\d+\.\d+\.\d+\.\d+$|^\[[\da-zA-Z:]+\]$/;
      reNetworkURI = /^(?:ftps?|https?|wss?):\/\//;
      reIPv4VeryCoarse = /\.\d+$/;
      reHostnameVeryCoarse = /[g-z_-]/;
    }
  });

  // src/js/text-utils.ts
  function orphanizeString(s) {
    return JSON.parse(JSON.stringify(s));
  }
  var LineIterator;
  var init_text_utils = __esm({
    "src/js/text-utils.ts"() {
      LineIterator = class {
        constructor(text, offset) {
          __publicField(this, "text");
          __publicField(this, "textLen");
          __publicField(this, "offset");
          this.text = text;
          this.textLen = this.text.length;
          this.offset = offset || 0;
        }
        next(offset) {
          if (offset !== void 0) {
            this.offset += offset;
          }
          let lineEnd = this.text.indexOf("\n", this.offset);
          if (lineEnd === -1) {
            lineEnd = this.text.indexOf("\r", this.offset);
            if (lineEnd === -1) {
              lineEnd = this.textLen;
            }
          }
          const line = this.text.slice(this.offset, lineEnd);
          this.offset = lineEnd + 1;
          return line;
        }
        peek(n) {
          const offset = this.offset;
          return this.text.slice(offset, offset + n);
        }
        charCodeAt(offset) {
          return this.text.charCodeAt(this.offset + offset);
        }
        eot() {
          return this.offset >= this.textLen;
        }
      };
    }
  });

  // src/js/tasks.ts
  function queueTask(func, timeout = 5e3) {
    if (typeof requestIdleCallback === "undefined") {
      return setTimeout(func, 1);
    }
    return requestIdleCallback(func, { timeout });
  }
  function dropTask(id) {
    if (typeof cancelIdleCallback === "undefined") {
      return clearTimeout(id);
    }
    return cancelIdleCallback(id);
  }
  var init_tasks = __esm({
    "src/js/tasks.ts"() {
    }
  });

  // src/lib/regexanalyzer/regex.js
  var regex_default;
  var init_regex = __esm({
    "src/lib/regexanalyzer/regex.js"() {
      regex_default = (function(root, name2, factory) {
        const __version__ = "1.2.0", PROTO = "prototype", OP = Object[PROTO], AP = Array[PROTO], Keys = Object.keys, to_string = OP.toString, HAS = OP.hasOwnProperty, fromCharCode = String.fromCharCode, fromCodePoint = String.fromCodePoint || String.fromCharCode, CHAR = "charAt", CHARCODE = "charCodeAt", CODEPOINT = String.prototype.codePointAt ? "codePointAt" : CHARCODE, toJSON = JSON.stringify, INF = Infinity, ESC = "\\", specialChars = {
          ".": "MatchAnyChar",
          "|": "MatchEither",
          "?": "MatchZeroOrOne",
          "*": "MatchZeroOrMore",
          "+": "MatchOneOrMore",
          "^": "MatchStart",
          "$": "MatchEnd",
          "{": "StartRepeats",
          "}": "EndRepeats",
          "(": "StartGroup",
          ")": "EndGroup",
          "[": "StartCharGroup",
          "]": "EndCharGroup"
        }, specialCharsEscaped = {
          "\\": "ESC",
          "/": "/",
          "0": "NULChar",
          "f": "FormFeed",
          "n": "LineFeed",
          "r": "CarriageReturn",
          "t": "HorizontalTab",
          "v": "VerticalTab",
          "b": "MatchWordBoundary",
          "B": "MatchNonWordBoundary",
          "s": "MatchSpaceChar",
          "S": "MatchNonSpaceChar",
          "w": "MatchWordChar",
          "W": "MatchNonWordChar",
          "d": "MatchDigitChar",
          "D": "MatchNonDigitChar"
        }, T_SEQUENCE = 1, T_ALTERNATION = 2, T_GROUP = 4, T_CHARGROUP = 8, T_QUANTIFIER = 16, T_UNICODECHAR = 32, T_HEXCHAR = 64, T_SPECIAL = 128, T_CHARS = 256, T_CHARRANGE = 512, T_STRING = 1024, T_COMMENT = 2048;
        function is_array(x2) {
          return x2 instanceof Array || "[object Array]" === to_string.call(x2);
        }
        function is_string(x2) {
          return x2 instanceof String || "[object String]" === to_string.call(x2);
        }
        function is_regexp(x2) {
          return x2 instanceof RegExp || "[object RegExp]" === to_string.call(x2);
        }
        function array(x2) {
          return is_array(x2) ? x2 : [x2];
        }
        function clone(obj, cloned) {
          cloned = cloned || {};
          for (const p in obj) if (HAS.call(obj, p)) cloned[p] = obj[p];
          return cloned;
        }
        function RE_OBJ(re2, flags, flavor) {
          const self2 = this;
          self2.re = re2;
          self2.flags = flags;
          self2.flavor = flavor;
          self2.len = re2.length;
          self2.pos = 0;
          self2.index = 0;
          self2.groupIndex = 0;
          self2.group = {};
          self2.inGroup = 0;
        }
        RE_OBJ[PROTO] = {
          constructor: RE_OBJ,
          re: null,
          flags: null,
          flavor: "",
          len: null,
          pos: null,
          index: null,
          groupIndex: null,
          inGroup: null,
          groups: null,
          dispose: function() {
            const self2 = this;
            self2.re = null;
            self2.flags = null;
            self2.flavor = null;
            self2.len = null;
            self2.pos = null;
            self2.index = null;
            self2.groupIndex = null;
            self2.group = null;
            self2.inGroup = null;
          }
        };
        function Node2(type, value, flags) {
          const self2 = this;
          if (!(self2 instanceof Node2)) return new Node2(type, value, flags);
          self2.type = type;
          self2.val = value;
          self2.flags = flags || {};
          switch (type) {
            case T_SEQUENCE:
              self2.typeName = "Sequence";
              break;
            case T_ALTERNATION:
              self2.typeName = "Alternation";
              break;
            case T_GROUP:
              self2.typeName = "Group";
              break;
            case T_CHARGROUP:
              self2.typeName = "CharacterGroup";
              break;
            case T_CHARS:
              self2.typeName = "Characters";
              break;
            case T_CHARRANGE:
              self2.typeName = "CharacterRange";
              break;
            case T_STRING:
              self2.typeName = "String";
              break;
            case T_QUANTIFIER:
              self2.typeName = "Quantifier";
              break;
            case T_UNICODECHAR:
              self2.typeName = "UnicodeChar";
              break;
            case T_HEXCHAR:
              self2.typeName = "HexChar";
              break;
            case T_SPECIAL:
              self2.typeName = "Special";
              break;
            case T_COMMENT:
              self2.typeName = "Comment";
              break;
            default:
              self2.typeName = "unspecified";
              break;
          }
        }
        ;
        Node2.toObjectStatic = function toObject(v2) {
          if (v2 instanceof Node2) {
            return v2.flags && Object.keys(v2.flags).length ? {
              type: v2.typeName,
              value: toObject(v2.val),
              flags: v2.flags
            } : {
              type: v2.typeName,
              value: toObject(v2.val)
            };
          } else if (is_array(v2)) {
            return v2.map(toObject);
          }
          return v2;
        };
        Node2[PROTO] = {
          constructor: Node2,
          type: null,
          typeName: null,
          val: null,
          flags: null,
          dispose: function() {
            const self2 = this;
            self2.val = null;
            self2.flags = null;
            self2.type = null;
            self2.typeName = null;
            return self2;
          },
          toObject: function() {
            return Node2.toObjectStatic(this);
          }
        };
        var rnd = function(a, b2) {
          return Math.round((b2 - a) * Math.random() + a);
        }, RE = function(re2, fl2) {
          return new RegExp(re2, fl2 || "");
        }, slice = function(a) {
          return AP.slice.apply(a, AP.slice.call(arguments, 1));
        }, flatten = function(a) {
          let r = [], i = 0;
          while (i < a.length) r = r.concat(a[i++]);
          return r;
        }, getArgs = function(args, asArray) {
          return flatten(slice(args));
        }, esc_re = function(s, esc, chargroup2) {
          let es2 = "", l = s.length, i = 0, c;
          if (chargroup2) {
            while (i < l) {
              c = s[CHAR](i++);
              es2 += /*('?' === c) || ('*' === c) || ('+' === c) ||*/
              ("-" === c || /*('.' === c) ||*/
              "^" === c || "$" === c || "|" === c || "{" === c || "}" === c || "(" === c || ")" === c || "[" === c || "]" === c || "/" === c || esc === c ? esc : "") + c;
            }
          } else {
            while (i < l) {
              c = s[CHAR](i++);
              es2 += ("?" === c || "*" === c || "+" === c || /*('-' === c) ||*/
              "." === c || "^" === c || "$" === c || "|" === c || "{" === c || "}" === c || "(" === c || ")" === c || "[" === c || "]" === c || "/" === c || esc === c ? esc : "") + c;
            }
          }
          return es2;
        }, pad = function(s, n, z) {
          let ps2 = String(s);
          z = z || "0";
          while (ps2.length < n) ps2 = z + ps2;
          return ps2;
        }, char_code = function(c) {
          return c[CODEPOINT](0);
        }, char_code_range = function(s) {
          return [s[CODEPOINT](0), s[CODEPOINT](s.length - 1)];
        }, character_range = function(first, last) {
          if (first && is_array(first)) {
            last = first[1];
            first = first[0];
          }
          let ch2, chars, start = first[CODEPOINT](0), end = last[CODEPOINT](0);
          if (end === start) return [fromCodePoint(start)];
          chars = [];
          for (ch2 = start; ch2 <= end; ++ch2) chars.push(fromCodePoint(ch2));
          return chars;
        }, concat = function(p1, p2) {
          if (p2) {
            let p, l;
            if (is_array(p2)) {
              for (p = 0, l = p2.length; p < l; ++p) p1[p2[p]] = 1;
            } else {
              for (p in p2) if (HAS.call(p2, p)) p1[p] = 1;
            }
          }
          return p1;
        }, BSPACES = "\r\n", SPACES = " 	\v", PUNCTS = "~!@#$%^&*()-+=[]{}\\|;:,./<>?", DIGITS = "0123456789", DIGITS_RANGE = char_code_range(DIGITS), HEXDIGITS_RANGES = [DIGITS_RANGE, [char_code("a"), char_code("f")], [char_code("A"), char_code("F")]], ALPHAS = `_${character_range("a", "z").join("")}${character_range("A", "Z").join("")}`, ALL = SPACES + PUNCTS + DIGITS + ALPHAS, ALL_ARY = ALL.split(""), match_chars = function(CHARS, s, pos, minlen, maxlen) {
          pos = pos || 0;
          minlen = minlen || 1;
          maxlen = maxlen || INF;
          let lp2 = pos, l = 0, sl2 = s.length, ch2;
          while (lp2 < sl2 && l <= maxlen && -1 < CHARS.indexOf(ch2 = s[CHAR](lp2))) {
            ++lp2;
            ++l;
          }
          return l >= minlen ? l : false;
        }, match_char_range = function(RANGE, s, pos, minlen, maxlen) {
          pos = pos || 0;
          minlen = minlen || 1;
          maxlen = maxlen || INF;
          let lp2 = pos, l = 0, sl2 = s.length, ch2;
          while (lp2 < sl2 && l <= maxlen && ((ch2 = s[CHARCODE](lp2)) >= RANGE[0] && ch2 <= RANGE[1])) {
            ++lp2;
            ++l;
          }
          return l >= minlen ? l : false;
        }, match_char_ranges = function(RANGES, s, pos, minlen, maxlen) {
          pos = pos || 0;
          minlen = minlen || 1;
          maxlen = maxlen || INF;
          let lp2 = pos, l = 0, sl2 = s.length, ch2, i, Rl2 = RANGES.length, RANGE, found = true;
          while (lp2 < sl2 && l <= maxlen && found) {
            ch2 = s[CHARCODE](lp2);
            found = false;
            for (i = 0; i < Rl2; ++i) {
              RANGE = RANGES[i];
              if (ch2 >= RANGE[0] && ch2 <= RANGE[1]) {
                ++lp2;
                ++l;
                found = true;
                break;
              }
            }
          }
          return l >= minlen ? l : false;
        }, punct = function() {
          return PUNCTS[CHAR](rnd(0, PUNCTS.length - 1));
        }, space = function(positive) {
          return false !== positive ? SPACES[CHAR](rnd(0, SPACES.length - 1)) : (punct() + digit() + alpha())[CHAR](rnd(0, 2));
        }, digit = function(positive) {
          return false !== positive ? DIGITS[CHAR](rnd(0, DIGITS.length - 1)) : (punct() + space() + alpha())[CHAR](rnd(0, 2));
        }, alpha = function(positive) {
          return false !== positive ? ALPHAS[CHAR](rnd(0, ALPHAS.length - 1)) : (punct() + space() + digit())[CHAR](rnd(0, 2));
        }, word = function(positive) {
          return false !== positive ? (ALPHAS + DIGITS)[CHAR](rnd(0, ALPHAS.length + DIGITS.length - 1)) : (punct() + space())[CHAR](rnd(0, 1));
        }, any = function() {
          return ALL[CHAR](rnd(0, ALL.length - 1));
        }, character = function(chars, positive) {
          if (false !== positive) return chars.length ? chars[rnd(0, chars.length - 1)] : "";
          const choices = ALL_ARY.filter((c) => {
            return 0 > chars.indexOf(c);
          });
          return choices.length ? choices[rnd(0, choices.length - 1)] : "";
        }, random_upper_or_lower = function(c) {
          return 0.5 < Math.random() ? c.toLowerCase() : c.toUpperCase();
        }, case_insensitive = function(chars, asArray) {
          if (asArray) {
            if (chars[CHAR]) chars = chars.split("");
            chars = chars.map(random_upper_or_lower);
            return chars;
          } else {
            return random_upper_or_lower(chars);
          }
        }, walk = function walk2(ret, node, state) {
          if (null == node || !state) return ret;
          let i, l, r, type = node instanceof Node2 ? node.type : null;
          if (null === type) {
            ret = state.reduce(ret, node, state);
          } else if (state.IGNORE & type) {
          } else if (state.MAP & type) {
            r = state.map(ret, node, state);
            if (null != state.ret) {
              ret = state.reduce(ret, node, state);
              state.ret = null;
            } else if (null != r) {
              r = array(r);
              for (i = 0, l = r ? r.length : 0; i < l; ++i) {
                state.node = node;
                ret = walk2(ret, r[i], state);
                if (state.stop) {
                  state.stop = null;
                  return ret;
                }
              }
            }
          } else if (state.REDUCE & type) {
            ret = state.reduce(ret, node, state);
          }
          state.node = null;
          return ret;
        }, map_src = function map_src2(ret, node, state) {
          const type = node.type;
          if (T_ALTERNATION === type) {
            const r = [];
            for (var i = 0, l = node.val.length - 1; i < l; ++i) r.push(node.val[i], "|");
            r.push(node.val[l]);
            return r;
          } else if (T_CHARGROUP === type) {
            return [].concat(`[${node.flags.NegativeMatch ? "^" : ""}`).concat(array(node.val)).concat("]");
          } else if (T_QUANTIFIER === type) {
            let q2 = "";
            if (node.flags.MatchZeroOrOne) q2 = "?";
            else if (node.flags.MatchZeroOrMore) q2 = "*";
            else if (node.flags.MatchOneOrMore) q2 = "+";
            else q2 = node.flags.min === node.flags.max ? `{${node.flags.min}}` : `{${node.flags.min},${-1 === node.flags.max ? "" : node.flags.max}}`;
            if (node.flags.min !== node.flags.max && !node.flags.isGreedy) q2 += "?";
            return [].concat(array(node.val)).concat(q2);
          } else if (T_GROUP === type) {
            let g2 = null;
            if (node.flags.NotCaptured) {
              g2 = [].concat("(?:").concat(array(node.val)).concat(")");
            } else if (node.flags.LookAhead) {
              g2 = [].concat("(?=").concat(array(node.val)).concat(")");
            } else if (node.flags.NegativeLookAhead) {
              g2 = [].concat("(?!").concat(array(node.val)).concat(")");
            } else if (node.flags.LookBehind) {
              g2 = [].concat("(?<=").concat(array(node.val)).concat(")");
            } else if (node.flags.NegativeLookBehind) {
              g2 = [].concat("(?<!").concat(array(node.val)).concat(")");
            } else if (node.flags.NamedGroup && !state.compatibility) {
              g2 = [].concat(`(?<${node.flags.GroupName}>`).concat(array(node.val)).concat(")");
            } else {
              g2 = [].concat("(").concat(array(node.val)).concat(")");
            }
            if (null != node.flags.GroupIndex) {
              ret.group[node.flags.GroupIndex] = node.flags.GroupIndex;
              if (node.flags.GroupName) ret.group[node.flags.GroupName] = node.flags.GroupIndex;
            }
            return g2;
          }
          return node.val;
        }, map_any = function map_any2(ret, node, state) {
          const type = node.type;
          if (T_ALTERNATION === type || T_CHARGROUP === type) {
            return node.val.length ? node.val[rnd(0, node.val.length - 1)] : null;
          } else if (T_QUANTIFIER === type) {
            let numrepeats, mmin, mmax, repeats;
            if (ret.length >= state.maxLength) {
              numrepeats = node.flags.min;
            } else {
              mmin = node.flags.min;
              mmax = -1 === node.flags.max ? mmin + 1 + 2 * state.maxLength : node.flags.max;
              numrepeats = rnd(mmin, mmax);
            }
            if (numrepeats) {
              repeats = new Array(numrepeats);
              for (let i = 0; i < numrepeats; ++i) repeats[i] = node.val;
              return repeats;
            } else {
              return null;
            }
          } else if (T_GROUP === type && node.flags.GroupIndex) {
            const sample = walk("", node.val, state);
            state.group[node.flags.GroupIndex] = sample;
            state.ret = sample;
            return null;
          } else {
            return node.val;
          }
        }, map_min = function map_min2(ret, node, state) {
          const type = node.type;
          if (T_ALTERNATION === type) {
            var i, l = node.val.length, cur, min = l ? walk(0, node.val[0], state) : 0;
            for (i = 1; i < l; ++i) {
              cur = walk(0, node.val[i], state);
              if (cur < min) min = cur;
            }
            if (l) state.ret = min;
            return null;
          } else if (T_CHARGROUP === type) {
            return node.val.length ? node.val[0] : null;
          } else if (T_QUANTIFIER === type) {
            if (0 === node.flags.min) return null;
            var i, nrepeats = node.flags.min, repeats = new Array(nrepeats);
            for (i = 0; i < nrepeats; ++i) repeats[i] = node.val;
            return repeats;
          } else if (T_GROUP === type && node.flags.GroupIndex) {
            var min = walk(0, node.val, state);
            state.group[node.flags.GroupIndex] = min;
            state.ret = min;
            return null;
          } else {
            return node.val;
          }
        }, map_max = function map_max2(ret, node, state) {
          const type = node.type;
          if (T_ALTERNATION === type) {
            var i, l = node.val.length, cur, max = l ? walk(0, node.val[0], state) : 0;
            if (-1 !== max) {
              for (i = 1; i < l; ++i) {
                cur = walk(0, node.val[i], state);
                if (-1 === cur) {
                  max = -1;
                  break;
                } else if (cur > max) {
                  max = cur;
                }
              }
            }
            if (l) state.ret = max;
            return null;
          } else if (T_CHARGROUP === type) {
            return node.val.length ? node.val[0] : null;
          } else if (T_QUANTIFIER === type) {
            max = walk(0, node.val, state);
            if (-1 === max) {
              state.ret = -1;
            } else if (0 < max) {
              if (-1 === node.flags.max) {
                state.ret = -1;
              } else if (0 < node.flags.max) {
                state.ret = node.flags.max * max;
              } else {
                state.ret = max;
              }
            }
            return null;
          } else if (T_GROUP === type && node.flags.GroupIndex) {
            var max = walk(0, node.val, state);
            state.group[node.flags.GroupIndex] = max;
            state.ret = max;
            return null;
          } else {
            return node.val;
          }
        }, map_1st = function map_1st2(ret, node, state) {
          const type = node.type;
          if (T_SEQUENCE === type) {
            let seq = [], i = 0, l = node.val.length, n;
            for (i = 0; i < l; ++i) {
              n = node.val[i];
              seq.push(n);
              if (T_QUANTIFIER === n.type && 0 === n.flags.min)
                continue;
              else if (T_SPECIAL === n.type && (n.flags.MatchStart || n.flags.MatchEnd))
                continue;
              break;
            }
            return seq.length ? seq : null;
          } else {
            return node.val;
          }
        }, reduce_len = function reduce_len2(ret, node, state) {
          if (null != state.ret) {
            if (-1 === state.ret) ret = -1;
            else ret += state.ret;
            return ret;
          }
          if (-1 === ret) return ret;
          if (node === +node) {
            ret += node;
            return ret;
          }
          if (T_SPECIAL === node.type && node.flags.MatchEnd) {
            state.stop = 1;
            return ret;
          }
          const type = node.type;
          if (T_CHARS === type || T_CHARRANGE === type || T_UNICODECHAR === type || T_HEXCHAR === type || T_SPECIAL === type && !node.flags.MatchStart && !node.flags.MatchEnd) {
            ret += node.flags.BackReference ? state.group[node.flags.GroupIndex] || 0 : 1;
          } else if (T_STRING === type) {
            ret += node.val.length;
          }
          return ret;
        }, reduce_str = function reduce_str2(ret, node, state) {
          if (null != state.ret) {
            ret += state.ret;
            return ret;
          }
          if (is_string(node)) {
            ret += node;
            return ret;
          }
          if (T_SPECIAL === node.type && node.flags.MatchEnd) {
            state.stop = 1;
            return ret;
          }
          let type = node.type, sample = null;
          if (T_CHARS === type) {
            sample = node.val;
          } else if (T_CHARRANGE === type) {
            const range = [node.val[0], node.val[1]];
            if (T_UNICODECHAR === range[0].type || T_HEXCHAR === range[0].type) range[0] = range[0].flags.Char;
            if (T_UNICODECHAR === range[1].type || T_HEXCHAR === range[1].type) range[1] = range[1].flags.Char;
            sample = character_range(range);
          } else if (T_UNICODECHAR === type || T_HEXCHAR === type) {
            sample = [node.flags.Char];
          } else if (T_SPECIAL === type && !node.flags.MatchStart && !node.flags.MatchEnd) {
            let part = node.val;
            if (node.flags.BackReference) {
              part = node.flags.GroupIndex;
              ret += HAS.call(state.group, part) ? state.group[part] : "";
              return ret;
            } else if ("D" === part) {
              sample = [digit(false)];
            } else if ("W" === part) {
              sample = [word(false)];
            } else if ("S" === part) {
              sample = [space(false)];
            } else if ("d" === part) {
              sample = [digit()];
            } else if ("w" === part) {
              sample = [word()];
            } else if ("s" === part) {
              sample = [space()];
            } else if ("." === part && node.flags.MatchAnyChar) {
              sample = [any()];
            } else {
              sample = [ESC + part];
            }
          } else if (T_STRING === type) {
            sample = node.val;
          }
          if (sample) {
            ret += T_STRING === type ? state.isCaseInsensitive ? case_insensitive(sample) : sample : character(state.isCaseInsensitive ? case_insensitive(sample, true) : sample, !state.node || !state.node.flags.NegativeMatch);
          }
          return ret;
        }, reduce_src = function reduce_src2(ret, node, state) {
          if (null != state.ret) {
            if (state.ret.src) ret.src += state.ret.src;
            if (state.ret.group) ret.group = clone(state.ret.group, ret.group);
            return ret;
          }
          if (is_string(node)) {
            ret.src += node;
            return ret;
          }
          const type = node.type;
          if (T_CHARS === type) {
            ret.src += state.escaped ? esc_re(node.val.join(""), ESC, 1) : node.val.join("");
          } else if (T_CHARRANGE === type) {
            const range = [node.val[0], node.val[1]];
            if (state.escaped) {
              if (T_UNICODECHAR === range[0].type) range[0] = range[0].flags.UnicodePoint ? `${ESC}u{${range[0].flags.Code}}` : `${ESC}u${pad(range[0].flags.Code, 4)}`;
              else if (T_HEXCHAR === range[0].type) range[0] = `${ESC}x${pad(range[0].flags.Code, 2)}`;
              else range[0] = esc_re(range[0], ESC, 1);
              if (T_UNICODECHAR === range[1].type) range[1] = range[1].flags.UnicodePoint ? `${ESC}u{${range[1].flags.Code}}` : `${ESC}u${pad(range[1].flags.Code, 4)}`;
              else if (T_HEXCHAR === range[1].type) range[1] = `${ESC}x${pad(range[1].flags.Code, 2)}`;
              else range[1] = esc_re(range[1], ESC, 1);
            } else {
              if (T_UNICODECHAR === range[0].type || T_HEXCHAR === range[0].type) range[0] = range[0].flags.Char;
              if (T_UNICODECHAR === range[1].type || T_HEXCHAR === range[1].type) range[1] = range[1].flags.Char;
            }
            ret.src += `${range[0]}-${range[1]}`;
          } else if (T_UNICODECHAR === type) {
            ret.src += node.flags.UnicodePoint ? `${ESC}u{${node.flags.Code}}` : state.escaped ? `${ESC}u${pad(node.flags.Code, 4)}` : node.flags.Char;
          } else if (T_HEXCHAR === type) {
            ret.src += state.escaped ? `${ESC}x${pad(node.flags.Code, 2)}` : node.flags.Char;
          } else if (T_SPECIAL === type) {
            if (node.flags.BackReference) {
              if (state.compatibility || node.flags.GroupIndex === node.flags.GroupName) {
                ret.src += ESC + node.flags.GroupIndex;
              } else {
                ret.src += `${ESC}k<${node.flags.GroupName}>`;
              }
            } else {
              ret.src += node.flags.MatchAnyChar || node.flags.MatchStart || node.flags.MatchEnd ? `${node.val}` : ESC + node.val;
            }
          } else if (T_STRING === type) {
            ret.src += state.escaped ? esc_re(node.val, ESC) : node.val;
          }
          return ret;
        }, reduce_peek = function reduce_peek2(ret, node, state) {
          if (null != state.ret) {
            ret.positive = concat(ret.positive, state.ret.positive);
            ret.negative = concat(ret.negative, state.ret.negative);
            return ret;
          }
          if (T_SPECIAL === node.type && node.flags.MatchEnd) {
            state.stop = 1;
            return ret;
          }
          const type = node.type, inCharGroup = state.node && T_CHARGROUP === state.node.type, inNegativeCharGroup = inCharGroup && state.node.flags.NegativeMatch, peek = inNegativeCharGroup ? "negative" : "positive";
          if (T_CHARS === type) {
            ret[peek] = concat(ret[peek], node.val);
          } else if (T_CHARRANGE === type) {
            const range = [node.val[0], node.val[1]];
            if (T_UNICODECHAR === range[0].type || T_HEXCHAR === range[0].type) range[0] = range[0].flags.Char;
            if (T_UNICODECHAR === range[1].type || T_HEXCHAR === range[1].type) range[1] = range[1].flags.Char;
            ret[peek] = concat(ret[peek], character_range(range));
          } else if (T_UNICODECHAR === type || T_HEXCHAR === type) {
            ret[peek][node.flags.Char] = 1;
          } else if (T_SPECIAL === type && !node.flags.BackReference && !node.flags.MatchStart && !node.flags.MatchEnd) {
            const part = node.val;
            if ("D" === part) {
              ret[inNegativeCharGroup ? "positive" : "negative"]["\\d"] = 1;
            } else if ("W" === part) {
              ret[inNegativeCharGroup ? "positive" : "negative"]["\\w"] = 1;
            } else if ("S" === part) {
              ret[inNegativeCharGroup ? "positive" : "negative"]["\\s"] = 1;
            } else if ("B" === part) {
              ret[inNegativeCharGroup ? "positive" : "negative"]["\\b"] = 1;
            } else {
              ret[peek][ESC + part] = 1;
            }
          } else if (T_STRING === type) {
            ret["positive"][node.val[CHAR](0)] = 1;
          }
          return ret;
        }, match_hex = function(s) {
          let m = false;
          if (s.length > 2 && "x" === s[CHAR](0)) {
            if (match_char_ranges(HEXDIGITS_RANGES, s, 1, 2, 2)) return [m = s.slice(0, 3), m.slice(1)];
          }
          return false;
        }, match_unicode = function(s, flags) {
          let m = false, l;
          if (s.length > 3 && "u" === s[CHAR](0)) {
            if (flags.u && "{" === s[CHAR](1) && (l = match_char_ranges(HEXDIGITS_RANGES, s, 2, 1, 6)) && "}" === s[CHAR](l + 2)) {
              return [m = s.slice(0, l + 3), m.slice(2, -1), 1];
            } else if (l = match_char_ranges(HEXDIGITS_RANGES, s, 1, 4, 4)) {
              return [m = s.slice(0, l + 1), m.slice(1), 0];
            }
          }
          return false;
        }, match_repeats = function(s) {
          let l, sl2 = s.length, pos = 0, m = false, hasComma = false;
          if (sl2 > 2 && "{" === s[CHAR](pos)) {
            m = ["", "", null];
            ++pos;
            if (l = match_chars(SPACES, s, pos)) pos += l;
            if (l = match_char_range(DIGITS_RANGE, s, pos)) {
              m[1] = s.slice(pos, pos + l);
              pos += l;
            } else {
              return false;
            }
            if (l = match_chars(SPACES, s, pos)) pos += l;
            if (pos < sl2 && "," === s[CHAR](pos)) {
              pos += 1;
              hasComma = true;
            }
            if (l = match_chars(SPACES, s, pos)) pos += l;
            if (l = match_char_range(DIGITS_RANGE, s, pos)) {
              m[2] = s.slice(pos, pos + l);
              pos += l;
            }
            if (l = match_chars(SPACES, s, pos)) pos += l;
            if (pos < sl2 && "}" === s[CHAR](pos)) {
              pos++;
              m[0] = s.slice(0, pos);
              if (!hasComma) m[2] = m[1];
              return m;
            } else {
              return false;
            }
          }
          return false;
        }, chargroup = function chargroup2(re_obj) {
          let sequence = [], chars = [], allchars = [], flags = {}, flag, ch2, lre, prevch = null, range, isRange = false, m, isUnicode, isHex, isSpecial, escaped = false;
          if ("^" === re_obj.re[CHAR](re_obj.pos)) {
            flags["NegativeMatch"] = 1;
            ++re_obj.pos;
          }
          lre = re_obj.len;
          while (re_obj.pos < lre) {
            isUnicode = false;
            isHex = false;
            isSpecial = false;
            m = null;
            prevch = ch2;
            ch2 = re_obj.re[CHAR](re_obj.pos++);
            escaped = ESC === ch2;
            if (escaped) ch2 = re_obj.re[CHAR](re_obj.pos++);
            if (escaped) {
              if ("u" === ch2) {
                m = match_unicode(re_obj.re.substr(re_obj.pos - 1), re_obj.flags);
                if (m) {
                  re_obj.pos += m[0].length - 1;
                  ch2 = Node2(T_UNICODECHAR, m[0], { "Char": m[2] ? fromCodePoint(parseInt(m[1], 16)) : fromCharCode(parseInt(m[1], 16)), "Code": m[1], "UnicodePoint": !!m[2] });
                  isUnicode = true;
                  isHex = false;
                }
              } else if ("x" === ch2) {
                m = match_hex(re_obj.re.substr(re_obj.pos - 1));
                if (m) {
                  re_obj.pos += m[0].length - 1;
                  ch2 = Node2(T_HEXCHAR, m[0], { "Char": fromCharCode(parseInt(m[1], 16)), "Code": m[1] });
                  isUnicode = true;
                  isHex = true;
                }
              } else if (HAS.call(specialCharsEscaped, ch2) && "/" !== ch2) {
                isSpecial = true;
                flag = {};
                flag[specialCharsEscaped[ch2]] = 1;
                ch2 = Node2(T_SPECIAL, ch2, flag);
              }
            }
            if (isRange) {
              if (ch2 instanceof Node2 && ch2.type === T_SPECIAL && -1 !== ["s", "S", "d", "D", "w", "W"].indexOf(ch2.val)) {
                if (range[0] instanceof Node2) {
                  sequence.push(range[0]);
                } else {
                  chars.push(range[0]);
                }
                chars.push("-");
                sequence.push(ch2);
              } else {
                if (chars.length) {
                  allchars = allchars.concat(chars);
                  chars = [];
                }
                range[1] = ch2;
                sequence.push(Node2(T_CHARRANGE, range));
              }
              isRange = false;
            } else {
              if (escaped) {
                if (isUnicode) {
                  if (chars.length) {
                    allchars = allchars.concat(chars);
                    chars = [];
                  }
                  sequence.push(ch2);
                } else if (isSpecial) {
                  if (chars.length) {
                    allchars = allchars.concat(chars);
                    chars = [];
                  }
                  sequence.push(ch2);
                } else {
                  chars.push(ch2);
                }
              } else {
                if ("]" === ch2) {
                  if (chars.length) {
                    allchars = allchars.concat(chars);
                    chars = [];
                  }
                  if (allchars.length) sequence.push(Node2(T_CHARS, allchars));
                  return Node2(T_CHARGROUP, sequence, flags);
                } else if ("-" === ch2) {
                  if (null == prevch || "]" === re_obj.re[CHAR](re_obj.pos) || prevch instanceof Node2 && prevch.type === T_SPECIAL && -1 !== ["s", "S", "d", "D", "w", "W"].indexOf(prevch.val)) {
                    chars.push(ch2);
                  } else {
                    range = [prevch, ""];
                    if (prevch instanceof Node2) sequence.pop();
                    else chars.pop();
                    isRange = true;
                  }
                } else {
                  chars.push(ch2);
                }
              }
            }
          }
          if (chars.length) {
            allchars = allchars.concat(chars);
            chars = [];
          }
          if (allchars.length) sequence.push(Node2(T_CHARS, allchars));
          return Node2(T_CHARGROUP, sequence, flags);
        }, analyze_re = function analyze_re2(re_obj) {
          let lre, ch2, m, word2 = "", wordlen = 0, alternation = [], sequence = [], flags = {}, flag, escaped = false, pre, pre3, captured;
          if (re_obj.inGroup > 0) {
            pre = re_obj.re.substr(re_obj.pos, 2);
            pre3 = re_obj.re.substr(re_obj.pos, 3);
            captured = 1;
            if ("?P=" === pre3) {
              flags["BackReference"] = 1;
              flags["GroupName"] = "";
              re_obj.pos += 3;
              lre = re_obj.len;
              while (re_obj.pos < lre) {
                ch2 = re_obj.re[CHAR](re_obj.pos++);
                if (")" === ch2) break;
                flags["GroupName"] += ch2;
              }
              flags["GroupIndex"] = HAS.call(re_obj.group, flags["GroupName"]) ? re_obj.group[flags["GroupName"]] : null;
              return Node2(T_SPECIAL, flags["GroupName"], flags);
            } else if ("?#" === pre) {
              flags["Comment"] = 1;
              re_obj.pos += 2;
              word2 = "";
              lre = re_obj.len;
              while (re_obj.pos < lre) {
                ch2 = re_obj.re[CHAR](re_obj.pos++);
                if (")" === ch2) break;
                word2 += ch2;
              }
              return Node2(T_COMMENT, word2);
            } else if ("?:" === pre) {
              flags["NotCaptured"] = 1;
              re_obj.pos += 2;
              captured = 0;
            } else if ("?=" === pre) {
              flags["LookAhead"] = 1;
              re_obj.pos += 2;
              captured = 0;
            } else if ("?!" === pre) {
              flags["NegativeLookAhead"] = 1;
              re_obj.pos += 2;
              captured = 0;
            } else if ("?<=" === pre3) {
              flags["LookBehind"] = 1;
              re_obj.pos += 3;
              captured = 0;
            } else if ("?<!" === pre3) {
              flags["NegativeLookBehind"] = 1;
              re_obj.pos += 3;
              captured = 0;
            } else if ("?<" === pre || "?P<" === pre3) {
              flags["NamedGroup"] = 1;
              flags["GroupName"] = "";
              re_obj.pos += "?<" === pre ? 2 : 3;
              lre = re_obj.len;
              while (re_obj.pos < lre) {
                ch2 = re_obj.re[CHAR](re_obj.pos++);
                if (">" === ch2) break;
                flags["GroupName"] += ch2;
              }
            }
            ++re_obj.index;
            if (captured) {
              ++re_obj.groupIndex;
              flags["GroupIndex"] = re_obj.groupIndex;
              re_obj.group[flags["GroupIndex"]] = flags["GroupIndex"];
              if (flags["GroupName"]) re_obj.group[flags["GroupName"]] = flags["GroupIndex"];
            }
          }
          lre = re_obj.len;
          while (re_obj.pos < lre) {
            ch2 = re_obj.re[CHAR](re_obj.pos++);
            escaped = ESC === ch2;
            if (escaped) ch2 = re_obj.re[CHAR](re_obj.pos++);
            if (escaped) {
              if ("u" === ch2) {
                m = match_unicode(re_obj.re.substr(re_obj.pos - 1), re_obj.flags);
                if (m) {
                  if (wordlen) {
                    sequence.push(Node2(T_STRING, word2));
                    word2 = "";
                    wordlen = 0;
                  }
                  re_obj.pos += m[0].length - 1;
                  sequence.push(Node2(T_UNICODECHAR, m[0], { "Char": m[2] ? fromCodePoint(parseInt(m[1], 16)) : fromCharCode(parseInt(m[1], 16)), "Code": m[1], "UnicodePoint": !!m[2] }));
                } else {
                  word2 += ch2;
                  wordlen += 1;
                }
              } else if ("x" === ch2) {
                m = match_hex(re_obj.re.substr(re_obj.pos - 1));
                if (m) {
                  if (wordlen) {
                    sequence.push(Node2(T_STRING, word2));
                    word2 = "";
                    wordlen = 0;
                  }
                  re_obj.pos += m[0].length - 1;
                  sequence.push(Node2(T_HEXCHAR, m[0], { "Char": fromCharCode(parseInt(m[1], 16)), "Code": m[1] }));
                } else {
                  word2 += ch2;
                  wordlen += 1;
                }
              } else if ("k" === ch2 && "<" === re_obj.re[CHAR](re_obj.pos)) {
                if (wordlen) {
                  sequence.push(Node2(T_STRING, word2));
                  word2 = "";
                  wordlen = 0;
                }
                re_obj.pos++;
                word2 = "";
                while (re_obj.pos < lre) {
                  ch2 = re_obj.re[CHAR](re_obj.pos);
                  if (">" === ch2) {
                    re_obj.pos++;
                    break;
                  } else {
                    word2 += ch2;
                    re_obj.pos++;
                  }
                }
                flag = {};
                flag["BackReference"] = 1;
                flag["GroupName"] = word2;
                flag["GroupIndex"] = HAS.call(re_obj.group, word2) ? re_obj.group[word2] : null;
                sequence.push(Node2(T_SPECIAL, word2, flag));
                word2 = "";
              } else if (HAS.call(specialCharsEscaped, ch2) && "/" !== ch2) {
                if (wordlen) {
                  sequence.push(Node2(T_STRING, word2));
                  word2 = "";
                  wordlen = 0;
                }
                flag = {};
                flag[specialCharsEscaped[ch2]] = 1;
                sequence.push(Node2(T_SPECIAL, ch2, flag));
              } else if ("1" <= ch2 && "9" >= ch2) {
                if (wordlen) {
                  sequence.push(Node2(T_STRING, word2));
                  word2 = "";
                  wordlen = 0;
                }
                word2 = ch2;
                while (re_obj.pos < lre) {
                  ch2 = re_obj.re[CHAR](re_obj.pos);
                  if ("0" <= ch2 && "9" >= ch2) {
                    word2 += ch2;
                    re_obj.pos++;
                  } else break;
                }
                flag = {};
                flag["BackReference"] = 1;
                flag["GroupName"] = word2;
                flag["GroupIndex"] = parseInt(word2, 10);
                sequence.push(Node2(T_SPECIAL, word2, flag));
                word2 = "";
              } else {
                word2 += ch2;
                wordlen += 1;
              }
            } else {
              if (re_obj.inGroup > 0 && ")" === ch2) {
                if (wordlen) {
                  sequence.push(Node2(T_STRING, word2));
                  word2 = "";
                  wordlen = 0;
                }
                if (alternation.length) {
                  alternation.push(Node2(T_SEQUENCE, sequence));
                  sequence = [];
                  flag = {};
                  flag[specialChars["|"]] = 1;
                  return Node2(T_GROUP, Node2(T_ALTERNATION, alternation, flag), flags);
                } else {
                  return Node2(T_GROUP, Node2(T_SEQUENCE, sequence), flags);
                }
              } else if ("|" === ch2) {
                if (wordlen) {
                  sequence.push(Node2(T_STRING, word2));
                  word2 = "";
                  wordlen = 0;
                }
                alternation.push(Node2(T_SEQUENCE, sequence));
                sequence = [];
              } else if ("[" === ch2) {
                if (wordlen) {
                  sequence.push(Node2(T_STRING, word2));
                  word2 = "";
                  wordlen = 0;
                }
                sequence.push(chargroup(re_obj));
              } else if ("(" === ch2) {
                if (wordlen) {
                  sequence.push(Node2(T_STRING, word2));
                  word2 = "";
                  wordlen = 0;
                }
                re_obj.inGroup += 1;
                sequence.push(analyze_re2(re_obj));
                re_obj.inGroup -= 1;
              } else if ("{" === ch2) {
                if (wordlen) {
                  sequence.push(Node2(T_STRING, word2));
                  word2 = "";
                  wordlen = 0;
                }
                m = match_repeats(re_obj.re.substr(re_obj.pos - 1));
                re_obj.pos += m[0].length - 1;
                flag = { val: m[0], "MatchMinimum": m[1], "MatchMaximum": m[2] || "unlimited", "min": parseInt(m[1], 10), "max": m[2] ? parseInt(m[2], 10) : -1 };
                flag[specialChars[ch2]] = 1;
                if (re_obj.pos < lre && "?" === re_obj.re[CHAR](re_obj.pos)) {
                  flag["isGreedy"] = 0;
                  re_obj.pos++;
                } else {
                  flag["isGreedy"] = 1;
                }
                var prev = sequence.pop();
                if (T_STRING === prev.type && prev.val.length > 1) {
                  sequence.push(Node2(T_STRING, prev.val.slice(0, -1)));
                  prev.val = prev.val.slice(-1);
                }
                sequence.push(Node2(T_QUANTIFIER, prev, flag));
              } else if ("*" === ch2 || "+" === ch2 || "?" === ch2) {
                if (wordlen) {
                  sequence.push(Node2(T_STRING, word2));
                  word2 = "";
                  wordlen = 0;
                }
                flag = {};
                flag[specialChars[ch2]] = 1;
                flag["min"] = "+" === ch2 ? 1 : 0;
                flag["max"] = "?" === ch2 ? 1 : -1;
                if (re_obj.pos < lre && "?" === re_obj.re[CHAR](re_obj.pos)) {
                  flag["isGreedy"] = 0;
                  re_obj.pos++;
                } else {
                  flag["isGreedy"] = 1;
                }
                var prev = sequence.pop();
                if (T_STRING === prev.type && prev.val.length > 1) {
                  sequence.push(Node2(T_STRING, prev.val.slice(0, -1)));
                  prev.val = prev.val.slice(-1);
                }
                sequence.push(Node2(T_QUANTIFIER, prev, flag));
              } else if (HAS.call(specialChars, ch2)) {
                if (wordlen) {
                  sequence.push(Node2(T_STRING, word2));
                  word2 = "";
                  wordlen = 0;
                }
                flag = {};
                flag[specialChars[ch2]] = 1;
                sequence.push(Node2(T_SPECIAL, ch2, flag));
              } else {
                word2 += ch2;
                wordlen += 1;
              }
            }
          }
          if (wordlen) {
            sequence.push(Node2(T_STRING, word2));
            word2 = "";
            wordlen = 0;
          }
          if (alternation.length) {
            alternation.push(Node2(T_SEQUENCE, sequence));
            sequence = [];
            flag = {};
            flags[specialChars["|"]] = 1;
            return Node2(T_ALTERNATION, alternation, flag);
          }
          return Node2(T_SEQUENCE, sequence);
        };
        function Analyzer(re2, delim, flavor) {
          if (!(this instanceof Analyzer)) return new Analyzer(re2, delim, flavor);
          if (re2) this.input(re2, delim, flavor);
        }
        Analyzer.VERSION = __version__;
        Analyzer[PROTO] = {
          constructor: Analyzer,
          ast: null,
          flavor: "",
          re: null,
          fl: null,
          src: null,
          grp: null,
          min: null,
          max: null,
          ch: null,
          bc: true,
          dispose: function() {
            const self2 = this;
            self2.ast = null;
            self2.flavor = null;
            self2.re = null;
            self2.fl = null;
            self2.src = null;
            self2.grp = null;
            self2.min = null;
            self2.max = null;
            self2.ch = null;
            return self2;
          },
          reset: function() {
            const self2 = this;
            self2.ast = null;
            self2.src = null;
            self2.grp = null;
            self2.min = null;
            self2.max = null;
            self2.ch = null;
            return self2;
          },
          backwardsCompatible: function(enable) {
            this.bc = !!enable;
            return this;
          },
          input: function(re2, delim, flavor) {
            const self2 = this;
            if (!arguments.length) return self2.re;
            if (re2) {
              delim = false === delim ? false : delim || "/";
              let l, ch2, fl2 = {};
              re2 = re2.toString();
              l = re2.length;
              if (delim) {
                while (0 < l) {
                  ch2 = re2[CHAR](l - 1);
                  if (delim === ch2) break;
                  else {
                    fl2[ch2] = 1;
                    --l;
                  }
                }
                if (0 < l) {
                  if (delim === re2[CHAR](0) && delim === re2[CHAR](l - 1)) re2 = re2.slice(1, l - 1);
                  else re2 = re2.slice(0, l);
                } else {
                  re2 = "";
                }
              }
              if (self2.re !== re2) self2.reset();
              self2.re = re2;
              self2.fl = fl2;
              self2.flavor = String(flavor || "");
            }
            return self2;
          },
          analyze: function() {
            const self2 = this;
            if (null != self2.re && null === self2.ast) {
              const re2 = new RE_OBJ(self2.re, self2.fl, self2.flavor);
              self2.ast = analyze_re(re2);
              re2.dispose();
            }
            return self2;
          },
          synthesize: function(escaped) {
            let self2 = this, state, re2;
            if (null == self2.re) return self2;
            if (null === self2.ast) {
              self2.analyze();
              self2.src = null;
              self2.grp = null;
            }
            if (null === self2.src) {
              state = {
                MAP: T_SEQUENCE | T_ALTERNATION | T_GROUP | T_CHARGROUP | T_QUANTIFIER,
                REDUCE: T_UNICODECHAR | T_HEXCHAR | T_SPECIAL | T_CHARS | T_CHARRANGE | T_STRING,
                IGNORE: T_COMMENT,
                map: map_src,
                reduce: reduce_src,
                escaped: false !== escaped,
                compatibility: self2.bc,
                group: {}
              };
              re2 = walk({ src: "", group: {} }, self2.ast, state);
              self2.src = re2.src;
              self2.grp = re2.group;
            }
            return self2;
          },
          source: function() {
            const self2 = this;
            if (null == self2.re) return null;
            if (null === self2.src) self2.synthesize();
            return self2.src;
          },
          groups: function(raw) {
            const self2 = this;
            if (null == self2.re) return null;
            if (null === self2.grp) self2.synthesize();
            return true === raw ? sel.grp : clone(self2.grp);
          },
          compile: function(flags, notBackwardsCompatible) {
            const self2 = this;
            if (null == self2.re) return null;
            flags = flags || self2.fl || {};
            return new RegExp(self2.source(), (flags.g || flags.G ? "g" : "") + (flags.i || flags.I ? "i" : "") + (flags.m || flags.M ? "m" : "") + (flags.y || flags.Y ? "y" : "") + (flags.u ? "u" : "") + (flags.d ? "d" : "") + (flags.s ? "s" : ""));
          },
          tree: function(flat) {
            const self2 = this;
            if (null == self2.re) return null;
            if (null === self2.ast) self2.analyze();
            return true === flat ? self2.ast.toObject() : self2.ast;
          },
          // experimental feature
          sample: function(maxlen, numsamples) {
            let self2 = this, state;
            if (null == self2.re) return null;
            if (null === self2.ast) self2.analyze();
            state = {
              MAP: T_SEQUENCE | T_ALTERNATION | T_GROUP | T_CHARGROUP | T_QUANTIFIER,
              REDUCE: T_UNICODECHAR | T_HEXCHAR | T_SPECIAL | T_CHARS | T_CHARRANGE | T_STRING,
              IGNORE: T_COMMENT,
              map: map_any,
              reduce: reduce_str,
              maxLength: maxlen | 0 || 1,
              isCaseInsensitive: null != self2.fl.i,
              group: {}
            };
            numsamples = numsamples | 0 || 1;
            if (1 < numsamples) {
              const samples = new Array(numsamples);
              for (let i = 0; i < numsamples; ++i) samples[i] = walk("", self2.ast, state);
              return samples;
            }
            return walk("", self2.ast, state);
          },
          // experimental feature
          minimum: function() {
            let self2 = this, state;
            if (null == self2.re) return 0;
            if (null === self2.ast) {
              self2.analyze();
              self2.min = null;
            }
            if (null === self2.min) {
              state = {
                MAP: T_SEQUENCE | T_ALTERNATION | T_GROUP | T_CHARGROUP | T_QUANTIFIER,
                REDUCE: T_UNICODECHAR | T_HEXCHAR | T_SPECIAL | T_CHARS | T_CHARRANGE | T_STRING,
                IGNORE: T_COMMENT,
                map: map_min,
                reduce: reduce_len,
                group: {}
              };
              self2.min = walk(0, self2.ast, state) | 0;
            }
            return self2.min;
          },
          // experimental feature
          maximum: function() {
            let self2 = this, state;
            if (null == self2.re) return 0;
            if (null === self2.ast) {
              self2.analyze();
              self2.max = null;
            }
            if (null === self2.max) {
              state = {
                MAP: T_SEQUENCE | T_ALTERNATION | T_GROUP | T_CHARGROUP | T_QUANTIFIER,
                REDUCE: T_UNICODECHAR | T_HEXCHAR | T_SPECIAL | T_CHARS | T_CHARRANGE | T_STRING,
                IGNORE: T_COMMENT,
                map: map_max,
                reduce: reduce_len,
                group: {}
              };
              self2.max = walk(0, self2.ast, state);
            }
            return self2.max;
          },
          // experimental feature
          peek: function() {
            let self2 = this, state, isCaseInsensitive, peek, n, c, p, cases;
            if (null == self2.re) return null;
            if (null === self2.ast) {
              self2.analyze();
              self2.ch = null;
            }
            if (null === self2.ch) {
              state = {
                MAP: T_SEQUENCE | T_ALTERNATION | T_GROUP | T_CHARGROUP | T_QUANTIFIER,
                REDUCE: T_UNICODECHAR | T_HEXCHAR | T_SPECIAL | T_CHARS | T_CHARRANGE | T_STRING,
                IGNORE: T_COMMENT,
                map: map_1st,
                reduce: reduce_peek,
                group: {}
              };
              self2.ch = walk({ positive: {}, negative: {} }, self2.ast, state);
            }
            peek = { positive: clone(self2.ch.positive), negative: clone(self2.ch.negative) };
            isCaseInsensitive = null != self2.fl.i;
            for (n in peek) {
              cases = {};
              p = peek[n];
              for (c in p) {
                if ("\\d" === c) {
                  delete p[c];
                  cases = concat(cases, character_range("0", "9"));
                } else if ("\\s" === c) {
                  delete p[c];
                  cases = concat(cases, ["\f", "\n", "\r", "	", "\v", "\xA0", "\u2028", "\u2029"]);
                } else if ("\\w" === c) {
                  delete p[c];
                  cases = concat(cases, ["_"].concat(character_range("0", "9")).concat(character_range("a", "z")).concat(character_range("A", "Z")));
                } else if ("\\b" === c) {
                  delete p[c];
                  cases[specialChars["b"]] = 1;
                } else if ("\\." === c) {
                  delete p[c];
                  cases[specialChars["."]] = 1;
                } else if (ESC !== c[CHAR](0) && isCaseInsensitive) {
                  cases[c.toLowerCase()] = 1;
                  cases[c.toUpperCase()] = 1;
                } else if (ESC === c[CHAR](0)) {
                  delete p[c];
                }
              }
              peek[n] = concat(p, cases);
            }
            return peek;
          }
        };
        Analyzer[PROTO].set = Analyzer[PROTO].input;
        function Composer() {
          const self2 = this;
          if (!(self2 instanceof Composer)) return new Composer();
          self2.re = null;
          self2.reset();
        }
        Composer.VERSION = __version__;
        Composer[PROTO] = {
          constructor: Composer,
          re: null,
          g: 0,
          grp: null,
          level: 0,
          ast: null,
          dispose: function() {
            const self2 = this;
            self2.re = null;
            self2.g = null;
            self2.grp = null;
            self2.level = null;
            self2.ast = null;
            return self2;
          },
          reset: function() {
            const self2 = this;
            self2.g = 0;
            self2.grp = {};
            self2.level = 0;
            self2.ast = [{ node: [], type: T_SEQUENCE, flag: "" }];
            return self2;
          },
          compose: function() {
            const self2 = this, fl2 = slice(arguments).join(""), src = self2.ast[0].node.join("");
            self2.re = {
              source: src,
              flags: fl2,
              groups: self2.grp,
              pattern: RE(src, fl2)
            };
            self2.reset();
            return self2.re;
          },
          partial: function(reset) {
            const self2 = this, re2 = self2.ast[0].node.join("");
            if (false !== reset) self2.reset();
            return re2;
          },
          token: function(token, escaped) {
            const self2 = this;
            if (null != token)
              self2.ast[self2.level].node.push(escaped ? esc_re(String(token), ESC) : String(token));
            return self2;
          },
          literal: function(literal) {
            return this.token(String(literal), true);
          },
          regexp: function(re2) {
            return this.token(String(re2), false);
          },
          SOL: function() {
            const self2 = this;
            self2.ast[self2.level].node.push("^");
            return self2;
          },
          SOF: function() {
            return this.SOL();
          },
          EOL: function() {
            const self2 = this;
            self2.ast[self2.level].node.push("$");
            return self2;
          },
          EOF: function() {
            return this.EOL();
          },
          LF: function() {
            const self2 = this;
            self2.ast[self2.level].node.push(`${ESC}n`);
            return self2;
          },
          CR: function() {
            const self2 = this;
            self2.ast[self2.level].node.push(`${ESC}r`);
            return self2;
          },
          TAB: function() {
            const self2 = this;
            self2.ast[self2.level].node.push(`${ESC}t`);
            return self2;
          },
          CTRL: function(code) {
            const self2 = this;
            self2.ast[self2.level].node.push(`${ESC}c${code || 0}`);
            return self2;
          },
          HEX: function(code) {
            const self2 = this;
            self2.ast[self2.level].node.push(`${ESC}x${pad(code || 0, 2)}`);
            return self2;
          },
          UNICODE: function(code, uni) {
            const self2 = this;
            self2.ast[self2.level].node.push(true === uni ? `${ESC}u{${String(code || 0)}}` : `${ESC}u${pad(code || 0, 4)}`);
            return self2;
          },
          backSpace: function() {
            const self2 = this;
            self2.ast[self2.level].node.push(`[${ESC}b]`);
            return self2;
          },
          any: function(multiline) {
            const self2 = this;
            self2.ast[self2.level].node.push(multiline ? `[${ESC}s${ESC}S]` : ".");
            return self2;
          },
          space: function(positive) {
            const self2 = this;
            if (arguments.length < 1) positive = true;
            self2.ast[self2.level].node.push(!positive ? `${ESC}S` : `${ESC}s`);
            return self2;
          },
          digit: function(positive) {
            const self2 = this;
            if (arguments.length < 1) positive = true;
            self2.ast[self2.level].node.push(!positive ? `${ESC}D` : `${ESC}d`);
            return self2;
          },
          word: function(positive) {
            const self2 = this;
            if (arguments.length < 1) positive = true;
            self2.ast[self2.level].node.push(!positive ? `${ESC}W` : `${ESC}w`);
            return self2;
          },
          boundary: function(positive) {
            const self2 = this;
            if (arguments.length < 1) positive = true;
            self2.ast[self2.level].node.push(!positive ? `${ESC}B` : `${ESC}b`);
            return self2;
          },
          characters: function() {
            const self2 = this;
            if (T_CHARGROUP === self2.ast[self2.level].type)
              self2.ast[self2.level].node.push(getArgs(arguments, 1).map((c) => {
                return esc_re(String(c), ESC, 1);
              }).join(""));
            return self2;
          },
          range: function(start, end) {
            const self2 = this;
            if (null != start && null != end && T_CHARGROUP === self2.ast[self2.level].type)
              self2.ast[self2.level].node.push(`${esc_re(String(start), ESC, 1)}-${esc_re(String(end), ESC, 1)}`);
            return self2;
          },
          backReference: function(n) {
            const self2 = this;
            self2.ast[self2.level].node.push(ESC + (HAS.call(self2.grp, n) ? self2.grp[n] : n | 0));
            return self2;
          },
          repeat: function(min, max, greedy) {
            const self2 = this;
            if (null == min) return self2;
            if (arguments.length < 3) greedy = true;
            const repeat = (null == max || min === max ? `{${String(min)}}` : `{${String(min)},${String(max)}}`) + (!greedy ? "?" : "");
            self2.ast[self2.level].node[self2.ast[self2.level].node.length - 1] += repeat;
            return self2;
          },
          zeroOrOne: function(greedy) {
            const self2 = this;
            if (arguments.length < 1) greedy = true;
            self2.ast[self2.level].node[self2.ast[self2.level].node.length - 1] += !greedy ? "??" : "?";
            return self2;
          },
          zeroOrMore: function(greedy) {
            const self2 = this;
            if (arguments.length < 1) greedy = true;
            self2.ast[self2.level].node[self2.ast[self2.level].node.length - 1] += !greedy ? "*?" : "*";
            return self2;
          },
          oneOrMore: function(greedy) {
            const self2 = this;
            if (arguments.length < 1) greedy = true;
            self2.ast[self2.level].node[self2.ast[self2.level].node.length - 1] += !greedy ? "+?" : "+";
            return self2;
          },
          alternate: function() {
            const self2 = this;
            self2.level++;
            self2.ast.push({ node: [], type: T_ALTERNATION, flag: "", sequences: [] });
            return self2;
          },
          either: function() {
            return this.alternate();
          },
          or_: function() {
            const self2 = this, ast = self2.ast[self2.level];
            if (T_ALTERNATION === ast.type && ast.node.length) {
              ast.sequences.push(ast.node.join(""));
              ast.node = [];
            }
            return self2;
          },
          group: function(opts, v2) {
            let self2 = this, type = T_GROUP, fl2 = "";
            if (is_string(opts)) {
              fl2 = opts;
              opts = {};
              opts[fl2] = v2;
              fl2 = "";
            } else {
              opts = opts || {};
            }
            if (!!opts["name"] || !!opts["named"]) {
              self2.g++;
              self2.grp[self2.g] = self2.g;
              self2.grp[opts.name || opts.named] = self2.g;
            } else if (true === opts["lookahead"] || false === opts["lookahead"]) {
              fl2 = false === opts["lookahead"] ? "?!" : "?=";
            } else if (true === opts["lookbehind"] || false === opts["lookbehind"]) {
              fl2 = false === opts["lookbehind"] ? "?<!" : "?<=";
            } else if (true === opts["nocapture"]) {
              fl2 = "?:";
            } else if (true === opts["characters"] || false === opts["characters"]) {
              type = T_CHARGROUP;
              fl2 = false === opts["characters"] ? "^" : "";
            } else {
              self2.g++;
              self2.grp[self2.g] = self2.g;
            }
            self2.level++;
            self2.ast.push({ node: [], type, flag: fl2 });
            return self2;
          },
          subGroup: function(opts) {
            return this.group(opts);
          },
          characterGroup: function(positive) {
            return this.group({ "characters": false !== positive });
          },
          namedGroup: function(name3) {
            return this.group({ "name": name3 });
          },
          nonCaptureGroup: function() {
            return this.group({ "nocapture": true });
          },
          lookAheadGroup: function(positive) {
            return this.group({ "lookahead": false !== positive });
          },
          lookBehindGroup: function(positive) {
            return this.group({ "lookbehind": false !== positive });
          },
          end: function(n) {
            let self2 = this, prev, type, flag, part, sequences;
            n = (arguments.length ? n | 0 : 1) || 1;
            while (n--) {
              prev = self2.ast.length ? self2.ast.pop() : null;
              type = prev ? prev.type : 0;
              flag = prev ? prev.flag : "";
              part = prev ? prev.node : [];
              if (T_ALTERNATION === type) {
                sequences = prev ? prev.sequences : [];
                part = !part.length ? sequences : sequences.concat(part.join(""));
              }
              if (0 < self2.level) {
                --self2.level;
                if (T_ALTERNATION === type)
                  self2.ast[self2.level].node.push(part.join("|"));
                else if (T_GROUP === type)
                  self2.ast[self2.level].node.push(`(${flag}${part.join("")})`);
                else if (T_CHARGROUP === type)
                  self2.ast[self2.level].node.push(`[${flag}${part.join("")}]`);
                else
                  self2.ast[self2.level].node.push(part.join(""));
              }
            }
            return self2;
          }
        };
        const CP = Composer[PROTO];
        CP.startOfLine = CP.SOL;
        CP.endOfLine = CP.EOL;
        CP.startOfInput = CP.SOF;
        CP.endOfInput = CP.EOF;
        CP.match = CP.token;
        CP.sub = CP.regexp;
        CP.lineFeed = CP.LF;
        CP.carriageReturn = CP.CR;
        CP.tabulate = CP.TAB;
        CP.wordBoundary = CP.boundary;
        CP.chars = CP.characters;
        CP.charGroup = CP.characterGroup;
        CP.namedSubGroup = CP.namedGroup;
        CP.nonCaptureSubGroup = CP.nonCaptureGroup;
        CP.lookAheadSubGroup = CP.lookAheadGroup;
        CP.lookBehindSubGroup = CP.lookBehindGroup;
        const Regex = {
          VERSION: __version__,
          Node: Node2,
          Analyzer,
          Composer
        };
        return Regex;
      })();
    }
  });

  // src/js/regex-analyzer.ts
  function isRE2(reStr) {
    if (RegexAnalyzer === null) {
      return true;
    }
    try {
      return _isRE2(RegexAnalyzer(reStr, false).tree());
    } catch (e) {
      console.warn("[uBR] regex-analyzer: isRE2 failed", e);
    }
    return false;
  }
  function tokenizableStrFromRegex(reStr) {
    return _literalStrFromRegex(reStr);
  }
  function _isRE2(node) {
    if (node instanceof Object === false) {
      return true;
    }
    if (node.flags instanceof Object) {
      if (node.flags.LookAhead === 1) {
        return false;
      }
      if (node.flags.NegativeLookAhead === 1) {
        return false;
      }
      if (node.flags.LookBehind === 1) {
        return false;
      }
      if (node.flags.NegativeLookBehind === 1) {
        return false;
      }
    }
    if (Array.isArray(node.val)) {
      for (const entry of node.val) {
        if (_isRE2(entry) === false) {
          return false;
        }
      }
    }
    if (node.val instanceof Object) {
      return _isRE2(node.val);
    }
    return true;
  }
  function _literalStrFromRegex(reStr) {
    if (RegexAnalyzer === null) {
      return "";
    }
    let s = "";
    try {
      s = tokenizableStrFromNode(
        RegexAnalyzer(reStr, false).tree()
      );
    } catch (e) {
      console.warn("[uBR] regex-analyzer: _literalStrFromRegex failed", e);
    }
    const reOptional = /[\x02\x03]+/;
    for (; ; ) {
      const match = reOptional.exec(s);
      if (match === null) {
        break;
      }
      const left = s.slice(0, match.index);
      const middle = match[0];
      const right = s.slice(match.index + middle.length);
      s = left;
      s += firstCharCodeClass(right) === 1 || firstCharCodeClass(middle) === 1 ? "" : "\0";
      s += lastCharCodeClass(left) === 1 || lastCharCodeClass(middle) === 1 ? "" : "\0";
      s += right;
    }
    return s;
  }
  function firstCharCodeClass(s) {
    if (s.length === 0) {
      return 0;
    }
    const c = s.charCodeAt(0);
    if (c === 1 || c === 3) {
      return 1;
    }
    return reCharCodeClass.test(s.charAt(0)) ? 1 : 0;
  }
  function lastCharCodeClass(s) {
    const i = s.length - 1;
    if (i === -1) {
      return 0;
    }
    const c = s.charCodeAt(i);
    if (c === 1 || c === 3) {
      return 1;
    }
    return reCharCodeClass.test(s.charAt(i)) ? 1 : 0;
  }
  function tokenizableStrFromNode(node) {
    switch (node.type) {
      case 1: {
        let s = "";
        for (let i = 0; i < node.val.length; i++) {
          s += tokenizableStrFromNode(node.val[i]);
        }
        return s;
      }
      case 2:
      /* T_ALTERNATION, 'Alternation' */
      case 8: {
        if (node.flags.NegativeMatch) {
          return "";
        }
        let firstChar = 0;
        let lastChar = 0;
        for (let i = 0; i < node.val.length; i++) {
          const s = tokenizableStrFromNode(node.val[i]);
          if (firstChar === 0 && firstCharCodeClass(s) === 1) {
            firstChar = 1;
          }
          if (lastChar === 0 && lastCharCodeClass(s) === 1) {
            lastChar = 1;
          }
          if (firstChar === 1 && lastChar === 1) {
            break;
          }
        }
        return String.fromCharCode(firstChar, lastChar);
      }
      case 4: {
        if (node.flags.NegativeLookAhead === 1 || node.flags.NegativeLookBehind === 1) {
          return "";
        }
        return tokenizableStrFromNode(node.val);
      }
      case 16: {
        if (node.flags.max === 0) {
          return "";
        }
        const s = tokenizableStrFromNode(node.val);
        const first = firstCharCodeClass(s);
        const last = lastCharCodeClass(s);
        if (node.flags.min !== 0) {
          return String.fromCharCode(first, last);
        }
        return String.fromCharCode(first + 2, last + 2);
      }
      case 64: {
        if (node.flags.Code === "01" || node.flags.Code === "02" || node.flags.Code === "03") {
          return "\0";
        }
        return node.flags.Char;
      }
      case 128: {
        const flags = node.flags;
        if (flags.EndCharGroup === 1 || // dangling `]`
        flags.EndGroup === 1 || // dangling `)`
        flags.EndRepeats === 1) {
          throw new Error("Unmatched bracket");
        }
        return flags.MatchEnd === 1 || flags.MatchStart === 1 || flags.MatchWordBoundary === 1 ? "\0" : "";
      }
      case 256: {
        for (let i = 0; i < node.val.length; i++) {
          if (firstCharCodeClass(node.val[i]) === 1) {
            return "";
          }
        }
        return "\0";
      }
      // Ranges are assumed to always involve token-related characters.
      case 512: {
        return "";
      }
      case 1024: {
        return node.val;
      }
      case 2048: {
        return "";
      }
      default:
        break;
    }
    return "";
  }
  function toHeaderPattern(reStr) {
    if (RegexAnalyzer === null) {
      return;
    }
    try {
      return _toHeaderPattern(RegexAnalyzer(reStr, false).tree());
    } catch (e) {
      console.warn("[uBR] regex-analyzer: toHeaderPattern failed", e);
    }
  }
  function _toHeaderPattern(branch, depth = 0) {
    switch (branch.type) {
      case 1: {
        let s = "";
        for (const node of branch.val) {
          const t = _toHeaderPattern(node, depth + 1);
          if (t === void 0) {
            return;
          }
          s += t;
        }
        if (depth === 0 && branch.val.length !== 0) {
          const first = branch.val[0];
          if (first.type !== 128 || first.val !== "^") {
            s = `*${s}`;
          }
          const last = branch.val.at(-1);
          if (last.type !== 128 || last.val !== "$") {
            s = `${s}*`;
          }
        }
        return s;
      }
      case 4: {
        if (branch.flags.NegativeLookAhead === 1 || branch.flags.NegativeLookBehind === 1) {
          return;
        }
        return _toHeaderPattern(branch.val, depth + 1);
      }
      case 64:
        return branch.flags.Char;
      case 128: {
        if (branch.val === "^") {
          return "";
        }
        if (branch.val === "$") {
          return "";
        }
        return;
      }
      case 1024:
        return branch.val;
      case 2048:
        return "";
      default:
        break;
    }
  }
  var RegexAnalyzer, reCharCodeClass;
  var init_regex_analyzer = __esm({
    "src/js/regex-analyzer.ts"() {
      init_regex();
      RegexAnalyzer = regex_default && regex_default.Analyzer || null;
      reCharCodeClass = /[%0-9A-Za-z]/;
    }
  });

  // src/js/biditrie.ts
  var VERSION2, PAGE_SIZE, HAYSTACK_START, HAYSTACK_SIZE, HAYSTACK_SIZE_SLOT, TRIE0_SLOT, TRIE1_SLOT, CHAR0_SLOT, CHAR1_SLOT, RESULT_L_SLOT, RESULT_R_SLOT, RESULT_IU_SLOT, TRIE0_START, CELL_BYTE_LENGTH, MIN_FREE_CELL_BYTE_LENGTH, CELL_AND, CELL_OR, SEGMENT_INFO, BCELL_NEXT_AND, BCELL_ALT_AND, BCELL_EXTRA, BCELL_EXTRA_MAX, toSegmentInfo, roundToPageSize, i32Checksum, BidiTrieContainer, getWasmModule, biditrie_default;
  var init_biditrie = __esm({
    "src/js/biditrie.ts"() {
      VERSION2 = 2;
      PAGE_SIZE = 65536 * 2;
      HAYSTACK_START = 0;
      HAYSTACK_SIZE = 8192;
      HAYSTACK_SIZE_SLOT = HAYSTACK_SIZE >>> 2;
      TRIE0_SLOT = HAYSTACK_SIZE_SLOT + 1;
      TRIE1_SLOT = HAYSTACK_SIZE_SLOT + 2;
      CHAR0_SLOT = HAYSTACK_SIZE_SLOT + 3;
      CHAR1_SLOT = HAYSTACK_SIZE_SLOT + 4;
      RESULT_L_SLOT = HAYSTACK_SIZE_SLOT + 5;
      RESULT_R_SLOT = HAYSTACK_SIZE_SLOT + 6;
      RESULT_IU_SLOT = HAYSTACK_SIZE_SLOT + 7;
      TRIE0_START = HAYSTACK_SIZE_SLOT + 8 << 2;
      CELL_BYTE_LENGTH = 12;
      MIN_FREE_CELL_BYTE_LENGTH = CELL_BYTE_LENGTH * 8;
      CELL_AND = 0;
      CELL_OR = 1;
      SEGMENT_INFO = 2;
      BCELL_NEXT_AND = 0;
      BCELL_ALT_AND = 1;
      BCELL_EXTRA = 2;
      BCELL_EXTRA_MAX = 16777215;
      toSegmentInfo = (aL, l, r) => r - l << 24 | aL + l;
      roundToPageSize = (v2) => v2 + PAGE_SIZE - 1 & ~(PAGE_SIZE - 1);
      i32Checksum = (buf32) => {
        const n = buf32.length;
        let hash = 177573 ^ n;
        for (let i = 0; i < n; i++) {
          hash = (hash << 5) + hash ^ buf32[i];
        }
        return hash;
      };
      BidiTrieContainer = class {
        constructor(extraHandler) {
          const len = PAGE_SIZE * 4;
          this.buf8 = new Uint8Array(len);
          this.buf32 = new Uint32Array(this.buf8.buffer);
          this.buf32[TRIE0_SLOT] = TRIE0_START;
          this.buf32[TRIE1_SLOT] = this.buf32[TRIE0_SLOT];
          this.buf32[CHAR0_SLOT] = len >>> 1;
          this.buf32[CHAR1_SLOT] = this.buf32[CHAR0_SLOT];
          this.haystack = this.buf8.subarray(
            HAYSTACK_START,
            HAYSTACK_START + HAYSTACK_SIZE
          );
          this.extraHandler = extraHandler;
          this.textDecoder = null;
          this.wasmMemory = null;
          this.lastStored = "";
          this.lastStoredLen = this.lastStoredIndex = 0;
        }
        //--------------------------------------------------------------------------
        // Public methods
        //--------------------------------------------------------------------------
        getHaystackLen() {
          return this.buf32[HAYSTACK_SIZE_SLOT];
        }
        setHaystackLen(v2) {
          if (v2 > HAYSTACK_SIZE) {
            v2 = HAYSTACK_SIZE;
          }
          this.buf32[HAYSTACK_SIZE_SLOT] = v2;
          return v2;
        }
        reset() {
          this.buf32[TRIE1_SLOT] = this.buf32[TRIE0_SLOT];
          this.buf32[CHAR1_SLOT] = this.buf32[CHAR0_SLOT];
          this.lastStored = "";
          this.lastStoredLen = this.lastStoredIndex = 0;
        }
        createTrie() {
          if (this.buf32[CHAR0_SLOT] - this.buf32[TRIE1_SLOT] < CELL_BYTE_LENGTH) {
            this.growBuf(CELL_BYTE_LENGTH, 0);
          }
          const iroot = this.buf32[TRIE1_SLOT] >>> 2;
          this.buf32[TRIE1_SLOT] += CELL_BYTE_LENGTH;
          this.buf32[iroot + CELL_OR] = 0;
          this.buf32[iroot + CELL_AND] = 0;
          this.buf32[iroot + SEGMENT_INFO] = 0;
          return iroot;
        }
        matches(icell, ai) {
          const buf32 = this.buf32;
          const buf8 = this.buf8;
          const char0 = buf32[CHAR0_SLOT];
          const aR = buf32[HAYSTACK_SIZE_SLOT];
          let al2 = ai, x2 = 0, y2 = 0;
          for (; ; ) {
            x2 = buf8[al2];
            al2 += 1;
            for (; ; ) {
              y2 = buf32[icell + SEGMENT_INFO];
              let bl2 = char0 + (y2 & 16777215);
              if (buf8[bl2] === x2) {
                y2 = (y2 >>> 24) - 1;
                if (y2 !== 0) {
                  x2 = al2 + y2;
                  if (x2 > aR) {
                    return 0;
                  }
                  for (; ; ) {
                    bl2 += 1;
                    if (buf8[bl2] !== buf8[al2]) {
                      return 0;
                    }
                    al2 += 1;
                    if (al2 === x2) {
                      break;
                    }
                  }
                }
                break;
              }
              icell = buf32[icell + CELL_OR];
              if (icell === 0) {
                return 0;
              }
            }
            icell = buf32[icell + CELL_AND];
            x2 = buf32[icell + BCELL_EXTRA];
            if (x2 <= BCELL_EXTRA_MAX) {
              if (x2 !== 0 && this.matchesExtra(ai, al2, x2) !== 0) {
                return 1;
              }
              x2 = buf32[icell + BCELL_ALT_AND];
              if (x2 !== 0 && this.matchesLeft(x2, ai, al2) !== 0) {
                return 1;
              }
              icell = buf32[icell + BCELL_NEXT_AND];
              if (icell === 0) {
                return 0;
              }
            }
            if (al2 === aR) {
              return 0;
            }
          }
          return 0;
        }
        matchesLeft(icell, ar2, r) {
          const buf32 = this.buf32;
          const buf8 = this.buf8;
          const char0 = buf32[CHAR0_SLOT];
          let x2 = 0, y2 = 0;
          for (; ; ) {
            if (ar2 === 0) {
              return 0;
            }
            ar2 -= 1;
            x2 = buf8[ar2];
            for (; ; ) {
              y2 = buf32[icell + SEGMENT_INFO];
              let br = char0 + (y2 & 16777215);
              y2 = (y2 >>> 24) - 1;
              br += y2;
              if (buf8[br] === x2) {
                if (y2 !== 0) {
                  x2 = ar2 - y2;
                  if (x2 < 0) {
                    return 0;
                  }
                  for (; ; ) {
                    ar2 -= 1;
                    br -= 1;
                    if (buf8[ar2] !== buf8[br]) {
                      return 0;
                    }
                    if (ar2 === x2) {
                      break;
                    }
                  }
                }
                break;
              }
              icell = buf32[icell + CELL_OR];
              if (icell === 0) {
                return 0;
              }
            }
            icell = buf32[icell + CELL_AND];
            x2 = buf32[icell + BCELL_EXTRA];
            if (x2 <= BCELL_EXTRA_MAX) {
              if (x2 !== 0 && this.matchesExtra(ar2, r, x2) !== 0) {
                return 1;
              }
              icell = buf32[icell + BCELL_NEXT_AND];
              if (icell === 0) {
                return 0;
              }
            }
          }
          return 0;
        }
        matchesExtra(l, r, ix) {
          let iu2 = 0;
          if (ix !== 1) {
            iu2 = this.extraHandler(l, r, ix);
            if (iu2 === 0) {
              return 0;
            }
          } else {
            iu2 = -1;
          }
          this.buf32[RESULT_IU_SLOT] = iu2;
          this.buf32[RESULT_L_SLOT] = l;
          this.buf32[RESULT_R_SLOT] = r;
          return 1;
        }
        get $l() {
          return this.buf32[RESULT_L_SLOT] | 0;
        }
        get $r() {
          return this.buf32[RESULT_R_SLOT] | 0;
        }
        get $iu() {
          return this.buf32[RESULT_IU_SLOT] | 0;
        }
        add(iroot, aL0, n, pivot = 0) {
          const aR = n;
          if (aR === 0) {
            return 0;
          }
          if (this.buf32[CHAR0_SLOT] - this.buf32[TRIE1_SLOT] < MIN_FREE_CELL_BYTE_LENGTH) {
            this.growBuf(MIN_FREE_CELL_BYTE_LENGTH, 0);
          }
          const buf32 = this.buf32;
          const char0 = buf32[CHAR0_SLOT];
          let icell = iroot;
          const aL = char0 + aL0;
          if (buf32[icell + SEGMENT_INFO] === 0) {
            buf32[icell + SEGMENT_INFO] = toSegmentInfo(aL0, pivot, aR);
            return this.addLeft(icell, aL0, pivot);
          }
          const buf8 = this.buf8;
          let al2 = pivot;
          let inext;
          for (; ; ) {
            const binfo = buf32[icell + SEGMENT_INFO];
            const bR = binfo >>> 24;
            if (bR === 0) {
              icell = buf32[icell + BCELL_NEXT_AND];
              continue;
            }
            let bl2 = char0 + (binfo & 16777215);
            if (buf8[bl2] !== buf8[aL + al2]) {
              inext = buf32[icell + CELL_OR];
              if (inext === 0) {
                inext = this.addCell(0, 0, toSegmentInfo(aL0, al2, aR));
                buf32[icell + CELL_OR] = inext;
                return this.addLeft(inext, aL0, pivot);
              }
              icell = inext;
              continue;
            }
            let bi2 = 1;
            al2 += 1;
            if (bR !== 1) {
              for (; ; ) {
                if (bi2 === bR) {
                  break;
                }
                if (al2 === aR) {
                  break;
                }
                if (buf8[bl2 + bi2] !== buf8[aL + al2]) {
                  break;
                }
                bi2 += 1;
                al2 += 1;
              }
            }
            if (bi2 === bR) {
              if (al2 === aR) {
                return this.addLeft(icell, aL0, pivot);
              }
              inext = buf32[icell + CELL_AND];
              if (buf32[inext + CELL_AND] !== 0) {
                icell = inext;
                continue;
              }
              icell = this.addCell(0, 0, toSegmentInfo(aL0, al2, aR));
              buf32[inext + CELL_AND] = icell;
              return this.addLeft(icell, aL0, pivot);
            }
            bl2 -= char0;
            buf32[icell + SEGMENT_INFO] = bi2 << 24 | bl2;
            inext = this.addCell(
              buf32[icell + CELL_AND],
              0,
              bR - bi2 << 24 | bl2 + bi2
            );
            buf32[icell + CELL_AND] = inext;
            if (al2 === aR) {
              return this.addLeft(icell, aL0, pivot);
            }
            icell = this.addCell(0, 0, toSegmentInfo(aL0, al2, aR));
            buf32[inext + CELL_OR] = icell;
            return this.addLeft(icell, aL0, pivot);
          }
        }
        addLeft(icell, aL0, pivot) {
          const buf32 = this.buf32;
          const char0 = buf32[CHAR0_SLOT];
          const aL = aL0 + char0;
          let iboundary = buf32[icell + CELL_AND];
          if (iboundary === 0 || buf32[iboundary + SEGMENT_INFO] > BCELL_EXTRA_MAX) {
            const inext2 = iboundary;
            iboundary = this.allocateCell();
            buf32[icell + CELL_AND] = iboundary;
            buf32[iboundary + BCELL_NEXT_AND] = inext2;
            if (pivot === 0) {
              return iboundary;
            }
          }
          if (buf32[iboundary + BCELL_EXTRA] === 1) {
            return iboundary;
          }
          if (pivot === 0) {
            return iboundary;
          }
          icell = buf32[iboundary + BCELL_ALT_AND];
          if (icell === 0) {
            icell = this.allocateCell();
            buf32[iboundary + BCELL_ALT_AND] = icell;
          }
          if (buf32[icell + SEGMENT_INFO] === 0) {
            buf32[icell + SEGMENT_INFO] = toSegmentInfo(aL0, 0, pivot);
            iboundary = this.allocateCell();
            buf32[icell + CELL_AND] = iboundary;
            return iboundary;
          }
          const buf8 = this.buf8;
          let ar2 = pivot, inext;
          for (; ; ) {
            const binfo = buf32[icell + SEGMENT_INFO];
            if (binfo <= BCELL_EXTRA_MAX) {
              inext = buf32[icell + CELL_AND];
              if (inext !== 0) {
                icell = inext;
                continue;
              }
              iboundary = this.allocateCell();
              buf32[icell + CELL_AND] = this.addCell(iboundary, 0, toSegmentInfo(aL0, 0, ar2));
              return iboundary;
            }
            const bL = char0 + (binfo & 16777215);
            const bR = bL + (binfo >>> 24);
            let br = bR;
            if (buf8[br - 1] !== buf8[aL + ar2 - 1]) {
              inext = buf32[icell + CELL_OR];
              if (inext === 0) {
                iboundary = this.allocateCell();
                inext = this.addCell(
                  iboundary,
                  0,
                  toSegmentInfo(aL0, 0, ar2)
                );
                buf32[icell + CELL_OR] = inext;
                return iboundary;
              }
              icell = inext;
              continue;
            }
            br -= 1;
            ar2 -= 1;
            if (br !== bL) {
              for (; ; ) {
                if (br === bL) {
                  break;
                }
                if (ar2 === 0) {
                  break;
                }
                if (buf8[br - 1] !== buf8[aL + ar2 - 1]) {
                  break;
                }
                br -= 1;
                ar2 -= 1;
              }
            }
            if (br === bL) {
              inext = buf32[icell + CELL_AND];
              if (ar2 === 0) {
                if (buf32[inext + BCELL_EXTRA] <= BCELL_EXTRA_MAX) {
                  return inext;
                }
                iboundary = this.allocateCell();
                buf32[iboundary + CELL_AND] = inext;
                buf32[icell + CELL_AND] = iboundary;
                return iboundary;
              } else {
                if (inext !== 0) {
                  icell = inext;
                  continue;
                }
                debugger;
                inext = this.addCell(0, 0, 0);
                buf32[icell + CELL_AND] = inext;
                buf32[inext + CELL_AND] = this.addCell(0, 0, toSegmentInfo(aL0, 0, ar2));
              }
            } else {
              buf32[icell + SEGMENT_INFO] = bR - br << 24 | br - char0;
              inext = this.addCell(
                buf32[icell + CELL_AND],
                0,
                br - bL << 24 | bL - char0
              );
              if (ar2 === 0) {
                iboundary = this.allocateCell();
                buf32[icell + CELL_AND] = iboundary;
                buf32[iboundary + CELL_AND] = inext;
                return iboundary;
              } else {
                buf32[icell + CELL_AND] = inext;
                iboundary = this.allocateCell();
                buf32[inext + CELL_OR] = this.addCell(
                  iboundary,
                  0,
                  toSegmentInfo(aL0, 0, ar2)
                );
                return iboundary;
              }
            }
          }
        }
        getExtra(iboundary) {
          return this.buf32[iboundary + BCELL_EXTRA];
        }
        setExtra(iboundary, v2) {
          this.buf32[iboundary + BCELL_EXTRA] = v2;
        }
        optimize() {
          this.shrinkBuf();
        }
        toSelfie() {
          const buf32 = this.buf32.subarray(0, this.buf32[CHAR1_SLOT] + 3 >>> 2);
          return {
            version: VERSION2,
            buf32,
            checksum: i32Checksum(buf32)
          };
        }
        fromSelfie(selfie) {
          if (typeof selfie !== "object" || selfie === null) {
            return false;
          }
          if (selfie.version !== VERSION2) {
            return false;
          }
          if (selfie.buf32 instanceof Uint32Array === false) {
            return false;
          }
          if (selfie.checksum !== i32Checksum(selfie.buf32)) {
            return false;
          }
          const byteLength = selfie.buf32.length << 2;
          if (byteLength === 0) {
            return false;
          }
          this.reallocateBuf(byteLength);
          this.buf32.set(selfie.buf32);
          return true;
        }
        storeString(s) {
          const n = s.length;
          if (n === this.lastStoredLen && s === this.lastStored) {
            return this.lastStoredIndex;
          }
          this.lastStored = s;
          this.lastStoredLen = n;
          if (this.buf8.length - this.buf32[CHAR1_SLOT] < n) {
            this.growBuf(0, n);
          }
          const offset = this.buf32[CHAR1_SLOT];
          this.buf32[CHAR1_SLOT] = offset + n;
          const buf8 = this.buf8;
          for (let i = 0; i < n; i++) {
            buf8[offset + i] = s.charCodeAt(i);
          }
          return this.lastStoredIndex = offset - this.buf32[CHAR0_SLOT];
        }
        extractString(i, n) {
          if (this.textDecoder === null) {
            this.textDecoder = new TextDecoder();
          }
          const offset = this.buf32[CHAR0_SLOT] + i;
          return this.textDecoder.decode(
            this.buf8.subarray(offset, offset + n)
          );
        }
        // WASMable.
        startsWith(haystackLeft, haystackRight, needleLeft, needleLen) {
          if (haystackLeft < 0 || haystackLeft + needleLen > haystackRight) {
            return 0;
          }
          const charCodes = this.buf8;
          needleLeft += this.buf32[CHAR0_SLOT];
          const needleRight = needleLeft + needleLen;
          while (charCodes[haystackLeft] === charCodes[needleLeft]) {
            needleLeft += 1;
            if (needleLeft === needleRight) {
              return 1;
            }
            haystackLeft += 1;
          }
          return 0;
        }
        // Find the left-most instance of substring in main string
        // WASMable.
        indexOf(haystackLeft, haystackEnd, needleLeft, needleLen) {
          if (needleLen === 0) {
            return haystackLeft;
          }
          haystackEnd -= needleLen;
          if (haystackEnd < haystackLeft) {
            return -1;
          }
          needleLeft += this.buf32[CHAR0_SLOT];
          const needleRight = needleLeft + needleLen;
          const charCodes = this.buf8;
          for (; ; ) {
            let i = haystackLeft;
            let j2 = needleLeft;
            while (charCodes[i] === charCodes[j2]) {
              j2 += 1;
              if (j2 === needleRight) {
                return haystackLeft;
              }
              i += 1;
            }
            haystackLeft += 1;
            if (haystackLeft > haystackEnd) {
              break;
            }
          }
          return -1;
        }
        // Find the right-most instance of substring in main string.
        // WASMable.
        lastIndexOf(haystackBeg, haystackEnd, needleLeft, needleLen) {
          if (needleLen === 0) {
            return haystackBeg;
          }
          let haystackLeft = haystackEnd - needleLen;
          if (haystackLeft < haystackBeg) {
            return -1;
          }
          needleLeft += this.buf32[CHAR0_SLOT];
          const needleRight = needleLeft + needleLen;
          const charCodes = this.buf8;
          for (; ; ) {
            let i = haystackLeft;
            let j2 = needleLeft;
            while (charCodes[i] === charCodes[j2]) {
              j2 += 1;
              if (j2 === needleRight) {
                return haystackLeft;
              }
              i += 1;
            }
            if (haystackLeft === haystackBeg) {
              break;
            }
            haystackLeft -= 1;
          }
          return -1;
        }
        dumpTrie(iroot) {
          for (const s of this.trieIterator(iroot)) {
            console.log(s);
          }
        }
        trieIterator(iroot) {
          return {
            value: void 0,
            done: false,
            next() {
              if (this.icell === 0) {
                if (this.forks.length === 0) {
                  this.value = void 0;
                  this.done = true;
                  return this;
                }
                this.pattern = this.forks.pop();
                this.dir = this.forks.pop();
                this.icell = this.forks.pop();
              }
              const buf32 = this.container.buf32;
              const buf8 = this.container.buf8;
              for (; ; ) {
                const ialt = buf32[this.icell + CELL_OR];
                const v2 = buf32[this.icell + SEGMENT_INFO];
                const offset = v2 & 16777215;
                const i0 = buf32[CHAR0_SLOT] + offset;
                const len = v2 >>> 24;
                for (let i = 0; i < len; i++) {
                  this.charBuf[i] = buf8[i0 + i];
                }
                if (len !== 0 && ialt !== 0) {
                  this.forks.push(ialt, this.dir, this.pattern);
                }
                const inext = buf32[this.icell + CELL_AND];
                if (len !== 0) {
                  const s = this.textDecoder.decode(
                    new Uint8Array(this.charBuf.buffer, 0, len)
                  );
                  if (this.dir > 0) {
                    this.pattern += s;
                  } else if (this.dir < 0) {
                    this.pattern = s + this.pattern;
                  }
                }
                this.icell = inext;
                if (len !== 0) {
                  continue;
                }
                if (ialt !== 0) {
                  if (inext === 0) {
                    this.icell = ialt;
                    this.dir = -1;
                  } else {
                    this.forks.push(ialt, -1, this.pattern);
                  }
                }
                if (offset !== 0) {
                  this.value = { pattern: this.pattern, iextra: offset };
                  return this;
                }
              }
            },
            container: this,
            icell: iroot,
            charBuf: new Uint8Array(256),
            pattern: "",
            dir: 1,
            forks: [],
            textDecoder: new TextDecoder(),
            [Symbol.iterator]() {
              return this;
            }
          };
        }
        async enableWASM(wasmModuleFetcher, path) {
          if (typeof WebAssembly !== "object") {
            return false;
          }
          if (this.wasmMemory instanceof WebAssembly.Memory) {
            return true;
          }
          const module = await getWasmModule(wasmModuleFetcher, path);
          if (module instanceof WebAssembly.Module === false) {
            return false;
          }
          const memory = new WebAssembly.Memory({
            initial: roundToPageSize(this.buf8.length) >>> 16
          });
          const instance = await WebAssembly.instantiate(module, {
            imports: { memory, extraHandler: this.extraHandler }
          });
          if (instance instanceof WebAssembly.Instance === false) {
            return false;
          }
          this.wasmMemory = memory;
          const curPageCount = memory.buffer.byteLength >>> 16;
          const newPageCount = roundToPageSize(this.buf8.byteLength) >>> 16;
          if (newPageCount > curPageCount) {
            memory.grow(newPageCount - curPageCount);
          }
          const buf8 = new Uint8Array(memory.buffer);
          buf8.set(this.buf8);
          this.buf8 = buf8;
          this.buf32 = new Uint32Array(this.buf8.buffer);
          this.haystack = this.buf8.subarray(
            HAYSTACK_START,
            HAYSTACK_START + HAYSTACK_SIZE
          );
          this.matches = instance.exports.matches;
          this.startsWith = instance.exports.startsWith;
          this.indexOf = instance.exports.indexOf;
          this.lastIndexOf = instance.exports.lastIndexOf;
          return true;
        }
        dumpInfo() {
          return [
            `Buffer size (Uint8Array): ${this.buf32[CHAR1_SLOT].toLocaleString("en")}`,
            `WASM: ${this.wasmMemory === null ? "disabled" : "enabled"}`
          ].join("\n");
        }
        //--------------------------------------------------------------------------
        // Private methods
        //--------------------------------------------------------------------------
        allocateCell() {
          let icell = this.buf32[TRIE1_SLOT];
          this.buf32[TRIE1_SLOT] = icell + CELL_BYTE_LENGTH;
          icell >>>= 2;
          this.buf32[icell + 0] = 0;
          this.buf32[icell + 1] = 0;
          this.buf32[icell + 2] = 0;
          return icell;
        }
        addCell(iand, ior, v2) {
          const icell = this.allocateCell();
          this.buf32[icell + CELL_AND] = iand;
          this.buf32[icell + CELL_OR] = ior;
          this.buf32[icell + SEGMENT_INFO] = v2;
          return icell;
        }
        growBuf(trieGrow, charGrow) {
          const char0 = Math.max(
            roundToPageSize(this.buf32[TRIE1_SLOT] + trieGrow),
            this.buf32[CHAR0_SLOT]
          );
          const char1 = char0 + this.buf32[CHAR1_SLOT] - this.buf32[CHAR0_SLOT];
          const bufLen = Math.max(
            roundToPageSize(char1 + charGrow),
            this.buf8.length
          );
          if (bufLen > this.buf8.length) {
            this.reallocateBuf(bufLen);
          }
          if (char0 !== this.buf32[CHAR0_SLOT]) {
            this.buf8.copyWithin(
              char0,
              this.buf32[CHAR0_SLOT],
              this.buf32[CHAR1_SLOT]
            );
            this.buf32[CHAR0_SLOT] = char0;
            this.buf32[CHAR1_SLOT] = char1;
          }
        }
        shrinkBuf() {
          const char0 = this.buf32[TRIE1_SLOT] + MIN_FREE_CELL_BYTE_LENGTH;
          const char1 = char0 + this.buf32[CHAR1_SLOT] - this.buf32[CHAR0_SLOT];
          const bufLen = char1 + 256;
          if (char0 !== this.buf32[CHAR0_SLOT]) {
            this.buf8.copyWithin(
              char0,
              this.buf32[CHAR0_SLOT],
              this.buf32[CHAR1_SLOT]
            );
            this.buf32[CHAR0_SLOT] = char0;
            this.buf32[CHAR1_SLOT] = char1;
          }
          if (bufLen < this.buf8.length) {
            this.reallocateBuf(bufLen);
          }
        }
        reallocateBuf(newSize) {
          newSize = roundToPageSize(newSize);
          if (newSize === this.buf8.length) {
            return;
          }
          if (this.wasmMemory === null) {
            const newBuf = new Uint8Array(newSize);
            newBuf.set(
              newBuf.length < this.buf8.length ? this.buf8.subarray(0, newBuf.length) : this.buf8
            );
            this.buf8 = newBuf;
          } else {
            const growBy = (newSize + 65535 >>> 16) - (this.buf8.length >>> 16);
            if (growBy <= 0) {
              return;
            }
            this.wasmMemory.grow(growBy);
            this.buf8 = new Uint8Array(this.wasmMemory.buffer);
          }
          this.buf32 = new Uint32Array(this.buf8.buffer);
          this.haystack = this.buf8.subarray(
            HAYSTACK_START,
            HAYSTACK_START + HAYSTACK_SIZE
          );
        }
      };
      getWasmModule = /* @__PURE__ */ (() => {
        let wasmModulePromise;
        return async function(wasmModuleFetcher, path) {
          if (wasmModulePromise instanceof Promise) {
            return wasmModulePromise;
          }
          if (typeof WebAssembly !== "object") {
            return;
          }
          if (typeof vAPI === "object" && vAPI.canWASM !== true) {
            return;
          }
          const uint32s = new Uint32Array(1);
          const uint8s = new Uint8Array(uint32s.buffer);
          uint32s[0] = 1;
          if (uint8s[0] !== 1) {
            return;
          }
          wasmModulePromise = wasmModuleFetcher(`${path}biditrie`).catch((reason) => {
            console.info(reason);
          });
          return wasmModulePromise;
        };
      })();
      biditrie_default = BidiTrieContainer;
    }
  });

  // src/js/static-filtering-io.ts
  var serialize2, unserialize, blockStartPrefix, blockEndPrefix, CompiledListWriter, CompiledListReader;
  var init_static_filtering_io = __esm({
    "src/js/static-filtering-io.ts"() {
      serialize2 = JSON.stringify;
      unserialize = JSON.parse;
      blockStartPrefix = "#block-start-";
      blockEndPrefix = "#block-end-";
      CompiledListWriter = class {
        constructor() {
          this.blockId = void 0;
          this.block = void 0;
          this.blocks = /* @__PURE__ */ new Map();
          this.properties = /* @__PURE__ */ new Map();
        }
        push(args) {
          this.block.push(serialize2(args));
        }
        pushMany(many) {
          for (const args of many) {
            this.block.push(serialize2(args));
          }
        }
        last() {
          if (Array.isArray(this.block) && this.block.length !== 0) {
            return this.block[this.block.length - 1];
          }
        }
        select(blockId) {
          if (blockId === this.blockId) {
            return;
          }
          this.blockId = blockId;
          this.block = this.blocks.get(blockId);
          if (this.block === void 0) {
            this.blocks.set(blockId, this.block = []);
          }
          return this;
        }
        toString() {
          const result = [];
          const sortedBlocks = Array.from(this.blocks).sort((a, b2) => a[0] - b2[0]);
          for (const [id, lines] of sortedBlocks) {
            if (lines.length === 0) {
              continue;
            }
            result.push(
              blockStartPrefix + id,
              lines.join("\n"),
              blockEndPrefix + id
            );
          }
          return result.join("\n");
        }
        static serialize(arg) {
          return serialize2(arg);
        }
      };
      CompiledListReader = class {
        constructor(raw, blockId) {
          this.block = "";
          this.len = 0;
          this.offset = 0;
          this.line = "";
          this.blocks = /* @__PURE__ */ new Map();
          this.properties = /* @__PURE__ */ new Map();
          const reBlockStart = new RegExp(`^${blockStartPrefix}([\\w:]+)\\n`, "gm");
          let match = reBlockStart.exec(raw);
          while (match !== null) {
            const sectionId = match[1];
            const beg = match.index + match[0].length;
            const end = raw.indexOf(blockEndPrefix + sectionId, beg);
            this.blocks.set(sectionId, raw.slice(beg, end));
            reBlockStart.lastIndex = end;
            match = reBlockStart.exec(raw);
          }
          if (blockId !== void 0) {
            this.select(blockId);
          }
        }
        next() {
          if (this.offset === this.len) {
            this.line = "";
            return false;
          }
          const pos = this.block.indexOf("\n", this.offset);
          if (pos !== -1) {
            this.line = this.block.slice(this.offset, pos);
            this.offset = pos + 1;
          } else {
            this.line = this.block.slice(this.offset);
            this.offset = this.len;
          }
          return true;
        }
        select(blockId) {
          this.block = this.blocks.get(blockId) || "";
          this.len = this.block.length;
          this.offset = 0;
          return this;
        }
        fingerprint() {
          return this.line;
        }
        args() {
          return unserialize(this.line);
        }
        static unserialize(arg) {
          return unserialize(arg);
        }
      };
    }
  });

  // src/js/filtering-context.ts
  var FRAME_ANY, FONT_ANY, INLINE_ANY, PING_ANY, SCRIPT_ANY, FONT, IMAGE, MEDIA, WEBSOCKET, XMLHTTPREQUEST, METHOD_GET, METHOD_POST, typeStrToIntMap, methodStrToBitMap, methodBitToStrMap, reIPv4, _FilteringContext, FilteringContext;
  var init_filtering_context = __esm({
    "src/js/filtering-context.ts"() {
      init_uri_utils();
      FRAME_ANY = 32 /* MAIN_FRAME */ | 2048 /* SUB_FRAME */ | 128 /* OBJECT */;
      FONT_ANY = 4 /* FONT */ | 16384 /* INLINE_FONT */;
      INLINE_ANY = 16384 /* INLINE_FONT */ | 32768 /* INLINE_SCRIPT */;
      PING_ANY = 1 /* BEACON */ | 2 /* CSP_REPORT */ | 256 /* PING */;
      SCRIPT_ANY = 512 /* SCRIPT */ | 32768 /* INLINE_SCRIPT */;
      FONT = 4 /* FONT */;
      IMAGE = 16 /* IMAGE */;
      MEDIA = 64 /* MEDIA */;
      WEBSOCKET = 4096 /* WEBSOCKET */;
      XMLHTTPREQUEST = 8192 /* XMLHTTPREQUEST */;
      METHOD_GET = 1 << 3;
      METHOD_POST = 1 << 7;
      typeStrToIntMap = {
        "no_type": 0 /* NO_TYPE */,
        "beacon": 1 /* BEACON */,
        "csp_report": 2 /* CSP_REPORT */,
        "font": 4 /* FONT */,
        "image": 16 /* IMAGE */,
        "imageset": 16 /* IMAGESET */,
        "main_frame": 32 /* MAIN_FRAME */,
        "media": 64 /* MEDIA */,
        "object": 128 /* OBJECT */,
        "object_subrequest": 128 /* OBJECT_SUBREQUEST */,
        "ping": 256 /* PING */,
        "script": 512 /* SCRIPT */,
        "stylesheet": 1024 /* STYLESHEET */,
        "sub_frame": 2048 /* SUB_FRAME */,
        "websocket": 4096 /* WEBSOCKET */,
        "xmlhttprequest": 8192 /* XMLHTTPREQUEST */,
        "inline-font": 16384 /* INLINE_FONT */,
        "inline-script": 32768 /* INLINE_SCRIPT */,
        "other": 65536 /* OTHER */
      };
      methodStrToBitMap = {
        "": 0 /* NONE */,
        "connect": 2 /* CONNECT */,
        "delete": 4 /* DELETE */,
        "get": 8 /* GET */,
        "head": 16 /* HEAD */,
        "options": 32 /* OPTIONS */,
        "patch": 64 /* PATCH */,
        "post": 128 /* POST */,
        "put": 256 /* PUT */,
        "CONNECT": 2 /* CONNECT */,
        "DELETE": 4 /* DELETE */,
        "GET": 8 /* GET */,
        "HEAD": 16 /* HEAD */,
        "OPTIONS": 32 /* OPTIONS */,
        "PATCH": 64 /* PATCH */,
        "POST": 128 /* POST */,
        "PUT": 256 /* PUT */
      };
      methodBitToStrMap = /* @__PURE__ */ new Map([
        [0 /* NONE */, ""],
        [2 /* CONNECT */, "connect"],
        [4 /* DELETE */, "delete"],
        [8 /* GET */, "get"],
        [16 /* HEAD */, "head"],
        [32 /* OPTIONS */, "options"],
        [64 /* PATCH */, "patch"],
        [128 /* POST */, "post"],
        [256 /* PUT */, "put"]
      ]);
      reIPv4 = /^\d+\.\d+\.\d+\.\d+$/;
      _FilteringContext = class _FilteringContext {
        constructor(other) {
          __publicField(this, "tstamp", 0);
          __publicField(this, "realm", "");
          __publicField(this, "method", 0);
          __publicField(this, "itype", 0 /* NO_TYPE */);
          __publicField(this, "stype");
          __publicField(this, "url");
          __publicField(this, "aliasURL");
          __publicField(this, "hostname");
          __publicField(this, "domain");
          __publicField(this, "ipaddress");
          __publicField(this, "docId", -1);
          __publicField(this, "frameId", -1);
          __publicField(this, "docOrigin");
          __publicField(this, "docHostname");
          __publicField(this, "docDomain");
          __publicField(this, "tabId");
          __publicField(this, "tabOrigin");
          __publicField(this, "tabHostname");
          __publicField(this, "tabDomain");
          __publicField(this, "redirectURL");
          __publicField(this, "filter");
          __publicField(this, "BEACON", 1 /* BEACON */);
          __publicField(this, "CSP_REPORT", 2 /* CSP_REPORT */);
          __publicField(this, "FONT", 4 /* FONT */);
          __publicField(this, "IMAGE", 16 /* IMAGE */);
          __publicField(this, "IMAGESET", 16 /* IMAGESET */);
          __publicField(this, "MAIN_FRAME", 32 /* MAIN_FRAME */);
          __publicField(this, "MEDIA", 64 /* MEDIA */);
          __publicField(this, "OBJECT", 128 /* OBJECT */);
          __publicField(this, "OBJECT_SUBREQUEST", 128 /* OBJECT_SUBREQUEST */);
          __publicField(this, "PING", 256 /* PING */);
          __publicField(this, "SCRIPT", 512 /* SCRIPT */);
          __publicField(this, "STYLESHEET", 1024 /* STYLESHEET */);
          __publicField(this, "SUB_FRAME", 2048 /* SUB_FRAME */);
          __publicField(this, "WEBSOCKET", 4096 /* WEBSOCKET */);
          __publicField(this, "XMLHTTPREQUEST", 8192 /* XMLHTTPREQUEST */);
          __publicField(this, "INLINE_FONT", 16384 /* INLINE_FONT */);
          __publicField(this, "INLINE_SCRIPT", 32768 /* INLINE_SCRIPT */);
          __publicField(this, "OTHER", 65536 /* OTHER */);
          __publicField(this, "FRAME_ANY", FRAME_ANY);
          __publicField(this, "FONT_ANY", FONT_ANY);
          __publicField(this, "INLINE_ANY", INLINE_ANY);
          __publicField(this, "PING_ANY", PING_ANY);
          __publicField(this, "SCRIPT_ANY", SCRIPT_ANY);
          __publicField(this, "METHOD_NONE", 0 /* NONE */);
          __publicField(this, "METHOD_CONNECT", 2 /* CONNECT */);
          __publicField(this, "METHOD_DELETE", 4 /* DELETE */);
          __publicField(this, "METHOD_GET", 8 /* GET */);
          __publicField(this, "METHOD_HEAD", 16 /* HEAD */);
          __publicField(this, "METHOD_OPTIONS", 32 /* OPTIONS */);
          __publicField(this, "METHOD_PATCH", 64 /* PATCH */);
          __publicField(this, "METHOD_POST", 128 /* POST */);
          __publicField(this, "METHOD_PUT", 256 /* PUT */);
          if (other instanceof _FilteringContext) {
            return this.fromFilteringContext(other);
          }
          this.tstamp = 0;
          this.realm = "";
          this.method = 0;
          this.itype = 0 /* NO_TYPE */;
          this.stype = void 0;
          this.url = void 0;
          this.aliasURL = void 0;
          this.hostname = void 0;
          this.domain = void 0;
          this.ipaddress = void 0;
          this.docId = -1;
          this.frameId = -1;
          this.docOrigin = void 0;
          this.docHostname = void 0;
          this.docDomain = void 0;
          this.tabId = void 0;
          this.tabOrigin = void 0;
          this.tabHostname = void 0;
          this.tabDomain = void 0;
          this.redirectURL = void 0;
          this.filter = void 0;
        }
        get type() {
          return this.stype;
        }
        set type(a) {
          this.itype = typeStrToIntMap[a] || 0 /* NO_TYPE */;
          this.stype = a;
        }
        isRootDocument() {
          return (this.itype & 32 /* MAIN_FRAME */) !== 0;
        }
        isDocument() {
          return (this.itype & FRAME_ANY) !== 0;
        }
        isFont() {
          return (this.itype & FONT_ANY) !== 0;
        }
        fromFilteringContext(other) {
          this.realm = other.realm;
          this.type = other.type;
          this.method = other.method;
          this.url = other.url;
          this.hostname = other.hostname;
          this.domain = other.domain;
          this.ipaddress = other.ipaddress;
          this.docId = other.docId;
          this.frameId = other.frameId;
          this.docOrigin = other.docOrigin;
          this.docHostname = other.docHostname;
          this.docDomain = other.docDomain;
          this.tabId = other.tabId;
          this.tabOrigin = other.tabOrigin;
          this.tabHostname = other.tabHostname;
          this.tabDomain = other.tabDomain;
          this.redirectURL = other.redirectURL;
          this.filter = void 0;
          return this;
        }
        fromDetails({ originURL, url, type }) {
          this.setDocOriginFromURL(originURL).setURL(url).setType(type);
          return this;
        }
        duplicate() {
          return new _FilteringContext(this);
        }
        setRealm(a) {
          this.realm = a;
          return this;
        }
        setType(a) {
          this.type = a;
          return this;
        }
        setURL(a) {
          if (a !== this.url) {
            this.hostname = this.domain = this.ipaddress = void 0;
            this.url = a;
          }
          return this;
        }
        getHostname() {
          if (this.hostname === void 0) {
            this.hostname = hostnameFromURI(this.url);
          }
          return this.hostname;
        }
        setHostname(a) {
          if (a !== this.hostname) {
            this.domain = void 0;
            this.hostname = a;
          }
          return this;
        }
        getDomain() {
          if (this.domain === void 0) {
            this.domain = domainFromHostname(this.getHostname());
          }
          return this.domain;
        }
        setDomain(a) {
          this.domain = a;
          return this;
        }
        getIPAddress() {
          if (this.ipaddress !== void 0) {
            return this.ipaddress;
          }
          const ipaddr = this.getHostname();
          const c0 = ipaddr.charCodeAt(0);
          if (c0 === 91) {
            return this.ipaddress = ipaddr.slice(1, -1);
          } else if (c0 <= 57 && c0 >= 48) {
            if (reIPv4.test(ipaddr)) {
              return this.ipaddress = ipaddr;
            }
          }
          return this.ipaddress = "";
        }
        setIPAddress(ipaddr) {
          this.ipaddress = ipaddr || void 0;
          return this;
        }
        getDocOrigin() {
          if (this.docOrigin === void 0) {
            this.docOrigin = this.tabOrigin;
          }
          return this.docOrigin;
        }
        setDocOrigin(a) {
          if (a !== this.docOrigin) {
            this.docHostname = this.docDomain = void 0;
            this.docOrigin = a;
          }
          return this;
        }
        setDocOriginFromURL(a) {
          return this.setDocOrigin(originFromURI(a));
        }
        getDocHostname() {
          if (this.docHostname === void 0) {
            this.docHostname = hostnameFromURI(this.getDocOrigin());
          }
          return this.docHostname;
        }
        setDocHostname(a) {
          if (a !== this.docHostname) {
            this.docDomain = void 0;
            this.docHostname = a;
          }
          return this;
        }
        getDocDomain() {
          if (this.docDomain === void 0) {
            this.docDomain = domainFromHostname(this.getDocHostname());
          }
          return this.docDomain;
        }
        setDocDomain(a) {
          this.docDomain = a;
          return this;
        }
        is3rdPartyToDoc() {
          let docDomain = this.getDocDomain();
          if (docDomain === "") {
            docDomain = this.docHostname;
          }
          if (this.domain !== void 0 && this.domain !== "") {
            return this.domain !== docDomain;
          }
          const hostname = this.getHostname();
          if (hostname.endsWith(docDomain) === false) {
            return true;
          }
          const i = hostname.length - docDomain.length;
          if (i === 0) {
            return false;
          }
          return hostname.charCodeAt(i - 1) !== 46;
        }
        setTabId(a) {
          this.tabId = a;
          return this;
        }
        getTabOrigin() {
          return this.tabOrigin;
        }
        setTabOrigin(a) {
          if (a !== this.tabOrigin) {
            this.tabHostname = this.tabDomain = void 0;
            this.tabOrigin = a;
          }
          return this;
        }
        setTabOriginFromURL(a) {
          return this.setTabOrigin(originFromURI(a));
        }
        getTabHostname() {
          if (this.tabHostname === void 0) {
            this.tabHostname = hostnameFromURI(this.getTabOrigin());
          }
          return this.tabHostname;
        }
        setTabHostname(a) {
          if (a !== this.tabHostname) {
            this.tabDomain = void 0;
            this.tabHostname = a;
          }
          return this;
        }
        getTabDomain() {
          if (this.tabDomain === void 0) {
            this.tabDomain = domainFromHostname(this.getTabHostname());
          }
          return this.tabDomain;
        }
        setTabDomain(a) {
          this.docDomain = a;
          return this;
        }
        is3rdPartyToTab() {
          let tabDomain = this.getTabDomain();
          if (tabDomain === "") {
            tabDomain = this.tabHostname;
          }
          if (this.domain !== void 0 && this.domain !== "") {
            return this.domain !== tabDomain;
          }
          const hostname = this.getHostname();
          if (hostname.endsWith(tabDomain) === false) {
            return true;
          }
          const i = hostname.length - tabDomain.length;
          if (i === 0) {
            return false;
          }
          return hostname.charCodeAt(i - 1) !== 46;
        }
        setFilter(a) {
          this.filter = a;
          return this;
        }
        pushFilter(a) {
          if (this.filter === void 0) {
            return this.setFilter(a);
          }
          if (Array.isArray(this.filter)) {
            this.filter.push(a);
          } else {
            this.filter = [this.filter, a];
          }
          return this;
        }
        pushFilters(a) {
          if (this.filter === void 0) {
            return this.setFilter(a);
          }
          if (Array.isArray(this.filter)) {
            this.filter.push(...a);
          } else {
            this.filter = [this.filter, ...a];
          }
          return this;
        }
        setMethod(a) {
          this.method = methodStrToBitMap[a] || 0;
          return this;
        }
        getMethodName() {
          return _FilteringContext.getMethodName(this.method);
        }
        static get(a) {
          return methodStrToBitMap[a] || 0;
        }
        static getMethod(a) {
          return methodStrToBitMap[a] || 0;
        }
        static getMethodName(a) {
          return methodBitToStrMap.get(a) || "";
        }
      };
      __publicField(_FilteringContext, "BEACON", 1 /* BEACON */);
      __publicField(_FilteringContext, "CSP_REPORT", 2 /* CSP_REPORT */);
      __publicField(_FilteringContext, "FONT", 4 /* FONT */);
      __publicField(_FilteringContext, "IMAGE", 16 /* IMAGE */);
      __publicField(_FilteringContext, "IMAGESET", 16 /* IMAGESET */);
      __publicField(_FilteringContext, "MAIN_FRAME", 32 /* MAIN_FRAME */);
      __publicField(_FilteringContext, "MEDIA", 64 /* MEDIA */);
      __publicField(_FilteringContext, "OBJECT", 128 /* OBJECT */);
      __publicField(_FilteringContext, "OBJECT_SUBREQUEST", 128 /* OBJECT_SUBREQUEST */);
      __publicField(_FilteringContext, "PING", 256 /* PING */);
      __publicField(_FilteringContext, "SCRIPT", 512 /* SCRIPT */);
      __publicField(_FilteringContext, "STYLESHEET", 1024 /* STYLESHEET */);
      __publicField(_FilteringContext, "SUB_FRAME", 2048 /* SUB_FRAME */);
      __publicField(_FilteringContext, "WEBSOCKET", 4096 /* WEBSOCKET */);
      __publicField(_FilteringContext, "XMLHTTPREQUEST", 8192 /* XMLHTTPREQUEST */);
      __publicField(_FilteringContext, "INLINE_FONT", 16384 /* INLINE_FONT */);
      __publicField(_FilteringContext, "INLINE_SCRIPT", 32768 /* INLINE_SCRIPT */);
      __publicField(_FilteringContext, "OTHER", 65536 /* OTHER */);
      __publicField(_FilteringContext, "FRAME_ANY", FRAME_ANY);
      __publicField(_FilteringContext, "FONT_ANY", FONT_ANY);
      __publicField(_FilteringContext, "INLINE_ANY", INLINE_ANY);
      __publicField(_FilteringContext, "PING_ANY", PING_ANY);
      __publicField(_FilteringContext, "SCRIPT_ANY", SCRIPT_ANY);
      __publicField(_FilteringContext, "METHOD_NONE", 0 /* NONE */);
      __publicField(_FilteringContext, "METHOD_CONNECT", 2 /* CONNECT */);
      __publicField(_FilteringContext, "METHOD_DELETE", 4 /* DELETE */);
      __publicField(_FilteringContext, "METHOD_GET", 8 /* GET */);
      __publicField(_FilteringContext, "METHOD_HEAD", 16 /* HEAD */);
      __publicField(_FilteringContext, "METHOD_OPTIONS", 32 /* OPTIONS */);
      __publicField(_FilteringContext, "METHOD_PATCH", 64 /* PATCH */);
      __publicField(_FilteringContext, "METHOD_POST", 128 /* POST */);
      __publicField(_FilteringContext, "METHOD_PUT", 256 /* PUT */);
      FilteringContext = _FilteringContext;
    }
  });

  // src/js/hntrie.ts
  var PAGE_SIZE2, TRIE0_SLOT2, TRIE1_SLOT2, CHAR0_SLOT2, CHAR1_SLOT2, TRIE0_START2, roundToPageSize2, i32Checksum2, HNTrieContainer, getWasmModule2, hntrie_default;
  var init_hntrie = __esm({
    "src/js/hntrie.ts"() {
      PAGE_SIZE2 = 65536;
      TRIE0_SLOT2 = 256 >>> 2;
      TRIE1_SLOT2 = TRIE0_SLOT2 + 1;
      CHAR0_SLOT2 = TRIE0_SLOT2 + 2;
      CHAR1_SLOT2 = TRIE0_SLOT2 + 3;
      TRIE0_START2 = TRIE0_SLOT2 + 4 << 2;
      roundToPageSize2 = (v2) => v2 + PAGE_SIZE2 - 1 & ~(PAGE_SIZE2 - 1);
      i32Checksum2 = (buf32) => {
        const n = buf32.length;
        let hash = 177573 ^ n;
        for (let i = 0; i < n; i++) {
          hash = (hash << 5) + hash ^ buf32[i];
        }
        return hash;
      };
      HNTrieContainer = class {
        constructor() {
          const len = PAGE_SIZE2 * 2;
          this.buf = new Uint8Array(len);
          this.buf32 = new Uint32Array(this.buf.buffer);
          this.needle = "";
          this.buf32[TRIE0_SLOT2] = TRIE0_START2;
          this.buf32[TRIE1_SLOT2] = this.buf32[TRIE0_SLOT2];
          this.buf32[CHAR0_SLOT2] = len >>> 1;
          this.buf32[CHAR1_SLOT2] = this.buf32[CHAR0_SLOT2];
          this.wasmMemory = null;
          this.lastStored = "";
          this.lastStoredLen = this.lastStoredIndex = 0;
        }
        //--------------------------------------------------------------------------
        // Public methods
        //--------------------------------------------------------------------------
        reset(details) {
          if (details instanceof Object && typeof details.byteLength === "number" && typeof details.char0 === "number") {
            if (details.byteLength > this.buf.byteLength) {
              this.reallocateBuf(details.byteLength);
            }
            this.buf32[CHAR0_SLOT2] = details.char0;
          }
          this.buf32[TRIE1_SLOT2] = this.buf32[TRIE0_SLOT2];
          this.buf32[CHAR1_SLOT2] = this.buf32[CHAR0_SLOT2];
          this.lastStored = "";
          this.lastStoredLen = this.lastStoredIndex = 0;
        }
        setNeedle(needle) {
          if (needle !== this.needle) {
            const buf = this.buf;
            let i = needle.length;
            if (i > 255) {
              i = 255;
            }
            buf[255] = i;
            while (i--) {
              buf[i] = needle.charCodeAt(i);
            }
            this.needle = needle;
          }
          return this;
        }
        matchesJS(iroot) {
          const buf32 = this.buf32;
          const buf8 = this.buf;
          const char0 = buf32[CHAR0_SLOT2];
          let ineedle = buf8[255];
          let icell = buf32[iroot + 0];
          if (icell === 0) {
            return -1;
          }
          let c = 0, v2 = 0, i0 = 0, n = 0;
          for (; ; ) {
            if (ineedle === 0) {
              return -1;
            }
            ineedle -= 1;
            c = buf8[ineedle];
            for (; ; ) {
              v2 = buf32[icell + 2];
              i0 = char0 + (v2 >>> 8);
              if (buf8[i0] === c) {
                break;
              }
              icell = buf32[icell + 0];
              if (icell === 0) {
                return -1;
              }
            }
            n = v2 & 127;
            if (n > 1) {
              n -= 1;
              if (n > ineedle) {
                return -1;
              }
              i0 += 1;
              const i1 = i0 + n;
              do {
                ineedle -= 1;
                if (buf8[i0] !== buf8[ineedle]) {
                  return -1;
                }
                i0 += 1;
              } while (i0 < i1);
            }
            if ((v2 & 128) !== 0) {
              if (ineedle === 0 || buf8[ineedle - 1] === 46) {
                return ineedle;
              }
            }
            icell = buf32[icell + 1];
            if (icell === 0) {
              break;
            }
          }
          return -1;
        }
        createTrie() {
          if (this.buf32[CHAR0_SLOT2] - this.buf32[TRIE1_SLOT2] < 12) {
            this.growBuf(12, 0);
          }
          const iroot = this.buf32[TRIE1_SLOT2] >>> 2;
          this.buf32[TRIE1_SLOT2] += 12;
          this.buf32[iroot + 0] = 0;
          this.buf32[iroot + 1] = 0;
          this.buf32[iroot + 2] = 0;
          return iroot;
        }
        createTrieFromIterable(hostnames) {
          const itrie = this.createTrie();
          for (const hn2 of hostnames) {
            if (hn2 === "") {
              continue;
            }
            this.setNeedle(hn2).add(itrie);
          }
          return itrie;
        }
        createTrieFromStoredDomainOpt(i, n) {
          const itrie = this.createTrie();
          const jend = i + n;
          let j2 = i, offset = 0, k2 = 0, c = 0;
          while (j2 !== jend) {
            offset = this.buf32[CHAR0_SLOT2];
            k2 = 0;
            for (; ; ) {
              if (j2 === jend) {
                break;
              }
              c = this.buf[offset + j2];
              j2 += 1;
              if (c === 124) {
                break;
              }
              if (k2 === 255) {
                continue;
              }
              this.buf[k2] = c;
              k2 += 1;
            }
            if (k2 !== 0) {
              this.buf[255] = k2;
              this.add(itrie);
            }
          }
          this.needle = "";
          this.buf[255] = 0;
          return itrie;
        }
        dumpTrie(iroot) {
          let hostnames = Array.from(this.trieIterator(iroot));
          if (String.prototype.padStart instanceof Function) {
            const maxlen = Math.min(
              hostnames.reduce((maxlen2, hn2) => Math.max(maxlen2, hn2.length), 0),
              64
            );
            hostnames = hostnames.map((hn2) => hn2.padStart(maxlen));
          }
          for (const hn2 of hostnames) {
            console.log(hn2);
          }
        }
        trieIterator(iroot) {
          return {
            value: void 0,
            done: false,
            next() {
              if (this.icell === 0) {
                if (this.forks.length === 0) {
                  this.value = void 0;
                  this.done = true;
                  return this;
                }
                this.charPtr = this.forks.pop();
                this.icell = this.forks.pop();
              }
              for (; ; ) {
                const idown = this.container.buf32[this.icell + 0];
                if (idown !== 0) {
                  this.forks.push(idown, this.charPtr);
                }
                const v2 = this.container.buf32[this.icell + 2];
                let i0 = this.container.buf32[CHAR0_SLOT2] + (v2 >>> 8);
                const i1 = i0 + (v2 & 127);
                while (i0 < i1) {
                  this.charPtr -= 1;
                  this.charBuf[this.charPtr] = this.container.buf[i0];
                  i0 += 1;
                }
                this.icell = this.container.buf32[this.icell + 1];
                if ((v2 & 128) !== 0) {
                  return this.toHostname();
                }
              }
            },
            toHostname() {
              this.value = this.textDecoder.decode(
                new Uint8Array(this.charBuf.buffer, this.charPtr)
              );
              return this;
            },
            container: this,
            icell: this.buf32[iroot],
            charBuf: new Uint8Array(256),
            charPtr: 256,
            forks: [],
            textDecoder: new TextDecoder(),
            [Symbol.iterator]() {
              return this;
            }
          };
        }
        // TODO:
        //   Rework code to add from a string already present in the character
        //   buffer, i.e. not having to go through setNeedle() when adding a new
        //   hostname to a trie. This will require much work though, and probably
        //   changing the order in which string segments are stored in the
        //   character buffer.
        addJS(iroot) {
          let lhnchar = this.buf[255];
          if (lhnchar === 0) {
            return 0;
          }
          if (this.buf32[CHAR0_SLOT2] - this.buf32[TRIE1_SLOT2] < 24 || this.buf.length - this.buf32[CHAR1_SLOT2] < 256) {
            this.growBuf(24, 256);
          }
          let icell = this.buf32[iroot + 0];
          if (icell === 0) {
            this.buf32[iroot + 0] = this.addLeafCell(lhnchar);
            return 1;
          }
          const char0 = this.buf32[CHAR0_SLOT2];
          let isegchar, lsegchar, boundaryBit, inext;
          for (; ; ) {
            const v2 = this.buf32[icell + 2];
            let isegchar0 = char0 + (v2 >>> 8);
            if (this.buf[isegchar0] !== this.buf[lhnchar - 1]) {
              inext = this.buf32[icell + 0];
              if (inext === 0) {
                this.buf32[icell + 0] = this.addLeafCell(lhnchar);
                return 1;
              }
              icell = inext;
              continue;
            }
            isegchar = 1;
            lhnchar -= 1;
            lsegchar = v2 & 127;
            if (lsegchar !== 1) {
              for (; ; ) {
                if (isegchar === lsegchar) {
                  break;
                }
                if (lhnchar === 0) {
                  break;
                }
                if (this.buf[isegchar0 + isegchar] !== this.buf[lhnchar - 1]) {
                  break;
                }
                isegchar += 1;
                lhnchar -= 1;
              }
            }
            boundaryBit = v2 & 128;
            if (isegchar === lsegchar) {
              if (lhnchar === 0) {
                if (boundaryBit !== 0) {
                  return 0;
                }
                this.buf32[icell + 2] = v2 | 128;
              } else {
                if (boundaryBit !== 0) {
                  if (this.buf[lhnchar - 1] === 46) {
                    return -1;
                  }
                }
                inext = this.buf32[icell + 1];
                if (inext !== 0) {
                  icell = inext;
                  continue;
                }
                this.buf32[icell + 1] = this.addLeafCell(lhnchar);
              }
            } else {
              isegchar0 -= char0;
              this.buf32[icell + 2] = isegchar0 << 8 | isegchar;
              inext = this.addCell(
                0,
                this.buf32[icell + 1],
                isegchar0 + isegchar << 8 | boundaryBit | lsegchar - isegchar
              );
              this.buf32[icell + 1] = inext;
              if (lhnchar !== 0) {
                this.buf32[inext + 0] = this.addLeafCell(lhnchar);
              } else {
                this.buf32[icell + 2] |= 128;
              }
            }
            return 1;
          }
        }
        optimize() {
          this.shrinkBuf();
          return {
            byteLength: this.buf.byteLength,
            char0: this.buf32[CHAR0_SLOT2]
          };
        }
        toSelfie() {
          const buf32 = this.buf32.subarray(0, this.buf32[CHAR1_SLOT2] + 3 >>> 2);
          return { buf32, checksum: i32Checksum2(buf32) };
        }
        fromSelfie(selfie) {
          if (typeof selfie !== "object" || selfie === null) {
            return false;
          }
          if (selfie.buf32 instanceof Uint32Array === false) {
            return false;
          }
          if (selfie.checksum !== i32Checksum2(selfie.buf32)) {
            return false;
          }
          this.needle = "";
          let byteLength = selfie.buf32.length << 2;
          if (byteLength === 0) {
            return false;
          }
          byteLength = roundToPageSize2(byteLength);
          if (this.wasmMemory !== null) {
            const pageCountBefore = this.buf.length >>> 16;
            const pageCountAfter = byteLength >>> 16;
            if (pageCountAfter > pageCountBefore) {
              this.wasmMemory.grow(pageCountAfter - pageCountBefore);
              this.buf = new Uint8Array(this.wasmMemory.buffer);
              this.buf32 = new Uint32Array(this.buf.buffer);
            }
            this.buf32.set(selfie.buf32);
          } else {
            this.buf32 = selfie.buf32;
            this.buf = new Uint8Array(this.buf32.buffer);
          }
          this.buf[255] = 0;
          return true;
        }
        // The following *Hostname() methods can be used to store hostname strings
        // outside the trie. This is useful to store/match hostnames which are
        // not part of a collection, and yet still benefit from storing the strings
        // into a trie container's character buffer.
        // TODO: WASM version of matchesHostname()
        storeHostname(hn2) {
          let n = hn2.length;
          if (n > 255) {
            hn2 = hn2.slice(-255);
            n = 255;
          }
          if (n === this.lastStoredLen && hn2 === this.lastStored) {
            return this.lastStoredIndex;
          }
          this.lastStored = hn2;
          this.lastStoredLen = n;
          if (this.buf.length - this.buf32[CHAR1_SLOT2] < n) {
            this.growBuf(0, n);
          }
          const offset = this.buf32[CHAR1_SLOT2];
          this.buf32[CHAR1_SLOT2] = offset + n;
          const buf8 = this.buf;
          for (let i = 0; i < n; i++) {
            buf8[offset + i] = hn2.charCodeAt(i);
          }
          return this.lastStoredIndex = offset - this.buf32[CHAR0_SLOT2];
        }
        extractHostname(i, n) {
          const textDecoder2 = new TextDecoder();
          const offset = this.buf32[CHAR0_SLOT2] + i;
          return textDecoder2.decode(this.buf.subarray(offset, offset + n));
        }
        storeDomainOpt(s) {
          const n = s.length;
          if (n === this.lastStoredLen && s === this.lastStored) {
            return this.lastStoredIndex;
          }
          this.lastStored = s;
          this.lastStoredLen = n;
          if (this.buf.length - this.buf32[CHAR1_SLOT2] < n) {
            this.growBuf(0, n);
          }
          const offset = this.buf32[CHAR1_SLOT2];
          this.buf32[CHAR1_SLOT2] = offset + n;
          const buf8 = this.buf;
          for (let i = 0; i < n; i++) {
            buf8[offset + i] = s.charCodeAt(i);
          }
          return this.lastStoredIndex = offset - this.buf32[CHAR0_SLOT2];
        }
        extractDomainOpt(i, n) {
          const textDecoder2 = new TextDecoder();
          const offset = this.buf32[CHAR0_SLOT2] + i;
          return textDecoder2.decode(this.buf.subarray(offset, offset + n));
        }
        matchesHostname(hn2, i, n) {
          this.setNeedle(hn2);
          const buf8 = this.buf;
          const hr = buf8[255];
          if (n > hr) {
            return false;
          }
          const hl2 = hr - n;
          const nl2 = this.buf32[CHAR0_SLOT2] + i;
          for (let j2 = 0; j2 < n; j2++) {
            if (buf8[nl2 + j2] !== buf8[hl2 + j2]) {
              return false;
            }
          }
          return n === hr || hn2.charCodeAt(hl2 - 1) === 46;
        }
        async enableWASM(wasmModuleFetcher, path) {
          if (typeof WebAssembly === "undefined") {
            return false;
          }
          if (this.wasmMemory instanceof WebAssembly.Memory) {
            return true;
          }
          const module = await getWasmModule2(wasmModuleFetcher, path);
          if (module instanceof WebAssembly.Module === false) {
            return false;
          }
          const memory = new WebAssembly.Memory({ initial: 2 });
          const instance = await WebAssembly.instantiate(module, {
            imports: {
              memory,
              growBuf: this.growBuf.bind(this, 24, 256)
            }
          });
          if (instance instanceof WebAssembly.Instance === false) {
            return false;
          }
          this.wasmMemory = memory;
          const curPageCount = memory.buffer.byteLength >>> 16;
          const newPageCount = roundToPageSize2(this.buf.byteLength) >>> 16;
          if (newPageCount > curPageCount) {
            memory.grow(newPageCount - curPageCount);
          }
          const buf = new Uint8Array(memory.buffer);
          buf.set(this.buf);
          this.buf = buf;
          this.buf32 = new Uint32Array(this.buf.buffer);
          this.matches = this.matchesWASM = instance.exports.matches;
          this.add = this.addWASM = instance.exports.add;
          return true;
        }
        dumpInfo() {
          return [
            `Buffer size (Uint8Array): ${this.buf32[CHAR1_SLOT2].toLocaleString("en")}`,
            `  Char segment size: ${(this.buf32[CHAR1_SLOT2] - this.buf32[CHAR0_SLOT2]).toLocaleString("en")}`,
            `WASM: ${this.wasmMemory === null ? "disabled" : "enabled"}`
          ].join("\n");
        }
        //--------------------------------------------------------------------------
        // Private methods
        //--------------------------------------------------------------------------
        addCell(idown, iright, v2) {
          let icell = this.buf32[TRIE1_SLOT2];
          this.buf32[TRIE1_SLOT2] = icell + 12;
          icell >>>= 2;
          this.buf32[icell + 0] = idown;
          this.buf32[icell + 1] = iright;
          this.buf32[icell + 2] = v2;
          return icell;
        }
        addLeafCell(lsegchar) {
          const r = this.buf32[TRIE1_SLOT2] >>> 2;
          let i = r;
          while (lsegchar > 127) {
            this.buf32[i + 0] = 0;
            this.buf32[i + 1] = i + 3;
            this.buf32[i + 2] = this.addSegment(lsegchar, lsegchar - 127);
            lsegchar -= 127;
            i += 3;
          }
          this.buf32[i + 0] = 0;
          this.buf32[i + 1] = 0;
          this.buf32[i + 2] = this.addSegment(lsegchar, 0) | 128;
          this.buf32[TRIE1_SLOT2] = i + 3 << 2;
          return r;
        }
        addSegment(lsegchar, lsegend) {
          if (lsegchar === 0) {
            return 0;
          }
          let char1 = this.buf32[CHAR1_SLOT2];
          const isegchar = char1 - this.buf32[CHAR0_SLOT2];
          let i = lsegchar;
          do {
            this.buf[char1++] = this.buf[--i];
          } while (i !== lsegend);
          this.buf32[CHAR1_SLOT2] = char1;
          return isegchar << 8 | lsegchar - lsegend;
        }
        growBuf(trieGrow, charGrow) {
          const char0 = Math.max(
            roundToPageSize2(this.buf32[TRIE1_SLOT2] + trieGrow),
            this.buf32[CHAR0_SLOT2]
          );
          const char1 = char0 + this.buf32[CHAR1_SLOT2] - this.buf32[CHAR0_SLOT2];
          const bufLen = Math.max(
            roundToPageSize2(char1 + charGrow),
            this.buf.length
          );
          this.resizeBuf(bufLen, char0);
        }
        shrinkBuf() {
          if (this.wasmMemory !== null) {
            return;
          }
          const char0 = this.buf32[TRIE1_SLOT2] + 24;
          const char1 = char0 + this.buf32[CHAR1_SLOT2] - this.buf32[CHAR0_SLOT2];
          const bufLen = char1 + 256;
          this.resizeBuf(bufLen, char0);
        }
        resizeBuf(bufLen, char0) {
          bufLen = roundToPageSize2(bufLen);
          if (bufLen === this.buf.length && char0 === this.buf32[CHAR0_SLOT2]) {
            return;
          }
          const charDataLen = this.buf32[CHAR1_SLOT2] - this.buf32[CHAR0_SLOT2];
          if (this.wasmMemory !== null) {
            const pageCount = (bufLen >>> 16) - (this.buf.byteLength >>> 16);
            if (pageCount > 0) {
              this.wasmMemory.grow(pageCount);
              this.buf = new Uint8Array(this.wasmMemory.buffer);
              this.buf32 = new Uint32Array(this.wasmMemory.buffer);
            }
          } else if (bufLen !== this.buf.length) {
            const newBuf = new Uint8Array(bufLen);
            newBuf.set(
              new Uint8Array(
                this.buf.buffer,
                0,
                this.buf32[TRIE1_SLOT2]
              ),
              0
            );
            newBuf.set(
              new Uint8Array(
                this.buf.buffer,
                this.buf32[CHAR0_SLOT2],
                charDataLen
              ),
              char0
            );
            this.buf = newBuf;
            this.buf32 = new Uint32Array(this.buf.buffer);
            this.buf32[CHAR0_SLOT2] = char0;
            this.buf32[CHAR1_SLOT2] = char0 + charDataLen;
          }
          if (char0 !== this.buf32[CHAR0_SLOT2]) {
            this.buf.set(
              new Uint8Array(
                this.buf.buffer,
                this.buf32[CHAR0_SLOT2],
                charDataLen
              ),
              char0
            );
            this.buf32[CHAR0_SLOT2] = char0;
            this.buf32[CHAR1_SLOT2] = char0 + charDataLen;
          }
        }
        reallocateBuf(newSize) {
          newSize = roundToPageSize2(newSize);
          if (newSize === this.buf.length) {
            return;
          }
          if (this.wasmMemory === null) {
            const newBuf = new Uint8Array(newSize);
            newBuf.set(
              newBuf.length < this.buf.length ? this.buf.subarray(0, newBuf.length) : this.buf
            );
            this.buf = newBuf;
          } else {
            const growBy = (newSize + 65535 >>> 16) - (this.buf.length >>> 16);
            if (growBy <= 0) {
              return;
            }
            this.wasmMemory.grow(growBy);
            this.buf = new Uint8Array(this.wasmMemory.buffer);
          }
          this.buf32 = new Uint32Array(this.buf.buffer);
        }
      };
      HNTrieContainer.prototype.matches = HNTrieContainer.prototype.matchesJS;
      HNTrieContainer.prototype.matchesWASM = null;
      HNTrieContainer.prototype.add = HNTrieContainer.prototype.addJS;
      HNTrieContainer.prototype.addWASM = null;
      getWasmModule2 = /* @__PURE__ */ (() => {
        let wasmModulePromise;
        return async function(wasmModuleFetcher, path) {
          if (wasmModulePromise instanceof Promise) {
            return wasmModulePromise;
          }
          const uint32s = new Uint32Array(1);
          const uint8s = new Uint8Array(uint32s.buffer);
          uint32s[0] = 1;
          if (uint8s[0] !== 1) {
            return;
          }
          wasmModulePromise = wasmModuleFetcher(`${path}hntrie`).catch((reason) => {
            console.info(reason);
          });
          return wasmModulePromise;
        };
      })();
      hntrie_default = HNTrieContainer;
    }
  });

  // src/js/urlskip.ts
  function urlSkip(url, blocked, steps) {
    try {
      let redirectBlocked = false;
      let urlout = url;
      for (const step of steps) {
        const urlin = urlout;
        const c0 = step.charCodeAt(0);
        if (c0 === 35 && step === "#") {
          const pos = urlin.indexOf("#");
          urlout = pos !== -1 ? urlin.slice(pos + 1) : "";
          continue;
        }
        if (c0 === 38) {
          const i = (parseInt(step.slice(1)) || 0) - 1;
          if (i < 0) {
            return;
          }
          const url2 = new URL(urlin);
          if (i >= url2.searchParams.size) {
            return;
          }
          const params = Array.from(url2.searchParams.keys());
          urlout = decodeURIComponent(params[i]);
          continue;
        }
        if (c0 === 43 && step === "+https") {
          const s = urlin.replace(/^https?:\/\//, "");
          if (/^[\w-]:\/\//.test(s)) {
            return;
          }
          urlout = `https://${s}`;
          continue;
        }
        if (c0 === 45) {
          if (step === "-base64") {
            urlout = self.atob(urlin);
            continue;
          }
          if (step === "-safebase64") {
            if (urlSkip.safeBase64Replacer === void 0) {
              urlSkip.safeBase64Map = { "-": "+", "_": "/" };
              urlSkip.safeBase64Replacer = (s) => urlSkip.safeBase64Map[s];
            }
            urlout = urlin.replace(/[-_]/g, urlSkip.safeBase64Replacer);
            urlout = self.atob(urlout);
            continue;
          }
          if (step === "-uricomponent") {
            urlout = decodeURIComponent(urlin);
            continue;
          }
          if (step === "-blocked") {
            redirectBlocked = true;
            continue;
          }
        }
        if (c0 === 47) {
          const re2 = new RegExp(step.slice(1, -1));
          const match = re2.exec(urlin);
          if (match === null) {
            return;
          }
          if (match.length <= 1) {
            return;
          }
          urlout = match[1];
          continue;
        }
        if (c0 === 63) {
          urlout = new URL(urlin).searchParams.get(step.slice(1));
          if (urlout === null) {
            return;
          }
          if (urlout.includes(" ")) {
            urlout = urlout.replace(/ /g, "%20");
          }
          continue;
        }
        return;
      }
      const urlfinal = new URL(urlout);
      if (urlfinal.protocol !== "https:") {
        if (urlfinal.protocol !== "http:") {
          return;
        }
      }
      if (blocked && redirectBlocked !== true) {
        return;
      }
      return urlout;
    } catch (e) {
      console.warn("[uBR] urlskip: URL processing failed", e);
    }
  }
  var init_urlskip = __esm({
    "src/js/urlskip.ts"() {
      urlSkip.safeBase64Map = {};
      urlSkip.safeBase64Replacer = void 0;
    }
  });

  // src/js/static-net-filtering.ts
  function parseRedirectRequestValue(directive) {
    if (directive.cache === null) {
      directive.cache = parseRedirectValue(directive.value);
    }
    return directive.cache;
  }
  function compareRedirectRequests(redirectEngine2, a, b2) {
    const { token: atok, priority: aint, bits: abits } = parseRedirectRequestValue(a);
    if (redirectEngine2.hasToken(atok) === false) {
      return -1;
    }
    const { token: btok, priority: bint, bits: bbits } = parseRedirectRequestValue(b2);
    if (redirectEngine2.hasToken(btok) === false) {
      return 1;
    }
    if (abits !== bbits) {
      if ((abits & IMPORTANT_REALM) !== 0) {
        return 1;
      }
      if ((bbits & IMPORTANT_REALM) !== 0) {
        return -1;
      }
      if ((abits & ALLOW_REALM) !== 0) {
        return -1;
      }
      if ((bbits & ALLOW_REALM) !== 0) {
        return 1;
      }
    }
    return aint - bint;
  }
  function parseQueryPruneValue2(directive) {
    if (directive.cache === null) {
      directive.cache = parseQueryPruneValue(directive.value);
    }
    return directive.cache;
  }
  var keyvalStore, BLOCK_REALM, ALLOW_REALM, IMPORTANT_REALM, BLOCKALLOW_REALM, BLOCKIMPORTANT_REALM, ANYPARTY_REALM, FIRSTPARTY_REALM, THIRDPARTY_REALM, ALLPARTIES_REALM, TYPE_REALM, HEADERS_REALM, REDIRECT_REALM, REMOVEPARAM_REALM, CSP_REALM, PERMISSIONS_REALM, URLTRANSFORM_REALM, REPLACE_REALM, URLSKIP_REALM, MODIFY_REALMS, TYPE_REALM_OFFSET, typeNameToTypeValue, otherTypeBitValue, bitFromType, allNetworkTypesBits, allTypesBits, unsupportedTypeBit, typeValueToTypeName, typeValueToDNRTypeName, MODIFIER_TYPE_REDIRECT, MODIFIER_TYPE_REDIRECTRULE, MODIFIER_TYPE_REMOVEPARAM, MODIFIER_TYPE_CSP, MODIFIER_TYPE_PERMISSIONS, MODIFIER_TYPE_URLTRANSFORM, MODIFIER_TYPE_REPLACE, MODIFIER_TYPE_URLSKIP, modifierBitsFromType, modifierTypeFromName, modifierNameFromType, MAX_TOKEN_LENGTH, NO_TOKEN_HASH, DOT_TOKEN_HASH, ANY_TOKEN_HASH, ANY_HTTPS_TOKEN_HASH, ANY_HTTP_TOKEN_HASH, EMPTY_TOKEN_HASH, INVALID_TOKEN_HASH, $requestMethodBit, $requestTypeValue, $requestURL, $requestURLRaw, $requestHostname, $requestAddress, $docHostname, $docDomain, $tokenBeg, $patternMatchLeft, $patternMatchRight, $isBlockImportant, $docEntity, $requestEntity, $httpHeaders, restrSeparator, reEscape, restrFromPlainPattern, restrFromGenericPattern, LogData, charClassMap, CHAR_CLASS_SEPARATOR, isSeparatorChar, FILTER_DATA_PAGE_SIZE, roundToFilterDataPageSize, filterData, filterDataWritePtr, filterDataGrow, filterDataShrink, filterDataAlloc, filterDataAllocLen, filterSequenceAdd, filterDataReset, filterDataToSelfie, filterDataFromSelfie, filterRefs, filterRefsWritePtr, filterRefAdd, filterRefsReset, filterRefsToSelfie, filterRefsFromSelfie, origHNTrieContainer, destHNTrieContainer, bidiTrieMatchExtra, bidiTrie, filterClasses, filterArgsToUnit, filterClassIdGenerator, registerFilterClass, filterFromCompiled, filterGetClass, filterMatch, filterHasOriginHit, filterGetDomainOpt, filterGetRegexPattern, filterIsBidiTrieable, filterToBidiTrie, filterMatchAndFetchModifiers, filterGetModifierType, filterLogData, filterDumpInfo, dnrRuleFromCompiled, dnrAddRuleError, dnrAddRuleWarning, FilterPatternAny, FilterImportant, FilterPatternPlain, FilterPatternPlain1, FilterPatternPlainX, FilterPatternGeneric, FilterAnchorHnLeft, FilterAnchorHn, FilterAnchorLeft, FilterAnchorRight, FilterTrailingSeparator, FilterRegex, FilterMethod, FilterNotType, DomainOptIterator, domainOptIterator, compileDomainOpt, FilterDomainHit, FilterDomainHitSet, FilterDomainRegexHit, FilterFromDomainHit, FilterFromDomainMiss, FilterFromEntityHit, FilterFromEntityMiss, FilterFromDomainHitSet, FilterFromDomainMissSet, FilterFromRegexHit, FilterFromRegexMiss, fromOptClasses, compileFromDomainOpt, FilterToDomainHit, FilterToDomainMiss, FilterToEntityHit, FilterToEntityMiss, FilterToDomainHitSet, FilterToDomainMissSet, FilterToRegexHit, FilterToRegexMiss, toOptClasses, compileToDomainOpt, FilterDenyAllow, FilterModifier, FilterModifierResult, FilterCollection, FilterDomainHitAny, FilterCompositeAll, FilterHostnameDict, FilterJustOrigin, FilterHTTPSJustOrigin, FilterHTTPJustOrigin, FilterPlainTrie, FilterBucket, FilterBucketIf, FilterBucketIfOriginHits, FilterBucketIfRegexHits, FilterStrictParty, FilterOnHeaders, _FilterOnResponseHeaders, FilterOnResponseHeaders, _FilterOnRequestHeaders, FilterOnRequestHeaders, _FilterIPAddress, FilterIPAddress, FilterMessage, urlTokenizer, FilterCompiler, FROM_BIT, TO_BIT, DENYALLOW_BIT, HEADER_BIT, STRICT_PARTY_BIT, MODIFY_BIT, NOT_TYPE_BIT, IMPORTANT_BIT, METHOD_BIT, IPADDRESS_BIT, MESSAGE_BIT, StaticNetFilteringEngine, staticNetFilteringEngine, static_net_filtering_default;
  var init_static_net_filtering = __esm({
    "src/js/static-net-filtering.ts"() {
      init_static_filtering_parser();
      init_uri_utils();
      init_tasks();
      init_regex_analyzer();
      init_biditrie();
      init_static_filtering_io();
      init_filtering_context();
      init_hntrie();
      init_urlskip();
      keyvalStore = typeof vAPI !== "undefined" ? vAPI.localStorage : { getItem() {
        return null;
      }, setItem() {
      }, removeItem() {
      } };
      BLOCK_REALM = 0;
      ALLOW_REALM = 1;
      IMPORTANT_REALM = 2;
      BLOCKALLOW_REALM = BLOCK_REALM | ALLOW_REALM | IMPORTANT_REALM;
      BLOCKIMPORTANT_REALM = BLOCK_REALM | IMPORTANT_REALM;
      ANYPARTY_REALM = 0;
      FIRSTPARTY_REALM = 8;
      THIRDPARTY_REALM = 16;
      ALLPARTIES_REALM = FIRSTPARTY_REALM | THIRDPARTY_REALM;
      TYPE_REALM = 992;
      HEADERS_REALM = 1024;
      REDIRECT_REALM = 2048;
      REMOVEPARAM_REALM = 4096;
      CSP_REALM = 6144;
      PERMISSIONS_REALM = 8192;
      URLTRANSFORM_REALM = 10240;
      REPLACE_REALM = 12288;
      URLSKIP_REALM = 14336;
      MODIFY_REALMS = 14336;
      TYPE_REALM_OFFSET = 5;
      typeNameToTypeValue = {
        "no_type": 0 << TYPE_REALM_OFFSET,
        "stylesheet": 1 << TYPE_REALM_OFFSET,
        "image": 2 << TYPE_REALM_OFFSET,
        "object": 3 << TYPE_REALM_OFFSET,
        "object_subrequest": 3 << TYPE_REALM_OFFSET,
        "script": 4 << TYPE_REALM_OFFSET,
        "fetch": 5 << TYPE_REALM_OFFSET,
        "xmlhttprequest": 5 << TYPE_REALM_OFFSET,
        "sub_frame": 6 << TYPE_REALM_OFFSET,
        "font": 7 << TYPE_REALM_OFFSET,
        "media": 8 << TYPE_REALM_OFFSET,
        "websocket": 9 << TYPE_REALM_OFFSET,
        "beacon": 10 << TYPE_REALM_OFFSET,
        "ping": 10 << TYPE_REALM_OFFSET,
        "other": 11 << TYPE_REALM_OFFSET,
        "popup": 12 << TYPE_REALM_OFFSET,
        // start of behavioral filtering
        "popunder": 13 << TYPE_REALM_OFFSET,
        "main_frame": 14 << TYPE_REALM_OFFSET,
        // start of 1p behavioral filtering
        "generichide": 15 << TYPE_REALM_OFFSET,
        "specifichide": 16 << TYPE_REALM_OFFSET,
        "inline-font": 17 << TYPE_REALM_OFFSET,
        "inline-script": 18 << TYPE_REALM_OFFSET,
        "cname": 19 << TYPE_REALM_OFFSET,
        "webrtc": 20 << TYPE_REALM_OFFSET,
        "unsupported": 21 << TYPE_REALM_OFFSET
      };
      otherTypeBitValue = typeNameToTypeValue.other;
      bitFromType = (type) => 1 << (typeNameToTypeValue[type] >>> TYPE_REALM_OFFSET) - 1;
      allNetworkTypesBits = (1 << (otherTypeBitValue >>> TYPE_REALM_OFFSET)) - 1;
      allTypesBits = allNetworkTypesBits | 1 << (typeNameToTypeValue["popup"] >>> TYPE_REALM_OFFSET) - 1 | 1 << (typeNameToTypeValue["main_frame"] >>> TYPE_REALM_OFFSET) - 1 | 1 << (typeNameToTypeValue["inline-font"] >>> TYPE_REALM_OFFSET) - 1 | 1 << (typeNameToTypeValue["inline-script"] >>> TYPE_REALM_OFFSET) - 1;
      unsupportedTypeBit = 1 << (typeNameToTypeValue["unsupported"] >>> TYPE_REALM_OFFSET) - 1;
      typeValueToTypeName = [
        "",
        "stylesheet",
        "image",
        "object",
        "script",
        "xhr",
        "frame",
        "font",
        "media",
        "websocket",
        "ping",
        "other",
        "popup",
        "popunder",
        "document",
        "generichide",
        "specifichide",
        "inline-font",
        "inline-script",
        "cname",
        "",
        "",
        "webrtc",
        "unsupported"
      ];
      typeValueToDNRTypeName = [
        "",
        "stylesheet",
        "image",
        "object",
        "script",
        "xmlhttprequest",
        "sub_frame",
        "font",
        "media",
        "websocket",
        "ping",
        "other"
      ];
      MODIFIER_TYPE_REDIRECT = 1;
      MODIFIER_TYPE_REDIRECTRULE = 2;
      MODIFIER_TYPE_REMOVEPARAM = 3;
      MODIFIER_TYPE_CSP = 4;
      MODIFIER_TYPE_PERMISSIONS = 5;
      MODIFIER_TYPE_URLTRANSFORM = 6;
      MODIFIER_TYPE_REPLACE = 7;
      MODIFIER_TYPE_URLSKIP = 8;
      modifierBitsFromType = /* @__PURE__ */ new Map([
        [MODIFIER_TYPE_REDIRECT, REDIRECT_REALM],
        [MODIFIER_TYPE_REDIRECTRULE, REDIRECT_REALM],
        [MODIFIER_TYPE_REMOVEPARAM, REMOVEPARAM_REALM],
        [MODIFIER_TYPE_CSP, CSP_REALM],
        [MODIFIER_TYPE_PERMISSIONS, PERMISSIONS_REALM],
        [MODIFIER_TYPE_URLTRANSFORM, URLTRANSFORM_REALM],
        [MODIFIER_TYPE_REPLACE, REPLACE_REALM],
        [MODIFIER_TYPE_URLSKIP, URLSKIP_REALM]
      ]);
      modifierTypeFromName = /* @__PURE__ */ new Map([
        ["redirect", MODIFIER_TYPE_REDIRECT],
        ["redirect-rule", MODIFIER_TYPE_REDIRECTRULE],
        ["removeparam", MODIFIER_TYPE_REMOVEPARAM],
        ["csp", MODIFIER_TYPE_CSP],
        ["permissions", MODIFIER_TYPE_PERMISSIONS],
        ["uritransform", MODIFIER_TYPE_URLTRANSFORM],
        ["replace", MODIFIER_TYPE_REPLACE],
        ["urlskip", MODIFIER_TYPE_URLSKIP]
      ]);
      modifierNameFromType = /* @__PURE__ */ new Map([
        [MODIFIER_TYPE_REDIRECT, "redirect"],
        [MODIFIER_TYPE_REDIRECTRULE, "redirect-rule"],
        [MODIFIER_TYPE_REMOVEPARAM, "removeparam"],
        [MODIFIER_TYPE_CSP, "csp"],
        [MODIFIER_TYPE_PERMISSIONS, "permissions"],
        [MODIFIER_TYPE_URLTRANSFORM, "uritransform"],
        [MODIFIER_TYPE_REPLACE, "replace"],
        [MODIFIER_TYPE_URLSKIP, "urlskip"]
      ]);
      MAX_TOKEN_LENGTH = 7;
      NO_TOKEN_HASH = 1342177280;
      DOT_TOKEN_HASH = 268435456;
      ANY_TOKEN_HASH = 536870912;
      ANY_HTTPS_TOKEN_HASH = 805306368;
      ANY_HTTP_TOKEN_HASH = 1073741824;
      EMPTY_TOKEN_HASH = 4026531840;
      INVALID_TOKEN_HASH = 4294967295;
      $requestMethodBit = 0;
      $requestTypeValue = 0;
      $requestURL = "";
      $requestURLRaw = "";
      $requestHostname = "";
      $requestAddress = "";
      $docHostname = "";
      $docDomain = "";
      $tokenBeg = 0;
      $patternMatchLeft = 0;
      $patternMatchRight = 0;
      $isBlockImportant = false;
      $docEntity = {
        entity: "",
        last: "",
        compute() {
          if (this.last !== $docHostname) {
            this.last = $docHostname;
            const pos = $docDomain.indexOf(".");
            this.entity = pos !== -1 ? `${$docHostname.slice(0, pos - $docDomain.length)}.*` : "";
          }
          return this.entity;
        }
      };
      $requestEntity = {
        entity: "",
        last: "",
        compute() {
          if (this.last !== $requestHostname) {
            this.last = $requestHostname;
            const requestDomain = domainFromHostname($requestHostname);
            const pos = requestDomain.indexOf(".");
            this.entity = pos !== -1 ? `${$requestHostname.slice(0, pos - requestDomain.length)}.*` : "";
          }
          return this.entity;
        }
      };
      $httpHeaders = {
        init(...headers) {
          this.headers = headers.flat();
          this.parsed.clear();
        },
        reset() {
          this.headers = [];
          this.parsed.clear();
        },
        lookup(name2) {
          let value = this.parsed.get(name2);
          if (value === void 0) {
            const headers = this.headers;
            const i = headers.findIndex((a) => name2 === a.name.toLowerCase());
            value = i !== -1 ? headers[i].value : null;
            this.parsed.set(name2, value);
          }
          return value ?? void 0;
        },
        headers: [],
        parsed: /* @__PURE__ */ new Map()
      };
      restrSeparator = "(?:[^%.0-9a-z_-]|$)";
      reEscape = /[.*+?^${}()|[\]\\]/g;
      restrFromPlainPattern = (s) => s.replace(reEscape, "\\$&");
      restrFromGenericPattern = function(s, anchor = 0) {
        let reStr = s.replace(restrFromGenericPattern.rePlainChars, "\\$&").replace(restrFromGenericPattern.reSeparators, restrSeparator).replace(restrFromGenericPattern.reDanglingAsterisks, "").replace(restrFromGenericPattern.reAsterisks, ".*?");
        if (anchor & 4) {
          reStr = (reStr.startsWith("\\.") ? restrFromGenericPattern.restrHostnameAnchor2 : restrFromGenericPattern.restrHostnameAnchor1) + reStr;
        } else if (anchor & 2) {
          reStr = `^${reStr}`;
        }
        if (anchor & 1) {
          reStr += "$";
        }
        return reStr;
      };
      restrFromGenericPattern.rePlainChars = /[.+?${}()|[\]\\]/g;
      restrFromGenericPattern.reSeparators = /\^/g;
      restrFromGenericPattern.reDanglingAsterisks = /^\*+|\*+$/g;
      restrFromGenericPattern.reAsterisks = /\*+/g;
      restrFromGenericPattern.restrHostnameAnchor1 = "^[^:]+://(?:[^/]+\\.)?";
      restrFromGenericPattern.restrHostnameAnchor2 = "^[^:]+://(?:[^/]+)?";
      LogData = class {
        constructor(categoryBits, tokenHash, iunit) {
          this.result = 0;
          this.source = "static";
          this.tokenHash = tokenHash;
          if (iunit === 0) {
            this.raw = this.regex = "";
            return;
          }
          this.result = (categoryBits & ALLOW_REALM) === 0 ? 1 : 2;
          const pattern = [];
          const regex = [];
          const options = [];
          const denyallow = [];
          const fromDomains = [];
          const toDomains = [];
          const logData = {
            pattern,
            regex,
            denyallow,
            fromDomains,
            toDomains,
            options,
            isRegex: false
          };
          filterLogData(iunit, logData);
          if ((categoryBits & THIRDPARTY_REALM) !== 0) {
            logData.options.unshift("3p");
          } else if ((categoryBits & FIRSTPARTY_REALM) !== 0) {
            logData.options.unshift("1p");
          }
          const type = categoryBits & TYPE_REALM;
          if (type !== 0) {
            logData.options.unshift(typeValueToTypeName[type >>> TYPE_REALM_OFFSET]);
          }
          let raw = logData.pattern.join("");
          if (logData.isRegex === false && raw.charCodeAt(0) === 47 && raw.charCodeAt(raw.length - 1) === 47) {
            raw += "*";
          }
          if ((categoryBits & ALLOW_REALM) !== 0) {
            raw = `@@${raw}`;
          }
          if (denyallow.length !== 0) {
            options.push(`denyallow=${denyallow.join("|")}`);
          }
          if (fromDomains.length !== 0) {
            options.push(`from=${fromDomains.join("|")}`);
          }
          if (toDomains.length !== 0) {
            options.push(`to=${toDomains.join("|")}`);
          }
          if (options.length !== 0) {
            raw += `$${options.join(",")}`;
          }
          this.raw = raw;
          this.regex = logData.regex.join("");
          if (logData.reason) {
            this.reason = logData.reason;
          }
        }
        isUntokenized() {
          return this.tokenHash === NO_TOKEN_HASH;
        }
        isPureHostname() {
          return this.tokenHash === DOT_TOKEN_HASH;
        }
        static requote(s) {
          if (/^\$|^(["'`]).*\1$|,/.test(s) === false) {
            return s;
          }
          if (s.includes("'") === false) {
            return `'${s}'`;
          }
          if (s.includes('"') === false) {
            return `"${s}"`;
          }
          if (s.includes("`") === false) {
            return `\`${s}\``;
          }
          return `'${s.replace(/'/g, "\\'")}'`;
        }
      };
      charClassMap = new Uint32Array(128);
      CHAR_CLASS_SEPARATOR = 1;
      {
        const reSeparators = /[^\w%.-]/;
        for (let i = 0; i < 128; i++) {
          if (reSeparators.test(String.fromCharCode(i))) {
            charClassMap[i] |= CHAR_CLASS_SEPARATOR;
          }
        }
      }
      isSeparatorChar = (c) => (charClassMap[c] & CHAR_CLASS_SEPARATOR) !== 0;
      FILTER_DATA_PAGE_SIZE = 65536;
      roundToFilterDataPageSize = (len) => len + FILTER_DATA_PAGE_SIZE - 1 & ~(FILTER_DATA_PAGE_SIZE - 1);
      filterData = new Int32Array(FILTER_DATA_PAGE_SIZE * 5);
      filterDataWritePtr = 2;
      filterDataGrow = (len) => {
        if (len <= filterData.length) {
          return;
        }
        const newLen = roundToFilterDataPageSize(len);
        const newBuf = new Int32Array(newLen);
        newBuf.set(filterData);
        filterData = newBuf;
      };
      filterDataShrink = () => {
        const newLen = Math.max(
          roundToFilterDataPageSize(filterDataWritePtr),
          FILTER_DATA_PAGE_SIZE
        );
        if (newLen >= filterData.length) {
          return;
        }
        const newBuf = new Int32Array(newLen);
        newBuf.set(filterData.subarray(0, filterDataWritePtr));
        filterData = newBuf;
      };
      filterDataAlloc = (...args) => {
        const len = args.length;
        const idata = filterDataAllocLen(len);
        for (let i = 0; i < len; i++) {
          filterData[idata + i] = args[i];
        }
        return idata;
      };
      filterDataAllocLen = (len) => {
        const idata = filterDataWritePtr;
        filterDataWritePtr += len;
        if (filterDataWritePtr > filterData.length) {
          filterDataGrow(filterDataWritePtr);
        }
        return idata;
      };
      filterSequenceAdd = (a, b2) => {
        const iseq = filterDataAllocLen(2);
        filterData[iseq + 0] = a;
        filterData[iseq + 1] = b2;
        return iseq;
      };
      filterDataReset = () => {
        filterData.fill(0);
        filterDataWritePtr = 2;
      };
      filterDataToSelfie = () => filterData.subarray(0, filterDataWritePtr);
      filterDataFromSelfie = (selfie) => {
        if (selfie instanceof Int32Array === false) {
          return false;
        }
        filterData = selfie;
        filterDataWritePtr = selfie.length;
        return true;
      };
      filterRefs = [null];
      filterRefsWritePtr = 1;
      filterRefAdd = (ref) => {
        const i = filterRefsWritePtr;
        filterRefs[i] = ref;
        filterRefsWritePtr += 1;
        return i;
      };
      filterRefsReset = () => {
        filterRefs.fill(null);
        filterRefsWritePtr = 1;
      };
      filterRefsToSelfie = () => filterRefs.slice(0, filterRefsWritePtr);
      filterRefsFromSelfie = (selfie) => {
        if (Array.isArray(selfie) === false) {
          return false;
        }
        for (let i = 0, n = selfie.length; i < n; i++) {
          filterRefs[i] = selfie[i];
        }
        filterRefsWritePtr = selfie.length;
        return true;
      };
      origHNTrieContainer = new hntrie_default();
      destHNTrieContainer = new hntrie_default();
      bidiTrieMatchExtra = (l, r, ix) => {
        for (; ; ) {
          $patternMatchLeft = l;
          $patternMatchRight = r;
          const iu2 = filterData[ix + 0];
          if (filterMatch(iu2)) {
            return iu2;
          }
          ix = filterData[ix + 1];
          if (ix === 0) {
            break;
          }
        }
        return 0;
      };
      bidiTrie = new biditrie_default(bidiTrieMatchExtra);
      filterClasses = [];
      filterArgsToUnit = /* @__PURE__ */ new Map();
      filterClassIdGenerator = 0;
      registerFilterClass = (fc2) => {
        const fid = filterClassIdGenerator++;
        fc2.fid = fid;
        fc2.fidstr = `${fid}`;
        filterClasses[fid] = fc2;
      };
      filterFromCompiled = (args) => {
        const fc2 = filterClasses[args[0]];
        const keygen = fc2.keyFromArgs;
        if (keygen === void 0) {
          return fc2.fromCompiled(args);
        }
        const key = `${fc2.fidstr} ${keygen(args) || ""}`;
        let idata = filterArgsToUnit.get(key);
        if (idata !== void 0) {
          return idata;
        }
        idata = fc2.fromCompiled(args);
        filterArgsToUnit.set(key, idata);
        return idata;
      };
      filterGetClass = (idata) => {
        return filterClasses[filterData[idata + 0]];
      };
      filterMatch = (idata) => filterClasses[filterData[idata + 0]].match(idata);
      filterHasOriginHit = (idata) => {
        const fc2 = filterClasses[filterData[idata + 0]];
        return fc2.hasOriginHit !== void 0 && fc2.hasOriginHit(idata);
      };
      filterGetDomainOpt = (idata, out) => {
        const fc2 = filterClasses[filterData[idata + 0]];
        if (fc2.getDomainOpt === void 0) {
          return;
        }
        const fromOpt = fc2.getDomainOpt(idata);
        if (out === void 0) {
          return fromOpt;
        }
        out.push(fromOpt);
      };
      filterGetRegexPattern = (idata, out) => {
        const fc2 = filterClasses[filterData[idata + 0]];
        if (fc2.hasRegexPattern === void 0) {
          return;
        }
        const reStr = fc2.getRegexPattern(idata);
        if (out === void 0) {
          return reStr;
        }
        out.push(reStr);
      };
      filterIsBidiTrieable = (idata) => {
        const fc2 = filterClasses[filterData[idata + 0]];
        if (fc2.isBidiTrieable === void 0) {
          return false;
        }
        return fc2.isBidiTrieable(idata) === true;
      };
      filterToBidiTrie = (idata) => {
        const fc2 = filterClasses[filterData[idata + 0]];
        if (fc2.toBidiTrie === void 0) {
          return;
        }
        return fc2.toBidiTrie(idata);
      };
      filterMatchAndFetchModifiers = (idata, env) => {
        const fc2 = filterClasses[filterData[idata + 0]];
        if (fc2.matchAndFetchModifiers === void 0) {
          return;
        }
        return fc2.matchAndFetchModifiers(idata, env);
      };
      filterGetModifierType = (idata) => {
        const fc2 = filterClasses[filterData[idata + 0]];
        if (fc2.getModifierType === void 0) {
          return;
        }
        return fc2.getModifierType(idata);
      };
      filterLogData = (idata, details) => {
        const fc2 = filterClasses[filterData[idata + 0]];
        if (fc2.logData === void 0) {
          return;
        }
        fc2.logData(idata, details);
      };
      filterDumpInfo = (idata) => {
        const fc2 = filterGetClass(idata);
        if (fc2.dumpInfo === void 0) {
          return;
        }
        return fc2.dumpInfo(idata);
      };
      dnrRuleFromCompiled = (args, rule) => {
        const fc2 = filterClasses[args[0]];
        if (fc2.dnrFromCompiled === void 0) {
          return false;
        }
        fc2.dnrFromCompiled(args, rule);
        return true;
      };
      dnrAddRuleError = (rule, msg) => {
        rule._error = rule._error || [];
        rule._error.push(msg);
      };
      dnrAddRuleWarning = (rule, msg) => {
        rule._warning = rule._warning || [];
        rule._warning.push(msg);
      };
      FilterPatternAny = class _FilterPatternAny {
        static match() {
          return true;
        }
        static compile() {
          return [_FilterPatternAny.fid];
        }
        static fromCompiled(args) {
          return filterDataAlloc(args[0]);
        }
        static keyFromArgs() {
        }
        static logData(idata, details) {
          details.pattern.push("*");
          details.regex.push("^");
        }
      };
      registerFilterClass(FilterPatternAny);
      FilterImportant = class _FilterImportant {
        static match() {
          return $isBlockImportant = true;
        }
        static compile() {
          return [_FilterImportant.fid];
        }
        static fromCompiled(args) {
          return filterDataAlloc(args[0]);
        }
        static dnrFromCompiled(args, rule) {
          rule.__important = true;
        }
        static keyFromArgs() {
        }
        static logData(idata, details) {
          details.options.unshift("important");
        }
      };
      registerFilterClass(FilterImportant);
      FilterPatternPlain = class _FilterPatternPlain {
        static isBidiTrieable(idata) {
          return filterData[idata + 2] <= 255;
        }
        static toBidiTrie(idata) {
          return {
            i: filterData[idata + 1],
            n: filterData[idata + 2],
            itok: filterData[idata + 3]
          };
        }
        static match(idata) {
          const left = $tokenBeg;
          const n = filterData[idata + 2];
          if (bidiTrie.startsWith(
            left,
            bidiTrie.getHaystackLen(),
            filterData[idata + 1],
            n
          ) === 0) {
            return false;
          }
          $patternMatchLeft = left;
          $patternMatchRight = left + n;
          return true;
        }
        static compile(details) {
          const { tokenBeg } = details;
          if (tokenBeg === 0) {
            return [_FilterPatternPlain.fid, details.pattern, 0];
          }
          if (tokenBeg === 1) {
            return [FilterPatternPlain1.fid, details.pattern, 1];
          }
          return [FilterPatternPlainX.fid, details.pattern, tokenBeg];
        }
        static fromCompiled(args) {
          const idata = filterDataAllocLen(4);
          filterData[idata + 0] = args[0];
          filterData[idata + 1] = bidiTrie.storeString(args[1]);
          filterData[idata + 2] = args[1].length;
          filterData[idata + 3] = args[2];
          return idata;
        }
        static dnrFromCompiled(args, rule) {
          if (rule.condition === void 0) {
            rule.condition = {};
          } else if (rule.condition.urlFilter !== void 0) {
            dnrAddRuleError(rule, `urlFilter already defined: ${rule.condition.urlFilter}`);
          }
          rule.condition.urlFilter = args[1];
        }
        static logData(idata, details) {
          const s = bidiTrie.extractString(
            filterData[idata + 1],
            filterData[idata + 2]
          );
          details.pattern.push(s);
          details.regex.push(restrFromPlainPattern(s));
          if (/^[0-9a-z%]{1,6}$/i.exec(s.slice(filterData[idata + 3])) !== null) {
            details.regex.push("(?![0-9A-Za-z%])");
          }
        }
        static dumpInfo(idata) {
          const pattern = bidiTrie.extractString(
            filterData[idata + 1],
            filterData[idata + 2]
          );
          return `${pattern} ${filterData[idata + 3]}`;
        }
      };
      FilterPatternPlain.isPatternPlain = true;
      registerFilterClass(FilterPatternPlain);
      FilterPatternPlain1 = class extends FilterPatternPlain {
        static match(idata) {
          const left = $tokenBeg - 1;
          const n = filterData[idata + 2];
          if (bidiTrie.startsWith(
            left,
            bidiTrie.getHaystackLen(),
            filterData[idata + 1],
            n
          ) === 0) {
            return false;
          }
          $patternMatchLeft = left;
          $patternMatchRight = left + n;
          return true;
        }
      };
      registerFilterClass(FilterPatternPlain1);
      FilterPatternPlainX = class extends FilterPatternPlain {
        static match(idata) {
          const left = $tokenBeg - filterData[idata + 3];
          const n = filterData[idata + 2];
          if (bidiTrie.startsWith(
            left,
            bidiTrie.getHaystackLen(),
            filterData[idata + 1],
            n
          ) === 0) {
            return false;
          }
          $patternMatchLeft = left;
          $patternMatchRight = left + n;
          return true;
        }
      };
      registerFilterClass(FilterPatternPlainX);
      FilterPatternGeneric = class _FilterPatternGeneric {
        static hasRegexPattern() {
          return true;
        }
        static getRegexPattern(idata) {
          return restrFromGenericPattern(
            bidiTrie.extractString(
              filterData[idata + 1],
              filterData[idata + 2]
            ),
            filterData[idata + 3]
          );
        }
        static match(idata) {
          const refs = filterRefs[filterData[idata + 4]];
          if (refs.$re === null) {
            refs.$re = new RegExp(this.getRegexPattern(idata));
          }
          return refs.$re.test($requestURL);
        }
        static compile(details) {
          const out = [
            _FilterPatternGeneric.fid,
            details.pattern,
            details.anchor
          ];
          details.anchor = 0;
          return out;
        }
        static fromCompiled(args) {
          const idata = filterDataAllocLen(5);
          filterData[idata + 0] = args[0];
          filterData[idata + 1] = bidiTrie.storeString(args[1]);
          filterData[idata + 2] = args[1].length;
          filterData[idata + 3] = args[2];
          filterData[idata + 4] = filterRefAdd({ $re: null });
          return idata;
        }
        static dnrFromCompiled(args, rule) {
          if (rule.condition === void 0) {
            rule.condition = {};
          } else if (rule.condition.urlFilter !== void 0) {
            dnrAddRuleError(rule, `urlFilter already defined: ${rule.condition.urlFilter}`);
          }
          let pattern = args[1];
          if (args[2] & 4) {
            if (pattern.startsWith(".")) {
              pattern = `*${pattern}`;
            }
            pattern = `||${pattern}`;
          } else if (args[2] & 2) {
            pattern = `|${pattern}`;
          }
          if (args[2] & 1) {
            pattern += "|";
          }
          rule.condition.urlFilter = pattern;
        }
        static keyFromArgs(args) {
          return `${args[1]}	${args[2]}`;
        }
        static logData(idata, details) {
          details.pattern.length = 0;
          const anchor = filterData[idata + 3];
          if ((anchor & 4) !== 0) {
            details.pattern.push("||");
          } else if ((anchor & 2) !== 0) {
            details.pattern.push("|");
          }
          const s = bidiTrie.extractString(
            filterData[idata + 1],
            filterData[idata + 2]
          );
          details.pattern.push(s);
          if ((anchor & 1) !== 0) {
            details.pattern.push("|");
          }
          details.regex.length = 0;
          details.regex.push(restrFromGenericPattern(s, anchor & ~4));
        }
        static dumpInfo(idata) {
          return bidiTrie.extractString(
            filterData[idata + 1],
            filterData[idata + 2]
          );
        }
      };
      FilterPatternGeneric.isSlow = true;
      registerFilterClass(FilterPatternGeneric);
      FilterAnchorHnLeft = class _FilterAnchorHnLeft {
        static match(idata) {
          const len = $requestHostname.length;
          const haystackCodes = bidiTrie.haystack;
          let lastBeg = filterData[idata + 2];
          let lastEnd = filterData[idata + 3];
          if (len !== filterData[idata + 1] || lastBeg === -1 || haystackCodes[lastBeg - 3] !== 58 || haystackCodes[lastBeg - 2] !== 47 || haystackCodes[lastBeg - 1] !== 47) {
            lastBeg = len !== 0 ? haystackCodes.indexOf(58) : -1;
            if (lastBeg !== -1) {
              if (lastBeg >= bidiTrie.getHaystackLen() || haystackCodes[lastBeg + 1] !== 47 || haystackCodes[lastBeg + 2] !== 47) {
                lastBeg = -1;
              }
            }
            if (lastBeg !== -1) {
              lastBeg += 3;
              lastEnd = lastBeg + len;
            } else {
              lastEnd = -1;
            }
            filterData[idata + 1] = len;
            filterData[idata + 2] = lastBeg;
            filterData[idata + 3] = lastEnd;
          }
          const left = $patternMatchLeft;
          return left < lastEnd && (left === lastBeg || left > lastBeg && haystackCodes[left - 1] === 46);
        }
        static compile() {
          return [_FilterAnchorHnLeft.fid];
        }
        static fromCompiled(args) {
          const idata = filterDataAllocLen(4);
          filterData[idata + 0] = args[0];
          filterData[idata + 1] = 0;
          filterData[idata + 2] = -1;
          filterData[idata + 3] = -1;
          return idata;
        }
        static dnrFromCompiled(args, rule) {
          rule.condition.urlFilter = `||${rule.condition.urlFilter}`;
        }
        static keyFromArgs() {
        }
        static logData(idata, details) {
          details.pattern.unshift("||");
        }
      };
      registerFilterClass(FilterAnchorHnLeft);
      FilterAnchorHn = class _FilterAnchorHn extends FilterAnchorHnLeft {
        static match(idata) {
          return super.match(idata) && filterData[idata + 3] === $patternMatchRight;
        }
        static compile() {
          return [_FilterAnchorHn.fid];
        }
        static dnrFromCompiled(args, rule) {
          rule.condition.requestDomains = [rule.condition.urlFilter];
          rule.condition.urlFilter = void 0;
        }
        static keyFromArgs() {
        }
        static logData(idata, details) {
          super.logData(idata, details);
          details.pattern.push("^");
          details.regex.push("\\.?", restrSeparator);
        }
      };
      registerFilterClass(FilterAnchorHn);
      FilterAnchorLeft = class _FilterAnchorLeft {
        static match() {
          return $patternMatchLeft === 0;
        }
        static compile() {
          return [_FilterAnchorLeft.fid];
        }
        static fromCompiled(args) {
          return filterDataAlloc(args[0]);
        }
        static dnrFromCompiled(args, rule) {
          rule.condition.urlFilter = `|${rule.condition.urlFilter}`;
        }
        static keyFromArgs() {
        }
        static logData(idata, details) {
          details.pattern.unshift("|");
          details.regex.unshift("^");
        }
      };
      registerFilterClass(FilterAnchorLeft);
      FilterAnchorRight = class _FilterAnchorRight {
        static match() {
          return $patternMatchRight === $requestURL.length;
        }
        static compile() {
          return [_FilterAnchorRight.fid];
        }
        static fromCompiled(args) {
          return filterDataAlloc(args[0]);
        }
        static dnrFromCompiled(args, rule) {
          rule.condition.urlFilter = `${rule.condition.urlFilter}|`;
        }
        static keyFromArgs() {
        }
        static logData(idata, details) {
          details.pattern.push("|");
          details.regex.push("$");
        }
      };
      registerFilterClass(FilterAnchorRight);
      FilterTrailingSeparator = class _FilterTrailingSeparator {
        static match() {
          if ($patternMatchRight === $requestURL.length) {
            return true;
          }
          if (isSeparatorChar(bidiTrie.haystack[$patternMatchRight])) {
            $patternMatchRight += 1;
            return true;
          }
          return false;
        }
        static compile() {
          return [_FilterTrailingSeparator.fid];
        }
        static fromCompiled(args) {
          return filterDataAlloc(args[0]);
        }
        static dnrFromCompiled(args, rule) {
          rule.condition.urlFilter = `${rule.condition.urlFilter}^`;
        }
        static keyFromArgs() {
        }
        static logData(idata, details) {
          details.pattern.push("^");
          details.regex.push(restrSeparator);
        }
      };
      registerFilterClass(FilterTrailingSeparator);
      FilterRegex = class _FilterRegex {
        static hasRegexPattern() {
          return true;
        }
        static getRegexPattern(idata) {
          return bidiTrie.extractString(
            filterData[idata + 1],
            filterData[idata + 2]
          );
        }
        static match(idata) {
          const refs = filterRefs[filterData[idata + 4]];
          if (refs.$re === null) {
            refs.$re = new RegExp(
              this.getRegexPattern(idata),
              filterData[idata + 3] === 0 ? "i" : ""
            );
          }
          if (refs.$re.test($requestURLRaw) === false) {
            return false;
          }
          return true;
        }
        static compile(details) {
          return [
            _FilterRegex.fid,
            details.pattern,
            details.optionValues.has("match-case") ? 1 : 0
          ];
        }
        static fromCompiled(args) {
          const idata = filterDataAllocLen(5);
          filterData[idata + 0] = args[0];
          filterData[idata + 1] = bidiTrie.storeString(args[1]);
          filterData[idata + 2] = args[1].length;
          filterData[idata + 3] = args[2];
          filterData[idata + 4] = filterRefAdd({ $re: null });
          return idata;
        }
        static dnrFromCompiled(args, rule) {
          if (rule.condition === void 0) {
            rule.condition = {};
          }
          if (isRE2(args[1]) === false) {
            dnrAddRuleError(rule, `regexFilter is not RE2-compatible: ${args[1]}`);
          }
          rule.condition.regexFilter = args[1];
          if (args[2] === 1) {
            rule.condition.isUrlFilterCaseSensitive = true;
          }
        }
        static keyFromArgs(args) {
          return `${args[1]}	${args[2]}`;
        }
        static logData(idata, details) {
          const s = bidiTrie.extractString(
            filterData[idata + 1],
            filterData[idata + 2]
          );
          details.pattern.push("/", s, "/");
          details.regex.push(s);
          details.isRegex = true;
          if (filterData[idata + 3] !== 0) {
            details.options.push("match-case");
          }
        }
        static dumpInfo(idata) {
          return [
            "/",
            bidiTrie.extractString(
              filterData[idata + 1],
              filterData[idata + 2]
            ),
            "/",
            filterData[idata + 3] !== 0 ? " (match-case)" : ""
          ].join("");
        }
      };
      FilterRegex.isSlow = true;
      registerFilterClass(FilterRegex);
      FilterMethod = class _FilterMethod {
        static match(idata) {
          if ($requestMethodBit === 0) {
            return false;
          }
          const methodBits = filterData[idata + 1];
          const notMethodBits = filterData[idata + 2];
          return methodBits !== 0 && ($requestMethodBit & methodBits) !== 0 || notMethodBits !== 0 && ($requestMethodBit & notMethodBits) === 0;
        }
        static compile(details) {
          return [_FilterMethod.fid, details.methodBits, details.notMethodBits];
        }
        static fromCompiled(args) {
          const idata = filterDataAllocLen(3);
          filterData[idata + 0] = args[0];
          filterData[idata + 1] = args[1];
          filterData[idata + 2] = args[2];
          return idata;
        }
        static dnrFromCompiled(args, rule) {
          rule.condition = rule.condition || {};
          const rc2 = rule.condition;
          let methodBits = args[1];
          let notMethodBits = args[2];
          if (methodBits !== 0 && rc2.requestMethods === void 0) {
            rc2.requestMethods = [];
          }
          if (notMethodBits !== 0 && rc2.excludedRequestMethods === void 0) {
            rc2.excludedRequestMethods = [];
          }
          for (let i = 1; methodBits !== 0 || notMethodBits !== 0; i++) {
            const bit = 1 << i;
            const methodName = FilteringContext.getMethodName(bit);
            if ((methodBits & bit) !== 0) {
              methodBits &= ~bit;
              rc2.requestMethods.push(methodName);
            } else if ((notMethodBits & bit) !== 0) {
              notMethodBits &= ~bit;
              rc2.excludedRequestMethods.push(methodName);
            }
          }
        }
        static keyFromArgs(args) {
          return `${args[1]} ${args[2]}`;
        }
        static logData(idata, details) {
          const methods = [];
          let methodBits = filterData[idata + 1];
          let notMethodBits = filterData[idata + 2];
          for (let i = 0; methodBits !== 0 || notMethodBits !== 0; i++) {
            const bit = 1 << i;
            const methodName = FilteringContext.getMethodName(bit);
            if ((methodBits & bit) !== 0) {
              methodBits &= ~bit;
              methods.push(methodName);
            } else if ((notMethodBits & bit) !== 0) {
              notMethodBits &= ~bit;
              methods.push(`~${methodName}`);
            }
          }
          details.options.push(`method=${methods.join("|")}`);
        }
        static dumpInfo(idata) {
          return `0b${filterData[idata + 1].toString(2)} 0b${filterData[idata + 2].toString(2)}`;
        }
      };
      registerFilterClass(FilterMethod);
      FilterNotType = class _FilterNotType {
        static match(idata) {
          return $requestTypeValue !== 0 && (filterData[idata + 1] & 1 << $requestTypeValue - 1) === 0;
        }
        static compile(details) {
          return [_FilterNotType.fid, details.notTypeBits];
        }
        static fromCompiled(args) {
          const idata = filterDataAllocLen(2);
          filterData[idata + 0] = args[0];
          filterData[idata + 1] = args[1];
          return idata;
        }
        static dnrFromCompiled(args, rule) {
          rule.condition = rule.condition || {};
          const rc2 = rule.condition;
          if (rc2.excludedResourceTypes === void 0) {
            rc2.excludedResourceTypes = ["main_frame"];
          }
          let bits = args[1];
          for (let i = 1; bits !== 0 && i < typeValueToDNRTypeName.length; i++) {
            const bit = 1 << i - 1;
            if ((bits & bit) === 0) {
              continue;
            }
            bits &= ~bit;
            const type = typeValueToDNRTypeName[i];
            if (type === void 0) {
              continue;
            }
            if (rc2.excludedResourceTypes.includes(type)) {
              continue;
            }
            rc2.excludedResourceTypes.push(type);
          }
        }
        static keyFromArgs(args) {
          return `${args[1]}`;
        }
        static logData(idata, details) {
          let bits = filterData[idata + 1];
          for (let i = 1; bits !== 0 && i < typeValueToTypeName.length; i++) {
            const bit = 1 << i - 1;
            if ((bits & bit) === 0) {
              continue;
            }
            bits &= ~bit;
            details.options.push(`~${typeValueToTypeName[i]}`);
          }
        }
        static dumpInfo(idata) {
          return `0b${filterData[idata + 1].toString(2)}`;
        }
      };
      registerFilterClass(FilterNotType);
      DomainOptIterator = class {
        constructor(domainOpt) {
          this.reset(domainOpt);
        }
        reset(domainOpt) {
          this.domainOpt = domainOpt;
          this.i = 0;
          this.value = void 0;
          this.done = false;
          return this;
        }
        next() {
          if (this.i === -1) {
            this.domainOpt = "";
            this.value = void 0;
            this.done = true;
            return this;
          }
          const pos = this.domainOpt.indexOf("|", this.i);
          if (pos !== -1) {
            this.value = this.domainOpt.slice(this.i, pos);
            this.i = pos + 1;
          } else {
            this.value = this.domainOpt.slice(this.i);
            this.i = -1;
          }
          return this;
        }
        [Symbol.iterator]() {
          return this;
        }
      };
      domainOptIterator = new DomainOptIterator("");
      compileDomainOpt = (ctors, iterable, prepend, units) => {
        const hostnameHits = [];
        const hostnameMisses = [];
        const entityHits = [];
        const entityMisses = [];
        const regexHits = [];
        const regexMisses = [];
        for (const s of iterable) {
          const len = s.length;
          const beg = len > 1 && s.charCodeAt(0) === 126 ? 1 : 0;
          if (len <= beg) {
            continue;
          }
          if (s.charCodeAt(beg) === 47) {
            if (beg === 0) {
              regexHits.push(s);
              continue;
            }
            regexMisses.push(s.slice(1));
            continue;
          }
          if (s.endsWith(".*") === false) {
            if (beg === 0) {
              hostnameHits.push(s);
              continue;
            }
            hostnameMisses.push(s.slice(1));
            continue;
          }
          if (beg === 0) {
            entityHits.push(s);
            continue;
          }
          entityMisses.push(s.slice(1));
          continue;
        }
        const toTrie = [];
        let trieWhich = 0;
        if (hostnameHits.length > 1) {
          toTrie.push(...hostnameHits);
          hostnameHits.length = 0;
          trieWhich |= 1;
        }
        if (entityHits.length > 1) {
          toTrie.push(...entityHits);
          entityHits.length = 0;
          trieWhich |= 2;
        }
        const compiledHit = [];
        if (toTrie.length !== 0) {
          compiledHit.push(
            ctors[2].compile(toTrie.sort(), trieWhich)
          );
        }
        for (const hn2 of hostnameHits) {
          compiledHit.push(ctors[0].compile(hn2));
        }
        for (const hn2 of entityHits) {
          compiledHit.push(ctors[1].compile(hn2));
        }
        for (const hn2 of regexHits) {
          compiledHit.push(ctors[3].compile(hn2));
        }
        if (compiledHit.length > 1) {
          compiledHit[0] = FilterDomainHitAny.compile(compiledHit.slice());
          compiledHit.length = 1;
        }
        toTrie.length = trieWhich = 0;
        if (hostnameMisses.length > 1) {
          toTrie.push(...hostnameMisses);
          hostnameMisses.length = 0;
          trieWhich |= 1;
        }
        if (entityMisses.length > 1) {
          toTrie.push(...entityMisses);
          entityMisses.length = 0;
          trieWhich |= 2;
        }
        const compiledMiss = [];
        if (toTrie.length !== 0) {
          compiledMiss.push(
            ctors[6].compile(toTrie.sort(), trieWhich)
          );
        }
        for (const hn2 of hostnameMisses) {
          compiledMiss.push(ctors[4].compile(hn2));
        }
        for (const hn2 of entityMisses) {
          compiledMiss.push(ctors[5].compile(hn2));
        }
        for (const hn2 of regexMisses) {
          compiledMiss.push(ctors[7].compile(hn2));
        }
        if (prepend) {
          if (compiledHit.length !== 0) {
            units.unshift(compiledHit[0]);
          }
          if (compiledMiss.length !== 0) {
            units.unshift(...compiledMiss);
          }
        } else {
          if (compiledMiss.length !== 0) {
            units.push(...compiledMiss);
          }
          if (compiledHit.length !== 0) {
            units.push(compiledHit[0]);
          }
        }
      };
      FilterDomainHit = class {
        static getDomainOpt(idata) {
          return this.hntrieContainer.extractHostname(
            filterData[idata + 1],
            filterData[idata + 2]
          );
        }
        static match(idata) {
          return this.hntrieContainer.matchesHostname(
            this.getMatchTarget(),
            filterData[idata + 1],
            filterData[idata + 2]
          );
        }
        static compile(hostname) {
          return [this.fid, hostname];
        }
        static fromCompiled(args) {
          const idata = filterDataAllocLen(3);
          filterData[idata + 0] = args[0];
          filterData[idata + 1] = this.hntrieContainer.storeHostname(args[1]);
          filterData[idata + 2] = args[1].length;
          return idata;
        }
        static dnrFromCompiled(args, rule) {
          rule.condition = rule.condition || {};
          const prop = this.dnrConditionName;
          if (rule.condition[prop] === void 0) {
            rule.condition[prop] = [];
          }
          rule.condition[prop].push(args[1]);
        }
        static dumpInfo(idata) {
          return this.getDomainOpt(idata);
        }
      };
      FilterDomainHitSet = class {
        static getDomainOpt(idata) {
          return this.hntrieContainer.extractDomainOpt(
            filterData[idata + 1],
            filterData[idata + 2]
          );
        }
        static getTrieCount(idata) {
          const itrie = filterData[idata + 4];
          if (itrie === 0) {
            return 0;
          }
          return Array.from(
            this.hntrieContainer.trieIterator(filterData[idata + 4])
          ).length;
        }
        static getLastResult(idata) {
          return filterData[idata + 5];
        }
        static getMatchedHostname(idata) {
          const lastResult = filterData[idata + 5];
          if (lastResult === -1) {
            return "";
          }
          return this.getMatchTarget(lastResult >>> 8).slice(lastResult & 255);
        }
        static match(idata) {
          const refs = filterRefs[filterData[idata + 6]];
          const docHostname = this.getMatchTarget(1);
          if (docHostname === refs.$last) {
            return filterData[idata + 5] !== -1;
          }
          refs.$last = docHostname;
          const which = filterData[idata + 3];
          const itrie = filterData[idata + 4] || this.toTrie(idata);
          if (itrie === 0) {
            return false;
          }
          if ((which & 1) !== 0) {
            const pos = this.hntrieContainer.setNeedle(docHostname).matches(itrie);
            if (pos !== -1) {
              filterData[idata + 5] = 1 << 8 | pos;
              return true;
            }
          }
          if ((which & 2) !== 0) {
            const pos = this.hntrieContainer.setNeedle(this.getMatchTarget(2)).matches(itrie);
            if (pos !== -1) {
              filterData[idata + 5] = 2 << 8 | pos;
              return true;
            }
          }
          filterData[idata + 5] = -1;
          return false;
        }
        static add(idata, hn2) {
          this.hntrieContainer.setNeedle(hn2).add(filterData[idata + 4]);
          filterData[idata + 3] |= hn2.charCodeAt(hn2.length - 1) !== 42 ? 1 : 2;
          filterData[idata + 5] = -1;
          filterRefs[filterData[idata + 6]].$last = "";
        }
        static create(fid = -1) {
          const idata = filterDataAllocLen(7);
          filterData[idata + 0] = fid !== -1 ? fid : this.fid;
          filterData[idata + 1] = 0;
          filterData[idata + 2] = 0;
          filterData[idata + 3] = 0;
          filterData[idata + 4] = this.hntrieContainer.createTrie();
          filterData[idata + 5] = -1;
          filterData[idata + 6] = filterRefAdd({ $last: "" });
          return idata;
        }
        static compile(hostnames, which) {
          const stringified = Array.isArray(hostnames) ? hostnames.join("|") : hostnames;
          return [this.fid, stringified, which];
        }
        static fromCompiled(args) {
          const idata = filterDataAllocLen(7);
          filterData[idata + 0] = args[0];
          filterData[idata + 1] = this.hntrieContainer.storeDomainOpt(args[1]);
          filterData[idata + 2] = args[1].length;
          filterData[idata + 3] = args[2];
          filterData[idata + 4] = 0;
          filterData[idata + 5] = -1;
          filterData[idata + 6] = filterRefAdd({ $last: "" });
          return idata;
        }
        static dnrFromCompiled(args, rule) {
          rule.condition = rule.condition || {};
          const prop = this.dnrConditionName;
          if (rule.condition[prop] === void 0) {
            rule.condition[prop] = [];
          }
          rule.condition[prop].push(...args[1].split("|"));
        }
        static toTrie(idata) {
          if (filterData[idata + 2] === 0) {
            return 0;
          }
          const itrie = filterData[idata + 4] = this.hntrieContainer.createTrieFromStoredDomainOpt(
            filterData[idata + 1],
            filterData[idata + 2]
          );
          return itrie;
        }
        static keyFromArgs(args) {
          return args[1];
        }
        static dumpInfo(idata) {
          return `0b${filterData[idata + 3].toString(2)} ${this.getDomainOpt(idata)}`;
        }
      };
      FilterDomainRegexHit = class {
        static getDomainOpt(idata) {
          const ref = filterRefs[filterData[idata + 1]];
          return ref.restr;
        }
        static match(idata) {
          const ref = filterRefs[filterData[idata + 1]];
          if (ref.$re === null) {
            ref.$re = new RegExp(ref.restr.slice(1, -1));
          }
          return ref.$re.test(this.getMatchTarget());
        }
        static compile(restr) {
          return [this.fid, restr];
        }
        static fromCompiled(args) {
          const idata = filterDataAllocLen(2);
          filterData[idata + 0] = args[0];
          filterData[idata + 1] = filterRefAdd({ restr: args[1], $re: null });
          return idata;
        }
        static dnrFromCompiled(args, rule) {
          rule.condition = rule.condition || {};
          const prop = this.dnrConditionName;
          if (rule.condition[prop] === void 0) {
            rule.condition[prop] = [];
          }
          rule.condition[prop].push(args[1]);
        }
        static dumpInfo(idata) {
          return this.getDomainOpt(idata);
        }
      };
      FilterFromDomainHit = class extends FilterDomainHit {
        static hasOriginHit() {
          return true;
        }
        static getMatchTarget() {
          return $docHostname;
        }
        static get dnrConditionName() {
          return "initiatorDomains";
        }
        static logData(idata, details) {
          details.fromDomains.push(this.getDomainOpt(idata));
        }
      };
      Object.defineProperty(FilterFromDomainHit, "hntrieContainer", {
        value: origHNTrieContainer
      });
      FilterFromDomainMiss = class extends FilterFromDomainHit {
        static hasOriginHit() {
          return false;
        }
        static get dnrConditionName() {
          return "excludedInitiatorDomains";
        }
        static match(idata) {
          return super.match(idata) === false;
        }
        static logData(idata, details) {
          details.fromDomains.push(`~${this.getDomainOpt(idata)}`);
        }
      };
      FilterFromEntityHit = class extends FilterFromDomainHit {
        static getMatchTarget() {
          return $docEntity.compute();
        }
      };
      FilterFromEntityMiss = class extends FilterFromDomainMiss {
        static getMatchTarget() {
          return $docEntity.compute();
        }
      };
      FilterFromDomainHitSet = class extends FilterDomainHitSet {
        static hasOriginHit() {
          return true;
        }
        static getMatchTarget(which) {
          return (which & 1) !== 0 ? $docHostname : $docEntity.compute();
        }
        static get dnrConditionName() {
          return "initiatorDomains";
        }
        static logData(idata, details) {
          details.fromDomains.push(this.getDomainOpt(idata));
        }
      };
      Object.defineProperty(FilterFromDomainHitSet, "hntrieContainer", {
        value: origHNTrieContainer
      });
      FilterFromDomainMissSet = class extends FilterFromDomainHitSet {
        static hasOriginHit() {
          return false;
        }
        static match(idata) {
          return super.match(idata) === false;
        }
        static get dnrConditionName() {
          return "excludedInitiatorDomains";
        }
        static logData(idata, details) {
          details.fromDomains.push(`~${this.getDomainOpt(idata).replace(/\|/g, "|~")}`);
        }
      };
      FilterFromRegexHit = class extends FilterDomainRegexHit {
        static getMatchTarget() {
          return $docHostname;
        }
        static get dnrConditionName() {
          return "initiatorDomains";
        }
        static logData(idata, details) {
          details.fromDomains.push(`${this.getDomainOpt(idata)}`);
        }
      };
      FilterFromRegexMiss = class extends FilterFromRegexHit {
        static match(idata) {
          return super.match(idata) === false;
        }
        static get dnrConditionName() {
          return "excludedInitiatorDomains";
        }
        static logData(idata, details) {
          details.fromDomains.push(`~${this.getDomainOpt(idata)}`);
        }
      };
      registerFilterClass(FilterFromDomainHit);
      registerFilterClass(FilterFromDomainMiss);
      registerFilterClass(FilterFromEntityHit);
      registerFilterClass(FilterFromEntityMiss);
      registerFilterClass(FilterFromDomainHitSet);
      registerFilterClass(FilterFromDomainMissSet);
      registerFilterClass(FilterFromRegexHit);
      registerFilterClass(FilterFromRegexMiss);
      fromOptClasses = [
        FilterFromDomainHit,
        FilterFromEntityHit,
        FilterFromDomainHitSet,
        FilterFromRegexHit,
        FilterFromDomainMiss,
        FilterFromEntityMiss,
        FilterFromDomainMissSet,
        FilterFromRegexMiss
      ];
      compileFromDomainOpt = (...args) => {
        return compileDomainOpt(fromOptClasses, ...args);
      };
      FilterToDomainHit = class extends FilterDomainHit {
        static getMatchTarget() {
          return $requestHostname;
        }
        static get dnrConditionName() {
          return "requestDomains";
        }
        static logData(idata, details) {
          details.toDomains.push(this.getDomainOpt(idata));
        }
      };
      Object.defineProperty(FilterToDomainHit, "hntrieContainer", {
        value: destHNTrieContainer
      });
      FilterToDomainMiss = class extends FilterToDomainHit {
        static get dnrConditionName() {
          return "excludedRequestDomains";
        }
        static match(idata) {
          return super.match(idata) === false;
        }
        static logData(idata, details) {
          details.toDomains.push(`~${this.getDomainOpt(idata)}`);
        }
      };
      FilterToEntityHit = class extends FilterToDomainHit {
        static getMatchTarget() {
          return $requestEntity.compute();
        }
      };
      FilterToEntityMiss = class extends FilterToDomainMiss {
        static getMatchTarget() {
          return $requestEntity.compute();
        }
      };
      FilterToDomainHitSet = class extends FilterDomainHitSet {
        static getMatchTarget(which) {
          return (which & 1) !== 0 ? $requestHostname : $requestEntity.compute();
        }
        static get dnrConditionName() {
          return "requestDomains";
        }
        static logData(idata, details) {
          details.toDomains.push(this.getDomainOpt(idata));
        }
      };
      Object.defineProperty(FilterToDomainHitSet, "hntrieContainer", {
        value: destHNTrieContainer
      });
      FilterToDomainMissSet = class extends FilterToDomainHitSet {
        static match(idata) {
          return super.match(idata) === false;
        }
        static get dnrConditionName() {
          return "excludedRequestDomains";
        }
        static logData(idata, details) {
          details.toDomains.push(`~${this.getDomainOpt(idata).replace(/\|/g, "|~")}`);
        }
      };
      FilterToRegexHit = class extends FilterDomainRegexHit {
        static getMatchTarget() {
          return $requestHostname;
        }
        static get dnrConditionName() {
          return "requestDomains";
        }
        static logData(idata, details) {
          details.toDomains.push(`${this.getDomainOpt(idata)}`);
        }
      };
      FilterToRegexMiss = class extends FilterToRegexHit {
        static match(idata) {
          return super.match(idata) === false;
        }
        static get dnrConditionName() {
          return "excludedRequestDomains";
        }
        static logData(idata, details) {
          details.toDomains.push(`~${this.getDomainOpt(idata)}`);
        }
      };
      registerFilterClass(FilterToDomainHit);
      registerFilterClass(FilterToDomainMiss);
      registerFilterClass(FilterToEntityHit);
      registerFilterClass(FilterToEntityMiss);
      registerFilterClass(FilterToDomainHitSet);
      registerFilterClass(FilterToDomainMissSet);
      registerFilterClass(FilterToRegexHit);
      registerFilterClass(FilterToRegexMiss);
      toOptClasses = [
        FilterToDomainHit,
        FilterToEntityHit,
        FilterToDomainHitSet,
        FilterToRegexHit,
        FilterToDomainMiss,
        FilterToEntityMiss,
        FilterToDomainMissSet,
        FilterToRegexMiss
      ];
      compileToDomainOpt = (...args) => {
        return compileDomainOpt(toOptClasses, ...args);
      };
      FilterDenyAllow = class extends FilterToDomainMissSet {
        static compile(details) {
          return super.compile(details.optionValues.get("denyallow"), 1);
        }
        static logData(idata, details) {
          details.denyallow.push(this.getDomainOpt(idata));
        }
      };
      registerFilterClass(FilterDenyAllow);
      FilterModifier = class _FilterModifier {
        static getModifierType(idata) {
          return filterData[idata + 2];
        }
        static match() {
          return true;
        }
        static matchAndFetchModifiers(idata, env) {
          if (this.getModifierType(idata) !== env.type) {
            return;
          }
          env.results.push(new FilterModifierResult(idata, env));
        }
        static compile(details) {
          return [
            _FilterModifier.fid,
            details.action,
            details.modifyType,
            details.modifyValue || ""
          ];
        }
        static fromCompiled(args) {
          const idata = filterDataAllocLen(4);
          filterData[idata + 0] = args[0];
          filterData[idata + 1] = args[1];
          filterData[idata + 2] = args[2];
          filterData[idata + 3] = filterRefAdd({
            value: args[3],
            $cache: null
          });
          return idata;
        }
        static dnrFromCompiled(args, rule) {
          rule.__modifierAction = args[1];
          rule.__modifierType = modifierNameFromType.get(args[2]);
          rule.__modifierValue = args[3];
        }
        static keyFromArgs(args) {
          return `${args[1]}	${args[2]}	${args[3]}`;
        }
        static logData(idata, details) {
          let opt = modifierNameFromType.get(filterData[idata + 2]);
          const refs = filterRefs[filterData[idata + 3]];
          if (refs.value !== "") {
            opt += `=${LogData.requote(refs.value)}`;
          }
          details.options.push(opt);
        }
        static dumpInfo(idata) {
          const s = modifierNameFromType.get(filterData[idata + 2]);
          const refs = filterRefs[filterData[idata + 3]];
          if (refs.value === "") {
            return s;
          }
          return `${s}=${refs.value}`;
        }
      };
      registerFilterClass(FilterModifier);
      FilterModifierResult = class {
        constructor(imodifierunit, env) {
          this.imodifierunit = imodifierunit;
          this.refs = filterRefs[filterData[imodifierunit + 3]];
          this.ireportedunit = env.iunit;
          this.th = env.th;
          this.bits = env.bits & ~BLOCKALLOW_REALM | filterData[imodifierunit + 1];
        }
        get result() {
          return (this.bits & ALLOW_REALM) === 0 ? 1 : 2;
        }
        get value() {
          return this.refs.value;
        }
        get cache() {
          return this.refs.$cache;
        }
        set cache(a) {
          this.refs.$cache = a;
        }
        logData() {
          const r = new LogData(this.bits, this.th, this.ireportedunit);
          r.result = this.result;
          r.modifier = true;
          return r;
        }
      };
      FilterCollection = class _FilterCollection {
        static getCount(idata) {
          let n = 0;
          this.forEach(idata, () => {
            n += 1;
          });
          return n;
        }
        static forEach(idata, fn2) {
          let i = filterData[idata + 1];
          if (i === 0) {
            return;
          }
          do {
            const iunit = filterData[i + 0];
            const r = fn2(iunit);
            if (r !== void 0) {
              return r;
            }
            i = filterData[i + 1];
          } while (i !== 0);
        }
        static unshift(idata, iunit) {
          filterData[idata + 1] = filterSequenceAdd(iunit, filterData[idata + 1]);
        }
        static shift(idata) {
          filterData[idata + 1] = filterData[filterData[idata + 1] + 1];
        }
        static create(fid = -1) {
          return filterDataAlloc(
            fid !== -1 ? fid : _FilterCollection.fid,
            0
          );
        }
        static compile(fc2, fdata) {
          return [fc2.fid, fdata];
        }
        static fromCompiled(args) {
          const units = args[1];
          const n = units.length;
          let iunit, inext = 0;
          let i = n;
          while (i--) {
            iunit = filterFromCompiled(units[i]);
            inext = filterSequenceAdd(iunit, inext);
          }
          const idata = filterDataAllocLen(2);
          filterData[idata + 0] = args[0];
          filterData[idata + 1] = inext;
          return idata;
        }
        static dnrFromCompiled(args, rule) {
          for (const unit of args[1]) {
            dnrRuleFromCompiled(unit, rule);
          }
        }
        static logData(idata, details) {
          this.forEach(idata, (iunit) => {
            filterLogData(iunit, details);
          });
        }
        static dumpInfo(idata) {
          return this.getCount(idata);
        }
      };
      registerFilterClass(FilterCollection);
      FilterDomainHitAny = class _FilterDomainHitAny extends FilterCollection {
        static getDomainOpt(idata) {
          const domainOpts = [];
          this.forEach(idata, (iunit) => {
            if (filterHasOriginHit(iunit) !== true) {
              return;
            }
            filterGetDomainOpt(iunit, domainOpts);
          });
          return domainOpts.join("|");
        }
        static hasOriginHit(idata) {
          this.forEach(idata, (iunit) => {
            if (filterHasOriginHit(iunit)) {
              return true;
            }
          });
          return false;
        }
        static match(idata) {
          let i = filterData[idata + 1];
          while (i !== 0) {
            if (filterMatch(filterData[i + 0])) {
              return true;
            }
            i = filterData[i + 1];
          }
          return false;
        }
        static compile(fdata) {
          return super.compile(_FilterDomainHitAny, fdata);
        }
        static fromCompiled(args) {
          return super.fromCompiled(args);
        }
      };
      registerFilterClass(FilterDomainHitAny);
      FilterCompositeAll = class _FilterCompositeAll extends FilterCollection {
        // FilterPatternPlain is assumed to be first filter in sequence. This can
        // be revisited if needed.
        static isBidiTrieable(idata) {
          return filterIsBidiTrieable(filterData[filterData[idata + 1] + 0]);
        }
        static toBidiTrie(idata) {
          const iseq = filterData[idata + 1];
          const details = filterToBidiTrie(filterData[iseq + 0]);
          this.shift(idata);
          return details;
        }
        static getDomainOpt(idata) {
          return this.forEach(idata, (iunit) => {
            if (filterHasOriginHit(iunit) !== true) {
              return;
            }
            return filterGetDomainOpt(iunit);
          });
        }
        static hasOriginHit(idata) {
          return this.forEach(idata, (iunit) => {
            if (filterHasOriginHit(iunit) === true) {
              return true;
            }
          }) || false;
        }
        static hasRegexPattern(idata) {
          return this.forEach(idata, (iunit) => {
            const fc2 = filterGetClass(iunit);
            if (fc2.hasRegexPattern === void 0) {
              return;
            }
            if (fc2.hasRegexPattern(iunit) === true) {
              return true;
            }
          }) || false;
        }
        static getRegexPattern(idata) {
          return this.forEach(idata, (iunit) => {
            const fc2 = filterGetClass(iunit);
            if (fc2.getRegexPattern === void 0) {
              return;
            }
            return fc2.getRegexPattern(iunit);
          });
        }
        static match(idata) {
          let i = filterData[idata + 1];
          while (i !== 0) {
            if (filterMatch(filterData[i + 0]) !== true) {
              return false;
            }
            i = filterData[i + 1];
          }
          return true;
        }
        // IMPORTANT: the modifier filter unit is assumed to be ALWAYS the
        // first unit in the sequence. This requirement ensures that we do
        // not have to traverse the sequence to find the modifier filter
        // unit.
        static getModifierType(idata) {
          const iseq = filterData[idata + 1];
          const iunit = filterData[iseq + 0];
          return filterGetModifierType(iunit);
        }
        static matchAndFetchModifiers(idata, env) {
          const iseq = filterData[idata + 1];
          const iunit = filterData[iseq + 0];
          if (filterGetModifierType(iunit) === env.type && this.match(idata)) {
            filterMatchAndFetchModifiers(iunit, env);
          }
        }
        static compile(fdata) {
          return super.compile(_FilterCompositeAll, fdata);
        }
        static fromCompiled(args) {
          return super.fromCompiled(args);
        }
      };
      registerFilterClass(FilterCompositeAll);
      FilterHostnameDict = class _FilterHostnameDict {
        static getCount(idata) {
          const itrie = filterData[idata + 1];
          if (itrie !== 0) {
            return Array.from(destHNTrieContainer.trieIterator(itrie)).length;
          }
          return filterRefs[filterData[idata + 3]].length;
        }
        static match(idata) {
          const itrie = filterData[idata + 1] || this.optimize(idata);
          return (filterData[idata + 2] = destHNTrieContainer.setNeedle($requestHostname).matches(itrie)) !== -1;
        }
        static add(idata, hn2) {
          const itrie = filterData[idata + 1];
          if (itrie === 0) {
            filterRefs[filterData[idata + 3]].push(hn2);
          } else {
            destHNTrieContainer.setNeedle(hn2).add(itrie);
          }
        }
        static optimize(idata) {
          const itrie = filterData[idata + 1];
          if (itrie !== 0) {
            return itrie;
          }
          const hostnames = filterRefs[filterData[idata + 3]];
          filterData[idata + 1] = destHNTrieContainer.createTrieFromIterable(hostnames);
          filterRefs[filterData[idata + 3]] = null;
          return filterData[idata + 1];
        }
        static create() {
          const idata = filterDataAllocLen(4);
          filterData[idata + 0] = _FilterHostnameDict.fid;
          filterData[idata + 1] = 0;
          filterData[idata + 2] = -1;
          filterData[idata + 3] = filterRefAdd([]);
          return idata;
        }
        static logData(idata, details) {
          const hostname = $requestHostname.slice(filterData[idata + 2]);
          details.pattern.push("||", hostname, "^");
          details.regex.push(
            restrFromPlainPattern(hostname),
            "\\.?",
            restrSeparator
          );
        }
        static dumpInfo(idata) {
          return this.getCount(idata);
        }
      };
      registerFilterClass(FilterHostnameDict);
      FilterJustOrigin = class _FilterJustOrigin extends FilterFromDomainHitSet {
        static create(fid = -1) {
          return super.create(fid !== -1 ? fid : _FilterJustOrigin.fid);
        }
        static logPattern(idata, details) {
          details.pattern.push("*");
          details.regex.push("^");
        }
        static logData(idata, details) {
          this.logPattern(idata, details);
          details.fromDomains.push(this.getMatchedHostname(idata));
        }
        static dumpInfo(idata) {
          return this.getTrieCount(idata);
        }
      };
      registerFilterClass(FilterJustOrigin);
      FilterHTTPSJustOrigin = class _FilterHTTPSJustOrigin extends FilterJustOrigin {
        static match(idata) {
          return $requestURL.startsWith("https://") && super.match(idata);
        }
        static create() {
          return super.create(_FilterHTTPSJustOrigin.fid);
        }
        static logPattern(idata, details) {
          details.pattern.push("|https://");
          details.regex.push("^https://");
        }
      };
      registerFilterClass(FilterHTTPSJustOrigin);
      FilterHTTPJustOrigin = class _FilterHTTPJustOrigin extends FilterJustOrigin {
        static match(idata) {
          return $requestURL.startsWith("http://") && super.match(idata);
        }
        static create() {
          return super.create(_FilterHTTPJustOrigin.fid);
        }
        static logPattern(idata, details) {
          details.pattern.push("|http://");
          details.regex.push("^http://");
        }
      };
      registerFilterClass(FilterHTTPJustOrigin);
      FilterPlainTrie = class _FilterPlainTrie {
        static match(idata) {
          if (bidiTrie.matches(filterData[idata + 1], $tokenBeg) !== 0) {
            filterData[idata + 2] = bidiTrie.$iu;
            return true;
          }
          return false;
        }
        static create() {
          const idata = filterDataAllocLen(3);
          filterData[idata + 0] = _FilterPlainTrie.fid;
          filterData[idata + 1] = bidiTrie.createTrie();
          filterData[idata + 2] = 0;
          return idata;
        }
        static addUnitToTrie(idata, iunit) {
          const trieDetails = filterToBidiTrie(iunit);
          const itrie = filterData[idata + 1];
          const id = bidiTrie.add(
            itrie,
            trieDetails.i,
            trieDetails.n,
            trieDetails.itok
          );
          const ix = bidiTrie.getExtra(id);
          if (ix === 1) {
            return;
          }
          const fc2 = filterGetClass(iunit);
          if (fc2.isPatternPlain) {
            bidiTrie.setExtra(id, 1);
            return;
          }
          if (fc2 === FilterCompositeAll && fc2.getCount(iunit) === 1) {
            iunit = filterData[filterData[iunit + 1] + 0];
          }
          bidiTrie.setExtra(id, filterSequenceAdd(iunit, ix));
        }
        static logData(idata, details) {
          const s = $requestURL.slice(bidiTrie.$l, bidiTrie.$r);
          details.pattern.push(s);
          details.regex.push(restrFromPlainPattern(s));
          if (filterData[idata + 2] !== -1) {
            filterLogData(filterData[idata + 2], details);
          }
        }
        static dumpInfo(idata) {
          return `${Array.from(bidiTrie.trieIterator(filterData[idata + 1])).length}`;
        }
      };
      registerFilterClass(FilterPlainTrie);
      FilterBucket = class _FilterBucket extends FilterCollection {
        static getCount(idata) {
          return filterData[idata + 2];
        }
        static forEach(idata, fn2) {
          return super.forEach(filterData[idata + 1], fn2);
        }
        static match(idata) {
          const icollection = filterData[idata + 1];
          let iseq = filterData[icollection + 1];
          while (iseq !== 0) {
            const iunit = filterData[iseq + 0];
            if (filterMatch(iunit)) {
              filterData[idata + 3] = iunit;
              return true;
            }
            iseq = filterData[iseq + 1];
          }
          return false;
        }
        static matchAndFetchModifiers(idata, env) {
          const icollection = filterData[idata + 1];
          let iseq = filterData[icollection + 1];
          while (iseq !== 0) {
            const iunit = filterData[iseq + 0];
            env.iunit = iunit;
            filterMatchAndFetchModifiers(iunit, env);
            iseq = filterData[iseq + 1];
          }
        }
        static unshift(idata, iunit) {
          super.unshift(filterData[idata + 1], iunit);
          filterData[idata + 2] += 1;
        }
        static shift(idata) {
          super.shift(filterData[idata + 1]);
          filterData[idata + 2] -= 1;
        }
        static create() {
          const idata = filterDataAllocLen(4);
          filterData[idata + 0] = _FilterBucket.fid;
          filterData[idata + 1] = FilterCollection.create();
          filterData[idata + 2] = 0;
          filterData[idata + 3] = 0;
          return idata;
        }
        static logData(idata, details) {
          filterLogData(filterData[idata + 3], details);
        }
        static optimize(idata, optimizeBits = 3) {
          if ((optimizeBits & 1) !== 0) {
            if (filterData[idata + 2] >= 3) {
              const iplaintrie = this.optimizePatternTests(idata);
              if (iplaintrie !== 0) {
                const icollection = filterData[idata + 1];
                const i = filterData[icollection + 1];
                if (i === 0) {
                  return iplaintrie;
                }
                this.unshift(idata, iplaintrie);
              }
            }
          }
          if ((optimizeBits & 2) !== 0) {
            if (filterData[idata + 2] >= 5) {
              const ioptimized = this.optimizeMatch(
                idata,
                FilterBucketIfOriginHits,
                5
              );
              if (ioptimized !== 0) {
                const icollection = filterData[idata + 1];
                const i = filterData[icollection + 1];
                if (i === 0) {
                  return ioptimized;
                }
                this.unshift(idata, ioptimized);
              }
            }
            if (filterData[idata + 2] >= 5) {
              const ioptimized = this.optimizeMatch(
                idata,
                FilterBucketIfRegexHits,
                5
              );
              if (ioptimized !== 0) {
                const icollection = filterData[idata + 1];
                const i = filterData[icollection + 1];
                if (i === 0) {
                  return ioptimized;
                }
                this.unshift(idata, ioptimized);
              }
            }
          }
          return 0;
        }
        static optimizePatternTests(idata) {
          const isrccollection = filterData[idata + 1];
          let n = 0;
          let iseq = filterData[isrccollection + 1];
          do {
            if (filterIsBidiTrieable(filterData[iseq + 0])) {
              n += 1;
            }
            iseq = filterData[iseq + 1];
          } while (iseq !== 0 && n < 3);
          if (n < 3) {
            return 0;
          }
          const iplaintrie = FilterPlainTrie.create();
          iseq = filterData[isrccollection + 1];
          let iprev = 0;
          for (; ; ) {
            const iunit = filterData[iseq + 0];
            const inext = filterData[iseq + 1];
            if (filterIsBidiTrieable(iunit)) {
              FilterPlainTrie.addUnitToTrie(iplaintrie, iunit);
              if (iprev !== 0) {
                filterData[iprev + 1] = inext;
              } else {
                filterData[isrccollection + 1] = inext;
              }
              filterData[idata + 2] -= 1;
            } else {
              iprev = iseq;
            }
            if (inext === 0) {
              break;
            }
            iseq = inext;
          }
          return iplaintrie;
        }
        static optimizeMatch(idata, fc2, min) {
          const isrccollection = filterData[idata + 1];
          const candidates = [];
          this.forEach(idata, (iunit) => {
            if (fc2.canCoalesce(iunit) === false) {
              return;
            }
            candidates.push(iunit);
          });
          if (candidates.length < min) {
            return 0;
          }
          const idesbucket = _FilterBucket.create();
          const idescollection = filterData[idesbucket + 1];
          let coalesced;
          let isrcseq = filterData[isrccollection + 1];
          let iprev = 0;
          for (; ; ) {
            const iunit = filterData[isrcseq + 0];
            const inext = filterData[isrcseq + 1];
            if (candidates.includes(iunit)) {
              coalesced = fc2.coalesce(iunit, coalesced);
              filterData[isrcseq + 1] = filterData[idescollection + 1];
              filterData[idescollection + 1] = isrcseq;
              filterData[idesbucket + 2] += 1;
              if (iprev !== 0) {
                filterData[iprev + 1] = inext;
              } else {
                filterData[isrccollection + 1] = inext;
              }
              filterData[idata + 2] -= 1;
            } else {
              iprev = isrcseq;
            }
            if (inext === 0) {
              break;
            }
            isrcseq = inext;
          }
          return fc2.create(coalesced, idesbucket);
        }
        static dumpInfo(idata) {
          return this.getCount(idata);
        }
      };
      registerFilterClass(FilterBucket);
      FilterBucketIf = class extends FilterBucket {
        static getCount(idata) {
          return super.getCount(filterData[idata + 1]);
        }
        static forEach(idata, fn2) {
          return super.forEach(filterData[idata + 1], fn2);
        }
        static match(idata) {
          return this.preTest(idata) && super.match(filterData[idata + 1]);
        }
        static matchAndFetchModifiers(idata, env) {
          if (this.preTest(idata)) {
            super.matchAndFetchModifiers(filterData[idata + 1], env);
          }
        }
        static create(fid, ibucket, itest) {
          const idata = filterDataAllocLen(3);
          filterData[idata + 0] = fid;
          filterData[idata + 1] = ibucket;
          filterData[idata + 2] = itest;
          return idata;
        }
        static logData(idata, details) {
          filterLogData(filterData[idata + 1], details);
        }
      };
      registerFilterClass(FilterBucketIf);
      FilterBucketIfOriginHits = class _FilterBucketIfOriginHits extends FilterBucketIf {
        static preTest(idata) {
          return filterMatch(filterData[idata + 2]);
        }
        static canCoalesce(iunit) {
          return filterHasOriginHit(iunit);
        }
        static coalesce(iunit, coalesced) {
          if (coalesced === void 0) {
            coalesced = /* @__PURE__ */ new Set();
          }
          const domainOpt = filterGetDomainOpt(iunit);
          if (domainOpt.includes("|")) {
            for (const hn2 of domainOptIterator.reset(domainOpt)) {
              coalesced.add(hn2);
            }
          } else {
            coalesced.add(domainOpt);
          }
          return coalesced;
        }
        static create(coalesced, ibucket) {
          const units = [];
          compileFromDomainOpt(coalesced, false, units);
          const ihittest = filterFromCompiled(units[0]);
          const ipretest = super.create(
            _FilterBucketIfOriginHits.fid,
            ibucket,
            ihittest
          );
          return ipretest;
        }
      };
      registerFilterClass(FilterBucketIfOriginHits);
      FilterBucketIfRegexHits = class _FilterBucketIfRegexHits extends FilterBucketIf {
        static preTest(idata) {
          return filterRefs[filterData[idata + 2]].test($requestURLRaw);
        }
        static canCoalesce(iunit) {
          const fc2 = filterGetClass(iunit);
          if (fc2.hasRegexPattern === void 0) {
            return false;
          }
          if (fc2.hasRegexPattern(iunit) !== true) {
            return false;
          }
          return true;
        }
        static coalesce(iunit, coalesced) {
          if (coalesced === void 0) {
            coalesced = /* @__PURE__ */ new Set();
          }
          coalesced.add(filterGetRegexPattern(iunit));
          return coalesced;
        }
        static create(coalesced, ibucket) {
          const reString = Array.from(coalesced).join("|");
          return super.create(
            _FilterBucketIfRegexHits.fid,
            ibucket,
            filterRefAdd(new RegExp(reString, "i"))
          );
        }
        static dumpInfo(idata) {
          return filterRefs[filterData[idata + 2]].source;
        }
      };
      registerFilterClass(FilterBucketIfRegexHits);
      FilterStrictParty = class _FilterStrictParty {
        // TODO: disregard `www.`?
        static match(idata) {
          return $requestHostname === $docHostname === (filterData[idata + 1] === 0);
        }
        static compile(details) {
          return [
            _FilterStrictParty.fid,
            details.strictParty > 0 ? 0 : 1
          ];
        }
        static fromCompiled(args) {
          return filterDataAlloc(
            args[0],
            // fid
            args[1]
          );
        }
        static dnrFromCompiled(args, rule) {
          const partyness = args[1] === 0 ? 1 : 3;
          dnrAddRuleError(rule, `strict${partyness}p not supported`);
        }
        static keyFromArgs(args) {
          return `${args[1]}`;
        }
        static logData(idata, details) {
          details.options.push(
            filterData[idata + 1] === 0 ? "strict1p" : "strict3p"
          );
        }
      };
      registerFilterClass(FilterStrictParty);
      FilterOnHeaders = class {
        static match(idata) {
          const refs = filterRefs[filterData[idata + 1]];
          if (refs.$parsed === null) {
            refs.$parsed = parseHeaderValue(refs.headerOpt);
          }
          const { bad, name: name2, not, value } = refs.$parsed;
          if (bad) {
            return false;
          }
          const headerValue = $httpHeaders.lookup(name2);
          if (headerValue === void 0) {
            return false;
          }
          if (value === "") {
            return true;
          }
          let { re: re2 } = refs.$parsed;
          if (re2 === void 0) {
            re2 = new RegExp(refs.$parsed.reStr, refs.$parsed.reFlags);
            refs.$parsed.re = re2;
          }
          return re2.test(headerValue) !== not;
        }
        static compile(fid, details) {
          const fc2 = filterClasses[fid];
          const parsed = parseHeaderValue(details.optionValues.get(`${fc2.headerRealm}header`));
          let normalized = parsed.name;
          if (parsed.value !== "") {
            normalized += `:${parsed.value}`;
          }
          return [fid, normalized];
        }
        static fromCompiled(args) {
          return filterDataAlloc(
            args[0],
            // fid
            filterRefAdd({
              headerOpt: args[1],
              $parsed: null
            })
          );
        }
        static dnrFromCompiled(fid, args, rule) {
          var _a2;
          const fc2 = filterClasses[fid];
          rule.condition || (rule.condition = {});
          const parsed = parseHeaderValue(args[1]);
          if (parsed.bad !== true) {
            const value = parsed.isRegex ? toHeaderPattern(parsed.reStr) : parsed.value;
            if (value !== void 0) {
              const prop = parsed.not ? `excludedR${fc2.headerRealm.slice(1)}Headers` : `${fc2.headerRealm}Headers`;
              (_a2 = rule.condition)[prop] || (_a2[prop] = []);
              const details = {
                header: parsed.name
              };
              if (value !== "") {
                details.values = [value];
              }
              rule.condition[prop].push(details);
              return;
            }
          }
          dnrAddRuleError(rule, `${fc2.headerRealm}header="${args[1]}" not supported`);
        }
        static logData(fid, idata, details) {
          const fc2 = filterClasses[fid];
          const irefs = filterData[idata + 1];
          const headerOpt = filterRefs[irefs].headerOpt;
          let opt = `${fc2.headerRealm}header`;
          if (headerOpt !== "") {
            opt += `=${LogData.requote(headerOpt)}`;
          }
          details.options.push(opt);
        }
      };
      _FilterOnResponseHeaders = class _FilterOnResponseHeaders extends FilterOnHeaders {
        static compile(details) {
          return super.compile(_FilterOnResponseHeaders.fid, details);
        }
        static dnrFromCompiled(args, rule) {
          super.dnrFromCompiled(_FilterOnResponseHeaders.fid, args, rule);
        }
        static logData(idata, details) {
          super.logData(_FilterOnResponseHeaders.fid, idata, details);
        }
      };
      __publicField(_FilterOnResponseHeaders, "headerRealm", "response");
      FilterOnResponseHeaders = _FilterOnResponseHeaders;
      registerFilterClass(FilterOnResponseHeaders);
      _FilterOnRequestHeaders = class _FilterOnRequestHeaders extends FilterOnHeaders {
        static compile(details) {
          return super.compile(_FilterOnRequestHeaders.fid, details);
        }
        static dnrFromCompiled(args, rule) {
          dnrAddRuleError(rule, `requestheader="${args[1]}" not supported`);
        }
        static logData(idata, details) {
          super.logData(_FilterOnRequestHeaders.fid, idata, details);
        }
      };
      __publicField(_FilterOnRequestHeaders, "headerRealm", "request");
      FilterOnRequestHeaders = _FilterOnRequestHeaders;
      registerFilterClass(FilterOnRequestHeaders);
      _FilterIPAddress = class _FilterIPAddress {
        static match(idata) {
          const ipaddr = $requestAddress;
          if (ipaddr === "") {
            return false;
          }
          const details = filterRefs[filterData[idata + 1]];
          switch (details.$type || this.TYPE_UNKNOWN) {
            case this.TYPE_LAN:
              return this.isLAN(ipaddr);
            case this.TYPE_LOOPBACK:
              return this.isLoopback(ipaddr);
            case this.TYPE_EQUAL:
            case this.TYPE_STARTSWITH:
            case this.TYPE_RE:
              return details.$pattern.test(ipaddr);
            default:
              break;
          }
          const { pattern } = details;
          if (pattern === "lan") {
            details.$type = this.TYPE_LAN;
          } else if (pattern === "loopback") {
            details.$type = this.TYPE_LOOPBACK;
          } else if (pattern.startsWith("/") && pattern.endsWith("/")) {
            details.$type = this.TYPE_RE;
            details.$pattern = new RegExp(pattern.slice(1, -1), "m");
          } else if (pattern.endsWith("*")) {
            details.$type = this.TYPE_STARTSWITH;
            details.$pattern = new RegExp(`^${restrFromPlainPattern(pattern.slice(0, -1))}`, "m");
          } else {
            details.$type = this.TYPE_EQUAL;
            details.$pattern = new RegExp(`^${restrFromPlainPattern(pattern)}$`, "m");
          }
          return this.match(idata);
        }
        // https://github.com/uBlockOrigin/uAssets/blob/master/filters/lan-block.txt
        // https://en.wikipedia.org/wiki/Reserved_IP_addresses
        // `ipaddr` is assumed well-formed
        static isLAN(ipaddr) {
          const c0 = ipaddr.charCodeAt(0);
          if (c0 === 48) {
            return ipaddr.startsWith("0.");
          }
          if (c0 === 49) {
            if (ipaddr.startsWith("10.")) {
              return true;
            }
            if (ipaddr.startsWith("127.")) {
              return true;
            }
            if (ipaddr.startsWith("169.254.")) {
              return true;
            }
            if (ipaddr.startsWith("172.")) {
              const v2 = parseInt(ipaddr.slice(4), 10);
              return v2 >= 16 && v2 <= 31;
            }
            return ipaddr.startsWith("192.168.");
          }
          if (c0 === 58) {
            if (ipaddr.startsWith("::") === false) {
              return false;
            }
            if (ipaddr === "::" || ipaddr === "::1") {
              return true;
            }
            if (ipaddr.startsWith("::ffff:") === false) {
              return false;
            }
            return this.reIPv6IPv4lan.test(ipaddr);
          }
          if (c0 === 102) {
            return this.reIPv6local.test(ipaddr);
          }
          return false;
        }
        static isLoopback(ipaddr) {
          return ipaddr === "127.0.0.1" || ipaddr === "::1";
        }
        static compile(details) {
          return [_FilterIPAddress.fid, details.optionValues.get("ipaddress")];
        }
        static fromCompiled(args) {
          const pattern = args[1];
          const details = { pattern };
          return filterDataAlloc(args[0], filterRefAdd(details));
        }
        static dnrFromCompiled(args, rule) {
          dnrAddRuleError(rule, `"ipaddress=${args[1]}" not supported`);
        }
        static logData(idata, details) {
          const irefs = filterData[idata + 1];
          details.options.push(`ipaddress=${LogData.requote(filterRefs[irefs].pattern)}`);
        }
      };
      __publicField(_FilterIPAddress, "TYPE_UNKNOWN", 0);
      __publicField(_FilterIPAddress, "TYPE_EQUAL", 1);
      __publicField(_FilterIPAddress, "TYPE_STARTSWITH", 2);
      __publicField(_FilterIPAddress, "TYPE_LAN", 3);
      __publicField(_FilterIPAddress, "TYPE_LOOPBACK", 4);
      __publicField(_FilterIPAddress, "TYPE_RE", 5);
      __publicField(_FilterIPAddress, "reIPv6IPv4lan", /^::ffff:(7f\w{2}|a\w{2}|a9fe|c0a8):\w+$/);
      __publicField(_FilterIPAddress, "reIPv6local", /^f[cd]\w{2}:/);
      FilterIPAddress = _FilterIPAddress;
      registerFilterClass(FilterIPAddress);
      FilterMessage = class _FilterMessage {
        static match() {
          return true;
        }
        static compile(details) {
          return [
            _FilterMessage.fid,
            encodeURIComponent(details.optionValues.get("reason"))
          ];
        }
        static fromCompiled(args) {
          const msg = args[1];
          return filterDataAlloc(args[0], bidiTrie.storeString(msg), msg.length);
        }
        static keyFromArgs(args) {
          return `${args[1]}`;
        }
        static logData(idata, details) {
          const reason = decodeURIComponent(
            bidiTrie.extractString(filterData[idata + 1], filterData[idata + 2])
          );
          details.reason = reason;
          details.options.push(`reason=${reason}`);
        }
      };
      registerFilterClass(FilterMessage);
      urlTokenizer = new class {
        constructor() {
          this._chars = "0123456789%abcdefghijklmnopqrstuvwxyz";
          this._validTokenChars = new Uint8Array(128);
          for (let i = 0, n = this._chars.length; i < n; i++) {
            this._validTokenChars[this._chars.charCodeAt(i)] = i + 1;
          }
          this._urlIn = "";
          this._urlOut = "";
          this._tokenized = false;
          this._hasQuery = 0;
          this._tokens = new Uint32Array(bidiTrie.haystack.length + 16);
          this.knownTokens = new Uint8Array(65536);
          this.resetKnownTokens();
        }
        setURL(url) {
          if (url !== this._urlIn) {
            this._urlIn = url;
            this._urlOut = url.toLowerCase();
            this._hasQuery = 0;
            this._tokenized = false;
          }
          return this._urlOut;
        }
        resetKnownTokens() {
          this.knownTokens.fill(0);
          this.addKnownToken(DOT_TOKEN_HASH);
          this.addKnownToken(ANY_TOKEN_HASH);
          this.addKnownToken(ANY_HTTPS_TOKEN_HASH);
          this.addKnownToken(ANY_HTTP_TOKEN_HASH);
          this.addKnownToken(NO_TOKEN_HASH);
        }
        addKnownToken(th2) {
          this.knownTokens[th2 & 65535] = 1;
        }
        // Tokenize on demand.
        getTokens() {
          if (this._tokenized) {
            return this._tokens;
          }
          let i = this._tokenize();
          this._tokens[i + 0] = ANY_TOKEN_HASH;
          this._tokens[i + 1] = 0;
          i += 2;
          if (this._urlOut.startsWith("https://")) {
            this._tokens[i + 0] = ANY_HTTPS_TOKEN_HASH;
            this._tokens[i + 1] = 0;
            i += 2;
          } else if (this._urlOut.startsWith("http://")) {
            this._tokens[i + 0] = ANY_HTTP_TOKEN_HASH;
            this._tokens[i + 1] = 0;
            i += 2;
          }
          this._tokens[i + 0] = NO_TOKEN_HASH;
          this._tokens[i + 1] = 0;
          this._tokens[i + 2] = INVALID_TOKEN_HASH;
          this._tokenized = true;
          return this._tokens;
        }
        hasQuery() {
          if (this._hasQuery === 0) {
            const i = this._urlOut.indexOf("?");
            this._hasQuery = i !== -1 ? i + 1 : -1;
          }
          return this._hasQuery > 0;
        }
        // http://www.cse.yorku.ca/~oz/hash.html#djb2
        tokenHashFromString(s) {
          const l = s.length;
          if (l === 0) {
            return EMPTY_TOKEN_HASH;
          }
          const vtc = this._validTokenChars;
          let th2 = vtc[s.charCodeAt(0)];
          for (let i = 1; i !== 7 && i !== l; i++) {
            th2 = (th2 << 5) + th2 ^ vtc[s.charCodeAt(i)];
          }
          return th2 & 268435455;
        }
        stringFromTokenHash(th2) {
          if (th2 === 0) {
            return "";
          }
          return th2.toString(16);
        }
        toSelfie() {
          return this.knownTokens;
        }
        fromSelfie(selfie) {
          this.knownTokens = selfie;
        }
        // https://github.com/chrisaljoudi/uBlock/issues/1118
        // We limit to a maximum number of tokens.
        _tokenize() {
          const tokens = this._tokens;
          const url = this._urlOut;
          const l = bidiTrie.setHaystackLen(url.length);
          if (l === 0) {
            return 0;
          }
          let j2 = 0;
          let hasq = -1;
          mainLoop: {
            const knownTokens = this.knownTokens;
            const vtc = this._validTokenChars;
            const charCodes = bidiTrie.haystack;
            let i = 0, n = 0, ti = 0, th2 = 0;
            for (; ; ) {
              for (; ; ) {
                if (i === l) {
                  break mainLoop;
                }
                const cc2 = url.charCodeAt(i);
                charCodes[i] = cc2;
                i += 1;
                th2 = vtc[cc2];
                if (th2 !== 0) {
                  break;
                }
                if (cc2 === 63) {
                  hasq = i;
                }
              }
              ti = i - 1;
              n = 1;
              for (; ; ) {
                if (i === l) {
                  break;
                }
                const cc2 = url.charCodeAt(i);
                charCodes[i] = cc2;
                i += 1;
                const v2 = vtc[cc2];
                if (v2 === 0) {
                  if (cc2 === 63) {
                    hasq = i;
                  }
                  break;
                }
                if (n === 7) {
                  continue;
                }
                th2 = (th2 << 5) + th2 ^ v2;
                n += 1;
              }
              if (knownTokens[th2 & 65535] !== 0) {
                tokens[j2 + 0] = th2 & 268435455;
                tokens[j2 + 1] = ti;
                j2 += 2;
              }
            }
          }
          this._hasQuery = hasq;
          return j2;
        }
      }();
      FilterCompiler = class _FilterCompiler {
        constructor(other = void 0) {
          if (other !== void 0) {
            return Object.assign(this, other);
          }
          this.reTokens = /[%0-9A-Za-z]+/g;
          this.optionValues = /* @__PURE__ */ new Map();
          this.tokenIdToNormalizedType = /* @__PURE__ */ new Map([
            [NODE_TYPE_NET_OPTION_NAME_CNAME, bitFromType("cname")],
            [NODE_TYPE_NET_OPTION_NAME_CSS, bitFromType("stylesheet")],
            [NODE_TYPE_NET_OPTION_NAME_DOC, bitFromType("main_frame")],
            [NODE_TYPE_NET_OPTION_NAME_FONT, bitFromType("font")],
            [NODE_TYPE_NET_OPTION_NAME_FRAME, bitFromType("sub_frame")],
            [NODE_TYPE_NET_OPTION_NAME_GENERICBLOCK, bitFromType("unsupported")],
            [NODE_TYPE_NET_OPTION_NAME_GHIDE, bitFromType("generichide")],
            [NODE_TYPE_NET_OPTION_NAME_IMAGE, bitFromType("image")],
            [NODE_TYPE_NET_OPTION_NAME_INLINEFONT, bitFromType("inline-font")],
            [NODE_TYPE_NET_OPTION_NAME_INLINESCRIPT, bitFromType("inline-script")],
            [NODE_TYPE_NET_OPTION_NAME_MEDIA, bitFromType("media")],
            [NODE_TYPE_NET_OPTION_NAME_OBJECT, bitFromType("object")],
            [NODE_TYPE_NET_OPTION_NAME_OTHER, bitFromType("other")],
            [NODE_TYPE_NET_OPTION_NAME_PING, bitFromType("ping")],
            [NODE_TYPE_NET_OPTION_NAME_POPUNDER, bitFromType("popunder")],
            [NODE_TYPE_NET_OPTION_NAME_POPUP, bitFromType("popup")],
            [NODE_TYPE_NET_OPTION_NAME_SCRIPT, bitFromType("script")],
            [NODE_TYPE_NET_OPTION_NAME_SHIDE, bitFromType("specifichide")],
            [NODE_TYPE_NET_OPTION_NAME_XHR, bitFromType("xmlhttprequest")],
            [NODE_TYPE_NET_OPTION_NAME_WEBRTC, bitFromType("unsupported")],
            [NODE_TYPE_NET_OPTION_NAME_WEBSOCKET, bitFromType("websocket")]
          ]);
          this.modifierIdToNormalizedId = /* @__PURE__ */ new Map([
            [NODE_TYPE_NET_OPTION_NAME_CSP, MODIFIER_TYPE_CSP],
            [NODE_TYPE_NET_OPTION_NAME_PERMISSIONS, MODIFIER_TYPE_PERMISSIONS],
            [NODE_TYPE_NET_OPTION_NAME_REDIRECT, MODIFIER_TYPE_REDIRECT],
            [NODE_TYPE_NET_OPTION_NAME_REDIRECTRULE, MODIFIER_TYPE_REDIRECTRULE],
            [NODE_TYPE_NET_OPTION_NAME_REMOVEPARAM, MODIFIER_TYPE_REMOVEPARAM],
            [NODE_TYPE_NET_OPTION_NAME_URLTRANSFORM, MODIFIER_TYPE_URLTRANSFORM],
            [NODE_TYPE_NET_OPTION_NAME_REPLACE, MODIFIER_TYPE_REPLACE],
            [NODE_TYPE_NET_OPTION_NAME_URLSKIP, MODIFIER_TYPE_URLSKIP]
          ]);
          this.badTokens = /* @__PURE__ */ new Map([
            ["https", 123617],
            ["com", 76987],
            ["js", 43620],
            ["www", 33129],
            ["jpg", 32221],
            ["images", 31812],
            ["css", 19715],
            ["png", 19140],
            ["static", 15724],
            ["net", 15239],
            ["de", 13155],
            ["img", 11109],
            ["assets", 10746],
            ["min", 7807],
            ["cdn", 7568],
            ["content", 6900],
            ["wp", 6444],
            ["fonts", 6095],
            ["svg", 5976],
            ["http", 5813],
            ["ssl", 5735],
            ["amazon", 5440],
            ["ru", 5427],
            ["fr", 5199],
            ["facebook", 5178],
            ["en", 5146],
            ["image", 5028],
            ["html", 4837],
            ["media", 4833],
            ["co", 4783],
            ["php", 3972],
            ["2019", 3943],
            ["org", 3924],
            ["jquery", 3531],
            ["02", 3438],
            ["api", 3382],
            ["gif", 3350],
            ["eu", 3322],
            ["prod", 3289],
            ["woff2", 3200],
            ["logo", 3194],
            ["themes", 3107],
            ["icon", 3048],
            ["google", 3026],
            ["v1", 3019],
            ["uploads", 2963],
            ["googleapis", 2860],
            ["v3", 2816],
            ["tv", 2762],
            ["icons", 2748],
            ["core", 2601],
            ["gstatic", 2581],
            ["ac", 2509],
            ["utag", 2466],
            ["id", 2459],
            ["ver", 2448],
            ["rsrc", 2387],
            ["files", 2361],
            ["uk", 2357],
            ["us", 2271],
            ["pl", 2262],
            ["common", 2205],
            ["public", 2076],
            ["01", 2016],
            ["na", 1957],
            ["v2", 1954],
            ["12", 1914],
            ["thumb", 1895],
            ["web", 1853],
            ["ui", 1841],
            ["default", 1825],
            ["main", 1737],
            ["false", 1715],
            ["2018", 1697],
            ["embed", 1639],
            ["player", 1634],
            ["dist", 1599],
            ["woff", 1593],
            ["global", 1593],
            ["json", 1572],
            ["11", 1566],
            ["600", 1559],
            ["app", 1556],
            ["styles", 1533],
            ["plugins", 1526],
            ["274", 1512],
            ["random", 1505],
            ["sites", 1505],
            ["imasdk", 1501],
            ["bridge3", 1501],
            ["news", 1496],
            ["width", 1494],
            ["thumbs", 1485],
            ["ttf", 1470],
            ["ajax", 1463],
            ["user", 1454],
            ["scripts", 1446],
            ["twitter", 1440],
            ["crop", 1431],
            ["new", 1412]
          ]);
          this.reset();
        }
        reset() {
          this.action = BLOCK_REALM;
          this.anchor = 0;
          this.badFilter = false;
          this.error = void 0;
          this.modifyType = void 0;
          this.modifyValue = void 0;
          this.pattern = "";
          this.party = ANYPARTY_REALM;
          this.optionUnitBits = 0;
          this.optionValues.clear();
          this.isPureHostname = false;
          this.isGeneric = false;
          this.isRegex = false;
          this.strictParty = 0;
          this.token = "*";
          this.tokenHash = NO_TOKEN_HASH;
          this.tokenBeg = 0;
          this.typeBits = 0;
          this.notTypeBits = 0;
          this.methodBits = 0;
          this.notMethodBits = 0;
          return this;
        }
        start() {
        }
        finish() {
        }
        clone() {
          return new _FilterCompiler(this);
        }
        normalizeRegexSource(s) {
          try {
            const re2 = new RegExp(s);
            return re2.source;
          } catch (e) {
            console.warn("[uBR] static-net-filtering: normalizeRegexSource failed", e);
          }
          return "";
        }
        processMethodOption(value) {
          for (const method of value.split("|")) {
            if (method.charCodeAt(0) === 126) {
              const bit = FilteringContext.getMethod(method.slice(1)) || 0;
              if (bit === 0) {
                continue;
              }
              this.notMethodBits |= bit;
            } else {
              const bit = FilteringContext.getMethod(method) || 0;
              if (bit === 0) {
                continue;
              }
              this.methodBits |= bit;
            }
          }
          this.methodBits &= ~this.notMethodBits;
        }
        // https://github.com/chrisaljoudi/uBlock/issues/589
        // Be ready to handle multiple negated types
        processTypeOption(id, not) {
          if (id !== -1) {
            const typeBit = this.tokenIdToNormalizedType.get(id);
            if (not) {
              this.notTypeBits |= typeBit;
            } else {
              this.typeBits |= typeBit;
            }
            return;
          }
          if (not) {
            this.notTypeBits |= allTypesBits;
          } else {
            this.typeBits |= allTypesBits;
          }
        }
        processPartyOption(firstParty, not) {
          if (not) {
            firstParty = !firstParty;
          }
          this.party |= firstParty ? FIRSTPARTY_REALM : THIRDPARTY_REALM;
        }
        processHostnameList(iter, out = []) {
          let i = 0;
          for (const { hn: hn2, not, bad } of iter) {
            if (bad) {
              return "";
            }
            out[i] = not ? `~${hn2}` : hn2;
            i += 1;
          }
          out.length = i;
          return i === 1 ? out[0] : out.join("|");
        }
        processModifierOption(modifier, value) {
          if (this.modifyType !== void 0) {
            return false;
          }
          const normalized = this.modifierIdToNormalizedId.get(modifier);
          if (normalized === void 0) {
            return false;
          }
          this.modifyType = normalized;
          this.modifyValue = value || "";
          return true;
        }
        processCspOption(value) {
          this.modifyType = MODIFIER_TYPE_CSP;
          this.modifyValue = value || "";
          this.optionUnitBits |= MODIFY_BIT;
          return true;
        }
        processOptionWithValue(parser, id) {
          switch (id) {
            case NODE_TYPE_NET_OPTION_NAME_CSP:
              if (this.processCspOption(parser.getNetOptionValue(id)) === false) {
                return false;
              }
              break;
            case NODE_TYPE_NET_OPTION_NAME_DENYALLOW: {
              const value = this.processHostnameList(
                parser.getNetFilterDenyallowOptionIterator()
              );
              if (value === "") {
                return false;
              }
              this.optionValues.set("denyallow", value);
              this.optionUnitBits |= DENYALLOW_BIT;
              break;
            }
            case NODE_TYPE_NET_OPTION_NAME_FROM: {
              const iter = parser.getNetFilterFromOptionIterator();
              const list = [];
              const value = this.processHostnameList(iter, list);
              if (value === "") {
                return false;
              }
              this.optionValues.set("from", value);
              this.optionValues.set("fromList", list);
              this.optionUnitBits |= FROM_BIT;
              break;
            }
            case NODE_TYPE_NET_OPTION_NAME_IPADDRESS:
              this.optionValues.set("ipaddress", parser.getNetOptionValue(id) || "");
              this.optionUnitBits |= IPADDRESS_BIT;
              break;
            case NODE_TYPE_NET_OPTION_NAME_METHOD:
              this.processMethodOption(parser.getNetOptionValue(id));
              this.optionUnitBits |= METHOD_BIT;
              break;
            case NODE_TYPE_NET_OPTION_NAME_PERMISSIONS:
            case NODE_TYPE_NET_OPTION_NAME_REDIRECTRULE:
            case NODE_TYPE_NET_OPTION_NAME_REMOVEPARAM:
            case NODE_TYPE_NET_OPTION_NAME_REPLACE:
            case NODE_TYPE_NET_OPTION_NAME_URLSKIP:
            case NODE_TYPE_NET_OPTION_NAME_URLTRANSFORM:
              if (this.processModifierOption(id, parser.getNetOptionValue(id)) === false) {
                return false;
              }
              this.optionUnitBits |= MODIFY_BIT;
              break;
            case NODE_TYPE_NET_OPTION_NAME_REASON:
              this.optionValues.set("reason", parser.getNetOptionValue(id));
              this.optionUnitBits |= MESSAGE_BIT;
              break;
            case NODE_TYPE_NET_OPTION_NAME_REDIRECT: {
              const actualId = this.action === ALLOW_REALM ? NODE_TYPE_NET_OPTION_NAME_REDIRECTRULE : id;
              if (this.processModifierOption(actualId, parser.getNetOptionValue(id)) === false) {
                return false;
              }
              this.optionUnitBits |= MODIFY_BIT;
              break;
            }
            case NODE_TYPE_NET_OPTION_NAME_REQUESTHEADER: {
              this.optionValues.set("requestheader", parser.getNetOptionValue(id) || "");
              this.optionUnitBits |= HEADER_BIT;
              break;
            }
            case NODE_TYPE_NET_OPTION_NAME_RESPONSEHEADER: {
              this.optionValues.set("responseheader", parser.getNetOptionValue(id) || "");
              this.optionUnitBits |= HEADER_BIT;
              break;
            }
            case NODE_TYPE_NET_OPTION_NAME_TO: {
              const iter = parser.getNetFilterToOptionIterator();
              const list = [];
              const value = this.processHostnameList(iter, list);
              if (value === "") {
                return false;
              }
              this.optionValues.set("to", value);
              this.optionValues.set("toList", list);
              this.optionUnitBits |= TO_BIT;
              break;
            }
            default:
              break;
          }
          return true;
        }
        process(parser) {
          this.reset();
          if (parser.hasError()) {
            return this.FILTER_INVALID;
          }
          if (parser.isException()) {
            this.action = ALLOW_REALM;
          }
          if (parser.isLeftHnAnchored()) {
            this.anchor |= 4;
          } else if (parser.isLeftAnchored()) {
            this.anchor |= 2;
          }
          if (parser.isRightAnchored()) {
            this.anchor |= 1;
          }
          this.pattern = parser.getNetPattern();
          if (parser.isHostnamePattern()) {
            this.isPureHostname = true;
          } else if (parser.isGenericPattern()) {
            this.isGeneric = true;
          } else if (parser.isRegexPattern()) {
            this.isRegex = true;
          }
          for (const type of parser.getNodeTypes()) {
            switch (type) {
              case NODE_TYPE_NET_OPTION_NAME_1P:
                this.processPartyOption(true, parser.isNegatedOption(type));
                break;
              case NODE_TYPE_NET_OPTION_NAME_STRICT1P:
                this.strictParty = this.strictParty === -1 ? 0 : 1;
                this.optionUnitBits |= STRICT_PARTY_BIT;
                break;
              case NODE_TYPE_NET_OPTION_NAME_3P:
                this.processPartyOption(false, parser.isNegatedOption(type));
                break;
              case NODE_TYPE_NET_OPTION_NAME_STRICT3P:
                this.strictParty = this.strictParty === 1 ? 0 : -1;
                this.optionUnitBits |= STRICT_PARTY_BIT;
                break;
              case NODE_TYPE_NET_OPTION_NAME_ALL:
                this.processTypeOption(-1);
                break;
              case NODE_TYPE_NET_OPTION_NAME_BADFILTER:
                this.badFilter = true;
                break;
              case NODE_TYPE_NET_OPTION_NAME_CNAME:
              case NODE_TYPE_NET_OPTION_NAME_CSS:
              case NODE_TYPE_NET_OPTION_NAME_DOC:
              case NODE_TYPE_NET_OPTION_NAME_FONT:
              case NODE_TYPE_NET_OPTION_NAME_FRAME:
              case NODE_TYPE_NET_OPTION_NAME_GENERICBLOCK:
              case NODE_TYPE_NET_OPTION_NAME_GHIDE:
              case NODE_TYPE_NET_OPTION_NAME_IMAGE:
              case NODE_TYPE_NET_OPTION_NAME_INLINEFONT:
              case NODE_TYPE_NET_OPTION_NAME_INLINESCRIPT:
              case NODE_TYPE_NET_OPTION_NAME_MEDIA:
              case NODE_TYPE_NET_OPTION_NAME_OBJECT:
              case NODE_TYPE_NET_OPTION_NAME_OTHER:
              case NODE_TYPE_NET_OPTION_NAME_PING:
              case NODE_TYPE_NET_OPTION_NAME_POPUNDER:
              case NODE_TYPE_NET_OPTION_NAME_POPUP:
              case NODE_TYPE_NET_OPTION_NAME_SCRIPT:
              case NODE_TYPE_NET_OPTION_NAME_SHIDE:
              case NODE_TYPE_NET_OPTION_NAME_XHR:
              case NODE_TYPE_NET_OPTION_NAME_WEBRTC:
              case NODE_TYPE_NET_OPTION_NAME_WEBSOCKET:
                this.processTypeOption(type, parser.isNegatedOption(type));
                break;
              case NODE_TYPE_NET_OPTION_NAME_CSP:
              case NODE_TYPE_NET_OPTION_NAME_DENYALLOW:
              case NODE_TYPE_NET_OPTION_NAME_FROM:
              case NODE_TYPE_NET_OPTION_NAME_IPADDRESS:
              case NODE_TYPE_NET_OPTION_NAME_METHOD:
              case NODE_TYPE_NET_OPTION_NAME_PERMISSIONS:
              case NODE_TYPE_NET_OPTION_NAME_REASON:
              case NODE_TYPE_NET_OPTION_NAME_REDIRECT:
              case NODE_TYPE_NET_OPTION_NAME_REDIRECTRULE:
              case NODE_TYPE_NET_OPTION_NAME_REMOVEPARAM:
              case NODE_TYPE_NET_OPTION_NAME_REPLACE:
              case NODE_TYPE_NET_OPTION_NAME_REQUESTHEADER:
              case NODE_TYPE_NET_OPTION_NAME_RESPONSEHEADER:
              case NODE_TYPE_NET_OPTION_NAME_TO:
              case NODE_TYPE_NET_OPTION_NAME_URLSKIP:
              case NODE_TYPE_NET_OPTION_NAME_URLTRANSFORM:
                if (this.processOptionWithValue(parser, type) === false) {
                  return this.FILTER_INVALID;
                }
                break;
              case NODE_TYPE_NET_OPTION_NAME_EHIDE: {
                const not = parser.isNegatedOption(type);
                this.processTypeOption(NODE_TYPE_NET_OPTION_NAME_SHIDE, not);
                this.processTypeOption(NODE_TYPE_NET_OPTION_NAME_GHIDE, not);
                break;
              }
              case NODE_TYPE_NET_OPTION_NAME_EMPTY: {
                const id = this.action === ALLOW_REALM ? NODE_TYPE_NET_OPTION_NAME_REDIRECTRULE : NODE_TYPE_NET_OPTION_NAME_REDIRECT;
                if (this.processModifierOption(id, "empty") === false) {
                  return this.FILTER_INVALID;
                }
                this.optionUnitBits |= MODIFY_BIT;
                break;
              }
              case NODE_TYPE_NET_OPTION_NAME_IMPORTANT:
                this.optionUnitBits |= IMPORTANT_BIT;
                this.action = BLOCKIMPORTANT_REALM;
                break;
              case NODE_TYPE_NET_OPTION_NAME_MATCHCASE:
                this.optionValues.set("match-case", true);
                break;
              case NODE_TYPE_NET_OPTION_NAME_MP4: {
                const id = this.action === ALLOW_REALM ? NODE_TYPE_NET_OPTION_NAME_REDIRECTRULE : NODE_TYPE_NET_OPTION_NAME_REDIRECT;
                if (this.processModifierOption(id, "noopmp4-1s") === false) {
                  return this.FILTER_INVALID;
                }
                this.processTypeOption(NODE_TYPE_NET_OPTION_NAME_MEDIA, false);
                this.optionUnitBits |= MODIFY_BIT;
                break;
              }
              default:
                break;
            }
          }
          if (this.party === ALLPARTIES_REALM) {
            this.party = ANYPARTY_REALM;
          }
          if (this.notTypeBits !== 0) {
            if ((this.typeBits && allNetworkTypesBits) === allNetworkTypesBits) {
              this.typeBits &= ~this.notTypeBits | allNetworkTypesBits;
            } else {
              this.typeBits &= ~this.notTypeBits;
            }
            this.optionUnitBits |= NOT_TYPE_BIT;
          }
          if (this.modifyType === MODIFIER_TYPE_CSP || this.modifyType === MODIFIER_TYPE_PERMISSIONS) {
            if (this.typeBits === 0) {
              this.processTypeOption(NODE_TYPE_NET_OPTION_NAME_DOC, false);
              this.processTypeOption(NODE_TYPE_NET_OPTION_NAME_FRAME, false);
            }
          }
          if (this.typeBits & unsupportedTypeBit) {
            this.typeBits &= ~unsupportedTypeBit;
            if (this.typeBits === 0) {
              return this.FILTER_UNSUPPORTED;
            }
          }
          if (this.isPureHostname && parser.hasOptions() === false) {
            this.anchor |= 4;
            return this.FILTER_OK;
          }
          if (this.isRegex) {
            return this.FILTER_OK;
          }
          if (this.pattern.length > 1024) {
            return this.FILTER_UNSUPPORTED;
          }
          return this.FILTER_OK;
        }
        // Given a string, find a good token. Tokens which are too generic,
        // i.e. very common with a high probability of ending up as a miss,
        // are not good. Avoid if possible. This has a significant positive
        // impact on performance.
        //
        // For pattern-less removeparam filters, try to derive a pattern from
        // the removeparam value.
        makeToken() {
          if (this.pattern === "*") {
            if (this.modifyType !== MODIFIER_TYPE_REMOVEPARAM) {
              return;
            }
            return this.extractTokenFromQuerypruneValue();
          }
          if (this.isRegex) {
            return this.extractTokenFromRegex(this.pattern);
          }
          this.extractTokenFromPattern(this.pattern);
        }
        // Note: a one-char token is better than a documented bad token.
        extractTokenFromPattern(pattern) {
          this.reTokens.lastIndex = 0;
          let bestMatch = null;
          let bestBadness = 2147483647;
          for (; ; ) {
            const match = this.reTokens.exec(pattern);
            if (match === null) {
              break;
            }
            const token = match[0];
            const badness = token.length > 1 ? this.badTokens.get(token) || 0 : 1;
            if (badness >= bestBadness) {
              continue;
            }
            if (match.index > 0) {
              const c = pattern.charCodeAt(match.index - 1);
              if (c === 42) {
                continue;
              }
            }
            if (token.length < MAX_TOKEN_LENGTH) {
              const lastIndex = this.reTokens.lastIndex;
              if (lastIndex < pattern.length) {
                const c = pattern.charCodeAt(lastIndex);
                if (c === 42) {
                  continue;
                }
              }
            }
            bestMatch = match;
            if (badness === 0) {
              break;
            }
            bestBadness = badness;
          }
          if (bestMatch !== null) {
            this.token = bestMatch[0];
            this.tokenHash = urlTokenizer.tokenHashFromString(this.token);
            this.tokenBeg = bestMatch.index;
          }
        }
        // https://github.com/gorhill/uBlock/issues/2781
        //   For efficiency purpose, try to extract a token from a regex-based
        //   filter.
        // https://github.com/uBlockOrigin/uBlock-issues/issues/1145#issuecomment-657036902
        //   Mind `\b` directives: `/\bads\b/` should result in token being `ads`,
        //   not `bads`.
        extractTokenFromRegex(pattern) {
          pattern = tokenizableStrFromRegex(pattern);
          this.reTokens.lastIndex = 0;
          let bestToken;
          let bestBadness = 2147483647;
          for (; ; ) {
            const matches = this.reTokens.exec(pattern);
            if (matches === null) {
              break;
            }
            const { 0: token, index } = matches;
            if (index === 0 || pattern.charAt(index - 1) === "") {
              continue;
            }
            const { lastIndex } = this.reTokens;
            if (token.length < MAX_TOKEN_LENGTH && (lastIndex === pattern.length || pattern.charAt(lastIndex) === "")) {
              continue;
            }
            const badness = token.length > 1 ? this.badTokens.get(token) || 0 : 1;
            if (badness < bestBadness) {
              bestToken = token;
              if (badness === 0) {
                break;
              }
              bestBadness = badness;
            }
          }
          if (bestToken !== void 0) {
            this.token = bestToken.toLowerCase();
            this.tokenHash = urlTokenizer.tokenHashFromString(this.token);
          }
        }
        // https://github.com/uBlockOrigin/uAssets/discussions/14683#discussioncomment-3559284
        //   If the removeparam value is a regex, unescape escaped commas
        extractTokenFromQuerypruneValue() {
          const pattern = this.modifyValue;
          if (pattern === "*" || pattern.charCodeAt(0) === 126) {
            return;
          }
          const match = /^\/(.+)\/i?$/.exec(pattern);
          if (match !== null) {
            return this.extractTokenFromRegex(
              match[1].replace(/(\{\d*)\\,/, "$1,")
            );
          }
          if (pattern.startsWith("|")) {
            return this.extractTokenFromRegex(`\\b${pattern.slice(1)}`);
          }
          this.extractTokenFromPattern(pattern.toLowerCase());
        }
        hasNoOptionUnits() {
          return this.optionUnitBits === 0;
        }
        isJustOrigin() {
          if (this.optionUnitBits !== FROM_BIT) {
            return false;
          }
          if (this.isRegex) {
            return false;
          }
          if (/[/~]/.test(this.optionValues.get("from"))) {
            return false;
          }
          if (this.pattern === "*") {
            return true;
          }
          if (this.anchor !== 2) {
            return false;
          }
          if (/^(?:http[s*]?:(?:\/\/)?)$/.test(this.pattern)) {
            return true;
          }
          return false;
        }
        domainIsEntity(s) {
          const l = s.length;
          return l > 2 && s.charCodeAt(l - 1) === 42 && s.charCodeAt(l - 2) === 46;
        }
        compile(parser, writer) {
          const r = this.process(parser);
          if (r === this.FILTER_INVALID) {
            return false;
          }
          if (r === this.FILTER_UNSUPPORTED) {
            const who = writer.properties.get("name") || "?";
            this.error = `Invalid network filter in ${who}: ${parser.raw}`;
            return false;
          }
          writer.select(
            this.badFilter ? "NETWORK_FILTERS:BAD" : "NETWORK_FILTERS:GOOD"
          );
          if (this.modifyType === MODIFIER_TYPE_REDIRECT) {
            this.modifyType = MODIFIER_TYPE_REDIRECTRULE;
            if (parser.options.toDNR !== true) {
              const parsedBlock = this.clone();
              parsedBlock.modifyType = void 0;
              parsedBlock.optionUnitBits &= ~MODIFY_BIT;
              parsedBlock.compileToFilter(writer);
            }
          }
          this.compileToFilter(writer);
          return true;
        }
        compileToFilter(writer) {
          if (this.isPureHostname && this.hasNoOptionUnits()) {
            this.tokenHash = DOT_TOKEN_HASH;
            this.compileToAtomicFilter(this.pattern, writer);
            return;
          }
          this.makeToken();
          if (this.isJustOrigin()) {
            if (this.pattern === "*" || this.pattern.startsWith("http*")) {
              this.tokenHash = ANY_TOKEN_HASH;
            } else if (this.pattern.startsWith("https")) {
              this.tokenHash = ANY_HTTPS_TOKEN_HASH;
            } else {
              this.tokenHash = ANY_HTTP_TOKEN_HASH;
            }
            for (const hn2 of this.optionValues.get("fromList")) {
              this.compileToAtomicFilter(hn2, writer);
            }
            return;
          }
          const units = [];
          const patternClass = this.compilePattern(units);
          if ((this.anchor & 4) !== 0) {
            if (this.isPureHostname) {
              units.push(FilterAnchorHn.compile());
            } else {
              units.push(FilterAnchorHnLeft.compile());
            }
          } else if ((this.anchor & 2) !== 0) {
            units.push(FilterAnchorLeft.compile());
          }
          if ((this.anchor & 1) !== 0) {
            units.push(FilterAnchorRight.compile());
          }
          if (this.methodBits !== 0 || this.notMethodBits !== 0) {
            units.push(FilterMethod.compile(this));
          }
          if (this.notTypeBits !== 0) {
            units.push(FilterNotType.compile(this));
          }
          if (this.strictParty !== 0) {
            units.push(FilterStrictParty.compile(this));
          }
          if ((this.optionUnitBits & FROM_BIT) !== 0) {
            compileFromDomainOpt(
              this.optionValues.get("fromList"),
              units.length !== 0 && patternClass.isSlow === true,
              units
            );
          }
          if ((this.optionUnitBits & TO_BIT) !== 0) {
            compileToDomainOpt(
              this.optionValues.get("toList"),
              units.length !== 0 && patternClass.isSlow === true,
              units
            );
          }
          if ((this.optionUnitBits & DENYALLOW_BIT) !== 0) {
            units.push(FilterDenyAllow.compile(this));
          }
          if ((this.optionUnitBits & IPADDRESS_BIT) !== 0) {
            units.push(FilterIPAddress.compile(this));
          }
          if ((this.optionUnitBits & HEADER_BIT) !== 0) {
            if (this.optionValues.has("requestheader")) {
              units.push(FilterOnRequestHeaders.compile(this));
            }
            if (this.optionValues.has("responseheader")) {
              units.push(FilterOnResponseHeaders.compile(this));
            }
            this.action |= HEADERS_REALM;
          }
          if ((this.optionUnitBits & MESSAGE_BIT) !== 0) {
            units.push(FilterMessage.compile(this));
          }
          if ((this.optionUnitBits & IMPORTANT_BIT) !== 0) {
            units.push(FilterImportant.compile());
          }
          if (this.modifyType !== void 0) {
            units.unshift(FilterModifier.compile(this));
            this.action = this.action & ~BLOCKALLOW_REALM | modifierBitsFromType.get(this.modifyType);
          }
          const fdata = units.length === 1 ? units[0] : FilterCompositeAll.compile(units);
          this.compileToAtomicFilter(fdata, writer);
          if ((this.optionUnitBits & IPADDRESS_BIT) !== 0) {
            if ((this.action & HEADERS_REALM) !== 0) {
              return;
            }
            this.action |= HEADERS_REALM;
            this.compileToAtomicFilter(fdata, writer);
          }
        }
        compilePattern(units) {
          if (this.isRegex) {
            units.push(FilterRegex.compile(this));
            return FilterRegex;
          }
          if (this.pattern === "*") {
            units.push(FilterPatternAny.compile());
            return FilterPatternAny;
          }
          if (this.tokenHash === NO_TOKEN_HASH) {
            units.push(FilterPatternGeneric.compile(this));
            return FilterPatternGeneric;
          }
          if (this.pattern.includes("*") === false) {
            const caretPos = this.pattern.indexOf("^");
            if (caretPos === -1) {
              units.push(FilterPatternPlain.compile(this));
              return FilterPatternPlain;
            }
            if (caretPos === this.pattern.length - 1) {
              this.pattern = this.pattern.slice(0, -1);
              units.push(FilterPatternPlain.compile(this));
              units.push(FilterTrailingSeparator.compile());
              return FilterPatternPlain;
            }
          }
          units.push(FilterPatternGeneric.compile(this));
          return FilterPatternGeneric;
        }
        compileToAtomicFilter(fdata, writer) {
          const catBits = this.action | this.party;
          let { typeBits } = this;
          if (typeBits === 0) {
            writer.push([catBits, this.tokenHash, fdata]);
            return;
          }
          if ((typeBits & allNetworkTypesBits) === allNetworkTypesBits) {
            writer.push([catBits, this.tokenHash, fdata]);
            typeBits &= ~allNetworkTypesBits;
            if (typeBits === 0) {
              return;
            }
          }
          let bitOffset = 1;
          do {
            if (typeBits & 1) {
              writer.push([
                catBits | bitOffset << TYPE_REALM_OFFSET,
                this.tokenHash,
                fdata
              ]);
            }
            bitOffset += 1;
            typeBits >>>= 1;
          } while (typeBits !== 0);
        }
      };
      FROM_BIT = 1;
      TO_BIT = 2;
      DENYALLOW_BIT = 4;
      HEADER_BIT = 8;
      STRICT_PARTY_BIT = 16;
      MODIFY_BIT = 32;
      NOT_TYPE_BIT = 64;
      IMPORTANT_BIT = 128;
      METHOD_BIT = 256;
      IPADDRESS_BIT = 512;
      MESSAGE_BIT = 1024;
      FilterCompiler.prototype.FILTER_OK = 0;
      FilterCompiler.prototype.FILTER_INVALID = 1;
      FilterCompiler.prototype.FILTER_UNSUPPORTED = 2;
      StaticNetFilteringEngine = function() {
        this.compilerVersion = "10";
        this.selfieVersion = "10";
        this.MAX_TOKEN_LENGTH = MAX_TOKEN_LENGTH;
        this.optimizeTaskId = void 0;
        this.bitsToBucket = /* @__PURE__ */ new Map();
        this.goodFilters = /* @__PURE__ */ new Set();
        this.badFilters = /* @__PURE__ */ new Set();
        this.unitsToOptimize = [];
        this.reset();
      };
      StaticNetFilteringEngine.prototype.prime = function() {
        origHNTrieContainer.reset(
          keyvalStore.getItem("SNFE.origHNTrieContainer.trieDetails")
        );
        destHNTrieContainer.reset(
          keyvalStore.getItem("SNFE.destHNTrieContainer.trieDetails")
        );
      };
      StaticNetFilteringEngine.prototype.reset = function() {
        this.processedFilterCount = 0;
        this.acceptedCount = 0;
        this.discardedCount = 0;
        this.goodFilters.clear();
        this.badFilters.clear();
        this.unitsToOptimize.length = 0;
        this.bitsToBucket.clear();
        urlTokenizer.resetKnownTokens();
        filterDataReset();
        filterRefsReset();
        origHNTrieContainer.reset();
        destHNTrieContainer.reset();
        bidiTrie.reset();
        filterArgsToUnit.clear();
        if (this.optimizeTaskId !== void 0) {
          dropTask(this.optimizeTaskId);
          this.optimizeTaskId = void 0;
        }
        this.notReady = false;
        this.$catBits = 0;
        this.$tokenHash = 0;
        this.$filterUnit = 0;
      };
      StaticNetFilteringEngine.prototype.freeze = function() {
        const unserialize2 = CompiledListReader.unserialize;
        for (const line of this.goodFilters) {
          if (this.badFilters.has(line)) {
            this.discardedCount += 1;
            continue;
          }
          const args = unserialize2(line);
          const bits = args[0];
          const bucket = this.bitsToBucket.get(bits) || /* @__PURE__ */ new Map();
          if (bucket.size === 0) {
            this.bitsToBucket.set(bits, bucket);
          }
          const tokenHash = args[1];
          const fdata = args[2];
          let iunit = bucket.get(tokenHash) || 0;
          if (tokenHash === DOT_TOKEN_HASH) {
            if (iunit === 0) {
              iunit = FilterHostnameDict.create();
              bucket.set(DOT_TOKEN_HASH, iunit);
              this.unitsToOptimize.push({ bits, tokenHash });
            }
            FilterHostnameDict.add(iunit, fdata);
            continue;
          }
          if (tokenHash === ANY_TOKEN_HASH) {
            if (iunit === 0) {
              iunit = FilterJustOrigin.create();
              bucket.set(ANY_TOKEN_HASH, iunit);
            }
            FilterJustOrigin.add(iunit, fdata);
            continue;
          }
          if (tokenHash === ANY_HTTPS_TOKEN_HASH) {
            if (iunit === 0) {
              iunit = FilterHTTPSJustOrigin.create();
              bucket.set(ANY_HTTPS_TOKEN_HASH, iunit);
            }
            FilterHTTPSJustOrigin.add(iunit, fdata);
            continue;
          }
          if (tokenHash === ANY_HTTP_TOKEN_HASH) {
            if (iunit === 0) {
              iunit = FilterHTTPJustOrigin.create();
              bucket.set(ANY_HTTP_TOKEN_HASH, iunit);
            }
            FilterHTTPJustOrigin.add(iunit, fdata);
            continue;
          }
          urlTokenizer.addKnownToken(tokenHash);
          this.addFilterUnit(bits, tokenHash, filterFromCompiled(fdata));
          if ((bits & BLOCKALLOW_REALM) === BLOCKIMPORTANT_REALM) {
            this.addFilterUnit(
              bits & ~IMPORTANT_REALM,
              tokenHash,
              filterFromCompiled(fdata)
            );
          }
        }
        this.badFilters.clear();
        this.goodFilters.clear();
        filterArgsToUnit.clear();
        this.notReady = false;
        if (this.optimizeTaskId !== void 0) {
          return;
        }
        this.optimizeTaskId = queueTask(() => {
          this.optimizeTaskId = void 0;
          this.optimize(30);
        }, 2e3);
      };
      StaticNetFilteringEngine.prototype.dnrFromCompiled = function(op2, context, ...args) {
        var _a2, _b;
        if (op2 === "begin") {
          Object.assign(context, {
            good: /* @__PURE__ */ new Set(),
            bad: new Set(context.bad),
            invalid: /* @__PURE__ */ new Set(),
            filterCount: 0,
            acceptedFilterCount: 0,
            rejectedFilterCount: 0
          });
          return;
        }
        if (op2 === "add") {
          const reader = args[0];
          reader.select("NETWORK_FILTERS:GOOD");
          while (reader.next()) {
            context.filterCount += 1;
            if (context.good.has(reader.line) === false) {
              context.good.add(reader.line);
            }
          }
          reader.select("NETWORK_FILTERS:BAD");
          while (reader.next()) {
            context.bad.add(reader.line);
          }
          return;
        }
        if (op2 !== "end") {
          return;
        }
        const { good, bad } = context;
        const unserialize2 = CompiledListReader.unserialize;
        const buckets = /* @__PURE__ */ new Map();
        for (const line of good) {
          if (bad.has(line)) {
            context.rejectedFilterCount += 1;
            continue;
          }
          context.acceptedFilterCount += 1;
          const args2 = unserialize2(line);
          const bits = args2[0];
          const tokenHash = args2[1];
          const fdata = args2[2];
          if (buckets.has(bits) === false) {
            buckets.set(bits, /* @__PURE__ */ new Map());
          }
          const bucket = buckets.get(bits);
          switch (tokenHash) {
            case DOT_TOKEN_HASH: {
              if (bucket.has(DOT_TOKEN_HASH) === false) {
                bucket.set(DOT_TOKEN_HASH, [{
                  condition: {
                    requestDomains: []
                  }
                }]);
              }
              const rule = bucket.get(DOT_TOKEN_HASH)[0];
              rule.condition.requestDomains.push(fdata);
              break;
            }
            case ANY_TOKEN_HASH: {
              if (bucket.has(ANY_TOKEN_HASH) === false) {
                bucket.set(ANY_TOKEN_HASH, [{
                  condition: {
                    initiatorDomains: []
                  }
                }]);
              }
              const rule = bucket.get(ANY_TOKEN_HASH)[0];
              rule.condition.initiatorDomains.push(fdata);
              break;
            }
            case ANY_HTTPS_TOKEN_HASH: {
              if (bucket.has(ANY_HTTPS_TOKEN_HASH) === false) {
                bucket.set(ANY_HTTPS_TOKEN_HASH, [{
                  condition: {
                    urlFilter: "|https://",
                    initiatorDomains: []
                  }
                }]);
              }
              const rule = bucket.get(ANY_HTTPS_TOKEN_HASH)[0];
              rule.condition.initiatorDomains.push(fdata);
              break;
            }
            case ANY_HTTP_TOKEN_HASH: {
              if (bucket.has(ANY_HTTP_TOKEN_HASH) === false) {
                bucket.set(ANY_HTTP_TOKEN_HASH, [{
                  condition: {
                    urlFilter: "|http://",
                    initiatorDomains: []
                  }
                }]);
              }
              const rule = bucket.get(ANY_HTTP_TOKEN_HASH)[0];
              rule.condition.initiatorDomains.push(fdata);
              break;
            }
            default: {
              if (bucket.has(EMPTY_TOKEN_HASH) === false) {
                bucket.set(EMPTY_TOKEN_HASH, []);
              }
              const rule = {};
              dnrRuleFromCompiled(fdata, rule);
              bucket.get(EMPTY_TOKEN_HASH).push(rule);
              break;
            }
          }
        }
        const realms = /* @__PURE__ */ new Map([
          [BLOCK_REALM, { type: "block", priority: 10 }],
          [ALLOW_REALM, { type: "allow", priority: 30 }],
          [BLOCK_REALM | IMPORTANT_REALM, { type: "block", priority: 10 }],
          [REDIRECT_REALM, { type: "redirect", priority: 11 }],
          [REMOVEPARAM_REALM, { type: "removeparam", priority: 1 }],
          [CSP_REALM, { type: "csp", priority: 1 }],
          [PERMISSIONS_REALM, { type: "permissions", priority: 1 }],
          [URLTRANSFORM_REALM, { type: "uritransform", priority: 1 }],
          [HEADERS_REALM, { type: "block", priority: 10 }],
          [HEADERS_REALM | ALLOW_REALM, { type: "allow", priority: 30 }],
          [HEADERS_REALM | IMPORTANT_REALM, { type: "allow", priority: 10 }],
          [URLSKIP_REALM, { type: "urlskip", priority: 1 }]
        ]);
        const partyness = /* @__PURE__ */ new Map([
          [ANYPARTY_REALM, ""],
          [FIRSTPARTY_REALM, "firstParty"],
          [THIRDPARTY_REALM, "thirdParty"]
        ]);
        const types = /* @__PURE__ */ new Set([
          "no_type",
          "stylesheet",
          "image",
          "object",
          "script",
          "xmlhttprequest",
          "sub_frame",
          "main_frame",
          "font",
          "media",
          "websocket",
          "ping",
          "other",
          "popup"
        ]);
        const ruleset = [];
        const seen = /* @__PURE__ */ new Set();
        for (const [realmBits, realmDetails] of realms) {
          for (const [partyBits, partyName] of partyness) {
            for (const typeName in typeNameToTypeValue) {
              if (types.has(typeName) === false) {
                continue;
              }
              const typeBits = typeNameToTypeValue[typeName];
              const bits = realmBits | partyBits | typeBits;
              const bucket = buckets.get(bits);
              if (bucket === void 0) {
                continue;
              }
              for (const rules of bucket.values()) {
                for (const rule of rules) {
                  rule.action = rule.action || {};
                  rule.action.type = realmDetails.type;
                  if (realmDetails.priority !== 0) {
                    rule.priority = (rule.priority || 0) + realmDetails.priority;
                  }
                  if (partyName !== "") {
                    rule.condition = rule.condition || {};
                    rule.condition.domainType = partyName;
                  }
                  if (typeName !== "no_type") {
                    rule.condition = rule.condition || {};
                    rule.condition.resourceTypes = [typeName];
                  }
                  const hash = JSON.stringify(rule);
                  if (seen.has(hash)) {
                    continue;
                  }
                  seen.add(hash);
                  ruleset.push(rule);
                }
              }
            }
          }
        }
        seen.clear();
        for (const rule of ruleset) {
          if (rule.__important !== true) {
            continue;
          }
          rule.priority ?? (rule.priority = 0);
          rule.priority += 30;
        }
        const generichideExclusions = [];
        const generichideInclusions = [];
        {
          const bucket = buckets.get(ALLOW_REALM | typeNameToTypeValue["generichide"]);
          if (bucket) {
            for (const rules of bucket.values()) {
              for (const rule of rules) {
                if (rule.condition === void 0) {
                  continue;
                }
                if (rule.condition.initiatorDomains) {
                  generichideExclusions.push(...rule.condition.initiatorDomains);
                } else if (rule.condition.requestDomains) {
                  generichideExclusions.push(...rule.condition.requestDomains);
                }
                if (rule.condition.excludedInitiatorDomains) {
                  generichideInclusions.push(...rule.condition.excludedInitiatorDomains);
                } else if (rule.condition.excludedRequestDomains) {
                  generichideInclusions.push(...rule.condition.excludedRequestDomains);
                }
              }
            }
          }
        }
        {
          const bucket = buckets.get(BLOCKIMPORTANT_REALM | typeNameToTypeValue["generichide"]);
          if (bucket) {
            for (const rules of bucket.values()) {
              for (const rule of rules) {
                if (rule.condition === void 0) {
                  continue;
                }
                if (rule.condition.initiatorDomains) {
                  generichideInclusions.push(...rule.condition.initiatorDomains);
                } else if (rule.condition.requestDomains) {
                  generichideInclusions.push(...rule.condition.requestDomains);
                }
              }
            }
          }
        }
        const isUnsupportedDomain = (hn2) => hn2.endsWith(".*") || hn2.startsWith("/");
        for (const rule of ruleset) {
          if (rule.condition === void 0) {
            continue;
          }
          for (const prop of ["Initiator", "Request"]) {
            const hitProp = `${prop.toLowerCase()}Domains`;
            if (Array.isArray(rule.condition[hitProp])) {
              if (rule.condition[hitProp].some((hn2) => isUnsupportedDomain(hn2))) {
                const domains = rule.condition[hitProp].filter(
                  (hn2) => isUnsupportedDomain(hn2) === false
                );
                if (domains.length === 0) {
                  dnrAddRuleError(rule, `Can't salvage rule with unsupported domain= option: ${rule.condition[hitProp].join("|")}`);
                } else {
                  dnrAddRuleWarning(rule, `Salvaged rule by ignoring ${rule.condition[hitProp].length - domains.length} unsupported domain= option: ${rule.condition[hitProp].join("|")}`);
                  rule.condition[hitProp] = domains;
                }
              }
            }
            const missProp = `excluded${prop}Domains`;
            if (Array.isArray(rule.condition[missProp])) {
              if (rule.condition[missProp].some((hn2) => isUnsupportedDomain(hn2))) {
                const domains = rule.condition[missProp].filter(
                  (hn2) => isUnsupportedDomain(hn2) === false
                );
                rule.condition[missProp] = domains.length !== 0 ? domains : void 0;
              }
            }
          }
        }
        for (const rule of ruleset) {
          if (rule.__modifierType === void 0) {
            continue;
          }
          let patchDomainOption = false;
          switch (rule.__modifierType) {
            case "csp":
              rule.action.type = "modifyHeaders";
              rule.action.responseHeaders = [{
                header: "content-security-policy",
                operation: "append",
                value: rule.__modifierValue
              }];
              if (rule.__modifierAction !== ALLOW_REALM) {
                break;
              }
              rule.action.type = "allow";
              rule.action.responseHeaders = void 0;
              break;
            case "permissions":
              rule.action.type = "modifyHeaders";
              rule.action.responseHeaders = [{
                header: "permissions-policy",
                operation: "append",
                value: rule.__modifierValue.split("|").join(", ")
              }];
              patchDomainOption = true;
              if (rule.__modifierAction !== ALLOW_REALM) {
                break;
              }
              rule.action.type = "allow";
              rule.action.responseHeaders = void 0;
              break;
            case "redirect-rule": {
              let token = rule.__modifierValue;
              if (token !== "") {
                const match = /:(\d+)$/.exec(token);
                if (match !== null) {
                  rule.priority += Math.min(parseInt(match[1], 10), 8);
                  token = token.slice(0, match.index);
                }
              }
              const resource = context.extensionPaths.get(token);
              if (rule.__modifierValue !== "" && resource === void 0) {
                dnrAddRuleError(rule, `Unpatchable redirect filter: ${rule.__modifierValue}`);
              }
              if (rule.__modifierAction !== ALLOW_REALM) {
                const extensionPath = resource || token;
                rule.action.type = "redirect";
                rule.action.redirect = { extensionPath };
              } else {
                rule.action.type = "block";
                rule.priority += 10;
              }
              break;
            }
            case "removeparam": {
              rule.action.type = "redirect";
              let paramName = rule.__modifierValue;
              if (paramName === "|") {
                paramName = "";
              }
              if (paramName !== "") {
                rule.action.redirect = {
                  transform: {
                    queryTransform: {
                      removeParams: [paramName]
                    }
                  }
                };
                if (/^~?\/.+\/$/.test(paramName)) {
                  dnrAddRuleError(rule, `Unsupported regex-based removeParam: ${paramName}`);
                }
              } else {
                rule.action.redirect = {
                  transform: {
                    query: ""
                  }
                };
              }
              rule.condition || (rule.condition = {});
              const { condition } = rule;
              if (condition.resourceTypes === void 0) {
                if (condition.excludedResourceTypes === void 0) {
                  condition.resourceTypes = [
                    "image",
                    "main_frame",
                    "sub_frame",
                    "xmlhttprequest"
                  ];
                }
              }
              const { urlFilter } = condition;
              if (urlFilter === void 0) {
                if (condition.regexFilter === void 0) {
                  if (paramName !== "") {
                    condition.urlFilter = `^${paramName}=`;
                  }
                }
              } else if (urlFilter.startsWith("||")) {
                if (urlFilter.toLowerCase().includes(paramName.toLowerCase()) === false) {
                  condition.urlFilter = `${condition.urlFilter}*^${paramName}=`;
                }
              }
              if (rule.__modifierAction !== ALLOW_REALM) {
                break;
              }
              rule.action.type = "allow";
              rule.action.redirect = void 0;
              break;
            }
            case "uritransform": {
              const parsed = parseReplaceByRegexValue(rule.__modifierValue);
              if (parsed.re !== void 0) {
                dnrAddRuleError(rule, `Incompatible with DNR: uritransform=${rule.__modifierValue}`);
                break;
              }
              if (rule.condition.regexFilter === void 0) {
                dnrAddRuleError(rule, `Incompatible with DNR (need regexFilter): uritransform=${rule.__modifierValue}`);
                break;
              }
              rule.action.type = "redirect";
              rule.action.redirect = {
                regexSubstitution: parsed.replacement.replace(/\$(\d+)/g, "\\$1")
              };
              break;
            }
            case "urlskip": {
              let urlFilter = rule.condition?.urlFilter;
              if (urlFilter === void 0) {
                break;
              }
              let anchor = 0;
              if (urlFilter.startsWith("||")) {
                anchor |= 4;
                urlFilter = urlFilter.slice(2);
              } else if (urlFilter.startsWith("|")) {
                anchor |= 2;
                urlFilter = urlFilter.slice(1);
              }
              if (urlFilter.endsWith("|")) {
                anchor |= 1;
                urlFilter = urlFilter.slice(0, -1);
              }
              rule.condition.urlFilter = void 0;
              rule.condition.regexFilter = restrFromGenericPattern(urlFilter, anchor);
              break;
            }
            default:
              dnrAddRuleError(rule, `Unsupported modifier ${rule.__modifierType}`);
              break;
          }
          if (patchDomainOption) {
            const domains = rule.condition.initiatorDomains;
            if (Array.isArray(domains) && domains.length !== 0) {
              (_a2 = rule.condition).requestDomains || (_a2.requestDomains = []);
              rule.condition.requestDomains.push(...domains);
              rule.condition.initiatorDomains = void 0;
            }
            const notDomains = rule.condition.excludedInitiatorDomains;
            if (Array.isArray(notDomains) && notDomains.length !== 0) {
              (_b = rule.condition).excludedRequestDomains || (_b.excludedRequestDomains = []);
              rule.condition.excludedRequestDomains.push(...notDomains);
              rule.condition.excludedInitiatorDomains = void 0;
            }
          }
        }
        for (const rule of ruleset) {
          if (rule.priority > 1) {
            continue;
          }
          rule.priority = void 0;
        }
        return {
          ruleset,
          filterCount: context.filterCount,
          acceptedFilterCount: context.acceptedFilterCount,
          rejectedFilterCount: context.rejectedFilterCount,
          generichideExclusions: Array.from(new Set(generichideExclusions)),
          generichideInclusions: Array.from(new Set(generichideInclusions))
        };
      };
      StaticNetFilteringEngine.prototype.addFilterUnit = function(bits, tokenHash, inewunit) {
        const bucket = this.bitsToBucket.get(bits) || /* @__PURE__ */ new Map();
        if (bucket.size === 0) {
          this.bitsToBucket.set(bits, bucket);
        }
        const istoredunit = bucket.get(tokenHash) || 0;
        if (istoredunit === 0) {
          bucket.set(tokenHash, inewunit);
          return;
        }
        if (filterData[istoredunit + 0] === FilterBucket.fid) {
          FilterBucket.unshift(istoredunit, inewunit);
          return;
        }
        const ibucketunit = FilterBucket.create();
        FilterBucket.unshift(ibucketunit, istoredunit);
        FilterBucket.unshift(ibucketunit, inewunit);
        bucket.set(tokenHash, ibucketunit);
        this.unitsToOptimize.push({ bits, tokenHash });
      };
      StaticNetFilteringEngine.prototype.optimize = function(throttle = 0) {
        if (this.optimizeTaskId !== void 0) {
          dropTask(this.optimizeTaskId);
          this.optimizeTaskId = void 0;
        }
        const later = (throttle2) => {
          this.optimizeTaskId = queueTask(() => {
            this.optimizeTaskId = void 0;
            this.optimize(throttle2);
          }, 1e3);
        };
        const t0 = Date.now();
        while (this.unitsToOptimize.length !== 0) {
          const { bits, tokenHash } = this.unitsToOptimize.pop();
          const bucket = this.bitsToBucket.get(bits);
          const iunit = bucket.get(tokenHash);
          const fc2 = filterGetClass(iunit);
          switch (fc2) {
            case FilterHostnameDict:
              FilterHostnameDict.optimize(iunit);
              break;
            case FilterBucket: {
              const optimizeBits = tokenHash === NO_TOKEN_HASH || (bits & MODIFY_REALMS) !== 0 ? 2 : 1;
              const inewunit = FilterBucket.optimize(iunit, optimizeBits);
              if (inewunit !== 0) {
                bucket.set(tokenHash, inewunit);
              }
              break;
            }
            default:
              break;
          }
          if (throttle > 0 && Date.now() - t0 > 40) {
            return later(throttle - 1);
          }
        }
        filterArgsToUnit.clear();
        keyvalStore.setItem(
          "SNFE.destHNTrieContainer.trieDetails",
          destHNTrieContainer.optimize()
        );
        filterDataShrink();
      };
      StaticNetFilteringEngine.prototype.toSelfie = function() {
        this.optimize(0);
        bidiTrie.optimize();
        keyvalStore.setItem(
          "SNFE.origHNTrieContainer.trieDetails",
          origHNTrieContainer.optimize()
        );
        return {
          version: this.selfieVersion,
          processedFilterCount: this.processedFilterCount,
          acceptedCount: this.acceptedCount,
          discardedCount: this.discardedCount,
          bitsToBucket: this.bitsToBucket,
          urlTokenizer: urlTokenizer.toSelfie(),
          destHNTrieContainer: destHNTrieContainer.toSelfie(),
          origHNTrieContainer: origHNTrieContainer.toSelfie(),
          bidiTrie: bidiTrie.toSelfie(),
          filterData: filterDataToSelfie(),
          filterRefs: filterRefsToSelfie()
        };
      };
      StaticNetFilteringEngine.prototype.serialize = function() {
        return this.toSelfie();
      };
      StaticNetFilteringEngine.prototype.fromSelfie = function(selfie) {
        if (typeof selfie !== "object" || selfie === null) {
          return;
        }
        this.reset();
        this.notReady = true;
        const results = [
          destHNTrieContainer.fromSelfie(selfie.destHNTrieContainer),
          origHNTrieContainer.fromSelfie(selfie.origHNTrieContainer),
          bidiTrie.fromSelfie(selfie.bidiTrie),
          filterDataFromSelfie(selfie.filterData),
          filterRefsFromSelfie(selfie.filterRefs)
        ];
        if (results.slice(1).every((v2) => v2 === true) === false) {
          return false;
        }
        if (selfie.version !== this.selfieVersion) {
          return false;
        }
        this.processedFilterCount = selfie.processedFilterCount;
        this.acceptedCount = selfie.acceptedCount;
        this.discardedCount = selfie.discardedCount;
        this.bitsToBucket = selfie.bitsToBucket;
        urlTokenizer.fromSelfie(selfie.urlTokenizer);
        this.notReady = false;
        return true;
      };
      StaticNetFilteringEngine.prototype.unserialize = function(selfie) {
        return this.fromSelfie(selfie);
      };
      StaticNetFilteringEngine.prototype.createCompiler = function() {
        return new FilterCompiler();
      };
      StaticNetFilteringEngine.prototype.fromCompiled = function(reader) {
        reader.select("NETWORK_FILTERS:GOOD");
        while (reader.next()) {
          this.acceptedCount += 1;
          if (this.goodFilters.has(reader.line)) {
            this.discardedCount += 1;
          } else {
            this.goodFilters.add(reader.line);
          }
        }
        reader.select("NETWORK_FILTERS:BAD");
        while (reader.next()) {
          this.badFilters.add(reader.line);
        }
      };
      StaticNetFilteringEngine.prototype.matchAndFetchModifiers = function(fctxt, modifierName) {
        if (this.notReady) {
          return;
        }
        const typeBits = typeNameToTypeValue[fctxt.type] || otherTypeBitValue;
        $requestURL = urlTokenizer.setURL(fctxt.url);
        $requestURLRaw = fctxt.url;
        $docHostname = fctxt.getDocHostname();
        $docDomain = fctxt.getDocDomain();
        $requestHostname = fctxt.getHostname();
        $requestMethodBit = fctxt.method || 0;
        $requestTypeValue = (typeBits & TYPE_REALM) >>> TYPE_REALM_OFFSET;
        $requestAddress = fctxt.getIPAddress();
        const modifierType = modifierTypeFromName.get(modifierName);
        const modifierBits = modifierBitsFromType.get(modifierType);
        const partyBits = fctxt.is3rdPartyToDoc() ? THIRDPARTY_REALM : FIRSTPARTY_REALM;
        const catBits00 = modifierBits;
        const catBits01 = modifierBits | typeBits;
        const catBits10 = modifierBits | partyBits;
        const catBits11 = modifierBits | typeBits | partyBits;
        const bucket00 = this.bitsToBucket.get(catBits00);
        const bucket01 = typeBits !== 0 ? this.bitsToBucket.get(catBits01) : void 0;
        const bucket10 = partyBits !== 0 ? this.bitsToBucket.get(catBits10) : void 0;
        const bucket11 = typeBits !== 0 && partyBits !== 0 ? this.bitsToBucket.get(catBits11) : void 0;
        if (bucket00 === void 0 && bucket01 === void 0 && bucket10 === void 0 && bucket11 === void 0) {
          return;
        }
        const results = [];
        const env = {
          type: modifierType || 0,
          bits: 0,
          th: 0,
          iunit: 0,
          results
        };
        const tokenHashes = urlTokenizer.getTokens();
        let i = 0;
        let th2 = 0, iunit = 0;
        for (; ; ) {
          th2 = tokenHashes[i];
          if (th2 === INVALID_TOKEN_HASH) {
            break;
          }
          env.th = th2;
          $tokenBeg = tokenHashes[i + 1];
          if (bucket00 !== void 0 && (iunit = bucket00.get(th2) || 0) !== 0) {
            env.bits = catBits00;
            env.iunit = iunit;
            filterMatchAndFetchModifiers(iunit, env);
          }
          if (bucket01 !== void 0 && (iunit = bucket01.get(th2) || 0) !== 0) {
            env.bits = catBits01;
            env.iunit = iunit;
            filterMatchAndFetchModifiers(iunit, env);
          }
          if (bucket10 !== void 0 && (iunit = bucket10.get(th2) || 0) !== 0) {
            env.bits = catBits10;
            env.iunit = iunit;
            filterMatchAndFetchModifiers(iunit, env);
          }
          if (bucket11 !== void 0 && (iunit = bucket11.get(th2) || 0) !== 0) {
            env.bits = catBits11;
            env.iunit = iunit;
            filterMatchAndFetchModifiers(iunit, env);
          }
          i += 2;
        }
        if (results.length === 0) {
          return;
        }
        if (results.length === 1) {
          const result = results[0];
          if ((result.bits & ALLOW_REALM) !== 0) {
            return;
          }
          return [result];
        }
        const toAddImportant = /* @__PURE__ */ new Map();
        const toAdd = /* @__PURE__ */ new Map();
        const toRemove = /* @__PURE__ */ new Map();
        for (const result of results) {
          const actionBits = result.bits & BLOCKALLOW_REALM;
          const modifyValue = result.value;
          if (actionBits === BLOCKIMPORTANT_REALM) {
            toAddImportant.set(modifyValue, result);
          } else if (actionBits === BLOCK_REALM) {
            toAdd.set(modifyValue, result);
          } else {
            toRemove.set(modifyValue, result);
          }
        }
        if (toAddImportant.size === 0 && toAdd.size === 0) {
          return;
        }
        if (toAddImportant.size !== 0) {
          for (const key of toAddImportant.keys()) {
            toAdd.delete(key);
            toRemove.delete(key);
          }
        }
        if (toRemove.size !== 0) {
          if (toRemove.has("") === false) {
            for (const key of toRemove.keys()) {
              if (toAdd.has(key)) {
                toAdd.delete(key);
              } else {
                toRemove.delete(key);
              }
            }
          } else if (toAdd.size !== 0) {
            toAdd.clear();
            if (toRemove.size !== 1) {
              const entry = toRemove.get("");
              toRemove.clear();
              toRemove.set("", entry);
            }
          } else {
            toRemove.clear();
          }
        }
        if (toAdd.size === 0 && toAddImportant.size === 0 && toRemove.size === 0) {
          return;
        }
        const out = Array.from(toAdd.values());
        if (toAddImportant.size !== 0) {
          out.push(...toAddImportant.values());
        }
        if (toRemove.size !== 0) {
          out.push(...toRemove.values());
        }
        return out;
      };
      StaticNetFilteringEngine.prototype.realmMatchString = function(realmBits, typeBits, partyBits) {
        if (this.notReady) {
          return false;
        }
        const exactType = typeBits & 2147483648;
        typeBits &= 2147483647;
        const catBits00 = realmBits;
        const catBits01 = realmBits | typeBits;
        const catBits10 = realmBits | partyBits;
        const catBits11 = realmBits | typeBits | partyBits;
        const bucket00 = exactType === 0 ? this.bitsToBucket.get(catBits00) : void 0;
        const bucket01 = exactType !== 0 || typeBits !== 0 ? this.bitsToBucket.get(catBits01) : void 0;
        const bucket10 = exactType === 0 && partyBits !== 0 ? this.bitsToBucket.get(catBits10) : void 0;
        const bucket11 = (exactType !== 0 || typeBits !== 0) && partyBits !== 0 ? this.bitsToBucket.get(catBits11) : void 0;
        if (bucket00 === void 0 && bucket01 === void 0 && bucket10 === void 0 && bucket11 === void 0) {
          return false;
        }
        let catBits = 0, iunit = 0;
        let tokenHash = DOT_TOKEN_HASH;
        if (bucket00 !== void 0 && (iunit = bucket00.get(DOT_TOKEN_HASH) || 0) !== 0 && filterMatch(iunit) === true) {
          catBits = catBits00;
        } else if (bucket01 !== void 0 && (iunit = bucket01.get(DOT_TOKEN_HASH) || 0) !== 0 && filterMatch(iunit) === true) {
          catBits = catBits01;
        } else if (bucket10 !== void 0 && (iunit = bucket10.get(DOT_TOKEN_HASH) || 0) !== 0 && filterMatch(iunit) === true) {
          catBits = catBits10;
        } else if (bucket11 !== void 0 && (iunit = bucket11.get(DOT_TOKEN_HASH) || 0) !== 0 && filterMatch(iunit) === true) {
          catBits = catBits11;
        } else {
          const tokenHashes = urlTokenizer.getTokens();
          let i = 0;
          for (; ; ) {
            tokenHash = tokenHashes[i];
            if (tokenHash === INVALID_TOKEN_HASH) {
              return false;
            }
            $tokenBeg = tokenHashes[i + 1];
            if (bucket00 !== void 0 && (iunit = bucket00.get(tokenHash) || 0) !== 0 && filterMatch(iunit) === true) {
              catBits = catBits00;
              break;
            }
            if (bucket01 !== void 0 && (iunit = bucket01.get(tokenHash) || 0) !== 0 && filterMatch(iunit) === true) {
              catBits = catBits01;
              break;
            }
            if (bucket10 !== void 0 && (iunit = bucket10.get(tokenHash) || 0) !== 0 && filterMatch(iunit) === true) {
              catBits = catBits10;
              break;
            }
            if (bucket11 !== void 0 && (iunit = bucket11.get(tokenHash) || 0) !== 0 && filterMatch(iunit) === true) {
              catBits = catBits11;
              break;
            }
            i += 2;
          }
        }
        this.$catBits = catBits;
        this.$tokenHash = tokenHash;
        this.$filterUnit = iunit;
        return true;
      };
      StaticNetFilteringEngine.prototype.matchRequestReverse = function(type, url) {
        const typeBits = typeNameToTypeValue[type] | 2147483648;
        $requestURL = urlTokenizer.setURL(url);
        $requestURLRaw = url;
        $requestMethodBit = 0;
        $requestTypeValue = (typeBits & TYPE_REALM) >>> TYPE_REALM_OFFSET;
        $requestAddress = "";
        $isBlockImportant = false;
        this.$filterUnit = 0;
        $docHostname = $requestHostname = hostnameFromNetworkURL(url);
        $docDomain = domainFromHostname($docHostname);
        if (this.realmMatchString(ALLOW_REALM, typeBits, FIRSTPARTY_REALM)) {
          if (this.realmMatchString(BLOCKIMPORTANT_REALM, typeBits, FIRSTPARTY_REALM)) {
            return 1;
          }
          return 2;
        }
        return 0;
      };
      StaticNetFilteringEngine.prototype.matchRequest = function(fctxt, modifiers = 0) {
        let typeBits = typeNameToTypeValue[fctxt.type];
        if (modifiers === 0) {
          if (typeBits === void 0) {
            typeBits = otherTypeBitValue;
          } else if (typeBits === 0 || typeBits > otherTypeBitValue) {
            modifiers |= 1;
          }
        }
        if ((modifiers & 1) !== 0) {
          if (typeBits === void 0) {
            return 0;
          }
          typeBits |= 2147483648;
        }
        const partyBits = fctxt.is3rdPartyToDoc() ? THIRDPARTY_REALM : FIRSTPARTY_REALM;
        $requestURL = urlTokenizer.setURL(fctxt.url);
        $requestURLRaw = fctxt.url;
        this.$filterUnit = 0;
        $docHostname = fctxt.getDocHostname();
        $docDomain = fctxt.getDocDomain();
        $requestHostname = fctxt.getHostname();
        $requestMethodBit = fctxt.method || 0;
        $requestTypeValue = (typeBits & TYPE_REALM) >>> TYPE_REALM_OFFSET;
        $requestAddress = fctxt.getIPAddress();
        $isBlockImportant = false;
        const r = this.realmMatchString(BLOCK_REALM, typeBits, partyBits);
        if (r || (modifiers & 2) !== 0) {
          if ($isBlockImportant) {
            return 1;
          }
          if (this.realmMatchString(ALLOW_REALM, typeBits, partyBits)) {
            if (this.realmMatchString(BLOCKIMPORTANT_REALM, typeBits, partyBits)) {
              return 1;
            }
            return 2;
          }
          if (r) {
            return 1;
          }
        }
        return 0;
      };
      StaticNetFilteringEngine.prototype.matchHeaders = function(fctxt, ...headers) {
        const typeBits = typeNameToTypeValue[fctxt.type] || otherTypeBitValue;
        const partyBits = fctxt.is3rdPartyToDoc() ? THIRDPARTY_REALM : FIRSTPARTY_REALM;
        $requestURL = urlTokenizer.setURL(fctxt.url);
        $requestURLRaw = fctxt.url;
        this.$filterUnit = 0;
        $docHostname = fctxt.getDocHostname();
        $docDomain = fctxt.getDocDomain();
        $requestHostname = fctxt.getHostname();
        $requestMethodBit = fctxt.method || 0;
        $requestTypeValue = (typeBits & TYPE_REALM) >>> TYPE_REALM_OFFSET;
        $requestAddress = fctxt.getIPAddress();
        $isBlockImportant = false;
        $httpHeaders.init(...headers);
        let r = 0;
        if (this.realmMatchString(HEADERS_REALM | BLOCK_REALM, typeBits, partyBits)) {
          r = 1;
        }
        if (r !== 0 && $isBlockImportant !== true) {
          if (this.realmMatchString(HEADERS_REALM | ALLOW_REALM, typeBits, partyBits)) {
            r = 2;
          } else if (this.realmMatchString(ALLOW_REALM, typeBits, partyBits)) {
            r = 2;
          }
          if (r === 2) {
            if (this.realmMatchString(HEADERS_REALM | BLOCKIMPORTANT_REALM, typeBits, partyBits)) {
              r = 1;
            }
          }
        }
        $httpHeaders.reset();
        return r;
      };
      StaticNetFilteringEngine.prototype.redirectRequest = function(redirectEngine2, fctxt) {
        const directives = this.matchAndFetchModifiers(fctxt, "redirect-rule");
        if (directives === void 0) {
          return;
        }
        const highest = directives.length - 1;
        if (highest !== 0) {
          directives.sort((a, b2) => compareRedirectRequests(redirectEngine2, a, b2));
        }
        const directive = directives[highest];
        if ((directive.bits & ALLOW_REALM) !== 0) {
          return directives;
        }
        const { token } = parseRedirectRequestValue(directive);
        fctxt.redirectURL = redirectEngine2.tokenToURL(fctxt, token);
        if (fctxt.redirectURL === void 0) {
          return;
        }
        return directives;
      };
      StaticNetFilteringEngine.prototype.transformURL = function(fctxt, out = []) {
        const directives = this.matchAndFetchModifiers(fctxt, "uritransform");
        if (directives === void 0) {
          return;
        }
        const redirectURL = new URL(fctxt.url);
        for (const directive of directives) {
          if ((directive.bits & ALLOW_REALM) !== 0) {
            out.push(directive);
            continue;
          }
          if (directive.cache === null) {
            directive.cache = parseReplaceByRegexValue(directive.value);
          }
          const cache = directive.cache;
          if (cache === void 0) {
            continue;
          }
          let { re: re2 } = cache;
          const before = redirectURL.href;
          if (re2 === void 0) {
            const logdata = directive.logData();
            if (logdata === void 0) {
              continue;
            }
            try {
              re2 = new RegExp(logdata.regex, cache.flags);
            } catch (e) {
              console.warn("[uBR] static-net-filtering: redirect regex failed", e);
              continue;
            }
          }
          if (re2.test(before) !== true) {
            continue;
          }
          const after = before.replace(re2, cache.replacement);
          try {
            void new URL(after);
          } catch (e) {
            console.warn("[uBR] static-net-filtering: redirect URL invalid", e);
            continue;
          }
          if (after === before) {
            continue;
          }
          redirectURL.href = after;
          out.push(directive);
          break;
        }
        if (out.length === 0) {
          return;
        }
        if (redirectURL.href !== fctxt.url) {
          fctxt.redirectURL = redirectURL.href;
        }
        return out;
      };
      StaticNetFilteringEngine.prototype.urlSkip = function(fctxt, blocked, out = []) {
        if (fctxt.redirectURL !== void 0) {
          return;
        }
        const directives = this.matchAndFetchModifiers(fctxt, "urlskip");
        if (directives === void 0) {
          return;
        }
        for (const directive of directives) {
          if ((directive.bits & ALLOW_REALM) !== 0) {
            out.push(directive);
            continue;
          }
          const urlin = fctxt.url;
          const value = directive.value;
          const steps = value.includes(" ") && value.split(/ +/) || [value];
          const urlout = urlSkip(urlin, blocked, steps, directive);
          if (urlout === void 0) {
            continue;
          }
          if (urlout === urlin) {
            continue;
          }
          fctxt.redirectURL = urlout;
          out.push(directive);
          break;
        }
        if (out.length === 0) {
          return;
        }
        return out;
      };
      StaticNetFilteringEngine.prototype.filterQuery = function(fctxt, out = []) {
        if (fctxt.redirectURL !== void 0) {
          return;
        }
        const directives = this.matchAndFetchModifiers(fctxt, "removeparam");
        if (directives === void 0) {
          return;
        }
        const url = fctxt.url;
        const qpos = url.indexOf("?");
        if (qpos === -1) {
          return;
        }
        let hpos = url.indexOf("#", qpos + 1);
        if (hpos === -1) {
          hpos = url.length;
        }
        const params = [];
        const query = url.slice(qpos + 1, hpos);
        for (let i = 0; i < query.length; ) {
          let pos = query.indexOf("&", i);
          if (pos === -1) {
            pos = query.length;
          }
          const kv = query.slice(i, pos);
          i = pos + 1;
          pos = kv.indexOf("=");
          if (pos !== -1) {
            params.push(kv.slice(0, pos), kv.slice(pos + 1));
          } else {
            params.push(kv, "");
          }
        }
        const beforeParamCount = params.length;
        for (const directive of directives) {
          if (params.length === 0) {
            break;
          }
          const isException = (directive.bits & ALLOW_REALM) !== 0;
          if (isException && directive.value === "") {
            out.push(directive);
            break;
          }
          const { all, bad, name: name2, not, re: re2 } = parseQueryPruneValue2(directive);
          if (bad) {
            continue;
          }
          if (all) {
            if (isException === false) {
              params.length = 0;
            }
            out.push(directive);
            break;
          }
          let filtered = false;
          let matched = false;
          let i = params.length;
          while (i > 0) {
            i -= 2;
            if (name2 !== void 0) {
              matched = params[i] === name2;
            } else if (re2 !== void 0) {
              const key = params[i + 0];
              const raw = params[i + 1];
              let value = raw;
              try {
                value = decodeURIComponent(value);
              } catch (e) {
                console.warn("[uBR] static-net-filtering: decodeURIComponent failed", e);
              }
              matched = re2.test(`${key}=${value}`);
            }
            if (matched === not) {
              continue;
            }
            if (isException === false) {
              params.splice(i, 2);
            }
            filtered = true;
          }
          if (filtered) {
            out.push(directive);
          }
        }
        if (out.length === 0) {
          return;
        }
        if (params.length !== beforeParamCount) {
          fctxt.redirectURL = url.slice(0, qpos);
          if (params.length !== 0) {
            const queryParts = [];
            for (let i = 0; i < params.length; i += 2) {
              const key = params[i + 0];
              const val = params[i + 1];
              queryParts.push(val !== "" ? `${key}=${val}` : key);
            }
            fctxt.redirectURL += `?${queryParts.join("&")}`;
          }
          if (hpos !== url.length) {
            fctxt.redirectURL += url.slice(hpos);
          }
        }
        return out;
      };
      StaticNetFilteringEngine.prototype.hasQuery = function(fctxt) {
        urlTokenizer.setURL(fctxt.url);
        return urlTokenizer.hasQuery();
      };
      StaticNetFilteringEngine.prototype.toLogData = function() {
        if (this.$filterUnit !== 0) {
          return new LogData(this.$catBits, this.$tokenHash, this.$filterUnit);
        }
      };
      StaticNetFilteringEngine.prototype.isBlockImportant = function() {
        return this.$filterUnit !== 0 && $isBlockImportant;
      };
      StaticNetFilteringEngine.prototype.getFilterCount = function() {
        return this.acceptedCount - this.discardedCount;
      };
      StaticNetFilteringEngine.prototype.enableWASM = function(wasmModuleFetcher, path) {
        return Promise.all([
          bidiTrie.enableWASM(wasmModuleFetcher, path),
          origHNTrieContainer.enableWASM(wasmModuleFetcher, path),
          destHNTrieContainer.enableWASM(wasmModuleFetcher, path)
        ]).then((results) => {
          return results.every((a) => a === true);
        });
      };
      StaticNetFilteringEngine.prototype.test = function(details) {
        const { url, type, from, redirectEngine: redirectEngine2 } = details;
        if (url === void 0) {
          return;
        }
        const fctxt = new FilteringContext();
        fctxt.setURL(url);
        fctxt.setType(type || "");
        fctxt.setDocOriginFromURL(from || "");
        const r = this.matchRequest(fctxt);
        const out = [`url: ${url}`];
        if (type) {
          out.push(`type: ${type}`);
        }
        if (from) {
          out.push(`context: ${from}`);
        }
        if (r !== 0) {
          const logdata = this.toLogData();
          if (r === 1) {
            out.push(`blocked: ${logdata.raw}`);
          } else if (r === 2) {
            out.push(`unblocked: ${logdata.raw}`);
          }
        } else {
          out.push("not blocked");
        }
        if (r !== 1) {
          const entries = this.transformURL(fctxt);
          if (entries) {
            for (const entry of entries) {
              out.push(`modified: ${entry.logData().raw}`);
            }
          }
          if (fctxt.redirectURL !== void 0 && this.hasQuery(fctxt)) {
            const entries2 = this.filterQuery(fctxt, "removeparam");
            if (entries2) {
              for (const entry of entries2) {
                out.push(`modified: ${entry.logData().raw}`);
              }
            }
          }
          if (fctxt.type === "main_frame" || fctxt.type === "sub_frame") {
            const csps = this.matchAndFetchModifiers(fctxt, "csp");
            if (csps) {
              for (const csp of csps) {
                out.push(`modified: ${csp.logData().raw}`);
              }
            }
            const pps = this.matchAndFetchModifiers(fctxt, "permissions");
            if (pps) {
              for (const pp2 of pps) {
                out.push(`modified: ${pp2.logData().raw}`);
              }
            }
          }
        } else if (redirectEngine2) {
          const redirects = this.redirectRequest(redirectEngine2, fctxt);
          if (redirects) {
            for (const redirect of redirects) {
              out.push(`modified: ${redirect.logData().raw}`);
            }
          }
        }
        const urlskips = this.matchAndFetchModifiers(fctxt, "urlskip");
        if (urlskips) {
          for (const urlskip of urlskips) {
            out.push(`modified: ${urlskip.logData().raw}`);
          }
        }
        return out.join("\n");
      };
      StaticNetFilteringEngine.prototype.bucketHistogram = function() {
        const results = [];
        for (const [bits, bucket] of this.bitsToBucket) {
          for (const [th2, iunit] of bucket) {
            const token = urlTokenizer.stringFromTokenHash(th2);
            const fc2 = filterGetClass(iunit);
            const count = fc2.getCount !== void 0 ? fc2.getCount(iunit) : 1;
            results.push({ bits: bits.toString(16), token, count, f: fc2.name });
          }
        }
        results.sort((a, b2) => {
          return b2.count - a.count;
        });
        console.info(results);
      };
      StaticNetFilteringEngine.prototype.dump = function() {
        const thConstants = /* @__PURE__ */ new Map([
          [NO_TOKEN_HASH, "NO_TOKEN_HASH"],
          [DOT_TOKEN_HASH, "DOT_TOKEN_HASH"],
          [ANY_TOKEN_HASH, "ANY_TOKEN_HASH"],
          [ANY_HTTPS_TOKEN_HASH, "ANY_HTTPS_TOKEN_HASH"],
          [ANY_HTTP_TOKEN_HASH, "ANY_HTTP_TOKEN_HASH"],
          [EMPTY_TOKEN_HASH, "EMPTY_TOKEN_HASH"]
        ]);
        const out = [];
        const toOutput = (depth, line) => {
          out.push(`${" ".repeat(depth * 2)}${line}`);
        };
        const dumpUnit = (idata, depth = 0) => {
          const fc2 = filterGetClass(idata);
          fcCounts.set(fc2.name, (fcCounts.get(fc2.name) || 0) + 1);
          const info = filterDumpInfo(idata) || "";
          toOutput(depth, info !== "" ? `${fc2.name}: ${info}` : fc2.name);
          switch (fc2) {
            case FilterBucket:
            case FilterCompositeAll:
            case FilterDomainHitAny: {
              fc2.forEach(idata, (i) => {
                dumpUnit(i, depth + 1);
              });
              break;
            }
            case FilterBucketIfOriginHits: {
              dumpUnit(filterData[idata + 2], depth + 1);
              dumpUnit(filterData[idata + 1], depth + 1);
              break;
            }
            case FilterBucketIfRegexHits: {
              dumpUnit(filterData[idata + 1], depth + 1);
              break;
            }
            case FilterPlainTrie: {
              for (const details of bidiTrie.trieIterator(filterData[idata + 1])) {
                toOutput(depth + 1, details.pattern);
                let ix = details.iextra;
                if (ix === 1) {
                  continue;
                }
                for (; ; ) {
                  if (ix === 0) {
                    break;
                  }
                  dumpUnit(filterData[ix + 0], depth + 2);
                  ix = filterData[ix + 1];
                }
              }
              break;
            }
            default:
              break;
          }
        };
        const fcCounts = /* @__PURE__ */ new Map();
        const thCounts = /* @__PURE__ */ new Set();
        const realms = /* @__PURE__ */ new Map([
          [BLOCK_REALM, "block"],
          [BLOCKIMPORTANT_REALM, "block-important"],
          [ALLOW_REALM, "unblock"],
          [REDIRECT_REALM, "redirect"],
          [REMOVEPARAM_REALM, "removeparam"],
          [CSP_REALM, "csp"],
          [PERMISSIONS_REALM, "permissions"],
          [URLTRANSFORM_REALM, "uritransform"],
          [REPLACE_REALM, "replace"],
          [URLSKIP_REALM, "urlskip"]
        ]);
        const partyness = /* @__PURE__ */ new Map([
          [ANYPARTY_REALM, "any-party"],
          [FIRSTPARTY_REALM, "1st-party"],
          [THIRDPARTY_REALM, "3rd-party"]
        ]);
        for (const [realmBits, realmName] of realms) {
          toOutput(1, `+ realm: ${realmName}`);
          for (const [partyBits, partyName] of partyness) {
            toOutput(2, `+ party: ${partyName}`);
            const processedTypeBits = /* @__PURE__ */ new Set();
            for (const typeName in typeNameToTypeValue) {
              const typeBits = typeNameToTypeValue[typeName];
              if (processedTypeBits.has(typeBits)) {
                continue;
              }
              processedTypeBits.add(typeBits);
              const bits = realmBits | partyBits | typeBits;
              const bucket = this.bitsToBucket.get(bits);
              if (bucket === void 0) {
                continue;
              }
              const thCount = bucket.size;
              toOutput(3, `+ type: ${typeName} (${thCount})`);
              for (const [th2, iunit] of bucket) {
                thCounts.add(th2);
                const ths = thConstants.has(th2) ? thConstants.get(th2) : `0x${th2.toString(16)}`;
                toOutput(4, `+ th: ${ths}`);
                dumpUnit(iunit, 5);
              }
            }
          }
        }
        const knownTokens = urlTokenizer.knownTokens.reduce((a, b2) => b2 !== 0 ? a + 1 : a, 0);
        out.unshift([
          "Static Network Filtering Engine internals:",
          `  Distinct token hashes: ${thCounts.size.toLocaleString("en")}`,
          `  Known-token sieve (Uint8Array): ${knownTokens.toLocaleString("en")} out of 65,536`,
          `  Filter data (Int32Array): ${filterDataWritePtr.toLocaleString("en")}`,
          `  Filter refs (JS array): ${filterRefsWritePtr.toLocaleString("en")}`,
          "  Origin trie container:",
          origHNTrieContainer.dumpInfo().split("\n").map((a) => `    ${a}`).join("\n"),
          "  Request trie container:",
          destHNTrieContainer.dumpInfo().split("\n").map((a) => `    ${a}`).join("\n"),
          "  Pattern trie container:",
          bidiTrie.dumpInfo().split("\n").map((a) => `    ${a}`).join("\n"),
          "+ Filter class stats:",
          Array.from(fcCounts).sort((a, b2) => b2[1] - a[1]).map((a) => `    ${a[0]}: ${a[1].toLocaleString("en")}`).join("\n"),
          "+ Filter tree:"
        ].join("\n"));
        return out.join("\n");
      };
      staticNetFilteringEngine = new StaticNetFilteringEngine();
      static_net_filtering_default = staticNetFilteringEngine;
    }
  });

  // src/core/smart-cosmetic/smart-rule-schema.ts
  function normalizeRuleState(state) {
    return STATE_ALIASES[state] ?? state;
  }
  function isValidStateTransition(from, to2, rule) {
    if (!STATE_TRANSITIONS[from].includes(to2)) return false;
    if (to2 === "active" && rule?.preview?.status && ACTIVE_BLOCKED_PREVIEW_STATUSES.has(rule.preview.status)) return false;
    return true;
  }
  var STATE_TRANSITIONS, STATE_ALIASES, ACTIVE_BLOCKED_PREVIEW_STATUSES, DEFAULT_BOUNDARY, DEFAULT_SAFETY, DEFAULT_PERFORMANCE, DEFAULT_FRAMES, DEFAULT_SHADOW, DEFAULT_RUNTIME, DEFAULT_CACHE, DEFAULT_LOGIC_OPTIONS, RULE_ID_PREFIX, SAFE_STYLE_PROPERTIES, DEFAULT_THRESHOLDS, DEFAULT_WEIGHT_PROFILES;
  var init_smart_rule_schema = __esm({
    "src/core/smart-cosmetic/smart-rule-schema.ts"() {
      STATE_TRANSITIONS = {
        draft: ["needs-preview", "previewed", "disabled"],
        "needs-preview": ["previewed", "disabled", "error"],
        previewed: ["active", "needs-preview", "disabled", "error", "partial"],
        active: ["disabled", "paused", "needs-preview", "previewed", "partial", "awaiting-scope"],
        disabled: ["draft", "active", "previewed"],
        paused: ["active", "disabled"],
        error: ["draft", "disabled"],
        "awaiting-scope": ["active", "disabled"],
        partial: ["active", "disabled", "needs-preview"]
      };
      STATE_ALIASES = {
        enabled: "active"
      };
      ACTIVE_BLOCKED_PREVIEW_STATUSES = /* @__PURE__ */ new Set(["required", "previewed", "stale", "forced-by-policy"]);
      DEFAULT_BOUNDARY = Object.freeze({
        mode: "repeated-card",
        maxDepth: 8,
        stopAtScope: true,
        allowCrossScope: false,
        allowScopeRoot: false,
        includeSelf: false,
        allowPageRoot: false
      });
      DEFAULT_SAFETY = Object.freeze({
        preview: "required",
        maxMatches: 100,
        maxPagePercent: 25,
        maxViewportAreaPercent: 40,
        minFeatures: 3,
        minLogicMatches: 1,
        allowPartialApply: false,
        warnIfScopeIsBody: true,
        warnIfNoWhereLogic: true,
        warnIfActionRemove: true,
        warnIfWeakExactReference: true,
        confirmPageRoot: false
      });
      DEFAULT_PERFORMANCE = Object.freeze({
        maxCandidates: 150,
        maxEvaluationsPerCycle: 150,
        maxAddedNodesPerCycle: 300,
        maxRegexMsPerRuleCycle: 10,
        maxRegexPatternLength: 256,
        maxTextRegexInputChars: 4096,
        debounceMs: 100,
        dependencyDepth: 3,
        dependencyPrefilter: true,
        preferCssPrefilter: true,
        metrics: "collect",
        sampleRate: 1
      });
      DEFAULT_FRAMES = Object.freeze({
        mode: "top-only",
        accounting: "per-frame",
        maxTotalMatches: 150
      });
      DEFAULT_SHADOW = Object.freeze({
        mode: "none",
        observeMutations: true,
        observeRecursive: false,
        allowHostAncestor: false,
        maxRootsPerCycle: 20
      });
      DEFAULT_RUNTIME = Object.freeze({
        observePathChanges: true,
        pathChangeDebounceMs: 200,
        reEvaluateOnPathChange: "smart",
        observeSubtree: true,
        observeAttributes: "auto",
        errorRecovery: "continue",
        onBudgetExceeded: "stop-cycle",
        maxConsecutivePartialCycles: 3
      });
      DEFAULT_CACHE = Object.freeze({
        maxEntries: 1e3,
        maxAgeMs: 6e4,
        evictionPolicy: "LRU",
        scope: "per-rule"
      });
      DEFAULT_LOGIC_OPTIONS = Object.freeze({
        stopAtScopeForAncestor: true
      });
      RULE_ID_PREFIX = "ubr:smart:";
      SAFE_STYLE_PROPERTIES = /* @__PURE__ */ new Set([
        "display",
        "visibility",
        "opacity",
        "outline",
        "outline-color",
        "outline-style",
        "outline-width",
        "outline-offset",
        "border",
        "border-color",
        "border-style",
        "border-width",
        "background-color",
        "color",
        "filter",
        "pointer-events"
      ]);
      DEFAULT_THRESHOLDS = Object.freeze({
        "hide-similar:similar:default-card": 0.82,
        "hide-similar:similar:": 0.82,
        "hide-similar:structural": 0.8,
        "smart-hide:similar:strong-logic": 0.74,
        "smart-hide:structural:strong-logic": 0.76,
        "smart-hide:similar:": 0.82,
        "smart-hide:similar:content-heavy": 0.78
      });
      DEFAULT_WEIGHT_PROFILES = Object.freeze({
        "default-card": Object.freeze({
          roleTag: 1,
          textTokens: 1.5,
          semanticText: 1.5,
          href: 2,
          dataAttrs: 1.5,
          aria: 1.2,
          structure: 2,
          visualBox: 1,
          classes: 0.25,
          nthChild: 0.5
        }),
        "structural-heavy": Object.freeze({
          structure: 4,
          roleTag: 1.5,
          classes: 0.25,
          visualBox: 1.5,
          textTokens: 0.5,
          semanticText: 0.8,
          href: 1,
          dataAttrs: 1.5,
          aria: 0.8,
          nthChild: 0.8
        }),
        "content-heavy": Object.freeze({
          textTokens: 3,
          semanticText: 3,
          href: 2,
          roleTag: 0.7,
          dataAttrs: 1.5,
          aria: 1.8,
          structure: 1,
          classes: 0.1,
          visualBox: 0.5,
          nthChild: 0.2
        })
      });
    }
  });

  // src/core/smart-cosmetic/matcher.ts
  function triggerFingerprintGC(activeSelectors) {
    if (typeof document === "undefined") return;
    if (activeSelectors && activeSelectors.length > 0) {
      gcFingerprintsNotInUse(activeSelectors);
    } else {
      invalidateFingerprintCache();
    }
  }
  function gcFingerprintsNotInUse(activeSelectors) {
    const inUse = /* @__PURE__ */ new Set();
    try {
      for (const sel2 of activeSelectors) {
        for (const el2 of document.querySelectorAll(sel2)) {
          inUse.add(el2);
        }
      }
    } catch (e) {
      console.warn("[uBR] matcher: gcFingerprintsNotInUse querySelectorAll failed", e);
    }
    const keys = [];
    try {
      for (const el2 of document.querySelectorAll("*")) {
        if (fingerprintCache.get(el2) !== void 0 && !inUse.has(el2)) {
          keys.push(el2);
        }
        if (keys.length > 500) break;
      }
    } catch (e) {
      console.warn('[uBR] matcher: gcFingerprintsNotInUse querySelectorAll("*") failed', e);
    }
    for (const key of keys) {
      fingerprintCache.delete(key);
    }
  }
  function invalidateFingerprintCache() {
    const _wm = fingerprintCache;
    const keys = [];
    try {
      for (const el2 of document.querySelectorAll("*")) {
        const fv = fingerprintCache.get(el2);
        if (fv !== void 0) {
          keys.push(el2);
        }
        if (keys.length > 500) break;
      }
    } catch (e) {
      console.warn('[uBR] matcher: invalidateFingerprintCache querySelectorAll("*") failed', e);
    }
    for (const key of keys) {
      fingerprintCache.delete(key);
    }
  }
  var fingerprintCache;
  var init_matcher = __esm({
    "src/core/smart-cosmetic/matcher.ts"() {
      init_smart_rule_schema();
      init_matcher();
      fingerprintCache = /* @__PURE__ */ new WeakMap();
    }
  });

  // src/core/smart-cosmetic/smart-rule-diagnostics.ts
  function diagnostic(code, message, severity = "error", hint) {
    return { code, message, severity, hint };
  }
  var DIAGNOSTIC_CODES;
  var init_smart_rule_diagnostics = __esm({
    "src/core/smart-cosmetic/smart-rule-diagnostics.ts"() {
      DIAGNOSTIC_CODES = {
        TARGET_INVALID: "target-invalid",
        TARGET_REGEX_UNSAFE: "target-regex-unsafe",
        PATH_INVALID: "path-invalid",
        PATH_REGEX_UNSAFE: "path-regex-unsafe",
        MISSING_SCOPE: "missing-scope",
        UNSAFE_SCOPE_SELECTOR: "unsafe-scope-selector",
        CANDIDATE_UNSAFE_SELECTOR: "candidate-unsafe-selector",
        CANDIDATE_REQUIRED: "candidate-required",
        SCOPE_IS_BODY: "scope-is-body",
        BOUNDARY_INVALID: "boundary-invalid",
        BOUNDARY_MODE_MISMATCH: "boundary-mode-mismatch",
        BOUNDARY_CROSS_SCOPE_CONFLICT: "boundary-cross-scope-conflict",
        MATCH_MODE_NONE_WITH_THRESHOLD: "threshold-with-mode-none",
        MATCH_MODE_NONE_WITH_REFERENCE: "reference-with-mode-none",
        MATCH_MODE_NONE_WITH_WEIGHTS: "weights-with-mode-none",
        MATCH_MODE_EXACT_WITH_THRESHOLD: "threshold-with-exact-mode",
        MATCH_SIMILAR_WITHOUT_REFERENCE: "similar-without-reference",
        MATCH_STRUCTURAL_WITHOUT_REFERENCE: "structural-without-reference",
        MATCH_WEAK_EXACT_REFERENCE: "weak-exact-reference",
        MATCH_MISSING_REFERENCE: "missing-match-reference",
        WEIGHTS_INVALID_PROFILE: "weights-invalid-profile",
        ACTION_SMART_ALLOW_INVALID: "smart-allow-invalid-action",
        ACTION_REMOVE_WARNING: "action-remove-warning",
        WHERE_MISSING_SMART: "where-missing-smart",
        LOGIC_INVALID: "logic-invalid",
        KEYWORDS_INACTIVE: "keywords-inactive",
        KEYWORDS_IGNORED_BY_EXPLICIT_LOGIC: "keywords-ignored-by-explicit-logic",
        KEYWORD_MERGE_CONFLICT: "keyword-merge-conflict",
        REGEX_UNSUPPORTED_FLAG: "regex-unsupported-flag",
        REGEX_UNCOMPILABLE: "regex-uncompilable",
        REGEX_BUDGET_RISK: "regex-budget-risk",
        REGEX_BUDGET_EXCEEDED: "regex-budget-exceeded",
        REGEX_INPUT_TRUNCATED: "regex-input-truncated",
        UNSAFE_REGEX: "unsafe-regex",
        SAFETY_MIN_FEATURES_MODE_NONE: "min-features-mode-none",
        SAFETY_MIN_LOGIC_MATCHES_IMPOSSIBLE: "min-logic-matches-impossible",
        SAFETY_PREVIEW_REQUIRED: "preview-required",
        SAFETY_PREVIEW_NOT_CONFIRMED: "preview-not-confirmed",
        PERFORMANCE_INVALID: "performance-invalid",
        FRAMES_INVALID: "frames-invalid",
        SHADOW_INVALID: "shadow-invalid",
        UNSAFE_SHADOW_SELECTOR: "unsupported-shadow-selector",
        RUNTIME_INVALID: "runtime-invalid",
        BROAD_RULE_WARNING: "broad-rule-warning",
        BROAD_EXACT_SCOPE: "broad-exact-scope",
        ALLOW_BROAD_REQUIRED: "allow-broad-required",
        LOW_SIMILARITY_THRESHOLD: "low-similarity-threshold",
        EXACT_MODE_SELECTOR_REQUIRED: "exact-mode-selector-required",
        IMPORT_COLLISION: "import-collision",
        IMPORT_SYNTAX_VERSION_UNSUPPORTED: "syntax-version-unsupported",
        EXPORT_LOSS: "export-loss",
        DEPRECATED_FIELD: "deprecated-field",
        UNSUPPORTED_COMPOSITION: "unsupported-composition-field",
        BOUNDARY_REQUIRED: "boundary-required",
        EXACT_HIDE_BROAD_BOUNDARY: "exact-hide-broad-boundary",
        ORIGINAL_SYNTAX_MISMATCH: "original-syntax-mismatch",
        ALLOW_BROAD_EXPECTED: "allow-broad-expected",
        STYLE_VAR_NOT_ALLOWED: "style-var-not-allowed",
        REGEX_STATIC_REJECTED: "regex-static-rejected",
        REGEX_TOO_LONG: "regex-too-long",
        CATCH_ALL_ALLOW: "catch-all-allow",
        MISSING_CANDIDATES: "missing-candidates",
        MISSING_BOUNDARY: "missing-boundary",
        UNSAFE_CANDIDATE_SELECTOR: "unsafe-candidate-selector",
        AMBIGUOUS_REFERENCE_FIRST_USED: "ambiguous-reference-first-used",
        REFERENCE_SELECTION_UNUSED: "reference-selection-unused",
        EXTERNAL_FILTERS_NOT_SIMULATED: "external-filters-not-simulated",
        EXTERNAL_FILTER_OVERRIDE_SMART_ALLOW: "external-filter-overrode-smart-allow",
        WEAK_EXACT_REFERENCE_WARN: "weak-exact-reference-warn",
        ALLOW_PAGE_ROOT_WITHOUT_CONFIRM: "allow-page-root-without-confirm",
        INCLUDE_SELF_NOT_SELECTOR_MODE: "include-self-not-selector-mode",
        KEYWORD_WORD_BOUNDARY_IGNORED: "keyword-word-boundary-ignored",
        UNSUPPORTED_NEW_SYNTAX: "unsupported-new-syntax",
        CUSTOM_WEIGHTS_NOT_SUPPORTED_V1: "custom-weights-not-supported-v1",
        INVALID_RULE_ACTION_COMBINATION: "invalid-rule-action-combination",
        ALLOW_SCOPE_ROOT_WITHOUT_STOP: "allow-scope-root-without-stop",
        WORD_BOUNDARY_REDUNDANT: "word-boundary-redundant",
        STYLE_FILTER_PERCENTAGE: "style-filter-percentage",
        MATCH_MODE_NONE_NO_WHERE: "match-mode-none-no-where",
        MATCH_MODE_SMART_ALLOW_INVALID: "match-mode-smart-allow-invalid",
        INVALID_BOUNDARY_DEPTH: "invalid-boundary-depth",
        DEPENDENCY_DEPTH_MAY_MISS_DESCENDANT: "dependency-depth-may-miss-descendant",
        SCOPE_ROOT_TOO_LARGE: "scope-root-too-large",
        SELECTOR_BOUNDARY_NOT_FOUND: "selector-boundary-not-found",
        BOUNDARY_LOW_CONFIDENCE: "boundary-low-confidence",
        FRAME_INACCESSIBLE: "frame-inaccessible",
        PARTIAL_SHADOW_DISCOVERY: "partial-shadow-discovery",
        CANDIDATE_BUDGET_EXCEEDED: "candidate-budget-exceeded",
        EXTERNAL_REMOVE_NOT_RESTORABLE: "external-remove-not-restorable",
        IMPORTANT_IGNORED_FOR_BUILT_IN: "important-ignored-for-built-in",
        MIN_FEATURES_UNUSED_EXACT: "min-features-unused-exact",
        MIN_FEATURES_INVALID_WITH_NONE: "min-features-invalid-with-none",
        INVALID_KEYWORD_WORD_MODE: "invalid-keyword-word-mode",
        THRESHOLD_NOT_VALID_FOR_MODE_NONE: "threshold-not-valid-for-mode-none"
      };
    }
  });

  // src/core/smart-cosmetic/smart-rule-validator.ts
  function isSafeSelector(selector) {
    return !UNSAFE_SELECTOR_PATTERNS.some((p) => p.test(selector));
  }
  function validateRegexFlags(flags) {
    const errors = [];
    for (const f of flags) {
      if (REJECTED_REGEX_FLAGS.has(f)) {
        errors.push(`Regex flag '${f}' is not allowed`);
      }
      if (!ALLOWED_REGEX_FLAGS.has(f)) {
        errors.push(`Unknown regex flag '${f}'`);
      }
    }
    return errors;
  }
  function hasStyleVar(style) {
    return /\bvar\(/.test(style);
  }
  function hasFilterPercentage(style) {
    const filterMatch2 = style.match(FILTER_FUNCTION_RE);
    if (!filterMatch2) return false;
    return FILTER_PERCENTAGE_RE.test(style);
  }
  function isRegexPatternSafe(pattern) {
    if (pattern.length > MAX_REGEX_PATTERN_LENGTH) {
      return { safe: false, reason: `Pattern exceeds ${MAX_REGEX_PATTERN_LENGTH} characters` };
    }
    if (NESTED_QUANTIFIER_RE.test(pattern)) {
      return { safe: false, reason: "Nested quantifier detected (potential ReDoS)" };
    }
    if (BACKREFERENCE_RE.test(pattern)) {
      return { safe: false, reason: "Backreferences not allowed in static patterns" };
    }
    if (NAMED_BACKREFERENCE_RE.test(pattern)) {
      return { safe: false, reason: "Named backreferences not allowed" };
    }
    if (CATASTROPHIC_ALTERNATION_RE.test(pattern)) {
      return { safe: false, reason: "Ambiguous alternation with quantifier (potential ReDoS)" };
    }
    if (LOOKBEHIND_RE.test(pattern)) {
      return { safe: false, reason: "Lookbehind assertions are not allowed" };
    }
    const altBranches = pattern.match(/\|/g);
    if (altBranches && altBranches.length > 50) {
      return { safe: false, reason: "Pattern has more than 50 alternation branches" };
    }
    if (/\.\*\+?\s*\.\*\+?/.test(pattern)) {
      return { safe: false, reason: "Unbounded dot-star before another unbounded repeat (potential ReDoS)" };
    }
    return { safe: true };
  }
  function validateRule(rule) {
    const diagnostics = [];
    if (rule.syntaxVersion !== 1) {
      diagnostics.push(diagnostic(DIAGNOSTIC_CODES.UNSUPPORTED_NEW_SYNTAX, `Unsupported syntax version ${rule.syntaxVersion}`));
    }
    if (rule.priority !== void 0 && (rule.priority < -1e3 || rule.priority > 1e3)) {
      diagnostics.push(diagnostic(DIAGNOSTIC_CODES.DEPRECATED_FIELD, `priority ${rule.priority} is out of range (-1000 to 1000)`));
    }
    if (rule.action.action === "style" && "style" in rule.action) {
      const s = rule.action.style;
      if (typeof s === "string") {
        if (hasStyleVar(s)) {
          diagnostics.push(diagnostic(DIAGNOSTIC_CODES.STYLE_VAR_NOT_ALLOWED, "var() is not allowed in v1 style() values"));
        }
        if (hasFilterPercentage(s)) {
          diagnostics.push(diagnostic(DIAGNOSTIC_CODES.STYLE_FILTER_PERCENTAGE, "filter functions must use numeric decimals, not percentages"));
        }
        const declarations = s.split(";").map((d2) => d2.trim()).filter(Boolean);
        for (const decl of declarations) {
          const colonIdx = decl.indexOf(":");
          if (colonIdx === -1) {
            diagnostics.push(diagnostic(DIAGNOSTIC_CODES.STYLE_VAR_NOT_ALLOWED, `Malformed style declaration: "${decl}"`));
            continue;
          }
          const prop = decl.slice(0, colonIdx).trim();
          const _val = decl.slice(colonIdx + 1).trim();
          if (!SAFE_STYLE_PROPERTIES.has(prop.replace(/^-(webkit|moz)-/i, ""))) {
            diagnostics.push(diagnostic(DIAGNOSTIC_CODES.STYLE_VAR_NOT_ALLOWED, `Style property "${prop}" is not in the allowed list`));
          }
        }
      }
    }
    if ("performance" in rule && rule.performance?.sampleRate !== void 0) {
      const sr2 = rule.performance.sampleRate;
      if (sr2 < 0 || sr2 > 1) {
        diagnostics.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, `sample-rate ${sr2} is out of range (0.0 to 1.0)`));
      }
    }
    if (rule.safety?.minFeatures !== void 0 && (rule.safety.minFeatures < 0 || rule.safety.minFeatures > 10)) {
      diagnostics.push(diagnostic(DIAGNOSTIC_CODES.SAFETY_MIN_FEATURES_MODE_NONE, `min-features ${rule.safety.minFeatures} is out of range (0 to 10)`));
    }
    if (rule.targets.length === 0) {
      diagnostics.push(diagnostic(DIAGNOSTIC_CODES.TARGET_INVALID, "At least one target is required"));
    }
    if (rule.supersedes !== void 0) {
      diagnostics.push(diagnostic(DIAGNOSTIC_CODES.UNSUPPORTED_COMPOSITION, "supersedes is not supported in v1", "warning"));
    }
    if (rule.overrides !== void 0) {
      diagnostics.push(diagnostic(DIAGNOSTIC_CODES.UNSUPPORTED_COMPOSITION, "overrides is not supported in v1", "warning"));
    }
    if (rule.action.action === "style" && rule.action.options?.important) {
    }
    if (rule.action.action !== "style" && rule.action.options?.important) {
      diagnostics.push(diagnostic(DIAGNOSTIC_CODES.IMPORTANT_IGNORED_FOR_BUILT_IN, "actionOptions.important is ignored for non-style actions", "warning"));
    }
    validateSafety(rule.safety, diagnostics);
    validatePerformance(rule.performance, diagnostics);
    validateCache(rule.cache, diagnostics);
    switch (rule.type) {
      case "hide-exact":
        validateHideExact(rule, diagnostics);
        break;
      case "hide-similar":
        validateHideSimilar(rule, diagnostics);
        break;
      case "smart-hide":
        validateSmartHide(rule, diagnostics);
        break;
      case "smart-allow":
        validateSmartAllow(rule, diagnostics);
        break;
    }
    return {
      valid: diagnostics.every((d2) => d2.severity !== "error"),
      diagnostics
    };
  }
  function validateHideExact(rule, diag) {
    validateTargets(rule.targets, diag);
    validatePaths(rule.paths, diag);
    if (!rule.selector) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.EXACT_MODE_SELECTOR_REQUIRED, "hide-exact requires a selector"));
    }
    if (rule.match) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.INVALID_RULE_ACTION_COMBINATION, "hide-exact must not have a match block"));
    }
    if (rule.candidates) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.CANDIDATE_REQUIRED, "hide-exact must not have candidates"));
    }
    if (rule.boundary) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.BOUNDARY_REQUIRED, "hide-exact must not have a boundary block"));
    }
    if (rule.where) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.LOGIC_INVALID, "hide-exact must not have where logic"));
    }
    if (rule.except) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.LOGIC_INVALID, "hide-exact must not have except logic"));
    }
    if (rule.keywords) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.KEYWORDS_INACTIVE, "hide-exact must not have keywords"));
    }
    if (!rule.scope || rule.scope.length === 0) {
      const isUniqueId = rule.selector && rule.selector.startsWith("#") && !rule.selector.includes(" ") && !rule.selector.includes(">");
      if (!isUniqueId) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.EXACT_HIDE_BROAD_BOUNDARY, "hide-exact without scope targets the full document", "warning"));
      }
    }
    if (rule.frames) validateFrames(rule.frames, diag);
    if (rule.runtime) validateRuntime(rule.runtime, diag);
    if (rule.shadow) validateShadow(rule.shadow, diag);
    if (rule.action.action === "unhide") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.INVALID_RULE_ACTION_COMBINATION, "hide-exact does not support action: unhide"));
    }
    if (rule.action.action === "remove") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.ACTION_REMOVE_WARNING, "action: remove can break page scripts", "warning"));
    }
    if (rule.metadata.originalSyntax && rule.metadata.originalSyntax !== rule.selector) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.ORIGINAL_SYNTAX_MISMATCH, "originalSyntax does not match compiled selector", "warning"));
    }
  }
  function validateHideSimilar(rule, diag) {
    validateTargets(rule.targets, diag);
    validatePaths(rule.paths, diag);
    if (!rule.scope || rule.scope.length === 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.MISSING_SCOPE, "hide-similar requires at least one scope selector"));
    }
    validateScope(rule.scope, diag, rule.safety?.warnIfScopeIsBody ?? true);
    if (!rule.candidates || rule.candidates.length === 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.MISSING_CANDIDATES, "hide-similar requires at least one candidate selector"));
    }
    validateCandidates(rule.candidates, diag);
    if (!rule.boundary) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.BOUNDARY_REQUIRED, "hide-similar requires a boundary block"));
    } else {
      validateBoundary(rule.boundary, diag);
    }
    if (rule.match) {
      const matchMode = rule.match.mode;
      if (matchMode === "none") {
        diag.push(diagnostic(DIAGNOSTIC_CODES.INVALID_RULE_ACTION_COMBINATION, "hide-similar does not support match.mode: none"));
      }
      validateMatch(rule.match, rule.type, rule.where, diag, rule.safety?.warnIfWeakExactReference ?? true);
      if (matchMode === "exact" && rule.scope?.some((s) => s === "body" || s === "html")) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.BROAD_RULE_WARNING, "exact match with body/html scope may match too broadly", "warning"));
      }
    } else {
      diag.push(diagnostic(DIAGNOSTIC_CODES.MATCH_MISSING_REFERENCE, "hide-similar requires a match block with a reference"));
    }
    if (rule.frames) validateFrames(rule.frames, diag);
    if (rule.runtime) validateRuntime(rule.runtime, diag);
    if (rule.shadow) validateShadow(rule.shadow, diag);
    if (rule.where) validateRegexCondition(rule.where, diag, "where");
    if (rule.except) validateRegexCondition(rule.except, diag, "except");
    if (rule.action.action === "unhide") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.INVALID_RULE_ACTION_COMBINATION, "hide-similar does not support action: unhide"));
    }
    const warnIfRemove = rule.safety?.warnIfActionRemove ?? true;
    if (rule.action.action === "remove" && warnIfRemove) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.ACTION_REMOVE_WARNING, "action: remove can break page scripts", "warning"));
    }
  }
  function validateSmartHide(rule, diag) {
    validateTargets(rule.targets, diag);
    validatePaths(rule.paths, diag);
    if (!rule.scope || rule.scope.length === 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.MISSING_SCOPE, "smart-hide requires at least one scope selector"));
    }
    validateScope(rule.scope, diag, rule.safety?.warnIfScopeIsBody ?? true);
    if (!rule.candidates || rule.candidates.length === 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.MISSING_CANDIDATES, "smart-hide requires at least one candidate selector"));
    }
    validateCandidates(rule.candidates, diag);
    if (!rule.boundary) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.BOUNDARY_REQUIRED, "smart-hide requires a boundary block"));
    } else {
      validateBoundary(rule.boundary, diag);
    }
    if (rule.match) {
      validateMatch(rule.match, rule.type, rule.where, diag, rule.safety?.warnIfWeakExactReference ?? true);
      if (rule.match.mode === "none" && rule.safety?.minFeatures !== void 0) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.MIN_FEATURES_INVALID_WITH_NONE, "min-features is invalid with match mode none"));
      }
      if (rule.match.mode === "exact" && rule.safety?.minFeatures !== void 0) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.MIN_FEATURES_UNUSED_EXACT, "min-features is unused with match mode exact", "warning"));
      }
      if (rule.match.mode === "exact" && rule.scope?.some((s) => s === "body" || s === "html")) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.BROAD_RULE_WARNING, "exact match with body/html scope may match too broadly", "warning"));
      }
    }
    if (!rule.where && !rule.keywords) {
      const warnIfNoWhere = rule.safety?.warnIfNoWhereLogic ?? true;
      if (rule.match?.mode === "none" && rule.safety?.minLogicMatches !== 0) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.MATCH_MODE_NONE_NO_WHERE, "match mode none without where logic or keywords is too broad; set safety.min-logic-matches: 0 to bypass"));
      } else if (warnIfNoWhere) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.WHERE_MISSING_SMART, "smart-hide without where logic or keywords may be too broad", "warning"));
      }
    }
    if (rule.frames) validateFrames(rule.frames, diag);
    if (rule.runtime) validateRuntime(rule.runtime, diag);
    if (rule.shadow) validateShadow(rule.shadow, diag);
    if (rule.keywords?.wordBoundary && rule.keywords?.matchMode === "regex") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.KEYWORD_WORD_BOUNDARY_IGNORED, "word-boundary is ignored for regex match mode", "warning"));
    }
    if (rule.keywords?.wordBoundary && rule.keywords?.matchMode === "word") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.WORD_BOUNDARY_REDUNDANT, "word-boundary is redundant for word match mode", "warning"));
    }
    if (rule.keywords && !rule.keywords.matchMode) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.KEYWORDS_INACTIVE, "keywords block requires match-mode", "warning"));
    }
    const VALID_KEYWORD_MODES = /* @__PURE__ */ new Set(["phrase", "word", "regex"]);
    if (rule.keywords?.matchMode && !VALID_KEYWORD_MODES.has(rule.keywords.matchMode)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.INVALID_KEYWORD_WORD_MODE, `Invalid keyword match-mode "${rule.keywords.matchMode}"`));
    }
    if (rule.keywords?.matchMode === "regex") {
      const allKw = [...rule.keywords.includeAny || [], ...rule.keywords.includeAll || [], ...rule.keywords.excludeAny || [], ...rule.keywords.excludeAll || []];
      for (const kw of allKw) {
        try {
          new RegExp(kw);
        } catch {
          diag.push(diagnostic(DIAGNOSTIC_CODES.REGEX_UNCOMPILABLE, `Invalid regex keyword: "${kw}"`));
        }
      }
    }
    if (rule.keywords?.matchMode === "word") {
      const allKw = [...rule.keywords.includeAny || [], ...rule.keywords.includeAll || [], ...rule.keywords.excludeAny || [], ...rule.keywords.excludeAll || []];
      for (const kw of allKw) {
        if (/[\s,.;:!?\-'"(){}\[\]/]/.test(kw)) {
          diag.push(diagnostic(DIAGNOSTIC_CODES.INVALID_KEYWORD_WORD_MODE, `Word-mode keyword "${kw}" contains whitespace or punctuation`, "warning"));
        }
      }
    }
    if (rule.boundary?.includeSelf && rule.boundary.mode !== "selector") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.INCLUDE_SELF_NOT_SELECTOR_MODE, "includeSelf requires selector boundary mode"));
    }
    if (rule.boundary?.allowPageRoot && !rule.safety?.confirmPageRoot) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.ALLOW_PAGE_ROOT_WITHOUT_CONFIRM, "allowPageRoot requires safety.confirmPageRoot"));
    }
    if (rule.where) validateRegexCondition(rule.where, diag, "where");
    if (rule.except) validateRegexCondition(rule.except, diag, "except");
    if (rule.keywords && rule.where) {
      if (!rule.keywordMerge) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.KEYWORDS_INACTIVE, "keywords block not merged because explicit where/except exists", "warning"));
      }
    }
    if (rule.keywordMerge && !rule.keywords) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.KEYWORD_MERGE_CONFLICT, "keywordMerge is true but no keywords block provided", "warning"));
    }
    if (rule.action.action === "unhide") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.INVALID_RULE_ACTION_COMBINATION, "smart-hide does not support action: unhide"));
    }
    const warnIfRemove = rule.safety?.warnIfActionRemove ?? true;
    if (rule.action.action === "remove" && warnIfRemove) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.ACTION_REMOVE_WARNING, "action: remove can break page scripts", "warning"));
    }
  }
  function validateSmartAllow(rule, diag) {
    validateTargets(rule.targets, diag);
    validatePaths(rule.paths, diag);
    if (!rule.scope || rule.scope.length === 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.MISSING_SCOPE, "smart-allow requires at least one scope selector"));
    }
    validateScope(rule.scope, diag, rule.safety?.warnIfScopeIsBody ?? true);
    if (!rule.candidates || rule.candidates.length === 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.MISSING_CANDIDATES, "smart-allow requires at least one candidate selector"));
    }
    validateCandidates(rule.candidates, diag);
    if (!rule.boundary) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.MISSING_BOUNDARY, "smart-allow requires a boundary block"));
    } else {
      validateBoundary(rule.boundary, diag);
    }
    if (rule.action.action !== "unhide") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.ACTION_SMART_ALLOW_INVALID, "smart-allow must use action: unhide"));
    }
    if (rule.match && rule.match.mode !== "none" && rule.match.mode !== "exact") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.MATCH_MODE_SMART_ALLOW_INVALID, "smart-allow must use match mode none or exact"));
    }
    if (rule.match) validateMatch(rule.match, rule.type, rule.where, diag, rule.safety?.warnIfWeakExactReference ?? true);
    if (!rule.where && !rule.keywords && (!rule.match || rule.match.mode === "none")) {
      if (!rule.allowBroad) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.CATCH_ALL_ALLOW, "smart-allow with no where/keywords and match mode none is a catch-all; requires allowBroad + enterprise + preview"));
      } else {
        diag.push(diagnostic(DIAGNOSTIC_CODES.ALLOW_BROAD_EXPECTED, "broad smart-allow with allowBroad requires enterprise policy", "warning"));
      }
    }
    if (rule.frames) validateFrames(rule.frames, diag);
    if (rule.runtime) validateRuntime(rule.runtime, diag);
    if (rule.shadow) validateShadow(rule.shadow, diag);
    if (rule.keywords?.wordBoundary && rule.keywords?.matchMode === "regex") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.KEYWORD_WORD_BOUNDARY_IGNORED, "word-boundary is ignored for regex match mode", "warning"));
    }
    if (rule.keywords?.wordBoundary && rule.keywords?.matchMode === "word") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.WORD_BOUNDARY_REDUNDANT, "word-boundary is redundant for word match mode", "warning"));
    }
    if (rule.keywords && !rule.keywords.matchMode) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.KEYWORDS_INACTIVE, "keywords block requires match-mode", "warning"));
    }
    const VALID_KEYWORD_MODES = /* @__PURE__ */ new Set(["phrase", "word", "regex"]);
    if (rule.keywords?.matchMode && !VALID_KEYWORD_MODES.has(rule.keywords.matchMode)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.INVALID_KEYWORD_WORD_MODE, `Invalid keyword match-mode "${rule.keywords.matchMode}"`));
    }
    if (rule.keywords?.matchMode === "regex") {
      const allKw = [...rule.keywords.includeAny || [], ...rule.keywords.includeAll || [], ...rule.keywords.excludeAny || [], ...rule.keywords.excludeAll || []];
      for (const kw of allKw) {
        try {
          new RegExp(kw);
        } catch {
          diag.push(diagnostic(DIAGNOSTIC_CODES.REGEX_UNCOMPILABLE, `Invalid regex keyword: "${kw}"`));
        }
      }
    }
    if (rule.where) validateRegexCondition(rule.where, diag, "where");
    if (rule.except) validateRegexCondition(rule.except, diag, "except");
  }
  function validateRegexPattern(pattern, diag, context) {
    try {
      new RegExp(pattern);
    } catch (e) {
      console.warn("[uBR] smart-rule-validator: invalid regex in", context, pattern, e);
      diag.push(diagnostic(DIAGNOSTIC_CODES.REGEX_UNCOMPILABLE, `Invalid regex in ${context}: ${pattern}`));
      return;
    }
    const safe = isRegexPatternSafe(pattern);
    if (!safe.safe) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.REGEX_STATIC_REJECTED, `Unsafe regex in ${context}: ${safe.reason}`));
    }
  }
  function validateRegexCondition(expr, diag, context) {
    if ("condition" in expr && "operator" in expr.condition) {
      const cond = expr.condition;
      if (cond.operator === "regex" && cond.pattern) {
        validateRegexPattern(cond.pattern, diag, context);
        if (cond.flags) {
          for (const err of validateRegexFlags(cond.flags)) {
            diag.push(diagnostic(DIAGNOSTIC_CODES.REGEX_UNSUPPORTED_FLAG, err, "warning"));
          }
        }
      }
      return;
    }
    const group = expr;
    for (const key of ["all", "any", "none"]) {
      const arr = group[key];
      if (arr) {
        for (const child of arr) {
          validateRegexCondition(child, diag, context);
        }
      }
    }
  }
  function validateTargets(targets, diag) {
    for (const t of targets) {
      if (!VALID_TARGET_FORMS.has(t.form)) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.TARGET_INVALID, `Invalid target form "${t.form}"`));
      }
      if (t.form === "regex") {
        validateRegexPattern(t.value, diag, `target ${t.value}`);
      }
    }
  }
  function validatePaths(paths, diag) {
    if (!paths) return;
    for (const p of paths) {
      if (!VALID_PATH_FORMS.has(p.form)) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.PATH_INVALID, `Invalid path form "${p.form}"`));
      }
      if (p.form === "regex") {
        validateRegexPattern(p.value, diag, `path ${p.value}`);
      }
    }
  }
  function validateScope(scope, diag, warnIfScopeIsBody = true) {
    if (!scope) return;
    for (const s of scope) {
      if (s === "body" || s === "html") {
        if (warnIfScopeIsBody) {
          diag.push(diagnostic(DIAGNOSTIC_CODES.SCOPE_IS_BODY, `Scope "${s}" is risky for generalized rules`, "warning"));
        }
        diag.push(diagnostic(DIAGNOSTIC_CODES.BROAD_EXACT_SCOPE, `Scope "${s}" is broad`, "warning"));
      }
      if (!isSafeSelector(s)) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.UNSAFE_SCOPE_SELECTOR, `Unsafe selector in scope: ${s}`));
      }
    }
  }
  function validateCandidates(candidates, diag) {
    if (!candidates) return;
    for (const c of candidates) {
      if (!isSafeSelector(c)) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.UNSAFE_CANDIDATE_SELECTOR, `Unsafe candidate selector: ${c}`));
      }
    }
  }
  function validateBoundary(boundary, diag) {
    const validModes = /* @__PURE__ */ new Set(["exact", "nearest-card", "repeated-card", "semantic-block", "visual-block", "ancestor-depth", "selector"]);
    if (!validModes.has(boundary.mode)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.BOUNDARY_INVALID, `Invalid boundary mode "${boundary.mode}"`));
    }
    if (boundary.stopAtScope !== false && boundary.allowCrossScope === true) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.BOUNDARY_CROSS_SCOPE_CONFLICT, "allow-cross-scope: true overrides stop-at-scope to false", "warning"));
    }
    if (boundary.mode === "ancestor-depth" && boundary.depth === void 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.BOUNDARY_MODE_MISMATCH, "ancestor-depth mode requires depth field", "error"));
    }
    if (boundary.mode === "selector" && !boundary.selector) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.BOUNDARY_MODE_MISMATCH, "selector mode requires a selector field", "error"));
    }
    if (boundary.depth !== void 0 && boundary.depth < 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.INVALID_BOUNDARY_DEPTH, "depth must not be negative"));
    }
    if (boundary.maxDepth !== void 0 && boundary.maxDepth < 1) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.BOUNDARY_INVALID, "maxDepth must be a positive integer"));
    }
    if (boundary.depth !== void 0 && boundary.maxDepth !== void 0 && boundary.depth >= boundary.maxDepth) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.INVALID_BOUNDARY_DEPTH, "depth must be less than maxDepth"));
    }
    if (boundary.mode === "exact") {
      if (boundary.selector || boundary.depth !== void 0 || boundary.maxDepth !== void 0) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.BOUNDARY_MODE_MISMATCH, "exact boundary mode does not use selector, depth, or maxDepth"));
      }
    }
    if (boundary.allowScopeRoot === true && boundary.stopAtScope === void 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.ALLOW_SCOPE_ROOT_WITHOUT_STOP, "allow-scope-root requires explicit stop-at-scope: true"));
    }
    if (boundary.allowScopeRoot === true && boundary.stopAtScope === false) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.ALLOW_SCOPE_ROOT_WITHOUT_STOP, "allow-scope-root requires stop-at-scope: true"));
    }
  }
  function validateFrames(frames, diag) {
    if (!frames) return;
    const validModes = /* @__PURE__ */ new Set(["top-only", "same-origin", "accessible"]);
    if (!validModes.has(frames.mode)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.FRAMES_INVALID, `Invalid frames mode "${frames.mode}"`));
    }
    if (frames.accounting && frames.accounting !== "per-frame" && frames.accounting !== "aggregate") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.FRAMES_INVALID, `Invalid frames accounting "${frames.accounting}"`));
    }
    if (frames.maxTotalMatches !== void 0 && frames.maxTotalMatches < 1) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.FRAMES_INVALID, "maxTotalMatches must be a positive integer"));
    }
  }
  function validateRuntime(runtime, diag) {
    if (!runtime) return;
    if (runtime.reEvaluateOnPathChange && !["never", "always", "smart"].includes(runtime.reEvaluateOnPathChange)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.RUNTIME_INVALID, `Invalid reEvaluateOnPathChange "${runtime.reEvaluateOnPathChange}"`));
    }
    if (runtime.errorRecovery && !["continue", "fail-safe"].includes(runtime.errorRecovery)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.RUNTIME_INVALID, `Invalid errorRecovery "${runtime.errorRecovery}"`));
    }
    if (runtime.onBudgetExceeded && !["stop-cycle", "warn", "pause-rule"].includes(runtime.onBudgetExceeded)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.RUNTIME_INVALID, `Invalid onBudgetExceeded "${runtime.onBudgetExceeded}"`));
    }
    if (runtime.onBudgetExceeded === "warn") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.RUNTIME_INVALID, "onBudgetExceeded: warn is restricted to preview mode", "warning"));
    }
    if (runtime.observePathChanges !== void 0 && typeof runtime.observePathChanges !== "boolean") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.RUNTIME_INVALID, "observePathChanges must be a boolean"));
    }
    if (runtime.observeSubtree !== void 0 && typeof runtime.observeSubtree !== "boolean") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.RUNTIME_INVALID, "observeSubtree must be a boolean"));
    }
    if (runtime.observeAttributes !== void 0 && runtime.observeAttributes !== "auto" && runtime.observeAttributes !== true && runtime.observeAttributes !== false) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.RUNTIME_INVALID, "observeAttributes must be auto, true, or false"));
    }
    if (runtime.pathChangeDebounceMs !== void 0 && runtime.pathChangeDebounceMs < 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.RUNTIME_INVALID, "pathChangeDebounceMs must not be negative"));
    }
  }
  function validateShadow(shadow, diag) {
    const validModes = /* @__PURE__ */ new Set(["none", "open", "open-recursive"]);
    if (!validModes.has(shadow.mode)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.UNSAFE_SHADOW_SELECTOR, `Invalid shadow mode "${shadow.mode}"`));
      return;
    }
    if (shadow.mode === "none") {
      if (shadow.observeRecursive === true) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.SHADOW_INVALID, "observeRecursive is unused when shadow mode is none", "warning"));
      }
      if (shadow.allowHostAncestor === true) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.SHADOW_INVALID, "allowHostAncestor is unused when shadow mode is none", "warning"));
      }
    }
    if (shadow.mode === "open-recursive" && shadow.observeRecursive === false) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.SHADOW_INVALID, "observeRecursive conflicts with open-recursive mode", "warning"));
    }
    if (shadow.mode !== "none" && shadow.mode !== "open-recursive" && shadow.observeRecursive === true) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.SHADOW_INVALID, "observeRecursive requires open-recursive mode", "warning"));
    }
    if (shadow.observeMutations !== void 0 && typeof shadow.observeMutations !== "boolean") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.SHADOW_INVALID, "observeMutations must be a boolean"));
    }
    if (shadow.allowHostAncestor !== void 0 && typeof shadow.allowHostAncestor !== "boolean") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.SHADOW_INVALID, "allowHostAncestor must be a boolean"));
    }
  }
  function validateCache(cache, diag) {
    if (!cache) return;
    if (cache.scope && cache.scope !== "per-rule" && cache.scope !== "per-page") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, `Invalid cache scope "${cache.scope}"`));
    }
    if (cache.evictionPolicy && cache.evictionPolicy !== "LRU") {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, `Invalid cache evictionPolicy "${cache.evictionPolicy}"`));
    }
    if (cache.maxEntries !== void 0 && cache.maxEntries < 1) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "cache maxEntries must be a positive integer"));
    }
    if (cache.maxAgeMs !== void 0 && cache.maxAgeMs < 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "cache maxAgeMs must not be negative"));
    }
  }
  function validateSafety(safety, diag) {
    if (!safety) return;
    if (safety.preview && !["required", "recommended", "optional", "skip"].includes(safety.preview)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.SAFETY_PREVIEW_REQUIRED, `Invalid safety preview "${safety.preview}"`));
    }
    if (safety.maxMatches !== void 0 && safety.maxMatches < 1) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "safety maxMatches must be a positive integer"));
    }
    if (safety.maxPagePercent !== void 0 && (safety.maxPagePercent < 1 || safety.maxPagePercent > 100)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "safety maxPagePercent must be 1-100"));
    }
    if (safety.maxViewportAreaPercent !== void 0 && (safety.maxViewportAreaPercent < 1 || safety.maxViewportAreaPercent > 100)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "safety maxViewportAreaPercent must be 1-100"));
    }
    if (safety.minLogicMatches !== void 0 && safety.minLogicMatches < 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.SAFETY_MIN_LOGIC_MATCHES_IMPOSSIBLE, "safety minLogicMatches must not be negative"));
    }
  }
  function validatePerformance(performance2, diag) {
    if (!performance2) return;
    if (performance2.maxCandidates !== void 0 && performance2.maxCandidates < 1) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "performance maxCandidates must be a positive integer"));
    }
    if (performance2.maxEvaluationsPerCycle !== void 0 && performance2.maxEvaluationsPerCycle < 1) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "performance maxEvaluationsPerCycle must be a positive integer"));
    }
    if (performance2.maxAddedNodesPerCycle !== void 0 && performance2.maxAddedNodesPerCycle < 1) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "performance maxAddedNodesPerCycle must be a positive integer"));
    }
    if (performance2.maxRegexMsPerRuleCycle !== void 0 && performance2.maxRegexMsPerRuleCycle < 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "performance maxRegexMsPerRuleCycle must not be negative"));
    }
    if (performance2.maxRegexPatternLength !== void 0 && performance2.maxRegexPatternLength < 1) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "performance maxRegexPatternLength must be a positive integer"));
    }
    if (performance2.maxTextRegexInputChars !== void 0 && performance2.maxTextRegexInputChars < 1) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "performance maxTextRegexInputChars must be a positive integer"));
    }
    if (performance2.debounceMs !== void 0 && performance2.debounceMs < 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "performance debounceMs must not be negative"));
    }
    if (performance2.dependencyDepth !== void 0 && performance2.dependencyDepth < 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, "performance dependencyDepth must not be negative"));
    }
    if (performance2.metrics && !["none", "collect"].includes(performance2.metrics)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.PERFORMANCE_INVALID, `Invalid metrics mode "${performance2.metrics}"`));
    }
  }
  function validateMatch(match, ruleType, where, diag, warnIfWeakExactReference = true) {
    if (match.mode === "none") {
      if (match.threshold !== void 0) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.THRESHOLD_NOT_VALID_FOR_MODE_NONE, "match.mode: none must not have threshold"));
      }
      if (match.reference !== void 0 && match.reference !== "none") {
        diag.push(diagnostic(DIAGNOSTIC_CODES.MATCH_MODE_NONE_WITH_REFERENCE, "match.mode: none must not have reference"));
      }
      if (match.weights !== void 0) {
        diag.push(diagnostic(DIAGNOSTIC_CODES.MATCH_MODE_NONE_WITH_WEIGHTS, "match.mode: none must not have weights"));
      }
      return;
    }
    if (match.mode === "similar" || match.mode === "structural") {
      if (!match.reference || match.reference === "none") {
        diag.push(diagnostic(
          match.mode === "structural" ? DIAGNOSTIC_CODES.MATCH_STRUCTURAL_WITHOUT_REFERENCE : DIAGNOSTIC_CODES.MATCH_SIMILAR_WITHOUT_REFERENCE,
          `match.mode: ${match.mode} requires a reference`
        ));
      }
    }
    if (match.referenceSelection && (match.reference === "picked" || match.reference === "none" || typeof match.reference === "string" && match.reference.startsWith("fingerprint:"))) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.REFERENCE_SELECTION_UNUSED, "reference-selection is unused for this reference type", "warning"));
    }
    if (match.mode === "exact" && match.threshold !== void 0) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.MATCH_MODE_EXACT_WITH_THRESHOLD, "match.mode: exact must not have threshold"));
    }
    if (match.mode === "exact" && (!match.reference || match.reference === "none") && warnIfWeakExactReference) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.MATCH_WEAK_EXACT_REFERENCE, "exact match mode requires a reference (picked or custom selector)", "warning"));
    }
    if (match.weights && !VALID_WEIGHT_PROFILES.has(match.weights)) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.CUSTOM_WEIGHTS_NOT_SUPPORTED_V1, `Custom weight profile "${match.weights}" is not supported in v1`));
    }
    if (match.threshold !== void 0 && match.threshold < 0.65) {
      diag.push(diagnostic(DIAGNOSTIC_CODES.LOW_SIMILARITY_THRESHOLD, `Low similarity threshold ${match.threshold} requires preview`, "warning"));
    }
  }
  var UNSAFE_SELECTOR_PATTERNS, ALLOWED_REGEX_FLAGS, REJECTED_REGEX_FLAGS, VALID_WEIGHT_PROFILES, MAX_REGEX_PATTERN_LENGTH, NESTED_QUANTIFIER_RE, BACKREFERENCE_RE, NAMED_BACKREFERENCE_RE, CATASTROPHIC_ALTERNATION_RE, LOOKBEHIND_RE, FILTER_FUNCTION_RE, FILTER_PERCENTAGE_RE, VALID_TARGET_FORMS, VALID_PATH_FORMS;
  var init_smart_rule_validator = __esm({
    "src/core/smart-cosmetic/smart-rule-validator.ts"() {
      init_smart_rule_diagnostics();
      init_smart_rule_schema();
      UNSAFE_SELECTOR_PATTERNS = [
        /:has\(/,
        /:has-text\(/,
        /:contains\(/,
        /:matches-css\(/,
        /:matches-property\(/,
        /:upward\(/,
        /:xpath\(/,
        /:host-context\(/,
        /::(before|after|part|slotted)/,
        /\\b/
      ];
      ALLOWED_REGEX_FLAGS = /* @__PURE__ */ new Set(["i", "m", "u", "s", "g"]);
      REJECTED_REGEX_FLAGS = /* @__PURE__ */ new Set(["v", "y", "d"]);
      VALID_WEIGHT_PROFILES = /* @__PURE__ */ new Set(["default-card", "structural-heavy", "content-heavy"]);
      MAX_REGEX_PATTERN_LENGTH = 256;
      NESTED_QUANTIFIER_RE = /\([^)]+\)[?*+][?*+{]/;
      BACKREFERENCE_RE = /\\(\d+)/;
      NAMED_BACKREFERENCE_RE = /\\k<[^>]+>/;
      CATASTROPHIC_ALTERNATION_RE = /\(\s*[^)]+\|\s*[^)]+\|\s*[^)]*\)[+*]/;
      LOOKBEHIND_RE = /\(\?<[=!]/;
      FILTER_FUNCTION_RE = /\bfilter\s*:\s*[^;]+/gi;
      FILTER_PERCENTAGE_RE = /([a-z-]+)\s*\(\s*\d+(\.\d+)?%/i;
      VALID_TARGET_FORMS = /* @__PURE__ */ new Set(["host", "domain", "entity", "regex"]);
      VALID_PATH_FORMS = /* @__PURE__ */ new Set(["exact", "glob", "regex"]);
    }
  });

  // src/core/smart-cosmetic/smart-rule-store.ts
  var smart_rule_store_exports = {};
  __export(smart_rule_store_exports, {
    SmartRuleStore: () => smart_rule_store_exports,
    Store: () => Store,
    smartRuleStore: () => smartRuleStore
  });
  async function migrateUserCosmeticFilters(storageApi) {
    const bin = await storageApi.get([STORAGE_KEY_MIGRATION, USER_FILTERS_KEY, STORAGE_KEY_RULES]);
    if (bin[STORAGE_KEY_MIGRATION] === true) return;
    const userFilters = bin[USER_FILTERS_KEY] || "";
    const lines = userFilters.split("\n");
    const cosmeticLines = [];
    const networkLines = [];
    for (const line of lines) {
      if (line.includes("##")) {
        cosmeticLines.push(line);
      } else {
        networkLines.push(line);
      }
    }
    if (cosmeticLines.length > 0) {
      const existingRules = bin[STORAGE_KEY_RULES] || {};
      for (const line of cosmeticLines) {
        const hashIdx = line.indexOf("##");
        const domainPart = hashIdx > 0 ? line.slice(0, hashIdx) : "*";
        const selector = line.slice(hashIdx + 2);
        if (!selector) continue;
        const id = `migrated:${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
        if (existingRules[id]) continue;
        const targets = domainPart === "*" ? [{ form: "host", value: "*" }] : domainPart.split(",").filter(Boolean).map((d2) => ({ form: "host", value: d2 }));
        existingRules[id] = {
          type: "hide-exact",
          id,
          syntaxVersion: 1,
          state: "active",
          targets,
          selector,
          action: { action: "hide" },
          metadata: { createdAt: (/* @__PURE__ */ new Date()).toISOString(), source: "migration" },
          collectionId: "migrated-cosmetic"
        };
      }
      await storageApi.set({ [STORAGE_KEY_RULES]: existingRules, [USER_FILTERS_KEY]: networkLines.join("\n") });
    }
    await storageApi.set({ [STORAGE_KEY_MIGRATION]: true });
  }
  function migrateRule(raw) {
    if (!raw) return;
    if (raw.performance && typeof raw.performance.metrics === "object" && raw.performance.metrics !== null) {
      const nested = raw.performance.metrics;
      raw.performance.metrics = typeof nested.metrics === "string" ? nested.metrics : "collect";
      if (typeof nested.sampleRate === "number" && raw.performance.sampleRate === void 0) {
        raw.performance.sampleRate = nested.sampleRate;
      }
    }
    if (raw.performance?.metrics === "debug") {
      raw.performance.metrics = "collect";
    }
    if (raw.performance) {
      delete raw.performance.cacheFeatureVectors;
    }
    if (raw.action?.options) {
      delete raw.action.options.styleWhitelist;
      delete raw.action.options.markOutlineMerge;
    }
    if (raw.boundary) {
      delete raw.boundary.ancestorDepth;
      if (raw.boundary.allowCrossScope === true) {
        raw.boundary.stopAtScope = false;
      }
    }
    if (raw.shadow) {
      delete raw.shadow.observeSubtree;
    }
    if (raw.runtime) {
      delete raw.runtime.observeAttributesTree;
    }
  }
  var STORAGE_KEY_RULES, STORAGE_KEY_COLLECTIONS, STORAGE_KEY_INDEX, STORAGE_KEY_MIGRATION, USER_FILTERS_KEY, Store, smartRuleStore;
  var init_smart_rule_store = __esm({
    "src/core/smart-cosmetic/smart-rule-store.ts"() {
      init_smart_rule_schema();
      init_matcher();
      init_smart_rule_validator();
      init_smart_rule_store();
      STORAGE_KEY_RULES = "smartCosmeticRules";
      STORAGE_KEY_COLLECTIONS = "smartCosmeticCollections";
      STORAGE_KEY_INDEX = "smartCosmeticIndex";
      STORAGE_KEY_MIGRATION = "cosmeticMigrationDone";
      USER_FILTERS_KEY = "user-filters";
      Store = class {
        constructor() {
          __publicField(this, "rules", /* @__PURE__ */ new Map());
          __publicField(this, "collections", /* @__PURE__ */ new Map());
          __publicField(this, "index", { byTarget: {}, byHost: {}, byType: {}, byCollection: {} });
          __publicField(this, "loaded", false);
          __publicField(this, "storageApi", typeof chrome !== "undefined" && chrome.storage?.local ? chrome.storage.local : { get: async () => ({}), set: async () => {
          }, remove: async () => {
          }, clear: async () => {
          } });
        }
        async load() {
          if (this.loaded) return;
          const { storageApi } = this;
          await migrateUserCosmeticFilters(storageApi);
          const bin = await storageApi.get([STORAGE_KEY_RULES, STORAGE_KEY_COLLECTIONS, STORAGE_KEY_INDEX]);
          const rawRules = bin[STORAGE_KEY_RULES] || {};
          for (const [id, raw] of Object.entries(rawRules)) {
            if (raw.state) raw.state = normalizeRuleState(raw.state);
            migrateRule(raw);
            this.rules.set(id, raw);
          }
          const rawCollections = bin[STORAGE_KEY_COLLECTIONS] || {};
          for (const [id, raw] of Object.entries(rawCollections)) {
            this.collections.set(id, raw);
          }
          this.index = bin[STORAGE_KEY_INDEX] || { byTarget: {}, byHost: {}, byType: {}, byCollection: {} };
          this.loaded = true;
        }
        assertLoaded() {
          if (!this.loaded) throw new Error("SmartRuleStore not loaded. Call load() first.");
        }
        async save() {
          return this.persist();
        }
        async persist() {
          const rules = {};
          this.rules.forEach((v2, k2) => {
            rules[k2] = v2;
          });
          const collections = {};
          this.collections.forEach((v2, k2) => {
            collections[k2] = v2;
          });
          await this.storageApi.set({
            [STORAGE_KEY_RULES]: rules,
            [STORAGE_KEY_COLLECTIONS]: collections,
            [STORAGE_KEY_INDEX]: this.index
          });
        }
        // --- Rule CRUD ---
        async addRule(rule) {
          this.assertLoaded();
          rule.state = normalizeRuleState(rule.state);
          const vr2 = validateRule(rule);
          if (!vr2.valid) return { ok: false, validation: vr2 };
          if (this.rules.has(rule.id)) {
            return { ok: false, validation: { valid: false, diagnostics: [{ code: "rule-id-conflict", message: `Rule ${rule.id} already exists`, severity: "error" }] } };
          }
          this.rules.set(rule.id, rule);
          this.indexRule(rule);
          await this.persist();
          return { ok: true };
        }
        async updateRule(id, updates) {
          this.assertLoaded();
          const existing = this.rules.get(id);
          if (!existing) return { ok: false, validation: { valid: false, diagnostics: [{ code: "rule-not-found", message: `Rule ${id} not found`, severity: "error" }] } };
          const merged = { ...existing, ...updates, id };
          merged.state = normalizeRuleState(merged.state);
          const vr2 = validateRule(merged);
          if (!vr2.valid) return { ok: false, validation: vr2 };
          this.unindexRule(existing);
          this.rules.set(id, merged);
          this.indexRule(merged);
          await this.persist();
          return { ok: true };
        }
        collectActiveSelectors() {
          const selectors = [];
          for (const rule of this.rules.values()) {
            if (rule.state !== "active") continue;
            const s = "selector" in rule ? rule.selector : void 0;
            if (s && typeof s === "string") selectors.push(s);
            const c = "candidates" in rule ? rule.candidates : void 0;
            if (Array.isArray(c)) selectors.push(...c);
          }
          return selectors;
        }
        async removeRule(id) {
          this.assertLoaded();
          const existing = this.rules.get(id);
          if (!existing) return false;
          this.unindexRule(existing);
          this.rules.delete(id);
          await this.persist();
          triggerFingerprintGC(this.collectActiveSelectors());
          return true;
        }
        async setRuleState(id, state) {
          this.assertLoaded();
          const rule = this.rules.get(id);
          if (!rule) return false;
          const fromState = normalizeRuleState(rule.state);
          const toState = normalizeRuleState(state);
          if (!isValidStateTransition(fromState, toState, rule)) return false;
          rule.state = toState;
          await this.persist();
          return true;
        }
        getRule(id) {
          this.assertLoaded();
          return this.rules.get(id);
        }
        getAllRules() {
          this.assertLoaded();
          return Array.from(this.rules.values());
        }
        getRulesByType(type) {
          this.assertLoaded();
          return (this.index.byType[type] || []).map((id) => this.rules.get(id)).filter((r) => r !== void 0);
        }
        getRulesByHost(hostname) {
          this.assertLoaded();
          const keys = /* @__PURE__ */ new Set();
          for (const h2 of [hostname, hostname.split(".").slice(-2).join("."), "*"]) {
            const ids = this.index.byHost[h2] || [];
            for (const id of ids) keys.add(id);
          }
          return Array.from(keys).map((id) => this.rules.get(id)).filter((r) => r !== void 0);
        }
        getRulesByCollection(collectionId) {
          this.assertLoaded();
          return (this.index.byCollection[collectionId] || []).map((id) => this.rules.get(id)).filter((r) => r !== void 0);
        }
        // --- Collection CRUD ---
        async addCollection(collection) {
          this.assertLoaded();
          if (this.collections.has(collection.id)) return false;
          this.collections.set(collection.id, collection);
          await this.persist();
          return true;
        }
        async removeCollection(id) {
          this.assertLoaded();
          if (!this.collections.has(id)) return false;
          const ruleIds = this.index.byCollection[id] || [];
          for (const ruleId of ruleIds) {
            const rule = this.rules.get(ruleId);
            if (rule) {
              this.unindexRule(rule);
              this.rules.delete(ruleId);
            }
          }
          this.collections.delete(id);
          await this.persist();
          triggerFingerprintGC(this.collectActiveSelectors());
          return true;
        }
        async updateCollection(id, updates) {
          this.assertLoaded();
          const existing = this.collections.get(id);
          if (!existing) return false;
          this.collections.set(id, { ...existing, ...updates });
          await this.persist();
          return true;
        }
        getCollection(id) {
          this.assertLoaded();
          return this.collections.get(id);
        }
        getAllCollections() {
          this.assertLoaded();
          return Array.from(this.collections.values());
        }
        // --- Bulk operations ---
        async replaceCollectionRules(collectionId, rules, collection) {
          this.assertLoaded();
          const oldIds = [...this.index.byCollection[collectionId] || []];
          for (const id of oldIds) {
            const old = this.rules.get(id);
            if (old) this.unindexRule(old);
            this.rules.delete(id);
          }
          for (const rule of rules) {
            this.rules.set(rule.id, rule);
            this.indexRule(rule);
          }
          this.collections.set(collectionId, collection);
          await this.persist();
          triggerFingerprintGC(this.collectActiveSelectors());
        }
        async clearAll() {
          this.rules.clear();
          this.collections.clear();
          this.index = { byTarget: {}, byHost: {}, byType: {}, byCollection: {} };
          await this.persist();
          triggerFingerprintGC(this.collectActiveSelectors());
        }
        getStats() {
          this.assertLoaded();
          const byType = {};
          const byState = {};
          for (const rule of this.rules.values()) {
            byType[rule.type] = (byType[rule.type] || 0) + 1;
            byState[rule.state] = (byState[rule.state] || 0) + 1;
          }
          return { ruleCount: this.rules.size, collectionCount: this.collections.size, byType, byState };
        }
        // --- Indexing (internal) ---
        indexRule(rule) {
          for (const target of rule.targets) {
            const key = `${target.form}:${target.value}`;
            this.addToIndex(this.index.byTarget, key, rule.id);
            if (target.form === "host" || target.form === "domain") {
              this.addToIndex(this.index.byHost, target.value, rule.id);
            }
          }
          this.addToIndex(this.index.byType, rule.type, rule.id);
          if (rule.collectionId) {
            this.addToIndex(this.index.byCollection, rule.collectionId, rule.id);
          }
        }
        unindexRule(rule) {
          for (const target of rule.targets) {
            const key = `${target.form}:${target.value}`;
            this.removeFromIndex(this.index.byTarget, key, rule.id);
            if (target.form === "host" || target.form === "domain") {
              this.removeFromIndex(this.index.byHost, target.value, rule.id);
            }
          }
          this.removeFromIndex(this.index.byType, rule.type, rule.id);
          if (rule.collectionId) {
            this.removeFromIndex(this.index.byCollection, rule.collectionId, rule.id);
          }
        }
        addToIndex(idx, key, id) {
          if (!idx[key]) idx[key] = [];
          if (!idx[key].includes(id)) idx[key].push(id);
        }
        removeFromIndex(idx, key, id) {
          if (!idx[key]) return;
          const i = idx[key].indexOf(id);
          if (i !== -1) idx[key].splice(i, 1);
          if (idx[key].length === 0) delete idx[key];
        }
      };
      smartRuleStore = new Store();
    }
  });

  // src/core/smart-cosmetic/smart-rule-parser.ts
  var smart_rule_parser_exports = {};
  __export(smart_rule_parser_exports, {
    extractRulesFromParsed: () => extractRulesFromParsed,
    parseSmartRules: () => parseSmartRules,
    parseYaml: () => parseYaml,
    parseYamlLines: () => parseYamlLines
  });
  function inferScalar(raw) {
    const trimmed = raw.trim();
    if (trimmed === "null" || trimmed === "~") return null;
    if (trimmed === "true") return true;
    if (trimmed === "false") return false;
    if (/^-?\d+(\.\d+)?$/.test(trimmed)) {
      return trimmed.includes(".") ? parseFloat(trimmed) : parseInt(trimmed, 10);
    }
    return trimmed;
  }
  function unquote(raw) {
    const s = raw.trim();
    if (s.startsWith("'") && s.endsWith("'") || s.startsWith('"') && s.endsWith('"')) {
      return s.slice(1, -1).replace(/\\"/g, '"').replace(/\\'/g, "'");
    }
    return s;
  }
  function getAssignableMap(top) {
    if (top.map) return top.map;
    if (top.list && top.list.length > 0) {
      const last = top.list[top.list.length - 1];
      if (typeof last === "object" && !Array.isArray(last) && last !== null) {
        return last;
      }
    }
    return null;
  }
  function parseYamlLines(lines) {
    const errors = [];
    const root = {};
    const stack = [{ indent: -1, parentKey: null, parent: null, map: root, list: null }];
    let i = 0;
    while (i < lines.length) {
      const raw = lines[i];
      i++;
      const trimmed = raw.trim();
      if (trimmed === "" || trimmed.startsWith("#")) continue;
      const indent2 = raw.length - raw.trimStart().length;
      while (stack.length > 1 && indent2 <= stack[stack.length - 1].indent) {
        stack.pop();
      }
      const top = stack[stack.length - 1];
      if (trimmed.startsWith("- ")) {
        const afterDash = trimmed.slice(2);
        if (top.list === null) {
          if (top.parent && top.parentKey && top.map) {
            const list = [];
            top.parent.map[top.parentKey] = list;
            top.map = null;
            top.list = list;
          } else {
            errors.push({ line: i, message: "List without parent key" });
            continue;
          }
        }
        const nestedColonIdx = afterDash.indexOf(":");
        const hasSpaceBeforeColon = afterDash.slice(0, nestedColonIdx).includes(" ");
        if (nestedColonIdx > 0 && !hasSpaceBeforeColon) {
          const itemKey = afterDash.slice(0, nestedColonIdx).trim();
          const itemVal = afterDash.slice(nestedColonIdx + 1).trim();
          if (itemVal === "") {
            const itemMap = {};
            top.list.push(itemMap);
            stack.push({ indent: indent2, parentKey: null, parent: top, map: itemMap, list: null });
          } else {
            const item = {};
            item[itemKey] = inferScalar(itemVal);
            top.list.push(item);
          }
        } else {
          top.list.push(inferScalar(afterDash));
        }
        continue;
      }
      const colonIdx = trimmed.indexOf(":");
      if (colonIdx === -1) {
        errors.push({ line: i, message: `Expected key: value on line ${i}: "${trimmed}"` });
        continue;
      }
      const key = trimmed.slice(0, colonIdx).trim();
      const rest = trimmed.slice(colonIdx + 1).trim();
      const target = getAssignableMap(top);
      if (!target) {
        errors.push({ line: i, message: `Cannot assign key "${key}" in current context` });
        continue;
      }
      if (rest === "") {
        const childMap = {};
        target[key] = childMap;
        stack.push({ indent: indent2, parentKey: key, parent: top, map: childMap, list: null });
        continue;
      }
      if (rest.startsWith("|")) {
        const blockLines = [];
        while (i < lines.length) {
          const nextRaw = lines[i];
          const nextTrimmed = nextRaw.trim();
          if (nextTrimmed === "" || nextTrimmed.startsWith("#")) {
            i++;
            continue;
          }
          const nextIndent = nextRaw.length - nextRaw.trimStart().length;
          if (nextIndent <= indent2) break;
          blockLines.push(nextTrimmed);
          i++;
        }
        target[key] = blockLines.join("\n");
        continue;
      }
      if (rest.startsWith("[")) {
        const items = rest.slice(1, -1).split(",").map((s) => inferScalar(s.trim()));
        target[key] = items;
        continue;
      }
      if (rest.startsWith("'") || rest.startsWith('"')) {
        target[key] = unquote(rest);
        continue;
      }
      if (rest.startsWith("/") && rest.length > 1) {
        target[key] = rest;
        continue;
      }
      target[key] = inferScalar(rest);
    }
    return { value: root, errors };
  }
  function parseYaml(text) {
    return parseYamlLines(text.split(/\r?\n/));
  }
  function extractRulesFromParsed(parsed) {
    const errors = [];
    const rules = [];
    if (typeof parsed !== "object" || parsed === null) {
      errors.push({ line: 0, message: "Expected a mapping at root" });
      return { rules, errors };
    }
    const root = parsed;
    if ("ubr-smart-rules" in root || "ubrSmartRules" in root) {
      const container = root["ubr-smart-rules"] || root["ubrSmartRules"];
      if (container && typeof container === "object" && "rules" in container) {
        const items = container.rules;
        if (Array.isArray(items)) {
          for (const item of items) {
            if (typeof item !== "object" || item === null) continue;
            const obj = item;
            const ruleType = findRuleType(obj);
            if (ruleType) {
              rules.push({ type: ruleType, data: obj });
            } else {
              errors.push({ line: 0, message: "Rule object does not contain a recognized rule type" });
            }
          }
          return { rules, errors };
        }
      }
      return { rules, errors };
    }
    const singleType = findRuleType(root);
    if (singleType) {
      rules.push({ type: singleType, data: root });
      return { rules, errors };
    }
    errors.push({ line: 0, message: "No recognized rule type found in input" });
    return { rules, errors };
  }
  function findRuleType(obj) {
    for (const key of RULE_TYPE_KEYS) {
      if (key in obj) return key;
    }
    return null;
  }
  function parseTargets(raw) {
    if (!Array.isArray(raw)) return [];
    const targets = [];
    for (const item of raw) {
      if (typeof item === "object" && item !== null) {
        const obj = item;
        const form = Object.keys(obj)[0];
        const value = obj[form];
        if (form && typeof value === "string" && ["host", "domain", "entity", "regex"].includes(form)) {
          targets.push({ form, value });
        }
      }
    }
    if (targets.length === 0 && typeof raw[0] === "string") {
      for (const s of raw) {
        const colonIdx = s.indexOf(":");
        if (colonIdx > 0) {
          const form = s.slice(0, colonIdx);
          const value = s.slice(colonIdx + 1);
          if (["host", "domain", "entity", "regex"].includes(form)) {
            targets.push({ form, value });
          }
        }
      }
    }
    return targets;
  }
  function parsePaths(raw) {
    if (!Array.isArray(raw)) return void 0;
    const paths = [];
    for (const item of raw) {
      if (typeof item === "object" && item !== null) {
        const obj = item;
        const form = Object.keys(obj)[0];
        const value = obj[form];
        if (form && typeof value === "string" && ["exact", "glob", "regex"].includes(form)) {
          paths.push({ form, value });
        }
      } else if (typeof item === "string") {
        const colonIdx = item.indexOf(":");
        if (colonIdx > 0) {
          const form = item.slice(0, colonIdx);
          const value = item.slice(colonIdx + 1);
          if (["exact", "glob", "regex"].includes(form)) {
            paths.push({ form, value });
          } else {
            paths.push({ form: "glob", value: item });
          }
        } else {
          paths.push({ form: "glob", value: item });
        }
      }
    }
    return paths.length > 0 ? paths : void 0;
  }
  function parseCandidates(raw) {
    if (!Array.isArray(raw)) return void 0;
    const candidates = raw.filter((c) => typeof c === "string");
    return candidates.length > 0 ? candidates : void 0;
  }
  function parseScope(raw) {
    if (!Array.isArray(raw)) return void 0;
    const scope = raw.filter((s) => typeof s === "string");
    return scope.length > 0 ? scope : void 0;
  }
  function parseBoundary(data) {
    const mode = String(data.mode || DEFAULT_BOUNDARY.mode);
    const allowCrossScope = data.allowCrossScope === true;
    return {
      mode,
      maxDepth: typeof data.maxDepth === "number" ? data.maxDepth : DEFAULT_BOUNDARY.maxDepth,
      stopAtScope: allowCrossScope ? false : data.stopAtScope !== false,
      allowCrossScope,
      allowScopeRoot: data.allowScopeRoot === true,
      includeSelf: data.includeSelf === true,
      allowPageRoot: data.allowPageRoot === true,
      selector: typeof data.selector === "string" ? data.selector : void 0,
      depth: typeof data.depth === "number" ? data.depth : void 0
    };
  }
  function parseMatch(data) {
    if (!data) return void 0;
    const mode = String(data.mode || "none");
    const match = { mode };
    if (typeof data.threshold === "number") match.threshold = data.threshold;
    if (data.reference === "picked" || data.reference === "none") match.reference = data.reference;
    else if (typeof data.reference === "string") match.reference = data.reference;
    if (data.referenceSelection === "error" || data.referenceSelection === "first") match.referenceSelection = data.referenceSelection;
    if (data.weights && typeof data.weights === "string") match.weights = data.weights;
    return match;
  }
  function parseKeywords(data) {
    if (!data) return void 0;
    const kw = {
      matchMode: typeof data.matchMode === "string" ? data.matchMode : void 0,
      fields: Array.isArray(data.fields) ? data.fields.filter((f) => typeof f === "string") : ["text"]
    };
    const includeArr = data.includeAny;
    if (includeArr && Array.isArray(includeArr)) {
      kw.includeAny = includeArr.filter((i) => typeof i === "string");
    }
    const includeAllArr = data.includeAll;
    if (includeAllArr && Array.isArray(includeAllArr)) {
      kw.includeAll = includeAllArr.filter((i) => typeof i === "string");
    }
    const excludeArr = data.excludeAny;
    if (excludeArr && Array.isArray(excludeArr)) {
      kw.excludeAny = excludeArr.filter((i) => typeof i === "string");
    }
    const excludeAllArr = data.excludeAll;
    if (excludeAllArr && Array.isArray(excludeAllArr)) {
      kw.excludeAll = excludeAllArr.filter((i) => typeof i === "string");
    }
    if (typeof data.caseSensitive === "boolean") kw.caseSensitive = data.caseSensitive;
    if (typeof data.wordBoundary === "boolean") {
      kw.wordBoundary = data.wordBoundary;
    }
    return kw;
  }
  function parseWhere(data, key) {
    const val = data[key];
    if (!val) return void 0;
    if (typeof val === "object" && !Array.isArray(val) && val !== null) {
      return val;
    }
    return void 0;
  }
  function parseAction(raw) {
    if (typeof raw === "string") {
      if (raw === "unhide") return { action: "unhide" };
      return { action: "hide" };
    }
    if (typeof raw === "object" && raw !== null) {
      const obj = raw;
      const actionType = String(obj.action || "hide");
      const options = obj.options;
      const parsedOptions = options ? {
        important: options.important === true
      } : void 0;
      if (actionType === "style" && obj.style) {
        return { action: "style", style: String(obj.style), options: parsedOptions };
      }
      if (actionType === "unhide") return { action: "unhide", options: parsedOptions };
      if (actionType === "collapse") return { action: "collapse", options: parsedOptions };
      if (actionType === "remove") return { action: "remove", options: parsedOptions };
      if (actionType === "mark") return { action: "mark", options: parsedOptions };
      return { action: "hide", options: parsedOptions };
    }
    return { action: "hide" };
  }
  function normalizeKeys(obj) {
    const result = {};
    for (const key of Object.keys(obj)) {
      const camel = key.replace(/-([a-z])/g, (_2, c) => c.toUpperCase());
      const val = obj[key];
      if (val !== null && typeof val === "object" && !Array.isArray(val)) {
        result[camel] = normalizeKeys(val);
      } else {
        result[camel] = val;
      }
    }
    return result;
  }
  function parseLogicOptions(data) {
    if (!data) return void 0;
    const opts = { stopAtScopeForAncestor: data.stopAtScopeForAncestor !== false };
    return opts;
  }
  function parseFrames(data) {
    if (!data) return void 0;
    if (typeof data === "string") return { mode: data };
    const result = { mode: String(data.mode || "top-only") };
    if (typeof data.accounting === "string") result.accounting = data.accounting;
    if (typeof data.maxTotalMatches === "number") result.maxTotalMatches = data.maxTotalMatches;
    return result;
  }
  function parseShadowBlock(data) {
    if (!data) return void 0;
    if (typeof data === "string") return { mode: data };
    const result = { mode: String(data.mode || "none") };
    if (typeof data.allowHostAncestor === "boolean") result.allowHostAncestor = data.allowHostAncestor;
    if (typeof data.maxRootsPerCycle === "number") result.maxRootsPerCycle = data.maxRootsPerCycle;
    if (typeof data.observeMutations === "boolean") result.observeMutations = data.observeMutations;
    if (typeof data.observeRecursive === "boolean") result.observeRecursive = data.observeRecursive;
    return result;
  }
  function parseRuntimeBlock(data) {
    if (!data) return void 0;
    const result = {};
    if (typeof data.reEvaluateOnPathChange === "string") result.reEvaluateOnPathChange = data.reEvaluateOnPathChange;
    if (typeof data.observeSubtree === "boolean") result.observeSubtree = data.observeSubtree;
    if (data.observeAttributes === "auto") result.observeAttributes = "auto";
    else if (data.observeAttributes === true || data.observeAttributes === false) result.observeAttributes = data.observeAttributes;
    if (typeof data.observePathChanges === "boolean") result.observePathChanges = data.observePathChanges;
    if (typeof data.pathChangeDebounceMs === "number") result.pathChangeDebounceMs = data.pathChangeDebounceMs;
    if (typeof data.errorRecovery === "string") result.errorRecovery = data.errorRecovery;
    if (typeof data.onBudgetExceeded === "string") result.onBudgetExceeded = data.onBudgetExceeded;
    return Object.keys(result).length > 0 ? result : void 0;
  }
  function parseCacheBlock(data) {
    if (!data) return void 0;
    const result = {};
    if (typeof data.scope === "string") result.scope = data.scope;
    if (typeof data.maxEntries === "number") result.maxEntries = data.maxEntries;
    if (typeof data.maxAgeMs === "number") result.maxAgeMs = data.maxAgeMs;
    if (typeof data.evictionPolicy === "string") result.evictionPolicy = data.evictionPolicy;
    return Object.keys(result).length > 0 ? result : void 0;
  }
  function parsePerformanceBlock(data) {
    if (!data) return void 0;
    const result = {};
    if (typeof data.maxCandidates === "number") result.maxCandidates = data.maxCandidates;
    if (typeof data.maxEvaluationsPerCycle === "number") result.maxEvaluationsPerCycle = data.maxEvaluationsPerCycle;
    if (typeof data.maxAddedNodesPerCycle === "number") result.maxAddedNodesPerCycle = data.maxAddedNodesPerCycle;
    if (typeof data.maxRegexMsPerRuleCycle === "number") result.maxRegexMsPerRuleCycle = data.maxRegexMsPerRuleCycle;
    if (typeof data.dependencyPrefilter === "boolean") result.dependencyPrefilter = data.dependencyPrefilter;
    if (typeof data.dependencyDepth === "number") result.dependencyDepth = data.dependencyDepth;
    if (typeof data.debounceMs === "number") result.debounceMs = data.debounceMs;
    if (typeof data.preferCssPrefilter === "boolean") result.preferCssPrefilter = data.preferCssPrefilter;
    if (typeof data.maxRegexPatternLength === "number") result.maxRegexPatternLength = data.maxRegexPatternLength;
    if (typeof data.maxTextRegexInputChars === "number") result.maxTextRegexInputChars = data.maxTextRegexInputChars;
    if (typeof data.metrics === "string") {
      result.metrics = data.metrics;
    } else if (data.metrics && typeof data.metrics === "object" && !Array.isArray(data.metrics)) {
      const m = data.metrics;
      if (typeof m.metrics === "string") result.metrics = m.metrics;
      if (typeof m.sampleRate === "number") result.sampleRate = m.sampleRate;
    }
    if (typeof data.sampleRate === "number") result.sampleRate = data.sampleRate;
    return Object.keys(result).length > 0 ? result : void 0;
  }
  function parseSafetyBlock(data) {
    if (!data) return void 0;
    const result = {};
    if (typeof data.preview === "string") result.preview = data.preview;
    if (typeof data.maxMatches === "number") result.maxMatches = data.maxMatches;
    if (typeof data.maxPagePercent === "number") result.maxPagePercent = data.maxPagePercent;
    if (typeof data.maxViewportAreaPercent === "number") result.maxViewportAreaPercent = data.maxViewportAreaPercent;
    if (typeof data.minFeatures === "number") result.minFeatures = data.minFeatures;
    if (typeof data.minLogicMatches === "number") result.minLogicMatches = data.minLogicMatches;
    if (typeof data.allowPartialApply === "boolean") result.allowPartialApply = data.allowPartialApply;
    if (typeof data.maxConsecutivePartial === "number") result.maxConsecutivePartial = data.maxConsecutivePartial;
    if (typeof data.partialCycleCount === "number") result.partialCycleCount = data.partialCycleCount;
    if (typeof data.warnIfScopeIsBody === "boolean") result.warnIfScopeIsBody = data.warnIfScopeIsBody;
    if (typeof data.warnIfNoWhereLogic === "boolean") result.warnIfNoWhereLogic = data.warnIfNoWhereLogic;
    if (typeof data.warnIfActionRemove === "boolean") result.warnIfActionRemove = data.warnIfActionRemove;
    if (typeof data.confirmPageRoot === "boolean") result.confirmPageRoot = data.confirmPageRoot;
    if (typeof data.warnIfWeakExactReference === "boolean") result.warnIfWeakExactReference = data.warnIfWeakExactReference;
    return Object.keys(result).length > 0 ? result : void 0;
  }
  function parseRuleMetadata(data, now) {
    if (!data) return void 0;
    const meta = { createdAt: now };
    if (typeof data.createdAt === "string") meta.createdAt = data.createdAt;
    if (typeof data.updatedAt === "string") meta.updatedAt = data.updatedAt;
    if (typeof data.source === "string") meta.source = data.source;
    if (typeof data.title === "string") meta.title = data.title;
    if (typeof data.description === "string") meta.description = data.description;
    if (typeof data.createdBy === "string") meta.createdBy = data.createdBy;
    if (typeof data.originalSyntax === "string") meta.originalSyntax = data.originalSyntax;
    if (typeof data.originalId === "string") meta.originalId = data.originalId;
    if (typeof data.history === "object" && data.history !== null) {
      meta.history = Array.isArray(data.history) ? data.history : void 0;
    }
    return meta;
  }
  function parseProvenance(data) {
    if (!data) return void 0;
    const result = {};
    if (typeof data.createdBy === "string") result.createdBy = data.createdBy;
    if (typeof data.createdFromHost === "string") result.createdFromHost = data.createdFromHost;
    if (typeof data.createdFromPath === "string") result.createdFromPath = data.createdFromPath;
    if (typeof data.originalRuleText === "string") result.originalRuleText = data.originalRuleText;
    if (typeof data.originalSource === "string") result.originalSource = data.originalSource;
    if (typeof data.source === "string") result.source = data.source;
    if (typeof data.originalRuleId === "string") result.originalRuleId = data.originalRuleId;
    if (typeof data.importTimestamp === "string") result.importTimestamp = data.importTimestamp;
    return Object.keys(result).length > 0 ? result : void 0;
  }
  function parseActionOptions(data) {
    if (!data) return void 0;
    const result = {};
    if (typeof data.important === "boolean") result.important = data.important;
    return Object.keys(result).length > 0 ? result : void 0;
  }
  function parsePreviewBlock(data) {
    if (!data) return void 0;
    const rawStatus = String(data.status || "none");
    const validStatuses = ["none", "required", "previewed", "confirmed", "stale", "forced-by-policy"];
    const status = validStatuses.includes(rawStatus) ? rawStatus : "none";
    const result = { status };
    if (typeof data.confirmationHash === "string") result.confirmationHash = data.confirmationHash;
    if (typeof data.confirmedAt === "string") result.confirmedAt = data.confirmedAt;
    if (typeof data.relaxedCapsUsed === "boolean") result.relaxedCapsUsed = data.relaxedCapsUsed;
    return result;
  }
  function createRuleId() {
    const hex = () => Math.floor(Math.random() * 256).toString(16).padStart(2, "0");
    const seg = (len) => Array.from({ length: len }, hex).join("");
    return `${RULE_ID_PREFIX}${seg(4)}-${seg(2)}-${seg(2)}-${seg(2)}-${seg(6)}`;
  }
  function intermediateToRule(intermediate) {
    const originalData = intermediate.data;
    const typeKey = intermediate.type;
    const camelKey = typeKey.replace(/-([a-z])/g, (_2, c) => c.toUpperCase());
    let ruleBody;
    if (originalData && typeof originalData === "object") {
      if (typeKey in originalData && typeof originalData[typeKey] === "object" && originalData[typeKey] !== null) {
        ruleBody = originalData[typeKey];
      } else if (camelKey in originalData && typeof originalData[camelKey] === "object" && originalData[camelKey] !== null) {
        ruleBody = originalData[camelKey];
      } else {
        ruleBody = originalData;
      }
    } else {
      return null;
    }
    ruleBody = normalizeKeys(ruleBody);
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const id = String(originalData && typeof originalData === "object" && "id" in originalData && typeof originalData["id"] === "string" ? originalData["id"] : ruleBody.id || createRuleId());
    const rawState = typeof ruleBody.state === "string" ? ruleBody.state : "";
    const state = rawState ? normalizeRuleState(rawState) : intermediate.type === "hide-exact" ? "active" : "needs-preview";
    const meta = parseRuleMetadata(ruleBody.metadata, now) || { createdAt: now };
    const provenance = ruleBody.provenance ? parseProvenance(ruleBody.provenance) : void 0;
    const targets = parseTargets(ruleBody.targets);
    if (targets.length === 0) return null;
    const priority = typeof ruleBody.priority === "number" ? ruleBody.priority : void 0;
    const actionObj = parseAction(ruleBody.action);
    const topLevelActionOptions = ruleBody.actionOptions ? parseActionOptions(ruleBody.actionOptions) : void 0;
    if (topLevelActionOptions && !actionObj.options) actionObj.options = topLevelActionOptions;
    const previewFromInput = ruleBody.preview ? parsePreviewBlock(ruleBody.preview) : void 0;
    const commonBlocks = () => ({
      priority,
      frames: ruleBody.frames ? parseFrames(ruleBody.frames) : void 0,
      shadow: ruleBody.shadow ? parseShadowBlock(ruleBody.shadow) : void 0,
      runtime: ruleBody.runtime ? parseRuntimeBlock(ruleBody.runtime) : void 0,
      cache: ruleBody.cache ? parseCacheBlock(ruleBody.cache) : void 0,
      performance: ruleBody.performance ? parsePerformanceBlock(ruleBody.performance) : void 0,
      safety: ruleBody.safety ? parseSafetyBlock(ruleBody.safety) : void 0,
      metadata: meta
    });
    switch (intermediate.type) {
      case "hide-exact": {
        const selector = String(ruleBody.selector || ruleBody.selectors || "");
        if (!selector) return null;
        const rule = {
          type: "hide-exact",
          id,
          syntaxVersion: 1,
          state,
          targets,
          selector,
          action: actionObj,
          preview: previewFromInput || { status: "confirmed" },
          ...commonBlocks()
        };
        if (ruleBody.paths) rule.paths = parsePaths(ruleBody.paths);
        if (ruleBody.scope) rule.scope = parseScope(ruleBody.scope);
        return rule;
      }
      case "hide-similar": {
        if (ruleBody.selector) return null;
        const rule = {
          type: "hide-similar",
          id,
          syntaxVersion: 1,
          state,
          targets,
          candidates: parseCandidates(ruleBody.candidates),
          boundary: ruleBody.boundary ? parseBoundary(ruleBody.boundary) : { ...DEFAULT_BOUNDARY, mode: "repeated-card" },
          match: parseMatch(ruleBody.match) || { mode: "similar" },
          action: actionObj,
          preview: previewFromInput || { status: "required" },
          ...commonBlocks()
        };
        if (ruleBody.paths) rule.paths = parsePaths(ruleBody.paths);
        if (ruleBody.scope) rule.scope = parseScope(ruleBody.scope);
        if (ruleBody.where) rule.where = parseWhere(ruleBody, "where");
        if (ruleBody.except) rule.except = parseWhere(ruleBody, "except");
        return rule;
      }
      case "smart-hide": {
        if (ruleBody.selector) return null;
        const candidates = parseCandidates(ruleBody.candidates);
        if (!candidates) return null;
        const rule = {
          type: "smart-hide",
          id,
          syntaxVersion: 1,
          state,
          targets,
          candidates,
          boundary: ruleBody.boundary ? parseBoundary(ruleBody.boundary) : { ...DEFAULT_BOUNDARY, mode: "repeated-card" },
          match: parseMatch(ruleBody.match),
          action: actionObj,
          preview: previewFromInput || { status: "required" },
          keywordMerge: ruleBody.keywordMerge === true,
          ...commonBlocks()
        };
        if (ruleBody.paths) rule.paths = parsePaths(ruleBody.paths);
        if (ruleBody.scope) rule.scope = parseScope(ruleBody.scope);
        if (ruleBody.where) rule.where = parseWhere(ruleBody, "where");
        if (ruleBody.except) rule.except = parseWhere(ruleBody, "except");
        if (ruleBody.keywords) rule.keywords = parseKeywords(ruleBody.keywords);
        return rule;
      }
      case "smart-allow": {
        if (ruleBody.selector) return null;
        if (topLevelActionOptions && !actionObj.options) actionObj.options = topLevelActionOptions;
        const rule = {
          type: "smart-allow",
          id,
          syntaxVersion: 1,
          state,
          targets,
          candidates: parseCandidates(ruleBody.candidates),
          boundary: ruleBody.boundary ? parseBoundary(ruleBody.boundary) : void 0,
          match: parseMatch(ruleBody.match) || { mode: "none" },
          allowBroad: ruleBody.allowBroad === true,
          logicOptions: ruleBody.logicOptions ? parseLogicOptions(ruleBody.logicOptions) : void 0,
          provenance,
          action: actionObj,
          preview: previewFromInput || { status: "confirmed" },
          ...commonBlocks()
        };
        if (ruleBody.paths) rule.paths = parsePaths(ruleBody.paths);
        if (ruleBody.scope) rule.scope = parseScope(ruleBody.scope);
        if (ruleBody.where) rule.where = parseWhere(ruleBody, "where");
        if (ruleBody.except) rule.except = parseWhere(ruleBody, "except");
        if (ruleBody.keywords) rule.keywords = parseKeywords(ruleBody.keywords);
        if (ruleBody.keywordMerge) rule.keywordMerge = ruleBody.keywordMerge === true;
        return rule;
      }
      default:
        return null;
    }
  }
  function parseSmartRules(yaml) {
    const errors = [];
    const parseResult = parseYaml(yaml);
    for (const pe2 of parseResult.errors) {
      errors.push(`Parse error near line ${pe2.line}: ${pe2.message}`);
    }
    const { rules: intermediates } = extractRulesFromParsed(parseResult.value);
    if (intermediates.length === 0) {
      errors.push("No rules found in YAML input");
      return { rules: [], errors };
    }
    const rules = [];
    for (const inter of intermediates) {
      const rule = intermediateToRule(inter);
      if (rule) {
        rules.push(rule);
      } else {
        errors.push(`Failed to parse rule: type=${inter.type}`);
      }
    }
    return { rules, errors };
  }
  var RULE_TYPE_KEYS;
  var init_smart_rule_parser = __esm({
    "src/core/smart-cosmetic/smart-rule-parser.ts"() {
      init_smart_rule_schema();
      RULE_TYPE_KEYS = /* @__PURE__ */ new Set(["hide-exact", "hide-similar", "smart-hide", "smart-allow", "hideExact", "hideSimilar", "smartHide", "smartAllow"]);
    }
  });

  // src/core/smart-cosmetic/smart-rule-collection.ts
  function delay(ms2) {
    return new Promise((resolve) => setTimeout(resolve, ms2));
  }
  var SMART_RULE_COLLECTION_DEFAULTS, CollectionManager, collectionManager;
  var init_smart_rule_collection = __esm({
    "src/core/smart-cosmetic/smart-rule-collection.ts"() {
      init_smart_rule_store();
      init_smart_rule_collection();
      SMART_RULE_COLLECTION_DEFAULTS = {
        updateIntervalMs: 24 * 60 * 60 * 1e3,
        retryDelayMs: 30 * 60 * 1e3,
        maxRetries: 3
      };
      CollectionManager = class {
        constructor() {
          __publicField(this, "updateTimers", /* @__PURE__ */ new Map());
          __publicField(this, "pendingUpdates", /* @__PURE__ */ new Map());
        }
        async registerCollection(collection) {
          const exists = smartRuleStore.getCollection(collection.id);
          if (exists) return false;
          await smartRuleStore.addCollection(collection);
          return true;
        }
        async unregisterCollection(id) {
          this.clearUpdateTimer(id);
          this.pendingUpdates.delete(id);
          return smartRuleStore.removeCollection(id);
        }
        getCollection(id) {
          return smartRuleStore.getCollection(id);
        }
        getAllCollections() {
          return smartRuleStore.getAllCollections();
        }
        scheduleUpdate(collectionId, intervalMs = SMART_RULE_COLLECTION_DEFAULTS.updateIntervalMs) {
          this.clearUpdateTimer(collectionId);
          const timer = setTimeout(async () => {
            await this.updateCollection(collectionId);
          }, intervalMs);
          this.updateTimers.set(collectionId, timer);
        }
        clearUpdateTimer(collectionId) {
          const existing = this.updateTimers.get(collectionId);
          if (existing) {
            clearTimeout(existing);
            this.updateTimers.delete(collectionId);
          }
        }
        async updateCollection(collectionId) {
          const inFlight = this.pendingUpdates.get(collectionId);
          if (inFlight) return inFlight.then(() => true);
          const promise = this.doUpdateCollection(collectionId);
          this.pendingUpdates.set(collectionId, promise);
          try {
            return await promise;
          } finally {
            this.pendingUpdates.delete(collectionId);
          }
        }
        async doUpdateCollection(collectionId) {
          const collection = smartRuleStore.getCollection(collectionId);
          if (!collection) return false;
          if (!collection.sourceUrl) return false;
          const metadata = collection.metadata || { listId: collectionId, syntaxVersion: 1 };
          const { parseYaml: parseYaml2, extractRulesFromParsed: extractRulesFromParsed2 } = await Promise.resolve().then(() => (init_smart_rule_parser(), smart_rule_parser_exports));
          let lastError;
          for (let attempt = 0; attempt <= SMART_RULE_COLLECTION_DEFAULTS.maxRetries; attempt++) {
            try {
              const response = await fetch(collection.sourceUrl);
              if (!response.ok) {
                lastError = `HTTP ${response.status}`;
                if (attempt < SMART_RULE_COLLECTION_DEFAULTS.maxRetries) {
                  await delay(SMART_RULE_COLLECTION_DEFAULTS.retryDelayMs * Math.pow(2, attempt));
                }
                continue;
              }
              const text = await response.text();
              const parsed = parseYaml2(text);
              if (parsed.errors.length > 0) {
                lastError = `Parse errors: ${parsed.errors.join(", ")}`;
                break;
              }
              const { rules: rawRules } = extractRulesFromParsed2(parsed.value);
              const convertedRules = rawRules.map((r) => ({
                ...r.data,
                id: r.data.id || `${collectionId}:${r.type}:${Math.random().toString(36).slice(2, 9)}`,
                type: r.type,
                syntaxVersion: collection.metadata?.syntaxVersion || 1,
                state: "active",
                metadata: { createdAt: (/* @__PURE__ */ new Date()).toISOString() },
                targets: r.data.targets || [],
                action: r.data.action || { action: "hide" },
                collectionId
              }));
              const updatedMeta = {
                ...metadata,
                updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
                sourceEtag: response.headers.get("ETag") || void 0
              };
              const updatedCollection = {
                ...collection,
                metadata: updatedMeta,
                lastUpdateCheck: Date.now(),
                lastUpdateSuccess: Date.now(),
                updateError: void 0
              };
              await smartRuleStore.replaceCollectionRules(collectionId, convertedRules, updatedCollection);
              this.scheduleUpdate(collectionId);
              return true;
            } catch (err) {
              lastError = String(err);
              if (attempt < SMART_RULE_COLLECTION_DEFAULTS.maxRetries) {
                await delay(SMART_RULE_COLLECTION_DEFAULTS.retryDelayMs * Math.pow(2, attempt));
              }
            }
          }
          await smartRuleStore.updateCollection(collectionId, {
            lastUpdateCheck: Date.now(),
            updateError: lastError
          });
          this.scheduleUpdate(collectionId, SMART_RULE_COLLECTION_DEFAULTS.retryDelayMs);
          return false;
        }
        getRulesByCollection(collectionId) {
          return smartRuleStore.getRulesByCollection(collectionId);
        }
        getStats() {
          const collections = this.getAllCollections();
          const total = collections.length;
          const withErrors = collections.filter((c) => c.updateError).length;
          return { total, withErrors };
        }
      };
      collectionManager = new CollectionManager();
    }
  });

  // src/core/smart-cosmetic/fetch-gateway.ts
  function delay2(ms2) {
    return new Promise((resolve) => setTimeout(resolve, ms2));
  }
  var DEFAULT_FETCH_OPTIONS, Gateway, fetchGateway;
  var init_fetch_gateway = __esm({
    "src/core/smart-cosmetic/fetch-gateway.ts"() {
      init_smart_rule_collection();
      init_smart_rule_store();
      init_fetch_gateway();
      DEFAULT_FETCH_OPTIONS = {
        defaultUpdateIntervalMs: 24 * 60 * 60 * 1e3,
        retryDelayMs: 30 * 60 * 1e3,
        maxRetries: 3
      };
      Gateway = class {
        constructor(_options = DEFAULT_FETCH_OPTIONS) {
          this._options = _options;
          __publicField(this, "etags", /* @__PURE__ */ new Map());
          __publicField(this, "cachedContents", /* @__PURE__ */ new Map());
          __publicField(this, "updateTimers", /* @__PURE__ */ new Map());
          __publicField(this, "activeFetches", /* @__PURE__ */ new Map());
        }
        async subscribe(url, collectionId) {
          const existing = await collectionManager.registerCollection({
            id: collectionId,
            sourceUrl: url,
            metadata: { listId: collectionId, syntaxVersion: 1 }
          });
          if (!existing) return false;
          this.scheduleNextUpdate(collectionId, 0);
          return true;
        }
        async unsubscribe(collectionId) {
          this.cancelTimer(collectionId);
          this.activeFetches.delete(collectionId);
          this.etags.delete(collectionId);
          this.cachedContents.delete(collectionId);
          return collectionManager.unregisterCollection(collectionId);
        }
        async fetchNow(collectionId) {
          const inFlight = this.activeFetches.get(collectionId);
          if (inFlight) return inFlight;
          const promise = this.doFetch(collectionId);
          this.activeFetches.set(collectionId, promise);
          try {
            return await promise;
          } finally {
            this.activeFetches.delete(collectionId);
          }
        }
        getCachedContent(collectionId) {
          return this.cachedContents.get(collectionId)?.content;
        }
        getEtag(collectionId) {
          return this.etags.get(collectionId);
        }
        async doFetch(collectionId) {
          const collection = smartRuleStore.getCollection(collectionId);
          if (!collection?.sourceUrl) {
            return { ok: false, error: "No source URL configured", collectionId };
          }
          const headers = {};
          const etag = this.etags.get(collectionId);
          if (etag) headers["If-None-Match"] = etag;
          let lastError;
          for (let attempt = 0; attempt <= this._options.maxRetries; attempt++) {
            try {
              const controller = new AbortController();
              const timeout = setTimeout(() => controller.abort(), 3e4);
              const response = await fetch(collection.sourceUrl, {
                headers,
                signal: controller.signal
              });
              clearTimeout(timeout);
              if (response.status === 304) {
                this.scheduleNextUpdate(collectionId);
                return { ok: true, status: 304, cached: true, collectionId };
              }
              if (!response.ok) {
                lastError = `HTTP ${response.status}`;
                if (attempt < this._options.maxRetries) {
                  await delay2(this._options.retryDelayMs * Math.pow(2, attempt));
                }
                continue;
              }
              const text = await response.text();
              const newEtag = response.headers.get("ETag");
              if (newEtag) this.etags.set(collectionId, newEtag);
              this.cachedContents.set(collectionId, { content: text, fetchedAt: Date.now() });
              const { parseSmartRules: parseSmartRules2 } = await Promise.resolve().then(() => (init_smart_rule_parser(), smart_rule_parser_exports));
              const parsed = parseSmartRules2(text);
              if (parsed.errors.length > 0) {
                return { ok: false, error: `YAML parse errors: ${parsed.errors.join("; ")}`, collectionId };
              }
              const rules = parsed.rules.map((r) => ({ ...r, collectionId }));
              const updatedMeta = {
                ...collection.metadata,
                updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
                sourceEtag: newEtag || void 0
              };
              const updatedCollection = {
                ...collection,
                metadata: updatedMeta,
                lastUpdateCheck: Date.now(),
                lastUpdateSuccess: Date.now(),
                updateError: void 0
              };
              await smartRuleStore.replaceCollectionRules(collectionId, rules, updatedCollection);
              this.scheduleNextUpdate(collectionId);
              return { ok: true, content: text, etag: newEtag || void 0, ruleCount: rules.length, collectionId };
            } catch (err) {
              console.warn("[uBR] fetch-gateway: fetch failed for collection", collectionId, err);
              lastError = String(err);
              if (attempt < this._options.maxRetries) {
                await delay2(this._options.retryDelayMs * Math.pow(2, attempt));
              }
            }
          }
          await smartRuleStore.updateCollection(collectionId, {
            lastUpdateCheck: Date.now(),
            updateError: lastError
          });
          this.scheduleNextUpdate(collectionId, this._options.retryDelayMs);
          return { ok: false, error: lastError, collectionId };
        }
        scheduleNextUpdate(collectionId, delayMs) {
          this.cancelTimer(collectionId);
          const interval = delayMs ?? this._options.defaultUpdateIntervalMs;
          const timer = setTimeout(() => {
            this.fetchNow(collectionId);
          }, interval);
          this.updateTimers.set(collectionId, timer);
        }
        cancelTimer(collectionId) {
          const existing = this.updateTimers.get(collectionId);
          if (existing) {
            clearTimeout(existing);
            this.updateTimers.delete(collectionId);
          }
        }
      };
      fetchGateway = new Gateway();
    }
  });

  // src/core/smart-cosmetic/conflict.ts
  function getPathScore(paths) {
    if (!paths || paths.length === 0) return 100;
    let maxScore = 0;
    for (const p of paths) {
      if (p.form === "glob") {
        maxScore = Math.max(maxScore, p.value?.includes("**") ? 250 : 300);
      } else {
        maxScore = Math.max(maxScore, PATH_WEIGHTS[p.form] || 100);
      }
    }
    return maxScore || 100;
  }
  function getRuleIntentScore(rule) {
    if (rule.type === "hide-exact") return 500;
    if (rule.type === "smart-allow") return 100;
    if ("match" in rule && rule.match?.mode === "exact") return 475;
    if (rule.type === "hide-similar") {
      const threshold = ("match" in rule && rule.match?.threshold) ?? 0.82;
      return threshold >= 0.82 ? 425 : 400;
    }
    if (rule.type === "smart-hide") {
      const matchMode = "match" in rule && rule.match?.mode || "none";
      if (matchMode === "none") return 325;
      const hasStrongLogic = "where" in rule && rule.where !== void 0;
      if (matchMode === "similar" || matchMode === "structural") return hasStrongLogic ? 375 : 350;
    }
    if (rule.action.action === "mark") return 200;
    return 150;
  }
  function getSelectorScopeScore(rule) {
    if ("selector" in rule && rule.selector) return 150;
    const scope = "scope" in rule ? rule.scope : void 0;
    if (scope && scope.length > 0) {
      if (scope.some((s) => s === "body" || s === "html")) return 50;
      if (scope.some((s) => s === "div" || s === "section" || s === "article")) return 250;
      return 300;
    }
    return 50;
  }
  function getLogicStrengthScore(rule) {
    const where = "where" in rule ? rule.where : void 0;
    const except = "except" in rule ? rule.except : void 0;
    if (!where) return 0;
    if (except) return 300;
    if (hasConditionType(where, "regex") || hasConditionType(where, "attribute")) return 250;
    if (hasConditionType(where, "text")) return 200;
    return 100;
  }
  function hasConditionType(expr, op2) {
    if (!expr || typeof expr !== "object") return false;
    const e = expr;
    if ("condition" in e && typeof e.condition === "object" && e.condition !== null) {
      const c = e.condition;
      if (c.operator === op2) return true;
    }
    const group = e;
    for (const key of ["all", "any", "none"]) {
      const arr = group[key];
      if (arr) {
        for (const child of arr) {
          if (hasConditionType(child, op2)) return true;
        }
      }
    }
    return false;
  }
  function getActionStrength(action) {
    return ACTION_STRENGTH[action.action] || 0;
  }
  function hasHideLikeAction(rule) {
    const a = rule.action.action;
    return a === "hide" || a === "collapse" || a === "remove" || a === "mark";
  }
  function getCreatedAt(rule) {
    return rule.metadata?.createdAt || "0";
  }
  function getConflictClass(rule) {
    if (rule.type === "smart-allow" && rule.action.action === "unhide") return "A" /* UserAllow */;
    if (rule.type === "hide-exact") return "B" /* UserExactHide */;
    if (rule.type === "smart-hide" || rule.type === "hide-similar") return "C" /* UserSmartHide */;
    return "F" /* GenericCosmetic */;
  }
  function getFullSpecificity(rule) {
    const targetScore = rule.targets.reduce((max, t) => Math.max(max, TARGET_WEIGHTS[t.form] || 100), 0);
    const pathScore = getPathScore(rule.paths);
    const ruleIntentScore = getRuleIntentScore(rule);
    const selectorScopeScore = getSelectorScopeScore(rule);
    let boundaryScore = 0;
    if ("boundary" in rule && rule.boundary) {
      boundaryScore = BOUNDARY_SCORES[rule.boundary.mode] || 0;
    }
    let matchScore = 0;
    if ("match" in rule && rule.match) {
      matchScore = (MATCH_MODE_SCORES[rule.match.mode] || MATCH_MODE_SCORES.none)(rule.match.threshold);
    }
    const logicStrengthScore = getLogicStrengthScore(rule);
    const sourceSpecificity = SOURCE_SPECIFICITY[rule.metadata?.source || ""] || 0;
    return {
      targetScore,
      pathScore,
      ruleIntentScore,
      selectorScopeScore,
      boundaryScore,
      matchScore,
      logicStrengthScore,
      sourceSpecificity
    };
  }
  function compareSpecificityTuples(a, b2) {
    for (const dim of SPECIFICITY_DIMS) {
      if (a[dim] !== b2[dim]) return b2[dim] - a[dim];
    }
    return 0;
  }
  function sortByConflictOrder(rules) {
    return [...rules].sort((a, b2) => {
      const classA = getConflictClass(a);
      const classB = getConflictClass(b2);
      if (classA !== classB) return classA.localeCompare(classB);
      const specDiff = compareSpecificityTuples(getFullSpecificity(a), getFullSpecificity(b2));
      if (specDiff !== 0) return specDiff;
      const strengthA = getActionStrength(a.action);
      const strengthB = getActionStrength(b2.action);
      if (strengthA !== strengthB) return strengthB - strengthA;
      const createdA = getCreatedAt(a);
      const createdB = getCreatedAt(b2);
      if (createdA !== createdB) return createdA < createdB ? -1 : 1;
      return a.id.localeCompare(b2.id);
    });
  }
  var ConflictClass, ACTION_STRENGTH, TARGET_WEIGHTS, PATH_WEIGHTS, BOUNDARY_SCORES, MATCH_MODE_SCORES, SOURCE_SPECIFICITY, SPECIFICITY_DIMS;
  var init_conflict = __esm({
    "src/core/smart-cosmetic/conflict.ts"() {
      init_conflict();
      ConflictClass = /* @__PURE__ */ ((ConflictClass2) => {
        ConflictClass2["UserAllow"] = "A";
        ConflictClass2["UserExactHide"] = "B";
        ConflictClass2["UserSmartHide"] = "C";
        ConflictClass2["GenericCosmetic"] = "F";
        return ConflictClass2;
      })(ConflictClass || {});
      ACTION_STRENGTH = {
        remove: 4,
        collapse: 3,
        hide: 2,
        mark: 1,
        unhide: 1
      };
      TARGET_WEIGHTS = {
        host: 500,
        domain: 400,
        entity: 350,
        regex: 300
      };
      PATH_WEIGHTS = {
        exact: 400,
        regex: 200
      };
      BOUNDARY_SCORES = {
        exact: 300,
        selector: 275,
        "ancestor-depth": 250,
        "repeated-card": 225,
        "semantic-block": 200,
        "nearest-card": 175,
        "visual-block": 150
      };
      MATCH_MODE_SCORES = {
        none: () => 100,
        exact: () => 300,
        similar: (t) => (t ?? 0.82) >= 0.82 ? 250 : 220,
        structural: (t) => (t ?? 0.8) >= 0.8 ? 240 : 220
      };
      SOURCE_SPECIFICITY = {
        picker: 3,
        "manual-editor": 2,
        migration: 1
      };
      SPECIFICITY_DIMS = [
        "targetScore",
        "pathScore",
        "ruleIntentScore",
        "selectorScopeScore",
        "boundaryScore",
        "matchScore",
        "logicStrengthScore",
        "sourceSpecificity"
      ];
    }
  });

  // src/core/smart-cosmetic/cosmetic-plan.ts
  function compileCosmeticPlan(rules, url, _hostname) {
    const plan = {
      cssSelectors: [],
      smartRules: [],
      allows: [],
      classicRules: [],
      diagnostics: [],
      exportLoss: []
    };
    const enabled = rules.filter((r) => r.state === "active");
    const path = getPathFromUrl(url);
    for (const rule of enabled) {
      if (!pathMatches(rule.paths, path)) continue;
      plan.diagnostics.push(`rule ${rule.id}: added to plan`);
      if (rule.type === "smart-allow") {
        plan.allows.push(rule);
        plan.classicRules.push({ rule });
        plan.exportLoss.push("lossless");
        continue;
      }
      if (!hasHideLikeAction(rule)) continue;
      if (rule.type === "hide-exact") {
        if (rule.selector) {
          const lossless = isLosslessSelector(rule.selector);
          plan.cssSelectors.push(rule.selector);
          plan.diagnostics.push(`rule ${rule.id}: compiled as CSS selector`);
          plan.classicRules.push({ rule, losslessSelectorCompile: lossless });
          plan.exportLoss.push("lossless");
        }
        continue;
      }
      if (rule.type === "smart-hide" || rule.type === "hide-similar") {
        plan.diagnostics.push(`rule ${rule.id}: compiled as smart evaluation`);
        plan.exportLoss.push("not-possible");
        const safety = { ...DEFAULT_SAFETY, ...rule.safety };
        const performance2 = { ...DEFAULT_PERFORMANCE, ...rule.performance };
        const frames = { ...DEFAULT_FRAMES, ...rule.frames };
        const shadow = { ...DEFAULT_SHADOW, ...rule.shadow };
        const runtime = { ...DEFAULT_RUNTIME, ...rule.runtime };
        plan.smartRules.push({
          rule,
          boundary: rule.boundary ?? DEFAULT_BOUNDARY,
          match: rule.match,
          where: rule.where,
          except: rule.except,
          candidates: ("candidates" in rule ? rule.candidates : rule.candidates) ?? [],
          safety,
          performance: performance2,
          frames,
          shadow,
          runtime
        });
      }
    }
    plan.cssSelectors = applyConflictResolution(plan.cssSelectors, plan.allows);
    plan.allows = plan.allows.filter((a) => {
      const conflictClass = getConflictClass(a);
      return conflictClass === "A" /* UserAllow */;
    });
    return plan;
  }
  function getPathFromUrl(url) {
    try {
      const u = new URL(url);
      return u.pathname + u.search + u.hash;
    } catch (e) {
      console.warn("[uBR] cosmetic-plan: getPathFromUrl URL parse failed", url, e);
      return "";
    }
  }
  function pathMatches(paths, currentPath) {
    if (!paths || paths.length === 0) return true;
    if (!currentPath) return true;
    for (const entry of paths) {
      switch (entry.form) {
        case "exact":
          if (currentPath === entry.value) return true;
          break;
        case "glob": {
          const pattern = entry.value.replace(/[.+^${}()|[\]\\]/g, "\\$&").replace(/\*/g, ".*").replace(/\?/g, ".");
          if (new RegExp(`^${pattern}$`).test(currentPath)) return true;
          break;
        }
        case "regex": {
          try {
            if (new RegExp(entry.value).test(currentPath)) return true;
          } catch (e) {
            console.warn("[uBR] cosmetic-plan: invalid regex in path match", entry.value, e);
          }
          break;
        }
      }
    }
    return false;
  }
  function isLosslessSelector(selector) {
    if (/::/.test(selector)) return false;
    if (/:has\(|:contains\(|:matches-property\(|:xpath\(/.test(selector)) return false;
    return true;
  }
  function applyConflictResolution(selectors, allows) {
    if (allows.length === 0 || selectors.length === 0) return selectors;
    const blocked = /* @__PURE__ */ new Set();
    for (const allow of allows) {
      if (allow.candidates) {
        for (const sel2 of allow.candidates) {
          blocked.add(sel2);
        }
      }
    }
    return selectors.filter((s) => !blocked.has(s));
  }
  var init_cosmetic_plan = __esm({
    "src/core/smart-cosmetic/cosmetic-plan.ts"() {
      init_smart_rule_schema();
      init_conflict();
      init_cosmetic_plan();
    }
  });

  // src/core/smart-cosmetic/export.ts
  var export_exports = {};
  __export(export_exports, {
    Export: () => export_exports,
    determineExportLoss: () => determineExportLoss,
    exportAllRules: () => exportAllRules,
    serializeToYaml: () => serializeToYaml
  });
  function determineExportLoss(rule) {
    if (rule.type === "hide-exact") {
      if (rule.selector && rule.targets) {
        return { code: "lossless", reason: "Simple CSS selector with targets" };
      }
      return { code: "partial", reason: "Hide-exact without selector or targets", affectedRuleIds: [rule.id] };
    }
    if (rule.type === "smart-allow") {
      if (canExportSmartAllowLossless(rule)) {
        return { code: "lossless", reason: "Simple unhide with candidate selector" };
      }
      return { code: "partial", reason: "Smart-allow with complex semantics", affectedRuleIds: [rule.id] };
    }
    if (rule.type === "hide-similar") {
      if (rule.match?.mode === "exact" && !rule.where && !rule.except) {
        return { code: "lossless", reason: "Exact match without logic" };
      }
      return { code: "approximate", reason: "Similarity-based hide cannot be exactly represented in CSS", affectedRuleIds: [rule.id] };
    }
    if (rule.type === "smart-hide") {
      if (hasFieldUsage(rule, "own-text")) {
        return { code: "partial", reason: "own-text field used \u2014 cannot be exactly represented in classic CSS", affectedRuleIds: [rule.id] };
      }
      if (hasFieldUsage(rule, "semantic-text")) {
        return { code: "not-possible", reason: "semantic-text field used \u2014 cannot be represented in classic CSS", affectedRuleIds: [rule.id] };
      }
      if (!rule.where && !rule.except && !rule.keywords && (!rule.match || rule.match.mode === "none")) {
        return { code: "partial", reason: "Smart-hide without logic and no match mode" };
      }
      if (rule.where || rule.except || rule.keywords) {
        return { code: "approximate", reason: "Smart-hide with logic cannot be exactly represented in classic CSS" };
      }
      return { code: "approximate", reason: "Smart-hide with semantic matching", affectedRuleIds: [rule.id] };
    }
    const neverRule = rule;
    return { code: "not-possible", reason: "Unknown rule type", affectedRuleIds: [neverRule.id] };
  }
  function hasFieldUsage(rule, fieldName) {
    function scanExpr(expr) {
      if (!expr || typeof expr !== "object") return false;
      const e = expr;
      if ("condition" in e && typeof e.condition === "object" && e.condition !== null) {
        const c = e.condition;
        if (c.field === fieldName) return true;
      }
      const group = e;
      for (const key of ["all", "any", "none"]) {
        const arr = group[key];
        if (arr) {
          for (const child of arr) if (scanExpr(child)) return true;
        }
      }
      return false;
    }
    const where = "where" in rule ? rule.where : void 0;
    const except = "except" in rule ? rule.except : void 0;
    return scanExpr(where) || scanExpr(except);
  }
  function canExportSmartAllowLossless(rule) {
    if (rule.action.action !== "unhide") return false;
    if (rule.where || rule.except || rule.keywords) return false;
    if (rule.match && rule.match.mode !== "none") return false;
    if (!rule.candidates || rule.candidates.length !== 1) return false;
    return true;
  }
  function escapeYamlValue(value) {
    if (/[:{}[\],&*?|>!%@`#]/.test(value) || value.startsWith(" ") || value.endsWith(" ") || value === "" || value === "~" || value === "null" || value === "true" || value === "false") {
      return JSON.stringify(value);
    }
    return value;
  }
  function indent(level) {
    return "  ".repeat(level);
  }
  function toKebab(s) {
    return s.replace(/([a-z])([A-Z])/g, "$1-$2").toLowerCase();
  }
  function serializeBlock(lines, level, blockName, block, fields) {
    if (!block) return;
    const hasValue = fields.some(([_yamlKey, key]) => block[key] !== void 0 && block[key] !== null);
    if (!hasValue) return;
    lines.push(`${indent(level)}${blockName}:`);
    for (const [yamlKey, key] of fields) {
      const val = block[key];
      if (val === void 0 || val === null) continue;
      if (typeof val === "string") {
        lines.push(`${indent(level + 1)}${yamlKey}: ${escapeYamlValue(val)}`);
      } else if (typeof val === "boolean") {
        lines.push(`${indent(level + 1)}${yamlKey}: ${val}`);
      } else if (typeof val === "number") {
        lines.push(`${indent(level + 1)}${yamlKey}: ${val}`);
      }
    }
  }
  function serializeActionBlock(lines, level, action) {
    const actionName = action.action;
    if (actionName === "style" && "style" in action) {
      lines.push(`${indent(level)}action: style`);
      lines.push(`${indent(level + 1)}style: ${escapeYamlValue(action.style)}`);
    } else {
      lines.push(`${indent(level)}action: ${actionName}`);
    }
    if (action.options) {
      if (action.options.important) {
        lines.push(`${indent(level + 1)}important: true`);
      }
    }
  }
  function serializeBoundaryBlock(lines, level, boundary) {
    if (!boundary) return;
    lines.push(`${indent(level)}boundary:`);
    lines.push(`${indent(level + 1)}mode: ${boundary.mode}`);
    for (const [yamlKey, key] of [["max-depth", "maxDepth"], ["selector", "selector"], ["depth", "depth"], ["stop-at-scope", "stopAtScope"], ["allow-cross-scope", "allowCrossScope"], ["allow-scope-root", "allowScopeRoot"], ["include-self", "includeSelf"], ["allow-page-root", "allowPageRoot"]]) {
      if (boundary[key] !== void 0 && boundary[key] !== null) {
        const val = boundary[key];
        if (typeof val === "boolean") {
          lines.push(`${indent(level + 1)}${yamlKey}: ${val}`);
        } else if (typeof val === "number") {
          lines.push(`${indent(level + 1)}${yamlKey}: ${val}`);
        } else if (typeof val === "string") {
          lines.push(`${indent(level + 1)}${yamlKey}: ${escapeYamlValue(val)}`);
        }
      }
    }
  }
  function serializeMatchBlock(lines, level, match) {
    if (!match) return;
    lines.push(`${indent(level)}match:`);
    lines.push(`${indent(level + 1)}mode: ${match.mode}`);
    for (const [yamlKey, key] of [["reference", "reference"], ["reference-selection", "referenceSelection"], ["threshold", "threshold"], ["weight-profile", "weights"]]) {
      if (match[key] !== void 0 && match[key] !== null) {
        const val = match[key];
        if (typeof val === "number") {
          lines.push(`${indent(level + 1)}${yamlKey}: ${val}`);
        } else if (typeof val === "string") {
          lines.push(`${indent(level + 1)}${yamlKey}: ${escapeYamlValue(val)}`);
        }
      }
    }
  }
  function serializeKeywordsBlock(lines, level, keywords) {
    if (!keywords) return;
    lines.push(`${indent(level)}keywords:`);
    if (keywords.matchMode) lines.push(`${indent(level + 1)}match-mode: ${keywords.matchMode}`);
    if (keywords.fields && keywords.fields.length > 0) lines.push(`${indent(level + 1)}fields: [${keywords.fields.join(", ")}]`);
    if (keywords.caseSensitive !== void 0) lines.push(`${indent(level + 1)}case-sensitive: ${keywords.caseSensitive}`);
    if (keywords.wordBoundary !== void 0) lines.push(`${indent(level + 1)}word-boundary: ${keywords.wordBoundary}`);
    for (const [yamlKey, arr] of [["include-any", "includeAny"], ["include-all", "includeAll"], ["exclude-any", "excludeAny"], ["exclude-all", "excludeAll"]]) {
      const items = keywords[arr];
      if (items && items.length > 0) {
        lines.push(`${indent(level + 1)}${yamlKey}:`);
        for (const item of items) lines.push(`${indent(level + 2)}- ${escapeYamlValue(item)}`);
      }
    }
  }
  function serializeCondition(lines, level, cond) {
    const c = cond;
    lines.push(`${indent(level)}condition:`);
    for (const key of ["field", "attr-name", "attr-family", "operator", "value", "pattern", "selector", "flags", "case-sensitive"]) {
      const yamlKey = toKebab(key);
      const val = c[key] ?? c[toKebab(key)];
      if (val !== void 0 && val !== null) {
        if (typeof val === "boolean") {
          lines.push(`${indent(level + 1)}${yamlKey}: ${val}`);
        } else {
          lines.push(`${indent(level + 1)}${yamlKey}: ${escapeYamlValue(String(val))}`);
        }
      }
    }
  }
  function serializeLogicExpression(lines, level, expr) {
    const group = expr;
    if (group.all || group.any || group.none) {
      for (const key of ["all", "any", "none"]) {
        const items = group[key];
        if (items && items.length > 0) {
          lines.push(`${indent(level)}${key}:`);
          for (const item of items) {
            lines.push(`${indent(level + 1)}-`);
            if (item.all || item.any || item.none) {
              serializeLogicExpression(lines, level + 2, item);
            } else {
              const expItem = item;
              serializeCondition(lines, level + 2, expItem.condition);
              if (expItem.label) lines.push(`${indent(level + 2)}label: ${escapeYamlValue(expItem.label)}`);
            }
          }
        }
      }
    } else {
      const expItem = expr;
      serializeCondition(lines, level, expItem.condition);
      if (expItem.label) lines.push(`${indent(level + 1)}label: ${escapeYamlValue(expItem.label)}`);
    }
  }
  function serializeWhereOrExcept(lines, level, key, expr) {
    if (!expr) return;
    lines.push(`${indent(level)}${key}:`);
    serializeLogicExpression(lines, level + 1, expr);
  }
  function serializeMetadata(lines, level, metadata) {
    if (!metadata) return;
    lines.push(`${indent(level)}metadata:`);
    for (const [yamlKey, key] of METADATA_FIELDS) {
      const val = metadata[key];
      if (val === void 0 || val === null) continue;
      if (typeof val === "string") {
        lines.push(`${indent(level + 1)}${yamlKey}: ${escapeYamlValue(val)}`);
      } else if (typeof val === "boolean") {
        lines.push(`${indent(level + 1)}${yamlKey}: ${val}`);
      } else if (typeof val === "number") {
        lines.push(`${indent(level + 1)}${yamlKey}: ${val}`);
      }
    }
    if (metadata.provenance) {
      serializeBlock(lines, level + 1, "provenance", metadata.provenance, PROVENANCE_FIELDS);
    }
  }
  function serializeLogicOptions(lines, level, logicOptions) {
    if (!logicOptions) return;
    lines.push(`${indent(level)}logic-options:`);
    if (logicOptions.stopAtScopeForAncestor !== void 0) {
      lines.push(`${indent(level + 1)}stop-at-scope-for-ancestor: ${logicOptions.stopAtScopeForAncestor}`);
    }
  }
  function serializeScope(lines, level, scope) {
    if (!scope || scope.length === 0) return;
    lines.push(`${indent(level)}scope:`);
    for (const s of scope) lines.push(`${indent(level + 1)}- ${escapeYamlValue(s)}`);
  }
  function serializeToYaml(rules) {
    const lines = [];
    lines.push("# uBR Smart Cosmetic Rules Export");
    lines.push(`# Generated: ${(/* @__PURE__ */ new Date()).toISOString()}`);
    lines.push(`# Total rules: ${rules.length}`);
    lines.push("");
    for (const rule of rules) {
      const loss = determineExportLoss(rule);
      lines.push(`# Export loss: ${loss.code}${loss.reason ? ` \u2014 ${loss.reason}` : ""}`);
      lines.push("");
      switch (rule.type) {
        case "hide-exact":
          serializeHideExact(lines, rule);
          break;
        case "hide-similar":
          serializeHideSimilar(lines, rule);
          break;
        case "smart-hide":
          serializeSmartHide(lines, rule);
          break;
        case "smart-allow":
          serializeSmartAllow(lines, rule);
          break;
      }
      lines.push("");
    }
    return lines.join("\n");
  }
  function serializeHideExact(lines, rule) {
    lines.push(`hide-exact:`);
    serializeTargets(lines, rule.targets);
    if (rule.paths) serializePaths(lines, rule.paths);
    lines.push(`${indent(1)}state: ${rule.state}`);
    serializeScope(lines, 1, rule.scope);
    lines.push(`${indent(1)}selector: ${escapeYamlValue(rule.selector)}`);
    serializeActionBlock(lines, 1, rule.action);
    serializeBlock(lines, 1, "safety", rule.safety, SAFETY_FIELDS);
    serializeBlock(lines, 1, "performance", rule.performance, PERFORMANCE_FIELDS);
    serializeBlock(lines, 1, "frames", rule.frames, FRAMES_FIELDS);
    serializeBlock(lines, 1, "shadow", rule.shadow, SHADOW_FIELDS);
    serializeBlock(lines, 1, "runtime", rule.runtime, RUNTIME_FIELDS);
    serializeBlock(lines, 1, "cache", rule.cache, CACHE_FIELDS);
    serializeMetadata(lines, 1, rule.metadata);
  }
  function serializeHideSimilar(lines, rule) {
    lines.push(`hide-similar:`);
    serializeTargets(lines, rule.targets);
    if (rule.paths) serializePaths(lines, rule.paths);
    lines.push(`${indent(1)}state: ${rule.state}`);
    serializeScope(lines, 1, rule.scope);
    serializeBoundaryBlock(lines, 1, rule.boundary);
    serializeMatchBlock(lines, 1, rule.match);
    serializeWhereOrExcept(lines, 1, "where", rule.where);
    serializeWhereOrExcept(lines, 1, "except", rule.except);
    if (rule.candidates && rule.candidates.length > 0) {
      lines.push(`${indent(1)}candidates:`);
      for (const c of rule.candidates) lines.push(`${indent(2)}- ${escapeYamlValue(c)}`);
    }
    serializeActionBlock(lines, 1, rule.action);
    serializeBlock(lines, 1, "safety", rule.safety, SAFETY_FIELDS);
    serializeBlock(lines, 1, "performance", rule.performance, PERFORMANCE_FIELDS);
    serializeBlock(lines, 1, "frames", rule.frames, FRAMES_FIELDS);
    serializeBlock(lines, 1, "shadow", rule.shadow, SHADOW_FIELDS);
    serializeBlock(lines, 1, "runtime", rule.runtime, RUNTIME_FIELDS);
    serializeBlock(lines, 1, "cache", rule.cache, CACHE_FIELDS);
    serializeMetadata(lines, 1, rule.metadata);
  }
  function serializeSmartHide(lines, rule) {
    lines.push(`smart-hide:`);
    serializeTargets(lines, rule.targets);
    if (rule.paths) serializePaths(lines, rule.paths);
    lines.push(`${indent(1)}state: ${rule.state}`);
    serializeScope(lines, 1, rule.scope);
    serializeBoundaryBlock(lines, 1, rule.boundary);
    serializeMatchBlock(lines, 1, rule.match);
    serializeWhereOrExcept(lines, 1, "where", rule.where);
    serializeWhereOrExcept(lines, 1, "except", rule.except);
    serializeKeywordsBlock(lines, 1, rule.keywords);
    if (rule.keywordMerge !== void 0) lines.push(`${indent(1)}keyword-merge: ${rule.keywordMerge}`);
    if (rule.candidates && rule.candidates.length > 0) {
      lines.push(`${indent(1)}candidates:`);
      for (const c of rule.candidates) lines.push(`${indent(2)}- ${escapeYamlValue(c)}`);
    }
    serializeActionBlock(lines, 1, rule.action);
    serializeBlock(lines, 1, "safety", rule.safety, SAFETY_FIELDS);
    serializeBlock(lines, 1, "performance", rule.performance, PERFORMANCE_FIELDS);
    serializeBlock(lines, 1, "frames", rule.frames, FRAMES_FIELDS);
    serializeBlock(lines, 1, "shadow", rule.shadow, SHADOW_FIELDS);
    serializeBlock(lines, 1, "runtime", rule.runtime, RUNTIME_FIELDS);
    serializeBlock(lines, 1, "cache", rule.cache, CACHE_FIELDS);
    serializeMetadata(lines, 1, rule.metadata);
  }
  function serializeSmartAllow(lines, rule) {
    lines.push(`smart-allow:`);
    serializeTargets(lines, rule.targets);
    if (rule.paths) serializePaths(lines, rule.paths);
    lines.push(`${indent(1)}state: ${rule.state}`);
    serializeScope(lines, 1, rule.scope);
    serializeBoundaryBlock(lines, 1, rule.boundary);
    serializeMatchBlock(lines, 1, rule.match);
    serializeWhereOrExcept(lines, 1, "where", rule.where);
    serializeWhereOrExcept(lines, 1, "except", rule.except);
    serializeKeywordsBlock(lines, 1, rule.keywords);
    if (rule.keywordMerge !== void 0) lines.push(`${indent(1)}keyword-merge: ${rule.keywordMerge}`);
    if (rule.allowBroad !== void 0) lines.push(`${indent(1)}allow-broad: ${rule.allowBroad}`);
    serializeLogicOptions(lines, 1, rule.logicOptions);
    if (rule.candidates && rule.candidates.length > 0) {
      lines.push(`${indent(1)}candidates:`);
      for (const c of rule.candidates) lines.push(`${indent(2)}- ${escapeYamlValue(c)}`);
    }
    serializeActionBlock(lines, 1, rule.action);
    serializeBlock(lines, 1, "safety", rule.safety, SAFETY_FIELDS);
    serializeBlock(lines, 1, "performance", rule.performance, PERFORMANCE_FIELDS);
    serializeBlock(lines, 1, "frames", rule.frames, FRAMES_FIELDS);
    serializeBlock(lines, 1, "shadow", rule.shadow, SHADOW_FIELDS);
    serializeBlock(lines, 1, "runtime", rule.runtime, RUNTIME_FIELDS);
    serializeBlock(lines, 1, "cache", rule.cache, CACHE_FIELDS);
    serializeMetadata(lines, 1, rule.metadata);
  }
  function serializeTargets(lines, targets) {
    if (targets.length > 0) {
      lines.push(`${indent(1)}targets:`);
      for (const t of targets) {
        lines.push(`${indent(2)}- ${t.form}: ${escapeYamlValue(t.value)}`);
      }
    }
  }
  function serializePaths(lines, paths) {
    lines.push(`${indent(1)}paths:`);
    for (const p of paths) {
      lines.push(`${indent(2)}- ${p.form}: ${escapeYamlValue(p.value)}`);
    }
  }
  async function exportAllRules() {
    const rules = smartRuleStore.getAllRules();
    const yaml = serializeToYaml(rules);
    const lossMetadata = [];
    for (const rule of rules) {
      const loss = determineExportLoss(rule);
      lossMetadata.push(loss);
    }
    return {
      yaml,
      classicLines: [],
      lossMetadata,
      totalRules: rules.length,
      losslessCount: lossMetadata.filter((l) => l.code === "lossless").length,
      partialCount: lossMetadata.filter((l) => l.code === "partial").length,
      approximateCount: lossMetadata.filter((l) => l.code === "approximate").length,
      notPossibleCount: lossMetadata.filter((l) => l.code === "not-possible").length
    };
  }
  var SAFETY_FIELDS, PERFORMANCE_FIELDS, FRAMES_FIELDS, SHADOW_FIELDS, RUNTIME_FIELDS, CACHE_FIELDS, METADATA_FIELDS, PROVENANCE_FIELDS;
  var init_export = __esm({
    "src/core/smart-cosmetic/export.ts"() {
      init_smart_rule_store();
      init_export();
      SAFETY_FIELDS = [
        ["preview", "preview"],
        ["max-matches", "maxMatches"],
        ["max-page-percent", "maxPagePercent"],
        ["max-viewport-area-percent", "maxViewportAreaPercent"],
        ["min-features", "minFeatures"],
        ["min-logic-matches", "minLogicMatches"],
        ["allow-partial-apply", "allowPartialApply"],
        ["max-consecutive-partial", "maxConsecutivePartial"],
        ["partial-cycle-count", "partialCycleCount"],
        ["warn-if-scope-is-body", "warnIfScopeIsBody"],
        ["warn-if-no-where-logic", "warnIfNoWhereLogic"],
        ["warn-if-action-remove", "warnIfActionRemove"],
        ["confirm-page-root", "confirmPageRoot"],
        ["warn-if-weak-exact-reference", "warnIfWeakExactReference"]
      ];
      PERFORMANCE_FIELDS = [
        ["max-candidates", "maxCandidates"],
        ["max-evaluations-per-cycle", "maxEvaluationsPerCycle"],
        ["max-added-nodes-per-cycle", "maxAddedNodesPerCycle"],
        ["max-regex-ms-per-rule-cycle", "maxRegexMsPerRuleCycle"],
        ["max-regex-pattern-length", "maxRegexPatternLength"],
        ["max-text-regex-input-chars", "maxTextRegexInputChars"],
        ["debounce-ms", "debounceMs"],
        ["dependency-depth", "dependencyDepth"],
        ["dependency-prefilter", "dependencyPrefilter"],
        ["prefer-css-prefilter", "preferCssPrefilter"],
        ["metrics", "metrics"],
        ["sample-rate", "sampleRate"]
      ];
      FRAMES_FIELDS = [
        ["mode", "mode"],
        ["accounting", "accounting"],
        ["max-total-matches", "maxTotalMatches"]
      ];
      SHADOW_FIELDS = [
        ["mode", "mode"],
        ["observe-mutations", "observeMutations"],
        ["observe-recursive", "observeRecursive"],
        ["allow-host-ancestor", "allowHostAncestor"],
        ["max-roots-per-cycle", "maxRootsPerCycle"]
      ];
      RUNTIME_FIELDS = [
        ["observe-path-changes", "observePathChanges"],
        ["path-change-debounce-ms", "pathChangeDebounceMs"],
        ["re-evaluate-on-path-change", "reEvaluateOnPathChange"],
        ["observe-subtree", "observeSubtree"],
        ["observe-attributes", "observeAttributes"],
        ["error-recovery", "errorRecovery"],
        ["on-budget-exceeded", "onBudgetExceeded"],
        ["max-consecutive-partial-cycles", "maxConsecutivePartialCycles"]
      ];
      CACHE_FIELDS = [
        ["max-entries", "maxEntries"],
        ["max-age-ms", "maxAgeMs"],
        ["eviction-policy", "evictionPolicy"],
        ["scope", "scope"]
      ];
      METADATA_FIELDS = [
        ["source", "source"],
        ["created-at", "createdAt"],
        ["updated-at", "updatedAt"],
        ["title", "title"],
        ["description", "description"],
        ["created-by", "createdBy"],
        ["original-syntax", "originalSyntax"],
        ["original-id", "originalId"]
      ];
      PROVENANCE_FIELDS = [
        ["source", "source"],
        ["original-rule-id", "originalRuleId"],
        ["import-timestamp", "importTimestamp"],
        ["created-by", "createdBy"],
        ["created-from-host", "createdFromHost"],
        ["created-from-path", "createdFromPath"],
        ["original-rule-text", "originalRuleText"],
        ["original-source", "originalSource"]
      ];
    }
  });

  // src/core/smart-cosmetic/engine.ts
  var engine_exports = {};
  __export(engine_exports, {
    ConflictClass: () => ConflictClass,
    Engine: () => Engine,
    SmartEngine: () => engine_exports,
    exportAllRules: () => exportAllRules,
    getActionStrength: () => getActionStrength,
    hasHideLikeAction: () => hasHideLikeAction,
    parseSmartRules: () => parseSmartRules,
    seedDemoRules: () => seedDemoRules,
    smartEngine: () => smartEngine,
    smartRuleStore: () => smartRuleStore
  });
  async function seedDemoRules() {
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const rules = [
      {
        type: "hide-exact",
        id: "demo:simple-ad",
        syntaxVersion: 1,
        state: "active",
        priority: 50,
        metadata: { createdAt: now, source: "migration" },
        preview: { status: "confirmed" },
        targets: [{ form: "host", value: "example.com" }],
        selector: ".ad, .ads, .advertisement",
        action: { action: "hide" }
      },
      {
        type: "smart-hide",
        id: "demo:smart-card",
        syntaxVersion: 1,
        state: "active",
        priority: 50,
        metadata: { createdAt: now, source: "migration" },
        preview: { status: "confirmed" },
        targets: [{ form: "host", value: "example.com" }],
        candidates: [".card", ".item", "article"],
        boundary: { mode: "repeated-card", maxDepth: 6 },
        match: { mode: "none" },
        where: { condition: { field: "text", operator: "contains", value: "sponsored" } },
        action: { action: "hide" }
      }
    ];
    for (const rule of rules) {
      await smartRuleStore.addRule(rule);
    }
  }
  var MAX_SELECTORS_PER_BATCH, Engine, smartEngine;
  var init_engine = __esm({
    "src/core/smart-cosmetic/engine.ts"() {
      init_smart_rule_store();
      init_fetch_gateway();
      init_cosmetic_plan();
      init_conflict();
      init_smart_rule_store();
      init_export();
      init_smart_rule_parser();
      init_engine();
      MAX_SELECTORS_PER_BATCH = 50;
      Engine = class {
        constructor() {
          __publicField(this, "tabs", /* @__PURE__ */ new Map());
          __publicField(this, "activeDownloads", /* @__PURE__ */ new Set());
          __publicField(this, "initialized", false);
          __publicField(this, "trustedHostnames", /* @__PURE__ */ new Set());
        }
        async init() {
          if (this.initialized) return;
          await smartRuleStore.load();
          this.initialized = true;
        }
        async refresh() {
          await smartRuleStore.load();
        }
        getRulesForTab(tabId) {
          const tab = this.tabs.get(tabId);
          if (!tab) return [];
          const allRules = smartRuleStore.getAllRules();
          const matching = allRules.filter((r) => {
            if (r.state !== "active") return false;
            return this.ruleMatchesHost(r, tab.hostname);
          });
          return sortByConflictOrder(matching);
        }
        getCosmeticPlanForTab(tabId, url) {
          const hostname = this.extractHostname(url);
          if (this.isSiteTrusted(url)) {
            return { matched: 0, hidden: 0, removed: 0, plan: compileCosmeticPlan([], url, hostname) };
          }
          let tab = this.tabs.get(tabId);
          if (!tab) {
            tab = { url, hostname, appliedSelectors: /* @__PURE__ */ new Set(), lastApply: 0 };
            this.tabs.set(tabId, tab);
          }
          tab.url = url;
          tab.hostname = hostname;
          tab.lastApply = Date.now();
          const rules = this.getRulesForTab(tabId);
          const plan = compileCosmeticPlan(rules, url, hostname);
          return {
            matched: plan.cssSelectors.length + plan.smartRules.length,
            hidden: plan.cssSelectors.length,
            removed: 0,
            plan
          };
        }
        applyRulesToTab(tabId, url) {
          const hostname = this.extractHostname(url);
          if (this.isSiteTrusted(url)) {
            return { matched: 0, hidden: 0, removed: 0, selectors: [] };
          }
          let tab = this.tabs.get(tabId);
          if (!tab) {
            tab = { url, hostname, appliedSelectors: /* @__PURE__ */ new Set(), lastApply: 0 };
            this.tabs.set(tabId, tab);
          }
          tab.url = url;
          tab.hostname = hostname;
          tab.lastApply = Date.now();
          const rules = this.getRulesForTab(tabId);
          const selectorSet = /* @__PURE__ */ new Set();
          const removedSelectors = /* @__PURE__ */ new Set();
          const hideRules = rules.filter((r) => r.type !== "smart-allow");
          const allowRules = rules.filter((r) => r.type === "smart-allow");
          for (const rule of hideRules) {
            if (!hasHideLikeAction(rule)) continue;
            const result = this.processRule(rule, tab);
            if (rule.action.action === "remove") {
              for (const sel2 of result.selectors) {
                removedSelectors.add(sel2);
              }
            }
            for (const sel2 of result.selectors) {
              selectorSet.add(sel2);
            }
          }
          for (const rule of allowRules) {
            const removed = this.processSmartAllowAgainst(rule, selectorSet);
            for (const sel2 of removed) {
              selectorSet.delete(sel2);
              removedSelectors.add(sel2);
            }
          }
          tab.appliedSelectors = /* @__PURE__ */ new Set([...selectorSet]);
          const selectors = Array.from(selectorSet);
          return {
            matched: selectors.length,
            hidden: selectors.length,
            removed: removedSelectors.size,
            selectors
          };
        }
        processSmartAllowAgainst(rule, currentSelectors) {
          if (currentSelectors.size === 0) return [];
          const removed = [];
          if (rule.candidates) {
            for (const sel2 of rule.candidates) {
              if (currentSelectors.has(sel2)) {
                removed.push(sel2);
              }
            }
          }
          if (rule.scope) {
            for (const sel2 of rule.scope) {
              if (currentSelectors.has(sel2)) {
                removed.push(sel2);
              }
            }
          }
          return removed;
        }
        processRule(rule, tab) {
          switch (rule.type) {
            case "hide-exact":
              return this.processHideExact(rule);
            case "hide-similar":
              return this.processHideSimilar(rule, tab);
            case "smart-hide":
              return this.processSmartHide(rule, tab);
            default:
              return { selectors: [] };
          }
        }
        processHideExact(rule) {
          if (!rule.selector) return { selectors: [] };
          return { selectors: [rule.selector] };
        }
        processHideSimilar(rule, _tab) {
          void _tab;
          const selectors = rule.candidates ? [...rule.candidates] : [];
          const safety = rule.safety;
          const cap = safety?.maxMatches ?? MAX_SELECTORS_PER_BATCH;
          return { selectors: selectors.slice(0, cap) };
        }
        processSmartHide(rule, _tab) {
          const selectors = rule.candidates ? [...rule.candidates] : [];
          const safety = rule.safety;
          const cap = safety?.maxMatches ?? MAX_SELECTORS_PER_BATCH;
          return { selectors: selectors.slice(0, cap) };
        }
        ruleMatchesHost(rule, hostname) {
          for (const target of rule.targets) {
            if (target.form === "host" && target.value === hostname) return true;
            if (target.form === "domain") {
              const parts = hostname.split(".");
              const registered = parts.slice(-2).join(".");
              if (registered === target.value) return true;
              if (hostname === target.value) return true;
            }
            if (target.form === "regex") {
              try {
                if (new RegExp(target.value).test(hostname)) return true;
              } catch (e) {
                console.warn("[uBR] engine: ruleMatchesHost regex test failed", target.value, e);
                return false;
              }
            }
            if (target.form === "entity") {
              const entityName = target.value.replace(/\.\*$/, "");
              if (hostname === entityName) return true;
              if (hostname.startsWith(`${entityName}.`)) return true;
              if (hostname.endsWith(`.${entityName}`)) return true;
              if (hostname.includes(`.${entityName}.`)) return true;
            }
            if (target.value === "*") {
              if (rule?.metadata?.scope === "test-only" || rule?.safety?.allowGlobal === true) {
                return true;
              }
              return false;
            }
          }
          return false;
        }
        async triggerUpdate(url) {
          const hostname = this.extractHostname(url);
          const rules = smartRuleStore.getRulesByHost(hostname);
          const collectionsToUpdate = /* @__PURE__ */ new Set();
          for (const rule of rules) {
            if (rule.collectionId && rule.state === "active") {
              const col = smartRuleStore.getCollection(rule.collectionId);
              if (col?.sourceUrl) {
                collectionsToUpdate.add(rule.collectionId);
              }
            }
          }
          for (const colId of collectionsToUpdate) {
            if (!this.activeDownloads.has(colId)) {
              this.activeDownloads.add(colId);
              fetchGateway.fetchNow(colId).finally(() => {
                this.activeDownloads.delete(colId);
              });
            }
          }
        }
        subscribeToCollection(url, collectionId) {
          return fetchGateway.subscribe(url, collectionId);
        }
        unsubscribeFromCollection(collectionId) {
          return fetchGateway.unsubscribe(collectionId);
        }
        removeTab(tabId) {
          this.tabs.delete(tabId);
        }
        getTabSelectors(tabId) {
          const tab = this.tabs.get(tabId);
          return tab ? Array.from(tab.appliedSelectors) : [];
        }
        getAllRules() {
          return smartRuleStore.getAllRules();
        }
        getStats() {
          const allRules = smartRuleStore.getAllRules();
          return {
            totalRules: allRules.length,
            enabledRules: allRules.filter((r) => r.state === "active").length,
            loadedCollections: smartRuleStore.getAllCollections().length,
            pendingFetches: this.activeDownloads.size,
            tabsActive: this.tabs.size
          };
        }
        extractHostname(url) {
          try {
            return new URL(url).hostname;
          } catch (e) {
            console.warn("[uBR] engine: extractHostname URL parse failed", url, e);
            return url;
          }
        }
        setTrustedHostnames(hostnames) {
          this.trustedHostnames = new Set(hostnames);
        }
        addTrustedHostname(hostname) {
          this.trustedHostnames.add(hostname);
        }
        isSiteTrusted(url) {
          const hostname = this.extractHostname(url);
          if (this.trustedHostnames.has(hostname)) return true;
          if (this.trustedHostnames.has(`*.${hostname.split(".").slice(-2).join(".")}`)) return true;
          return false;
        }
      };
      smartEngine = new Engine();
    }
  });

  // src/js/static-dnr-filtering.ts
  var static_dnr_filtering_exports = {};
  __export(static_dnr_filtering_exports, {
    dnrRulesetFromRawLists: () => dnrRulesetFromRawLists,
    mergeRules: () => mergeRules
  });
  function addGenericCosmeticFilter(context, selector, isException) {
    if (selector === void 0) {
      return;
    }
    if (selector.length <= 1) {
      return;
    }
    if (selector.charCodeAt(0) === 123) {
      return;
    }
    const key = keyFromSelector(selector);
    if (isException) {
      if (context.genericCosmeticExceptions === void 0) {
        context.genericCosmeticExceptions = [];
      }
      context.genericCosmeticExceptions.push({ key, selector });
      return;
    }
    if (context.genericCosmeticFilters === void 0) {
      context.genericCosmeticFilters = [];
    }
    context.genericCosmeticFilters.push({ key, selector });
  }
  function addExtendedToDNR(context, parser) {
    if (parser.isExtendedFilter() === false) {
      return false;
    }
    if (parser.isScriptletFilter()) {
      if (parser.hasOptions() === false) {
        return;
      }
      if (context.scriptletFilters === void 0) {
        context.scriptletFilters = /* @__PURE__ */ new Map();
      }
      const exception2 = parser.isException();
      const args = parser.getScriptletArgs();
      const argsToken = JSON.stringify(args);
      for (const { hn: hn2, not, bad } of parser.getExtFilterDomainIterator()) {
        if (bad) {
          continue;
        }
        if (exception2) {
          continue;
        }
        let details2 = context.scriptletFilters.get(argsToken);
        if (details2 === void 0) {
          context.scriptletFilters.set(argsToken, details2 = { args });
          if (context.trustedSource) {
            details2.trustedSource = true;
          }
        }
        if (not) {
          if (details2.excludeMatches === void 0) {
            details2.excludeMatches = [];
          }
          details2.excludeMatches.push(hn2);
          continue;
        }
        if (details2.matches === void 0) {
          details2.matches = [];
        }
        if (details2.matches.includes("*")) {
          continue;
        }
        if (hn2 === "*") {
          details2.matches = ["*"];
          continue;
        }
        details2.matches.push(hn2);
      }
      return;
    }
    if (parser.isResponseheaderFilter()) {
      if (parser.hasError()) {
        return;
      }
      if (parser.hasOptions() === false) {
        return;
      }
      if (parser.isException()) {
        return;
      }
      const node = parser.getBranchFromType(NODE_TYPE_EXT_PATTERN_RESPONSEHEADER);
      if (node === 0) {
        return;
      }
      const header = parser.getNodeString(node);
      if (context.responseHeaderRules === void 0) {
        context.responseHeaderRules = [];
      }
      const rule = {
        action: {
          responseHeaders: [
            {
              header,
              operation: "remove"
            }
          ],
          type: "modifyHeaders"
        },
        condition: {
          resourceTypes: [
            "main_frame",
            "sub_frame"
          ]
        }
      };
      for (const { hn: hn2, not, bad } of parser.getExtFilterDomainIterator()) {
        if (bad) {
          continue;
        }
        if (isRegexOrPath(hn2)) {
          continue;
        }
        if (not) {
          if (rule.condition.excludedInitiatorDomains === void 0) {
            rule.condition.excludedInitiatorDomains = [];
          }
          rule.condition.excludedInitiatorDomains.push(hn2);
          continue;
        }
        if (hn2 === "*") {
          if (rule.condition.initiatorDomains !== void 0) {
            rule.condition.initiatorDomains = void 0;
          }
          continue;
        }
        if (rule.condition.initiatorDomains === void 0) {
          rule.condition.initiatorDomains = [];
        }
        rule.condition.initiatorDomains.push(hn2);
      }
      context.responseHeaderRules.push(rule);
      return;
    }
    if ((parser.flavorBits & parser.BITFlavorExtHTML) !== 0) {
      return;
    }
    if (parser.hasOptions() === false) {
      const { compiled: compiled2, exception: exception2 } = parser.result;
      addGenericCosmeticFilter(context, compiled2, exception2);
      return;
    }
    if (context.specificCosmeticFilters === void 0) {
      context.specificCosmeticFilters = /* @__PURE__ */ new Map();
    }
    const { compiled, exception, raw } = parser.result;
    if (compiled === void 0) {
      context.specificCosmeticFilters.set(`Invalid filter: ${raw}`, {
        rejected: true
      });
      return;
    }
    const matches = [];
    const excludeMatches = [];
    for (const { hn: hn2, not, bad } of parser.getExtFilterDomainIterator()) {
      if (bad) {
        continue;
      }
      if (not && exception) {
        continue;
      }
      if (not || exception) {
        excludeMatches.push(hn2);
      } else if (hn2 !== "*") {
        matches.push(hn2);
      }
    }
    if (matches.length === 0 && excludeMatches.length === 0) {
      return;
    }
    if (exception === false && matches.length === 0 && excludeMatches.length !== 0) {
      addGenericCosmeticFilter(context, compiled, false);
    }
    const key = compiled.startsWith("{") === false && keyFromSelector(compiled);
    let details = context.specificCosmeticFilters.get(compiled);
    if (details === void 0) {
      context.specificCosmeticFilters.set(compiled, details = {});
      if (key) {
        details.key = key;
      }
    }
    if (matches.length) {
      if (details.matches === void 0) {
        details.matches = [];
      }
      if (matches.includes("*")) {
        details.matches = ["*"];
      } else if (details.matches.includes("*") === false) {
        details.matches.push(...matches);
      }
    }
    if (excludeMatches.length) {
      if (details.excludeMatches === void 0) {
        details.excludeMatches = [];
      }
      details.excludeMatches.push(...excludeMatches);
    }
  }
  function addToDNR(context, list) {
    const env = context.env || [];
    const writer = new CompiledListWriter();
    const lineIter = new LineIterator(
      utils.preparser.prune(list.text, env)
    );
    const parser = new AstFilterParser({
      toDNR: true,
      nativeCssHas: env.includes("native_css_has"),
      badTypes: [NODE_TYPE_NET_OPTION_NAME_REDIRECTRULE],
      trustedSource: list.trustedSource || void 0
    });
    const compiler = static_net_filtering_default.createCompiler();
    writer.properties.set("name", list.name);
    compiler.start(writer);
    while (lineIter.eot() === false) {
      let line = lineIter.next();
      while (line.endsWith(" \\")) {
        if (lineIter.peek(4) !== "    ") {
          break;
        }
        line = line.slice(0, -2).trim() + lineIter.next().trim();
      }
      parser.parse(line);
      if (parser.isComment()) {
        if (line === `!#trusted on ${context.secret}`) {
          parser.options.trustedSource = true;
          context.trustedSource = true;
        } else if (line === `!#trusted off ${context.secret}`) {
          parser.options.trustedSource = false;
          context.trustedSource = false;
        }
        continue;
      }
      if (parser.isFilter() === false) {
        continue;
      }
      if (parser.hasError()) {
        if (parser.astError === AST_ERROR_OPTION_EXCLUDED) {
          context.invalid.add(`Incompatible with DNR: ${line}`);
        } else {
          context.invalid.add(`Rejected filter: ${line}`);
        }
        continue;
      }
      if (parser.isExtendedFilter()) {
        addExtendedToDNR(context, parser);
        continue;
      }
      if (parser.isNetworkFilter() === false) {
        continue;
      }
      if (compiler.compile(parser, writer)) {
        continue;
      }
      if (compiler.error !== void 0) {
        context.invalid.add(compiler.error);
      }
    }
    compiler.finish(writer);
    static_net_filtering_default.dnrFromCompiled(
      "add",
      context,
      new CompiledListReader(writer.toString())
    );
  }
  function mergeRules(rulesetMap, mergeTarget) {
    const sorter = (_2, v2) => {
      if (Array.isArray(v2)) {
        return typeof v2[0] === "string" ? v2.sort() : v2;
      }
      if (v2 instanceof Object) {
        const sorted = {};
        for (const kk of Object.keys(v2).sort()) {
          sorted[kk] = v2[kk];
        }
        return sorted;
      }
      return v2;
    };
    const ruleHasher = (rule, target) => {
      return JSON.stringify(rule, (k2, v2) => {
        if (k2.startsWith("_")) {
          return;
        }
        if (k2 === target) {
          return;
        }
        return sorter(k2, v2);
      });
    };
    const extractTargetValue = (obj, target) => {
      for (const [k2, v2] of Object.entries(obj)) {
        if (Array.isArray(v2) && k2 === target) {
          return v2;
        }
        if (v2 instanceof Object) {
          const r = extractTargetValue(v2, target);
          if (r !== void 0) {
            return r;
          }
        }
      }
    };
    const extractTargetOwner = (obj, target) => {
      for (const [k2, v2] of Object.entries(obj)) {
        if (Array.isArray(v2) && k2 === target) {
          return obj;
        }
        if (v2 instanceof Object) {
          const r = extractTargetOwner(v2, target);
          if (r !== void 0) {
            return r;
          }
        }
      }
    };
    const mergeMap = /* @__PURE__ */ new Map();
    for (const [id, rule] of rulesetMap) {
      if (rule._error !== void 0) {
        continue;
      }
      const hash = ruleHasher(rule, mergeTarget);
      if (mergeMap.has(hash) === false) {
        mergeMap.set(hash, []);
      }
      mergeMap.get(hash).push(id);
    }
    for (const ids of mergeMap.values()) {
      if (ids.length === 1) {
        continue;
      }
      const leftHand = rulesetMap.get(ids[0]);
      const leftHandSet = new Set(
        extractTargetValue(leftHand, mergeTarget) || []
      );
      for (let i = 1; i < ids.length; i++) {
        const rightHandId = ids[i];
        const rightHand = rulesetMap.get(rightHandId);
        const rightHandArray = extractTargetValue(rightHand, mergeTarget);
        if (rightHandArray !== void 0) {
          if (leftHandSet.size !== 0) {
            for (const item of rightHandArray) {
              leftHandSet.add(item);
            }
          }
        } else {
          leftHandSet.clear();
        }
        rulesetMap.delete(rightHandId);
      }
      const leftHandOwner = extractTargetOwner(leftHand, mergeTarget);
      if (leftHandSet.size > 1) {
        leftHandOwner[mergeTarget] = Array.from(leftHandSet).sort();
      } else if (leftHandSet.size === 0) {
        if (leftHandOwner !== void 0) {
          leftHandOwner[mergeTarget] = void 0;
        }
      }
    }
  }
  function finalizeRuleset(context, network) {
    const ruleset = network.ruleset;
    const rulesetMap = /* @__PURE__ */ new Map();
    {
      let ruleId = 1;
      for (const rule of ruleset) {
        rulesetMap.set(ruleId++, rule);
      }
    }
    mergeRules(rulesetMap, "resourceTypes");
    mergeRules(rulesetMap, "removeParams");
    mergeRules(rulesetMap, "initiatorDomains");
    mergeRules(rulesetMap, "requestDomains");
    mergeRules(rulesetMap, "responseHeaders");
    for (const rule of rulesetMap.values()) {
      const { condition } = rule;
      if (condition?.requestDomains === void 0) {
        continue;
      }
      if (condition.requestDomains.length !== 1) {
        continue;
      }
      if (condition.urlFilter !== void 0) {
        continue;
      }
      if (condition.regexFilter !== void 0) {
        continue;
      }
      condition.urlFilter = `||${condition.requestDomains[0]}^`;
      condition.requestDomains = void 0;
    }
    const rulesetFinal = [];
    {
      let ruleId = 1;
      for (const rule of rulesetMap.values()) {
        if (rule._error === void 0) {
          rule.id = ruleId++;
        } else {
          rule.id = 0;
        }
        rulesetFinal.push(rule);
      }
      for (const invalid of context.invalid) {
        rulesetFinal.push({ _error: [invalid] });
      }
    }
    network.ruleset = rulesetFinal;
  }
  async function dnrRulesetFromRawLists(lists, options = {}) {
    const context = Object.assign({}, options);
    context.bad = options.networkBad;
    static_net_filtering_default.dnrFromCompiled("begin", context);
    context.extensionPaths = new Map(context.extensionPaths || []);
    const toLoad = [];
    const toDNR = (context2, list) => addToDNR(context2, list);
    for (const list of lists) {
      if (list instanceof Promise) {
        toLoad.push(list.then((list2) => toDNR(context, list2)));
      } else {
        toLoad.push(toDNR(context, list));
      }
    }
    await Promise.all(toLoad);
    const result = {
      network: static_net_filtering_default.dnrFromCompiled("end", context),
      networkBad: context.bad,
      genericCosmeticFilters: context.genericCosmeticFilters,
      genericCosmeticExceptions: context.genericCosmeticExceptions,
      specificCosmetic: context.specificCosmeticFilters,
      scriptlet: context.scriptletFilters
    };
    if (context.responseHeaderRules) {
      result.network.ruleset.push(...context.responseHeaderRules);
    }
    finalizeRuleset(context, result.network);
    return result;
  }
  var isRegexOrPath, rePlainSelector, rePlainSelectorEx, rePlainSelectorEscaped, reEscapeSequence, keyFromSelector;
  var init_static_dnr_filtering = __esm({
    "src/js/static-dnr-filtering.ts"() {
      init_static_filtering_parser();
      init_static_filtering_io();
      init_text_utils();
      init_static_net_filtering();
      isRegexOrPath = (hn2) => hn2.includes("/");
      rePlainSelector = /^[#.][\w\\-]+/;
      rePlainSelectorEx = /^[^#.[(]+([#.][\w-]+)|([#.][\w-]+)$/;
      rePlainSelectorEscaped = /^[#.](?:\\[0-9A-Fa-f]+ |\\.|\w|-)+/;
      reEscapeSequence = /\\([0-9A-Fa-f]+ |.)/g;
      keyFromSelector = (selector) => {
        let key = "";
        let matches = rePlainSelector.exec(selector);
        if (matches) {
          key = matches[0];
        } else {
          matches = rePlainSelectorEx.exec(selector);
          if (matches === null) {
            return;
          }
          key = matches[1] || matches[2];
        }
        if (key.indexOf("\\") === -1) {
          return key;
        }
        matches = rePlainSelectorEscaped.exec(selector);
        if (matches === null) {
          return;
        }
        key = "";
        const escaped = matches[0];
        let beg = 0;
        reEscapeSequence.lastIndex = 0;
        for (; ; ) {
          matches = reEscapeSequence.exec(escaped);
          if (matches === null) {
            return key + escaped.slice(beg);
          }
          key += escaped.slice(beg, matches.index);
          beg = reEscapeSequence.lastIndex;
          if (matches[1].length === 1) {
            key += matches[1];
          } else {
            key += String.fromCharCode(parseInt(matches[1], 16));
          }
        }
      };
    }
  });

  // src/core/smart-cosmetic/logic-evaluator.ts
  var logic_evaluator_exports = {};
  __export(logic_evaluator_exports, {
    LogicEvaluator: () => logic_evaluator_exports,
    evaluateKeywordBlock: () => evaluateKeywordBlock,
    evaluateRuleLogic: () => evaluateRuleLogic,
    evaluateWhereExcept: () => evaluateWhereExcept,
    getDependencySelectors: () => getDependencySelectors
  });
  function evaluateWhereExcept(element, where, except, options) {
    const diagnostics = [];
    let whereMatchedCount = 0;
    let wherePassed = true;
    let exceptPassed = false;
    const evalOpts = options ?? {};
    if (where) {
      const whereResult = evaluateLogicExpression(element, where, "where", diagnostics, evalOpts);
      wherePassed = whereResult.passed;
      whereMatchedCount = whereResult.matchedCount;
    }
    if (except) {
      const exceptResult = evaluateLogicExpression(element, except, "except", diagnostics, evalOpts);
      exceptPassed = exceptResult.passed;
    }
    const minMatches = options?.minLogicMatches ?? 1;
    const matchCountOk = whereMatchedCount >= minMatches || !where;
    const passed = wherePassed && matchCountOk && !exceptPassed;
    return { passed, matchedCount: whereMatchedCount, diagnostics };
  }
  function evaluateLogicExpression(element, expr, context, diagnostics, options) {
    if ("condition" in expr) {
      const item = expr;
      const result = evaluateCondition(element, item.condition, diagnostics, options);
      return { passed: result, matchedCount: result ? 1 : 0 };
    }
    const group = expr;
    if (group.all) {
      let count = 0;
      for (const sub of group.all) {
        const r = evaluateLogicExpression(element, sub, context, diagnostics, options);
        if (!r.passed) return { passed: false, matchedCount: count };
        count += r.matchedCount;
      }
      return { passed: true, matchedCount: count };
    }
    if (group.any) {
      let count = 0;
      let found = false;
      for (const sub of group.any) {
        const r = evaluateLogicExpression(element, sub, context, diagnostics, options);
        if (r.passed) found = true;
        if (found) count += r.matchedCount;
      }
      if (found) return { passed: true, matchedCount: count };
      return { passed: false, matchedCount: 0 };
    }
    if (group.none) {
      let count = 0;
      for (const sub of group.none) {
        const r = evaluateLogicExpression(element, sub, context, diagnostics, options);
        if (r.passed) return { passed: false, matchedCount: count };
        count += r.matchedCount;
      }
      return { passed: true, matchedCount: 0 };
    }
    return { passed: false, matchedCount: 0 };
  }
  function evaluateCondition(element, condition, diagnostics, options) {
    const start = performance.now();
    const timeout = DEFAULT_PERFORMANCE.maxRegexMsPerRuleCycle ?? 10;
    try {
      const result = doEvaluateCondition(element, condition, diagnostics, options);
      const elapsed = performance.now() - start;
      if (elapsed > timeout) {
        diagnostics.push({
          code: "LOGIC_TIMEOUT",
          message: `Condition evaluation exceeded ${timeout}ms (${elapsed.toFixed(0)}ms)`
        });
      }
      return result;
    } catch (e) {
      diagnostics.push({
        code: "LOGIC_ERROR",
        message: `Condition evaluation error: ${e instanceof Error ? e.message : String(e)}`
      });
      return false;
    }
  }
  function doEvaluateCondition(element, condition, diagnostics, options) {
    if ("attrName" in condition && !("operator" in condition)) {
      const c = condition;
      return evaluateAttributeCondition(element, c);
    }
    if ("attrFamily" in condition) {
      const c = condition;
      return evaluateAttributeFamilyCondition(element, c);
    }
    if ("field" in condition && "operator" in condition) {
      const op2 = condition.operator;
      if (op2 === ">=" || op2 === "<=" || op2 === ">" || op2 === "<" || op2 === "==") {
        return evaluateNumericCondition(element, condition);
      }
    }
    if ("operator" in condition) {
      if (condition.operator === "count") {
        try {
          return condition.value && element.querySelectorAll(condition.value).length > 0;
        } catch (e) {
          console.warn("[uBR] logic-evaluator: count querySelectorAll failed", e);
          return false;
        }
      }
      if (condition.operator === "density") {
        try {
          const total = element.children.length;
          if (total === 0) return false;
          const matching = element.querySelectorAll(condition.value).length;
          return matching / total > 0;
        } catch (e) {
          console.warn("[uBR] logic-evaluator: density querySelectorAll failed", e);
          return false;
        }
      }
      switch (condition.operator) {
        case "equals":
        case "not-equals":
        case "contains":
        case "starts-with":
        case "ends-with": {
          const c = condition;
          return evaluateSimpleCondition(element, c, condition.operator === "not-equals");
        }
        case "regex": {
          const c = condition;
          return evaluateRegexCondition(element, c, diagnostics);
        }
        case "exists":
        case "not-exists": {
          const c = condition;
          return evaluateExistenceCondition(element, c);
        }
        case "selector-matches": {
          const c = condition;
          return element.matches?.(c.selector) ?? false;
        }
        case "has-descendant": {
          const c = condition;
          return evaluateHasDescendant(element, c, diagnostics);
        }
        case "has-ancestor": {
          const c = condition;
          return evaluateHasAncestor(element, c, diagnostics, options?.scopeRoot, options?.stopAtScopeForAncestor);
        }
        default:
          return false;
      }
    }
    return false;
  }
  function getFieldValue(element, field) {
    switch (field) {
      case "text": {
        if (element.shadowRoot) {
          return (element.shadowRoot.textContent ?? "").trim();
        }
        return (element.textContent ?? "").trim();
      }
      case "all-text": {
        if (element.shadowRoot) {
          return element.shadowRoot.textContent ?? "";
        }
        return element.textContent ?? "";
      }
      case "own-text": {
        let text = "";
        for (const node of element.childNodes) {
          if (node.nodeType === Node.TEXT_NODE) {
            text += node.textContent ?? "";
          }
        }
        return text.trim();
      }
      case "semantic-text": {
        const clone = element.cloneNode(true);
        for (const el2 of clone.querySelectorAll('script, style, template, [aria-hidden="true"]')) {
          el2.remove();
        }
        return (clone.textContent ?? "").trim();
      }
      case "aria-label":
        return element.getAttribute("aria-label") ?? "";
      case "alt":
        return element.getAttribute?.("alt") ?? "";
      case "href":
        return element.href ?? "";
      case "role":
        return element.getAttribute("role") ?? "";
      case "tag":
        return element.tagName.toLowerCase();
      default:
        if (field.startsWith("data-") || field.startsWith("aria-")) {
          return element.getAttribute(field) ?? "";
        }
        return element.getAttribute(field) ?? "";
    }
  }
  function getNumericFieldValue(element, field) {
    switch (field) {
      case "text-length":
        return (element.textContent ?? "").trim().length;
      case "link-count":
        return element.querySelectorAll("a[href]").length;
      case "link-density": {
        const textLen = (element.textContent ?? "").trim().length;
        if (textLen === 0) return 0;
        const linkTextLen = Array.from(element.querySelectorAll("a[href]")).reduce((s, a) => s + (a.textContent ?? "").trim().length, 0);
        return linkTextLen / textLen;
      }
      case "child-count":
        return element.children.length;
      default:
        return 0;
    }
  }
  function getAllMatchingAttributeValues(element, prefix) {
    const result = /* @__PURE__ */ new Map();
    const prefixLen = prefix.length;
    if (element.hasAttributes?.()) {
      for (let i = 0; i < element.attributes.length; i++) {
        const attr = element.attributes[i];
        if (attr.name.startsWith(prefix) && attr.name.length > prefixLen) {
          result.set(attr.name, attr.value);
        }
      }
    }
    return result;
  }
  function evaluateSimpleCondition(element, cond, negate) {
    const value = getFieldValue(element, cond.field);
    if (cond.field === "all-text") {
      const match = applySimpleMatch(value, cond.operator, cond.value, cond.caseSensitive);
      return negate ? !match : match;
    }
    switch (cond.operator) {
      case "equals":
        return compareText(value, cond.value, !negate, cond.caseSensitive);
      case "contains":
        return compareTextContains(value, cond.value, !negate, cond.caseSensitive);
      case "starts-with":
        return compareTextStartsWith(value, cond.value, !negate, cond.caseSensitive);
      case "ends-with":
        return compareTextEndsWith(value, cond.value, !negate, cond.caseSensitive);
      default:
        return false;
    }
  }
  function applySimpleMatch(text, operator, value, caseSensitive) {
    const a = caseSensitive ? text : text.toLowerCase();
    const b2 = caseSensitive ? value : value.toLowerCase();
    switch (operator) {
      case "equals":
        return a === b2;
      case "contains":
        return a.includes(b2);
      case "starts-with":
        return a.startsWith(b2);
      case "ends-with":
        return a.endsWith(b2);
      default:
        return false;
    }
  }
  function compareText(value, expected, shouldEqual, caseSensitive) {
    const a = caseSensitive ? value : value.toLowerCase();
    const b2 = caseSensitive ? expected : expected.toLowerCase();
    return shouldEqual ? a === b2 : a !== b2;
  }
  function compareTextContains(haystack, needle, shouldContain, caseSensitive) {
    const a = caseSensitive ? haystack : haystack.toLowerCase();
    const b2 = caseSensitive ? needle : needle.toLowerCase();
    return shouldContain ? a.includes(b2) : !a.includes(b2);
  }
  function compareTextStartsWith(text, prefix, shouldStart, caseSensitive) {
    const a = caseSensitive ? text : text.toLowerCase();
    const b2 = caseSensitive ? prefix : prefix.toLowerCase();
    return shouldStart ? a.startsWith(b2) : !a.startsWith(b2);
  }
  function compareTextEndsWith(text, suffix, shouldEnd, caseSensitive) {
    const a = caseSensitive ? text : text.toLowerCase();
    const b2 = caseSensitive ? suffix : suffix.toLowerCase();
    return shouldEnd ? a.endsWith(b2) : !a.endsWith(b2);
  }
  function evaluateRegexCondition(element, cond, diagnostics) {
    let value = getFieldValue(element, cond.field);
    if (value.length > MAX_TEXT_REGEX_INPUT_CHARS) {
      value = value.slice(0, MAX_TEXT_REGEX_INPUT_CHARS);
      diagnostics.push({ code: "TEXT_TRUNCATED", message: `Regex input truncated to ${MAX_TEXT_REGEX_INPUT_CHARS} chars` });
    }
    try {
      const flags = cond.flags ?? "";
      const re2 = new RegExp(cond.pattern, flags.includes("i") ? "i" : "");
      return re2.test(value);
    } catch (e) {
      console.warn("[uBR] logic-evaluator: regex test failed", cond.pattern, e);
      return false;
    }
  }
  function evaluateExistenceCondition(element, cond) {
    const value = getFieldValue(element, cond.field);
    const exists = value.length > 0;
    return cond.operator === "exists" ? exists : !exists;
  }
  function evaluateHasDescendant(element, cond, diagnostics) {
    try {
      const descendants = element.querySelectorAll(cond.selector);
      if (descendants.length > 0) return true;
      if (element.shadowRoot) {
        const shadowResult = element.shadowRoot.querySelector(cond.selector);
        if (shadowResult) return true;
      }
      return false;
    } catch {
      diagnostics.push({
        code: "INVALID_SELECTOR",
        message: `Invalid has-descendant selector: ${cond.selector}`
      });
      return false;
    }
  }
  function evaluateHasAncestor(element, cond, diagnostics, scopeRoot, stopAtScopeForAncestor) {
    let current = element.parentElement;
    let depth = 0;
    try {
      while (current && depth < MAX_ANCESTOR_DEPTH) {
        if (stopAtScopeForAncestor && scopeRoot && current === scopeRoot) return false;
        if (current.matches?.(cond.selector)) return true;
        current = current.parentElement;
        depth++;
      }
    } catch {
      diagnostics.push({
        code: "INVALID_SELECTOR",
        message: `Invalid has-ancestor selector: ${cond.selector}`
      });
    }
    return false;
  }
  function evaluateNumericCondition(element, cond) {
    const value = getNumericFieldValue(element, cond.field);
    switch (cond.operator) {
      case ">=":
        return value >= cond.value;
      case "<=":
        return value <= cond.value;
      case ">":
        return value > cond.value;
      case "<":
        return value < cond.value;
      case "==":
        return value === cond.value;
      default:
        return false;
    }
  }
  function evaluateAttributeCondition(element, cond) {
    const attrName = cond.attrName;
    if (cond.family === "data" || cond.family === "aria") {
      const prefix = cond.family === "data" ? "data-" : "aria-";
      const attrs = getAllMatchingAttributeValues(element, prefix);
      return attrs.size > 0;
    }
    const val = element.getAttribute(attrName) ?? "";
    return val.length > 0;
  }
  function applyAttributeValueMatch(value, operator, cond) {
    const pattern = cond.pattern;
    switch (operator) {
      case "equals":
        return value.toLowerCase() === pattern.toLowerCase();
      case "contains":
        return value.toLowerCase().includes(pattern.toLowerCase());
      case "regex": {
        try {
          return new RegExp(pattern, "i").test(value);
        } catch (e) {
          console.warn("[uBR] logic-evaluator: applyAttributeValueMatch regex failed", pattern, e);
          return false;
        }
      }
      default:
        return value.length > 0;
    }
  }
  function evaluateAttributeFamilyCondition(element, cond) {
    const prefix = cond.attrFamily === "data-*" ? "data-" : "aria-";
    const attrs = getAllMatchingAttributeValues(element, prefix);
    for (const [, val] of attrs) {
      if (applyAttributeValueMatch(val, cond.operator, cond)) return true;
    }
    return false;
  }
  function evaluateKeywordBlock(element, keywords) {
    const diagnostics = [];
    const fields = keywords.fields ?? ["text"];
    const text = fields.map((f) => getFieldValue(element, f)).join(" ").toLowerCase();
    const matchMode = keywords.matchMode ?? "phrase";
    const caseSensitive = keywords.caseSensitive ?? false;
    const wordBoundary = keywords.wordBoundary ?? false;
    let matchedCount = 0;
    function matchesKeyword(text2, keyword) {
      const t = caseSensitive ? text2 : text2.toLowerCase();
      const k2 = caseSensitive ? keyword : keyword.toLowerCase();
      switch (matchMode) {
        case "phrase":
          return t.includes(k2);
        case "word": {
          const pattern = wordBoundary ? new RegExp(`\\b${escapeRegex(k2)}\\b`, caseSensitive ? "" : "i") : new RegExp(escapeRegex(k2), caseSensitive ? "" : "i");
          return pattern.test(t);
        }
        case "regex": {
          try {
            return new RegExp(keyword, caseSensitive ? "" : "i").test(text2);
          } catch (e) {
            console.warn("[uBR] logic-evaluator: matchesKeyword regex failed", keyword, e);
            return false;
          }
        }
        default:
          return false;
      }
    }
    if (keywords.includeAny && keywords.includeAny.length > 0) {
      const anyPass = keywords.includeAny.some((kw) => matchesKeyword(text, kw));
      if (!anyPass) {
        return { passed: false, matchedCount: 0, diagnostics };
      }
      matchedCount++;
    }
    if (keywords.includeAll && keywords.includeAll.length > 0) {
      const allPass = keywords.includeAll.every((kw) => matchesKeyword(text, kw));
      if (!allPass) {
        return { passed: false, matchedCount: 0, diagnostics };
      }
      matchedCount++;
    }
    if (keywords.excludeAny && keywords.excludeAny.length > 0) {
      const anyExcluded = keywords.excludeAny.some((kw) => matchesKeyword(text, kw));
      if (anyExcluded) {
        return { passed: false, matchedCount: 0, diagnostics };
      }
    }
    if (keywords.excludeAll && keywords.excludeAll.length > 0) {
      const allExcluded = keywords.excludeAll.every((kw) => matchesKeyword(text, kw));
      if (allExcluded) {
        return { passed: false, matchedCount: 0, diagnostics };
      }
    }
    return { passed: true, matchedCount, diagnostics };
  }
  function escapeRegex(s) {
    return s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  }
  function evaluateRuleLogic(element, rule) {
    const isSmartHide = rule.type === "smart-hide";
    const smartHide = isSmartHide ? rule : null;
    if (isSmartHide && smartHide.keywords) {
      if (smartHide.keywordMerge) {
        if (smartHide.where) {
          const whereResult = evaluateWhereExcept(element, smartHide.where);
          if (!whereResult.passed) return whereResult;
        }
        const kwResult2 = evaluateKeywordBlock(element, smartHide.keywords);
        if (!kwResult2.passed) return kwResult2;
        if (rule.except) {
          const exceptResult = evaluateWhereExcept(element, void 0, rule.except);
          if (exceptResult.passed) return { passed: false, matchedCount: 0, diagnostics: exceptResult.diagnostics };
        }
        return { passed: true, matchedCount: 1, diagnostics: [] };
      }
      if (smartHide.where) {
        return evaluateWhereExcept(element, smartHide.where, rule.except, { minLogicMatches: rule.safety?.minLogicMatches });
      }
      const kwResult = evaluateKeywordBlock(element, smartHide.keywords);
      if (!kwResult.passed) return kwResult;
      if (rule.except) {
        const exceptResult = evaluateWhereExcept(element, void 0, rule.except);
        if (exceptResult.passed) return { passed: false, matchedCount: 0, diagnostics: exceptResult.diagnostics };
      }
      return { passed: true, matchedCount: 1, diagnostics: [] };
    }
    return evaluateWhereExcept(
      element,
      rule.where,
      rule.except,
      { minLogicMatches: rule.safety?.minLogicMatches }
    );
  }
  function getDependencySelectors(expr) {
    const selectors = [];
    collectDependencySelectors(expr, selectors);
    return selectors;
  }
  function collectDependencySelectors(expr, out) {
    if ("condition" in expr) {
      const item = expr;
      const c = item.condition;
      if ("operator" in c) {
        if (c.operator === "has-descendant") out.push(c.selector);
        if (c.operator === "selector-matches") out.push(c.selector);
        if (c.operator === "has-ancestor") out.push(c.selector);
      }
      return;
    }
    const group = expr;
    for (const sub of group.all ?? []) collectDependencySelectors(sub, out);
    for (const sub of group.any ?? []) collectDependencySelectors(sub, out);
    for (const sub of group.none ?? []) collectDependencySelectors(sub, out);
  }
  var MAX_ANCESTOR_DEPTH, MAX_TEXT_REGEX_INPUT_CHARS;
  var init_logic_evaluator = __esm({
    "src/core/smart-cosmetic/logic-evaluator.ts"() {
      init_smart_rule_schema();
      init_logic_evaluator();
      MAX_ANCESTOR_DEPTH = 100;
      MAX_TEXT_REGEX_INPUT_CHARS = 4096;
    }
  });

  // src/js/s14e-serializer.ts
  var VERSION = 1;
  var SEPARATORCHAR = " ";
  var SEPARATORCHARCODE = SEPARATORCHAR.charCodeAt(0);
  var SENTINELCHAR = "!";
  var SENTINELCHARCODE = SENTINELCHAR.charCodeAt(0);
  var MAGICPREFIX = `UOSC_${VERSION}${SEPARATORCHAR}`;
  var MAGICLZ4PREFIX = `UOSC/lz4_${VERSION}${SEPARATORCHAR}`;
  var FAILMARK = Number.MAX_SAFE_INTEGER;
  var SAFECHARS = "&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[]^_`abcdefghijklmnopqrstuvwxyz{|}~";
  var NUMSAFECHARS = SAFECHARS.length;
  var BITS_PER_SAFECHARS = Math.log2(NUMSAFECHARS);
  var { intToChar, intToCharCode, charCodeToInt } = (() => {
    const intToChar2 = [];
    const intToCharCode2 = [];
    const charCodeToInt2 = [];
    for (let i = 0; i < NUMSAFECHARS; i++) {
      intToChar2[i] = SAFECHARS.charAt(i);
      intToCharCode2[i] = SAFECHARS.charCodeAt(i);
      charCodeToInt2[i] = 0;
    }
    for (let i = NUMSAFECHARS; i < 128; i++) {
      intToChar2[i] = "";
      intToCharCode2[i] = 0;
      charCodeToInt2[i] = 0;
    }
    for (let i = 0; i < SAFECHARS.length; i++) {
      charCodeToInt2[SAFECHARS.charCodeAt(i)] = i;
    }
    return { intToChar: intToChar2, intToCharCode: intToCharCode2, charCodeToInt: charCodeToInt2 };
  })();
  var iota = 1;
  var I_STRING_SMALL = iota++;
  var I_STRING_LARGE = iota++;
  var I_ZERO = iota++;
  var I_INTEGER_SMALL_POS = iota++;
  var I_INTEGER_SMALL_NEG = iota++;
  var I_INTEGER_LARGE_POS = iota++;
  var I_INTEGER_LARGE_NEG = iota++;
  var I_BOOL_FALSE = iota++;
  var I_BOOL_TRUE = iota++;
  var I_NULL = iota++;
  var I_UNDEFINED = iota++;
  var I_FLOAT = iota++;
  var I_REGEXP = iota++;
  var I_DATE = iota++;
  var I_REFERENCE = iota++;
  var I_OBJECT_SMALL = iota++;
  var I_OBJECT_LARGE = iota++;
  var I_ARRAY_SMALL = iota++;
  var I_ARRAY_LARGE = iota++;
  var I_SET_SMALL = iota++;
  var I_SET_LARGE = iota++;
  var I_MAP_SMALL = iota++;
  var I_MAP_LARGE = iota++;
  var I_ARRAYBUFFER = iota++;
  var I_INT8ARRAY = iota++;
  var I_UINT8ARRAY = iota++;
  var I_UINT8CLAMPEDARRAY = iota++;
  var I_INT16ARRAY = iota++;
  var I_UINT16ARRAY = iota++;
  var I_INT32ARRAY = iota++;
  var I_UINT32ARRAY = iota++;
  var I_FLOAT32ARRAY = iota++;
  var I_FLOAT64ARRAY = iota++;
  var I_DATAVIEW = iota++;
  var C_STRING_SMALL = intToChar[I_STRING_SMALL];
  var C_STRING_LARGE = intToChar[I_STRING_LARGE];
  var C_ZERO = intToChar[I_ZERO];
  var C_INTEGER_SMALL_POS = intToChar[I_INTEGER_SMALL_POS];
  var C_INTEGER_SMALL_NEG = intToChar[I_INTEGER_SMALL_NEG];
  var C_INTEGER_LARGE_POS = intToChar[I_INTEGER_LARGE_POS];
  var C_INTEGER_LARGE_NEG = intToChar[I_INTEGER_LARGE_NEG];
  var C_BOOL_FALSE = intToChar[I_BOOL_FALSE];
  var C_BOOL_TRUE = intToChar[I_BOOL_TRUE];
  var C_NULL = intToChar[I_NULL];
  var C_UNDEFINED = intToChar[I_UNDEFINED];
  var C_FLOAT = intToChar[I_FLOAT];
  var C_REGEXP = intToChar[I_REGEXP];
  var C_DATE = intToChar[I_DATE];
  var C_REFERENCE = intToChar[I_REFERENCE];
  var C_OBJECT_SMALL = intToChar[I_OBJECT_SMALL];
  var C_OBJECT_LARGE = intToChar[I_OBJECT_LARGE];
  var C_ARRAY_SMALL = intToChar[I_ARRAY_SMALL];
  var C_ARRAY_LARGE = intToChar[I_ARRAY_LARGE];
  var C_SET_SMALL = intToChar[I_SET_SMALL];
  var C_SET_LARGE = intToChar[I_SET_LARGE];
  var C_MAP_SMALL = intToChar[I_MAP_SMALL];
  var C_MAP_LARGE = intToChar[I_MAP_LARGE];
  var C_ARRAYBUFFER = intToChar[I_ARRAYBUFFER];
  var C_INT8ARRAY = intToChar[I_INT8ARRAY];
  var C_UINT8ARRAY = intToChar[I_UINT8ARRAY];
  var C_UINT8CLAMPEDARRAY = intToChar[I_UINT8CLAMPEDARRAY];
  var C_INT16ARRAY = intToChar[I_INT16ARRAY];
  var C_UINT16ARRAY = intToChar[I_UINT16ARRAY];
  var C_INT32ARRAY = intToChar[I_INT32ARRAY];
  var C_UINT32ARRAY = intToChar[I_UINT32ARRAY];
  var C_FLOAT32ARRAY = intToChar[I_FLOAT32ARRAY];
  var C_FLOAT64ARRAY = intToChar[I_FLOAT64ARRAY];
  var C_DATAVIEW = intToChar[I_DATAVIEW];
  var I_STRING = I_STRING_SMALL;
  var I_NUMBER = I_FLOAT;
  var I_BOOL = I_BOOL_FALSE;
  var I_OBJECT = I_OBJECT_SMALL;
  var I_ARRAY = I_ARRAY_SMALL;
  var I_SET = I_SET_SMALL;
  var I_MAP = I_MAP_SMALL;
  var typeToSerializedInt = {
    "string": I_STRING,
    "number": I_NUMBER,
    "boolean": I_BOOL,
    "object": I_OBJECT
  };
  var xtypeToSerializedInt = {
    "[object RegExp]": I_REGEXP,
    "[object Date]": I_DATE,
    "[object Array]": I_ARRAY,
    "[object Set]": I_SET,
    "[object Map]": I_MAP,
    "[object ArrayBuffer]": I_ARRAYBUFFER,
    "[object Int8Array]": I_INT8ARRAY,
    "[object Uint8Array]": I_UINT8ARRAY,
    "[object Uint8ClampedArray]": I_UINT8CLAMPEDARRAY,
    "[object Int16Array]": I_INT16ARRAY,
    "[object Uint16Array]": I_UINT16ARRAY,
    "[object Int32Array]": I_INT32ARRAY,
    "[object Uint32Array]": I_UINT32ARRAY,
    "[object Float32Array]": I_FLOAT32ARRAY,
    "[object Float64Array]": I_FLOAT64ARRAY,
    "[object DataView]": I_DATAVIEW
  };
  var xtypeToSerializedChar = {
    "[object Int8Array]": C_INT8ARRAY,
    "[object Uint8Array]": C_UINT8ARRAY,
    "[object Uint8ClampedArray]": C_UINT8CLAMPEDARRAY,
    "[object Int16Array]": C_INT16ARRAY,
    "[object Uint16Array]": C_UINT16ARRAY,
    "[object Int32Array]": C_INT32ARRAY,
    "[object Uint32Array]": C_UINT32ARRAY,
    "[object Float32Array]": C_FLOAT32ARRAY,
    "[object Float64Array]": C_FLOAT64ARRAY
  };
  var toArrayBufferViewConstructor = {
    [`${I_INT8ARRAY}`]: Int8Array,
    [`${I_UINT8ARRAY}`]: Uint8Array,
    [`${I_UINT8CLAMPEDARRAY}`]: Uint8ClampedArray,
    [`${I_INT16ARRAY}`]: Int16Array,
    [`${I_UINT16ARRAY}`]: Uint16Array,
    [`${I_INT32ARRAY}`]: Int32Array,
    [`${I_UINT32ARRAY}`]: Uint32Array,
    [`${I_FLOAT32ARRAY}`]: Float32Array,
    [`${I_FLOAT64ARRAY}`]: Float64Array,
    [`${I_DATAVIEW}`]: DataView
  };
  var textCodec = {
    decoder: null,
    encoder: null,
    decode(...args) {
      if (this.decoder === null) {
        this.decoder = new globalThis.TextDecoder();
      }
      return this.decoder.decode(...args);
    },
    encode(...args) {
      if (this.encoder === null) {
        this.encoder = new globalThis.TextEncoder();
      }
      return this.encoder.encode(...args);
    },
    encodeInto(...args) {
      if (this.encoder === null) {
        this.encoder = new globalThis.TextEncoder();
      }
      return this.encoder.encodeInto(...args);
    }
  };
  var isInteger = Number.isInteger;
  var writeRefs = /* @__PURE__ */ new Map();
  var writeBuffer = [];
  var readRefs = /* @__PURE__ */ new Map();
  var readStr = "";
  var readPtr = 0;
  var readEnd = 0;
  var refCounter = 1;
  var uint8Input = null;
  var uint8InputFromAsciiStr = (s) => {
    if (uint8Input === null || uint8Input.length < s.length) {
      uint8Input = new Uint8Array(s.length + 1023 & ~1023);
    }
    textCodec.encodeInto(s, uint8Input);
    return uint8Input;
  };
  var isInstanceOf = (o, s) => {
    return typeof o === "object" && o !== null && (s === "Object" || Object.prototype.toString.call(o) === `[object ${s}]`);
  };
  var shouldCompress = (s, options) => options.compress === true && (options.compressThreshold === void 0 || options.compressThreshold <= s.length);
  var strFromLargeUint = (i) => {
    let r = 0, s = "";
    for (; ; ) {
      r = i % NUMSAFECHARS;
      s += intToChar[r];
      i -= r;
      if (i === 0) {
        break;
      }
      i /= NUMSAFECHARS;
    }
    return s + SEPARATORCHAR;
  };
  var deserializeLargeUint = () => {
    let c = readStr.charCodeAt(readPtr++);
    let n = charCodeToInt[c];
    let m = 1;
    while ((c = readStr.charCodeAt(readPtr++)) !== SEPARATORCHARCODE) {
      m *= NUMSAFECHARS;
      n += m * charCodeToInt[c];
    }
    return n;
  };
  var sparseValueLen = (v2) => v2 !== 0 ? (Math.log2(v2) / BITS_PER_SAFECHARS | 0) + 2 : 1;
  var analyzeArrayBuffer = (arrbuf) => {
    const byteLength = arrbuf.byteLength;
    const uint32len = byteLength >>> 2;
    const uint32arr = new Uint32Array(arrbuf, 0, uint32len);
    let notzeroCount = 0;
    for (let i = uint32len - 1; i >= 0; i--) {
      if (uint32arr[i] === 0) {
        continue;
      }
      notzeroCount = i + 1;
      break;
    }
    const end = notzeroCount + 1 <= uint32len ? notzeroCount << 2 : byteLength;
    const endUint32 = end >>> 2;
    const remUint8 = end & 3;
    const denseSize = endUint32 * 5 + (remUint8 ? remUint8 + 1 : 0);
    let sparseSize = 0;
    for (let i = 0; i < endUint32; i++) {
      sparseSize += sparseValueLen(uint32arr[i]);
      if (sparseSize > denseSize) {
        return { end, dense: true, denseSize };
      }
    }
    if (remUint8 !== 0) {
      sparseSize += 1;
      const uint8arr = new Uint8Array(arrbuf, endUint32 << 2);
      for (let i = 0; i < remUint8; i++) {
        sparseSize += sparseValueLen(uint8arr[i]);
      }
    }
    return { end, dense: false, sparseSize };
  };
  var denseArrayBufferToStr = (arrbuf, details) => {
    const end = details.end;
    const m = end % 4;
    const n = end - m;
    const uin32len = n >>> 2;
    const uint32arr = new Uint32Array(arrbuf, 0, uin32len);
    const output = new Uint8Array(details.denseSize);
    let j2 = 0, v2 = 0;
    for (let i = 0; i < uin32len; i++) {
      v2 = uint32arr[i];
      output[j2 + 0] = intToCharCode[v2 % NUMSAFECHARS];
      v2 = v2 / NUMSAFECHARS | 0;
      output[j2 + 1] = intToCharCode[v2 % NUMSAFECHARS];
      v2 = v2 / NUMSAFECHARS | 0;
      output[j2 + 2] = intToCharCode[v2 % NUMSAFECHARS];
      v2 = v2 / NUMSAFECHARS | 0;
      output[j2 + 3] = intToCharCode[v2 % NUMSAFECHARS];
      v2 = v2 / NUMSAFECHARS | 0;
      output[j2 + 4] = intToCharCode[v2];
      j2 += 5;
    }
    if (m !== 0) {
      const uint8arr = new Uint8Array(arrbuf, n);
      v2 = uint8arr[0];
      if (m > 1) {
        v2 += uint8arr[1] << 8;
        if (m > 2) {
          v2 += uint8arr[2] << 16;
        }
      }
      output[j2 + 0] = intToCharCode[v2 % NUMSAFECHARS];
      v2 = v2 / NUMSAFECHARS | 0;
      output[j2 + 1] = intToCharCode[v2 % NUMSAFECHARS];
      if (m > 1) {
        v2 = v2 / NUMSAFECHARS | 0;
        output[j2 + 2] = intToCharCode[v2 % NUMSAFECHARS];
        if (m > 2) {
          v2 = v2 / NUMSAFECHARS | 0;
          output[j2 + 3] = intToCharCode[v2 % NUMSAFECHARS];
        }
      }
    }
    return textCodec.decode(output);
  };
  var BASE88_POW1 = NUMSAFECHARS;
  var BASE88_POW2 = NUMSAFECHARS * BASE88_POW1;
  var BASE88_POW3 = NUMSAFECHARS * BASE88_POW2;
  var BASE88_POW4 = NUMSAFECHARS * BASE88_POW3;
  var denseArrayBufferFromStr = (denseStr, arrbuf) => {
    const input = uint8InputFromAsciiStr(denseStr);
    const end = denseStr.length;
    const m = end % 5;
    const n = end - m;
    const uin32len = n / 5 * 4 >>> 2;
    const uint32arr = new Uint32Array(arrbuf, 0, uin32len);
    let j2 = 0, v2 = 0;
    for (let i = 0; i < n; i += 5) {
      v2 = charCodeToInt[input[i + 0]];
      v2 += charCodeToInt[input[i + 1]] * BASE88_POW1;
      v2 += charCodeToInt[input[i + 2]] * BASE88_POW2;
      v2 += charCodeToInt[input[i + 3]] * BASE88_POW3;
      v2 += charCodeToInt[input[i + 4]] * BASE88_POW4;
      uint32arr[j2++] = v2;
    }
    if (m === 0) {
      return;
    }
    v2 = charCodeToInt[input[n + 0]] + charCodeToInt[input[n + 1]] * BASE88_POW1;
    if (m > 2) {
      v2 += charCodeToInt[input[n + 2]] * BASE88_POW2;
      if (m > 3) {
        v2 += charCodeToInt[input[n + 3]] * BASE88_POW3;
      }
    }
    const uint8arr = new Uint8Array(arrbuf, j2 << 2);
    uint8arr[0] = v2 & 255;
    if (v2 !== 0) {
      v2 >>>= 8;
      uint8arr[1] = v2 & 255;
      if (v2 !== 0) {
        v2 >>>= 8;
        uint8arr[2] = v2 & 255;
      }
    }
  };
  var sparseArrayBufferToStr = (arrbuf, details) => {
    const end = details.end;
    const uint8out = new Uint8Array(details.sparseSize);
    const uint32len = end >>> 2;
    const uint32arr = new Uint32Array(arrbuf, 0, uint32len);
    let j2 = 0, n = 0, r = 0;
    for (let i = 0; i < uint32len; i++) {
      n = uint32arr[i];
      if (n !== 0) {
        for (; ; ) {
          r = n % NUMSAFECHARS;
          uint8out[j2++] = intToCharCode[r];
          n -= r;
          if (n === 0) {
            break;
          }
          n /= NUMSAFECHARS;
        }
      }
      uint8out[j2++] = SEPARATORCHARCODE;
    }
    const uint8rem = end & 3;
    if (uint8rem !== 0) {
      uint8out[j2++] = SENTINELCHARCODE;
      const uint8arr = new Uint8Array(arrbuf, end - uint8rem, uint8rem);
      for (let i = 0; i < uint8rem; i++) {
        n = uint8arr[i];
        if (n !== 0) {
          for (; ; ) {
            r = n % NUMSAFECHARS;
            uint8out[j2++] = intToCharCode[r];
            n -= r;
            if (n === 0) {
              break;
            }
            n /= NUMSAFECHARS;
          }
        }
        uint8out[j2++] = SEPARATORCHARCODE;
      }
    }
    return textCodec.decode(uint8out);
  };
  var sparseArrayBufferFromStr = (sparseStr, arrbuf) => {
    const sparseLen = sparseStr.length;
    const input = uint8InputFromAsciiStr(sparseStr);
    const end = arrbuf.byteLength;
    const uint32len = end >>> 2;
    const uint32arr = new Uint32Array(arrbuf, 0, uint32len);
    let i = 0, j2 = 0, c = 0, n = 0, m = 0;
    for (; j2 < sparseLen; i++) {
      c = input[j2++];
      if (c === SEPARATORCHARCODE) {
        continue;
      }
      if (c === SENTINELCHARCODE) {
        break;
      }
      n = charCodeToInt[c];
      m = 1;
      for (; ; ) {
        c = input[j2++];
        if (c === SEPARATORCHARCODE) {
          break;
        }
        m *= NUMSAFECHARS;
        n += m * charCodeToInt[c];
      }
      uint32arr[i] = n;
    }
    if (c === SENTINELCHARCODE) {
      i <<= 2;
      const uint8arr = new Uint8Array(arrbuf, i);
      for (; j2 < sparseLen; i++) {
        c = input[j2++];
        if (c === SEPARATORCHARCODE) {
          continue;
        }
        n = charCodeToInt[c];
        m = 1;
        for (; ; ) {
          c = input[j2++];
          if (c === SEPARATORCHARCODE) {
            break;
          }
          m *= NUMSAFECHARS;
          n += m * charCodeToInt[c];
        }
        uint8arr[i] = n;
      }
    }
  };
  var _serialize = (data) => {
    if (data === 0) {
      writeBuffer.push(C_ZERO);
      return;
    }
    if (data === null) {
      writeBuffer.push(C_NULL);
      return;
    }
    if (data === void 0) {
      writeBuffer.push(C_UNDEFINED);
      return;
    }
    switch (typeToSerializedInt[typeof data]) {
      case I_STRING: {
        const length = data.length;
        if (length < NUMSAFECHARS) {
          writeBuffer.push(C_STRING_SMALL + intToChar[length], data);
        } else {
          writeBuffer.push(C_STRING_LARGE + strFromLargeUint(length), data);
        }
        return;
      }
      case I_NUMBER:
        if (isInteger(data)) {
          if (data >= NUMSAFECHARS) {
            writeBuffer.push(C_INTEGER_LARGE_POS + strFromLargeUint(data));
          } else if (data > 0) {
            writeBuffer.push(C_INTEGER_SMALL_POS + intToChar[data]);
          } else if (data > -NUMSAFECHARS) {
            writeBuffer.push(C_INTEGER_SMALL_NEG + intToChar[-data]);
          } else {
            writeBuffer.push(C_INTEGER_LARGE_NEG + strFromLargeUint(-data));
          }
        } else {
          const s = `${data}`;
          writeBuffer.push(C_FLOAT + strFromLargeUint(s.length) + s);
        }
        return;
      case I_BOOL:
        writeBuffer.push(data ? C_BOOL_TRUE : C_BOOL_FALSE);
        return;
      case I_OBJECT:
        break;
      default:
        return;
    }
    const xtypeName = Object.prototype.toString.call(data);
    const xtypeInt = xtypeToSerializedInt[xtypeName];
    if (xtypeInt === I_REGEXP) {
      writeBuffer.push(C_REGEXP);
      _serialize(data.source);
      _serialize(data.flags);
      return;
    }
    if (xtypeInt === I_DATE) {
      writeBuffer.push(C_DATE);
      _serialize(data.getTime());
      return;
    }
    const ref = writeRefs.get(data);
    if (ref !== void 0) {
      writeBuffer.push(C_REFERENCE + strFromLargeUint(ref));
      return;
    }
    writeRefs.set(data, refCounter++);
    switch (xtypeInt) {
      case I_ARRAY: {
        const size = data.length;
        if (size < NUMSAFECHARS) {
          writeBuffer.push(C_ARRAY_SMALL + intToChar[size]);
        } else {
          writeBuffer.push(C_ARRAY_LARGE + strFromLargeUint(size));
        }
        for (const v2 of data) {
          _serialize(v2);
        }
        return;
      }
      case I_SET: {
        const size = data.size;
        if (size < NUMSAFECHARS) {
          writeBuffer.push(C_SET_SMALL + intToChar[size]);
        } else {
          writeBuffer.push(C_SET_LARGE + strFromLargeUint(size));
        }
        for (const v2 of data) {
          _serialize(v2);
        }
        return;
      }
      case I_MAP: {
        const size = data.size;
        if (size < NUMSAFECHARS) {
          writeBuffer.push(C_MAP_SMALL + intToChar[size]);
        } else {
          writeBuffer.push(C_MAP_LARGE + strFromLargeUint(size));
        }
        for (const [k2, v2] of data) {
          _serialize(k2);
          _serialize(v2);
        }
        return;
      }
      case I_ARRAYBUFFER: {
        const byteLength = data.byteLength;
        writeBuffer.push(C_ARRAYBUFFER + strFromLargeUint(byteLength));
        _serialize(data.maxByteLength);
        const arrbuffDetails = analyzeArrayBuffer(data);
        _serialize(arrbuffDetails.dense);
        const str = arrbuffDetails.dense ? denseArrayBufferToStr(data, arrbuffDetails) : sparseArrayBufferToStr(data, arrbuffDetails);
        _serialize(str);
        return;
      }
      case I_INT8ARRAY:
      case I_UINT8ARRAY:
      case I_UINT8CLAMPEDARRAY:
      case I_INT16ARRAY:
      case I_UINT16ARRAY:
      case I_INT32ARRAY:
      case I_UINT32ARRAY:
      case I_FLOAT32ARRAY:
      case I_FLOAT64ARRAY:
        writeBuffer.push(
          xtypeToSerializedChar[xtypeName],
          strFromLargeUint(data.byteOffset),
          strFromLargeUint(data.length)
        );
        _serialize(data.buffer);
        return;
      case I_DATAVIEW:
        writeBuffer.push(C_DATAVIEW, strFromLargeUint(data.byteOffset), strFromLargeUint(data.byteLength));
        _serialize(data.buffer);
        return;
      default: {
        const keys = Object.keys(data);
        const size = keys.length;
        if (size < NUMSAFECHARS) {
          writeBuffer.push(C_OBJECT_SMALL + intToChar[size]);
        } else {
          writeBuffer.push(C_OBJECT_LARGE + strFromLargeUint(size));
        }
        for (const key of keys) {
          _serialize(key);
          _serialize(data[key]);
        }
        break;
      }
    }
  };
  var _deserialize = () => {
    if (readPtr >= readEnd) {
      return;
    }
    const type = charCodeToInt[readStr.charCodeAt(readPtr++)];
    switch (type) {
      // Primitive types
      case I_STRING_SMALL:
      case I_STRING_LARGE: {
        const size = type === I_STRING_SMALL ? charCodeToInt[readStr.charCodeAt(readPtr++)] : deserializeLargeUint();
        const beg = readPtr;
        readPtr += size;
        return readStr.slice(beg, readPtr);
      }
      case I_ZERO:
        return 0;
      case I_INTEGER_SMALL_POS:
        return charCodeToInt[readStr.charCodeAt(readPtr++)];
      case I_INTEGER_SMALL_NEG:
        return -charCodeToInt[readStr.charCodeAt(readPtr++)];
      case I_INTEGER_LARGE_POS:
        return deserializeLargeUint();
      case I_INTEGER_LARGE_NEG:
        return -deserializeLargeUint();
      case I_BOOL_FALSE:
        return false;
      case I_BOOL_TRUE:
        return true;
      case I_NULL:
        return null;
      case I_UNDEFINED:
        return;
      case I_FLOAT: {
        const size = deserializeLargeUint();
        const beg = readPtr;
        readPtr += size;
        return parseFloat(readStr.slice(beg, readPtr));
      }
      case I_REGEXP: {
        const source = _deserialize();
        const flags = _deserialize();
        return new RegExp(source, flags);
      }
      case I_DATE: {
        const time = _deserialize();
        return new Date(time);
      }
      case I_REFERENCE: {
        const ref = deserializeLargeUint();
        return readRefs.get(ref);
      }
      case I_OBJECT_SMALL:
      case I_OBJECT_LARGE: {
        const out = {};
        readRefs.set(refCounter++, out);
        const entries = [];
        const size = type === I_OBJECT_SMALL ? charCodeToInt[readStr.charCodeAt(readPtr++)] : deserializeLargeUint();
        for (let i = 0; i < size; i++) {
          const k2 = _deserialize();
          const v2 = _deserialize();
          entries.push([k2, v2]);
        }
        Object.assign(out, Object.fromEntries(entries));
        return out;
      }
      case I_ARRAY_SMALL:
      case I_ARRAY_LARGE: {
        const out = [];
        readRefs.set(refCounter++, out);
        const size = type === I_ARRAY_SMALL ? charCodeToInt[readStr.charCodeAt(readPtr++)] : deserializeLargeUint();
        for (let i = 0; i < size; i++) {
          out.push(_deserialize());
        }
        return out;
      }
      case I_SET_SMALL:
      case I_SET_LARGE: {
        const out = /* @__PURE__ */ new Set();
        readRefs.set(refCounter++, out);
        const size = type === I_SET_SMALL ? charCodeToInt[readStr.charCodeAt(readPtr++)] : deserializeLargeUint();
        for (let i = 0; i < size; i++) {
          out.add(_deserialize());
        }
        return out;
      }
      case I_MAP_SMALL:
      case I_MAP_LARGE: {
        const out = /* @__PURE__ */ new Map();
        readRefs.set(refCounter++, out);
        const size = type === I_MAP_SMALL ? charCodeToInt[readStr.charCodeAt(readPtr++)] : deserializeLargeUint();
        for (let i = 0; i < size; i++) {
          const k2 = _deserialize();
          const v2 = _deserialize();
          out.set(k2, v2);
        }
        return out;
      }
      case I_ARRAYBUFFER: {
        const byteLength = deserializeLargeUint();
        const maxByteLength = _deserialize();
        let options;
        if (maxByteLength !== 0 && maxByteLength !== byteLength) {
          options = { maxByteLength };
        }
        const arrbuf = new ArrayBuffer(byteLength, options);
        const dense = _deserialize();
        const str = _deserialize();
        if (dense) {
          denseArrayBufferFromStr(str, arrbuf);
        } else {
          sparseArrayBufferFromStr(str, arrbuf);
        }
        readRefs.set(refCounter++, arrbuf);
        return arrbuf;
      }
      case I_INT8ARRAY:
      case I_UINT8ARRAY:
      case I_UINT8CLAMPEDARRAY:
      case I_INT16ARRAY:
      case I_UINT16ARRAY:
      case I_INT32ARRAY:
      case I_UINT32ARRAY:
      case I_FLOAT32ARRAY:
      case I_FLOAT64ARRAY:
      case I_DATAVIEW: {
        const byteOffset = deserializeLargeUint();
        const length = deserializeLargeUint();
        const ref = refCounter++;
        const arrayBuffer = _deserialize();
        const ctor = toArrayBufferViewConstructor[`${type}`];
        const out = new ctor(arrayBuffer, byteOffset, length);
        readRefs.set(ref, out);
        return out;
      }
      default:
        break;
    }
    readPtr = FAILMARK;
  };
  var LZ4BlockJS = class {
    constructor() {
      this.hashTable = void 0;
      this.outputBuffer = void 0;
    }
    reset() {
      this.hashTable = void 0;
      this.outputBuffer = void 0;
    }
    growOutputBuffer(size) {
      if (this.outputBuffer !== void 0) {
        if (this.outputBuffer.byteLength >= size) {
          return;
        }
      }
      this.outputBuffer = new ArrayBuffer(size + 65535 & 2147418112);
    }
    encodeBound(size) {
      return size > 2113929216 ? 0 : size + (size / 255 | 0) + 16;
    }
    encodeBlock(iBuf, oOffset) {
      const iLen = iBuf.byteLength;
      if (iLen >= 2113929216) {
        throw new RangeError();
      }
      const lastMatchPos = iLen - 12;
      const lastLiteralPos = iLen - 5;
      if (this.hashTable === void 0) {
        this.hashTable = new Int32Array(65536);
      }
      this.hashTable.fill(-65536);
      if (isInstanceOf(iBuf, "ArrayBuffer")) {
        iBuf = new Uint8Array(iBuf);
      }
      const oLen = oOffset + this.encodeBound(iLen);
      this.growOutputBuffer(oLen);
      const oBuf = new Uint8Array(this.outputBuffer, 0, oLen);
      let iPos = 0;
      let oPos = oOffset;
      let anchorPos = 0;
      for (; ; ) {
        let refPos;
        let mOffset;
        let sequence = iBuf[iPos] << 8 | iBuf[iPos + 1] << 16 | iBuf[iPos + 2] << 24;
        while (iPos <= lastMatchPos) {
          sequence = sequence >>> 8 | iBuf[iPos + 3] << 24;
          const hash = (sequence * 40503 & 65535) + (sequence * 31153 >>> 16) & 65535;
          refPos = this.hashTable[hash];
          this.hashTable[hash] = iPos;
          mOffset = iPos - refPos;
          if (mOffset < 65536 && iBuf[refPos + 0] === (sequence & 255) && iBuf[refPos + 1] === (sequence >>> 8 & 255) && iBuf[refPos + 2] === (sequence >>> 16 & 255) && iBuf[refPos + 3] === (sequence >>> 24 & 255)) {
            break;
          }
          iPos += 1;
        }
        if (iPos > lastMatchPos) {
          break;
        }
        let lLen2 = iPos - anchorPos;
        let mLen = iPos;
        iPos += 4;
        refPos += 4;
        while (iPos < lastLiteralPos && iBuf[iPos] === iBuf[refPos]) {
          iPos += 1;
          refPos += 1;
        }
        mLen = iPos - mLen;
        const token = mLen < 19 ? mLen - 4 : 15;
        if (lLen2 >= 15) {
          oBuf[oPos++] = 240 | token;
          let l = lLen2 - 15;
          while (l >= 255) {
            oBuf[oPos++] = 255;
            l -= 255;
          }
          oBuf[oPos++] = l;
        } else {
          oBuf[oPos++] = lLen2 << 4 | token;
        }
        while (lLen2--) {
          oBuf[oPos++] = iBuf[anchorPos++];
        }
        if (mLen === 0) {
          break;
        }
        oBuf[oPos + 0] = mOffset;
        oBuf[oPos + 1] = mOffset >>> 8;
        oPos += 2;
        if (mLen >= 19) {
          let l = mLen - 19;
          while (l >= 255) {
            oBuf[oPos++] = 255;
            l -= 255;
          }
          oBuf[oPos++] = l;
        }
        anchorPos = iPos;
      }
      let lLen = iLen - anchorPos;
      if (lLen >= 15) {
        oBuf[oPos++] = 240;
        let l = lLen - 15;
        while (l >= 255) {
          oBuf[oPos++] = 255;
          l -= 255;
        }
        oBuf[oPos++] = l;
      } else {
        oBuf[oPos++] = lLen << 4;
      }
      while (lLen--) {
        oBuf[oPos++] = iBuf[anchorPos++];
      }
      return new Uint8Array(oBuf.buffer, 0, oPos);
    }
    decodeBlock(iBuf, iOffset, oLen) {
      const iLen = iBuf.byteLength;
      this.growOutputBuffer(oLen);
      const oBuf = new Uint8Array(this.outputBuffer, 0, oLen);
      let iPos = iOffset, oPos = 0;
      while (iPos < iLen) {
        const token = iBuf[iPos++];
        let clen = token >>> 4;
        if (clen !== 0) {
          if (clen === 15) {
            let l;
            for (; ; ) {
              l = iBuf[iPos++];
              if (l !== 255) {
                break;
              }
              clen += 255;
            }
            clen += l;
          }
          const end2 = iPos + clen;
          while (iPos < end2) {
            oBuf[oPos++] = iBuf[iPos++];
          }
          if (iPos === iLen) {
            break;
          }
        }
        const mOffset = iBuf[iPos + 0] | iBuf[iPos + 1] << 8;
        if (mOffset === 0 || mOffset > oPos) {
          return;
        }
        iPos += 2;
        clen = (token & 15) + 4;
        if (clen === 19) {
          let l;
          for (; ; ) {
            l = iBuf[iPos++];
            if (l !== 255) {
              break;
            }
            clen += 255;
          }
          clen += l;
        }
        const end = oPos + clen;
        let mPos = oPos - mOffset;
        while (oPos < end) {
          oBuf[oPos++] = oBuf[mPos++];
        }
      }
      return oBuf;
    }
    encode(input, outputOffset) {
      if (isInstanceOf(input, "ArrayBuffer")) {
        input = new Uint8Array(input);
      } else if (isInstanceOf(input, "Uint8Array") === false) {
        throw new TypeError();
      }
      return this.encodeBlock(input, outputOffset);
    }
    decode(input, inputOffset, outputSize) {
      if (isInstanceOf(input, "ArrayBuffer")) {
        input = new Uint8Array(input);
      } else if (isInstanceOf(input, "Uint8Array") === false) {
        throw new TypeError();
      }
      return this.decodeBlock(input, inputOffset, outputSize);
    }
  };
  var serialize = (data, options = {}) => {
    refCounter = 1;
    _serialize(data);
    writeBuffer.unshift(MAGICPREFIX);
    const s = writeBuffer.join("");
    writeRefs.clear();
    writeBuffer.length = 0;
    if (shouldCompress(s, options) === false) {
      return s;
    }
    const lz4Util = new LZ4BlockJS();
    const uint8ArrayBefore = textCodec.encode(s);
    const uint8ArrayAfter = lz4Util.encode(uint8ArrayBefore, 0);
    const lz4 = {
      size: uint8ArrayBefore.length,
      data: new Uint8Array(uint8ArrayAfter)
    };
    refCounter = 1;
    _serialize(lz4);
    writeBuffer.unshift(MAGICLZ4PREFIX);
    const t = writeBuffer.join("");
    writeRefs.clear();
    writeBuffer.length = 0;
    const ratio = t.length / s.length;
    return ratio <= 0.85 ? t : s;
  };
  var deserializeById = (blockid, s) => {
    refCounter = 1;
    readStr = s;
    readEnd = s.length;
    readPtr = blockid.length;
    const data = _deserialize();
    readRefs.clear();
    readStr = "";
    if (readPtr === FAILMARK) {
      return;
    }
    return data;
  };
  var deserialize = (s) => {
    if (s.startsWith(MAGICLZ4PREFIX)) {
      const lz4 = deserializeById(MAGICLZ4PREFIX, s);
      if (lz4) {
        const lz4Util = new LZ4BlockJS();
        const uint8ArrayAfter = lz4Util.decode(lz4.data, 0, lz4.size);
        if (uint8ArrayAfter) {
          s = textCodec.decode(new Uint8Array(uint8ArrayAfter));
        }
      }
    }
    const data = s.startsWith(MAGICPREFIX) ? deserializeById(MAGICPREFIX, s) : void 0;
    uint8Input = null;
    return data;
  };
  var isSerialized = (s) => typeof s === "string" && (s.startsWith(MAGICLZ4PREFIX) || s.startsWith(MAGICPREFIX));
  async function serializeAsync(data, options = {}) {
    return serialize(data, options);
  }
  async function deserializeAsync(data, options = {}) {
    if (isSerialized(data) === false) {
      return data;
    }
    return deserialize(data, options);
  }

  // src/js/filter-storage.ts
  var pendingWrites = [];
  async function flushWrites() {
    while (pendingWrites.length !== 0) {
      const promises = pendingWrites.slice();
      pendingWrites.length = 0;
      await Promise.all(promises);
    }
  }
  async function readFromStorage(key) {
    await flushWrites();
    try {
      const result = await browser.storage.local.get(key);
      return result[key] ?? void 0;
    } catch (e) {
      console.warn("[uBR] filter-storage: readFromStorage failed", key, e);
      return void 0;
    }
  }
  function writeToStorage(key, value) {
    pendingWrites.push(
      browser.storage.local.set({ [key]: value }).catch((e) => {
        console.warn("[uBR] filter-storage: writeToStorage failed", key, e);
      })
    );
  }
  function removeFromStorage(key) {
    pendingWrites.push(
      browser.storage.local.remove(key).catch((e) => {
        console.warn("[uBR] filter-storage: removeFromStorage failed", key, e);
      })
    );
  }
  async function keysFromStorage() {
    await flushWrites();
    try {
      const result = await browser.storage.local.get(null);
      return Object.keys(result);
    } catch (e) {
      console.warn("[uBR] filter-storage: keysFromStorage failed", e);
      return [];
    }
  }
  async function getAllCustomFilterKeys() {
    const storageKeys = await keysFromStorage() || [];
    return storageKeys.filter((key) => key.startsWith("site."));
  }
  async function customFiltersFromHostname(hostname) {
    const promises = [];
    let hn2 = hostname;
    while (hn2 !== "") {
      promises.push(readFromStorage(`site.${hn2}`));
      const pos = hn2.indexOf(".");
      if (pos === -1) {
        break;
      }
      hn2 = hn2.slice(pos + 1);
    }
    const results = await Promise.all(promises);
    const out = [];
    for (let i = 0; i < promises.length; i++) {
      const selectors = results[i];
      if (selectors === void 0) {
        continue;
      }
      for (const selector of selectors) {
        out.push(selector.startsWith("0") ? selector.slice(1) : selector);
      }
    }
    return out.sort();
  }
  async function getAllCustomFilters() {
    const collect = async (key) => {
      const selectors = await readFromStorage(key);
      const filtered = (selectors || []).map((a) => a.startsWith("0") ? a.slice(1) : a);
      return [key.slice(5), filtered];
    };
    const keys = await getAllCustomFilterKeys();
    const promises = keys.map((k2) => collect(k2));
    return Promise.all(promises);
  }
  async function addCustomFilters(hostname, toAdd) {
    if (hostname === "") {
      return false;
    }
    const key = `site.${hostname}`;
    const selectors = await readFromStorage(key) || [];
    const countBefore = selectors.length;
    for (const selector of toAdd) {
      if (selectors.includes(selector)) {
        continue;
      }
      selectors.push(selector);
    }
    if (selectors.length === countBefore) {
      return false;
    }
    selectors.sort();
    writeToStorage(key, selectors);
    return true;
  }
  async function removeAllCustomFilters(hostname) {
    if (hostname === "*") {
      const keys = await getAllCustomFilterKeys();
      if (keys.length === 0) {
        return false;
      }
      for (const key2 of keys) {
        removeFromStorage(key2);
      }
      return true;
    }
    const key = `site.${hostname}`;
    const selectors = await readFromStorage(key) || [];
    removeFromStorage(key);
    return selectors.length !== 0;
  }
  async function removeCustomFiltersByKey(key, toRemove) {
    const selectors = await readFromStorage(key);
    if (selectors === void 0) {
      return false;
    }
    const beforeCount = selectors.length;
    for (const selector of toRemove) {
      let i = selectors.indexOf(selector);
      if (i === -1) {
        i = selectors.indexOf(`0${selector}`);
        if (i === -1) {
          continue;
        }
      }
      selectors.splice(i, 1);
    }
    const afterCount = selectors.length;
    if (afterCount === beforeCount) {
      return false;
    }
    if (afterCount !== 0) {
      writeToStorage(key, selectors);
    } else {
      removeFromStorage(key);
    }
    return true;
  }
  async function removeCustomFilters(hostname, selectors) {
    const promises = [];
    let hn2 = hostname;
    while (hn2 !== "") {
      promises.push(removeCustomFiltersByKey(`site.${hn2}`, selectors));
      const pos = hn2.indexOf(".");
      if (pos === -1) {
        break;
      }
      hn2 = hn2.slice(pos + 1);
    }
    const results = await Promise.all(promises);
    return results.some((a) => a);
  }

  // src/js/messaging.ts
  init_static_filtering_parser();
  init_uri_utils();

  // src/js/dynamic-net-filtering.ts
  init_uri_utils();
  init_text_utils();
  init_punycode();

  // src/js/dom.ts
  var normalizeTarget = (target) => {
    if (typeof target === "string") {
      return Array.from(qsa$(target));
    }
    if (target instanceof Element) {
      return [target];
    }
    if (target === null) {
      return [];
    }
    if (Array.isArray(target)) {
      return target;
    }
    return Array.from(target);
  };
  var makeEventHandler = (selector, callback) => {
    return function(event) {
      const dispatcher = event.currentTarget;
      if (dispatcher instanceof HTMLElement === false || typeof dispatcher.querySelectorAll !== "function") {
        return;
      }
      const receiver = event.target;
      const ancestor = receiver?.closest(selector);
      if (ancestor === receiver && ancestor !== dispatcher && dispatcher.contains(ancestor)) {
        callback.call(receiver, event);
      }
    };
  };
  var dom = class {
    static attr(target, attr, value) {
      for (const elem of normalizeTarget(target)) {
        if (value === void 0) {
          return elem.getAttribute(attr);
        }
        if (value === null) {
          elem.removeAttribute(attr);
        } else {
          elem.setAttribute(attr, value);
        }
      }
      return void 0;
    }
    static clear(target) {
      for (const elem of normalizeTarget(target)) {
        while (elem.firstChild !== null) {
          elem.removeChild(elem.firstChild);
        }
      }
    }
    static clone(target) {
      const elements = normalizeTarget(target);
      if (elements.length === 0) {
        return null;
      }
      return elements[0].cloneNode(true);
    }
    static create(a) {
      if (typeof a === "string") {
        return document.createElement(a);
      }
      return void 0;
    }
    static prop(target, prop, value) {
      for (const elem of normalizeTarget(target)) {
        if (value === void 0) {
          return elem[prop];
        }
        elem[prop] = value;
      }
      return void 0;
    }
    static text(target, text) {
      const targets = normalizeTarget(target);
      if (text === void 0) {
        return targets.length !== 0 ? targets[0].textContent ?? void 0 : void 0;
      }
      for (const elem of targets) {
        elem.textContent = text;
      }
      return void 0;
    }
    static remove(target) {
      for (const elem of normalizeTarget(target)) {
        elem.remove();
      }
    }
    static empty(target) {
      for (const elem of normalizeTarget(target)) {
        while (elem.firstElementChild !== null) {
          elem.firstElementChild.remove();
        }
      }
    }
    // target, type, callback, [options]
    // target, type, subtarget, callback, [options]
    static on(target, type, subtarget, callback, options) {
      let actualCallback;
      let actualOptions;
      if (typeof subtarget === "function") {
        actualOptions = options;
        actualCallback = subtarget;
        subtarget = "";
        if (typeof actualOptions === "boolean") {
          actualOptions = { capture: true };
        }
      } else {
        actualCallback = makeEventHandler(subtarget, callback);
        if (actualOptions === void 0 || typeof actualOptions === "boolean") {
          actualOptions = { capture: true };
        } else {
          actualOptions.capture = true;
        }
      }
      const targets = target instanceof Window || target instanceof Document ? [target] : normalizeTarget(target);
      for (const elem of targets) {
        elem.addEventListener(type, actualCallback, actualOptions);
      }
    }
    static off(target, type, callback, options) {
      if (typeof callback !== "function") {
        return;
      }
      let actualOptions;
      if (typeof options === "boolean") {
        actualOptions = { capture: true };
      } else {
        actualOptions = options;
      }
      const targets = target instanceof Window || target instanceof Document ? [target] : normalizeTarget(target);
      for (const elem of targets) {
        elem.removeEventListener(type, callback, actualOptions);
      }
    }
    static onFirstShown(fn2, elem) {
      let observer = new IntersectionObserver((entries) => {
        if (entries.every((a) => a.isIntersecting === false)) {
          return;
        }
        try {
          fn2();
        } catch (e) {
          console.warn("[uBR] dom: onFirstShown callback failed", e);
        }
        observer?.disconnect();
        observer = void 0;
      });
      observer.observe(elem);
    }
  };
  __publicField(dom, "cl", null);
  __publicField(dom, "root", null);
  __publicField(dom, "html", null);
  __publicField(dom, "head", null);
  __publicField(dom, "body", null);
  dom.cl = class DomCl {
    static add(target, name2) {
      for (const elem of normalizeTarget(target)) {
        elem.classList.add(name2);
      }
    }
    static remove(target, ...names) {
      for (const elem of normalizeTarget(target)) {
        elem.classList.remove(...names);
      }
    }
    static toggle(target, name2, state) {
      let r;
      for (const elem of normalizeTarget(target)) {
        r = elem.classList.toggle(name2, state);
      }
      return r;
    }
    static has(target, name2) {
      for (const elem of normalizeTarget(target)) {
        if (elem.classList.contains(name2)) {
          return true;
        }
      }
      return false;
    }
  };
  function qs$(a, b2) {
    if (typeof a === "string") {
      return document.querySelector(a);
    }
    if (a === null) {
      return null;
    }
    return a.querySelector(b2 ?? "");
  }
  function qsa$(a, b2) {
    if (typeof a === "string") {
      return document.querySelectorAll(a);
    }
    if (a === null) {
      return [];
    }
    return a.querySelectorAll(b2 ?? "");
  }
  dom.root = qs$(":root");
  dom.html = document.documentElement;
  dom.head = document.head;
  dom.body = document.body;

  // src/js/i18n.ts
  var i18n = null;
  if (typeof self.browser !== "undefined" && self.browser instanceof Object && !(self.browser instanceof Element)) {
    i18n = self.browser.i18n;
  } else if (typeof self.chrome !== "undefined" && self.chrome.i18n) {
    i18n = self.chrome.i18n;
  }
  if (!i18n) {
    i18n = {
      getMessage: function(key, _args) {
        return key;
      },
      safeTemplateToDOM: function(_id, _dict, parent) {
        if (parent === void 0) {
          return document.createDocumentFragment();
        }
        return parent;
      },
      render: function(_context) {
      },
      renderElapsedTimeToString: function(_tstamp) {
        return "";
      },
      patchUnicodeFlags: function(_text) {
        return document.createDocumentFragment();
      }
    };
  }
  var i18n$ = (...args) => i18n.getMessage(args[0], args.slice(1));
  var isBackgroundProcess = document.title === "uBlock Resurrected Background Page";
  if (isBackgroundProcess !== true) {
    document.body.setAttribute(
      "dir",
      ["ar", "he", "fa", "ps", "ur"].indexOf(i18n$("@@ui_locale")) !== -1 ? "rtl" : "ltr"
    );
    const allowedTags = /* @__PURE__ */ new Set([
      "a",
      "b",
      "code",
      "em",
      "i",
      "span",
      "u"
    ]);
    const expandHtmlEntities = /* @__PURE__ */ (() => {
      const entities = /* @__PURE__ */ new Map([
        ["&shy;", "\xAD"],
        ["&ldquo;", "\u201C"],
        ["&rdquo;", "\u201D"],
        ["&lsquo;", "\u2018"],
        ["&rsquo;", "\u2019"],
        ["&lt;", "<"],
        ["&gt;", ">"]
      ]);
      const decodeEntities = (match) => {
        return entities.get(match) || match;
      };
      return function(text) {
        if (text.indexOf("&") !== -1) {
          text = text.replace(/&[a-z]+;/g, decodeEntities);
        }
        return text;
      };
    })();
    const safeTextToTextNode = function(text) {
      return document.createTextNode(expandHtmlEntities(text));
    };
    const sanitizeElement = function(node) {
      if (allowedTags.has(node.localName) === false) {
        return null;
      }
      node.removeAttribute("style");
      let child = node.firstElementChild;
      while (child !== null) {
        const next = child.nextElementSibling;
        if (sanitizeElement(child) === null) {
          child.remove();
        }
        child = next;
      }
      return node;
    };
    const safeTextToDOM = function(text, parent) {
      if (text === "") {
        return;
      }
      const hasTemplate = text.indexOf("{{") !== -1;
      if (text.indexOf("<") === -1 && !hasTemplate) {
        const toInsert = safeTextToTextNode(text);
        if (parent.childNodes.length !== 0) {
          const toRemove = [];
          let child = parent.firstChild;
          while (child !== null) {
            const next = child.nextSibling;
            if (child.nodeType === 3 && child.nodeValue !== null) {
              toRemove.push(child);
            }
            child = next;
          }
          for (const node2 of toRemove) {
            node2.remove();
          }
        }
        parent.appendChild(toInsert);
        return;
      }
      text = text.replace(/^<p>|<\/p>/g, "").replace(/<p>/g, "\n\n");
      const domParser = new DOMParser();
      const parsedDoc = domParser.parseFromString(text, "text/html");
      if (parent.childNodes.length !== 0) {
        const toRemove = [];
        let child = parent.firstChild;
        while (child !== null) {
          const next = child.nextSibling;
          if (child.nodeType === 3 && child.nodeValue !== null) {
            toRemove.push(child);
          }
          child = next;
        }
        for (const node2 of toRemove) {
          node2.remove();
        }
      }
      let node = parsedDoc.body.firstChild;
      while (node !== null) {
        const next = node.nextSibling;
        switch (node.nodeType) {
          case 1:
            if (sanitizeElement(node) === null) {
              break;
            }
            parent.appendChild(node);
            break;
          case 3:
            parent.appendChild(node);
            break;
          default:
            break;
        }
        node = next;
      }
    };
    i18n.safeTemplateToDOM = function(id, dict, parent) {
      if (parent === void 0) {
        parent = document.createDocumentFragment();
      }
      let textin = i18n$(id);
      if (textin === "") {
        return parent;
      }
      if (textin.indexOf("{{") === -1) {
        safeTextToDOM(textin, parent);
        return parent;
      }
      const re2 = /\{\{\w+\}\}/g;
      let textout = "";
      for (; ; ) {
        const match = re2.exec(textin);
        if (match === null) {
          textout += textin;
          break;
        }
        textout += textin.slice(0, match.index);
        const prop = match[0].slice(2, -2);
        if (Object.hasOwn(dict, prop)) {
          textout += dict[prop].replace(/</g, "&lt;").replace(/>/g, "&gt;");
        } else {
          textout += prop;
        }
        textin = textin.slice(re2.lastIndex);
      }
      safeTextToDOM(textout, parent);
      return parent;
    };
    i18n.render = function(context) {
      const docu = document;
      const root = context || docu;
      const elems = root.querySelectorAll("[data-i18n]");
      for (let i = 0; i < elems.length; i++) {
        const elem = elems[i];
        const text = i18n$(elem.getAttribute("data-i18n") || "");
        if (!text) {
          continue;
        }
        if (text.indexOf("{{") === -1) {
          safeTextToDOM(text, elem);
          if (elem.tagName === "TITLE") {
            document.title = text;
          }
          continue;
        }
        const parts = text.split(/(\{\{[^}]+\}\})/);
        const fragment = document.createDocumentFragment();
        let textBefore = "";
        for (let j2 = 0; j2 < parts.length; j2++) {
          const part = parts[j2];
          if (part === "") {
            continue;
          }
          if (part.startsWith("{{") && part.endsWith("}}")) {
            const pos = part.indexOf(":");
            if (pos !== -1) {
              part.slice(0, pos) + part.slice(-2);
            }
            const selector = part.slice(2, -2);
            let node;
            if (selector.charCodeAt(0) !== 46) {
              node = elem.querySelector(`.${selector}`);
            }
            if (node instanceof Element === false) {
              node = elem.querySelector(selector);
            }
            if (node instanceof Element) {
              safeTextToDOM(textBefore, fragment);
              fragment.appendChild(node);
              textBefore = "";
              continue;
            }
          }
          textBefore += part;
        }
        if (textBefore !== "") {
          safeTextToDOM(textBefore, fragment);
        }
        elem.appendChild(fragment);
      }
      const elemsTitle = root.querySelectorAll("[data-i18n-title]");
      for (let i = 0; i < elemsTitle.length; i++) {
        const elem = elemsTitle[i];
        const text = i18n$(elem.getAttribute("data-i18n-title") || "");
        if (!text) {
          continue;
        }
        elem.setAttribute("title", expandHtmlEntities(text));
      }
      const elemsPlaceholder = root.querySelectorAll("[placeholder]");
      for (let i = 0; i < elemsPlaceholder.length; i++) {
        const elem = elemsPlaceholder[i];
        const text = i18n$(elem.getAttribute("placeholder") || "");
        if (text === "") {
          continue;
        }
        elem.setAttribute("placeholder", text);
      }
      const elemsTip = root.querySelectorAll("[data-i18n-tip]");
      for (let i = 0; i < elemsTip.length; i++) {
        const elem = elemsTip[i];
        const text = i18n$(elem.getAttribute("data-i18n-tip") || "").replace(/<br>/g, "\n").replace(/\n{3,}/g, "\n\n");
        elem.setAttribute("data-tip", text);
        if (elem.getAttribute("aria-label") === "data-tip") {
          elem.setAttribute("aria-label", text);
        }
      }
      const elemsLabel = root.querySelectorAll("[data-i18n-label]");
      for (let i = 0; i < elemsLabel.length; i++) {
        const elem = elemsLabel[i];
        const text = i18n$(elem.getAttribute("data-i18n-label") || "");
        elem.setAttribute("label", text);
      }
    };
    i18n.renderElapsedTimeToString = function(tstamp) {
      let value = (Date.now() - tstamp) / 6e4;
      if (value < 2) {
        return i18n$("elapsedOneMinuteAgo");
      }
      if (value < 60) {
        return i18n$("elapsedManyMinutesAgo").replace("{{value}}", Math.floor(value).toLocaleString());
      }
      value /= 60;
      if (value < 2) {
        return i18n$("elapsedOneHourAgo");
      }
      if (value < 24) {
        return i18n$("elapsedManyHoursAgo").replace("{{value}}", Math.floor(value).toLocaleString());
      }
      value /= 24;
      if (value < 2) {
        return i18n$("elapsedOneDayAgo");
      }
      return i18n$("elapsedManyDaysAgo").replace("{{value}}", Math.floor(value).toLocaleString());
    };
    const unicodeFlagToImageSrc = /* @__PURE__ */ new Map([
      ["\u{1F1E6}\u{1F1F1}", "al"],
      ["\u{1F1E6}\u{1F1F7}", "ar"],
      ["\u{1F1E6}\u{1F1F9}", "at"],
      ["\u{1F1E7}\u{1F1E6}", "ba"],
      ["\u{1F1E7}\u{1F1EA}", "be"],
      ["\u{1F1E7}\u{1F1EC}", "bg"],
      ["\u{1F1E7}\u{1F1F7}", "br"],
      ["\u{1F1E8}\u{1F1E6}", "ca"],
      ["\u{1F1E8}\u{1F1ED}", "ch"],
      ["\u{1F1E8}\u{1F1F3}", "cn"],
      ["\u{1F1E8}\u{1F1F4}", "co"],
      ["\u{1F1E8}\u{1F1FE}", "cy"],
      ["\u{1F1E8}\u{1F1FF}", "cz"],
      ["\u{1F1E9}\u{1F1EA}", "de"],
      ["\u{1F1E9}\u{1F1F0}", "dk"],
      ["\u{1F1E9}\u{1F1FF}", "dz"],
      ["\u{1F1EA}\u{1F1EA}", "ee"],
      ["\u{1F1EA}\u{1F1EC}", "eg"],
      ["\u{1F1EA}\u{1F1F8}", "es"],
      ["\u{1F1EB}\u{1F1EE}", "fi"],
      ["\u{1F1EB}\u{1F1F4}", "fo"],
      ["\u{1F1EB}\u{1F1F7}", "fr"],
      ["\u{1F1EC}\u{1F1F7}", "gr"],
      ["\u{1F1ED}\u{1F1F7}", "hr"],
      ["\u{1F1ED}\u{1F1FA}", "hu"],
      ["\u{1F1EE}\u{1F1E9}", "id"],
      ["\u{1F1EE}\u{1F1F1}", "il"],
      ["\u{1F1EE}\u{1F1F3}", "in"],
      ["\u{1F1EE}\u{1F1F7}", "ir"],
      ["\u{1F1EE}\u{1F1F8}", "is"],
      ["\u{1F1EE}\u{1F1F9}", "it"],
      ["\u{1F1EF}\u{1F1F5}", "jp"],
      ["\u{1F1F0}\u{1F1F7}", "kr"],
      ["\u{1F1F0}\u{1F1FF}", "kz"],
      ["\u{1F1F1}\u{1F1F0}", "lk"],
      ["\u{1F1F1}\u{1F1F9}", "lt"],
      ["\u{1F1F1}\u{1F1FB}", "lv"],
      ["\u{1F1F2}\u{1F1E6}", "ma"],
      ["\u{1F1F2}\u{1F1E9}", "md"],
      ["\u{1F1F2}\u{1F1F0}", "mk"],
      ["\u{1F1F2}\u{1F1FD}", "mx"],
      ["\u{1F1F2}\u{1F1FE}", "my"],
      ["\u{1F1F3}\u{1F1F1}", "nl"],
      ["\u{1F1F3}\u{1F1F4}", "no"],
      ["\u{1F1F3}\u{1F1F5}", "np"],
      ["\u{1F1F5}\u{1F1F1}", "pl"],
      ["\u{1F1F5}\u{1F1F9}", "pt"],
      ["\u{1F1F7}\u{1F1F4}", "ro"],
      ["\u{1F1F7}\u{1F1F8}", "rs"],
      ["\u{1F1F7}\u{1F1FA}", "ru"],
      ["\u{1F1F8}\u{1F1E6}", "sa"],
      ["\u{1F1F8}\u{1F1EE}", "si"],
      ["\u{1F1F8}\u{1F1F0}", "sk"],
      ["\u{1F1F8}\u{1F1EA}", "se"],
      ["\u{1F1F8}\u{1F1F7}", "sr"],
      ["\u{1F1F9}\u{1F1ED}", "th"],
      ["\u{1F1F9}\u{1F1EF}", "tj"],
      ["\u{1F1F9}\u{1F1FC}", "tw"],
      ["\u{1F1F9}\u{1F1F7}", "tr"],
      ["\u{1F1FA}\u{1F1E6}", "ua"],
      ["\u{1F1FA}\u{1F1FF}", "uz"],
      ["\u{1F1FB}\u{1F1F3}", "vn"],
      ["\u{1F1FD}\u{1F1F0}", "xk"]
    ]);
    const reUnicodeFlags = new RegExp(
      Array.from(unicodeFlagToImageSrc).map((a) => a[0]).join("|"),
      "gu"
    );
    i18n.patchUnicodeFlags = function(text) {
      const fragment = document.createDocumentFragment();
      let i = 0;
      for (; ; ) {
        const match = reUnicodeFlags.exec(text);
        if (match === null) {
          break;
        }
        if (match.index > i) {
          fragment.append(text.slice(i, match.index));
        }
        const img = document.createElement("img");
        const countryCode = unicodeFlagToImageSrc.get(match[0]);
        img.src = `/img/flags-of-the-world/${countryCode}.png`;
        img.title = countryCode;
        img.classList.add("countryFlag");
        fragment.append(img, "\u200A");
        i = reUnicodeFlags.lastIndex;
      }
      if (i < text.length) {
        fragment.append(text.slice(i));
      }
      return fragment;
    };
    i18n.render();
  }

  // src/js/dashboard-common.ts
  self.uBlockDashboard = self.uBlockDashboard || {};
  self.uBlockDashboard.mergeNewLines = function(text, newText) {
    const fromDict = /* @__PURE__ */ new Map();
    let lineBeg = 0;
    let textEnd = text.length;
    while (lineBeg < textEnd) {
      let lineEnd = text.indexOf("\n", lineBeg);
      if (lineEnd === -1) {
        lineEnd = text.indexOf("\r", lineBeg);
        if (lineEnd === -1) {
          lineEnd = textEnd;
        }
      }
      const line = text.slice(lineBeg, lineEnd).trim();
      lineBeg = lineEnd + 1;
      if (line.length === 0) {
        continue;
      }
      const hash = line.slice(0, 8);
      const bucket = fromDict.get(hash);
      if (bucket === void 0) {
        fromDict.set(hash, line);
      } else if (typeof bucket === "string") {
        fromDict.set(hash, [bucket, line]);
      } else {
        bucket.push(line);
      }
    }
    const out = [""];
    lineBeg = 0;
    textEnd = newText.length;
    while (lineBeg < textEnd) {
      let lineEnd = newText.indexOf("\n", lineBeg);
      if (lineEnd === -1) {
        lineEnd = newText.indexOf("\r", lineBeg);
        if (lineEnd === -1) {
          lineEnd = textEnd;
        }
      }
      const line = newText.slice(lineBeg, lineEnd).trim();
      lineBeg = lineEnd + 1;
      if (line.length === 0) {
        if (out[out.length - 1] !== "") {
          out.push("");
        }
        continue;
      }
      const bucket = fromDict.get(line.slice(0, 8));
      if (bucket === void 0) {
        out.push(line);
        continue;
      }
      if (typeof bucket === "string" && line !== bucket) {
        out.push(line);
        continue;
      }
      if (bucket.indexOf(line) === -1) {
        out.push(line);
      }
    }
    const append = out.join("\n").trim();
    if (text !== "" && append !== "") {
      text += "\n\n";
    }
    return text + append;
  };
  self.uBlockDashboard.dateNowToSensibleString = function() {
    const now = new Date(Date.now() - (/* @__PURE__ */ new Date()).getTimezoneOffset() * 6e4);
    return now.toISOString().replace(/\.\d+Z$/, "").replace(/:/g, ".").replace("T", "_");
  };
  self.uBlockDashboard.patchCodeMirrorEditor = (function() {
    let grabFocusTarget;
    const grabFocus = function() {
      if (grabFocusTarget) {
        grabFocusTarget.focus();
      }
      grabFocusTarget = void 0;
    };
    const grabFocusTimer = vAPI.defer.create(grabFocus);
    const grabFocusAsync = function(cm) {
      grabFocusTarget = cm;
      grabFocusTimer.on(1);
    };
    const patchSelectAll = function(cm, details) {
      const vp2 = cm.getViewport();
      if (details.ranges.length !== 1) {
        return;
      }
      const range = details.ranges[0];
      const lineFrom = range.anchor.line;
      let lineTo = range.head.line;
      if (lineTo === lineFrom) {
        return;
      }
      if (range.head.ch !== 0) {
        lineTo += 1;
      }
      if (lineFrom !== vp2.from || lineTo !== vp2.to) {
        return;
      }
      details.update([
        {
          anchor: { line: 0, ch: 0 },
          head: { line: cm.lineCount(), ch: 0 }
        }
      ]);
      grabFocusAsync(cm);
    };
    let lastGutterClick = 0;
    let lastGutterLine = 0;
    const onGutterClicked = function(cm, line, gutter) {
      if (gutter !== "CodeMirror-linenumbers") {
        return;
      }
      grabFocusAsync(cm);
      const delta = Date.now() - lastGutterClick;
      if (delta >= 500 || line !== lastGutterLine) {
        cm.setSelection(
          { line, ch: 0 },
          { line: line + 1, ch: 0 }
        );
        lastGutterClick = Date.now();
        lastGutterLine = line;
        return;
      }
      let lineFrom = 0;
      let lineTo = cm.lineCount();
      const foldFn = cm.getHelper({ line, ch: 0 }, "fold");
      if (foldFn instanceof Function) {
        const range = foldFn(cm, { line, ch: 0 });
        if (range !== void 0) {
          lineFrom = range.from.line;
          lineTo = range.to.line + 1;
        }
      }
      cm.setSelection(
        { line: lineFrom, ch: 0 },
        { line: lineTo, ch: 0 },
        { scroll: false }
      );
      lastGutterClick = 0;
    };
    return function(cm) {
      if (cm.options.inputStyle === "contenteditable") {
        cm.on("beforeSelectionChange", patchSelectAll);
      }
      cm.on("gutterClick", onGutterClicked);
    };
  })();
  self.uBlockDashboard.openOrSelectPage = function(url, options = {}) {
    let ev;
    if (url instanceof MouseEvent) {
      ev = url;
      url = dom.attr(ev.target, "href");
    }
    const details = Object.assign({ url, select: true, index: -1 }, options);
    vAPI.messaging.send("default", {
      what: "gotoURL",
      details
    });
    if (ev) {
      ev.preventDefault();
    }
  };
  dom.attr("a", "target", "_blank");
  dom.attr('a[href*="dashboard.html"]', "target", "_parent");

  // src/js/dynamic-net-filtering.ts
  var supportedDynamicTypes = /* @__PURE__ */ Object.create(null);
  Object.assign(supportedDynamicTypes, {
    "3p": true,
    "image": true,
    "inline-script": true,
    "1p-script": true,
    "3p-script": true,
    "3p-frame": true
  });
  var typeBitOffsets = /* @__PURE__ */ Object.create(null);
  Object.assign(typeBitOffsets, {
    "*": 0,
    "inline-script": 2,
    "1p-script": 4,
    "3p-script": 6,
    "3p-frame": 8,
    "image": 10,
    "3p": 12
  });
  var nameToActionMap = /* @__PURE__ */ Object.create(null);
  Object.assign(nameToActionMap, {
    "block": 1,
    "allow": 2,
    "noop": 3
  });
  var intToActionMap = /* @__PURE__ */ new Map([
    [1, "block"],
    [2, "allow"],
    [3, "noop"]
  ]);
  var reBadHostname = /[^0-9a-z_.[\]:%-]/;
  var reNotASCII = /[^\x20-\x7F]/;
  var decomposedSource = [];
  var decomposedDestination = [];
  function is3rdParty(srcHostname, desHostname) {
    if (desHostname === "*" || srcHostname === "*" || srcHostname === "") {
      return false;
    }
    const srcDomain = domainFromHostname(srcHostname) || srcHostname;
    if (desHostname.endsWith(srcDomain) === false) {
      return true;
    }
    return desHostname.length !== srcDomain.length && desHostname.charAt(desHostname.length - srcDomain.length - 1) !== ".";
  }
  var _DynamicHostRuleFiltering = class _DynamicHostRuleFiltering {
    constructor() {
      __publicField(this, "r");
      __publicField(this, "type");
      __publicField(this, "y");
      __publicField(this, "z");
      __publicField(this, "rules");
      __publicField(this, "changed");
      this.reset();
    }
    reset() {
      this.r = 0;
      this.type = "";
      this.y = "";
      this.z = "";
      this.rules = /* @__PURE__ */ new Map();
      this.changed = false;
    }
    assign(other) {
      for (const k2 of this.rules.keys()) {
        if (other.rules.has(k2) === false) {
          this.rules.delete(k2);
          this.changed = true;
        }
      }
      for (const entry of other.rules) {
        if (this.rules.get(entry[0]) !== entry[1]) {
          this.rules.set(entry[0], entry[1]);
          this.changed = true;
        }
      }
    }
    copyRules(from, srcHostname, desHostnames) {
      let thisBits = this.rules.get("* *");
      let fromBits = from.rules.get("* *");
      if (fromBits !== thisBits) {
        if (fromBits !== void 0) {
          this.rules.set("* *", fromBits);
        } else {
          this.rules.delete("* *");
        }
        this.changed = true;
      }
      let key = `${srcHostname} *`;
      thisBits = this.rules.get(key);
      fromBits = from.rules.get(key);
      if (fromBits !== thisBits) {
        if (fromBits !== void 0) {
          this.rules.set(key, fromBits);
        } else {
          this.rules.delete(key);
        }
        this.changed = true;
      }
      for (const desHostname in desHostnames) {
        key = `* ${desHostname}`;
        thisBits = this.rules.get(key);
        fromBits = from.rules.get(key);
        if (fromBits !== thisBits) {
          if (fromBits !== void 0) {
            this.rules.set(key, fromBits);
          } else {
            this.rules.delete(key);
          }
          this.changed = true;
        }
        key = `${srcHostname} ${desHostname}`;
        thisBits = this.rules.get(key);
        fromBits = from.rules.get(key);
        if (fromBits !== thisBits) {
          if (fromBits !== void 0) {
            this.rules.set(key, fromBits);
          } else {
            this.rules.delete(key);
          }
          this.changed = true;
        }
      }
      return this.changed;
    }
    hasSameRules(other, srcHostname, desHostnames) {
      let key = "* *";
      if (this.rules.get(key) !== other.rules.get(key)) {
        return false;
      }
      key = `${srcHostname} *`;
      if (this.rules.get(key) !== other.rules.get(key)) {
        return false;
      }
      for (const desHostname in desHostnames) {
        key = `* ${desHostname}`;
        if (this.rules.get(key) !== other.rules.get(key)) {
          return false;
        }
        key = `${srcHostname} ${desHostname}`;
        if (this.rules.get(key) !== other.rules.get(key)) {
          return false;
        }
      }
      return true;
    }
    setCell(srcHostname, desHostname, type, state) {
      const bitOffset = typeBitOffsets[type];
      const k2 = `${srcHostname} ${desHostname}`;
      const oldBitmap = this.rules.get(k2) || 0;
      const newBitmap = oldBitmap & ~(3 << bitOffset) | state << bitOffset;
      if (newBitmap === oldBitmap) {
        return false;
      }
      if (newBitmap === 0) {
        this.rules.delete(k2);
      } else {
        this.rules.set(k2, newBitmap);
      }
      this.changed = true;
      return true;
    }
    unsetCell(srcHostname, desHostname, type) {
      this.evaluateCellZY(srcHostname, desHostname, type);
      if (this.r === 0) {
        return false;
      }
      this.setCell(srcHostname, desHostname, type, 0);
      this.changed = true;
      return true;
    }
    evaluateCell(srcHostname, desHostname, type) {
      const key = `${srcHostname} ${desHostname}`;
      const bitmap = this.rules.get(key);
      if (bitmap === void 0) {
        return 0;
      }
      return bitmap >> typeBitOffsets[type] & 3;
    }
    clearRegisters() {
      this.r = 0;
      this.type = this.y = this.z = "";
      return this;
    }
    evaluateCellZ(srcHostname, desHostname, type) {
      decomposeHostname(srcHostname, decomposedSource);
      this.type = type;
      const bitOffset = typeBitOffsets[type];
      for (const srchn of decomposedSource) {
        this.z = srchn;
        let v2 = this.rules.get(`${srchn} ${desHostname}`);
        if (v2 === void 0) {
          continue;
        }
        v2 = v2 >>> bitOffset & 3;
        if (v2 === 0) {
          continue;
        }
        return this.r = v2;
      }
      this.r = 0;
      return 0;
    }
    evaluateCellZY(srcHostname, desHostname, type) {
      if (desHostname === "") {
        this.r = 0;
        return 0;
      }
      decomposeHostname(desHostname, decomposedDestination);
      for (const deshn of decomposedDestination) {
        if (deshn === "*") {
          break;
        }
        this.y = deshn;
        if (this.evaluateCellZ(srcHostname, deshn, "*") !== 0) {
          return this.r;
        }
      }
      const thirdParty = is3rdParty(srcHostname, desHostname);
      this.y = "*";
      if (thirdParty) {
        if (type === "script") {
          if (this.evaluateCellZ(srcHostname, "*", "3p-script") !== 0) {
            return this.r;
          }
        } else if (type === "sub_frame" || type === "object") {
          if (this.evaluateCellZ(srcHostname, "*", "3p-frame") !== 0) {
            return this.r;
          }
        }
        if (this.evaluateCellZ(srcHostname, "*", "3p") !== 0) {
          return this.r;
        }
      } else if (type === "script") {
        if (this.evaluateCellZ(srcHostname, "*", "1p-script") !== 0) {
          return this.r;
        }
      }
      if (supportedDynamicTypes[type] !== void 0) {
        if (this.evaluateCellZ(srcHostname, "*", type) !== 0) {
          return this.r;
        }
        if (type.startsWith("3p-")) {
          if (this.evaluateCellZ(srcHostname, "*", "3p") !== 0) {
            return this.r;
          }
        }
      }
      if (this.evaluateCellZ(srcHostname, "*", "*") !== 0) {
        return this.r;
      }
      this.type = "";
      return 0;
    }
    mustAllowCellZY(srcHostname, desHostname, type) {
      return this.evaluateCellZY(srcHostname, desHostname, type) === 2;
    }
    mustBlockOrAllow() {
      return this.r === 1 || this.r === 2;
    }
    mustBlock() {
      return this.r === 1;
    }
    mustAbort() {
      return this.r === 3;
    }
    lookupRuleData(src, des, type) {
      const r = this.evaluateCellZY(src, des, type);
      if (r === 0) {
        return;
      }
      return `${this.z} ${this.y} ${this.type} ${r}`;
    }
    toLogData() {
      if (this.r === 0 || this.type === "") {
        return;
      }
      return {
        source: "dynamicHost",
        result: this.r,
        raw: `${this.z} ${this.y} ${this.type} ${intToActionMap.get(this.r)}`
      };
    }
    srcHostnameFromRule(rule) {
      return rule.slice(0, rule.indexOf(" "));
    }
    desHostnameFromRule(rule) {
      return rule.slice(rule.indexOf(" ") + 1);
    }
    toArray() {
      const out = [];
      for (const key of this.rules.keys()) {
        const srchn = this.srcHostnameFromRule(key);
        const deshn = this.desHostnameFromRule(key);
        const srchnPretty = srchn.includes("xn--") && punycode_default ? punycode_default.toUnicode(srchn) : srchn;
        const deshnPretty = deshn.includes("xn--") && punycode_default ? punycode_default.toUnicode(deshn) : deshn;
        for (const type in typeBitOffsets) {
          if (typeBitOffsets[type] === void 0) {
            continue;
          }
          const val = this.evaluateCell(srchn, deshn, type);
          if (val === 0) {
            continue;
          }
          const action = intToActionMap.get(val);
          if (action === void 0) {
            continue;
          }
          out.push(`${srchnPretty} ${deshnPretty} ${type} ${action}`);
        }
      }
      return out;
    }
    toString() {
      return this.toArray().join("\n");
    }
    fromString(text, append) {
      const lineIter = new LineIterator(text);
      if (append !== true) {
        this.reset();
      }
      while (lineIter.eot() === false) {
        this.addFromRuleParts(lineIter.next().trim().split(/\s+/));
      }
    }
    validateRuleParts(parts) {
      if (parts.length < 4) {
        return;
      }
      if (parts[0].endsWith(":")) {
        return;
      }
      if (parts[1].includes("/")) {
        return;
      }
      if (typeBitOffsets[parts[2]] === void 0) {
        return;
      }
      if (nameToActionMap[parts[3]] === void 0) {
        return;
      }
      if (parts[1] !== "*" && parts[2] !== "*") {
        return;
      }
      if (punycode_default !== void 0) {
        if (reNotASCII.test(parts[0])) {
          parts[0] = punycode_default.toASCII(parts[0]);
        }
        if (reNotASCII.test(parts[1])) {
          parts[1] = punycode_default.toASCII(parts[1]);
        }
      }
      if (parts[0] !== "*" && reBadHostname.test(parts[0]) || parts[1] !== "*" && reBadHostname.test(parts[1])) {
        return;
      }
      return parts;
    }
    addFromRuleParts(parts) {
      if (this.validateRuleParts(parts) !== void 0) {
        this.setCell(parts[0], parts[1], parts[2], nameToActionMap[parts[3]]);
        return true;
      }
      return false;
    }
    removeFromRuleParts(parts) {
      if (this.validateRuleParts(parts) !== void 0) {
        this.setCell(parts[0], parts[1], parts[2], 0);
        return true;
      }
      return false;
    }
    toSelfie() {
      return {
        magicId: _DynamicHostRuleFiltering.magicId,
        rules: Array.from(this.rules)
      };
    }
    fromSelfie(selfie) {
      if (selfie.magicId !== _DynamicHostRuleFiltering.magicId) {
        return false;
      }
      this.rules = new Map(selfie.rules);
      this.changed = true;
      return true;
    }
  };
  __publicField(_DynamicHostRuleFiltering, "magicId", 1);
  var DynamicHostRuleFiltering = _DynamicHostRuleFiltering;
  var dynamic_net_filtering_default = DynamicHostRuleFiltering;

  // src/js/hnswitches.ts
  init_text_utils();
  init_uri_utils();
  init_punycode();
  var decomposedSource2 = [];
  var switchBitOffsets = /* @__PURE__ */ Object.create(null);
  Object.assign(switchBitOffsets, {
    "no-strict-blocking": 0,
    "no-popups": 2,
    "no-cosmetic-filtering": 4,
    "no-remote-fonts": 6,
    "no-large-media": 8,
    "no-csp-reports": 10,
    "no-scripting": 12
  });
  var switchStateToNameMap = /* @__PURE__ */ Object.create(null);
  Object.assign(switchStateToNameMap, {
    "1": "true",
    "2": "false"
  });
  var nameToSwitchStateMap = /* @__PURE__ */ Object.create(null);
  Object.assign(nameToSwitchStateMap, {
    "true": 1,
    "false": 2,
    "on": 1,
    "off": 2
  });
  var reNotASCII2 = /[^\x20-\x7F]/;
  var DynamicSwitchRuleFiltering = class {
    constructor() {
      __publicField(this, "switches");
      __publicField(this, "n");
      __publicField(this, "z");
      __publicField(this, "r");
      __publicField(this, "changed");
      this.reset();
    }
    reset() {
      this.switches = /* @__PURE__ */ new Map();
      this.n = "";
      this.z = "";
      this.r = 0;
      this.changed = true;
    }
    assign(from) {
      for (const hn2 of this.switches.keys()) {
        if (from.switches.has(hn2) === false) {
          this.switches.delete(hn2);
          this.changed = true;
        }
      }
      for (const [hn2, bits] of from.switches) {
        if (this.switches.get(hn2) !== bits) {
          this.switches.set(hn2, bits);
          this.changed = true;
        }
      }
    }
    copyRules(from, srcHostname) {
      const thisBits = this.switches.get(srcHostname);
      const fromBits = from.switches.get(srcHostname);
      if (fromBits !== thisBits) {
        if (fromBits !== void 0) {
          this.switches.set(srcHostname, fromBits);
        } else {
          this.switches.delete(srcHostname);
        }
        this.changed = true;
      }
      return this.changed;
    }
    hasSameRules(other, srcHostname) {
      return this.switches.get(srcHostname) === other.switches.get(srcHostname);
    }
    toggle(switchName, hostname, newVal) {
      const bitOffset = switchBitOffsets[switchName];
      if (bitOffset === void 0) {
        return false;
      }
      if (newVal === this.evaluate(switchName, hostname)) {
        return false;
      }
      let bits = this.switches.get(hostname) || 0;
      bits &= ~(3 << bitOffset);
      bits |= newVal << bitOffset;
      if (bits === 0) {
        this.switches.delete(hostname);
      } else {
        this.switches.set(hostname, bits);
      }
      this.changed = true;
      return true;
    }
    toggleOneZ(switchName, hostname, newState) {
      const bitOffset = switchBitOffsets[switchName];
      if (bitOffset === void 0) {
        return false;
      }
      let state = this.evaluateZ(switchName, hostname);
      if (newState === state) {
        return false;
      }
      if (newState === void 0) {
        newState = !state;
      }
      let bits = this.switches.get(hostname) || 0;
      bits &= ~(3 << bitOffset);
      if (bits === 0) {
        this.switches.delete(hostname);
      } else {
        this.switches.set(hostname, bits);
      }
      state = this.evaluateZ(switchName, hostname);
      if (state !== newState) {
        this.switches.set(hostname, bits | (newState ? 1 : 2) << bitOffset);
      }
      this.changed = true;
      return true;
    }
    toggleBranchZ(switchName, targetHostname, newState) {
      this.toggleOneZ(switchName, targetHostname, newState);
      const targetLen = targetHostname.length;
      for (const hostname of this.switches.keys()) {
        if (hostname === targetHostname) {
          continue;
        }
        if (hostname.length <= targetLen) {
          continue;
        }
        if (hostname.endsWith(targetHostname) === false) {
          continue;
        }
        if (hostname.charAt(hostname.length - targetLen - 1) !== ".") {
          continue;
        }
        this.toggle(switchName, hostname, 0);
      }
      return this.changed;
    }
    toggleZ(switchName, hostname, deep, newState) {
      if (deep === true) {
        return this.toggleBranchZ(switchName, hostname, newState);
      }
      return this.toggleOneZ(switchName, hostname, newState);
    }
    evaluate(switchName, hostname) {
      const bits = this.switches.get(hostname);
      if (bits === void 0) {
        return 0;
      }
      const bitOffset = switchBitOffsets[switchName];
      if (bitOffset === void 0) {
        return 0;
      }
      return bits >>> bitOffset & 3;
    }
    evaluateZ(switchName, hostname) {
      const bitOffset = switchBitOffsets[switchName];
      if (bitOffset === void 0) {
        this.r = 0;
        return false;
      }
      this.n = switchName;
      for (const shn of decomposeHostname(hostname, decomposedSource2)) {
        let bits = this.switches.get(shn);
        if (bits === void 0) {
          continue;
        }
        bits = bits >>> bitOffset & 3;
        if (bits === 0) {
          continue;
        }
        this.z = shn;
        this.r = bits;
        return bits === 1;
      }
      this.r = 0;
      return false;
    }
    toLogData() {
      return {
        source: "switch",
        result: this.r,
        raw: `${this.n}: ${this.z} true`
      };
    }
    toArray() {
      const out = [];
      for (const hostname of this.switches.keys()) {
        const prettyHn = hostname.includes("xn--") && punycode_default ? punycode_default.toUnicode(hostname) : hostname;
        for (const switchName in switchBitOffsets) {
          if (switchBitOffsets[switchName] === void 0) {
            continue;
          }
          const val = this.evaluate(switchName, hostname);
          if (val === 0) {
            continue;
          }
          out.push(`${switchName}: ${prettyHn} ${switchStateToNameMap[val]}`);
        }
      }
      return out;
    }
    toString() {
      return this.toArray().join("\n");
    }
    fromString(text, append) {
      const lineIter = new LineIterator(text);
      if (append !== true) {
        this.reset();
      }
      while (lineIter.eot() === false) {
        this.addFromRuleParts(lineIter.next().trim().split(/\s+/));
      }
    }
    validateRuleParts(parts) {
      if (parts.length < 3) {
        return;
      }
      if (parts[0].endsWith(":") === false) {
        return;
      }
      if (nameToSwitchStateMap[parts[2]] === void 0) {
        return;
      }
      if (reNotASCII2.test(parts[1]) && punycode_default !== void 0) {
        parts[1] = punycode_default.toASCII(parts[1]);
      }
      return parts;
    }
    addFromRuleParts(parts) {
      if (this.validateRuleParts(parts) === void 0) {
        return false;
      }
      const switchName = parts[0].slice(0, -1);
      if (switchBitOffsets[switchName] === void 0) {
        return false;
      }
      this.toggle(switchName, parts[1], nameToSwitchStateMap[parts[2]]);
      return true;
    }
    removeFromRuleParts(parts) {
      if (this.validateRuleParts(parts) !== void 0) {
        this.toggle(parts[0].slice(0, -1), parts[1], 0);
        return true;
      }
      return false;
    }
  };
  var hnswitches_default = DynamicSwitchRuleFiltering;

  // src/js/url-net-filtering.ts
  init_text_utils();
  init_uri_utils();
  var actionToNameMap = {
    1: "block",
    2: "allow",
    3: "noop"
  };
  var nameToActionMap2 = /* @__PURE__ */ Object.create(null);
  Object.assign(nameToActionMap2, {
    "block": 1,
    "allow": 2,
    "noop": 3
  });
  var knownInvalidTypes = /* @__PURE__ */ new Set([
    "doc",
    "main_frame"
  ]);
  var intToActionMap2 = /* @__PURE__ */ new Map([
    [1, " block"],
    [2, " allow"],
    [3, " noop"]
  ]);
  var decomposedSource3 = [];
  var RuleEntry = class {
    constructor(url, action) {
      __publicField(this, "url");
      __publicField(this, "action");
      this.url = url;
      this.action = action;
    }
  };
  function indexOfURL(entries, url) {
    const urlLen = url.length;
    for (let i = 0; i < entries.length; i++) {
      const entry = entries[i];
      if (entry.url.length > urlLen) {
        break;
      }
      if (entry.url === url) {
        return i;
      }
    }
    return -1;
  }
  function indexOfMatch(entries, url) {
    const urlLen = url.length;
    let i = entries.length;
    while (i--) {
      if (entries[i].url.length <= urlLen) {
        break;
      }
    }
    if (i !== -1) {
      do {
        if (url.startsWith(entries[i].url)) {
          return i;
        }
      } while (i--);
    }
    return -1;
  }
  function indexFromLength(entries, len) {
    for (let i = 0; i < entries.length; i++) {
      if (entries[i].url.length > len) {
        return i;
      }
    }
    return -1;
  }
  function addRuleEntry(entries, url, action) {
    const entry = new RuleEntry(url, action);
    const i = indexFromLength(entries, url.length);
    if (i === -1) {
      entries.push(entry);
    } else {
      entries.splice(i, 0, entry);
    }
  }
  var DynamicURLRuleFiltering = class {
    constructor() {
      __publicField(this, "rules");
      __publicField(this, "context");
      __publicField(this, "url");
      __publicField(this, "type");
      __publicField(this, "r");
      __publicField(this, "changed");
      this.reset();
    }
    reset() {
      this.rules = /* @__PURE__ */ new Map();
      this.context = "";
      this.url = "";
      this.type = "";
      this.r = 0;
      this.changed = false;
    }
    assign(other) {
      for (const key of this.rules.keys()) {
        if (other.rules.has(key) === false) {
          this.rules.delete(key);
        }
      }
      for (const entry of other.rules) {
        this.rules.set(entry[0], entry[1].slice());
      }
      this.changed = true;
    }
    setRule(srcHostname, url, type, action) {
      if (action === 0) {
        return this.removeRule(srcHostname, url, type);
      }
      const bucketKey = `${srcHostname} ${type}`;
      let entries = this.rules.get(bucketKey);
      if (entries === void 0) {
        entries = [];
        this.rules.set(bucketKey, entries);
      }
      const i = indexOfURL(entries, url);
      if (i !== -1) {
        const entry = entries[i];
        if (entry.action === action) {
          return false;
        }
        entry.action = action;
      } else {
        addRuleEntry(entries, url, action);
      }
      this.changed = true;
      return true;
    }
    removeRule(srcHostname, url, type) {
      const bucketKey = `${srcHostname} ${type}`;
      const entries = this.rules.get(bucketKey);
      if (entries === void 0) {
        return false;
      }
      const i = indexOfURL(entries, url);
      if (i === -1) {
        return false;
      }
      entries.splice(i, 1);
      if (entries.length === 0) {
        this.rules.delete(bucketKey);
      }
      this.changed = true;
      return true;
    }
    evaluateZ(context, target, type) {
      this.r = 0;
      if (this.rules.size === 0) {
        return 0;
      }
      decomposeHostname(context, decomposedSource3);
      for (const srchn of decomposedSource3) {
        this.context = srchn;
        let entries = this.rules.get(`${srchn} ${type}`);
        if (entries !== void 0) {
          const i = indexOfMatch(entries, target);
          if (i !== -1) {
            const entry = entries[i];
            this.url = entry.url;
            this.type = type;
            this.r = entry.action;
            return this.r;
          }
        }
        entries = this.rules.get(`${srchn} *`);
        if (entries !== void 0) {
          const i = indexOfMatch(entries, target);
          if (i !== -1) {
            const entry = entries[i];
            this.url = entry.url;
            this.type = "*";
            this.r = entry.action;
            return this.r;
          }
        }
      }
      return 0;
    }
    mustAllowCellZ(context, target, type) {
      return this.evaluateZ(context, target, type) === 2;
    }
    mustBlockOrAllow() {
      return this.r === 1 || this.r === 2;
    }
    toLogData() {
      if (this.r === 0) {
        return;
      }
      const { context, url, type } = this;
      return {
        source: "dynamicUrl",
        result: this.r,
        rule: [context, url, type, intToActionMap2.get(this.r)],
        raw: `${context} ${url} ${type} ${intToActionMap2.get(this.r)}`
      };
    }
    copyRules(other, context, urls, type) {
      let i = urls.length;
      while (i--) {
        const url = urls[i];
        other.evaluateZ(context, url, type);
        const otherOwn = other.r !== 0 && other.context === context && other.url === url && other.type === type;
        this.evaluateZ(context, url, type);
        const thisOwn = this.r !== 0 && this.context === context && this.url === url && this.type === type;
        if (otherOwn && !thisOwn || other.r !== this.r) {
          this.setRule(context, url, type, other.r);
          this.changed = true;
        }
        if (!otherOwn && thisOwn) {
          this.removeRule(context, url, type);
          this.changed = true;
        }
      }
      return this.changed;
    }
    toArray() {
      const out = [];
      for (const [key, entries] of this.rules) {
        let pos = key.indexOf(" ");
        const hn2 = key.slice(0, pos);
        pos = key.lastIndexOf(" ");
        const type = key.slice(pos + 1);
        for (const { url, action } of entries) {
          out.push(`${hn2} ${url} ${type} ${actionToNameMap[action]}`);
        }
      }
      return out;
    }
    toString() {
      return this.toArray().sort().join("\n");
    }
    fromString(text) {
      this.reset();
      const lineIter = new LineIterator(text);
      while (lineIter.eot() === false) {
        this.addFromRuleParts(lineIter.next().trim().split(/\s+/));
      }
    }
    validateRuleParts(parts) {
      if (parts.length !== 4) {
        return;
      }
      if (parts[1].indexOf("://") <= 0) {
        return;
      }
      if (/[^a-z_-]/.test(parts[2]) && parts[2] !== "*" || knownInvalidTypes.has(parts[2])) {
        return;
      }
      if (nameToActionMap2[parts[3]] === void 0) {
        return;
      }
      return parts;
    }
    addFromRuleParts(parts) {
      if (this.validateRuleParts(parts) !== void 0) {
        this.setRule(parts[0], parts[1], parts[2], nameToActionMap2[parts[3]]);
        return true;
      }
      return false;
    }
    removeFromRuleParts(parts) {
      if (this.validateRuleParts(parts) !== void 0) {
        this.removeRule(parts[0], parts[1], parts[2]);
        return true;
      }
      return false;
    }
  };
  var url_net_filtering_default = DynamicURLRuleFiltering;

  // src/js/filtering-compiler.ts
  init_static_net_filtering();
  static_net_filtering_default.compile = function() {
    const rules = [];
    const ruleId = { value: 1 };
    const filterCount = this.getFilterCount();
    console.log(`[compile] Total filters: ${filterCount}`);
    const stats = this.getStats();
    console.log(`[compile] Engine stats:`, stats);
    return rules;
  };
  static_net_filtering_default.getFilterCount = function() {
    return this.filterDataWritePtr || 0;
  };
  static_net_filtering_default.getStats = function() {
    const out = [];
    if (typeof this.dump === "function") {
      this.dump(out);
    }
    return {
      filterCount: this.filterDataWritePtr || 0,
      dump: out.join("\n")
    };
  };
  static_net_filtering_default.filterToDNRRule = function(filter) {
    return null;
  };
  static_net_filtering_default.getAllFilters = function() {
    const filters = [];
    return filters;
  };
  static_net_filtering_default.toDNRRules = async function() {
    console.log("[toDNRRules] Starting compilation...");
    const rules = [];
    const filters = this.getAllFilters();
    for (const filter of filters) {
      const rule = this.filterToDNRRule(filter);
      if (rule) {
        rules.push(rule);
      }
    }
    console.log(`[toDNRRules] Compiled ${rules.length} rules`);
    return rules;
  };
  static_net_filtering_default.getBlockedCount = function() {
    return this.blockedCount || 0;
  };
  static_net_filtering_default.getAllowedCount = function() {
    return this.allowedCount || 0;
  };

  // src/js/filtering-engines.ts
  var permanentFirewall = new dynamic_net_filtering_default();
  var sessionFirewall = new dynamic_net_filtering_default();
  var permanentURLFiltering = new url_net_filtering_default();
  var sessionURLFiltering = new url_net_filtering_default();
  var permanentSwitches = new hnswitches_default();
  var sessionSwitches = new hnswitches_default();

  // src/js/console.js
  function ubologSet(state = false) {
    if (state) {
      if (ubolog.process instanceof Function) {
        ubolog.process();
      }
      ubolog = ubologDo;
    } else {
      ubolog = ubologIgnore;
    }
  }
  function ubologDo(...args) {
    console.info("[uBO]", ...args);
  }
  function ubologIgnore() {
  }
  var ubolog = (() => {
    const pending = [];
    const store = function(...args) {
      pending.push(args);
    };
    store.process = function() {
      for (const args of pending) {
        ubologDo(...args);
      }
    };
    return store;
  })();

  // src/js/webext.ts
  var webext = {
    tabs: {
      sendMessage: function(tabId, message, options) {
        return chrome.tabs.sendMessage(tabId, message, options);
      }
    },
    storage: {
      local: void 0,
      session: void 0,
      sync: void 0
    }
  };
  try {
    if (typeof chrome !== "undefined" && chrome.storage) {
      webext.storage.local = chrome.storage.local;
      webext.storage.session = chrome.storage.session;
      webext.storage.sync = chrome.storage.sync;
    }
  } catch (e) {
    console.warn("[uBR] webext: storage initialization failed", e);
  }
  window.webext = webext;
  var webext_default = webext;

  // src/js/background.ts
  init_uri_utils();
  init_filtering_context();

  // src/js/broadcast.ts
  var webRequestAPI = typeof self.browser !== "undefined" ? self.browser.webRequest : self.chrome?.webRequest;
  var broadcastChannel;
  function broadcast(message) {
    if (broadcastChannel === void 0) {
      broadcastChannel = new self.BroadcastChannel("uBR");
    }
    broadcastChannel.postMessage(message);
  }
  async function broadcastToAll(message) {
    broadcast(message);
    const tabs = await vAPI.tabs.query({
      discarded: false
    });
    const bcmessage = Object.assign({ broadcast: true }, message);
    for (const tab of tabs) {
      webext_default.tabs.sendMessage(tab.id, bcmessage, {}).catch((e) => {
        console.warn("[uBR] broadcast: tabs.sendMessage failed for tab", tab.id, e);
      });
    }
  }
  function onBroadcast(listener) {
    const bc2 = new self.BroadcastChannel("uBR");
    bc2.onmessage = (ev) => listener(ev.data || {});
    return bc2;
  }
  function filteringBehaviorChanged(details = {}) {
    const dir = details.direction;
    if (typeof dir !== "number" || dir >= 0) {
      filteringBehaviorChanged.throttle.offon(727);
    }
    broadcast(Object.assign({ what: "filteringBehaviorChanged" }, details));
  }
  filteringBehaviorChanged.throttle = vAPI.defer.create(() => {
    const { history, max } = filteringBehaviorChanged;
    const now = Date.now() / 1e3 | 0;
    if (history.length >= max) {
      if (now - history[0] <= 10 * 60) {
        return;
      }
      history.shift();
    }
    history.push(now);
    vAPI.net.handlerBehaviorChanged();
  });
  filteringBehaviorChanged.history = [];
  filteringBehaviorChanged.max = Math.min(
    (webRequestAPI?.MAX_HANDLER_BEHAVIOR_CHANGED_CALLS_PER_10_MINUTES ?? 20) - 1,
    19
  );

  // src/js/logger.ts
  var buffer = null;
  var lastReadTime = 0;
  var writePtr = 0;
  var logBufferObsoleteAfter = 30 * 1e3;
  var janitorTimer = null;
  var initJanitor = () => {
    if (janitorTimer === null) {
      janitorTimer = background_default.vAPI.defer.create(() => {
        if (buffer === null) {
          return;
        }
        if (lastReadTime >= Date.now() - logBufferObsoleteAfter) {
          return initJanitor().on(logBufferObsoleteAfter);
        }
        logger.enabled = false;
        buffer = null;
        writePtr = 0;
        logger.ownerId = void 0;
        broadcastToAll({ what: "loggerDisabled" });
      });
    }
    return janitorTimer;
  };
  var boxEntry = (details) => {
    details.tstamp = Date.now() / 1e3 | 0;
    return JSON.stringify(details);
  };
  var pushOne = (box) => {
    if (writePtr !== 0 && box === buffer[writePtr - 1]) {
      return;
    }
    if (writePtr === buffer.length) {
      buffer.push(box);
    } else {
      buffer[writePtr] = box;
    }
    writePtr += 1;
  };
  var logger = {
    enabled: false,
    ownerId: void 0,
    writeOne(details) {
      if (buffer === null) {
        return;
      }
      pushOne(boxEntry(details));
    },
    readAll(ownerId) {
      this.ownerId = ownerId;
      if (buffer === null) {
        this.enabled = true;
        buffer = [];
        initJanitor().on(logBufferObsoleteAfter);
        broadcast({ what: "loggerEnabled" });
      }
      const out = buffer.slice(0, writePtr);
      buffer.fill("", 0, writePtr);
      writePtr = 0;
      lastReadTime = Date.now();
      return out;
    }
  };
  var logger_default = logger;

  // src/js/blocker-core/core/types/index.ts
  var RESOURCE_TYPES = [
    "script",
    "image",
    "stylesheet",
    "font",
    "xmlhttprequest",
    "sub_frame",
    "main_frame",
    "media"
  ];
  var RESOURCE_TYPE_TO_DNR = {
    script: "script",
    image: "image",
    stylesheet: "stylesheet",
    font: "font",
    xmlhttprequest: "xmlhttprequest",
    sub_frame: "sub_frame",
    main_frame: "main_frame",
    media: "media"
  };
  var PROFILE_DEFAULTS = {
    strict: {
      script: "block",
      sub_frame: "block",
      xmlhttprequest: "block",
      image: "allow",
      stylesheet: "allow",
      font: "allow",
      media: "allow",
      main_frame: "allow"
    },
    balanced: {
      script: "allow",
      sub_frame: "allow",
      xmlhttprequest: "allow",
      image: "allow",
      stylesheet: "allow",
      font: "allow",
      media: "allow",
      main_frame: "allow"
    },
    relaxed: {
      script: "allow",
      sub_frame: "allow",
      xmlhttprequest: "allow",
      image: "allow",
      stylesheet: "allow",
      font: "allow",
      media: "allow",
      main_frame: "allow"
    },
    custom: {}
  };
  var PRIORITY_MAP = {
    temporary: 110,
    permanent: 100,
    profile: 9,
    static: 9
  };
  var DYNAMIC_RULE_MIN = 1;
  var SESSION_RULE_MIN = 1e6;

  // src/js/blocker-core/core/policy/priority.ts
  function computeRulePriority(scope, action) {
    const basePriority = PRIORITY_MAP[scope] ?? 100;
    return action === "allow" ? basePriority + 1 : basePriority;
  }

  // src/js/blocker-core/core/policy/index.ts
  function normalizeSite(site) {
    try {
      const url = new URL(site.startsWith("http") ? site : `https://${site}`);
      return url.hostname.toLowerCase();
    } catch (e) {
      console.warn("[uBR] policy: normalizeSite failed", e);
      return site.toLowerCase();
    }
  }
  function normalizeDomain(domain) {
    return domain.toLowerCase().replace(/^(https?:\/\/)?(www\.)?/, "").replace(/\/.*$/, "").replace(/:\d+$/, "");
  }
  function normalizeResourceTypes(types) {
    if (!types || types.length === 0) return void 0;
    const normalized = types.map((t) => t.toLowerCase());
    const unique = [...new Set(normalized)];
    return unique.length > 0 ? unique : void 0;
  }
  function normalizeExcludedDomains(domains) {
    if (!domains || domains.length === 0) return void 0;
    const normalized = domains.map(normalizeDomain).filter((d2) => d2.length > 0);
    return normalized.length > 0 ? [...new Set(normalized)] : void 0;
  }
  function normalizeDomainRule(rule) {
    return {
      allow: rule.allow ?? false,
      resourceTypes: normalizeResourceTypes(rule.resourceTypes),
      temporary: rule.temporary ?? false,
      excludedDomains: normalizeExcludedDomains(rule.excludedDomains)
    };
  }
  function normalizeSitePolicy(policy) {
    const site = normalizeSite(policy.site ?? "");
    const profile = validateProfile(policy.profile) ? policy.profile : "balanced";
    const resourceDefaults = {};
    const defaults = PROFILE_DEFAULTS[profile];
    for (const type of RESOURCE_TYPES) {
      if (policy.resourceDefaults?.[type]) {
        resourceDefaults[type] = policy.resourceDefaults[type];
      } else if (defaults[type]) {
        resourceDefaults[type] = defaults[type];
      }
    }
    const rules = {};
    if (policy.rules) {
      for (const [domain, rule] of Object.entries(policy.rules)) {
        if (rule && (rule.allow !== void 0 || rule.resourceTypes)) {
          rules[normalizeDomain(domain)] = normalizeDomainRule(rule);
        }
      }
    }
    return {
      site,
      resourceDefaults,
      rules,
      profile
    };
  }
  function validateProfile(profile) {
    return ["strict", "balanced", "relaxed", "custom"].includes(profile ?? "");
  }
  function validateResourceType(type) {
    return RESOURCE_TYPES.includes(type);
  }
  function validateDomainRule(rule) {
    if (typeof rule !== "object" || rule === null) return false;
    const r = rule;
    if (typeof r.allow !== "boolean") return false;
    if (r.resourceTypes !== void 0) {
      if (!Array.isArray(r.resourceTypes)) return false;
      for (const t of r.resourceTypes) {
        if (typeof t !== "string" || !validateResourceType(t)) return false;
      }
    }
    if (r.excludedDomains !== void 0) {
      if (!Array.isArray(r.excludedDomains)) return false;
      for (const d2 of r.excludedDomains) {
        if (typeof d2 !== "string") return false;
      }
    }
    return true;
  }
  function validateSitePolicy(policy) {
    if (typeof policy !== "object" || policy === null) return false;
    const p = policy;
    if (typeof p.site !== "string" || p.site.length === 0) return false;
    if (!validateProfile(p.profile)) return false;
    if (p.resourceDefaults !== void 0 && typeof p.resourceDefaults !== "object") return false;
    if (p.rules !== void 0) {
      if (typeof p.rules !== "object") return false;
      const rules = p.rules;
      if (!rules) return false;
      for (const rule of Object.values(rules)) {
        if (!validateDomainRule(rule)) return false;
      }
    }
    return true;
  }
  function validateStoredPolicy(policy) {
    if (typeof policy !== "object" || policy === null) return false;
    const p = policy;
    if (typeof p.version !== "number") return false;
    if (typeof p.sites !== "object" || p.sites === null) return false;
    for (const sitePolicy of Object.values(p.sites)) {
      if (!validateSitePolicy(sitePolicy)) return false;
    }
    return true;
  }
  function createDefaultStoredPolicy() {
    return {
      version: 1,
      sites: {},
      createdAt: Date.now(),
      updatedAt: Date.now()
    };
  }
  function mergeSitePolicy(existing, updates) {
    const normalized = normalizeSitePolicy({
      ...existing,
      ...updates,
      site: existing.site
    });
    return normalized;
  }

  // src/js/blocker-core/core/compiler/index.ts
  function generatePolicyKey(descriptor, site) {
    const domainKey = [...descriptor.domains].sort().join(",");
    const typeKey = [...descriptor.resourceTypes].sort().join(",");
    const exclusionKey = descriptor.excludedDomains ? [...descriptor.excludedDomains].sort().join(",") : "";
    return `${site}|${domainKey}|${descriptor.action}|${typeKey}|${descriptor.scope}|${exclusionKey}`;
  }
  function generateDefaultRuleKey(site, resourceType, action) {
    return `${site}|*|${action}|${resourceType}|default|`;
  }
  function compileSitePolicy(options) {
    const { site, policy, startDynamicId, startSessionId, includeTemporary = true, includePermanent = true, excludeMainFrame = true } = options;
    const dynamicRules = [];
    const sessionRules = [];
    const mapping = {};
    let nextDynamicId = startDynamicId;
    let nextSessionId = startSessionId;
    const descriptors = generateRuleDescriptors(policy, site, excludeMainFrame);
    for (const desc of descriptors) {
      const isTemporary = desc.scope === "temporary";
      if (isTemporary && !includeTemporary) continue;
      if (!isTemporary && !includePermanent) continue;
      const ruleId = isTemporary ? nextSessionId++ : nextDynamicId++;
      const rule = {
        id: ruleId,
        priority: desc.priority,
        action: { type: desc.action },
        condition: {
          initiatorDomains: [site],
          domains: desc.domains,
          resourceTypes: desc.resourceTypes.map((rt2) => RESOURCE_TYPE_TO_DNR[rt2])
        }
      };
      if (desc.excludedDomains && desc.excludedDomains.length > 0) {
        rule.condition.excludedDomains = desc.excludedDomains;
      }
      const policyKey = generatePolicyKey(desc, site);
      mapping[policyKey] = { policyKey, ruleId, ruleType: isTemporary ? "session" : "dynamic" };
      if (isTemporary) {
        sessionRules.push(rule);
      } else {
        dynamicRules.push(rule);
      }
    }
    const finalNextDynamicId = addDefaultRules(site, policy, dynamicRules, sessionRules, nextDynamicId, nextSessionId, mapping);
    return {
      dynamicRules,
      sessionRules,
      nextDynamicId: finalNextDynamicId,
      nextSessionId,
      mapping
    };
  }
  function generateRuleDescriptors(policy, site, excludeMainFrame) {
    const descriptors = [];
    const domainActions = /* @__PURE__ */ new Map();
    for (const [domain, rule] of Object.entries(policy.rules)) {
      const normalizedDomain = normalizeDomain(domain);
      if (!normalizedDomain) continue;
      const scope = rule.temporary ? "temporary" : "permanent";
      const resourceTypes = new Set(rule.resourceTypes ?? ["script", "image", "stylesheet", "font", "xmlhttprequest", "sub_frame", "media"]);
      if (excludeMainFrame) {
        resourceTypes.delete("main_frame");
      }
      const existing = domainActions.get(normalizedDomain);
      if (existing) {
        if (scope === "temporary" || scope === existing.scope && rule.allow === existing.allow) {
          for (const rt2 of resourceTypes) {
            existing.resourceTypes.add(rt2);
          }
        } else if (scope === "permanent" && existing.scope === "temporary") {
          continue;
        }
      } else {
        domainActions.set(normalizedDomain, {
          allow: rule.allow,
          scope,
          resourceTypes,
          excludedDomains: rule.excludedDomains
        });
      }
    }
    for (const [domain, action] of domainActions) {
      if (action.resourceTypes.size === 0) continue;
      descriptors.push({
        domains: [domain],
        resourceTypes: [...action.resourceTypes],
        action: action.allow ? "allow" : "block",
        scope: action.scope,
        excludedDomains: action.excludedDomains,
        priority: computeRulePriority(action.scope, action.allow ? "allow" : "block")
      });
    }
    return compactDescriptors(descriptors);
  }
  function compactDescriptors(descriptors) {
    const byKey = /* @__PURE__ */ new Map();
    for (const desc of descriptors) {
      const sortedTypes = [...desc.resourceTypes].sort().join(",");
      const sortedExclusions = desc.excludedDomains ? [...desc.excludedDomains].sort().join(",") : "";
      const key = `${desc.action}|${desc.scope}|${desc.priority}|${sortedTypes}|${sortedExclusions}`;
      const existing = byKey.get(key);
      if (existing) {
        const combinedTypes = /* @__PURE__ */ new Set([...existing.resourceTypes, ...desc.resourceTypes]);
        existing.resourceTypes = [...combinedTypes];
        const combinedDomains = [...existing.domains, ...desc.domains].sort();
        existing.domains = combinedDomains;
      } else {
        byKey.set(key, { ...desc, resourceTypes: [...desc.resourceTypes], domains: [...desc.domains] });
      }
    }
    return Array.from(byKey.values());
  }
  function addDefaultRules(site, policy, dynamicRules, sessionRules, nextDynamicId, nextSessionId, mapping) {
    const resourceTypes = ["script", "image", "stylesheet", "font", "xmlhttprequest", "sub_frame", "media"];
    let currentId = nextDynamicId;
    for (const rt2 of resourceTypes) {
      const defaultAction = policy.resourceDefaults[rt2];
      if (!defaultAction) continue;
      const allRules = [...dynamicRules, ...sessionRules];
      const explicitResourceTypeRules = allRules.filter((r) => r.condition.resourceTypes?.includes(RESOURCE_TYPE_TO_DNR[rt2]));
      const coveredDomains = /* @__PURE__ */ new Set();
      for (const rule of explicitResourceTypeRules) {
        const domains = rule.condition.domains ?? [];
        for (const d2 of domains) coveredDomains.add(d2);
      }
      const policyDomains = Object.keys(policy.rules);
      const hasUncoveredDomain = policyDomains.some((d2) => !coveredDomains.has(d2));
      if (!explicitResourceTypeRules.length || hasUncoveredDomain) {
        const isBlock = defaultAction === "block";
        const ruleId = currentId++;
        const rule = {
          id: ruleId,
          priority: 1,
          action: { type: isBlock ? "block" : "allow" },
          condition: {
            initiatorDomains: [site],
            resourceTypes: [RESOURCE_TYPE_TO_DNR[rt2]]
          }
        };
        dynamicRules.push(rule);
        const policyKey = generateDefaultRuleKey(site, rt2, isBlock ? "block" : "allow");
        mapping[policyKey] = { policyKey, ruleId, ruleType: "dynamic" };
      }
    }
    return currentId;
  }

  // src/js/blocker-core/core/storage/id-allocator.ts
  function createDefaultAllocatorState() {
    return {
      nextDynamicId: DYNAMIC_RULE_MIN,
      nextSessionId: SESSION_RULE_MIN,
      freedDynamicIds: [],
      freedSessionIds: []
    };
  }
  function serializeMapping(map) {
    const result = {};
    for (const [key, value] of map) {
      result[key] = value;
    }
    return result;
  }
  function deserializeMapping(obj) {
    const map = /* @__PURE__ */ new Map();
    for (const [key, value] of Object.entries(obj)) {
      map.set(key, value);
    }
    return map;
  }

  // src/js/blocker-core/core/storage/index.ts
  var STORAGE_SCHEMA_VERSION = 1;
  function createDefaultStorageSchema() {
    return {
      version: STORAGE_SCHEMA_VERSION,
      policy: createDefaultStoredPolicy(),
      compiledState: {},
      ruleMapping: {},
      idAllocator: createDefaultAllocatorState(),
      budget: {
        dynamicRuleCount: 0,
        sessionRuleCount: 0,
        dynamicCeiling: 3e4,
        sessionCeiling: 5e3,
        perSiteRules: {},
        globalOverridePool: 3e3
      },
      observedDomains: [],
      cosmeticSelectors: [],
      temporaryOverrides: {},
      lastUpdated: Date.now()
    };
  }
  function validateStorageSchema(data) {
    const errors = [];
    const warnings = [];
    if (typeof data !== "object" || data === null) {
      errors.push("Storage data must be an object");
      return { valid: false, errors, warnings };
    }
    const schema = data;
    if (schema.version === void 0) {
      errors.push("Missing schema version");
    } else if (typeof schema.version !== "number") {
      errors.push("Schema version must be a number");
    }
    if (schema.policy !== void 0) {
      if (!validateStoredPolicy(schema.policy)) {
        errors.push("Invalid stored policy");
      }
    }
    if (schema.budget !== void 0) {
      const budget = schema.budget;
      if (typeof budget.dynamicRuleCount !== "number" || typeof budget.sessionRuleCount !== "number") {
        errors.push("Invalid budget state");
      }
    }
    if (schema.idAllocator !== void 0) {
      const alloc = schema.idAllocator;
      if (typeof alloc.nextDynamicId !== "number" || typeof alloc.nextSessionId !== "number") {
        errors.push("Invalid ID allocator state");
      }
    }
    return {
      valid: errors.length === 0,
      errors,
      warnings
    };
  }
  function migrateSchema(oldData, fromVersion) {
    const schema = createDefaultStorageSchema();
    schema.version = fromVersion;
    if (fromVersion < 1) {
      if (oldData.policy) {
        schema.policy = oldData.policy;
      }
      if (oldData.budget) {
        schema.budget = oldData.budget;
      }
    }
    schema.version = STORAGE_SCHEMA_VERSION;
    schema.lastUpdated = Date.now();
    return schema;
  }
  function validateAndMigrateStorage(data, currentVersion = STORAGE_SCHEMA_VERSION) {
    if (data === null || data === void 0) {
      return {
        success: true,
        schema: createDefaultStorageSchema(),
        needsMigration: false
      };
    }
    const validation = validateStorageSchema(data);
    if (!validation.valid) {
      const record2 = data;
      if (record2.version === void 0 || typeof record2.version !== "number") {
        return {
          success: false,
          error: `Corrupted storage: ${validation.errors.join(", ")}`
        };
      }
      if (record2.version < currentVersion) {
        const migrated = migrateSchema(record2, record2.version);
        return {
          success: true,
          schema: migrated,
          needsMigration: true,
          migrationFromVersion: record2.version
        };
      }
      return {
        success: false,
        error: `Invalid storage: ${validation.errors.join(", ")}`
      };
    }
    const record = data;
    if (record.version < currentVersion) {
      const migrated = migrateSchema(record, record.version);
      return {
        success: true,
        schema: migrated,
        needsMigration: true,
        migrationFromVersion: record.version
      };
    }
    return {
      success: true,
      schema: record,
      needsMigration: false
    };
  }
  function serializeStorageSchema(schema) {
    return {
      version: schema.version,
      policy: schema.policy,
      compiledState: schema.compiledState,
      ruleMapping: serializeMapping(new Map(Object.entries(schema.ruleMapping))),
      idAllocator: schema.idAllocator,
      budget: schema.budget,
      observedDomains: schema.observedDomains,
      cosmeticSelectors: schema.cosmeticSelectors,
      temporaryOverrides: schema.temporaryOverrides,
      lastUpdated: schema.lastUpdated
    };
  }
  function deserializeStorageSchema(data) {
    const result = validateAndMigrateStorage(data);
    if (!result.success || !result.schema) {
      return null;
    }
    const schema = result.schema;
    if (typeof schema.ruleMapping === "object" && schema.ruleMapping !== null) {
      schema.ruleMapping = deserializeMapping(schema.ruleMapping);
    }
    return schema;
  }
  function normalizeCosmeticSelectors(selectors) {
    const seen = /* @__PURE__ */ new Set();
    const normalized = [];
    for (const selector of selectors) {
      const key = `${selector.site}|${selector.selector}`;
      if (!seen.has(key)) {
        seen.add(key);
        normalized.push({
          ...selector,
          site: selector.site.toLowerCase(),
          selector: selector.selector.trim()
        });
      }
    }
    return normalized;
  }

  // src/js/blocker-core/adapters/dnr/index.ts
  var FirefoxDNRAdapter = class {
    async getDynamicRules() {
      if (typeof browser !== "undefined" && browser.declarativeNetRequest) {
        return browser.declarativeNetRequest.getDynamicRules();
      }
      return [];
    }
    async updateDynamicRules(options) {
      const addRules = options.addRules?.map((rule) => ({
        id: rule.id,
        priority: rule.priority,
        action: rule.action,
        condition: rule.condition
      })) ?? [];
      if (typeof browser !== "undefined" && browser.declarativeNetRequest) {
        await browser.declarativeNetRequest.updateDynamicRules({
          addRules,
          removeRuleIds: options.removeRuleIds ?? []
        });
      } else {
        console.warn("[FirefoxDNRAdapter] DNR API not available in MV2 mode");
      }
    }
    async getSessionRules() {
      if (typeof browser !== "undefined" && browser.declarativeNetRequest) {
        return browser.declarativeNetRequest.getSessionRules();
      }
      return [];
    }
    async updateSessionRules(options) {
      const addRules = options.addRules?.map((rule) => ({
        id: rule.id,
        priority: rule.priority,
        action: rule.action,
        condition: rule.condition
      })) ?? [];
      if (typeof browser !== "undefined" && browser.declarativeNetRequest) {
        await browser.declarativeNetRequest.updateSessionRules({
          addRules,
          removeRuleIds: options.removeRuleIds ?? []
        });
      }
    }
    async getAvailableStaticRuleCount() {
      if (typeof browser !== "undefined" && browser.declarativeNetRequest) {
        return browser.declarativeNetRequest.getAvailableStaticRuleCount();
      }
      return 0;
    }
    async getMatchedRules(options) {
      const filterOptions = {};
      if (options?.tabId !== void 0) {
        filterOptions.tabId = options.tabId;
      }
      if (options?.initiator) {
        filterOptions.initiator = options.initiator;
      }
      if (typeof browser !== "undefined" && browser.declarativeNetRequest) {
        const result = await browser.declarativeNetRequest.getMatchedRules(filterOptions);
        return result.rules ?? [];
      }
      return [];
    }
    async installDynamicRules(dynamicRules, sessionRules = []) {
      const allRules = [...dynamicRules, ...sessionRules];
      if (allRules.length === 0) return;
      if (typeof browser !== "undefined" && browser.declarativeNetRequest) {
        await browser.declarativeNetRequest.updateDynamicRules({
          addRules: allRules.map((rule) => ({
            id: rule.id,
            priority: rule.priority,
            action: rule.action,
            condition: rule.condition
          })),
          removeRuleIds: []
        });
        console.log(`[FirefoxDNRAdapter] Installed ${allRules.length} DNR rules`);
      } else {
        console.log(`[FirefoxDNRAdapter] Would install ${allRules.length} rules (webRequest mode)`);
      }
    }
    async clearDynamicRules(site) {
      if (typeof browser !== "undefined" && browser.declarativeNetRequest) {
        const rules = await browser.declarativeNetRequest.getDynamicRules();
        const siteRules = rules.filter(
          (r) => r.condition && typeof r.condition === "object" && "initiatorDomains" in r.condition && Array.isArray(r.condition.initiatorDomains) && r.condition.initiatorDomains?.includes(site)
        );
        if (siteRules.length > 0) {
          await browser.declarativeNetRequest.updateDynamicRules({
            removeRuleIds: siteRules.map((r) => r.id)
          });
        }
      }
    }
  };
  var ChromeDNRAdapter = class {
    async getDynamicRules() {
      return chrome.declarativeNetRequest.getDynamicRules();
    }
    async updateDynamicRules(options) {
      const addRules = options.addRules?.map((rule) => ({
        id: rule.id,
        priority: rule.priority,
        action: rule.action,
        condition: rule.condition
      })) ?? [];
      await chrome.declarativeNetRequest.updateDynamicRules({
        addRules,
        removeRuleIds: options.removeRuleIds ?? []
      });
    }
    async getSessionRules() {
      return chrome.declarativeNetRequest.getSessionRules();
    }
    async updateSessionRules(options) {
      const addRules = options.addRules?.map((rule) => ({
        id: rule.id,
        priority: rule.priority,
        action: rule.action,
        condition: rule.condition
      })) ?? [];
      await chrome.declarativeNetRequest.updateSessionRules({
        addRules,
        removeRuleIds: options.removeRuleIds ?? []
      });
    }
    async getAvailableStaticRuleCount() {
      return chrome.declarativeNetRequest.getAvailableStaticRuleCount();
    }
    async getMatchedRules(options) {
      const filterOptions = {};
      if (options?.tabId !== void 0) {
        filterOptions.tabId = options.tabId;
      }
      if (options?.initiator) {
        filterOptions.initiator = options.initiator;
      }
      const result = await chrome.declarativeNetRequest.getMatchedRules(filterOptions);
      return result.rules ?? [];
    }
    async installDynamicRules(dynamicRules, sessionRules = []) {
      const allRules = [...dynamicRules, ...sessionRules];
      if (allRules.length === 0) return;
      const existingRules = await chrome.declarativeNetRequest.getDynamicRules();
      const existingIds = new Set(existingRules.map((r) => r.id));
      const newRules = allRules.filter((r) => !existingIds.has(r.id));
      if (newRules.length > 0) {
        await chrome.declarativeNetRequest.updateDynamicRules({
          addRules: newRules.map((rule) => ({
            id: rule.id,
            priority: rule.priority,
            action: rule.action,
            condition: rule.condition
          })),
          removeRuleIds: []
        });
        console.log(`[ChromeDNRAdapter] Installed ${newRules.length} new rules`);
      }
    }
    async clearDynamicRules(site) {
      const rules = await chrome.declarativeNetRequest.getDynamicRules();
      const siteRules = rules.filter(
        (r) => r.condition && typeof r.condition === "object" && "initiatorDomains" in r.condition && Array.isArray(r.condition.initiatorDomains) && r.condition.initiatorDomains?.includes(site)
      );
      if (siteRules.length > 0) {
        await chrome.declarativeNetRequest.updateDynamicRules({
          removeRuleIds: siteRules.map((r) => r.id)
        });
      }
    }
  };
  function createFirefoxDNRAdapter() {
    return new FirefoxDNRAdapter();
  }
  function createChromeDNRAdapter() {
    return new ChromeDNRAdapter();
  }

  // src/js/blocker-adapter.ts
  var blocker = {
    initialized: false,
    storage: null,
    policy: null,
    compiler: null,
    dnrAdapter: null,
    budget: null
  };
  async function initBlocker() {
    if (blocker.initialized) return;
    console.log("[BlockerAdapter] Initializing Blocker Core...");
    try {
      const stored = await browser.storage.local.get("blocker_storage");
      if (stored.blocker_storage) {
        const result = deserializeStorageSchema(stored.blocker_storage);
        if (result && result.success && result.schema) {
          blocker.storage = result.schema;
          console.log("[BlockerAdapter] Loaded existing storage");
        } else {
          blocker.storage = createDefaultStorageSchema();
          console.log("[BlockerAdapter] Created new storage (deserialization failed)");
        }
      } else {
        blocker.storage = createDefaultStorageSchema();
        console.log("[BlockerAdapter] Created new storage");
      }
      blocker.policy = blocker.storage.policy;
      const isFirefox = typeof browser !== "undefined" && typeof browser.runtime?.getBrowserInfo !== "undefined";
      if (isFirefox) {
        blocker.dnrAdapter = createFirefoxDNRAdapter();
        console.log("[BlockerAdapter] Using Firefox DNR adapter");
      } else {
        blocker.dnrAdapter = createChromeDNRAdapter();
        console.log("[BlockerAdapter] Using Chrome DNR adapter");
      }
      blocker.budget = blocker.storage.budget || {
        dynamicRuleCount: 0,
        sessionRuleCount: 0,
        dynamicCeiling: 3e4,
        sessionCeiling: 5e3,
        perSiteRules: {},
        globalOverridePool: 3e3
      };
      blocker.initialized = true;
      console.log("[BlockerAdapter] Blocker Core initialized successfully");
    } catch (err) {
      console.error("[BlockerAdapter] Failed to initialize Blocker Core:", err);
      blocker.initialized = true;
    }
  }
  async function saveStorage() {
    if (!blocker.storage) return;
    blocker.storage.lastUpdated = Date.now();
    const serialized = serializeStorageSchema(blocker.storage);
    try {
      await browser.storage.local.set({ blocker_storage: serialized });
    } catch (err) {
      console.error("[BlockerAdapter] Failed to save storage:", err);
    }
  }
  async function createSitePolicy(site, profile = "balanced") {
    await initBlocker();
    const sitePolicy = normalizeSitePolicy({
      site,
      profile,
      rules: {},
      resourceDefaults: PROFILE_DEFAULTS[profile]
    });
    blocker.policy = mergeSitePolicy(blocker.policy, sitePolicy);
    blocker.storage.policy = blocker.policy;
    await saveStorage();
    return sitePolicy;
  }
  async function updateSitePolicy2(site, updates) {
    await initBlocker();
    const existing = blocker.policy.sites[site];
    if (!existing) {
      return createSitePolicy(site);
    }
    const merged = mergeSitePolicy(existing, updates);
    blocker.policy = mergeSitePolicy(blocker.policy, merged);
    blocker.storage.policy = blocker.policy;
    await saveStorage();
    return merged;
  }
  async function compileSite(site, options = {}) {
    await initBlocker();
    const sitePolicy = blocker.policy.sites[site];
    if (!sitePolicy) {
      console.warn(`[BlockerAdapter] No policy found for site: ${site}`);
      return null;
    }
    const compilerOptions = {
      site,
      policy: sitePolicy,
      startDynamicId: blocker.storage.idAllocator.nextDynamicId || 1e6,
      startSessionId: blocker.storage.idAllocator.nextSessionId || 2e6,
      includeTemporary: true,
      includePermanent: true,
      excludeMainFrame: true
    };
    const result = compileSitePolicy(compilerOptions);
    blocker.storage.idAllocator.nextDynamicId = result.nextDynamicId;
    blocker.storage.idAllocator.nextSessionId = result.nextSessionId;
    blocker.storage.ruleMapping = {
      ...blocker.storage.ruleMapping,
      ...Object.fromEntries(
        Object.entries(result.mapping).map(([k2, v2]) => [k2, v2])
      )
    };
    blocker.storage.budget.dynamicRuleCount += result.dynamicRules.length;
    blocker.storage.budget.sessionRuleCount += result.sessionRules.length;
    await saveStorage();
    return result;
  }
  async function installRules(site) {
    await initBlocker();
    const compiled = await compileSite(site);
    if (!compiled) return false;
    try {
      await blocker.dnrAdapter.installDynamicRules(
        compiled.dynamicRules,
        compiled.sessionRules
      );
      console.log(`[BlockerAdapter] Installed ${compiled.dynamicRules.length} dynamic and ${compiled.sessionRules.length} session rules for ${site}`);
      return true;
    } catch (err) {
      console.error("[BlockerAdapter] Failed to install rules:", err);
      return false;
    }
  }
  async function clearRules(site) {
    await initBlocker();
    try {
      await blocker.dnrAdapter.clearDynamicRules(site);
      console.log(`[BlockerAdapter] Cleared rules for ${site}`);
      return true;
    } catch (err) {
      console.error("[BlockerAdapter] Failed to clear rules:", err);
      return false;
    }
  }
  function getBudgetState() {
    return blocker.budget || {
      dynamicRuleCount: 0,
      sessionRuleCount: 0,
      dynamicCeiling: 3e4,
      sessionCeiling: 5e3
    };
  }
  function getPolicy() {
    return blocker.policy || { sites: {} };
  }
  async function addCosmeticSelector(site, selector, isActive = false) {
    await initBlocker();
    const cosmeticSelector = {
      site,
      selector,
      status: isActive ? "active" : "saved",
      createdAt: Date.now()
    };
    blocker.storage.cosmeticSelectors = normalizeCosmeticSelectors([
      ...blocker.storage.cosmeticSelectors || [],
      cosmeticSelector
    ]);
    await saveStorage();
    return cosmeticSelector;
  }
  async function removeCosmeticSelector(site, selector) {
    await initBlocker();
    blocker.storage.cosmeticSelectors = (blocker.storage.cosmeticSelectors || []).filter((s) => !(s.site === site && s.selector === selector));
    await saveStorage();
  }
  function getCosmeticSelectors(site) {
    return (blocker.storage.cosmeticSelectors || []).filter((s) => s.site === site);
  }
  function getAllCosmeticSelectors() {
    return blocker.storage.cosmeticSelectors || [];
  }
  async function snapshotState2() {
    await initBlocker();
    return {
      policy: blocker.policy,
      storage: blocker.storage,
      budget: blocker.budget,
      timestamp: Date.now()
    };
  }
  async function restoreFromSnapshot2(snapshot) {
    if (!snapshot || !snapshot.storage) return false;
    blocker.storage = snapshot.storage;
    blocker.policy = snapshot.policy;
    blocker.budget = snapshot.budget;
    await saveStorage();
    console.log("[BlockerAdapter] Restored from snapshot");
    return true;
  }
  function isInitialized() {
    return blocker.initialized;
  }
  var blocker_adapter_default = {
    init: initBlocker,
    createSitePolicy,
    updateSitePolicy: updateSitePolicy2,
    compileSite,
    installRules,
    clearRules,
    getBudgetState,
    getPolicy,
    addCosmeticSelector,
    removeCosmeticSelector,
    getCosmeticSelectors,
    getAllCosmeticSelectors,
    snapshotState: snapshotState2,
    restoreFromSnapshot: restoreFromSnapshot2,
    isInitialized
  };

  // src/js/background.ts
  if (vAPI.webextFlavor === void 0) {
    vAPI.webextFlavor = { major: 0, soup: /* @__PURE__ */ new Set(["ublock"]) };
  }
  var hiddenSettingsDefault = {
    allowGenericProceduralFilters: false,
    assetFetchTimeout: 30,
    autoCommentFilterTemplate: "{{date}} {{origin}}",
    autoUpdateAssetFetchPeriod: 5,
    autoUpdateDelayAfterLaunch: 37,
    autoUpdatePeriod: 1,
    benchmarkDatasetURL: "unset",
    blockingProfiles: "11111/#F00 11010/#C0F 11001/#00F 00001",
    cacheStorageCompression: true,
    cacheStorageCompressionThreshold: 65536,
    cacheStorageMultithread: 2,
    cacheControlForFirefox1376932: "unset",
    cloudStorageCompression: true,
    cnameIgnoreList: "unset",
    cnameIgnore1stParty: true,
    cnameIgnoreExceptions: true,
    cnameIgnoreRootDocument: true,
    cnameReplayFullURL: false,
    consoleLogLevel: "unset",
    debugAssetsJson: false,
    debugScriptlets: false,
    debugScriptletInjector: false,
    differentialUpdate: true,
    disableWebAssembly: false,
    dnsCacheTTL: 600,
    dnsResolveEnabled: true,
    extensionUpdateForceReload: false,
    filterAuthorMode: false,
    loggerPopupType: "popup",
    manualUpdateAssetFetchPeriod: 500,
    modifyWebextFlavor: "unset",
    noScriptingCSP: "script-src http: https:",
    popupFontSize: "unset",
    popupPanelDisabledSections: 0,
    popupPanelHeightMode: 0,
    popupPanelLockedSections: 0,
    popupPanelOrientation: "unset",
    requestJournalProcessPeriod: 1e3,
    requestStatsDisabled: false,
    selfieDelayInSeconds: 53,
    strictBlockingBypassDuration: 120,
    toolbarWarningTimeout: 60,
    trustedListPrefixes: "ublock-",
    uiPopupConfig: "unset",
    uiStyles: "unset",
    updateAssetBypassBrowserCache: false,
    userResourcesLocation: "unset"
  };
  if (vAPI.webextFlavor.soup.has("devbuild")) {
    hiddenSettingsDefault.consoleLogLevel = "info";
    hiddenSettingsDefault.cacheStorageAPI = "unset";
    ubologSet(true);
  }
  var userSettingsDefault = {
    advancedUserEnabled: false,
    alwaysDetachLogger: true,
    autoUpdate: true,
    cloudStorageEnabled: false,
    cnameUncloakEnabled: true,
    collapseBlocked: true,
    colorBlindFriendly: false,
    contextMenuEnabled: true,
    uiAccentCustom: false,
    uiAccentCustom0: "#aca0f7",
    uiTheme: "auto",
    externalLists: "",
    firewallPaneMinimized: true,
    hyperlinkAuditingDisabled: true,
    ignoreGenericCosmeticFilters: false,
    importedLists: [],
    largeMediaSize: 50,
    popupPanelSections: 7,
    prefetchingDisabled: true,
    requestLogMaxEntries: 1e3,
    showIconBadge: true,
    suspendUntilListsAreLoaded: Boolean(vAPI.Net?.canSuspend?.()),
    tooltipsDisabled: false,
    userFiltersTrusted: false,
    webrtcIPAddressHidden: false
  };
  var dynamicFilteringDefault = [
    "behind-the-scene * * noop",
    "behind-the-scene * image noop",
    "behind-the-scene * 3p noop",
    "behind-the-scene * inline-script noop",
    "behind-the-scene * 1p-script noop",
    "behind-the-scene * 3p-script noop",
    "behind-the-scene * 3p-frame noop"
  ];
  var hostnameSwitchesDefault = [
    "no-large-media: behind-the-scene false"
  ];
  if (vAPI.webextFlavor.soup.has("firefox")) {
    hostnameSwitchesDefault.push("no-csp-reports: * true");
  }
  var \u00B5Block = {
    // jshint ignore:line
    alarmQueue: [],
    userSettingsDefault,
    userSettings: Object.assign({}, userSettingsDefault),
    hiddenSettingsDefault,
    hiddenSettingsAdmin: {},
    hiddenSettings: Object.assign({}, hiddenSettingsDefault),
    dynamicFilteringDefault,
    hostnameSwitchesDefault,
    noDashboard: false,
    // Features detection.
    privacySettingsSupported: vAPI.browserSettings instanceof Object,
    cloudStorageSupported: vAPI.cloud instanceof Object,
    canFilterResponseData: typeof browser.webRequest.filterResponseData === "function",
    // https://github.com/chrisaljoudi/uBlock/issues/180
    // Whitelist directives need to be loaded once the PSL is available
    netWhitelist: /* @__PURE__ */ new Map(),
    netWhitelistModifyTime: 0,
    netWhitelistDefault: [
      "about-scheme",
      "chrome-scheme",
      "chrome-extension-scheme",
      "edge-scheme",
      "moz-extension-scheme",
      "opera-scheme",
      "vivaldi-scheme",
      "wyciwyg-scheme"
    ],
    requestStats: {
      blockedCount: 0,
      allowedCount: 0
    },
    // Read-only
    systemSettings: {
      compiledMagic: 71,
      // Increase when compiled format changes
      selfieMagic: 71
      // Increase when selfie format changes
    },
    // https://github.com/uBlockOrigin/uBlock-issues/issues/759#issuecomment-546654501
    //   The assumption is that cache storage state reflects whether
    //   compiled or selfie assets are available or not. The properties
    //   below is to no longer rely on this assumption -- though it's still
    //   not clear how the assumption could be wrong, and it's still not
    //   clear whether relying on those properties will really solve the
    //   issue. It's just an attempt at hardening.
    compiledFormatChanged: false,
    selfieIsInvalid: false,
    restoreBackupSettings: {
      lastRestoreFile: "",
      lastRestoreTime: 0,
      lastBackupFile: "",
      lastBackupTime: 0
    },
    commandShortcuts: /* @__PURE__ */ new Map(),
    // Allows to fully customize uBR's assets, typically set through admin
    // settings. The content of 'assets.json' will also tell which filter
    // lists to enable by default when uBR is first installed.
    assetsBootstrapLocation: void 0,
    assetsJsonPath: vAPI.webextFlavor.soup.has("devbuild") ? "/assets/assets.dev.json" : "/assets/assets.json",
    userFiltersPath: "user-filters",
    pslAssetKey: "public_suffix_list.dat",
    selectedFilterLists: [],
    availableFilterLists: {},
    badLists: /* @__PURE__ */ new Map(),
    inMemoryFilters: [],
    inMemoryFiltersCompiled: "",
    // https://github.com/uBlockOrigin/uBlock-issues/issues/974
    //   This can be used to defer filtering decision-making.
    readyToFilter: false,
    pageStores: /* @__PURE__ */ new Map(),
    pageStoresToken: 0,
    storageQuota: vAPI.storage?.QUOTA_BYTES ?? 0,
    storageUsed: 0,
    noopFunc: function() {
    },
    apiErrorCount: 0,
    maybeGoodPopup: {
      tabId: 0,
      url: ""
    },
    epickerArgs: {
      eprom: null,
      mouse: false,
      target: "",
      zap: false
    },
    scriptlets: {},
    cspNoInlineScript: "script-src 'unsafe-eval' * blob: data:",
    cspNoInlineFont: "font-src *",
    liveBlockingProfiles: [],
    blockingProfileColorCache: /* @__PURE__ */ new Map(),
    parsedTrustedListPrefixes: [],
    uiAccentStylesheet: "",
    openNewTab(details) {
      const url = details.url;
      const tabId = details.tabId;
      if (details.newTab) {
        chrome.tabs.create({ url, active: true }).catch(() => {
        });
      } else if (typeof tabId === "number") {
        chrome.tabs.update(tabId, { url, active: true }).catch(() => {
        });
      } else {
        chrome.tabs.query({ active: true, currentWindow: true }, (tabs) => {
          if (tabs[0]?.id) {
            chrome.tabs.update(tabs[0].id, { url, active: true }).catch(() => {
            });
          }
        });
      }
    }
  };
  \u00B5Block.isReadyPromise = new Promise((resolve) => {
    \u00B5Block.isReadyResolve = resolve;
  });
  \u00B5Block.domainFromHostname = domainFromHostname;
  \u00B5Block.hostnameFromURI = hostnameFromURI;
  \u00B5Block.FilteringContext = class extends FilteringContext {
    duplicate() {
      return new \u00B5Block.FilteringContext(this);
    }
    fromTabId(tabId) {
      const tabContext = \u00B5Block.tabContextManager.mustLookup(tabId);
      this.tabOrigin = tabContext.origin;
      this.tabHostname = tabContext.rootHostname;
      this.tabDomain = tabContext.rootDomain;
      this.tabId = tabContext.tabId;
      return this;
    }
    maybeFromDocumentURL(documentUrl) {
      if (documentUrl === void 0) {
        return;
      }
      if (documentUrl.startsWith(this.tabOrigin)) {
        return;
      }
      this.tabOrigin = originFromURI(\u00B5Block.normalizeTabURL(0, documentUrl));
      this.tabHostname = hostnameFromURI(this.tabOrigin);
      this.tabDomain = domainFromHostname(this.tabHostname);
    }
    // https://github.com/uBlockOrigin/uBlock-issues/issues/459
    //   In case of a request for frame and if ever no context is specified,
    //   assume the origin of the context is the same as the request itself.
    fromWebrequestDetails(details) {
      const tabId = details.tabId;
      this.type = details.type;
      const isMainFrame = this.itype === this.MAIN_FRAME;
      if (isMainFrame && tabId > 0) {
        \u00B5Block.tabContextManager.push(tabId, details.url);
      }
      this.fromTabId(tabId);
      this.realm = "";
      this.setMethod(details.method);
      this.setURL(details.url);
      this.setIPAddress(details.ip);
      this.aliasURL = details.aliasURL || void 0;
      this.redirectURL = void 0;
      this.filter = void 0;
      if (this.itype !== this.SUB_FRAME) {
        this.docId = details.frameId;
        this.frameId = -1;
      } else {
        this.docId = details.parentFrameId;
        this.frameId = details.frameId;
      }
      if (this.tabId > 0) {
        if (this.docId === 0) {
          if (isMainFrame === false) {
            this.maybeFromDocumentURL(details.documentUrl);
          }
          this.docOrigin = this.tabOrigin;
          this.docHostname = this.tabHostname;
          this.docDomain = this.tabDomain;
          return this;
        }
        if (details.documentUrl !== void 0) {
          this.setDocOriginFromURL(details.documentUrl);
          return this;
        }
        const pageStore = \u00B5Block.pageStoreFromTabId(this.tabId);
        const docStore = pageStore && pageStore.getFrameStore(this.docId);
        if (docStore) {
          this.setDocOriginFromURL(docStore.rawURL);
        } else {
          this.setDocOrigin(this.tabOrigin);
        }
        return this;
      }
      if (details.documentUrl !== void 0) {
        const origin2 = originFromURI(
          \u00B5Block.normalizeTabURL(0, details.documentUrl)
        );
        this.setDocOrigin(origin2).setTabOrigin(origin2);
        return this;
      }
      const origin = this.isDocument() ? originFromURI(this.url) : this.tabOrigin;
      this.setDocOrigin(origin).setTabOrigin(origin);
      return this;
    }
    getTabOrigin() {
      if (this.tabOrigin === void 0) {
        const tabContext = \u00B5Block.tabContextManager.mustLookup(this.tabId);
        this.tabOrigin = tabContext.origin;
        this.tabHostname = tabContext.rootHostname;
        this.tabDomain = tabContext.rootDomain;
      }
      return super.getTabOrigin();
    }
    toLogger() {
      const details = {
        tstamp: 0,
        realm: this.realm,
        method: this.getMethodName(),
        type: this.stype,
        tabId: this.tabId,
        tabDomain: this.getTabDomain(),
        tabHostname: this.getTabHostname(),
        docDomain: this.getDocDomain(),
        docHostname: this.getDocHostname(),
        domain: this.getDomain(),
        hostname: this.getHostname(),
        url: this.url,
        aliasURL: this.aliasURL,
        filter: void 0
      };
      if (Array.isArray(this.filter) === false) {
        details.filter = this.filter;
        return logger_default.writeOne(details);
      }
      for (const filter of this.filter) {
        details.filter = filter;
        logger_default.writeOne(details);
      }
    }
  };
  \u00B5Block.filteringContext = new \u00B5Block.FilteringContext();
  self.\u00B5Block = \u00B5Block;
  (async () => {
    try {
      await blocker_adapter_default.init();
      console.log("[uBR] Blocker Core adapter initialized");
    } catch (e) {
      console.error("[uBR] Failed to initialize Blocker Core:", e);
    }
  })();
  (async () => {
    try {
      const { smartEngine: smartEngine2 } = await Promise.resolve().then(() => (init_engine(), engine_exports));
      await smartEngine2.init();
      const stats = smartEngine2.getStats();
      console.log("[uBR] Smart Cosmetic Engine initialized:", stats);
    } catch (e) {
      console.error("[uBR] Failed to initialize Smart Cosmetic Engine:", e);
    }
  })();
  var background_default = \u00B5Block;

  // src/js/cachestorage.ts
  var STORAGE_NAME = "uBlock0CacheStorage";
  var extensionStorage = webext_default.storage?.local ?? {
    get: async () => ({}),
    set: async () => {
    },
    remove: async () => {
    },
    clear: async () => {
    }
  };
  var pendingWrite = /* @__PURE__ */ new Map();
  var keysFromGetArg = (arg) => {
    if (arg === null || arg === void 0) {
      return [];
    }
    const type = typeof arg;
    if (type === "string") {
      return [arg];
    }
    if (Array.isArray(arg)) {
      return arg;
    }
    if (type !== "object") {
      return;
    }
    return Object.keys(arg);
  };
  var fastCache = "indexedDB";
  var cacheStorage = (() => {
    const exGet = async (api2, wanted, outbin) => {
      ubolog("cacheStorage.get:", api2.name || "storage.local", wanted.join());
      const missing = [];
      for (const key of wanted) {
        if (pendingWrite.has(key)) {
          outbin[key] = pendingWrite.get(key);
        } else {
          missing.push(key);
        }
      }
      if (missing.length === 0) {
        return;
      }
      return api2.get(missing).then((inbin) => {
        inbin = inbin || {};
        const found = Object.keys(inbin);
        Object.assign(outbin, inbin);
        if (found.length === wanted.length) {
          return;
        }
        const missing2 = [];
        for (const key of wanted) {
          if (Object.hasOwn(outbin, key)) {
            continue;
          }
          missing2.push(key);
        }
        return missing2;
      });
    };
    const compress = async (bin, key, data) => {
      const \u00B5bhs = background_default.hiddenSettings;
      const after = await serializeAsync(data, {
        compress: \u00B5bhs.cacheStorageCompression,
        compressThreshold: \u00B5bhs.cacheStorageCompressionThreshold,
        multithreaded: \u00B5bhs.cacheStorageMultithread
      });
      bin[key] = after;
    };
    const decompress = async (bin, key) => {
      const data = bin[key];
      if (isSerialized(data) === false) {
        return;
      }
      const \u00B5bhs = background_default.hiddenSettings;
      const isLarge = data.length >= \u00B5bhs.cacheStorageCompressionThreshold;
      bin[key] = await deserializeAsync(data, {
        multithreaded: isLarge && \u00B5bhs.cacheStorageMultithread || 1
      });
    };
    const api = {
      get(argbin) {
        const outbin = {};
        return exGet(
          cacheAPIs[fastCache],
          keysFromGetArg(argbin),
          outbin
        ).then((wanted) => {
          if (wanted === void 0) {
            return;
          }
          return exGet(extensionStorage, wanted, outbin);
        }).then((wanted) => {
          if (wanted === void 0) {
            return;
          }
          if (argbin instanceof Object === false) {
            return;
          }
          if (Array.isArray(argbin)) {
            return;
          }
          for (const key of wanted) {
            if (Object.hasOwn(argbin, key) === false) {
              continue;
            }
            outbin[key] = argbin[key];
          }
        }).then(() => {
          const promises = [];
          for (const key of Object.keys(outbin)) {
            promises.push(decompress(outbin, key));
          }
          return Promise.all(promises).then(() => outbin);
        }).catch((reason) => {
          console.warn("[uBR] cachestorage", reason);
        });
      },
      async keys(regex) {
        const results = await Promise.all([
          cacheAPIs[fastCache].keys(regex),
          extensionStorage.get(null).catch((e) => {
            console.warn("[uBR] cachestorage: extensionStorage.get(null) failed", e);
          })
        ]);
        const keys = new Set(results[0]);
        const bin = results[1] || {};
        for (const key of Object.keys(bin)) {
          if (regex && regex.test(key) === false) {
            continue;
          }
          keys.add(key);
        }
        return keys;
      },
      async set(rawbin) {
        const keys = Object.keys(rawbin);
        if (keys.length === 0) {
          return;
        }
        ubolog("cacheStorage.set:", keys.join());
        for (const key of keys) {
          pendingWrite.set(key, rawbin[key]);
        }
        try {
          const serializedbin = {};
          const promises = [];
          for (const key of keys) {
            promises.push(compress(serializedbin, key, rawbin[key]));
          }
          await Promise.all(promises);
          await Promise.all([
            cacheAPIs[fastCache].set(rawbin, serializedbin),
            extensionStorage.set(serializedbin)
          ]);
        } catch (reason) {
          console.warn("[uBR] cachestorage", reason);
        }
        for (const key of keys) {
          pendingWrite.delete(key);
        }
      },
      remove(...args) {
        cacheAPIs[fastCache].remove(...args);
        return extensionStorage.remove(...args).catch((reason) => {
          console.warn("[uBR] cachestorage", reason);
        });
      },
      clear(...args) {
        cacheAPIs[fastCache].clear(...args);
        return extensionStorage.clear(...args).catch((reason) => {
          console.warn("[uBR] cachestorage", reason);
        });
      },
      select(api2) {
        if (Object.hasOwn(cacheAPIs, api2) === false) {
          return fastCache;
        }
        fastCache = api2;
        for (const k2 of Object.keys(cacheAPIs)) {
          if (k2 === api2) {
            continue;
          }
          cacheAPIs[k2]["clear"]();
        }
        return fastCache;
      }
    };
    if (extensionStorage.getBytesInUse instanceof Function) {
      api.getBytesInUse = function(...args) {
        return extensionStorage.getBytesInUse(...args).catch((reason) => {
          console.warn("[uBR] cachestorage", reason);
        });
      };
    }
    return api;
  })();
  var cacheAPI = (() => {
    const caches = globalThis.caches;
    let cacheStoragePromise;
    const getAPI = () => {
      if (cacheStoragePromise !== void 0) {
        return cacheStoragePromise;
      }
      cacheStoragePromise = new Promise((resolve) => {
        if (typeof caches !== "object" || caches === null) {
          ubolog("CacheStorage API not available");
          resolve(null);
          return;
        }
        resolve(caches.open(STORAGE_NAME));
      }).catch((reason) => {
        console.warn("[uBR] cachestorage", reason);
        return null;
      });
      return cacheStoragePromise;
    };
    const urlPrefix = "https://ublock0.invalid/";
    const keyToURL = (key) => `${urlPrefix}${encodeURIComponent(key)}`;
    const urlToKey = (url) => decodeURIComponent(url.slice(urlPrefix.length));
    const shouldCache = (bin) => {
      const out = {};
      for (const key of Object.keys(bin)) {
        if (key.startsWith("cache/")) {
          if (/^cache\/(compiled|selfie)\//.test(key) === false) {
            continue;
          }
        }
        out[key] = bin[key];
      }
      if (Object.keys(out).length !== 0) {
        return out;
      }
    };
    const getOne = async (key) => {
      const cache = await getAPI();
      if (cache === null) {
        return;
      }
      return cache.match(keyToURL(key)).then((response) => {
        if (response === void 0) {
          return;
        }
        return response.text();
      }).then((text) => {
        if (text === void 0) {
          return;
        }
        return { key, text };
      }).catch((reason) => {
        console.warn("[uBR] cachestorage", reason);
      });
    };
    const getAll = async () => {
      const cache = await getAPI();
      if (cache === null) {
        return;
      }
      return cache.keys().then((requests) => {
        const promises = [];
        for (const request of requests) {
          promises.push(getOne(urlToKey(request.url)));
        }
        return Promise.all(promises);
      }).then((responses) => {
        const bin = {};
        for (const response of responses) {
          if (response === void 0) {
            continue;
          }
          bin[response.key] = response.text;
        }
        return bin;
      }).catch((reason) => {
        console.warn("[uBR] cachestorage", reason);
      });
    };
    const setOne = async (key, text) => {
      if (text === void 0) {
        return removeOne(key);
      }
      const blob = new Blob([text], { type: "text/plain;charset=utf-8" });
      const cache = await getAPI();
      if (cache === null) {
        return;
      }
      return cache.put(keyToURL(key), new Response(blob)).catch((reason) => {
        console.warn("[uBR] cachestorage", reason);
      });
    };
    const removeOne = async (key) => {
      const cache = await getAPI();
      if (cache === null) {
        return;
      }
      return cache.delete(keyToURL(key)).catch((reason) => {
        console.warn("[uBR] cachestorage", reason);
      });
    };
    return {
      name: "cacheAPI",
      async get(arg) {
        const keys = keysFromGetArg(arg);
        if (keys === void 0) {
          return;
        }
        if (keys.length === 0) {
          return getAll();
        }
        const bin = {};
        const toFetch = keys.slice();
        const hasDefault = typeof arg === "object" && Array.isArray(arg) === false;
        for (let i = 0; i < toFetch.length; i++) {
          const key = toFetch[i];
          if (hasDefault && arg[key] !== void 0) {
            bin[key] = arg[key];
          }
          toFetch[i] = getOne(key);
        }
        const responses = await Promise.all(toFetch);
        for (const response of responses) {
          if (response === void 0) {
            continue;
          }
          const { key, text } = response;
          if (typeof key !== "string") {
            continue;
          }
          if (typeof text !== "string") {
            continue;
          }
          bin[key] = text;
        }
        if (Object.keys(bin).length === 0) {
          return;
        }
        return bin;
      },
      async keys(regex) {
        const cache = await getAPI();
        if (cache === null) {
          return [];
        }
        return cache.keys().then(
          (requests) => requests.map((r) => urlToKey(r.url)).filter((k2) => regex === void 0 || regex.test(k2))
        ).catch((e) => {
          console.warn("[uBR] cachestorage: cacheAPI.keys failed", e);
          return [];
        });
      },
      async set(rawbin, serializedbin) {
        const bin = shouldCache(serializedbin);
        if (bin === void 0) {
          return;
        }
        const keys = Object.keys(bin);
        const promises = [];
        for (const key of keys) {
          promises.push(setOne(key, bin[key]));
        }
        return Promise.all(promises);
      },
      remove(keys) {
        const toRemove = [];
        if (typeof keys === "string") {
          toRemove.push(removeOne(keys));
        } else if (Array.isArray(keys)) {
          for (const key of keys) {
            toRemove.push(removeOne(key));
          }
        }
        return Promise.all(toRemove);
      },
      async clear() {
        if (typeof caches !== "object" || caches === null) {
          return;
        }
        return globalThis.caches.delete(STORAGE_NAME).catch((reason) => {
          console.warn("[uBR] cachestorage", reason);
        });
      },
      shutdown() {
        cacheStoragePromise = void 0;
        return this.clear();
      }
    };
  })();
  var memoryStorage = (() => {
    const sessionStorage = vAPI.sessionStorage;
    const shouldCache = (bin) => {
      const out = {};
      for (const key of Object.keys(bin)) {
        if (key.startsWith("cache/compiled/")) {
          continue;
        }
        out[key] = bin[key];
      }
      if (Object.keys(out).length !== 0) {
        return out;
      }
    };
    return {
      name: "memoryStorage",
      get(...args) {
        return sessionStorage.get(...args).then((bin) => {
          return bin;
        }).catch((reason) => {
          console.warn("[uBR] cachestorage", reason);
        });
      },
      async keys(regex) {
        const bin = await this.get(null);
        const keys = [];
        for (const key of Object.keys(bin || {})) {
          if (regex && regex.test(key) === false) {
            continue;
          }
          keys.push(key);
        }
        return keys;
      },
      async set(rawbin, serializedbin) {
        const bin = shouldCache(serializedbin);
        if (bin === void 0) {
          return;
        }
        return sessionStorage.set(bin).catch((reason) => {
          console.warn("[uBR] cachestorage", reason);
        });
      },
      remove(...args) {
        return sessionStorage.remove(...args).catch((reason) => {
          console.warn("[uBR] cachestorage", reason);
        });
      },
      clear(...args) {
        return sessionStorage.clear(...args).catch((reason) => {
          console.warn("[uBR] cachestorage", reason);
        });
      },
      shutdown() {
        return this.clear();
      }
    };
  })();
  var idbStorage = /* @__PURE__ */ (() => {
    let dbPromise;
    const getDb = function() {
      if (dbPromise !== void 0) {
        return dbPromise;
      }
      dbPromise = new Promise((resolve) => {
        const req = indexedDB.open(STORAGE_NAME, 1);
        req.onupgradeneeded = (ev) => {
          if (ev.oldVersion === 1) {
            return;
          }
          try {
            const db2 = ev.target.result;
            db2.createObjectStore(STORAGE_NAME, { keyPath: "key" });
          } catch (e) {
            console.warn("[uBR] cachestorage: idbStorage getDb createObjectStore failed", e);
            req.onerror();
          }
        };
        req.onsuccess = (ev) => {
          if (resolve === void 0) {
            return;
          }
          resolve(ev.target.result || null);
          resolve = void 0;
        };
        req.onerror = req.onblocked = () => {
          if (resolve === void 0) {
            return;
          }
          console.warn("[uBR] cachestorage", req.error);
          resolve(null);
          resolve = void 0;
        };
        vAPI.defer.once(1e4).then(() => {
          if (resolve === void 0) {
            return;
          }
          resolve(null);
          resolve = void 0;
        });
      }).catch((reason) => {
        console.warn("[uBR] cachestorage: idbStorage getDb failed", reason);
        return null;
      });
      return dbPromise;
    };
    const shouldCache = (key) => {
      if (key.startsWith("cache/") === false) {
        return true;
      }
      return /^cache\/(compiled|selfie)\//.test(key);
    };
    const getAllEntries = async function() {
      const db2 = await getDb();
      if (db2 === null) {
        return [];
      }
      return new Promise((resolve) => {
        const entries = [];
        const transaction = db2.transaction(STORAGE_NAME, "readonly");
        transaction.oncomplete = transaction.onerror = transaction.onabort = () => {
          resolve(Promise.all(entries));
        };
        const table = transaction.objectStore(STORAGE_NAME);
        const req = table.openCursor();
        req.onsuccess = (ev) => {
          const cursor = ev.target && ev.target.result;
          if (!cursor) {
            return;
          }
          const { key, value } = cursor.value;
          if (value instanceof Blob === false) {
            entries.push({ key, value });
          }
          cursor.continue();
        };
      }).catch((reason) => {
        console.warn("[uBR] cachestorage: idbStorage getAllEntries failed", reason);
        return [];
      });
    };
    const getAllKeys = async function(regex) {
      const db2 = await getDb();
      if (db2 === null) {
        return [];
      }
      return new Promise((resolve) => {
        const keys = [];
        const transaction = db2.transaction(STORAGE_NAME, "readonly");
        transaction.oncomplete = transaction.onerror = transaction.onabort = () => {
          resolve(keys);
        };
        const table = transaction.objectStore(STORAGE_NAME);
        const req = table.openCursor();
        req.onsuccess = (ev) => {
          const cursor = ev.target && ev.target.result;
          if (!cursor) {
            return;
          }
          if (regex && regex.test(cursor.key) === false) {
            return;
          }
          keys.push(cursor.key);
          cursor.continue();
        };
      }).catch((reason) => {
        console.warn("[uBR] cachestorage: idbStorage getAllKeys failed", reason);
        return [];
      });
    };
    const getEntries = async function(keys) {
      const db2 = await getDb();
      if (db2 === null) {
        return [];
      }
      return new Promise((resolve) => {
        const entries = [];
        const gotOne = (ev) => {
          const { result } = ev.target;
          if (typeof result !== "object") {
            return;
          }
          if (result === null) {
            return;
          }
          const { key, value } = result;
          if (value instanceof Blob) {
            return;
          }
          entries.push({ key, value });
        };
        const transaction = db2.transaction(STORAGE_NAME, "readonly");
        transaction.oncomplete = transaction.onerror = transaction.onabort = () => {
          resolve(Promise.all(entries));
        };
        const table = transaction.objectStore(STORAGE_NAME);
        for (const key of keys) {
          const req = table.get(key);
          req.onsuccess = gotOne;
          req.onerror = () => {
            console.warn("[uBR] cachestorage: idbStorage getEntries table.get failed");
          };
        }
      }).catch((reason) => {
        console.warn("[uBR] cachestorage: idbStorage getEntries failed", reason);
        return [];
      });
    };
    const getAll = async () => {
      const entries = await getAllEntries();
      const outbin = {};
      for (const { key, value } of entries) {
        outbin[key] = value;
      }
      return outbin;
    };
    const setEntries = async (inbin) => {
      const keys = Object.keys(inbin);
      if (keys.length === 0) {
        return;
      }
      const db2 = await getDb();
      if (db2 === null) {
        return;
      }
      return new Promise((resolve) => {
        const entries = [];
        for (const key of keys) {
          entries.push({ key, value: inbin[key] });
        }
        const transaction = db2.transaction(STORAGE_NAME, "readwrite");
        transaction.oncomplete = transaction.onerror = transaction.onabort = () => {
          resolve();
        };
        const table = transaction.objectStore(STORAGE_NAME);
        for (const entry of entries) {
          table.put(entry);
        }
      }).catch((reason) => {
        console.warn("[uBR] cachestorage: idbStorage setEntries failed", reason);
      });
    };
    const deleteEntries = async (arg) => {
      const keys = Array.isArray(arg) ? arg.slice() : [arg];
      if (keys.length === 0) {
        return;
      }
      const db2 = await getDb();
      if (db2 === null) {
        return;
      }
      return new Promise((resolve) => {
        const transaction = db2.transaction(STORAGE_NAME, "readwrite");
        transaction.oncomplete = transaction.onerror = transaction.onabort = () => {
          resolve();
        };
        const table = transaction.objectStore(STORAGE_NAME);
        for (const key of keys) {
          table.delete(key);
        }
      }).catch((reason) => {
        console.warn("[uBR] cachestorage: idbStorage deleteEntries failed", reason);
      });
    };
    return {
      name: "idbStorage",
      async get(argbin) {
        const keys = keysFromGetArg(argbin);
        if (keys === void 0) {
          return;
        }
        if (keys.length === 0) {
          return getAll();
        }
        const entries = await getEntries(keys);
        const outbin = {};
        const toRemove = [];
        for (const { key, value } of entries) {
          if (shouldCache(key) === false) {
            toRemove.push(key);
            continue;
          }
          outbin[key] = value;
        }
        if (argbin instanceof Object && Array.isArray(argbin) === false) {
          for (const key of keys) {
            if (Object.hasOwn(outbin, key)) {
              continue;
            }
            outbin[key] = argbin[key];
          }
        }
        if (toRemove.length !== 0) {
          deleteEntries(toRemove);
        }
        return outbin;
      },
      async set(rawbin) {
        const bin = {};
        for (const key of Object.keys(rawbin)) {
          if (shouldCache(key) === false) {
            continue;
          }
          bin[key] = rawbin[key];
        }
        return setEntries(bin);
      },
      keys(...args) {
        return getAllKeys(...args);
      },
      remove(...args) {
        return deleteEntries(...args);
      },
      clear() {
        return getDb().then((db2) => {
          if (db2 === null) {
            return;
          }
          db2.close();
          indexedDB.deleteDatabase(STORAGE_NAME);
        }).catch((reason) => {
          console.warn("[uBR] cachestorage: idbStorage clear failed", reason);
        });
      },
      async shutdown() {
        await this.clear();
        dbPromise = void 0;
      }
    };
  })();
  var cacheAPIs = {
    "indexedDB": idbStorage,
    "cacheAPI": cacheAPI,
    "browser.storage.session": memoryStorage
  };
  var cachestorage_default = cacheStorage;

  // src/js/base64-custom.ts
  var valToDigit = new Uint8Array(64);
  var digitToVal = new Uint8Array(128);
  {
    const chars = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz@%";
    for (let i = 0, n = chars.length; i < n; i++) {
      const c = chars.charCodeAt(i);
      valToDigit[i] = c;
      digitToVal[c] = i;
    }
  }
  var denseBase64 = {
    magic: "DenseBase64_1",
    encode: function(input) {
      const m = input.length % 3;
      const n = input.length - m;
      let outputLength = n / 3 * 4;
      if (m !== 0) {
        outputLength += m + 1;
      }
      const output = new Uint8Array(outputLength);
      let j2 = 0;
      for (let i = 0; i < n; i += 3) {
        const i1 = input[i + 0];
        const i2 = input[i + 1];
        const i3 = input[i + 2];
        output[j2 + 0] = valToDigit[i1 >>> 2];
        output[j2 + 1] = valToDigit[i1 << 4 & 48 | i2 >>> 4];
        output[j2 + 2] = valToDigit[i2 << 2 & 60 | i3 >>> 6];
        output[j2 + 3] = valToDigit[i3 & 63];
        j2 += 4;
      }
      if (m !== 0) {
        const i1 = input[n];
        output[j2 + 0] = valToDigit[i1 >>> 2];
        if (m === 1) {
          output[j2 + 1] = valToDigit[i1 << 4 & 48];
        } else {
          const i2 = input[n + 1];
          output[j2 + 1] = valToDigit[i1 << 4 & 48 | i2 >>> 4];
          output[j2 + 2] = valToDigit[i2 << 2 & 60];
        }
      }
      const textDecoder2 = new TextDecoder();
      const b64str = textDecoder2.decode(output);
      return this.magic + b64str;
    },
    decode: function(instr, arrbuf) {
      if (instr.startsWith(this.magic) === false) {
        throw new Error("Invalid \xB5Block.denseBase64 encoding");
      }
      const outputLength = this.decodeSize(instr);
      const outbuf = arrbuf instanceof ArrayBuffer === false ? new Uint8Array(outputLength) : new Uint8Array(arrbuf);
      const inputLength = instr.length - this.magic.length;
      let i = this.magic.length;
      let j2 = 0;
      const m = inputLength & 3;
      const n = i + inputLength - m;
      while (i < n) {
        const i1 = digitToVal[instr.charCodeAt(i + 0)];
        const i2 = digitToVal[instr.charCodeAt(i + 1)];
        const i3 = digitToVal[instr.charCodeAt(i + 2)];
        const i4 = digitToVal[instr.charCodeAt(i + 3)];
        i += 4;
        outbuf[j2 + 0] = i1 << 2 | i2 >>> 4;
        outbuf[j2 + 1] = i2 << 4 & 240 | i3 >>> 2;
        outbuf[j2 + 2] = i3 << 6 & 192 | i4;
        j2 += 3;
      }
      if (m !== 0) {
        const i1 = digitToVal[instr.charCodeAt(i + 0)];
        const i2 = digitToVal[instr.charCodeAt(i + 1)];
        outbuf[j2 + 0] = i1 << 2 | i2 >>> 4;
        if (m === 3) {
          const i3 = digitToVal[instr.charCodeAt(i + 2)];
          outbuf[j2 + 1] = i2 << 4 & 240 | i3 >>> 2;
        }
      }
      return outbuf;
    },
    decodeSize: function(instr) {
      if (instr.startsWith(this.magic) === false) {
        return 0;
      }
      const inputLength = instr.length - this.magic.length;
      const m = inputLength & 3;
      const n = inputLength - m;
      let outputLength = (n >>> 2) * 3;
      if (m !== 0) {
        outputLength += m - 1;
      }
      return outputLength;
    }
  };

  // src/js/assets.ts
  init_static_filtering_parser();
  init_text_utils();
  var reIsExternalPath = /^(?:[a-z-]+):\/\//;
  var reIsUserAsset = /^user-/;
  var errorCantConnectTo = i18n$("errorCantConnectTo");
  var MS_PER_HOUR = 60 * 60 * 1e3;
  var MS_PER_DAY = 24 * MS_PER_HOUR;
  var MINUTES_PER_DAY = 24 * 60;
  var EXPIRES_DEFAULT = 7;
  var assets = {};
  var remoteServerFriendly = false;
  var stringIsNotEmpty = (s) => typeof s === "string" && s !== "";
  var parseExpires = (s) => {
    const matches = s.match(/(\d+)\s*([wdhm]?)/i);
    if (matches === null) {
      return;
    }
    let updateAfter = parseInt(matches[1], 10);
    if (updateAfter === 0) {
      return;
    }
    if (matches[2] === "w") {
      updateAfter *= 7 * 24;
    } else if (matches[2] === "h") {
      updateAfter = Math.max(updateAfter, 4) / 24;
    } else if (matches[2] === "m") {
      updateAfter = Math.max(updateAfter, 240) / 1440;
    }
    return updateAfter;
  };
  var extractMetadataFromList = (content, fields) => {
    const out = {};
    const head = content.slice(0, 1024);
    for (const field of fields) {
      const normalizedField = field.replace(/\s+/g, "-");
      const re2 = new RegExp(`^(?:! *|# +)${normalizedField.replace(/-/g, "(?: +|-)")}: *(.+)$`, "im");
      const match = re2.exec(head);
      let value = match && match[1].trim() || void 0;
      if (value !== void 0 && value.startsWith("%")) {
        value = void 0;
      }
      const lowerField = field.toLowerCase().replace(
        /-[a-z]/g,
        (s) => s.charAt(1).toUpperCase()
      );
      out[lowerField] = value !== void 0 ? orphanizeString(value) : void 0;
    }
    if (out.lastModified !== void 0) {
      out.lastModified = new Date(out.lastModified).getTime() || 0;
    }
    if (out.expires !== void 0) {
      out.expires = parseExpires(out.expires);
    }
    if (out.diffExpires !== void 0) {
      out.diffExpires = parseExpires(out.diffExpires);
    }
    return out;
  };
  assets.extractMetadataFromList = extractMetadataFromList;
  var resourceTimeFromXhr = (xhr) => {
    if (typeof xhr.response !== "string") {
      return 0;
    }
    const metadata = extractMetadataFromList(xhr.response, [
      "Last-Modified"
    ]);
    return metadata.lastModified || 0;
  };
  var resourceTimeFromParts = (parts, time) => {
    const goodParts = parts.filter((part) => typeof part === "object");
    return goodParts.reduce(
      (acc, part) => (part.resourceTime || 0) > acc ? part.resourceTime : acc,
      time
    );
  };
  var resourceIsStale = (networkDetails, cacheDetails) => {
    if (typeof networkDetails.resourceTime !== "number") {
      return false;
    }
    if (networkDetails.resourceTime === 0) {
      return false;
    }
    if (typeof cacheDetails.resourceTime !== "number") {
      return false;
    }
    if (cacheDetails.resourceTime === 0) {
      return false;
    }
    if (networkDetails.resourceTime < cacheDetails.resourceTime) {
      ubolog(`Skip ${networkDetails.url}
	older than ${cacheDetails.remoteURL}`);
      return true;
    }
    return false;
  };
  var getUpdateAfterTime = (assetKey, diff = false) => {
    const entry = assetCacheRegistry[assetKey];
    if (entry) {
      if (diff && typeof entry.diffExpires === "number") {
        return entry.diffExpires * MS_PER_DAY;
      }
      if (typeof entry.expires === "number") {
        return entry.expires * MS_PER_DAY;
      }
    }
    if (assetSourceRegistry) {
      const srcEntry = assetSourceRegistry[assetKey];
      if (srcEntry && typeof srcEntry.updateAfter === "number") {
        return srcEntry.updateAfter * MS_PER_DAY;
      }
    }
    return EXPIRES_DEFAULT * MS_PER_DAY;
  };
  var getWriteTime = (assetKey) => {
    const entry = assetCacheRegistry[assetKey];
    if (entry) {
      return entry.writeTime || 0;
    }
    return 0;
  };
  var isDiffUpdatableAsset = (content) => {
    if (typeof content !== "string") {
      return false;
    }
    const data = extractMetadataFromList(content, [
      "Diff-Path"
    ]);
    return typeof data.diffPath === "string" && data.diffPath.startsWith("%") === false;
  };
  var getContentURLs = (assetKey, options = {}) => {
    const contentURLs = [];
    const entry = assetSourceRegistry[assetKey];
    if (entry instanceof Object === false) {
      return contentURLs;
    }
    if (typeof entry.contentURL === "string") {
      contentURLs.push(entry.contentURL);
    } else if (Array.isArray(entry.contentURL)) {
      contentURLs.push(...entry.contentURL);
    } else if (reIsExternalPath.test(assetKey)) {
      contentURLs.push(assetKey);
    }
    if (options.favorLocal) {
      contentURLs.sort((a, b2) => {
        if (reIsExternalPath.test(a)) {
          return 1;
        }
        if (reIsExternalPath.test(b2)) {
          return -1;
        }
        return 0;
      });
    }
    if (options.favorOrigin !== true && Array.isArray(entry.cdnURLs)) {
      const cdnURLs = entry.cdnURLs.slice();
      for (let i = 0, n = cdnURLs.length; i < n; i++) {
        const j2 = Math.floor(Math.random() * n);
        if (j2 === i) {
          continue;
        }
        [cdnURLs[j2], cdnURLs[i]] = [cdnURLs[i], cdnURLs[j2]];
      }
      if (options.favorLocal) {
        contentURLs.push(...cdnURLs);
      } else {
        contentURLs.unshift(...cdnURLs);
      }
    }
    return contentURLs;
  };
  var observers = [];
  assets.addObserver = function(observer) {
    if (observers.indexOf(observer) === -1) {
      observers.push(observer);
    }
  };
  assets.removeObserver = function(observer) {
    let pos;
    while ((pos = observers.indexOf(observer)) !== -1) {
      observers.splice(pos, 1);
    }
  };
  var fireNotification = function(topic, details) {
    let result;
    for (const observer of observers) {
      const r = observer(topic, details);
      if (r !== void 0) {
        result = r;
      }
    }
    return result;
  };
  assets.fetch = function(url, options = {}) {
    return new Promise((resolve, reject) => {
      const timeoutAfter = background_default.hiddenSettings.assetFetchTimeout || 30;
      const xhr = new XMLHttpRequest();
      let contentLoaded = 0;
      const cleanup = function() {
        xhr.removeEventListener("load", onLoadEvent);
        xhr.removeEventListener("error", onErrorEvent);
        xhr.removeEventListener("abort", onErrorEvent);
        xhr.removeEventListener("progress", onProgressEvent);
        timeoutTimer.off();
      };
      const fail = function(details, msg) {
        logger_default.writeOne({
          realm: "message",
          type: "error",
          text: msg
        });
        details.content = "";
        details.error = msg;
        reject(details);
      };
      const onLoadEvent = function() {
        cleanup();
        const details = {
          url,
          statusCode: this.status || 200,
          statusText: this.statusText || ""
        };
        if (details.statusCode < 200 || details.statusCode >= 300) {
          return fail(details, `${url}: ${details.statusCode} ${details.statusText}`);
        }
        details.content = this.response;
        details.resourceTime = resourceTimeFromXhr(this);
        resolve(details);
      };
      const onErrorEvent = function() {
        cleanup();
        fail({ url }, errorCantConnectTo.replace("{{msg}}", url));
      };
      const onTimeout = function() {
        xhr.abort();
      };
      const onProgressEvent = function(ev) {
        if (ev.loaded === contentLoaded) {
          return;
        }
        contentLoaded = ev.loaded;
        timeoutTimer.offon({ sec: timeoutAfter });
      };
      const timeoutTimer = vAPI.defer.create(onTimeout);
      try {
        xhr.open("get", url, true);
        xhr.addEventListener("load", onLoadEvent);
        xhr.addEventListener("error", onErrorEvent);
        xhr.addEventListener("abort", onErrorEvent);
        xhr.addEventListener("progress", onProgressEvent);
        xhr.responseType = options.responseType || "text";
        xhr.send();
        timeoutTimer.on({ sec: timeoutAfter });
      } catch (e) {
        console.warn("[uBR] assets: xhr send/open failed", url, e);
        onErrorEvent.call(xhr);
      }
    });
  };
  assets.fetchText = async function(url) {
    const isExternal = reIsExternalPath.test(url);
    let actualUrl = isExternal ? url : vAPI.getURL(url);
    if (isExternal && remoteServerFriendly !== true) {
      const cacheBypassToken = background_default.hiddenSettings.updateAssetBypassBrowserCache ? Math.floor(Date.now() / 1e3) % 86413 : Math.floor(Date.now() / 36e5) % 13;
      const queryValue = `_=${cacheBypassToken}`;
      if (actualUrl.indexOf("?") === -1) {
        actualUrl += "?";
      } else {
        actualUrl += "&";
      }
      actualUrl += queryValue;
    }
    let details = { content: "", url };
    try {
      details = await assets.fetch(actualUrl);
      if (stringIsNotEmpty(details.content) === false) {
        details.content = "";
      }
      const text = details.content.trim();
      if (text.startsWith("<") && text.endsWith(">")) {
        details.content = "";
        details.error = "assets.fetchText(): Not a text file";
      }
    } catch (ex) {
      details = ex;
    }
    details.url = url;
    return details;
  };
  assets.fetchFilterList = async function(mainlistURL) {
    const toParsedURL = (url) => {
      try {
        return new URL(url.trim());
      } catch (e) {
        console.warn("[uBR] assets: toParsedURL URL parse failed", url, e);
      }
    };
    let rootDirectoryURL = toParsedURL(
      reIsExternalPath.test(mainlistURL) ? mainlistURL : vAPI.getURL(mainlistURL)
    );
    if (rootDirectoryURL !== void 0) {
      const pos = rootDirectoryURL.pathname.lastIndexOf("/");
      if (pos !== -1) {
        rootDirectoryURL.pathname = rootDirectoryURL.pathname.slice(0, pos + 1);
      } else {
        rootDirectoryURL = void 0;
      }
    }
    const sublistURLs = /* @__PURE__ */ new Set();
    const processIncludeDirectives = function(results) {
      const out = [];
      const reInclude = /^!#include +(\S+)[^\n\r]*(?:[\n\r]+|$)/gm;
      for (const result of results) {
        if (typeof result === "string") {
          out.push(result);
          continue;
        }
        if (result instanceof Object === false) {
          continue;
        }
        const rawContent = result.content;
        if (typeof rawContent !== "string") {
          continue;
        }
        const content = `${rawContent.trimEnd()}
`;
        const slices = utils.preparser.splitter(
          content,
          vAPI.webextFlavor.env
        );
        for (let i = 0, n = slices.length - 1; i < n; i++) {
          const slice = content.slice(slices[i + 0], slices[i + 1]);
          if ((i & 1) !== 0) {
            out.push(slice);
            continue;
          }
          let lastIndex = 0;
          for (; ; ) {
            if (rootDirectoryURL === void 0) {
              break;
            }
            const match = reInclude.exec(slice);
            if (match === null) {
              break;
            }
            if (toParsedURL(match[1]) !== void 0) {
              continue;
            }
            if (match[1].indexOf("..") !== -1) {
              continue;
            }
            const pos = result.url.lastIndexOf("/");
            if (pos === -1) {
              continue;
            }
            const subURL = result.url.slice(0, pos + 1) + match[1].trim();
            if (sublistURLs.has(subURL)) {
              continue;
            }
            sublistURLs.add(subURL);
            out.push(
              slice.slice(lastIndex, match.index + match[0].length),
              `! >>>>>>>> ${subURL}
`,
              assets.fetchText(subURL),
              `! <<<<<<<< ${subURL}
`
            );
            lastIndex = reInclude.lastIndex;
          }
          out.push(lastIndex === 0 ? slice : slice.slice(lastIndex));
        }
      }
      return out;
    };
    let allParts = [
      this.fetchText(mainlistURL)
    ];
    let resourceTime = 0;
    do {
      allParts = await Promise.all(allParts);
      const part = allParts.find((part2) => typeof part2 === "object" && part2.error !== void 0);
      if (part !== void 0) {
        return { url: mainlistURL, content: "", error: part.error };
      }
      resourceTime = resourceTimeFromParts(allParts, resourceTime);
      if (allParts.length === 1 && allParts[0] instanceof Object) {
        if (isDiffUpdatableAsset(allParts[0].content)) {
          allParts[0] = allParts[0].content;
          break;
        }
      }
      allParts = processIncludeDirectives(allParts);
    } while (allParts.some((part) => typeof part !== "string"));
    return {
      url: mainlistURL,
      resourceTime,
      content: allParts.length === 1 ? allParts[0] : allParts.join("")
    };
  };
  var assetSourceRegistryPromise;
  var assetSourceRegistry = /* @__PURE__ */ Object.create(null);
  function getAssetSourceRegistry() {
    if (assetSourceRegistryPromise === void 0) {
      assetSourceRegistryPromise = cachestorage_default.get(
        "assetSourceRegistry"
      ).then((bin) => {
        if (bin instanceof Object) {
          if (bin.assetSourceRegistry instanceof Object) {
            assetSourceRegistry = bin.assetSourceRegistry;
            ubolog("Loaded assetSourceRegistry");
            return assetSourceRegistry;
          }
        }
        return assets.fetchText(
          background_default.assetsBootstrapLocation || background_default.assetsJsonPath
        ).then((details) => {
          return details.content !== "" ? details : assets.fetchText(background_default.assetsJsonPath);
        }).then((details) => {
          updateAssetSourceRegistry(details.content, true);
          ubolog("Loaded assetSourceRegistry");
          return assetSourceRegistry;
        });
      });
    }
    return assetSourceRegistryPromise;
  }
  function registerAssetSource(assetKey, newDict) {
    const currentDict = assetSourceRegistry[assetKey] || {};
    for (const [k2, v2] of Object.entries(newDict)) {
      if (v2 === void 0 || v2 === null) {
        delete currentDict[k2];
      } else {
        currentDict[k2] = v2;
      }
    }
    let contentURL = newDict.contentURL;
    if (contentURL !== void 0) {
      if (typeof contentURL === "string") {
        contentURL = currentDict.contentURL = [contentURL];
      } else if (Array.isArray(contentURL) === false) {
        contentURL = currentDict.contentURL = [];
      }
      let remoteURLCount = 0;
      for (let i = 0; i < contentURL.length; i++) {
        if (reIsExternalPath.test(contentURL[i])) {
          remoteURLCount += 1;
        }
      }
      currentDict.hasLocalURL = remoteURLCount !== contentURL.length;
      currentDict.hasRemoteURL = remoteURLCount !== 0;
    } else if (currentDict.contentURL === void 0) {
      currentDict.contentURL = [];
    }
    if (currentDict.submitter) {
      currentDict.submitTime = Date.now();
    }
    assetSourceRegistry[assetKey] = currentDict;
  }
  function unregisterAssetSource(assetKey) {
    assetCacheRemove(assetKey);
    delete assetSourceRegistry[assetKey];
  }
  var saveAssetSourceRegistry = (() => {
    const save = () => {
      timer.off();
      cachestorage_default.set({ assetSourceRegistry });
    };
    const timer = vAPI.defer.create(save);
    return function(lazily) {
      if (lazily) {
        timer.offon(500);
      } else {
        save();
      }
    };
  })();
  async function assetSourceGetDetails(assetKey) {
    await getAssetSourceRegistry();
    const entry = assetSourceRegistry[assetKey];
    if (entry === void 0) {
      return;
    }
    return entry;
  }
  function updateAssetSourceRegistry(json, silent = false) {
    let newDict;
    try {
      newDict = JSON.parse(json);
      newDict["assets.json"].defaultListset = Array.from(Object.entries(newDict)).filter((a) => a[1] && a[1].content === "filters" && a[1].off === void 0).map((a) => a[0]);
    } catch (e) {
      console.warn("[uBR] assets: updateAssetSourceRegistry JSON parse failed", e);
    }
    if (newDict instanceof Object === false) {
      return;
    }
    const oldDict = assetSourceRegistry;
    fireNotification("assets.json-updated", { newDict, oldDict });
    for (const assetKey in oldDict) {
      if (newDict[assetKey] === void 0 && oldDict[assetKey].submitter === void 0) {
        unregisterAssetSource(assetKey);
      }
    }
    for (const assetKey in newDict) {
      if (oldDict[assetKey] === void 0 && !silent) {
        fireNotification(
          "builtin-asset-source-added",
          { assetKey, entry: newDict[assetKey] }
        );
      }
      registerAssetSource(assetKey, newDict[assetKey]);
    }
    saveAssetSourceRegistry();
  }
  assets.registerAssetSource = async function(assetKey, details) {
    await getAssetSourceRegistry();
    registerAssetSource(assetKey, details);
    saveAssetSourceRegistry(true);
  };
  assets.unregisterAssetSource = async function(assetKey) {
    await getAssetSourceRegistry();
    unregisterAssetSource(assetKey);
    saveAssetSourceRegistry(true);
  };
  var assetCacheRegistryStartTime = Date.now();
  var assetCacheRegistryPromise;
  var assetCacheRegistry = {};
  function getAssetCacheRegistry() {
    if (assetCacheRegistryPromise !== void 0) {
      return assetCacheRegistryPromise;
    }
    assetCacheRegistryPromise = cachestorage_default.get(
      "assetCacheRegistry"
    ).then((bin) => {
      if (bin instanceof Object === false) {
        return;
      }
      if (bin.assetCacheRegistry instanceof Object === false) {
        return;
      }
      if (Object.keys(assetCacheRegistry).length !== 0) {
        return console.error("getAssetCacheRegistry(): assetCacheRegistry reassigned!");
      }
      ubolog("Loaded assetCacheRegistry");
      assetCacheRegistry = bin.assetCacheRegistry;
    }).then(
      () => assetCacheRegistry
    );
    return assetCacheRegistryPromise;
  }
  var saveAssetCacheRegistry = (() => {
    const save = () => {
      timer.off();
      return cachestorage_default.set({ assetCacheRegistry });
    };
    const timer = vAPI.defer.create(save);
    return (throttle = 0) => {
      if (throttle === 0) {
        return save();
      }
      timer.offon({ sec: throttle });
    };
  })();
  async function assetCacheRead(assetKey, updateReadTime = false) {
    const t0 = Date.now();
    const internalKey = `cache/${assetKey}`;
    const reportBack = function(content) {
      if (content instanceof Blob) {
        content = "";
      }
      const details = { assetKey, content };
      if (content === "" || content === void 0) {
        details.error = "ENOTFOUND";
      }
      return details;
    };
    const [, bin] = await Promise.all([
      getAssetCacheRegistry(),
      cachestorage_default.get(internalKey)
    ]);
    if (bin instanceof Object === false) {
      return reportBack("");
    }
    if (Object.hasOwn(bin, internalKey) === false) {
      return reportBack("");
    }
    const entry = assetCacheRegistry[assetKey];
    if (entry === void 0) {
      return reportBack("");
    }
    entry.readTime = Date.now();
    if (updateReadTime) {
      saveAssetCacheRegistry(23);
    }
    return reportBack(bin[internalKey]);
  }
  async function assetCacheWrite(assetKey, content, options = {}) {
    if (content === "" || content === void 0) {
      return assetCacheRemove(assetKey);
    }
    const cacheDict = await getAssetCacheRegistry();
    const { resourceTime, url } = options;
    const entry = cacheDict[assetKey] || {};
    entry.writeTime = entry.readTime = Date.now();
    entry.resourceTime = resourceTime || 0;
    if (typeof url === "string") {
      entry.remoteURL = url;
    }
    cacheDict[assetKey] = entry;
    await cachestorage_default.set({ [`cache/${assetKey}`]: content });
    saveAssetCacheRegistry(3);
    const result = { assetKey, content };
    if (options.silent !== true) {
      fireNotification("after-asset-updated", result);
    }
    return result;
  }
  async function assetCacheRemove(pattern, options = {}) {
    const cacheDict = await getAssetCacheRegistry();
    const removedEntries = [];
    const removedContent = [];
    for (const assetKey in cacheDict) {
      if (pattern instanceof RegExp) {
        if (pattern.test(assetKey) === false) {
          continue;
        }
      } else if (typeof pattern === "string") {
        if (assetKey !== pattern) {
          continue;
        }
      }
      removedEntries.push(assetKey);
      removedContent.push(`cache/${assetKey}`);
      delete cacheDict[assetKey];
    }
    if (options.janitor && pattern instanceof RegExp) {
      const re2 = new RegExp(
        pattern.source.replace(/^\^/, "^cache\\/"),
        pattern.flags
      );
      const keys = await cachestorage_default.keys(re2);
      for (const key of keys) {
        removedContent.push(key);
        ubolog(`Removing stray ${key}`);
      }
    }
    if (removedContent.length !== 0) {
      await Promise.all([
        cachestorage_default.remove(removedContent),
        cachestorage_default.set({ assetCacheRegistry })
      ]);
    }
    for (let i = 0; i < removedEntries.length; i++) {
      fireNotification("after-asset-updated", {
        assetKey: removedEntries[i]
      });
    }
    return removedEntries.map((assetKey) => ({ assetKey }));
  }
  async function assetCacheGetDetails(assetKey) {
    const cacheDict = await getAssetCacheRegistry();
    const entry = cacheDict[assetKey];
    if (entry === void 0) {
      return;
    }
    return entry;
  }
  async function assetCacheSetDetails(assetKey, details) {
    const cacheDict = await getAssetCacheRegistry();
    const entry = cacheDict[assetKey];
    if (entry === void 0) {
      return;
    }
    let modified = false;
    for (const [k2, v2] of Object.entries(details)) {
      if (v2 === void 0) {
        if (entry[k2] !== void 0) {
          delete entry[k2];
          modified = true;
          continue;
        }
      }
      if (v2 !== entry[k2]) {
        entry[k2] = v2;
        modified = true;
      }
    }
    if (modified) {
      saveAssetCacheRegistry(3);
    }
  }
  async function assetCacheMarkAsDirty(pattern, exclude) {
    const cacheDict = await getAssetCacheRegistry();
    let mustSave = false;
    for (const assetKey in cacheDict) {
      if (pattern instanceof RegExp) {
        if (pattern.test(assetKey) === false) {
          continue;
        }
      } else if (typeof pattern === "string") {
        if (assetKey !== pattern) {
          continue;
        }
      } else if (Array.isArray(pattern)) {
        if (pattern.indexOf(assetKey) === -1) {
          continue;
        }
      }
      if (exclude instanceof RegExp) {
        if (exclude.test(assetKey)) {
          continue;
        }
      } else if (typeof exclude === "string") {
        if (assetKey === exclude) {
          continue;
        }
      } else if (Array.isArray(exclude)) {
        if (exclude.indexOf(assetKey) !== -1) {
          continue;
        }
      }
      const cacheEntry = cacheDict[assetKey];
      if (!cacheEntry.writeTime) {
        continue;
      }
      cacheDict[assetKey].writeTime = 0;
      mustSave = true;
    }
    if (mustSave) {
      cachestorage_default.set({ assetCacheRegistry });
    }
  }
  var readUserAsset = async function(assetKey) {
    const bin = await vAPI.storage.get(assetKey);
    const content = bin instanceof Object && typeof bin[assetKey] === "string" ? bin[assetKey] : "";
    return { assetKey, content };
  };
  var saveUserAsset = function(assetKey, content) {
    return vAPI.storage.set({ [assetKey]: content }).then(() => {
      return { assetKey, content };
    });
  };
  assets.get = async function(assetKey, options = {}) {
    if (assetKey === background_default.userFiltersPath) {
      return readUserAsset(assetKey);
    }
    if (background_default.readyToFilter !== true) {
      if (options.favorLocal === void 0) {
        options.favorLocal = true;
      }
    }
    let assetDetails = {};
    const reportBack = (content, url = "", err) => {
      const details2 = { assetKey, content };
      if (err !== void 0) {
        details2.error = assetDetails.lastError = err;
      } else {
        assetDetails.lastError = void 0;
      }
      if (options.needSourceURL) {
        if (url === "" && assetCacheRegistry instanceof Object && assetCacheRegistry[assetKey] instanceof Object) {
          details2.sourceURL = assetCacheRegistry[assetKey].remoteURL;
        }
        if (reIsExternalPath.test(url)) {
          details2.sourceURL = url;
        }
      }
      return details2;
    };
    const updateReadTime = /^(?:compiled|selfie)\//.test(assetKey) === false;
    const details = await assetCacheRead(assetKey, updateReadTime);
    if (details.content !== "") {
      return reportBack(details.content);
    }
    const assetRegistry = await getAssetSourceRegistry();
    assetDetails = assetRegistry[assetKey] || {};
    const contentURLs = getContentURLs(assetKey, options);
    if (contentURLs.length === 0 && reIsExternalPath.test(assetKey)) {
      assetDetails.content = "filters";
      contentURLs.push(assetKey);
    }
    let error = "ENOTFOUND";
    for (const contentURL of contentURLs) {
      ubolog(`Fetching ${contentURL} from remote server `);
      const details2 = assetDetails.content === "filters" ? await assets.fetchFilterList(contentURL) : await assets.fetchText(contentURL);
      if (details2.error !== void 0) {
        error = details2.error;
      }
      if (details2.content === "") {
        continue;
      }
      if (reIsExternalPath.test(contentURL) && options.dontCache !== true) {
        assetCacheWrite(assetKey, details2.content, {
          url: contentURL,
          silent: options.silent === true
        });
        registerAssetSource(assetKey, { error: void 0 });
        if (assetDetails.content === "filters") {
          const metadata = extractMetadataFromList(details2.content, [
            "Last-Modified",
            "Expires",
            "Diff-Name",
            "Diff-Path",
            "Diff-Expires"
          ]);
          metadata.diffUpdated = void 0;
          assetCacheSetDetails(assetKey, metadata);
        }
      }
      return reportBack(details2.content, contentURL);
    }
    if (assetRegistry[assetKey] !== void 0) {
      registerAssetSource(assetKey, {
        error: { time: Date.now(), error }
      });
    }
    return reportBack("", "", error);
  };
  async function getRemote(assetKey, options = {}) {
    const [
      assetDetails = {},
      cacheDetails = {}
    ] = await Promise.all([
      assetSourceGetDetails(assetKey),
      assetCacheGetDetails(assetKey)
    ]);
    let error;
    let stale = false;
    const reportBack = function(content, url = "", err = "") {
      const details = { assetKey, content, url };
      if (err !== "") {
        details.error = assetDetails.lastError = err;
      } else {
        assetDetails.lastError = void 0;
      }
      return details;
    };
    for (const contentURL of getContentURLs(assetKey, options)) {
      if (reIsExternalPath.test(contentURL) === false) {
        continue;
      }
      const result = assetDetails.content === "filters" ? await assets.fetchFilterList(contentURL) : await assets.fetchText(contentURL);
      if (stringIsNotEmpty(result.content) === false) {
        error = result.statusText;
        if (result.statusCode === 0) {
          error = "network error";
        }
        continue;
      }
      error = void 0;
      if (options.favorOrigin !== true) {
        stale = resourceIsStale(result, cacheDetails);
        if (stale) {
          continue;
        }
      }
      assetCacheWrite(assetKey, result.content, {
        url: contentURL,
        resourceTime: result.resourceTime || 0
      });
      if (assetDetails.content === "filters") {
        const metadata = extractMetadataFromList(result.content, [
          "Last-Modified",
          "Expires",
          "Diff-Name",
          "Diff-Path",
          "Diff-Expires"
        ]);
        metadata.diffUpdated = void 0;
        assetCacheSetDetails(assetKey, metadata);
      }
      registerAssetSource(assetKey, { birthtime: void 0, error: void 0 });
      return reportBack(result.content, contentURL);
    }
    if (error !== void 0) {
      registerAssetSource(assetKey, { error: { time: Date.now(), error } });
      return reportBack("", "", "ENOTFOUND");
    }
    if (stale) {
      assetCacheSetDetails(assetKey, { writeTime: cacheDetails.resourceTime });
    }
    return reportBack("");
  }
  assets.put = async function(assetKey, content) {
    return reIsUserAsset.test(assetKey) ? await saveUserAsset(assetKey, content) : await assetCacheWrite(assetKey, content);
  };
  assets.toCache = async function(assetKey, content) {
    return assetCacheWrite(assetKey, content);
  };
  assets.fromCache = async function(assetKey) {
    const details = await assetCacheRead(assetKey);
    return details && details.content;
  };
  assets.metadata = async function() {
    await Promise.all([
      getAssetSourceRegistry(),
      getAssetCacheRegistry()
    ]);
    const assetDict = JSON.parse(JSON.stringify(assetSourceRegistry));
    const cacheDict = assetCacheRegistry;
    const now = Date.now();
    for (const assetKey in assetDict) {
      const assetEntry = assetDict[assetKey];
      const cacheEntry = cacheDict[assetKey];
      if (assetEntry.content === "filters" && assetEntry.external !== true) {
        assetEntry.isDefault = assetEntry.off === void 0 || assetEntry.off === true && background_default.listMatchesEnvironment(assetEntry);
      }
      if (cacheEntry) {
        assetEntry.cached = true;
        assetEntry.writeTime = cacheEntry.writeTime;
        const obsoleteAfter = cacheEntry.writeTime + getUpdateAfterTime(assetKey);
        assetEntry.obsolete = obsoleteAfter < now;
        assetEntry.remoteURL = cacheEntry.remoteURL;
        if (cacheEntry.diffUpdated) {
          assetEntry.diffUpdated = cacheEntry.diffUpdated;
        }
      } else if (assetEntry.contentURL && assetEntry.contentURL.length !== 0) {
        assetEntry.writeTime = 0;
        assetEntry.obsolete = true;
      }
    }
    return assetDict;
  };
  assets.purge = assetCacheMarkAsDirty;
  assets.remove = function(...args) {
    return assetCacheRemove(...args);
  };
  assets.rmrf = function() {
    return assetCacheRemove(/./);
  };
  assets.getUpdateAges = async function(conditions = {}) {
    const assetDict = await assets.metadata();
    const now = Date.now();
    const out = [];
    for (const [assetKey, asset] of Object.entries(assetDict)) {
      if (asset.hasRemoteURL !== true) {
        continue;
      }
      const tokens = conditions[asset.content];
      if (Array.isArray(tokens) === false) {
        continue;
      }
      if (tokens.includes("*") === false) {
        if (tokens.includes(assetKey) === false) {
          continue;
        }
      }
      const age = now - (asset.writeTime || 0);
      out.push({
        assetKey,
        age,
        ageNormalized: age / Math.max(1, getUpdateAfterTime(assetKey))
      });
    }
    return out;
  };
  var updaterAssetDelayDefault = 12e4;
  var updaterUpdated = [];
  var updaterFetched = /* @__PURE__ */ new Set();
  var updaterStatus;
  var updaterAssetDelay = updaterAssetDelayDefault;
  var updaterAuto = false;
  async function getUpdateCandidates() {
    const [assetDict, cacheDict] = await Promise.all([
      getAssetSourceRegistry(),
      getAssetCacheRegistry()
    ]);
    const toUpdate = [];
    for (const assetKey in assetDict) {
      const assetEntry = assetDict[assetKey];
      if (assetEntry.hasRemoteURL !== true) {
        continue;
      }
      if (updaterFetched.has(assetKey)) {
        continue;
      }
      const cacheEntry = cacheDict[assetKey];
      if (fireNotification("before-asset-updated", {
        assetKey,
        type: assetEntry.content
      }) === true) {
        toUpdate.push(assetKey);
        continue;
      }
      if (cacheEntry && cacheEntry.readTime < assetCacheRegistryStartTime) {
        await assetCacheRemove(assetKey);
      }
    }
    toUpdate.sort((a, b2) => {
      const ta2 = cacheDict[a] !== void 0 ? cacheDict[a].writeTime : 0;
      const tb = cacheDict[b2] !== void 0 ? cacheDict[b2].writeTime : 0;
      return ta2 - tb;
    });
    return toUpdate;
  }
  async function updateNext() {
    const toUpdate = await getUpdateCandidates();
    const now = Date.now();
    const toHardUpdate = [];
    while (toUpdate.length !== 0) {
      const assetKey2 = toUpdate.shift();
      const writeTime = getWriteTime(assetKey2);
      const updateDelay = getUpdateAfterTime(assetKey2);
      if (writeTime + updateDelay > now) {
        continue;
      }
      toHardUpdate.push(assetKey2);
    }
    if (toHardUpdate.length === 0) {
      return updateDone();
    }
    const assetKey = toHardUpdate.pop();
    updaterFetched.add(assetKey);
    remoteServerFriendly = updaterAuto;
    let result;
    if (assetKey !== "assets.json" || background_default.hiddenSettings.debugAssetsJson !== true) {
      result = await getRemote(assetKey, { favorOrigin: updaterAuto === false });
    } else {
      result = await assets.fetchText(background_default.assetsJsonPath);
      result.assetKey = "assets.json";
    }
    remoteServerFriendly = false;
    if (result.error) {
      ubolog(`Full updater: failed to update ${assetKey}`);
      fireNotification("asset-update-failed", { assetKey: result.assetKey });
    } else {
      ubolog(`Full updater: successfully updated ${assetKey}`);
      updaterUpdated.push(result.assetKey);
      if (result.assetKey === "assets.json" && result.content !== "") {
        updateAssetSourceRegistry(result.content);
      }
    }
    updaterTimer.on(updaterAssetDelay);
  }
  var updaterTimer = vAPI.defer.create(updateNext);
  function updateDone() {
    const assetKeys = updaterUpdated.slice(0);
    updaterFetched.clear();
    updaterUpdated.length = 0;
    updaterStatus = void 0;
    updaterAuto = false;
    updaterAssetDelay = updaterAssetDelayDefault;
    ubolog("Updater: cycle end");
    if (assetKeys.length) {
      ubolog(`Updater: ${assetKeys.join()} were updated`);
    }
    fireNotification("after-assets-updated", { assetKeys });
  }
  assets.updateStart = function(_details) {
    updateDone();
  };
  assets.updateStop = function() {
    updaterTimer.off();
    if (updaterStatus !== void 0) {
      updateDone();
    }
  };
  assets.isUpdating = function() {
    return updaterStatus === "updating" && updaterAssetDelay <= background_default.hiddenSettings.manualUpdateAssetFetchPeriod;
  };
  var assets_default = assets;

  // src/js/lz4.ts
  var promisedInstance;
  var textEncoder;
  var textDecoder;
  var ttlCount = 0;
  var ttlDelay = 6e4;
  var init = function() {
    ttlDelay = background_default.hiddenSettings.autoUpdateAssetFetchPeriod * 2 * 1e3;
    if (promisedInstance === void 0) {
      let flavor;
      if (background_default.hiddenSettings.disableWebAssembly === true) {
        flavor = "js";
      }
      promisedInstance = self.lz4BlockCodec.createInstance(flavor);
    }
    return promisedInstance;
  };
  var destroy = function() {
    promisedInstance = void 0;
    textEncoder = textDecoder = void 0;
    ttlCount = 0;
  };
  var ttlTimer = vAPI.defer.create(destroy);
  var ttlManage = function(count) {
    ttlTimer.off();
    ttlCount += count;
    if (ttlCount > 0) {
      return;
    }
    ttlTimer.on(ttlDelay);
  };
  var encodeValue = function(lz4CodecInstance, dataIn) {
    if (!lz4CodecInstance) {
      return;
    }
    if (textEncoder === void 0) {
      textEncoder = new TextEncoder();
    }
    const inputArray = textEncoder.encode(dataIn);
    const inputSize = inputArray.byteLength;
    const outputArray = lz4CodecInstance.encodeBlock(inputArray, 8);
    if (outputArray instanceof Uint8Array === false) {
      return;
    }
    outputArray[0] = 24;
    outputArray[1] = 77;
    outputArray[2] = 34;
    outputArray[3] = 4;
    outputArray[4] = inputSize >>> 0 & 255;
    outputArray[5] = inputSize >>> 8 & 255;
    outputArray[6] = inputSize >>> 16 & 255;
    outputArray[7] = inputSize >>> 24 & 255;
    return outputArray;
  };
  var decodeValue = function(lz4CodecInstance, inputArray) {
    if (!lz4CodecInstance) {
      return;
    }
    if (inputArray[0] !== 24 || inputArray[1] !== 77 || inputArray[2] !== 34 || inputArray[3] !== 4) {
      console.error("decodeValue: invalid input array");
      return;
    }
    const outputSize = inputArray[4] << 0 | inputArray[5] << 8 | inputArray[6] << 16 | inputArray[7] << 24;
    const outputArray = lz4CodecInstance.decodeBlock(inputArray, 8, outputSize);
    if (outputArray instanceof Uint8Array === false) {
      return;
    }
    if (textDecoder === void 0) {
      textDecoder = new TextDecoder();
    }
    const s = textDecoder.decode(outputArray);
    return s;
  };
  var lz4Codec = {
    // Arguments:
    //   dataIn: must be a string
    // Returns:
    //   A Uint8Array, or the input string as is if compression is not
    //   possible.
    encode: async function(dataIn, serialize3 = void 0) {
      if (typeof dataIn !== "string" || dataIn.length < 4096) {
        return dataIn;
      }
      ttlManage(1);
      const lz4CodecInstance = await init();
      let dataOut = encodeValue(lz4CodecInstance, dataIn);
      ttlManage(-1);
      if (serialize3 instanceof Function) {
        dataOut = await serialize3(dataOut);
      }
      return dataOut || dataIn;
    },
    // Arguments:
    //   dataIn: must be a Uint8Array
    // Returns:
    //   A string, or the input argument as is if decompression is not
    //   possible.
    decode: async function(dataIn, deserialize2 = void 0) {
      if (deserialize2 instanceof Function) {
        dataIn = await deserialize2(dataIn);
      }
      if (dataIn instanceof Uint8Array === false) {
        return dataIn;
      }
      ttlManage(1);
      const lz4CodecInstance = await init();
      const dataOut = decodeValue(lz4CodecInstance, dataIn);
      ttlManage(-1);
      return dataOut || dataIn;
    },
    relinquish: function() {
      ttlDelay = 1;
      ttlManage(0);
    }
  };
  var lz4_default = lz4Codec;

  // src/js/messaging.ts
  init_publicsuffixlist();
  init_punycode();

  // src/js/redirect-engine.ts
  init_text_utils();

  // src/js/redirect-resources.ts
  var redirect_resources_default = /* @__PURE__ */ new Map([
    ["1x1.gif", {
      alias: "1x1-transparent.gif",
      data: "blob"
    }],
    ["2x2.png", {
      alias: "2x2-transparent.png",
      data: "blob"
    }],
    ["3x2.png", {
      alias: "3x2-transparent.png",
      data: "blob"
    }],
    ["32x32.png", {
      alias: "32x32-transparent.png",
      data: "blob"
    }],
    ["amazon_ads.js", {
      alias: "amazon-adsystem.com/aax2/amzn_ads.js",
      data: "text"
    }],
    ["amazon_apstag.js", {}],
    ["ampproject_v0.js", {
      alias: "ampproject.org/v0.js"
    }],
    ["adthrive_abd.js", {
      data: "text"
    }],
    ["chartbeat.js", {
      alias: "static.chartbeat.com/chartbeat.js"
    }],
    ["click2load.html", {
      params: ["aliasURL", "url"]
    }],
    ["doubleclick_instream_ad_status.js", {
      alias: "doubleclick.net/instream/ad_status.js",
      data: "text"
    }],
    ["empty", {
      data: "text"
      // Important!
    }],
    ["fingerprint2.js", {
      data: "text"
    }],
    ["fingerprint3.js", {
      data: "text"
    }],
    ["google-analytics_analytics.js", {
      alias: [
        "google-analytics.com/analytics.js",
        "googletagmanager_gtm.js",
        "googletagmanager.com/gtm.js"
      ],
      data: "text"
    }],
    ["google-analytics_cx_api.js", {
      alias: "google-analytics.com/cx/api.js"
    }],
    ["google-analytics_ga.js", {
      alias: "google-analytics.com/ga.js",
      data: "text"
    }],
    ["google-analytics_inpage_linkid.js", {
      alias: "google-analytics.com/inpage_linkid.js"
    }],
    ["google-ima.js", {
      alias: "google-ima3",
      /* adguard compatibility */
      data: "text"
    }],
    ["googlesyndication_adsbygoogle.js", {
      alias: [
        "googlesyndication.com/adsbygoogle.js",
        "googlesyndication-adsbygoogle"
        /* adguard compatibility */
      ],
      data: "text"
    }],
    ["googletagservices_gpt.js", {
      alias: [
        "googletagservices.com/gpt.js",
        "googletagservices-gpt"
        /* adguard compatibility */
      ],
      data: "text"
    }],
    ["hd-main.js", {}],
    ["nitropay_ads.js", {
      data: "text"
    }],
    ["nobab.js", {
      alias: ["bab-defuser.js", "prevent-bab.js"],
      data: "text"
    }],
    ["nobab2.js", {
      data: "text"
    }],
    ["noeval.js", {
      data: "text"
    }],
    ["noeval-silent.js", {
      alias: "silent-noeval.js",
      data: "text"
    }],
    ["nofab.js", {
      alias: "fuckadblock.js-3.2.0",
      data: "text"
    }],
    ["noop-0.1s.mp3", {
      alias: ["noopmp3-0.1s", "abp-resource:blank-mp3"],
      data: "blob"
    }],
    ["noop-0.5s.mp3", {}],
    ["noop-1s.mp4", {
      alias: ["noopmp4-1s", "abp-resource:blank-mp4"],
      data: "blob"
    }],
    ["noop.css", {
      data: "text"
    }],
    ["noop.html", {
      alias: "noopframe"
    }],
    ["noop.js", {
      alias: ["noopjs", "abp-resource:blank-js"],
      data: "text"
    }],
    ["noop.json", {
      alias: ["noopjson"],
      data: "text"
    }],
    ["noop.txt", {
      alias: "nooptext",
      data: "text"
    }],
    ["noop-vast2.xml", {
      alias: "noopvast-2.0",
      data: "text"
    }],
    ["noop-vast3.xml", {
      alias: "noopvast-3.0",
      data: "text"
    }],
    ["noop-vast4.xml", {
      alias: "noopvast-4.0",
      data: "text"
    }],
    ["noop-vmap1.xml", {
      alias: ["noop-vmap1.0.xml", "noopvmap-1.0"],
      data: "text"
    }],
    ["outbrain-widget.js", {
      alias: "widgets.outbrain.com/outbrain.js"
    }],
    ["popads.js", {
      alias: ["popads.net.js", "prevent-popads-net.js"],
      data: "text"
    }],
    ["popads-dummy.js", {
      data: "text"
    }],
    ["prebid-ads.js", {
      data: "text"
    }],
    ["scorecardresearch_beacon.js", {
      alias: "scorecardresearch.com/beacon.js"
    }],
    ["sensors-analytics.js", {
      data: "text"
    }]
  ]);

  // src/js/resources/base.js
  var registeredScriptlets = [];
  var registerScriptlet = (fn2, details) => {
    if (typeof details !== "object") {
      throw new ReferenceError("Missing scriptlet details");
    }
    details.fn = fn2;
    fn2.details = details;
    if (Array.isArray(details.dependencies)) {
      details.dependencies.forEach((fn3, i, array) => {
        if (typeof fn3 !== "function") {
          return;
        }
        array[i] = fn3.details.name;
      });
    }
    registeredScriptlets.push(details);
  };

  // src/js/resources/safe-self.js
  function safeSelf() {
    if (scriptletGlobals.safeSelf) {
      return scriptletGlobals.safeSelf;
    }
    const self2 = globalThis;
    const safe = {
      "Array_from": Array.from,
      "Error": self2.Error,
      "Function_toStringFn": self2.Function.prototype.toString,
      "Function_toString": (thisArg) => safe.Function_toStringFn.call(thisArg),
      "Math_floor": Math.floor,
      "Math_max": Math.max,
      "Math_min": Math.min,
      "Math_random": Math.random,
      "Object": Object,
      "Object_defineProperty": Object.defineProperty.bind(Object),
      "Object_defineProperties": Object.defineProperties.bind(Object),
      "Object_fromEntries": Object.fromEntries.bind(Object),
      "Object_getOwnPropertyDescriptor": Object.getOwnPropertyDescriptor.bind(Object),
      "Object_hasOwn": Object.hasOwn.bind(Object),
      "Object_toString": Object.prototype.toString,
      "RegExp": self2.RegExp,
      "RegExp_test": self2.RegExp.prototype.test,
      "RegExp_exec": self2.RegExp.prototype.exec,
      "Request_clone": self2.Request.prototype.clone,
      "String": self2.String,
      "String_fromCharCode": String.fromCharCode,
      "String_split": String.prototype.split,
      "XMLHttpRequest": self2.XMLHttpRequest,
      "addEventListener": self2.EventTarget.prototype.addEventListener,
      "removeEventListener": self2.EventTarget.prototype.removeEventListener,
      "fetch": self2.fetch,
      "JSON": self2.JSON,
      "JSON_parseFn": self2.JSON.parse,
      "JSON_stringifyFn": self2.JSON.stringify,
      "JSON_parse": (...args) => safe.JSON_parseFn.call(safe.JSON, ...args),
      "JSON_stringify": (...args) => safe.JSON_stringifyFn.call(safe.JSON, ...args),
      "log": console.log.bind(console),
      // Properties
      logLevel: 0,
      // Methods
      makeLogPrefix(...args) {
        return this.sendToLogger && `[${args.join(" \u205D ")}]` || "";
      },
      uboLog(...args) {
        if (this.sendToLogger === void 0) {
          return;
        }
        if (args === void 0 || args[0] === "") {
          return;
        }
        return this.sendToLogger("info", ...args);
      },
      uboErr(...args) {
        if (this.sendToLogger === void 0) {
          return;
        }
        if (args === void 0 || args[0] === "") {
          return;
        }
        return this.sendToLogger("error", ...args);
      },
      escapeRegexChars(s) {
        return s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
      },
      initPattern(pattern, options = {}) {
        if (pattern === "") {
          return { matchAll: true, expect: true };
        }
        const expect = options.canNegate !== true || pattern.startsWith("!") === false;
        if (expect === false) {
          pattern = pattern.slice(1);
        }
        const match = /^\/(.+)\/([gimsu]*)$/.exec(pattern);
        if (match !== null) {
          return {
            re: new this.RegExp(
              match[1],
              match[2] || options.flags
            ),
            expect
          };
        }
        if (options.flags !== void 0) {
          return {
            re: new this.RegExp(
              this.escapeRegexChars(pattern),
              options.flags
            ),
            expect
          };
        }
        return { pattern, expect };
      },
      testPattern(details, haystack) {
        if (details.matchAll) {
          return true;
        }
        if (details.re) {
          return this.RegExp_test.call(details.re, haystack) === details.expect;
        }
        return haystack.includes(details.pattern) === details.expect;
      },
      patternToRegex(pattern, flags = void 0, verbatim = false) {
        if (pattern === "") {
          return /^/;
        }
        const match = /^\/(.+)\/([gimsu]*)$/.exec(pattern);
        if (match === null) {
          const reStr = this.escapeRegexChars(pattern);
          return new RegExp(verbatim ? `^${reStr}$` : reStr, flags);
        }
        try {
          return new RegExp(match[1], match[2] || void 0);
        } catch (e) {
          console.warn("[uBR] safe-self: RegExp pattern failed", e);
        }
        return /^/;
      },
      getExtraArgs(args, offset = 0) {
        const entries = args.slice(offset).reduce((out, v2, i, a) => {
          if ((i & 1) === 0) {
            const rawValue = a[i + 1];
            const value = /^\d+$/.test(rawValue) ? parseInt(rawValue, 10) : rawValue;
            out.push([a[i], value]);
          }
          return out;
        }, []);
        return this.Object_fromEntries(entries);
      },
      onIdle(fn2, options) {
        if (self2.requestIdleCallback) {
          return self2.requestIdleCallback(fn2, options);
        }
        return self2.requestAnimationFrame(fn2);
      },
      offIdle(id) {
        if (self2.requestIdleCallback) {
          return self2.cancelIdleCallback(id);
        }
        return self2.cancelAnimationFrame(id);
      }
    };
    scriptletGlobals.safeSelf = safe;
    if (scriptletGlobals.bcSecret === void 0) {
      return safe;
    }
    safe.logLevel = scriptletGlobals.logLevel || 1;
    let lastLogType = "";
    let lastLogText = "";
    let lastLogTime = 0;
    safe.toLogText = (type, ...args) => {
      if (args.length === 0) {
        return;
      }
      const text = `[${document.location.hostname || document.location.href}]${args.join(" ")}`;
      if (text === lastLogText && type === lastLogType) {
        if (Date.now() - lastLogTime < 5e3) {
          return;
        }
      }
      lastLogType = type;
      lastLogText = text;
      lastLogTime = Date.now();
      return text;
    };
    try {
      const bc2 = new self2.BroadcastChannel(scriptletGlobals.bcSecret);
      let bcBuffer = [];
      safe.sendToLogger = (type, ...args) => {
        const text = safe.toLogText(type, ...args);
        if (text === void 0) {
          return;
        }
        if (bcBuffer === void 0) {
          return bc2.postMessage({ what: "messageToLogger", type, text });
        }
        bcBuffer.push({ type, text });
      };
      bc2.onmessage = (ev) => {
        const msg = ev.data;
        switch (msg) {
          case "iamready!":
            if (bcBuffer === void 0) {
              break;
            }
            bcBuffer.forEach(
              ({ type, text }) => bc2.postMessage({ what: "messageToLogger", type, text })
            );
            bcBuffer = void 0;
            break;
          case "setScriptletLogLevelToOne":
            safe.logLevel = 1;
            break;
          case "setScriptletLogLevelToTwo":
            safe.logLevel = 2;
            break;
        }
      };
      bc2.postMessage("areyouready?");
    } catch (e) {
      console.warn("[uBR] safe-self: BroadcastChannel failed", e);
      safe.sendToLogger = (type, ...args) => {
        const text = safe.toLogText(type, ...args);
        if (text === void 0) {
          return;
        }
        safe.log(`uBO ${text}`);
      };
    }
    return safe;
  }
  registerScriptlet(safeSelf, {
    name: "safe-self.fn"
  });

  // src/js/resources/run-at.js
  function runAt(fn2, when) {
    const intFromReadyState = (state) => {
      const targets = {
        "loading": 1,
        "asap": 1,
        "interactive": 2,
        "end": 2,
        "2": 2,
        "complete": 3,
        "idle": 3,
        "3": 3
      };
      const tokens = Array.isArray(state) ? state : [state];
      for (const token of tokens) {
        const prop = `${token}`;
        if (Object.hasOwn(targets, prop) === false) {
          continue;
        }
        return targets[prop];
      }
      return 0;
    };
    const runAt2 = intFromReadyState(when);
    if (intFromReadyState(document.readyState) >= runAt2) {
      fn2();
      return;
    }
    const onStateChange = () => {
      if (intFromReadyState(document.readyState) < runAt2) {
        return;
      }
      fn2();
      safe.removeEventListener.apply(document, args);
    };
    const safe = safeSelf();
    const args = ["readystatechange", onStateChange, { capture: true }];
    safe.addEventListener.apply(document, args);
  }
  registerScriptlet(runAt, {
    name: "run-at.fn",
    dependencies: [
      safeSelf
    ]
  });
  function runAtHtmlElementFn(fn2) {
    if (document.documentElement) {
      fn2();
      return;
    }
    const observer = new MutationObserver(() => {
      observer.disconnect();
      fn2();
    });
    observer.observe(document, { childList: true });
  }
  registerScriptlet(runAtHtmlElementFn, {
    name: "run-at-html-element.fn"
  });

  // src/js/resources/attribute.js
  function setAttrFn(trusted = false, logPrefix, selector = "", attr = "", value = "", options = {}) {
    if (selector === "") {
      return;
    }
    if (attr === "") {
      return;
    }
    const safe = safeSelf();
    const copyFrom = trusted === false && /^\[.+\]$/.test(value) ? value.slice(1, -1) : "";
    const extractValue = (elem) => copyFrom !== "" ? elem.getAttribute(copyFrom) || "" : value;
    const applySetAttr = () => {
      let elems;
      try {
        elems = document.querySelectorAll(selector);
      } catch (e) {
        console.warn("[uBR] attribute: querySelectorAll failed", selector, e);
        return false;
      }
      for (const elem of elems) {
        const before = elem.getAttribute(attr);
        const after = extractValue(elem);
        if (after === before) {
          continue;
        }
        if (after !== "" && /^on/i.test(attr)) {
          if (attr.toLowerCase() in elem) {
            continue;
          }
        }
        elem.setAttribute(attr, after);
        safe.uboLog(logPrefix, `${attr}="${after}"`);
      }
      return true;
    };
    let observer, timer;
    const onDomChanged = (mutations) => {
      if (timer !== void 0) {
        return;
      }
      let shouldWork = false;
      for (const mutation of mutations) {
        if (mutation.addedNodes.length === 0) {
          continue;
        }
        for (const node of mutation.addedNodes) {
          if (node.nodeType !== 1) {
            continue;
          }
          shouldWork = true;
          break;
        }
        if (shouldWork) {
          break;
        }
      }
      if (shouldWork === false) {
        return;
      }
      timer = self.requestAnimationFrame(() => {
        timer = void 0;
        applySetAttr();
      });
    };
    const start = () => {
      if (applySetAttr() === false) {
        return;
      }
      observer = new MutationObserver(onDomChanged);
      observer.observe(document.body, {
        subtree: true,
        childList: true
      });
    };
    runAt(() => {
      start();
    }, options.runAt || "idle");
  }
  registerScriptlet(setAttrFn, {
    name: "set-attr.fn",
    dependencies: [
      runAt,
      safeSelf
    ]
  });
  function setAttr(selector = "", attr = "", value = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("set-attr", selector, attr, value);
    const validValues = ["", "false", "true"];
    if (validValues.includes(value.toLowerCase()) === false) {
      if (/^\d+$/.test(value)) {
        const n = parseInt(value, 10);
        if (n >= 32768) {
          return;
        }
        value = `${n}`;
      } else if (/^\[.+\]$/.test(value) === false) {
        return;
      }
    }
    const options = safe.getExtraArgs(Array.from(arguments), 3);
    setAttrFn(false, logPrefix, selector, attr, value, options);
  }
  registerScriptlet(setAttr, {
    name: "set-attr.js",
    dependencies: [
      safeSelf,
      setAttrFn
    ],
    world: "ISOLATED"
  });
  function trustedSetAttr(selector = "", attr = "", value = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("trusted-set-attr", selector, attr, value);
    const options = safe.getExtraArgs(Array.from(arguments), 3);
    setAttrFn(true, logPrefix, selector, attr, value, options);
  }
  registerScriptlet(trustedSetAttr, {
    name: "trusted-set-attr.js",
    requiresTrust: true,
    dependencies: [
      safeSelf,
      setAttrFn
    ],
    world: "ISOLATED"
  });
  function removeAttr(rawToken = "", rawSelector = "", behavior = "") {
    if (typeof rawToken !== "string") {
      return;
    }
    if (rawToken === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("remove-attr", rawToken, rawSelector, behavior);
    const tokens = safe.String_split.call(rawToken, /\s*\|\s*/);
    const selector = tokens.map((a) => `${rawSelector}[${CSS.escape(a)}]`).join(",");
    if (safe.logLevel > 1) {
      safe.uboLog(logPrefix, `Target selector:
	${selector}`);
    }
    const asap = /\basap\b/.test(behavior);
    let timerId;
    const rmattrAsync = () => {
      if (timerId !== void 0) {
        return;
      }
      timerId = safe.onIdle(() => {
        timerId = void 0;
        rmattr();
      }, { timeout: 17 });
    };
    const rmattr = () => {
      if (timerId !== void 0) {
        safe.offIdle(timerId);
        timerId = void 0;
      }
      try {
        const nodes = document.querySelectorAll(selector);
        for (const node of nodes) {
          for (const attr of tokens) {
            if (node.hasAttribute(attr) === false) {
              continue;
            }
            node.removeAttribute(attr);
            safe.uboLog(logPrefix, `Removed attribute '${attr}'`);
          }
        }
      } catch (e) {
        console.warn("[uBR] attribute: walker traversal failed", e);
      }
    };
    const mutationHandler = (mutations) => {
      if (timerId !== void 0) {
        return;
      }
      let skip = true;
      for (let i = 0; i < mutations.length && skip; i++) {
        const { type, addedNodes, removedNodes } = mutations[i];
        if (type === "attributes") {
          skip = false;
        }
        for (let j2 = 0; j2 < addedNodes.length && skip; j2++) {
          if (addedNodes[j2].nodeType === 1) {
            skip = false;
            break;
          }
        }
        for (let j2 = 0; j2 < removedNodes.length && skip; j2++) {
          if (removedNodes[j2].nodeType === 1) {
            skip = false;
            break;
          }
        }
      }
      if (skip) {
        return;
      }
      asap ? rmattr() : rmattrAsync();
    };
    const start = () => {
      rmattr();
      if (/\bstay\b/.test(behavior) === false) {
        return;
      }
      const observer = new MutationObserver(mutationHandler);
      observer.observe(document, {
        attributes: true,
        attributeFilter: tokens,
        childList: true,
        subtree: true
      });
    };
    runAt(() => {
      start();
    }, safe.String_split.call(behavior, /\s+/));
  }
  registerScriptlet(removeAttr, {
    name: "remove-attr.js",
    aliases: [
      "ra.js"
    ],
    dependencies: [
      runAt,
      safeSelf
    ]
  });

  // src/js/resources/create-html.js
  function trustedCreateHTML(parentSelector, htmlStr = "", durationStr = "") {
    if (parentSelector === "") {
      return;
    }
    if (htmlStr === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("trusted-create-html", parentSelector, htmlStr, durationStr);
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 3);
    self.trustedCreateHTML = true;
    let ancestor = self.frameElement;
    while (ancestor) {
      const doc = ancestor.ownerDocument;
      if (doc === null) {
        break;
      }
      const win = doc.defaultView;
      if (win === null) {
        break;
      }
      if (win.trustedCreateHTML) {
        return;
      }
      ancestor = ancestor.frameElement;
    }
    const duration = parseInt(durationStr, 10);
    const domParser = new DOMParser();
    const externalDoc = domParser.parseFromString(htmlStr, "text/html");
    const toAppend = [];
    while (externalDoc.body.firstChild !== null) {
      toAppend.push(document.adoptNode(externalDoc.body.firstChild));
    }
    if (toAppend.length === 0) {
      return;
    }
    const toRemove = [];
    const remove = () => {
      for (const node of toRemove) {
        if (node.parentNode === null) {
          continue;
        }
        node.parentNode.removeChild(node);
      }
      safe.uboLog(logPrefix, "Node(s) removed");
    };
    const appendOne = (target, nodes) => {
      for (const node of nodes) {
        target.append(node);
        if (isNaN(duration)) {
          continue;
        }
        toRemove.push(node);
      }
    };
    const append = () => {
      const targets = document.querySelectorAll(parentSelector);
      if (targets.length === 0) {
        return false;
      }
      const limit = Math.min(targets.length, extraArgs.limit || 1) - 1;
      for (let i = 0; i < limit; i++) {
        appendOne(targets[i], toAppend.map((a) => a.cloneNode(true)));
      }
      appendOne(targets[limit], toAppend);
      safe.uboLog(logPrefix, "Node(s) appended");
      if (toRemove.length === 0) {
        return true;
      }
      setTimeout(remove, duration);
      return true;
    };
    const start = () => {
      if (append()) {
        return;
      }
      const observer = new MutationObserver(() => {
        if (append() === false) {
          return;
        }
        observer.disconnect();
      });
      const observerOptions = {
        childList: true,
        subtree: true
      };
      if (/[#.[]/.test(parentSelector)) {
        observerOptions.attributes = true;
        if (parentSelector.includes("[") === false) {
          observerOptions.attributeFilter = [];
          if (parentSelector.includes("#")) {
            observerOptions.attributeFilter.push("id");
          }
          if (parentSelector.includes(".")) {
            observerOptions.attributeFilter.push("class");
          }
        }
      }
      observer.observe(document, observerOptions);
    };
    runAt(start, extraArgs.runAt || "loading");
  }
  registerScriptlet(trustedCreateHTML, {
    name: "trusted-create-html.js",
    requiresTrust: true,
    dependencies: [
      runAt,
      safeSelf
    ],
    world: "ISOLATED"
  });

  // src/js/resources/href-sanitizer.js
  init_urlskip();
  registerScriptlet(urlSkip, {
    name: "urlskip.fn"
  });
  function hrefSanitizer(selector = "", source = "") {
    if (typeof selector !== "string") {
      return;
    }
    if (selector === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("href-sanitizer", selector, source);
    if (source === "") {
      source = "text";
    }
    const sanitizeCopycats = (href, text) => {
      let elems = [];
      try {
        elems = document.querySelectorAll(`a[href="${href}"`);
      } catch (e) {
        console.warn("[uBR] href-sanitizer: sanitizeCopycats querySelectorAll failed", e);
      }
      for (const elem of elems) {
        elem.setAttribute("href", text);
      }
      return elems.length;
    };
    const validateURL = (text) => {
      if (typeof text !== "string") {
        return "";
      }
      if (text === "") {
        return "";
      }
      if (/[\x00-\x20\x7f]/.test(text)) {
        return "";
      }
      try {
        const url = new URL(text, document.location);
        return url.href;
      } catch (e) {
        console.warn("[uBR] href-sanitizer: validateURL failed", e);
      }
      return "";
    };
    const extractURL = (elem, source2) => {
      if (/^\[.*\]$/.test(source2)) {
        return elem.getAttribute(source2.slice(1, -1).trim()) || "";
      }
      if (source2 === "text") {
        return elem.textContent.replace(/^[^\x21-\x7e]+/, "").replace(/[^\x21-\x7e]+$/, "");
      }
      const steps = source2.replace(/(\S)\?/g, "\\1 ?").split(/\s+/);
      const url = urlSkip(elem.href, false, steps);
      if (url === void 0) {
        return;
      }
      return url.replace(/ /g, "%20");
    };
    const sanitize = () => {
      let elems = [];
      try {
        elems = document.querySelectorAll(selector);
      } catch (e) {
        console.warn("[uBR] href-sanitizer: sanitize querySelectorAll failed", e);
        return false;
      }
      for (const elem of elems) {
        if (elem.localName !== "a") {
          continue;
        }
        if (elem.hasAttribute("href") === false) {
          continue;
        }
        const href = elem.getAttribute("href");
        const text = extractURL(elem, source);
        const hrefAfter = validateURL(text);
        if (hrefAfter === "") {
          continue;
        }
        if (hrefAfter === href) {
          continue;
        }
        elem.setAttribute("href", hrefAfter);
        const count = sanitizeCopycats(href, hrefAfter);
        safe.uboLog(logPrefix, `Sanitized ${count + 1} links to
${hrefAfter}`);
      }
      return true;
    };
    let observer, timer;
    const onDomChanged = (mutations) => {
      if (timer !== void 0) {
        return;
      }
      let shouldSanitize = false;
      for (const mutation of mutations) {
        if (mutation.addedNodes.length === 0) {
          continue;
        }
        for (const node of mutation.addedNodes) {
          if (node.nodeType !== 1) {
            continue;
          }
          shouldSanitize = true;
          break;
        }
        if (shouldSanitize) {
          break;
        }
      }
      if (shouldSanitize === false) {
        return;
      }
      timer = safe.onIdle(() => {
        timer = void 0;
        sanitize();
      });
    };
    const start = () => {
      if (sanitize() === false) {
        return;
      }
      observer = new MutationObserver(onDomChanged);
      observer.observe(document.body, {
        subtree: true,
        childList: true
      });
    };
    runAt(() => {
      start();
    }, "interactive");
  }
  registerScriptlet(hrefSanitizer, {
    name: "href-sanitizer.js",
    world: "ISOLATED",
    aliases: [
      "urlskip.js"
    ],
    dependencies: [
      runAt,
      safeSelf,
      urlSkip
    ]
  });

  // src/js/resources/utils.js
  function getRandomTokenFn() {
    const safe = safeSelf();
    return safe.String_fromCharCode(Date.now() % 26 + 97) + safe.Math_floor(safe.Math_random() * 982451653 + 982451653).toString(36);
  }
  registerScriptlet(getRandomTokenFn, {
    name: "get-random-token.fn",
    dependencies: [
      safeSelf
    ]
  });
  function getExceptionTokenFn() {
    const token = getRandomTokenFn();
    const oe = self.onerror;
    self.onerror = function(msg, ...args) {
      if (typeof msg === "string" && msg.includes(token)) {
        return true;
      }
      if (oe instanceof Function) {
        return oe.call(this, msg, ...args);
      }
    }.bind();
    return token;
  }
  registerScriptlet(getExceptionTokenFn, {
    name: "get-exception-token.fn",
    dependencies: [
      getRandomTokenFn
    ]
  });
  function collateFetchArgumentsFn(resource, options) {
    const safe = safeSelf();
    const props = [
      "body",
      "cache",
      "credentials",
      "duplex",
      "headers",
      "integrity",
      "keepalive",
      "method",
      "mode",
      "priority",
      "redirect",
      "referrer",
      "referrerPolicy",
      "url"
    ];
    const out = {};
    if (collateFetchArgumentsFn.collateKnownProps === void 0) {
      collateFetchArgumentsFn.collateKnownProps = (src, out2) => {
        for (const prop of props) {
          if (src[prop] === void 0) {
            continue;
          }
          out2[prop] = src[prop];
        }
      };
    }
    if (typeof resource !== "object" || safe.Object_toString.call(resource) !== "[object Request]") {
      out.url = `${resource}`;
    } else {
      let clone;
      try {
        clone = safe.Request_clone.call(resource);
      } catch (e) {
        console.warn("[uBR] utils: Request_clone failed", e);
      }
      collateFetchArgumentsFn.collateKnownProps(clone || resource, out);
    }
    if (typeof options === "object" && options !== null) {
      collateFetchArgumentsFn.collateKnownProps(options, out);
    }
    return out;
  }
  registerScriptlet(collateFetchArgumentsFn, {
    name: "collate-fetch-arguments.fn",
    dependencies: [
      safeSelf
    ]
  });
  function parsePropertiesToMatchFn(propsToMatch, implicit = "") {
    const safe = safeSelf();
    const needles = /* @__PURE__ */ new Map();
    if (propsToMatch === void 0 || propsToMatch === "") {
      return needles;
    }
    const options = { canNegate: true };
    for (const needle of safe.String_split.call(propsToMatch, /\s+/)) {
      let [prop, pattern] = safe.String_split.call(needle, ":");
      if (prop === "") {
        continue;
      }
      if (pattern !== void 0 && /[^$\w -]/.test(prop)) {
        prop = `${prop}:${pattern}`;
        pattern = void 0;
      }
      if (pattern !== void 0) {
        needles.set(prop, safe.initPattern(pattern, options));
      } else if (implicit !== "") {
        needles.set(implicit, safe.initPattern(prop, options));
      }
    }
    return needles;
  }
  registerScriptlet(parsePropertiesToMatchFn, {
    name: "parse-properties-to-match.fn",
    dependencies: [
      safeSelf
    ]
  });
  function matchObjectPropertiesFn(propNeedles, ...objs) {
    const safe = safeSelf();
    const matched = [];
    for (const obj of objs) {
      if (obj instanceof Object === false) {
        continue;
      }
      for (const [prop, details] of propNeedles) {
        let value = obj[prop];
        if (value === void 0) {
          continue;
        }
        if (typeof value !== "string") {
          try {
            value = safe.JSON_stringify(value);
          } catch (e) {
            console.warn("[uBR] utils: matchObjectPropertiesFn JSON_stringify failed", e);
          }
          if (typeof value !== "string") {
            continue;
          }
        }
        if (safe.testPattern(details, value) === false) {
          return;
        }
        matched.push(`${prop}: ${value}`);
      }
    }
    return matched;
  }
  registerScriptlet(matchObjectPropertiesFn, {
    name: "match-object-properties.fn",
    dependencies: [
      safeSelf
    ]
  });
  function generateContentFn(trusted, directive) {
    const safe = safeSelf();
    const randomize = (len) => {
      const chunks = [];
      let textSize = 0;
      do {
        const s = safe.Math_random().toString(36).slice(2);
        chunks.push(s);
        textSize += s.length;
      } while (textSize < len);
      return chunks.join(" ").slice(0, len);
    };
    if (directive === "true") {
      return randomize(10);
    }
    if (directive === "emptyObj") {
      return "{}";
    }
    if (directive === "emptyArr") {
      return "[]";
    }
    if (directive === "emptyStr") {
      return "";
    }
    if (directive.startsWith("length:")) {
      const match = /^length:(\d+)(?:-(\d+))?$/.exec(directive);
      if (match === null) {
        return "";
      }
      const min = parseInt(match[1], 10);
      const extent = safe.Math_max(parseInt(match[2], 10) || 0, min) - min;
      const len = safe.Math_min(min + extent * safe.Math_random(), 5e5);
      return randomize(len | 0);
    }
    if (directive.startsWith("war:")) {
      if (scriptletGlobals.warOrigin === void 0) {
        return "";
      }
      return new Promise((resolve) => {
        const warOrigin = scriptletGlobals.warOrigin;
        const warName = directive.slice(4);
        const fullpath = [warOrigin, "/", warName];
        const warSecret2 = scriptletGlobals.warSecret;
        if (warSecret2 !== void 0) {
          fullpath.push("?secret=", warSecret2);
        }
        const warXHR = new safe.XMLHttpRequest();
        warXHR.responseType = "text";
        warXHR.onloadend = (ev) => {
          resolve(ev.target.responseText || "");
        };
        warXHR.open("GET", fullpath.join(""));
        warXHR.send();
      }).catch((e) => {
        console.warn("[uBR] resources/utils: WAR fetch failed", e);
        return "";
      });
    }
    if (directive.startsWith("join:")) {
      const parts = directive.slice(7).split(directive.slice(5, 7)).map((a) => generateContentFn(trusted, a));
      return parts.some((a) => a instanceof Promise) ? Promise.all(parts).then((parts2) => parts2.join("")) : parts.join("");
    }
    if (trusted) {
      return directive;
    }
    return "";
  }
  registerScriptlet(generateContentFn, {
    name: "generate-content.fn",
    dependencies: [
      safeSelf
    ]
  });

  // src/js/resources/shared.js
  init_arglist_parser();
  init_jsonpath();
  var ArglistParser2 = ArglistParser;
  registerScriptlet(ArglistParser2, {
    name: "arglist-parser.fn"
  });
  var JSONPath2 = JSONPath;
  registerScriptlet(JSONPath2, {
    name: "jsonpath.fn"
  });

  // src/js/resources/proxy-apply.js
  function proxyApplyFn(target = "", handler = "") {
    let context = globalThis;
    let prop = target;
    for (; ; ) {
      const pos = prop.indexOf(".");
      if (pos === -1) {
        break;
      }
      context = context[prop.slice(0, pos)];
      if (context instanceof Object === false) {
        return;
      }
      prop = prop.slice(pos + 1);
    }
    const fn2 = context[prop];
    if (typeof fn2 !== "function") {
      return;
    }
    if (proxyApplyFn.CtorContext === void 0) {
      proxyApplyFn.ctorContexts = [];
      proxyApplyFn.CtorContext = class {
        constructor(...args) {
          this.init(...args);
        }
        init(callFn, callArgs) {
          this.callFn = callFn;
          this.callArgs = callArgs;
          return this;
        }
        reflect() {
          const r = Reflect.construct(this.callFn, this.callArgs);
          this.callFn = this.callArgs = this.private = void 0;
          proxyApplyFn.ctorContexts.push(this);
          return r;
        }
        static factory(...args) {
          return proxyApplyFn.ctorContexts.length !== 0 ? proxyApplyFn.ctorContexts.pop().init(...args) : new proxyApplyFn.CtorContext(...args);
        }
      };
      proxyApplyFn.applyContexts = [];
      proxyApplyFn.ApplyContext = class {
        constructor(...args) {
          this.init(...args);
        }
        init(callFn, thisArg, callArgs) {
          this.callFn = callFn;
          this.thisArg = thisArg;
          this.callArgs = callArgs;
          return this;
        }
        reflect() {
          const r = Reflect.apply(this.callFn, this.thisArg, this.callArgs);
          this.callFn = this.thisArg = this.callArgs = this.private = void 0;
          proxyApplyFn.applyContexts.push(this);
          return r;
        }
        static factory(...args) {
          return proxyApplyFn.applyContexts.length !== 0 ? proxyApplyFn.applyContexts.pop().init(...args) : new proxyApplyFn.ApplyContext(...args);
        }
      };
      proxyApplyFn.isCtor = /* @__PURE__ */ new Map();
      proxyApplyFn.proxies = /* @__PURE__ */ new WeakMap();
      proxyApplyFn.nativeToString = Function.prototype.toString;
      const proxiedToString = new Proxy(Function.prototype.toString, {
        apply(target2, thisArg) {
          let proxied = thisArg;
          for (; ; ) {
            const fn3 = proxyApplyFn.proxies.get(proxied);
            if (fn3 === void 0) {
              break;
            }
            proxied = fn3;
          }
          return proxyApplyFn.nativeToString.call(proxied);
        }
      });
      proxyApplyFn.proxies.set(proxiedToString, proxyApplyFn.nativeToString);
      Function.prototype.toString = proxiedToString;
    }
    if (proxyApplyFn.isCtor.has(target) === false) {
      proxyApplyFn.isCtor.set(target, fn2.prototype?.constructor === fn2);
    }
    const proxyDetails = {
      apply(target2, thisArg, args) {
        return handler(proxyApplyFn.ApplyContext.factory(target2, thisArg, args));
      }
    };
    if (proxyApplyFn.isCtor.get(target)) {
      proxyDetails.construct = function(target2, args) {
        return handler(proxyApplyFn.CtorContext.factory(target2, args));
      };
    }
    const proxiedTarget = new Proxy(fn2, proxyDetails);
    proxyApplyFn.proxies.set(proxiedTarget, fn2);
    context[prop] = proxiedTarget;
  }
  registerScriptlet(proxyApplyFn, {
    name: "proxy-apply.fn"
  });

  // src/js/resources/json-edit.js
  function editOutboundObjectFn(trusted = false, propChain = "", jsonq = "") {
    if (propChain === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix(
      `${trusted ? "trusted-" : ""}edit-outbound-object`,
      propChain,
      jsonq
    );
    const jsonp = JSONPath2.create(jsonq);
    if (jsonp.valid === false || jsonp.value !== void 0 && trusted !== true) {
      return safe.uboLog(logPrefix, "Bad JSONPath query");
    }
    proxyApplyFn(propChain, (context) => {
      const obj = context.reflect();
      const objAfter = jsonp.apply(obj);
      if (objAfter === void 0) {
        return obj;
      }
      safe.uboLog(logPrefix, "Edited");
      if (safe.logLevel > 1) {
        safe.uboLog(logPrefix, `After edit:
${safe.JSON_stringify(objAfter, null, 2)}`);
      }
      return objAfter;
    });
  }
  registerScriptlet(editOutboundObjectFn, {
    name: "edit-outbound-object.fn",
    dependencies: [
      JSONPath2,
      proxyApplyFn,
      safeSelf
    ]
  });
  function editOutboundObject(propChain = "", jsonq = "") {
    editOutboundObjectFn(false, propChain, jsonq);
  }
  registerScriptlet(editOutboundObject, {
    name: "edit-outbound-object.js",
    dependencies: [
      editOutboundObjectFn
    ]
  });
  function trustedEditOutboundObject(propChain = "", jsonq = "") {
    editOutboundObjectFn(true, propChain, jsonq);
  }
  registerScriptlet(trustedEditOutboundObject, {
    name: "trusted-edit-outbound-object.js",
    requiresTrust: true,
    dependencies: [
      editOutboundObjectFn
    ]
  });
  function jsonEdit(jsonq = "") {
    editOutboundObjectFn(false, "JSON.parse", jsonq);
  }
  registerScriptlet(jsonEdit, {
    name: "json-edit.js",
    dependencies: [
      editOutboundObjectFn
    ]
  });
  function trustedJsonEdit(jsonq = "") {
    editOutboundObjectFn(true, "JSON.parse", jsonq);
  }
  registerScriptlet(trustedJsonEdit, {
    name: "trusted-json-edit.js",
    requiresTrust: true,
    dependencies: [
      editOutboundObjectFn
    ]
  });
  function editInboundObjectFn(trusted = false, propChain = "", argPosRaw = "", jsonq = "") {
    if (propChain === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix(
      `${trusted ? "trusted-" : ""}edit-inbound-object`,
      propChain,
      jsonq
    );
    const jsonp = JSONPath2.create(jsonq);
    if (jsonp.valid === false || jsonp.value !== void 0 && trusted !== true) {
      return safe.uboLog(logPrefix, "Bad JSONPath query");
    }
    const argPos = parseInt(argPosRaw, 10);
    if (isNaN(argPos)) {
      return;
    }
    const getArgPos = (args) => {
      if (Array.isArray(args) === false) {
        return;
      }
      if (argPos >= 0) {
        if (args.length <= argPos) {
          return;
        }
        return argPos;
      }
      if (args.length < -argPos) {
        return;
      }
      return args.length + argPos;
    };
    const editObj = (obj) => {
      let clone;
      try {
        clone = safe.JSON_parse(safe.JSON_stringify(obj));
      } catch (e) {
        console.warn("[uBR] json-edit: JSON clone failed", e);
      }
      if (typeof clone !== "object" || clone === null) {
        return;
      }
      const objAfter = jsonp.apply(clone);
      if (objAfter === void 0) {
        return;
      }
      safe.uboLog(logPrefix, "Edited");
      if (safe.logLevel > 1) {
        safe.uboLog(logPrefix, `After edit:
${safe.JSON_stringify(objAfter, null, 2)}`);
      }
      return objAfter;
    };
    proxyApplyFn(propChain, (context) => {
      const i = getArgPos(context.callArgs);
      if (i !== void 0) {
        const obj = editObj(context.callArgs[i]);
        if (obj) {
          context.callArgs[i] = obj;
        }
      }
      return context.reflect();
    });
  }
  registerScriptlet(editInboundObjectFn, {
    name: "edit-inbound-object.fn",
    dependencies: [
      JSONPath2,
      proxyApplyFn,
      safeSelf
    ]
  });
  function editInboundObject(propChain = "", argPos = "", jsonq = "") {
    editInboundObjectFn(false, propChain, argPos, jsonq);
  }
  registerScriptlet(editInboundObject, {
    name: "edit-inbound-object.js",
    dependencies: [
      editInboundObjectFn
    ]
  });
  function trustedEditInboundObject(propChain = "", argPos = "", jsonq = "") {
    editInboundObjectFn(true, propChain, argPos, jsonq);
  }
  registerScriptlet(trustedEditInboundObject, {
    name: "trusted-edit-inbound-object.js",
    requiresTrust: true,
    dependencies: [
      editInboundObjectFn
    ]
  });
  function jsonEditXhrResponseFn(trusted, jsonq = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix(
      `${trusted ? "trusted-" : ""}json-edit-xhr-response`,
      jsonq
    );
    const xhrInstances = /* @__PURE__ */ new WeakMap();
    const jsonp = JSONPath2.create(jsonq);
    if (jsonp.valid === false || jsonp.value !== void 0 && trusted !== true) {
      return safe.uboLog(logPrefix, "Bad JSONPath query");
    }
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 2);
    const propNeedles = parsePropertiesToMatchFn(extraArgs.propsToMatch, "url");
    self.XMLHttpRequest = class extends self.XMLHttpRequest {
      open(method, url, ...args) {
        const xhrDetails = { method, url };
        const matched = propNeedles.size === 0 || matchObjectPropertiesFn(propNeedles, xhrDetails);
        if (matched) {
          if (safe.logLevel > 1 && Array.isArray(matched)) {
            safe.uboLog(logPrefix, `Matched "propsToMatch":
	${matched.join("\n	")}`);
          }
          xhrInstances.set(this, xhrDetails);
        }
        return super.open(method, url, ...args);
      }
      get response() {
        const innerResponse = super.response;
        const xhrDetails = xhrInstances.get(this);
        if (xhrDetails === void 0) {
          return innerResponse;
        }
        const responseLength = typeof innerResponse === "string" ? innerResponse.length : void 0;
        if (xhrDetails.lastResponseLength !== responseLength) {
          xhrDetails.response = void 0;
          xhrDetails.lastResponseLength = responseLength;
        }
        if (xhrDetails.response !== void 0) {
          return xhrDetails.response;
        }
        let obj;
        if (typeof innerResponse === "object") {
          obj = innerResponse;
        } else if (typeof innerResponse === "string") {
          try {
            obj = safe.JSON_parse(innerResponse);
          } catch (e) {
            console.warn("[uBR] json-edit: JSON_parse innerResponse failed", e);
          }
        }
        if (typeof obj !== "object" || obj === null) {
          return xhrDetails.response = innerResponse;
        }
        const objAfter = jsonp.apply(obj);
        if (objAfter === void 0) {
          return xhrDetails.response = innerResponse;
        }
        safe.uboLog(logPrefix, "Edited");
        const outerResponse = typeof innerResponse === "string" ? JSONPath2.toJSON(objAfter, safe.JSON_stringify) : objAfter;
        return xhrDetails.response = outerResponse;
      }
      get responseText() {
        const response = this.response;
        return typeof response !== "string" ? super.responseText : response;
      }
    };
  }
  registerScriptlet(jsonEditXhrResponseFn, {
    name: "json-edit-xhr-response.fn",
    dependencies: [
      JSONPath2,
      matchObjectPropertiesFn,
      parsePropertiesToMatchFn,
      safeSelf
    ]
  });
  function jsonEditXhrResponse(jsonq = "", ...args) {
    jsonEditXhrResponseFn(false, jsonq, ...args);
  }
  registerScriptlet(jsonEditXhrResponse, {
    name: "json-edit-xhr-response.js",
    dependencies: [
      jsonEditXhrResponseFn
    ]
  });
  function trustedJsonEditXhrResponse(jsonq = "", ...args) {
    jsonEditXhrResponseFn(true, jsonq, ...args);
  }
  registerScriptlet(trustedJsonEditXhrResponse, {
    name: "trusted-json-edit-xhr-response.js",
    requiresTrust: true,
    dependencies: [
      jsonEditXhrResponseFn
    ]
  });
  function jsonEditXhrRequestFn(trusted, jsonq = "") {
    var _instances, filterBody_fn, _a2;
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix(
      `${trusted ? "trusted-" : ""}json-edit-xhr-request`,
      jsonq
    );
    const xhrInstances = /* @__PURE__ */ new WeakMap();
    const jsonp = JSONPath2.create(jsonq);
    if (jsonp.valid === false || jsonp.value !== void 0 && trusted !== true) {
      return safe.uboLog(logPrefix, "Bad JSONPath query");
    }
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 2);
    const propNeedles = parsePropertiesToMatchFn(extraArgs.propsToMatch, "url");
    self.XMLHttpRequest = (_a2 = class extends self.XMLHttpRequest {
      constructor() {
        super(...arguments);
        __privateAdd(this, _instances);
      }
      open(method, url, ...args) {
        const xhrDetails = { method, url };
        const matched = propNeedles.size === 0 || matchObjectPropertiesFn(propNeedles, xhrDetails);
        if (matched) {
          if (safe.logLevel > 1 && Array.isArray(matched)) {
            safe.uboLog(logPrefix, `Matched "propsToMatch":
	${matched.join("\n	")}`);
          }
          xhrInstances.set(this, xhrDetails);
        }
        return super.open(method, url, ...args);
      }
      send(body) {
        const xhrDetails = xhrInstances.get(this);
        if (xhrDetails) {
          body = __privateMethod(this, _instances, filterBody_fn).call(this, body) || body;
        }
        super.send(body);
      }
    }, _instances = new WeakSet(), filterBody_fn = function(body) {
      if (typeof body !== "string") {
        return;
      }
      let data;
      try {
        data = safe.JSON_parse(body);
      } catch (e) {
        console.warn("[uBR] json-edit: JSON_parse body failed", e);
      }
      if (data instanceof Object === false) {
        return;
      }
      const objAfter = jsonp.apply(data);
      if (objAfter === void 0) {
        return;
      }
      body = safe.JSON_stringify(objAfter);
      safe.uboLog(logPrefix, "Edited");
      if (safe.logLevel > 1) {
        safe.uboLog(logPrefix, `After edit:
${body}`);
      }
      return body;
    }, _a2);
  }
  registerScriptlet(jsonEditXhrRequestFn, {
    name: "json-edit-xhr-request.fn",
    dependencies: [
      JSONPath2,
      matchObjectPropertiesFn,
      parsePropertiesToMatchFn,
      safeSelf
    ]
  });
  function jsonEditXhrRequest(jsonq = "", ...args) {
    jsonEditXhrRequestFn(false, jsonq, ...args);
  }
  registerScriptlet(jsonEditXhrRequest, {
    name: "json-edit-xhr-request.js",
    dependencies: [
      jsonEditXhrRequestFn
    ]
  });
  function trustedJsonEditXhrRequest(jsonq = "", ...args) {
    jsonEditXhrRequestFn(true, jsonq, ...args);
  }
  registerScriptlet(trustedJsonEditXhrRequest, {
    name: "trusted-json-edit-xhr-request.js",
    requiresTrust: true,
    dependencies: [
      jsonEditXhrRequestFn
    ]
  });
  function jsonEditFetchResponseFn(trusted, jsonq = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix(
      `${trusted ? "trusted-" : ""}json-edit-fetch-response`,
      jsonq
    );
    const jsonp = JSONPath2.create(jsonq);
    if (jsonp.valid === false || jsonp.value !== void 0 && trusted !== true) {
      return safe.uboLog(logPrefix, "Bad JSONPath query");
    }
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 2);
    const propNeedles = parsePropertiesToMatchFn(extraArgs.propsToMatch, "url");
    proxyApplyFn("fetch", (context) => {
      const args = context.callArgs;
      const fetchPromise = context.reflect();
      if (propNeedles.size !== 0) {
        const props = collateFetchArgumentsFn(...args);
        const matched = matchObjectPropertiesFn(propNeedles, props);
        if (matched === void 0) {
          return fetchPromise;
        }
        if (safe.logLevel > 1) {
          safe.uboLog(logPrefix, `Matched "propsToMatch":
	${matched.join("\n	")}`);
        }
      }
      return fetchPromise.then((responseBefore) => {
        const response = responseBefore.clone();
        return response.json().then((obj) => {
          if (typeof obj !== "object") {
            return responseBefore;
          }
          const objAfter = jsonp.apply(obj);
          if (objAfter === void 0) {
            return responseBefore;
          }
          safe.uboLog(logPrefix, "Edited");
          const responseAfter = Response.json(objAfter, {
            status: responseBefore.status,
            statusText: responseBefore.statusText,
            headers: responseBefore.headers
          });
          Object.defineProperties(responseAfter, {
            ok: { value: responseBefore.ok },
            redirected: { value: responseBefore.redirected },
            type: { value: responseBefore.type },
            url: { value: responseBefore.url }
          });
          return responseAfter;
        }).catch((reason) => {
          safe.uboErr(logPrefix, "Error:", reason);
          return responseBefore;
        });
      }).catch((reason) => {
        safe.uboErr(logPrefix, "Error:", reason);
        return fetchPromise;
      });
    });
  }
  registerScriptlet(jsonEditFetchResponseFn, {
    name: "json-edit-fetch-response.fn",
    dependencies: [
      collateFetchArgumentsFn,
      JSONPath2,
      matchObjectPropertiesFn,
      parsePropertiesToMatchFn,
      proxyApplyFn,
      safeSelf
    ]
  });
  function jsonEditFetchResponse(jsonq = "", ...args) {
    jsonEditFetchResponseFn(false, jsonq, ...args);
  }
  registerScriptlet(jsonEditFetchResponse, {
    name: "json-edit-fetch-response.js",
    dependencies: [
      jsonEditFetchResponseFn
    ]
  });
  function trustedJsonEditFetchResponse(jsonq = "", ...args) {
    jsonEditFetchResponseFn(true, jsonq, ...args);
  }
  registerScriptlet(trustedJsonEditFetchResponse, {
    name: "trusted-json-edit-fetch-response.js",
    requiresTrust: true,
    dependencies: [
      jsonEditFetchResponseFn
    ]
  });
  function jsonEditFetchRequestFn(trusted, jsonq = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix(
      `${trusted ? "trusted-" : ""}json-edit-fetch-request`,
      jsonq
    );
    const jsonp = JSONPath2.create(jsonq);
    if (jsonp.valid === false || jsonp.value !== void 0 && trusted !== true) {
      return safe.uboLog(logPrefix, "Bad JSONPath query");
    }
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 2);
    const propNeedles = parsePropertiesToMatchFn(extraArgs.propsToMatch, "url");
    const filterBody = (body) => {
      if (typeof body !== "string") {
        return;
      }
      let data;
      try {
        data = safe.JSON_parse(body);
      } catch (e) {
        console.warn("[uBR] json-edit: filterBody JSON_parse failed", e);
      }
      if (data instanceof Object === false) {
        return;
      }
      const objAfter = jsonp.apply(data);
      if (objAfter === void 0) {
        return;
      }
      return safe.JSON_stringify(objAfter);
    };
    const proxyHandler = (context) => {
      const args = context.callArgs;
      const [resource, options] = args;
      const bodyBefore = options?.body;
      if (Boolean(bodyBefore) === false) {
        return context.reflect();
      }
      const bodyAfter = filterBody(bodyBefore);
      if (bodyAfter === void 0 || bodyAfter === bodyBefore) {
        return context.reflect();
      }
      if (propNeedles.size !== 0) {
        const props = collateFetchArgumentsFn(resource, options);
        const matched = matchObjectPropertiesFn(propNeedles, props);
        if (matched === void 0) {
          return context.reflect();
        }
        if (safe.logLevel > 1) {
          safe.uboLog(logPrefix, `Matched "propsToMatch":
	${matched.join("\n	")}`);
        }
      }
      safe.uboLog(logPrefix, "Edited");
      if (safe.logLevel > 1) {
        safe.uboLog(logPrefix, `After edit:
${bodyAfter}`);
      }
      options.body = bodyAfter;
      return context.reflect();
    };
    proxyApplyFn("fetch", proxyHandler);
    proxyApplyFn("Request", proxyHandler);
  }
  registerScriptlet(jsonEditFetchRequestFn, {
    name: "json-edit-fetch-request.fn",
    dependencies: [
      collateFetchArgumentsFn,
      JSONPath2,
      matchObjectPropertiesFn,
      parsePropertiesToMatchFn,
      proxyApplyFn,
      safeSelf
    ]
  });
  function jsonEditFetchRequest(jsonq = "", ...args) {
    jsonEditFetchRequestFn(false, jsonq, ...args);
  }
  registerScriptlet(jsonEditFetchRequest, {
    name: "json-edit-fetch-request.js",
    dependencies: [
      jsonEditFetchRequestFn
    ]
  });
  function trustedJsonEditFetchRequest(jsonq = "", ...args) {
    jsonEditFetchRequestFn(true, jsonq, ...args);
  }
  registerScriptlet(trustedJsonEditFetchRequest, {
    name: "trusted-json-edit-fetch-request.js",
    requiresTrust: true,
    dependencies: [
      jsonEditFetchRequestFn
    ]
  });
  function jsonlEditFn(jsonp, text = "") {
    const safe = safeSelf();
    const lineSeparator = /\r?\n/.exec(text)?.[0] || "\n";
    const linesBefore = text.split("\n");
    const linesAfter = [];
    for (const lineBefore of linesBefore) {
      let obj;
      try {
        obj = safe.JSON_parse(lineBefore);
      } catch (e) {
        console.warn("[uBR] json-edit: jsonlEditFn JSON_parse failed", e);
      }
      if (typeof obj !== "object" || obj === null) {
        linesAfter.push(lineBefore);
        continue;
      }
      const objAfter = jsonp.apply(obj);
      if (objAfter === void 0) {
        linesAfter.push(lineBefore);
        continue;
      }
      const lineAfter = safe.JSON_stringify(objAfter);
      linesAfter.push(lineAfter);
    }
    return linesAfter.join(lineSeparator);
  }
  registerScriptlet(jsonlEditFn, {
    name: "jsonl-edit.fn",
    dependencies: [
      JSONPath2,
      safeSelf
    ]
  });
  function jsonlEditXhrResponseFn(trusted, jsonq = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix(
      `${trusted ? "trusted-" : ""}jsonl-edit-xhr-response`,
      jsonq
    );
    const xhrInstances = /* @__PURE__ */ new WeakMap();
    const jsonp = JSONPath2.create(jsonq);
    if (jsonp.valid === false || jsonp.value !== void 0 && trusted !== true) {
      return safe.uboLog(logPrefix, "Bad JSONPath query");
    }
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 2);
    const propNeedles = parsePropertiesToMatchFn(extraArgs.propsToMatch, "url");
    self.XMLHttpRequest = class extends self.XMLHttpRequest {
      open(method, url, ...args) {
        const xhrDetails = { method, url };
        const matched = propNeedles.size === 0 || matchObjectPropertiesFn(propNeedles, xhrDetails);
        if (matched) {
          if (safe.logLevel > 1 && Array.isArray(matched)) {
            safe.uboLog(logPrefix, `Matched "propsToMatch":
	${matched.join("\n	")}`);
          }
          xhrInstances.set(this, xhrDetails);
        }
        return super.open(method, url, ...args);
      }
      get response() {
        const innerResponse = super.response;
        const xhrDetails = xhrInstances.get(this);
        if (xhrDetails === void 0) {
          return innerResponse;
        }
        const responseLength = typeof innerResponse === "string" ? innerResponse.length : void 0;
        if (xhrDetails.lastResponseLength !== responseLength) {
          xhrDetails.response = void 0;
          xhrDetails.lastResponseLength = responseLength;
        }
        if (xhrDetails.response !== void 0) {
          return xhrDetails.response;
        }
        if (typeof innerResponse !== "string") {
          return xhrDetails.response = innerResponse;
        }
        const outerResponse = jsonlEditFn(jsonp, innerResponse);
        if (outerResponse !== innerResponse) {
          safe.uboLog(logPrefix, "Pruned");
        }
        return xhrDetails.response = outerResponse;
      }
      get responseText() {
        const response = this.response;
        return typeof response !== "string" ? super.responseText : response;
      }
    };
  }
  registerScriptlet(jsonlEditXhrResponseFn, {
    name: "jsonl-edit-xhr-response.fn",
    dependencies: [
      JSONPath2,
      jsonlEditFn,
      matchObjectPropertiesFn,
      parsePropertiesToMatchFn,
      safeSelf
    ]
  });
  function jsonlEditXhrResponse(jsonq = "", ...args) {
    jsonlEditXhrResponseFn(false, jsonq, ...args);
  }
  registerScriptlet(jsonlEditXhrResponse, {
    name: "jsonl-edit-xhr-response.js",
    dependencies: [
      jsonlEditXhrResponseFn
    ]
  });
  function trustedJsonlEditXhrResponse(jsonq = "", ...args) {
    jsonlEditXhrResponseFn(true, jsonq, ...args);
  }
  registerScriptlet(trustedJsonlEditXhrResponse, {
    name: "trusted-jsonl-edit-xhr-response.js",
    requiresTrust: true,
    dependencies: [
      jsonlEditXhrResponseFn
    ]
  });
  function jsonlEditFetchResponseFn(trusted, jsonq = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix(
      `${trusted ? "trusted-" : ""}jsonl-edit-fetch-response`,
      jsonq
    );
    const jsonp = JSONPath2.create(jsonq);
    if (jsonp.valid === false || jsonp.value !== void 0 && trusted !== true) {
      return safe.uboLog(logPrefix, "Bad JSONPath query");
    }
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 2);
    const propNeedles = parsePropertiesToMatchFn(extraArgs.propsToMatch, "url");
    const logall = jsonq === "";
    proxyApplyFn("fetch", (context) => {
      const args = context.callArgs;
      const fetchPromise = context.reflect();
      if (propNeedles.size !== 0) {
        const props = collateFetchArgumentsFn(...args);
        const matched = matchObjectPropertiesFn(propNeedles, props);
        if (matched === void 0) {
          return fetchPromise;
        }
        if (safe.logLevel > 1) {
          safe.uboLog(logPrefix, `Matched "propsToMatch":
	${matched.join("\n	")}`);
        }
      }
      return fetchPromise.then((responseBefore) => {
        const response = responseBefore.clone();
        return response.text().then((textBefore) => {
          if (typeof textBefore !== "string") {
            return textBefore;
          }
          if (logall) {
            safe.uboLog(logPrefix, textBefore);
            return responseBefore;
          }
          const textAfter = jsonlEditFn(jsonp, textBefore);
          if (textAfter === textBefore) {
            return responseBefore;
          }
          safe.uboLog(logPrefix, "Pruned");
          const responseAfter = new Response(textAfter, {
            status: responseBefore.status,
            statusText: responseBefore.statusText,
            headers: responseBefore.headers
          });
          Object.defineProperties(responseAfter, {
            ok: { value: responseBefore.ok },
            redirected: { value: responseBefore.redirected },
            type: { value: responseBefore.type },
            url: { value: responseBefore.url }
          });
          return responseAfter;
        }).catch((reason) => {
          safe.uboErr(logPrefix, "Error:", reason);
          return responseBefore;
        });
      }).catch((reason) => {
        safe.uboErr(logPrefix, "Error:", reason);
        return fetchPromise;
      });
    });
  }
  registerScriptlet(jsonlEditFetchResponseFn, {
    name: "jsonl-edit-fetch-response.fn",
    dependencies: [
      collateFetchArgumentsFn,
      JSONPath2,
      jsonlEditFn,
      matchObjectPropertiesFn,
      parsePropertiesToMatchFn,
      proxyApplyFn,
      safeSelf
    ]
  });
  function jsonlEditFetchResponse(jsonq = "", ...args) {
    jsonlEditFetchResponseFn(false, jsonq, ...args);
  }
  registerScriptlet(jsonlEditFetchResponse, {
    name: "jsonl-edit-fetch-response.js",
    dependencies: [
      jsonlEditFetchResponseFn
    ]
  });
  function trustedJsonlEditFetchResponse(jsonq = "", ...args) {
    jsonlEditFetchResponseFn(true, jsonq, ...args);
  }
  registerScriptlet(trustedJsonlEditFetchResponse, {
    name: "trusted-jsonl-edit-fetch-response.js",
    requiresTrust: true,
    dependencies: [
      jsonlEditFetchResponseFn
    ]
  });

  // src/js/resources/stack-trace.js
  function matchesStackTraceFn(needleDetails, logLevel = "") {
    const safe = safeSelf();
    const exceptionToken = getExceptionTokenFn();
    const error = new safe.Error(exceptionToken);
    const docURL = new URL(self.location.href);
    docURL.hash = "";
    const reLine = /(.*?@)?(\S+)(:\d+):\d+\)?$/;
    const lines = [];
    for (let line of safe.String_split.call(error.stack, /[\n\r]+/)) {
      if (line.includes(exceptionToken)) {
        continue;
      }
      line = line.trim();
      const match = safe.RegExp_exec.call(reLine, line);
      if (match === null) {
        continue;
      }
      let url = match[2];
      if (url.startsWith("(")) {
        url = url.slice(1);
      }
      if (url === docURL.href) {
        url = "inlineScript";
      } else if (url.startsWith("<anonymous>")) {
        url = "injectedScript";
      }
      let fn2 = match[1] !== void 0 ? match[1].slice(0, -1) : line.slice(0, match.index).trim();
      if (fn2.startsWith("at")) {
        fn2 = fn2.slice(2).trim();
      }
      const rowcol = match[3];
      lines.push(` ${`${fn2} ${url}${rowcol}:1`.trim()}`);
    }
    lines[0] = `stackDepth:${lines.length - 1}`;
    const stack = lines.join("	");
    const r = needleDetails.matchAll !== true && safe.testPattern(needleDetails, stack);
    if (logLevel === "all" || logLevel === "match" && r || logLevel === "nomatch" && !r) {
      safe.uboLog(stack.replace(/\t/g, "\n"));
    }
    return r;
  }
  registerScriptlet(matchesStackTraceFn, {
    name: "matches-stack-trace.fn",
    dependencies: [
      getExceptionTokenFn,
      safeSelf
    ]
  });
  function abortOnStackTrace(chain = "", needle = "") {
    if (typeof chain !== "string") {
      return;
    }
    const safe = safeSelf();
    const needleDetails = safe.initPattern(needle, { canNegate: true });
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 2);
    if (needle === "") {
      extraArgs.log = "all";
    }
    const makeProxy = function(owner2, chain2) {
      const pos = chain2.indexOf(".");
      if (pos === -1) {
        let v3 = owner2[chain2];
        Object.defineProperty(owner2, chain2, {
          get: function() {
            const log = safe.logLevel > 1 ? "all" : "match";
            if (matchesStackTraceFn(needleDetails, log)) {
              throw new ReferenceError(getExceptionTokenFn());
            }
            return v3;
          },
          set: function(a) {
            const log = safe.logLevel > 1 ? "all" : "match";
            if (matchesStackTraceFn(needleDetails, log)) {
              throw new ReferenceError(getExceptionTokenFn());
            }
            v3 = a;
          }
        });
        return;
      }
      const prop = chain2.slice(0, pos);
      let v2 = owner2[prop];
      chain2 = chain2.slice(pos + 1);
      if (v2) {
        makeProxy(v2, chain2);
        return;
      }
      const desc = Object.getOwnPropertyDescriptor(owner2, prop);
      if (desc && desc.set !== void 0) {
        return;
      }
      Object.defineProperty(owner2, prop, {
        get: function() {
          return v2;
        },
        set: function(a) {
          v2 = a;
          if (a instanceof Object) {
            makeProxy(a, chain2);
          }
        }
      });
    };
    const owner = window;
    makeProxy(owner, chain);
  }
  registerScriptlet(abortOnStackTrace, {
    name: "abort-on-stack-trace.js",
    aliases: [
      "aost.js"
    ],
    dependencies: [
      getExceptionTokenFn,
      matchesStackTraceFn,
      safeSelf
    ]
  });

  // src/js/resources/object-prune.js
  function objectFindOwnerFn(root, path, prune = false) {
    const safe = safeSelf();
    let owner = root;
    let chain = path;
    for (; ; ) {
      if (typeof owner !== "object" || owner === null) {
        return false;
      }
      const pos = chain.indexOf(".");
      if (pos === -1) {
        if (prune === false) {
          return safe.Object_hasOwn(owner, chain);
        }
        let modified = false;
        if (chain === "*") {
          for (const key in owner) {
            if (safe.Object_hasOwn(owner, key) === false) {
              continue;
            }
            delete owner[key];
            modified = true;
          }
        } else if (safe.Object_hasOwn(owner, chain)) {
          delete owner[chain];
          modified = true;
        }
        return modified;
      }
      const prop = chain.slice(0, pos);
      const next = chain.slice(pos + 1);
      let found = false;
      if (prop === "[-]" && Array.isArray(owner)) {
        let i = owner.length;
        while (i--) {
          if (objectFindOwnerFn(owner[i], next) === false) {
            continue;
          }
          owner.splice(i, 1);
          found = true;
        }
        return found;
      }
      if (prop === "{-}" && owner instanceof Object) {
        for (const key of Object.keys(owner)) {
          if (objectFindOwnerFn(owner[key], next) === false) {
            continue;
          }
          delete owner[key];
          found = true;
        }
        return found;
      }
      if (prop === "[]" && Array.isArray(owner) || prop === "{}" && owner instanceof Object || prop === "*" && owner instanceof Object) {
        for (const key of Object.keys(owner)) {
          if (objectFindOwnerFn(owner[key], next, prune) === false) {
            continue;
          }
          found = true;
        }
        return found;
      }
      if (safe.Object_hasOwn(owner, prop) === false) {
        return false;
      }
      owner = owner[prop];
      chain = chain.slice(pos + 1);
    }
  }
  registerScriptlet(objectFindOwnerFn, {
    name: "object-find-owner.fn",
    dependencies: [
      safeSelf
    ]
  });
  function objectPruneFn(obj, rawPrunePaths, rawNeedlePaths, stackNeedleDetails = { matchAll: true }, extraArgs = {}) {
    if (typeof rawPrunePaths !== "string") {
      return;
    }
    const safe = safeSelf();
    const prunePaths = rawPrunePaths !== "" ? safe.String_split.call(rawPrunePaths, / +/) : [];
    const needlePaths = prunePaths.length !== 0 && rawNeedlePaths !== "" ? safe.String_split.call(rawNeedlePaths, / +/) : [];
    if (stackNeedleDetails.matchAll !== true) {
      if (matchesStackTraceFn(stackNeedleDetails, extraArgs.logstack) === false) {
        return;
      }
    }
    if (objectPruneFn.mustProcess === void 0) {
      objectPruneFn.mustProcess = (root, needlePaths2) => {
        for (const needlePath of needlePaths2) {
          if (objectFindOwnerFn(root, needlePath) === false) {
            return false;
          }
        }
        return true;
      };
    }
    if (prunePaths.length === 0) {
      return;
    }
    let outcome = "nomatch";
    if (objectPruneFn.mustProcess(obj, needlePaths)) {
      for (const path of prunePaths) {
        if (objectFindOwnerFn(obj, path, true)) {
          outcome = "match";
        }
      }
    }
    if (outcome === "match") {
      return obj;
    }
  }
  registerScriptlet(objectPruneFn, {
    name: "object-prune.fn",
    dependencies: [
      matchesStackTraceFn,
      objectFindOwnerFn,
      safeSelf
    ]
  });
  function trustedPruneInboundObject(entryPoint = "", argPos = "", rawPrunePaths = "", rawNeedlePaths = "") {
    if (entryPoint === "") {
      return;
    }
    let context = globalThis;
    let prop = entryPoint;
    for (; ; ) {
      const pos = prop.indexOf(".");
      if (pos === -1) {
        break;
      }
      context = context[prop.slice(0, pos)];
      if (context instanceof Object === false) {
        return;
      }
      prop = prop.slice(pos + 1);
    }
    if (typeof context[prop] !== "function") {
      return;
    }
    const argIndex = parseInt(argPos);
    if (isNaN(argIndex)) {
      return;
    }
    if (argIndex < 1) {
      return;
    }
    const safe = safeSelf();
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 4);
    const needlePaths = [];
    if (rawPrunePaths !== "") {
      needlePaths.push(...safe.String_split.call(rawPrunePaths, / +/));
    }
    if (rawNeedlePaths !== "") {
      needlePaths.push(...safe.String_split.call(rawNeedlePaths, / +/));
    }
    const stackNeedle = safe.initPattern(extraArgs.stackToMatch || "", { canNegate: true });
    const mustProcess = (root) => {
      for (const needlePath of needlePaths) {
        if (objectFindOwnerFn(root, needlePath) === false) {
          return false;
        }
      }
      return true;
    };
    context[prop] = new Proxy(context[prop], {
      apply: function(target, thisArg, args) {
        const targetArg = argIndex <= args.length ? args[argIndex - 1] : void 0;
        if (targetArg instanceof Object && mustProcess(targetArg)) {
          let objBefore = targetArg;
          if (extraArgs.dontOverwrite) {
            try {
              objBefore = safe.JSON_parse(safe.JSON_stringify(targetArg));
            } catch (e) {
              console.warn("[uBR] object-prune: JSON_parse/stringify failed", e);
              objBefore = void 0;
            }
          }
          if (objBefore !== void 0) {
            const objAfter = objectPruneFn(
              objBefore,
              rawPrunePaths,
              rawNeedlePaths,
              stackNeedle,
              extraArgs
            );
            args[argIndex - 1] = objAfter || objBefore;
          }
        }
        return Reflect.apply(target, thisArg, args);
      }
    });
  }
  registerScriptlet(trustedPruneInboundObject, {
    name: "trusted-prune-inbound-object.js",
    requiresTrust: true,
    dependencies: [
      objectFindOwnerFn,
      objectPruneFn,
      safeSelf
    ]
  });
  function trustedPruneOutboundObject(propChain = "", rawPrunePaths = "", rawNeedlePaths = "") {
    if (propChain === "") {
      return;
    }
    const safe = safeSelf();
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 3);
    proxyApplyFn(propChain, (context) => {
      const objBefore = context.reflect();
      if (objBefore instanceof Object === false) {
        return objBefore;
      }
      const objAfter = objectPruneFn(
        objBefore,
        rawPrunePaths,
        rawNeedlePaths,
        { matchAll: true },
        extraArgs
      );
      return objAfter || objBefore;
    });
  }
  registerScriptlet(trustedPruneOutboundObject, {
    name: "trusted-prune-outbound-object.js",
    requiresTrust: true,
    dependencies: [
      objectPruneFn,
      proxyApplyFn,
      safeSelf
    ]
  });

  // src/js/resources/json-prune.js
  function jsonPrune(rawPrunePaths = "", rawNeedlePaths = "", stackNeedle = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("json-prune", rawPrunePaths, rawNeedlePaths, stackNeedle);
    const stackNeedleDetails = safe.initPattern(stackNeedle, { canNegate: true });
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 3);
    proxyApplyFn("JSON.parse", (context) => {
      const objBefore = context.reflect();
      if (rawPrunePaths === "") {
        safe.uboLog(logPrefix, safe.JSON_stringify(objBefore, null, 2));
      }
      const objAfter = objectPruneFn(
        objBefore,
        rawPrunePaths,
        rawNeedlePaths,
        stackNeedleDetails,
        extraArgs
      );
      if (objAfter === void 0) {
        return objBefore;
      }
      safe.uboLog(logPrefix, "Pruned");
      if (safe.logLevel > 1) {
        safe.uboLog(logPrefix, `After pruning:
${safe.JSON_stringify(objAfter, null, 2)}`);
      }
      return objAfter;
    });
  }
  registerScriptlet(jsonPrune, {
    name: "json-prune.js",
    dependencies: [
      objectPruneFn,
      proxyApplyFn,
      safeSelf
    ]
  });
  function jsonPruneFetchResponse(rawPrunePaths = "", rawNeedlePaths = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("json-prune-fetch-response", rawPrunePaths, rawNeedlePaths);
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 2);
    const propNeedles = parsePropertiesToMatchFn(extraArgs.propsToMatch, "url");
    const stackNeedle = safe.initPattern(extraArgs.stackToMatch || "", { canNegate: true });
    const logall = rawPrunePaths === "";
    const applyHandler = function(target, thisArg, args) {
      const fetchPromise = Reflect.apply(target, thisArg, args);
      if (propNeedles.size !== 0) {
        const props = collateFetchArgumentsFn(...args);
        const matched = matchObjectPropertiesFn(propNeedles, props);
        if (matched === void 0) {
          return fetchPromise;
        }
        if (safe.logLevel > 1) {
          safe.uboLog(logPrefix, `Matched "propsToMatch":
	${matched.join("\n	")}`);
        }
      }
      return fetchPromise.then((responseBefore) => {
        const response = responseBefore.clone();
        return response.json().then((objBefore) => {
          if (typeof objBefore !== "object") {
            return responseBefore;
          }
          if (logall) {
            safe.uboLog(logPrefix, safe.JSON_stringify(objBefore, null, 2));
            return responseBefore;
          }
          const objAfter = objectPruneFn(
            objBefore,
            rawPrunePaths,
            rawNeedlePaths,
            stackNeedle,
            extraArgs
          );
          if (typeof objAfter !== "object") {
            return responseBefore;
          }
          safe.uboLog(logPrefix, "Pruned");
          const responseAfter = Response.json(objAfter, {
            status: responseBefore.status,
            statusText: responseBefore.statusText,
            headers: responseBefore.headers
          });
          Object.defineProperties(responseAfter, {
            ok: { value: responseBefore.ok },
            redirected: { value: responseBefore.redirected },
            type: { value: responseBefore.type },
            url: { value: responseBefore.url }
          });
          return responseAfter;
        }).catch((reason) => {
          safe.uboErr(logPrefix, "Error:", reason);
          return responseBefore;
        });
      }).catch((reason) => {
        safe.uboErr(logPrefix, "Error:", reason);
        return fetchPromise;
      });
    };
    self.fetch = new Proxy(self.fetch, {
      apply: applyHandler
    });
  }
  registerScriptlet(jsonPruneFetchResponse, {
    name: "json-prune-fetch-response.js",
    dependencies: [
      collateFetchArgumentsFn,
      matchObjectPropertiesFn,
      objectPruneFn,
      parsePropertiesToMatchFn,
      safeSelf
    ]
  });
  function jsonPruneXhrResponse(rawPrunePaths = "", rawNeedlePaths = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("json-prune-xhr-response", rawPrunePaths, rawNeedlePaths);
    const xhrInstances = /* @__PURE__ */ new WeakMap();
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 2);
    const propNeedles = parsePropertiesToMatchFn(extraArgs.propsToMatch, "url");
    const stackNeedle = safe.initPattern(extraArgs.stackToMatch || "", { canNegate: true });
    self.XMLHttpRequest = class extends self.XMLHttpRequest {
      open(method, url, ...args) {
        const xhrDetails = { method, url };
        let outcome = "match";
        if (propNeedles.size !== 0) {
          if (matchObjectPropertiesFn(propNeedles, xhrDetails) === void 0) {
            outcome = "nomatch";
          }
        }
        if (outcome === "match") {
          if (safe.logLevel > 1) {
            safe.uboLog(logPrefix, `Matched optional "propsToMatch", "${extraArgs.propsToMatch}"`);
          }
          xhrInstances.set(this, xhrDetails);
        }
        return super.open(method, url, ...args);
      }
      get response() {
        const innerResponse = super.response;
        const xhrDetails = xhrInstances.get(this);
        if (xhrDetails === void 0) {
          return innerResponse;
        }
        const responseLength = typeof innerResponse === "string" ? innerResponse.length : void 0;
        if (xhrDetails.lastResponseLength !== responseLength) {
          xhrDetails.response = void 0;
          xhrDetails.lastResponseLength = responseLength;
        }
        if (xhrDetails.response !== void 0) {
          return xhrDetails.response;
        }
        let objBefore;
        if (typeof innerResponse === "object") {
          objBefore = innerResponse;
        } else if (typeof innerResponse === "string") {
          try {
            objBefore = safe.JSON_parse(innerResponse);
          } catch (e) {
            console.warn("[uBR] json-prune: JSON_parse failed in responseHandler", e);
          }
        }
        if (typeof objBefore !== "object") {
          return xhrDetails.response = innerResponse;
        }
        const objAfter = objectPruneFn(
          objBefore,
          rawPrunePaths,
          rawNeedlePaths,
          stackNeedle,
          extraArgs
        );
        let outerResponse;
        if (typeof objAfter === "object") {
          outerResponse = typeof innerResponse === "string" ? safe.JSON_stringify(objAfter) : objAfter;
          safe.uboLog(logPrefix, "Pruned");
        } else {
          outerResponse = innerResponse;
        }
        return xhrDetails.response = outerResponse;
      }
      get responseText() {
        const response = this.response;
        return typeof response !== "string" ? super.responseText : response;
      }
    };
  }
  registerScriptlet(jsonPruneXhrResponse, {
    name: "json-prune-xhr-response.js",
    dependencies: [
      matchObjectPropertiesFn,
      objectPruneFn,
      parsePropertiesToMatchFn,
      safeSelf
    ]
  });
  function evaldataPrune(rawPrunePaths = "", rawNeedlePaths = "") {
    proxyApplyFn("eval", (context) => {
      const before = context.reflect();
      if (typeof before !== "object") {
        return before;
      }
      if (before === null) {
        return null;
      }
      const after = objectPruneFn(before, rawPrunePaths, rawNeedlePaths);
      return after || before;
    });
  }
  registerScriptlet(evaldataPrune, {
    name: "evaldata-prune.js",
    dependencies: [
      objectPruneFn,
      proxyApplyFn
    ]
  });

  // src/js/resources/noeval.js
  function noEvalIf(needle = "") {
    if (typeof needle !== "string") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("noeval-if", needle);
    const reNeedle = safe.patternToRegex(needle);
    proxyApplyFn("eval", (context) => {
      const { callArgs } = context;
      const a = String(callArgs[0]);
      if (needle !== "" && reNeedle.test(a)) {
        safe.uboLog(logPrefix, "Prevented:\n", a);
        return;
      }
      if (needle === "" || safe.logLevel > 1) {
        safe.uboLog(logPrefix, "Not prevented:\n", a);
      }
      return context.reflect();
    });
  }
  registerScriptlet(noEvalIf, {
    name: "noeval-if.js",
    aliases: [
      "prevent-eval-if.js"
    ],
    dependencies: [
      proxyApplyFn,
      safeSelf
    ]
  });

  // src/js/resources/prevent-addeventlistener.js
  function preventAddEventListener(type = "", pattern = "") {
    const safe = safeSelf();
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 2);
    const logPrefix = safe.makeLogPrefix("prevent-addEventListener", type, pattern);
    const reType = safe.patternToRegex(type, void 0, true);
    const rePattern = safe.patternToRegex(pattern);
    const targetSelector = extraArgs.elements || void 0;
    const elementMatches = (elem) => {
      if (targetSelector === "window") {
        return elem === window;
      }
      if (targetSelector === "document") {
        return elem === document;
      }
      if (elem && elem.matches && elem.matches(targetSelector)) {
        return true;
      }
      const elems = Array.from(document.querySelectorAll(targetSelector));
      return elems.includes(elem);
    };
    const elementDetails = (elem) => {
      if (elem instanceof Window) {
        return "window";
      }
      if (elem instanceof Document) {
        return "document";
      }
      if (elem instanceof Element === false) {
        return "?";
      }
      const parts = [];
      const id = String(elem.id);
      if (id !== "") {
        parts.push(`#${CSS.escape(id)}`);
      }
      for (let i = 0; i < elem.classList.length; i++) {
        parts.push(`.${CSS.escape(elem.classList.item(i))}`);
      }
      for (let i = 0; i < elem.attributes.length; i++) {
        const attr = elem.attributes.item(i);
        if (attr.name === "id") {
          continue;
        }
        if (attr.name === "class") {
          continue;
        }
        parts.push(`[${CSS.escape(attr.name)}="${attr.value}"]`);
      }
      return parts.join("");
    };
    const shouldPrevent = (thisArg, type2, handler) => {
      const matchesType = safe.RegExp_test.call(reType, type2);
      const matchesHandler = safe.RegExp_test.call(rePattern, handler);
      const matchesEither = matchesType || matchesHandler;
      const matchesBoth = matchesType && matchesHandler;
      if (safe.logLevel > 1 && matchesEither) {
        debugger;
      }
      if (matchesBoth && targetSelector !== void 0) {
        if (elementMatches(thisArg) === false) {
          return false;
        }
      }
      return matchesBoth;
    };
    const proxyFn = function(context) {
      const { callArgs, thisArg } = context;
      let t, h2;
      try {
        t = String(callArgs[0]);
        if (typeof callArgs[1] === "function") {
          h2 = String(safe.Function_toString(callArgs[1]));
        } else if (typeof callArgs[1] === "object" && callArgs[1] !== null) {
          if (typeof callArgs[1].handleEvent === "function") {
            h2 = String(safe.Function_toString(callArgs[1].handleEvent));
          }
        } else {
          h2 = String(callArgs[1]);
        }
      } catch (e) {
        console.warn("[uBR] prevent-addeventlistener: handler toString failed", e);
      }
      if (type === "" && pattern === "") {
        safe.uboLog(logPrefix, `Called: ${t}
${h2}
${elementDetails(thisArg)}`);
      } else if (shouldPrevent(thisArg, t, h2)) {
        return safe.uboLog(logPrefix, `Prevented: ${t}
${h2}
${elementDetails(thisArg)}`);
      }
      return context.reflect();
    };
    runAt(() => {
      proxyApplyFn("EventTarget.prototype.addEventListener", proxyFn);
      if (extraArgs.protect) {
        const { addEventListener } = EventTarget.prototype;
        Object.defineProperty(EventTarget.prototype, "addEventListener", {
          set() {
          },
          get() {
            return addEventListener;
          }
        });
      }
      proxyApplyFn("document.addEventListener", proxyFn);
      if (extraArgs.protect) {
        const { addEventListener } = document;
        Object.defineProperty(document, "addEventListener", {
          set() {
          },
          get() {
            return addEventListener;
          }
        });
      }
    }, extraArgs.runAt);
  }
  registerScriptlet(preventAddEventListener, {
    name: "prevent-addEventListener.js",
    aliases: [
      "addEventListener-defuser.js",
      "aeld.js"
    ],
    dependencies: [
      proxyApplyFn,
      runAt,
      safeSelf
    ]
  });

  // src/js/resources/prevent-dialog.js
  function preventDialog(selector = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("prevent-dialog", selector);
    const prevent = () => {
      debouncer = void 0;
      const elems = document.querySelectorAll(`dialog${selector}`);
      for (const elem of elems) {
        if (typeof elem.close !== "function") {
          continue;
        }
        if (elem.open === false) {
          continue;
        }
        elem.close();
        safe.uboLog(logPrefix, "Closed");
      }
    };
    let debouncer;
    const observer = new MutationObserver(() => {
      if (debouncer !== void 0) {
        return;
      }
      debouncer = requestAnimationFrame(prevent);
    });
    observer.observe(document, {
      attributes: true,
      childList: true,
      subtree: true
    });
  }
  registerScriptlet(preventDialog, {
    name: "prevent-dialog.js",
    dependencies: [
      safeSelf
    ],
    world: "ISOLATED"
  });

  // src/js/resources/prevent-fetch.js
  function preventFetchFn(trusted = false, propsToMatch = "", responseBody = "", responseType = "") {
    const safe = safeSelf();
    const setTimeout2 = self.setTimeout;
    const scriptletName = `${trusted ? "trusted-" : ""}prevent-fetch`;
    const logPrefix = safe.makeLogPrefix(
      scriptletName,
      propsToMatch,
      responseBody,
      responseType
    );
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 4);
    const propNeedles = parsePropertiesToMatchFn(propsToMatch, "url");
    const validResponseProps = {
      ok: [false, true],
      status: [403],
      statusText: ["", "Not Found"],
      type: ["basic", "cors", "default", "error", "opaque"]
    };
    const responseProps = {
      statusText: { value: "OK" }
    };
    const responseHeaders = {};
    if (/^\{.*\}$/.test(responseType)) {
      try {
        Object.entries(JSON.parse(responseType)).forEach(([p, v2]) => {
          if (p === "headers" && trusted) {
            Object.assign(responseHeaders, v2);
            return;
          }
          if (validResponseProps[p] === void 0) {
            return;
          }
          if (validResponseProps[p].includes(v2) === false) {
            return;
          }
          responseProps[p] = { value: v2 };
        });
      } catch (e) {
        console.warn("[uBR] prevent-fetch: responseType parsing failed", e);
      }
    } else if (responseType !== "") {
      if (validResponseProps.type.includes(responseType)) {
        responseProps.type = { value: responseType };
      }
    }
    proxyApplyFn("fetch", (context) => {
      const { callArgs } = context;
      const details = collateFetchArgumentsFn(...callArgs);
      if (safe.logLevel > 1 || propsToMatch === "" && responseBody === "") {
        const out = Array.from(Object.entries(details)).map((a) => `${a[0]}:${a[1]}`);
        safe.uboLog(logPrefix, `Called: ${out.join("\n")}`);
      }
      if (propsToMatch === "" && responseBody === "") {
        return context.reflect();
      }
      const matched = matchObjectPropertiesFn(propNeedles, details);
      if (matched === void 0 || matched.length === 0) {
        return context.reflect();
      }
      return Promise.resolve(generateContentFn(trusted, responseBody)).then((text) => {
        safe.uboLog(logPrefix, `Prevented with response "${text}"`);
        const headers = Object.assign({}, responseHeaders);
        if (headers["content-length"] === void 0) {
          headers["content-length"] = text.length;
        }
        const response = new Response(text, { headers });
        const props = Object.assign(
          { url: { value: details.url } },
          responseProps
        );
        safe.Object_defineProperties(response, props);
        if (extraArgs.throttle) {
          return new Promise((resolve) => {
            setTimeout2(() => {
              resolve(response);
            }, extraArgs.throttle);
          });
        }
        return response;
      });
    });
  }
  registerScriptlet(preventFetchFn, {
    name: "prevent-fetch.fn",
    dependencies: [
      collateFetchArgumentsFn,
      generateContentFn,
      matchObjectPropertiesFn,
      parsePropertiesToMatchFn,
      proxyApplyFn,
      safeSelf
    ]
  });
  function preventFetch(...args) {
    preventFetchFn(false, ...args);
  }
  registerScriptlet(preventFetch, {
    name: "prevent-fetch.js",
    aliases: [
      "no-fetch-if.js"
    ],
    dependencies: [
      preventFetchFn
    ]
  });
  function trustedPreventFetch(...args) {
    preventFetchFn(true, ...args);
  }
  registerScriptlet(trustedPreventFetch, {
    name: "trusted-prevent-fetch.js",
    requiresTrust: true,
    dependencies: [
      preventFetchFn
    ]
  });

  // src/js/resources/prevent-innerHTML.js
  function freezeElementProperty(property = "", selector = "", pattern = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("freeze-element-property", property, selector, pattern);
    const matcher = safe.initPattern(pattern, { canNegate: true });
    const owner = (() => {
      if (Object.hasOwn(Element.prototype, property)) {
        return Element.prototype;
      }
      if (Object.hasOwn(HTMLElement.prototype, property)) {
        return HTMLElement.prototype;
      }
      if (Object.hasOwn(Node.prototype, property)) {
        return Node.prototype;
      }
      return null;
    })();
    if (owner === null) {
      return;
    }
    const current = safe.Object_getOwnPropertyDescriptor(owner, property);
    if (current === void 0) {
      return;
    }
    const shouldPreventSet = (elem, a) => {
      if (selector !== "") {
        if (typeof elem.matches !== "function") {
          return false;
        }
        if (elem.matches(selector) === false) {
          return false;
        }
      }
      return safe.testPattern(matcher, `${a}`);
    };
    Object.defineProperty(owner, property, {
      get: function() {
        return current.get ? current.get.call(this) : current.value;
      },
      set: function(a) {
        if (shouldPreventSet(this, a)) {
          safe.uboLog(logPrefix, "Assignment prevented");
        } else if (current.set) {
          current.set.call(this, a);
        }
        if (safe.logLevel > 1) {
          safe.uboLog(logPrefix, `Assigned:
${a}`);
        }
        current.value = a;
      }
    });
  }
  registerScriptlet(freezeElementProperty, {
    name: "freeze-element-property.js",
    dependencies: [
      safeSelf
    ]
  });
  function preventInnerHTML(selector = "", pattern = "") {
    freezeElementProperty("innerHTML", selector, pattern);
  }
  registerScriptlet(preventInnerHTML, {
    name: "prevent-innerHTML.js",
    dependencies: [
      freezeElementProperty
    ]
  });

  // src/js/resources/prevent-navigation.js
  function preventNavigation(pattern = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("prevent-navigation", pattern);
    const needle = pattern === "location.href" ? self.location.href : pattern;
    const matcher = safe.initPattern(needle, { canNegate: true });
    self.navigation.addEventListener("navigate", (ev) => {
      if (ev.userInitiated) {
        return;
      }
      const { url } = ev.destination;
      if (pattern === "") {
        safe.uboLog(logPrefix, `Navigation to ${url}`);
        return;
      }
      if (safe.testPattern(matcher, url)) {
        ev.preventDefault();
        safe.uboLog(logPrefix, `Prevented navigation to ${url}`);
      }
    });
  }
  registerScriptlet(preventNavigation, {
    name: "prevent-navigation.js",
    dependencies: [
      safeSelf
    ],
    world: "ISOLATED"
  });

  // src/js/resources/prevent-settimeout.js
  var RangeParser = class {
    constructor(s) {
      this.not = s.charAt(0) === "!";
      if (this.not) {
        s = s.slice(1);
      }
      if (s === "") {
        return;
      }
      const pos = s.indexOf("-");
      if (pos !== 0) {
        this.min = this.max = parseInt(s, 10) || 0;
      }
      if (pos !== -1) {
        this.max = parseInt(s.slice(pos + 1), 10) || Number.MAX_SAFE_INTEGER;
      }
    }
    unbound() {
      return this.min === void 0 && this.max === void 0;
    }
    test(v2) {
      const n = Math.min(Math.max(Number(v2) || 0, 0), Number.MAX_SAFE_INTEGER);
      if (this.min === this.max) {
        return (this.min === void 0 || n === this.min) !== this.not;
      }
      if (this.min === void 0) {
        return n <= this.max !== this.not;
      }
      if (this.max === void 0) {
        return n >= this.min !== this.not;
      }
      return (n >= this.min && n <= this.max) !== this.not;
    }
  };
  registerScriptlet(RangeParser, {
    name: "range-parser.fn"
  });
  function preventSetTimeout(needleRaw = "", delayRaw = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("prevent-setTimeout", needleRaw, delayRaw);
    const needleNot = needleRaw.charAt(0) === "!";
    const reNeedle = safe.patternToRegex(needleNot ? needleRaw.slice(1) : needleRaw);
    const range = new RangeParser(delayRaw);
    proxyApplyFn("setTimeout", (context) => {
      const { callArgs } = context;
      const a = callArgs[0] instanceof Function ? safe.String(safe.Function_toString(callArgs[0])) : safe.String(callArgs[0]);
      const b2 = callArgs[1];
      if (needleRaw === "" && range.unbound()) {
        safe.uboLog(logPrefix, `Called:
${a}
${b2}`);
        return context.reflect();
      }
      if (reNeedle.test(a) !== needleNot && range.test(b2)) {
        callArgs[0] = function() {
        };
        safe.uboLog(logPrefix, `Prevented:
${a}
${b2}`);
      }
      return context.reflect();
    });
  }
  registerScriptlet(preventSetTimeout, {
    name: "prevent-setTimeout.js",
    aliases: [
      "no-setTimeout-if.js",
      "nostif.js",
      "setTimeout-defuser.js"
    ],
    dependencies: [
      proxyApplyFn,
      RangeParser,
      safeSelf
    ]
  });
  function preventSetInterval(needleRaw = "", delayRaw = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("prevent-setInterval", needleRaw, delayRaw);
    const needleNot = needleRaw.charAt(0) === "!";
    const reNeedle = safe.patternToRegex(needleNot ? needleRaw.slice(1) : needleRaw);
    const range = new RangeParser(delayRaw);
    proxyApplyFn("setInterval", (context) => {
      const { callArgs } = context;
      const a = callArgs[0] instanceof Function ? safe.String(safe.Function_toString(callArgs[0])) : safe.String(callArgs[0]);
      const b2 = callArgs[1];
      if (needleRaw === "" && range.unbound()) {
        safe.uboLog(logPrefix, `Called:
${a}
${b2}`);
        return context.reflect();
      }
      if (reNeedle.test(a) !== needleNot && range.test(b2)) {
        callArgs[0] = function() {
        };
        safe.uboLog(logPrefix, `Prevented:
${a}
${b2}`);
      }
      return context.reflect();
    });
  }
  registerScriptlet(preventSetInterval, {
    name: "prevent-setInterval.js",
    aliases: [
      "no-setInterval-if.js",
      "nosiif.js",
      "setInterval-defuser.js"
    ],
    dependencies: [
      proxyApplyFn,
      RangeParser,
      safeSelf
    ]
  });
  function preventRequestAnimationFrame(needleRaw = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("prevent-requestAnimationFrame", needleRaw);
    const needleNot = needleRaw.charAt(0) === "!";
    const reNeedle = safe.patternToRegex(needleNot ? needleRaw.slice(1) : needleRaw);
    proxyApplyFn("requestAnimationFrame", (context) => {
      const { callArgs } = context;
      const a = callArgs[0] instanceof Function ? safe.String(safe.Function_toString(callArgs[0])) : safe.String(callArgs[0]);
      if (needleRaw === "") {
        safe.uboLog(logPrefix, `Called:
${a}`);
      } else if (reNeedle.test(a) !== needleNot) {
        callArgs[0] = function() {
        };
        safe.uboLog(logPrefix, `Prevented:
${a}`);
      }
      return context.reflect();
    });
  }
  registerScriptlet(preventRequestAnimationFrame, {
    name: "prevent-requestAnimationFrame.js",
    aliases: [
      "no-requestAnimationFrame-if.js",
      "norafif.js"
    ],
    dependencies: [
      proxyApplyFn,
      safeSelf
    ]
  });

  // src/js/resources/prevent-xhr.js
  function preventXhrFn(trusted = false, propsToMatch = "", directive = "") {
    if (typeof propsToMatch !== "string") {
      return;
    }
    const safe = safeSelf();
    const scriptletName = trusted ? "trusted-prevent-xhr" : "prevent-xhr";
    const logPrefix = safe.makeLogPrefix(scriptletName, propsToMatch, directive);
    const xhrInstances = /* @__PURE__ */ new WeakMap();
    const propNeedles = parsePropertiesToMatchFn(propsToMatch, "url");
    const warOrigin = scriptletGlobals.warOrigin;
    const safeDispatchEvent = (xhr, type) => {
      try {
        xhr.dispatchEvent(new Event(type));
      } catch (e) {
        console.warn("[uBR] prevent-xhr: dispatchEvent failed", e);
      }
    };
    proxyApplyFn("XMLHttpRequest.prototype.open", (context) => {
      const { thisArg, callArgs } = context;
      xhrInstances.delete(thisArg);
      const [method, url, ...args] = callArgs;
      if (warOrigin !== void 0 && url.startsWith(warOrigin)) {
        return context.reflect();
      }
      const haystack = { method, url };
      if (propsToMatch === "" && directive === "") {
        safe.uboLog(logPrefix, `Called: ${safe.JSON_stringify(haystack, null, 2)}`);
        return context.reflect();
      }
      if (matchObjectPropertiesFn(propNeedles, haystack)) {
        const xhrDetails = Object.assign(haystack, {
          xhr: thisArg,
          defer: args.length === 0 || !!args[0],
          directive,
          headers: {
            "date": "",
            "content-type": "",
            "content-length": ""
          },
          url: haystack.url,
          props: {
            response: { value: "" },
            responseText: { value: "" },
            responseXML: { value: null }
          }
        });
        xhrInstances.set(thisArg, xhrDetails);
      }
      return context.reflect();
    });
    proxyApplyFn("XMLHttpRequest.prototype.send", (context) => {
      const { thisArg } = context;
      const xhrDetails = xhrInstances.get(thisArg);
      if (xhrDetails === void 0) {
        return context.reflect();
      }
      xhrDetails.headers["date"] = (/* @__PURE__ */ new Date()).toUTCString();
      let xhrText = "";
      switch (thisArg.responseType) {
        case "arraybuffer":
          xhrDetails.props.response.value = new ArrayBuffer(0);
          xhrDetails.headers["content-type"] = "application/octet-stream";
          break;
        case "blob":
          xhrDetails.props.response.value = new Blob([]);
          xhrDetails.headers["content-type"] = "application/octet-stream";
          break;
        case "document": {
          const parser = new DOMParser();
          const doc = parser.parseFromString("", "text/html");
          xhrDetails.props.response.value = doc;
          xhrDetails.props.responseXML.value = doc;
          xhrDetails.headers["content-type"] = "text/html";
          break;
        }
        case "json":
          xhrDetails.props.response.value = {};
          xhrDetails.props.responseText.value = "{}";
          xhrDetails.headers["content-type"] = "application/json";
          break;
        default: {
          if (directive === "") {
            break;
          }
          xhrText = generateContentFn(trusted, xhrDetails.directive);
          if (xhrText instanceof Promise) {
            xhrText = xhrText.then((text) => {
              xhrDetails.props.response.value = text;
              xhrDetails.props.responseText.value = text;
            });
          } else {
            xhrDetails.props.response.value = xhrText;
            xhrDetails.props.responseText.value = xhrText;
          }
          xhrDetails.headers["content-type"] = "text/plain";
          break;
        }
      }
      if (xhrDetails.defer === false) {
        xhrDetails.headers["content-length"] = `${xhrDetails.props.response.value}`.length;
        Object.defineProperties(xhrDetails.xhr, {
          readyState: { value: 4 },
          responseURL: { value: xhrDetails.url },
          status: { value: 200 },
          statusText: { value: "OK" }
        });
        Object.defineProperties(xhrDetails.xhr, xhrDetails.props);
        return;
      }
      Promise.resolve(xhrText).then(() => xhrDetails).then((details) => {
        Object.defineProperties(details.xhr, {
          readyState: { value: 1, configurable: true },
          responseURL: { value: xhrDetails.url }
        });
        safeDispatchEvent(details.xhr, "readystatechange");
        return details;
      }).then((details) => {
        xhrDetails.headers["content-length"] = `${details.props.response.value}`.length;
        Object.defineProperties(details.xhr, {
          readyState: { value: 2, configurable: true },
          status: { value: 200 },
          statusText: { value: "OK" }
        });
        safeDispatchEvent(details.xhr, "readystatechange");
        return details;
      }).then((details) => {
        Object.defineProperties(details.xhr, {
          readyState: { value: 3, configurable: true }
        });
        Object.defineProperties(details.xhr, details.props);
        safeDispatchEvent(details.xhr, "readystatechange");
        return details;
      }).then((details) => {
        Object.defineProperties(details.xhr, {
          readyState: { value: 4 }
        });
        safeDispatchEvent(details.xhr, "readystatechange");
        safeDispatchEvent(details.xhr, "load");
        safeDispatchEvent(details.xhr, "loadend");
        safe.uboLog(logPrefix, `Prevented with response:
${details.xhr.response}`);
      });
    });
    proxyApplyFn("XMLHttpRequest.prototype.getResponseHeader", (context) => {
      const { thisArg } = context;
      const xhrDetails = xhrInstances.get(thisArg);
      if (xhrDetails === void 0 || thisArg.readyState < thisArg.HEADERS_RECEIVED) {
        return context.reflect();
      }
      const headerName = `${context.callArgs[0]}`;
      const value = xhrDetails.headers[headerName.toLowerCase()];
      if (value !== void 0 && value !== "") {
        return value;
      }
      return null;
    });
    proxyApplyFn("XMLHttpRequest.prototype.getAllResponseHeaders", (context) => {
      const { thisArg } = context;
      const xhrDetails = xhrInstances.get(thisArg);
      if (xhrDetails === void 0 || thisArg.readyState < thisArg.HEADERS_RECEIVED) {
        return context.reflect();
      }
      const out = [];
      for (const [name2, value] of Object.entries(xhrDetails.headers)) {
        if (!value) {
          continue;
        }
        out.push(`${name2}: ${value}`);
      }
      if (out.length !== 0) {
        out.push("");
      }
      return out.join("\r\n");
    });
  }
  registerScriptlet(preventXhrFn, {
    name: "prevent-xhr.fn",
    dependencies: [
      generateContentFn,
      matchObjectPropertiesFn,
      parsePropertiesToMatchFn,
      proxyApplyFn,
      safeSelf
    ]
  });
  function preventXhr(...args) {
    return preventXhrFn(false, ...args);
  }
  registerScriptlet(preventXhr, {
    name: "prevent-xhr.js",
    aliases: [
      "no-xhr-if.js"
    ],
    dependencies: [
      preventXhrFn
    ]
  });
  function trustedPreventXhr(...args) {
    return preventXhrFn(true, ...args);
  }
  registerScriptlet(trustedPreventXhr, {
    name: "trusted-prevent-xhr.js",
    dependencies: [
      preventXhrFn
    ]
  });

  // src/js/resources/parse-replace.js
  function parseReplaceFn(s) {
    if (s.charCodeAt(0) !== 47) {
      return;
    }
    const parser = new ArglistParser2("/");
    parser.nextArg(s, 1);
    let pattern = s.slice(parser.argBeg, parser.argEnd);
    if (parser.transform) {
      pattern = parser.normalizeArg(pattern);
    }
    if (pattern === "") {
      return;
    }
    parser.nextArg(s, parser.separatorEnd);
    let replacement = s.slice(parser.argBeg, parser.argEnd);
    if (parser.separatorEnd === parser.separatorBeg) {
      return;
    }
    if (parser.transform) {
      replacement = parser.normalizeArg(replacement);
    }
    const flags = s.slice(parser.separatorEnd);
    try {
      return { re: new RegExp(pattern, flags), replacement };
    } catch (e) {
      console.warn("[uBR] parse-replace: RegExp failed", e);
    }
  }
  registerScriptlet(parseReplaceFn, {
    name: "parse-replace.fn",
    dependencies: [
      ArglistParser2
    ]
  });

  // src/js/resources/set-constant.js
  function validateConstantFn(trusted, raw, extraArgs = {}) {
    const safe = safeSelf();
    let value;
    if (raw === "undefined") {
      value = void 0;
    } else if (raw === "false") {
      value = false;
    } else if (raw === "true") {
      value = true;
    } else if (raw === "null") {
      value = null;
    } else if (raw === "''" || raw === "") {
      value = "";
    } else if (raw === "[]" || raw === "emptyArr") {
      value = [];
    } else if (raw === "{}" || raw === "emptyObj") {
      value = {};
    } else if (raw === "noopFunc") {
      value = function() {
      };
    } else if (raw === "trueFunc") {
      value = function() {
        return true;
      };
    } else if (raw === "falseFunc") {
      value = function() {
        return false;
      };
    } else if (raw === "throwFunc") {
      value = function() {
        throw "";
      };
    } else if (/^-?\d+$/.test(raw)) {
      value = parseInt(raw);
      if (isNaN(raw)) {
        return;
      }
      if (Math.abs(raw) > 32767) {
        return;
      }
    } else if (trusted) {
      if (raw.startsWith("json:")) {
        try {
          value = safe.JSON_parse(raw.slice(5));
        } catch (e) {
          console.warn("[uBR] set-constant: JSON_parse failed", e);
          return;
        }
      } else if (raw.startsWith("{") && raw.endsWith("}")) {
        try {
          value = safe.JSON_parse(raw).value;
        } catch (e) {
          console.warn("[uBR] set-constant: JSON_parse value failed", e);
          return;
        }
      }
    } else {
      return;
    }
    if (extraArgs.as !== void 0) {
      if (extraArgs.as === "function") {
        return () => value;
      } else if (extraArgs.as === "callback") {
        return () => (() => value);
      } else if (extraArgs.as === "resolved") {
        return Promise.resolve(value);
      } else if (extraArgs.as === "rejected") {
        return Promise.reject(value);
      }
    }
    return value;
  }
  registerScriptlet(validateConstantFn, {
    name: "validate-constant.fn",
    dependencies: [
      safeSelf
    ]
  });
  function setConstantFn(trusted = false, chain = "", rawValue = "") {
    if (chain === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("set-constant", chain, rawValue);
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 3);
    function setConstant2(chain2, rawValue2) {
      const trappedProp = (() => {
        const pos = chain2.lastIndexOf(".");
        if (pos === -1) {
          return chain2;
        }
        return chain2.slice(pos + 1);
      })();
      const cloakFunc = (fn2) => {
        safe.Object_defineProperty(fn2, "name", { value: trappedProp });
        return new Proxy(fn2, {
          defineProperty(target, prop) {
            if (prop !== "toString") {
              return Reflect.defineProperty(...arguments);
            }
            return true;
          },
          deleteProperty(target, prop) {
            if (prop !== "toString") {
              return Reflect.deleteProperty(...arguments);
            }
            return true;
          },
          get(target, prop) {
            if (prop === "toString") {
              return function() {
                return `function ${trappedProp}() { [native code] }`;
              }.bind(null);
            }
            return Reflect.get(...arguments);
          }
        });
      };
      if (trappedProp === "") {
        return;
      }
      const thisScript = document.currentScript;
      let normalValue = validateConstantFn(trusted, rawValue2, extraArgs);
      if (rawValue2 === "noopFunc" || rawValue2 === "trueFunc" || rawValue2 === "falseFunc") {
        normalValue = cloakFunc(normalValue);
      }
      let aborted = false;
      const mustAbort = function(v2) {
        if (trusted) {
          return false;
        }
        if (aborted) {
          return true;
        }
        aborted = v2 !== void 0 && v2 !== null && (normalValue !== void 0 && normalValue !== null) && typeof v2 !== typeof normalValue;
        if (aborted) {
          safe.uboLog(logPrefix, `Aborted because value set to ${v2}`);
        }
        return aborted;
      };
      const trapProp = function(owner, prop, configurable, handler) {
        if (handler.init(configurable ? owner[prop] : normalValue) === false) {
          return;
        }
        const odesc = safe.Object_getOwnPropertyDescriptor(owner, prop);
        let prevGetter, prevSetter;
        if (odesc instanceof safe.Object) {
          owner[prop] = normalValue;
          if (odesc.get instanceof Function) {
            prevGetter = odesc.get;
          }
          if (odesc.set instanceof Function) {
            prevSetter = odesc.set;
          }
        }
        try {
          safe.Object_defineProperty(owner, prop, {
            configurable,
            get() {
              if (prevGetter !== void 0) {
                prevGetter();
              }
              return handler.getter();
            },
            set(a) {
              if (prevSetter !== void 0) {
                prevSetter(a);
              }
              handler.setter(a);
            }
          });
          safe.uboLog(logPrefix, "Trap installed");
        } catch (ex) {
          safe.uboErr(logPrefix, ex);
        }
      };
      const trapChain = function(owner, chain3) {
        const pos = chain3.indexOf(".");
        if (pos === -1) {
          trapProp(owner, chain3, false, {
            v: void 0,
            init: function(v3) {
              if (mustAbort(v3)) {
                return false;
              }
              this.v = v3;
              return true;
            },
            getter: function() {
              if (document.currentScript === thisScript) {
                return this.v;
              }
              safe.uboLog(logPrefix, "Property read");
              return normalValue;
            },
            setter: function(a) {
              if (mustAbort(a) === false) {
                return;
              }
              normalValue = a;
            }
          });
          return;
        }
        const prop = chain3.slice(0, pos);
        const v2 = owner[prop];
        chain3 = chain3.slice(pos + 1);
        if (v2 instanceof safe.Object || typeof v2 === "object" && v2 !== null) {
          trapChain(v2, chain3);
          return;
        }
        trapProp(owner, prop, true, {
          v: void 0,
          init: function(v3) {
            this.v = v3;
            return true;
          },
          getter: function() {
            return this.v;
          },
          setter: function(a) {
            this.v = a;
            if (a instanceof safe.Object) {
              trapChain(a, chain3);
            }
          }
        });
      };
      trapChain(window, chain2);
    }
    runAt(() => {
      setConstant2(chain, rawValue);
    }, extraArgs.runAt);
  }
  registerScriptlet(setConstantFn, {
    name: "set-constant.fn",
    dependencies: [
      runAt,
      safeSelf,
      validateConstantFn
    ]
  });
  function setConstant(...args) {
    setConstantFn(false, ...args);
  }
  registerScriptlet(setConstant, {
    name: "set-constant.js",
    aliases: [
      "set.js"
    ],
    dependencies: [
      setConstantFn
    ]
  });
  function trustedSetConstant(...args) {
    setConstantFn(true, ...args);
  }
  registerScriptlet(trustedSetConstant, {
    name: "trusted-set-constant.js",
    requiresTrust: true,
    aliases: [
      "trusted-set.js"
    ],
    dependencies: [
      setConstantFn
    ]
  });

  // src/js/resources/replace-argument.js
  function trustedReplaceArgument(propChain = "", argposRaw = "", argraw = "") {
    if (propChain === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("trusted-replace-argument", propChain, argposRaw, argraw);
    const argoffset = parseInt(argposRaw, 10) || 0;
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 3);
    let replacer;
    if (argraw.startsWith("repl:/")) {
      const parsed = parseReplaceFn(argraw.slice(5));
      if (parsed === void 0) {
        return;
      }
      replacer = (arg) => `${arg}`.replace(replacer.re, replacer.replacement);
      Object.assign(replacer, parsed);
    } else if (argraw.startsWith("add:")) {
      const delta = parseFloat(argraw.slice(4));
      if (isNaN(delta)) {
        return;
      }
      replacer = (arg) => Number(arg) + delta;
    } else {
      const value = validateConstantFn(true, argraw, extraArgs);
      replacer = () => value;
    }
    const reCondition = extraArgs.condition ? safe.patternToRegex(extraArgs.condition) : /^/;
    const getArg = (context) => {
      if (argposRaw === "this") {
        return context.thisArg;
      }
      const { callArgs } = context;
      const argpos = argoffset >= 0 ? argoffset : callArgs.length - argoffset;
      if (argpos < 0 || argpos >= callArgs.length) {
        return;
      }
      context.private = { argpos };
      return callArgs[argpos];
    };
    const setArg = (context, value) => {
      if (argposRaw === "this") {
        if (value !== context.thisArg) {
          context.thisArg = value;
        }
      } else if (context.private) {
        context.callArgs[context.private.argpos] = value;
      }
    };
    proxyApplyFn(propChain, (context) => {
      if (argposRaw === "") {
        safe.uboLog(logPrefix, `Arguments:
${context.callArgs.join("\n")}`);
        return context.reflect();
      }
      const argBefore = getArg(context);
      if (extraArgs.condition !== void 0) {
        if (safe.RegExp_test.call(reCondition, argBefore) === false) {
          return context.reflect();
        }
      }
      const argAfter = replacer(argBefore);
      if (argAfter !== argBefore) {
        setArg(context, argAfter);
        safe.uboLog(logPrefix, `Replaced argument:
Before: ${JSON.stringify(argBefore)}
After: ${argAfter}`);
      }
      return context.reflect();
    });
  }
  registerScriptlet(trustedReplaceArgument, {
    name: "trusted-replace-argument.js",
    requiresTrust: true,
    dependencies: [
      parseReplaceFn,
      proxyApplyFn,
      safeSelf,
      validateConstantFn
    ]
  });

  // src/js/resources/spoof-css.js
  function spoofCSS(selector, ...args) {
    if (typeof selector !== "string") {
      return;
    }
    if (selector === "") {
      return;
    }
    const toCamelCase = (s) => s.replace(/-[a-z]/g, (s2) => s2.charAt(1).toUpperCase());
    const propToValueMap = /* @__PURE__ */ new Map();
    const privatePropToValueMap = /* @__PURE__ */ new Map();
    for (let i = 0; i < args.length; i += 2) {
      const prop = toCamelCase(args[i + 0]);
      if (prop === "") {
        break;
      }
      const value = args[i + 1];
      if (typeof value !== "string") {
        break;
      }
      if (prop.charCodeAt(0) === 95) {
        privatePropToValueMap.set(prop, value);
      } else {
        propToValueMap.set(prop, value);
      }
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("spoof-css", selector, ...args);
    const instanceProperties = ["cssText", "length", "parentRule"];
    const spoofStyle = (prop, real) => {
      const normalProp = toCamelCase(prop);
      const shouldSpoof = propToValueMap.has(normalProp);
      const value = shouldSpoof ? propToValueMap.get(normalProp) : real;
      if (shouldSpoof) {
        safe.uboLog(logPrefix, `Spoofing ${prop} to ${value}`);
      }
      return value;
    };
    const cloackFunc = (fn2, thisArg, name2) => {
      const trap = fn2.bind(thisArg);
      Object.defineProperty(trap, "name", { value: name2 });
      Object.defineProperty(trap, "toString", {
        value: () => `function ${name2}() { [native code] }`
      });
      return trap;
    };
    self.getComputedStyle = new Proxy(self.getComputedStyle, {
      apply: function(target, thisArg, args2) {
        if (privatePropToValueMap.has("_debug")) {
          debugger;
        }
        const style = Reflect.apply(target, thisArg, args2);
        const targetElements = new WeakSet(document.querySelectorAll(selector));
        if (targetElements.has(args2[0]) === false) {
          return style;
        }
        const proxiedStyle = new Proxy(style, {
          get(target2, prop) {
            if (typeof target2[prop] === "function") {
              if (prop === "getPropertyValue") {
                return cloackFunc((prop2) => {
                  return spoofStyle(prop2, target2[prop2]);
                }, target2, "getPropertyValue");
              }
              return cloackFunc(target2[prop], target2, prop);
            }
            if (instanceProperties.includes(prop)) {
              return Reflect.get(target2, prop);
            }
            return spoofStyle(prop, Reflect.get(target2, prop));
          },
          getOwnPropertyDescriptor(target2, prop) {
            if (propToValueMap.has(prop)) {
              return {
                configurable: true,
                enumerable: true,
                value: propToValueMap.get(prop),
                writable: true
              };
            }
            return Reflect.getOwnPropertyDescriptor(target2, prop);
          }
        });
        return proxiedStyle;
      },
      get(target, prop) {
        if (prop === "toString") {
          return target.toString.bind(target);
        }
        return Reflect.get(target, prop);
      }
    });
    Element.prototype.getBoundingClientRect = new Proxy(Element.prototype.getBoundingClientRect, {
      apply: function(target, thisArg, args2) {
        if (privatePropToValueMap.has("_debug")) {
          debugger;
        }
        const rect = Reflect.apply(target, thisArg, args2);
        const targetElements = new WeakSet(document.querySelectorAll(selector));
        if (targetElements.has(thisArg) === false) {
          return rect;
        }
        let { x: x2, y: y2, height, width } = rect;
        if (privatePropToValueMap.has("_rectx")) {
          x2 = parseFloat(privatePropToValueMap.get("_rectx"));
        }
        if (privatePropToValueMap.has("_recty")) {
          y2 = parseFloat(privatePropToValueMap.get("_recty"));
        }
        if (privatePropToValueMap.has("_rectw")) {
          width = parseFloat(privatePropToValueMap.get("_rectw"));
        } else if (propToValueMap.has("width")) {
          width = parseFloat(propToValueMap.get("width"));
        }
        if (privatePropToValueMap.has("_recth")) {
          height = parseFloat(privatePropToValueMap.get("_recth"));
        } else if (propToValueMap.has("height")) {
          height = parseFloat(propToValueMap.get("height"));
        }
        return new self.DOMRect(x2, y2, width, height);
      },
      get(target, prop) {
        if (prop === "toString") {
          return target.toString.bind(target);
        }
        return Reflect.get(target, prop);
      }
    });
  }
  registerScriptlet(spoofCSS, {
    name: "spoof-css.js",
    dependencies: [
      safeSelf
    ]
  });

  // src/js/resources/cookie.js
  function getSafeCookieValuesFn() {
    return [
      "accept",
      "reject",
      "accepted",
      "rejected",
      "notaccepted",
      "allow",
      "disallow",
      "deny",
      "allowed",
      "denied",
      "approved",
      "disapproved",
      "checked",
      "unchecked",
      "dismiss",
      "dismissed",
      "enable",
      "disable",
      "enabled",
      "disabled",
      "essential",
      "nonessential",
      "forbidden",
      "forever",
      "hide",
      "hidden",
      "necessary",
      "required",
      "ok",
      "on",
      "off",
      "true",
      "t",
      "false",
      "f",
      "yes",
      "y",
      "no",
      "n",
      "all",
      "none",
      "functional",
      "granted",
      "done",
      "decline",
      "declined",
      "closed",
      "next",
      "mandatory",
      "disagree",
      "agree"
    ];
  }
  registerScriptlet(getSafeCookieValuesFn, {
    name: "get-safe-cookie-values.fn"
  });
  function getAllCookiesFn() {
    const safe = safeSelf();
    return safe.String_split.call(document.cookie, /\s*;\s*/).map((s) => {
      const pos = s.indexOf("=");
      if (pos === 0) {
        return;
      }
      if (pos === -1) {
        return `${s.trim()}=`;
      }
      const key = s.slice(0, pos).trim();
      const value = s.slice(pos + 1).trim();
      return { key, value };
    }).filter((s) => s !== void 0);
  }
  registerScriptlet(getAllCookiesFn, {
    name: "get-all-cookies.fn",
    dependencies: [
      safeSelf
    ]
  });
  function getCookieFn(name2 = "") {
    const safe = safeSelf();
    for (const s of safe.String_split.call(document.cookie, /\s*;\s*/)) {
      const pos = s.indexOf("=");
      if (pos === -1) {
        continue;
      }
      if (s.slice(0, pos) !== name2) {
        continue;
      }
      return s.slice(pos + 1).trim();
    }
  }
  registerScriptlet(getCookieFn, {
    name: "get-cookie.fn",
    dependencies: [
      safeSelf
    ]
  });
  function setCookieFn(trusted = false, name2 = "", value = "", expires = "", path = "", options = {}) {
    if (trusted === false && /[^!#$%&'*+\-.0-9A-Z[\]^_`a-z|~]/.test(name2)) {
      name2 = encodeURIComponent(name2);
    }
    if (/[^ -:<-[\]-~]/.test(value)) {
      value = encodeURIComponent(value);
    }
    const cookieBefore = getCookieFn(name2);
    if (cookieBefore !== void 0 && options.dontOverwrite) {
      return;
    }
    if (cookieBefore === value && options.reload) {
      return;
    }
    const cookieParts = [name2, "=", value];
    if (expires !== "") {
      cookieParts.push("; expires=", expires);
    }
    if (path === "") {
      path = "/";
    } else if (path === "none") {
      path = "";
    }
    if (path !== "" && path !== "/") {
      return;
    }
    if (path === "/") {
      cookieParts.push("; path=/");
    }
    if (trusted) {
      if (options.domain) {
        let domain = options.domain;
        if (/^\/.+\//.test(domain)) {
          const baseURL = new URL(document.baseURI);
          const reDomain = new RegExp(domain.slice(1, -1));
          const match = reDomain.exec(baseURL.hostname);
          domain = match ? match[0] : void 0;
        }
        if (domain) {
          cookieParts.push(`; domain=${domain}`);
        }
      }
      cookieParts.push("; Secure");
    } else if (/^__(Host|Secure)-/.test(name2)) {
      cookieParts.push("; Secure");
    }
    try {
      document.cookie = cookieParts.join("");
    } catch (e) {
      console.warn("[uBR] cookie: document.cookie set failed", e);
    }
    const done = getCookieFn(name2) === value;
    if (done && options.reload) {
      window.location.reload();
    }
    return done;
  }
  registerScriptlet(setCookieFn, {
    name: "set-cookie.fn",
    dependencies: [
      getCookieFn
    ]
  });
  function setCookie(name2 = "", value = "", path = "") {
    if (name2 === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("set-cookie", name2, value, path);
    const normalized = value.toLowerCase();
    const match = /^("?)(.+)\1$/.exec(normalized);
    const unquoted = match && match[2] || normalized;
    const validValues = getSafeCookieValuesFn();
    if (validValues.includes(unquoted) === false) {
      if (/^-?\d+$/.test(unquoted) === false) {
        return;
      }
      const n = parseInt(value, 10) || 0;
      if (n < -32767 || n > 32767) {
        return;
      }
    }
    const done = setCookieFn(
      false,
      name2,
      value,
      "",
      path,
      safe.getExtraArgs(Array.from(arguments), 3)
    );
    if (done) {
      safe.uboLog(logPrefix, "Done");
    }
  }
  registerScriptlet(setCookie, {
    name: "set-cookie.js",
    world: "ISOLATED",
    dependencies: [
      getSafeCookieValuesFn,
      safeSelf,
      setCookieFn
    ]
  });
  function setCookieReload(name2, value, path, ...args) {
    setCookie(name2, value, path, "reload", "1", ...args);
  }
  registerScriptlet(setCookieReload, {
    name: "set-cookie-reload.js",
    world: "ISOLATED",
    dependencies: [
      setCookie
    ]
  });
  function trustedSetCookie(name2 = "", value = "", offsetExpiresSec = "", path = "") {
    if (name2 === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("set-cookie", name2, value, path);
    const time = /* @__PURE__ */ new Date();
    if (value.includes("$now$")) {
      value = value.replaceAll("$now$", time.getTime());
    }
    if (value.includes("$currentDate$")) {
      value = value.replaceAll("$currentDate$", time.toUTCString());
    }
    if (value.includes("$currentISODate$")) {
      value = value.replaceAll("$currentISODate$", time.toISOString());
    }
    let expires = "";
    if (offsetExpiresSec !== "") {
      if (offsetExpiresSec === "1day") {
        time.setDate(time.getDate() + 1);
      } else if (offsetExpiresSec === "1year") {
        time.setFullYear(time.getFullYear() + 1);
      } else {
        if (/^\d+$/.test(offsetExpiresSec) === false) {
          return;
        }
        time.setSeconds(time.getSeconds() + parseInt(offsetExpiresSec, 10));
      }
      expires = time.toUTCString();
    }
    const done = setCookieFn(
      true,
      name2,
      value,
      expires,
      path,
      safeSelf().getExtraArgs(Array.from(arguments), 4)
    );
    if (done) {
      safe.uboLog(logPrefix, "Done");
    }
  }
  registerScriptlet(trustedSetCookie, {
    name: "trusted-set-cookie.js",
    requiresTrust: true,
    world: "ISOLATED",
    dependencies: [
      safeSelf,
      setCookieFn
    ]
  });
  function trustedSetCookieReload(name2, value, offsetExpiresSec, path, ...args) {
    trustedSetCookie(name2, value, offsetExpiresSec, path, "reload", "1", ...args);
  }
  registerScriptlet(trustedSetCookieReload, {
    name: "trusted-set-cookie-reload.js",
    requiresTrust: true,
    world: "ISOLATED",
    dependencies: [
      trustedSetCookie
    ]
  });
  function removeCookie(needle = "") {
    if (typeof needle !== "string") {
      return;
    }
    const safe = safeSelf();
    const reName = safe.patternToRegex(needle);
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 1);
    const throttle = (fn2, ms2 = 500) => {
      if (throttle.timer !== void 0) {
        return;
      }
      throttle.timer = setTimeout(() => {
        throttle.timer = void 0;
        fn2();
      }, ms2);
    };
    const baseURL = new URL(document.baseURI);
    let targetDomain = extraArgs.domain;
    if (targetDomain && /^\/.+\//.test(targetDomain)) {
      const reDomain = new RegExp(targetDomain.slice(1, -1));
      const match = reDomain.exec(baseURL.hostname);
      targetDomain = match ? match[0] : void 0;
    }
    const remove = () => {
      safe.String_split.call(document.cookie, ";").forEach((cookieStr) => {
        const pos = cookieStr.indexOf("=");
        if (pos === -1) {
          return;
        }
        const cookieName = cookieStr.slice(0, pos).trim();
        if (reName.test(cookieName) === false) {
          return;
        }
        const part1 = `${cookieName}=`;
        const part2a = `; domain=${baseURL.hostname}`;
        const part2b = `; domain=.${baseURL.hostname}`;
        let part2c, part2d;
        if (targetDomain) {
          part2c = `; domain=${targetDomain}`;
          part2d = `; domain=.${targetDomain}`;
        } else if (document.domain) {
          const domain = document.domain;
          if (domain !== baseURL.hostname) {
            part2c = `; domain=.${domain}`;
          }
          if (domain.startsWith("www.")) {
            part2d = `; domain=${domain.replace("www", "")}`;
          }
        }
        const part3 = "; path=/";
        const part4 = "; Max-Age=-1000; expires=Thu, 01 Jan 1970 00:00:00 GMT";
        document.cookie = part1 + part4;
        document.cookie = part1 + part2a + part4;
        document.cookie = part1 + part2b + part4;
        document.cookie = part1 + part3 + part4;
        document.cookie = part1 + part2a + part3 + part4;
        document.cookie = part1 + part2b + part3 + part4;
        if (part2c !== void 0) {
          document.cookie = part1 + part2c + part3 + part4;
        }
        if (part2d !== void 0) {
          document.cookie = part1 + part2d + part3 + part4;
        }
      });
    };
    remove();
    window.addEventListener("beforeunload", remove);
    if (typeof extraArgs.when !== "string") {
      return;
    }
    const supportedEventTypes = ["scroll", "keydown"];
    const eventTypes = safe.String_split.call(extraArgs.when, /\s/);
    for (const type of eventTypes) {
      if (supportedEventTypes.includes(type) === false) {
        continue;
      }
      document.addEventListener(type, () => {
        throttle(remove);
      }, { passive: true });
    }
  }
  registerScriptlet(removeCookie, {
    name: "remove-cookie.js",
    aliases: [
      "cookie-remover.js"
    ],
    world: "ISOLATED",
    dependencies: [
      safeSelf
    ]
  });

  // src/js/resources/localstorage.js
  function getAllLocalStorageFn(which = "localStorage") {
    const storage2 = self[which];
    const out = [];
    for (let i = 0; i < storage2.length; i++) {
      const key = storage2.key(i);
      const value = storage2.getItem(key);
      return { key, value };
    }
    return out;
  }
  registerScriptlet(getAllLocalStorageFn, {
    name: "get-all-local-storage.fn"
  });
  function setLocalStorageItemFn(which = "local", trusted = false, key = "", value = "", options = {}) {
    if (key === "") {
      return;
    }
    if (value === "emptyArr") {
      value = "[]";
    } else if (value === "emptyObj") {
      value = "{}";
    }
    const trustedValues = [
      "",
      "undefined",
      "null",
      "{}",
      "[]",
      '""',
      "$remove$",
      ...getSafeCookieValuesFn()
    ];
    if (trusted) {
      if (value.includes("$now$")) {
        value = value.replaceAll("$now$", Date.now());
      }
      if (value.includes("$currentDate$")) {
        value = value.replaceAll("$currentDate$", `${Date()}`);
      }
      if (value.includes("$currentISODate$")) {
        value = value.replaceAll("$currentISODate$", (/* @__PURE__ */ new Date()).toISOString());
      }
    } else {
      const normalized = value.toLowerCase();
      const match = /^("?)(.+)\1$/.exec(normalized);
      const unquoted = match && match[2] || normalized;
      if (trustedValues.includes(unquoted) === false) {
        if (/^-?\d+$/.test(unquoted) === false) {
          return;
        }
        const n = parseInt(unquoted, 10) || 0;
        if (n < -32767 || n > 32767) {
          return;
        }
      }
    }
    let modified = false;
    try {
      const storage2 = self[`${which}Storage`];
      if (value === "$remove$") {
        const safe = safeSelf();
        const pattern = safe.patternToRegex(key, void 0, true);
        const toRemove = [];
        for (let i = 0, n = storage2.length; i < n; i++) {
          const key2 = storage2.key(i);
          if (pattern.test(key2)) {
            toRemove.push(key2);
          }
        }
        modified = toRemove.length !== 0;
        for (const key2 of toRemove) {
          storage2.removeItem(key2);
        }
      } else {
        const before = storage2.getItem(key);
        const after = `${value}`;
        modified = after !== before;
        if (modified) {
          storage2.setItem(key, after);
        }
      }
    } catch (e) {
      console.warn("[uBR] localstorage: setItem or conversion failed", e);
    }
    if (modified && typeof options.reload === "number") {
      setTimeout(() => {
        window.location.reload();
      }, options.reload);
    }
  }
  registerScriptlet(setLocalStorageItemFn, {
    name: "set-local-storage-item.fn",
    dependencies: [
      getSafeCookieValuesFn,
      safeSelf
    ]
  });
  function removeCacheStorageItem(cacheNamePattern = "", requestPattern = "") {
    if (cacheNamePattern === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("remove-cache-storage-item", cacheNamePattern, requestPattern);
    const cacheStorage2 = self.caches;
    if (cacheStorage2 instanceof Object === false) {
      return;
    }
    const reCache = safe.patternToRegex(cacheNamePattern, void 0, true);
    const reRequest = safe.patternToRegex(requestPattern, void 0, true);
    cacheStorage2.keys().then((cacheNames) => {
      for (const cacheName of cacheNames) {
        if (reCache.test(cacheName) === false) {
          continue;
        }
        if (requestPattern === "") {
          cacheStorage2.delete(cacheName).then((result) => {
            if (safe.logLevel > 1) {
              safe.uboLog(logPrefix, `Deleting ${cacheName}`);
            }
            if (result !== true) {
              return;
            }
            safe.uboLog(logPrefix, `Deleted ${cacheName}: ${result}`);
          });
          continue;
        }
        cacheStorage2.open(cacheName).then((cache) => {
          cache.keys().then((requests) => {
            for (const request of requests) {
              if (reRequest.test(request.url) === false) {
                continue;
              }
              if (safe.logLevel > 1) {
                safe.uboLog(logPrefix, `Deleting ${cacheName}/${request.url}`);
              }
              cache.delete(request).then((result) => {
                if (result !== true) {
                  return;
                }
                safe.uboLog(logPrefix, `Deleted ${cacheName}/${request.url}: ${result}`);
              });
            }
          });
        });
      }
    });
  }
  registerScriptlet(removeCacheStorageItem, {
    name: "remove-cache-storage-item.fn",
    world: "ISOLATED",
    dependencies: [
      safeSelf
    ]
  });
  function setLocalStorageItem(key = "", value = "") {
    const safe = safeSelf();
    const options = safe.getExtraArgs(Array.from(arguments), 2);
    setLocalStorageItemFn("local", false, key, value, options);
  }
  registerScriptlet(setLocalStorageItem, {
    name: "set-local-storage-item.js",
    world: "ISOLATED",
    dependencies: [
      safeSelf,
      setLocalStorageItemFn
    ]
  });
  function setSessionStorageItem(key = "", value = "") {
    const safe = safeSelf();
    const options = safe.getExtraArgs(Array.from(arguments), 2);
    setLocalStorageItemFn("session", false, key, value, options);
  }
  registerScriptlet(setSessionStorageItem, {
    name: "set-session-storage-item.js",
    world: "ISOLATED",
    dependencies: [
      safeSelf,
      setLocalStorageItemFn
    ]
  });
  function trustedSetLocalStorageItem(key = "", value = "") {
    const safe = safeSelf();
    const options = safe.getExtraArgs(Array.from(arguments), 2);
    setLocalStorageItemFn("local", true, key, value, options);
  }
  registerScriptlet(trustedSetLocalStorageItem, {
    name: "trusted-set-local-storage-item.js",
    requiresTrust: true,
    world: "ISOLATED",
    dependencies: [
      safeSelf,
      setLocalStorageItemFn
    ]
  });
  function trustedSetSessionStorageItem(key = "", value = "") {
    const safe = safeSelf();
    const options = safe.getExtraArgs(Array.from(arguments), 2);
    setLocalStorageItemFn("session", true, key, value, options);
  }
  registerScriptlet(trustedSetSessionStorageItem, {
    name: "trusted-set-session-storage-item.js",
    requiresTrust: true,
    world: "ISOLATED",
    dependencies: [
      safeSelf,
      setLocalStorageItemFn
    ]
  });

  // src/js/resources/scriptlets.js
  var builtinScriptlets = registeredScriptlets;
  builtinScriptlets.push({
    name: "should-debug.fn",
    fn: shouldDebug
  });
  function shouldDebug(details) {
    if (details instanceof Object === false) {
      return false;
    }
    return scriptletGlobals.canDebug && details.debug;
  }
  builtinScriptlets.push({
    name: "abort-current-script.fn",
    fn: abortCurrentScriptFn,
    dependencies: [
      "get-exception-token.fn",
      "safe-self.fn",
      "should-debug.fn"
    ]
  });
  function abortCurrentScriptFn(target = "", needle = "", context = "") {
    if (typeof target !== "string") {
      return;
    }
    if (target === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("abort-current-script", target, needle, context);
    const reNeedle = safe.patternToRegex(needle);
    const reContext = safe.patternToRegex(context);
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 3);
    const thisScript = document.currentScript;
    const chain = safe.String_split.call(target, ".");
    let owner = window;
    let prop;
    for (; ; ) {
      prop = chain.shift();
      if (chain.length === 0) {
        break;
      }
      if (prop in owner === false) {
        break;
      }
      owner = owner[prop];
      if (owner instanceof Object === false) {
        return;
      }
    }
    let value;
    let desc = Object.getOwnPropertyDescriptor(owner, prop);
    if (desc instanceof Object === false || desc.get instanceof Function === false) {
      value = owner[prop];
      desc = void 0;
    }
    const debug = shouldDebug(extraArgs);
    const exceptionToken = getExceptionTokenFn();
    const scriptTexts = /* @__PURE__ */ new WeakMap();
    const textContentGetter = Object.getOwnPropertyDescriptor(Node.prototype, "textContent").get;
    const getScriptText = (elem) => {
      let text = textContentGetter.call(elem);
      if (text.trim() !== "") {
        return text;
      }
      if (scriptTexts.has(elem)) {
        return scriptTexts.get(elem);
      }
      const [, mime, content] = /^data:([^,]*),(.+)$/.exec(elem.src.trim()) || ["", "", ""];
      try {
        switch (true) {
          case mime.endsWith(";base64"):
            text = self.atob(content);
            break;
          default:
            text = self.decodeURIComponent(content);
            break;
        }
      } catch (e) {
        console.warn("[uBR] scriptlets: decode content failed", e);
      }
      scriptTexts.set(elem, text);
      return text;
    };
    const validate = () => {
      const e = document.currentScript;
      if (e instanceof HTMLScriptElement === false) {
        return;
      }
      if (e === thisScript) {
        return;
      }
      if (context !== "" && reContext.test(e.src) === false) {
        if (debug === "nomatch" || debug === "all") {
          debugger;
        }
        return;
      }
      if (safe.logLevel > 1 && context !== "") {
        safe.uboLog(logPrefix, `Matched src
${e.src}`);
      }
      const scriptText = getScriptText(e);
      if (reNeedle.test(scriptText) === false) {
        if (debug === "nomatch" || debug === "all") {
          debugger;
        }
        return;
      }
      if (safe.logLevel > 1) {
        safe.uboLog(logPrefix, `Matched text
${scriptText}`);
      }
      if (debug === "match" || debug === "all") {
        debugger;
      }
      safe.uboLog(logPrefix, "Aborted");
      throw new ReferenceError(exceptionToken);
    };
    if (debug === "install") {
      debugger;
    }
    try {
      Object.defineProperty(owner, prop, {
        get: function() {
          validate();
          return desc instanceof Object ? desc.get.call(owner) : value;
        },
        set: function(a) {
          validate();
          if (desc instanceof Object) {
            desc.set.call(owner, a);
          } else {
            value = a;
          }
        }
      });
    } catch (ex) {
      safe.uboErr(logPrefix, `Error: ${ex}`);
    }
  }
  builtinScriptlets.push({
    name: "replace-node-text.fn",
    fn: replaceNodeTextFn,
    dependencies: [
      "get-random-token.fn",
      "run-at.fn",
      "safe-self.fn"
    ]
  });
  function replaceNodeTextFn(nodeName = "", pattern = "", replacement = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("replace-node-text.fn", ...Array.from(arguments));
    const reNodeName = safe.patternToRegex(nodeName, "i", true);
    const rePattern = safe.patternToRegex(pattern, "gms");
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 3);
    const reIncludes = extraArgs.includes || extraArgs.condition ? safe.patternToRegex(extraArgs.includes || extraArgs.condition, "ms") : null;
    const reExcludes = extraArgs.excludes ? safe.patternToRegex(extraArgs.excludes, "ms") : null;
    const stop = (takeRecord = true) => {
      if (takeRecord) {
        handleMutations(observer.takeRecords());
      }
      observer.disconnect();
      if (safe.logLevel > 1) {
        safe.uboLog(logPrefix, "Quitting");
      }
    };
    const textContentFactory = (() => {
      const out = { createScript: (s) => s };
      const { trustedTypes: tt2 } = self;
      if (tt2 instanceof Object) {
        if (typeof tt2.getPropertyType === "function") {
          if (tt2.getPropertyType("script", "textContent") === "TrustedScript") {
            return tt2.createPolicy(getRandomTokenFn(), out);
          }
        }
      }
      return out;
    })();
    let sedCount = extraArgs.sedCount || 0;
    const handleNode = (node) => {
      const before = node.textContent;
      if (reIncludes) {
        reIncludes.lastIndex = 0;
        if (safe.RegExp_test.call(reIncludes, before) === false) {
          return true;
        }
      }
      if (reExcludes) {
        reExcludes.lastIndex = 0;
        if (safe.RegExp_test.call(reExcludes, before)) {
          return true;
        }
      }
      rePattern.lastIndex = 0;
      if (safe.RegExp_test.call(rePattern, before) === false) {
        return true;
      }
      rePattern.lastIndex = 0;
      const after = pattern !== "" ? before.replace(rePattern, replacement) : replacement;
      node.textContent = node.nodeName === "SCRIPT" ? textContentFactory.createScript(after) : after;
      if (safe.logLevel > 1) {
        safe.uboLog(logPrefix, `Text before:
${before.trim()}`);
      }
      safe.uboLog(logPrefix, `Text after:
${after.trim()}`);
      return sedCount === 0 || (sedCount -= 1) !== 0;
    };
    const handleMutations = (mutations) => {
      for (const mutation of mutations) {
        for (const node of mutation.addedNodes) {
          if (reNodeName.test(node.nodeName) === false) {
            continue;
          }
          if (handleNode(node)) {
            continue;
          }
          stop(false);
          return;
        }
      }
    };
    const observer = new MutationObserver(handleMutations);
    observer.observe(document, { childList: true, subtree: true });
    if (document.documentElement) {
      const treeWalker = document.createTreeWalker(
        document.documentElement,
        NodeFilter.SHOW_ELEMENT | NodeFilter.SHOW_TEXT
      );
      let count = 0;
      for (; ; ) {
        const node = treeWalker.nextNode();
        count += 1;
        if (node === null) {
          break;
        }
        if (reNodeName.test(node.nodeName) === false) {
          continue;
        }
        if (node === document.currentScript) {
          continue;
        }
        if (handleNode(node)) {
          continue;
        }
        stop();
        break;
      }
      safe.uboLog(logPrefix, `${count} nodes present before installing mutation observer`);
    }
    if (extraArgs.stay) {
      return;
    }
    runAt(() => {
      const quitAfter = extraArgs.quitAfter || 0;
      if (quitAfter !== 0) {
        setTimeout(() => {
          stop();
        }, quitAfter);
      } else {
        stop();
      }
    }, "interactive");
  }
  builtinScriptlets.push({
    name: "replace-fetch-response.fn",
    fn: replaceFetchResponseFn,
    dependencies: [
      "collate-fetch-arguments.fn",
      "match-object-properties.fn",
      "parse-properties-to-match.fn",
      "safe-self.fn"
    ]
  });
  function replaceFetchResponseFn(trusted = false, pattern = "", replacement = "", propsToMatch = "") {
    if (trusted !== true) {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("replace-fetch-response", pattern, replacement, propsToMatch);
    if (pattern === "*") {
      pattern = ".*";
    }
    const rePattern = safe.patternToRegex(pattern);
    const propNeedles = parsePropertiesToMatchFn(propsToMatch, "url");
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 4);
    const reIncludes = extraArgs.includes ? safe.patternToRegex(extraArgs.includes) : null;
    self.fetch = new Proxy(self.fetch, {
      apply: function(target, thisArg, args) {
        const fetchPromise = Reflect.apply(target, thisArg, args);
        if (pattern === "") {
          return fetchPromise;
        }
        if (propNeedles.size !== 0) {
          const props = collateFetchArgumentsFn(...args);
          const matched = matchObjectPropertiesFn(propNeedles, props);
          if (matched === void 0) {
            return fetchPromise;
          }
          if (safe.logLevel > 1) {
            safe.uboLog(logPrefix, `Matched "propsToMatch":
	${matched.join("\n	")}`);
          }
        }
        return fetchPromise.then((responseBefore) => {
          const response = responseBefore.clone();
          return response.text().then((textBefore) => {
            if (reIncludes && reIncludes.test(textBefore) === false) {
              return responseBefore;
            }
            const textAfter = textBefore.replace(rePattern, replacement);
            if (textAfter === textBefore) {
              return responseBefore;
            }
            safe.uboLog(logPrefix, "Replaced");
            const responseAfter = new Response(textAfter, {
              status: responseBefore.status,
              statusText: responseBefore.statusText,
              headers: responseBefore.headers
            });
            Object.defineProperties(responseAfter, {
              ok: { value: responseBefore.ok },
              redirected: { value: responseBefore.redirected },
              type: { value: responseBefore.type },
              url: { value: responseBefore.url }
            });
            return responseAfter;
          }).catch((reason) => {
            safe.uboErr(logPrefix, reason);
            return responseBefore;
          });
        }).catch((reason) => {
          safe.uboErr(logPrefix, reason);
          return fetchPromise;
        });
      }
    });
  }
  builtinScriptlets.push({
    name: "abort-current-script.js",
    aliases: [
      "acs.js",
      "abort-current-inline-script.js",
      "acis.js"
    ],
    fn: abortCurrentScript,
    dependencies: [
      "abort-current-script.fn",
      "run-at-html-element.fn"
    ]
  });
  function abortCurrentScript(...args) {
    runAtHtmlElementFn(() => {
      abortCurrentScriptFn(...args);
    });
  }
  builtinScriptlets.push({
    name: "abort-on-property-read.js",
    aliases: [
      "aopr.js"
    ],
    fn: abortOnPropertyRead,
    dependencies: [
      "get-exception-token.fn",
      "safe-self.fn"
    ]
  });
  function abortOnPropertyRead(chain = "") {
    if (typeof chain !== "string") {
      return;
    }
    if (chain === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("abort-on-property-read", chain);
    const exceptionToken = getExceptionTokenFn();
    const abort = function() {
      safe.uboLog(logPrefix, "Aborted");
      throw new ReferenceError(exceptionToken);
    };
    const makeProxy = function(owner2, chain2) {
      const pos = chain2.indexOf(".");
      if (pos === -1) {
        const desc2 = Object.getOwnPropertyDescriptor(owner2, chain2);
        if (!desc2 || desc2.get !== abort) {
          Object.defineProperty(owner2, chain2, {
            get: abort,
            set: function() {
            }
          });
        }
        return;
      }
      const prop = chain2.slice(0, pos);
      let v2 = owner2[prop];
      chain2 = chain2.slice(pos + 1);
      if (v2) {
        makeProxy(v2, chain2);
        return;
      }
      const desc = Object.getOwnPropertyDescriptor(owner2, prop);
      if (desc && desc.set !== void 0) {
        return;
      }
      Object.defineProperty(owner2, prop, {
        get: function() {
          return v2;
        },
        set: function(a) {
          v2 = a;
          if (a instanceof Object) {
            makeProxy(a, chain2);
          }
        }
      });
    };
    const owner = window;
    makeProxy(owner, chain);
  }
  builtinScriptlets.push({
    name: "abort-on-property-write.js",
    aliases: [
      "aopw.js"
    ],
    fn: abortOnPropertyWrite,
    dependencies: [
      "get-exception-token.fn",
      "safe-self.fn"
    ]
  });
  function abortOnPropertyWrite(prop = "") {
    if (typeof prop !== "string") {
      return;
    }
    if (prop === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("abort-on-property-write", prop);
    const exceptionToken = getExceptionTokenFn();
    let owner = window;
    for (; ; ) {
      const pos = prop.indexOf(".");
      if (pos === -1) {
        break;
      }
      owner = owner[prop.slice(0, pos)];
      if (owner instanceof Object === false) {
        return;
      }
      prop = prop.slice(pos + 1);
    }
    delete owner[prop];
    Object.defineProperty(owner, prop, {
      set: function() {
        safe.uboLog(logPrefix, "Aborted");
        throw new ReferenceError(exceptionToken);
      }
    });
  }
  builtinScriptlets.push({
    name: "adjust-setInterval.js",
    aliases: [
      "nano-setInterval-booster.js",
      "nano-sib.js"
    ],
    fn: adjustSetInterval,
    dependencies: [
      "safe-self.fn"
    ]
  });
  function adjustSetInterval(needleArg = "", delayArg = "", boostArg = "") {
    if (typeof needleArg !== "string") {
      return;
    }
    const safe = safeSelf();
    const reNeedle = safe.patternToRegex(needleArg);
    let delay3 = delayArg !== "*" ? parseInt(delayArg, 10) : -1;
    if (isNaN(delay3) || isFinite(delay3) === false) {
      delay3 = 1e3;
    }
    let boost = parseFloat(boostArg);
    boost = isNaN(boost) === false && isFinite(boost) ? Math.min(Math.max(boost, 1e-3), 50) : 0.05;
    self.setInterval = new Proxy(self.setInterval, {
      apply: function(target, thisArg, args) {
        const [a, b2] = args;
        if ((delay3 === -1 || b2 === delay3) && reNeedle.test(a.toString())) {
          args[1] = b2 * boost;
        }
        return target.apply(thisArg, args);
      }
    });
  }
  builtinScriptlets.push({
    name: "adjust-setTimeout.js",
    aliases: [
      "nano-setTimeout-booster.js",
      "nano-stb.js"
    ],
    fn: adjustSetTimeout,
    dependencies: [
      "safe-self.fn"
    ]
  });
  function adjustSetTimeout(needleArg = "", delayArg = "", boostArg = "") {
    if (typeof needleArg !== "string") {
      return;
    }
    const safe = safeSelf();
    const reNeedle = safe.patternToRegex(needleArg);
    let delay3 = delayArg !== "*" ? parseInt(delayArg, 10) : -1;
    if (isNaN(delay3) || isFinite(delay3) === false) {
      delay3 = 1e3;
    }
    let boost = parseFloat(boostArg);
    boost = isNaN(boost) === false && isFinite(boost) ? Math.min(Math.max(boost, 1e-3), 50) : 0.05;
    self.setTimeout = new Proxy(self.setTimeout, {
      apply: function(target, thisArg, args) {
        const [a, b2] = args;
        if ((delay3 === -1 || b2 === delay3) && reNeedle.test(a.toString())) {
          args[1] = b2 * boost;
        }
        return target.apply(thisArg, args);
      }
    });
  }
  builtinScriptlets.push({
    name: "prevent-refresh.js",
    aliases: [
      "refresh-defuser.js"
    ],
    fn: preventRefresh,
    world: "ISOLATED",
    dependencies: [
      "safe-self.fn"
    ]
  });
  function preventRefresh(delay3 = "") {
    if (typeof delay3 !== "string") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("prevent-refresh", delay3);
    const stop = (content) => {
      window.stop();
      safe.uboLog(logPrefix, `Prevented "${content}"`);
    };
    const defuse = () => {
      const meta = document.querySelector('meta[http-equiv="refresh" i][content]');
      if (meta === null) {
        return;
      }
      const content = meta.getAttribute("content") || "";
      const ms2 = delay3 === "" ? Math.max(parseFloat(content) || 0, 0) * 500 : 0;
      if (ms2 === 0) {
        stop(content);
      } else {
        setTimeout(() => {
          stop(content);
        }, ms2);
      }
    };
    self.addEventListener("load", defuse, { capture: true, once: true });
  }
  builtinScriptlets.push({
    name: "remove-class.js",
    aliases: [
      "rc.js"
    ],
    fn: removeClass,
    world: "ISOLATED",
    dependencies: [
      "run-at.fn",
      "safe-self.fn"
    ]
  });
  function removeClass(rawToken = "", rawSelector = "", behavior = "") {
    if (typeof rawToken !== "string") {
      return;
    }
    if (rawToken === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("remove-class", rawToken, rawSelector, behavior);
    const tokens = safe.String_split.call(rawToken, /\s*\|\s*/);
    const selector = tokens.map((a) => `${rawSelector}.${CSS.escape(a)}`).join(",");
    if (safe.logLevel > 1) {
      safe.uboLog(logPrefix, `Target selector:
	${selector}`);
    }
    const mustStay = /\bstay\b/.test(behavior);
    let timer;
    const rmclass = () => {
      timer = void 0;
      try {
        const nodes = document.querySelectorAll(selector);
        for (const node of nodes) {
          node.classList.remove(...tokens);
          safe.uboLog(logPrefix, "Removed class(es)");
        }
      } catch (e) {
        console.warn("[uBR] scriptlets: rmclass querySelectorAll failed", e);
      }
      if (mustStay) {
        return;
      }
      if (document.readyState !== "complete") {
        return;
      }
      observer.disconnect();
    };
    const mutationHandler = (mutations) => {
      if (timer !== void 0) {
        return;
      }
      let skip = true;
      for (let i = 0; i < mutations.length && skip; i++) {
        const { type, addedNodes, removedNodes } = mutations[i];
        if (type === "attributes") {
          skip = false;
        }
        for (let j2 = 0; j2 < addedNodes.length && skip; j2++) {
          if (addedNodes[j2].nodeType === 1) {
            skip = false;
            break;
          }
        }
        for (let j2 = 0; j2 < removedNodes.length && skip; j2++) {
          if (removedNodes[j2].nodeType === 1) {
            skip = false;
            break;
          }
        }
      }
      if (skip) {
        return;
      }
      timer = safe.onIdle(rmclass, { timeout: 67 });
    };
    const observer = new MutationObserver(mutationHandler);
    const start = () => {
      rmclass();
      observer.observe(document, {
        attributes: true,
        attributeFilter: ["class"],
        childList: true,
        subtree: true
      });
    };
    runAt(() => {
      start();
    }, /\bcomplete\b/.test(behavior) ? "idle" : "loading");
  }
  builtinScriptlets.push({
    name: "webrtc-if.js",
    fn: webrtcIf,
    dependencies: [
      "safe-self.fn"
    ]
  });
  function webrtcIf(good = "") {
    if (typeof good !== "string") {
      return;
    }
    const safe = safeSelf();
    const reGood = safe.patternToRegex(good);
    const rtcName = window.RTCPeerConnection ? "RTCPeerConnection" : window.webkitRTCPeerConnection ? "webkitRTCPeerConnection" : "";
    if (rtcName === "") {
      return;
    }
    const log = console.log.bind(console);
    const neuteredPeerConnections = /* @__PURE__ */ new WeakSet();
    const isGoodConfig = function(instance, config) {
      if (neuteredPeerConnections.has(instance)) {
        return false;
      }
      if (config instanceof Object === false) {
        return true;
      }
      if (Array.isArray(config.iceServers) === false) {
        return true;
      }
      for (const server of config.iceServers) {
        const urls = typeof server.urls === "string" ? [server.urls] : server.urls;
        if (Array.isArray(urls)) {
          for (const url of urls) {
            if (reGood.test(url)) {
              return true;
            }
          }
        }
        if (typeof server.username === "string") {
          if (reGood.test(server.username)) {
            return true;
          }
        }
        if (typeof server.credential === "string") {
          if (reGood.test(server.credential)) {
            return true;
          }
        }
      }
      neuteredPeerConnections.add(instance);
      return false;
    };
    const peerConnectionCtor = window[rtcName];
    const peerConnectionProto = peerConnectionCtor.prototype;
    peerConnectionProto.createDataChannel = new Proxy(peerConnectionProto.createDataChannel, {
      apply: function(target, thisArg, args) {
        if (isGoodConfig(target, args[1]) === false) {
          log("uBO:", args[1]);
          return Reflect.apply(target, thisArg, args.slice(0, 1));
        }
        return Reflect.apply(target, thisArg, args);
      }
    });
    window[rtcName] = new Proxy(peerConnectionCtor, {
      construct: function(target, args) {
        if (isGoodConfig(target, args[0]) === false) {
          log("uBO:", args[0]);
          return Reflect.construct(target);
        }
        return Reflect.construct(target, args);
      }
    });
  }
  builtinScriptlets.push({
    name: "prevent-window-open.js",
    aliases: [
      "nowoif.js",
      "no-window-open-if.js",
      "window.open-defuser.js"
    ],
    fn: noWindowOpenIf,
    dependencies: [
      "proxy-apply.fn",
      "safe-self.fn"
    ]
  });
  function noWindowOpenIf(pattern = "", delay3 = "", decoy = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("no-window-open-if", pattern, delay3, decoy);
    const targetMatchResult = pattern.startsWith("!") === false;
    if (targetMatchResult === false) {
      pattern = pattern.slice(1);
    }
    const rePattern = safe.patternToRegex(pattern);
    const autoRemoveAfter = (parseFloat(delay3) || 0) * 1e3;
    const setTimeout2 = self.setTimeout;
    const createDecoy = function(tag, urlProp, url) {
      const decoyElem = document.createElement(tag);
      decoyElem[urlProp] = url;
      decoyElem.style.setProperty("height", "1px", "important");
      decoyElem.style.setProperty("position", "fixed", "important");
      decoyElem.style.setProperty("top", "-1px", "important");
      decoyElem.style.setProperty("width", "1px", "important");
      document.body.appendChild(decoyElem);
      setTimeout2(() => {
        decoyElem.remove();
      }, autoRemoveAfter);
      return decoyElem;
    };
    const noopFunc = function() {
    };
    proxyApplyFn("open", (context) => {
      if (pattern === "debug" && safe.logLevel !== 0) {
        debugger;
        return context.reflect();
      }
      const { callArgs } = context;
      const haystack = callArgs.join(" ");
      if (rePattern.test(haystack) !== targetMatchResult) {
        if (safe.logLevel > 1) {
          safe.uboLog(logPrefix, `Allowed (${callArgs.join(", ")})`);
        }
        return context.reflect();
      }
      safe.uboLog(logPrefix, `Prevented (${callArgs.join(", ")})`);
      if (delay3 === "") {
        return null;
      }
      if (decoy === "blank") {
        callArgs[0] = "about:blank";
        const r = context.reflect();
        setTimeout2(() => {
          r.close();
        }, autoRemoveAfter);
        return r;
      }
      const decoyElem = decoy === "obj" ? createDecoy("object", "data", ...callArgs) : createDecoy("iframe", "src", ...callArgs);
      let popup = decoyElem.contentWindow;
      if (typeof popup === "object" && popup !== null) {
        Object.defineProperty(popup, "closed", { value: false });
      } else {
        popup = new Proxy(self, {
          get: function(target, prop, ...args) {
            if (prop === "closed") {
              return false;
            }
            const r = Reflect.get(target, prop, ...args);
            if (typeof r === "function") {
              return noopFunc;
            }
            return r;
          },
          set: function(...args) {
            return Reflect.set(...args);
          }
        });
      }
      if (safe.logLevel !== 0) {
        popup = new Proxy(popup, {
          get: function(target, prop, ...args) {
            const r = Reflect.get(target, prop, ...args);
            safe.uboLog(logPrefix, `popup / get ${prop} === ${r}`);
            if (typeof r === "function") {
              return (...args2) => {
                return r.call(target, ...args2);
              };
            }
            return r;
          },
          set: function(target, prop, value, ...args) {
            safe.uboLog(logPrefix, `popup / set ${prop} = ${value}`);
            return Reflect.set(target, prop, value, ...args);
          }
        });
      }
      return popup;
    });
  }
  builtinScriptlets.push({
    name: "close-window.js",
    aliases: [
      "window-close-if.js"
    ],
    fn: closeWindow,
    world: "ISOLATED",
    dependencies: [
      "safe-self.fn"
    ]
  });
  function closeWindow(arg1 = "") {
    if (typeof arg1 !== "string") {
      return;
    }
    const safe = safeSelf();
    let subject = "";
    if (/^\/.*\/$/.test(arg1)) {
      subject = window.location.href;
    } else if (arg1 !== "") {
      subject = `${window.location.pathname}${window.location.search}`;
    }
    try {
      const re2 = safe.patternToRegex(arg1);
      if (re2.test(subject)) {
        window.close();
      }
    } catch (ex) {
      console.log(ex);
    }
  }
  builtinScriptlets.push({
    name: "window.name-defuser.js",
    fn: windowNameDefuser
  });
  function windowNameDefuser() {
    if (window === window.top) {
      window.name = "";
    }
  }
  builtinScriptlets.push({
    name: "overlay-buster.js",
    fn: overlayBuster
  });
  function overlayBuster(allFrames) {
    if (allFrames === "" && window !== window.top) {
      return;
    }
    let tstart;
    const ttl = 3e4;
    let delay3 = 0;
    const delayStep = 50;
    const buster = function() {
      let docEl = document.documentElement, bodyEl = document.body, vw = Math.min(docEl.clientWidth, window.innerWidth), vh2 = Math.min(docEl.clientHeight, window.innerHeight), tol = Math.min(vw, vh2) * 0.05, el2 = document.elementFromPoint(vw / 2, vh2 / 2), style, rect;
      for (; ; ) {
        if (el2 === null || el2.parentNode === null || el2 === bodyEl) {
          break;
        }
        style = window.getComputedStyle(el2);
        if (parseInt(style.zIndex, 10) >= 1e3 || style.position === "fixed") {
          rect = el2.getBoundingClientRect();
          if (rect.left <= tol && rect.top <= tol && vw - rect.right <= tol && vh2 - rect.bottom < tol) {
            el2.parentNode.removeChild(el2);
            tstart = Date.now();
            el2 = document.elementFromPoint(vw / 2, vh2 / 2);
            bodyEl.style.setProperty("overflow", "auto", "important");
            docEl.style.setProperty("overflow", "auto", "important");
            continue;
          }
        }
        el2 = el2.parentNode;
      }
      if (Date.now() - tstart < ttl) {
        delay3 = Math.min(delay3 + delayStep, 1e3);
        setTimeout(buster, delay3);
      }
    };
    const domReady = function(ev) {
      if (ev) {
        document.removeEventListener(ev.type, domReady);
      }
      tstart = Date.now();
      setTimeout(buster, delay3);
    };
    if (document.readyState === "loading") {
      document.addEventListener("DOMContentLoaded", domReady);
    } else {
      domReady();
    }
  }
  builtinScriptlets.push({
    name: "alert-buster.js",
    fn: alertBuster
  });
  function alertBuster() {
    window.alert = new Proxy(window.alert, {
      apply: function(a) {
        console.info(a);
      },
      get(target, prop) {
        if (prop === "toString") {
          return target.toString.bind(target);
        }
        return Reflect.get(target, prop);
      }
    });
  }
  builtinScriptlets.push({
    name: "nowebrtc.js",
    fn: noWebrtc
  });
  function noWebrtc() {
    const rtcName = window.RTCPeerConnection ? "RTCPeerConnection" : window.webkitRTCPeerConnection ? "webkitRTCPeerConnection" : "";
    if (rtcName === "") {
      return;
    }
    const log = console.log.bind(console);
    const pc2 = function(cfg) {
      log("Document tried to create an RTCPeerConnection: %o", cfg);
    };
    const noop = function() {
    };
    pc2.prototype = {
      close: noop,
      createDataChannel: noop,
      createOffer: noop,
      setRemoteDescription: noop,
      toString: function() {
        return "[object RTCPeerConnection]";
      }
    };
    const z = window[rtcName];
    window[rtcName] = pc2.bind(window);
    if (z.prototype) {
      z.prototype.createDataChannel = function() {
        return {
          close: function() {
          },
          send: function() {
          }
        };
      }.bind(null);
    }
  }
  builtinScriptlets.push({
    name: "disable-newtab-links.js",
    fn: disableNewtabLinks
  });
  function disableNewtabLinks() {
    document.addEventListener("click", (ev) => {
      let target = ev.target;
      while (target !== null) {
        if (target.localName === "a" && target.hasAttribute("target")) {
          ev.stopPropagation();
          ev.preventDefault();
          break;
        }
        target = target.parentNode;
      }
    }, { capture: true });
  }
  builtinScriptlets.push({
    name: "xml-prune.js",
    fn: xmlPrune,
    dependencies: [
      "safe-self.fn"
    ]
  });
  function xmlPrune(selector = "", selectorCheck = "", urlPattern = "") {
    if (typeof selector !== "string") {
      return;
    }
    if (selector === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("xml-prune", selector, selectorCheck, urlPattern);
    const reUrl = safe.patternToRegex(urlPattern);
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 3);
    const queryAll = (xmlDoc, selector2) => {
      const isXpath = /^xpath\(.+\)$/.test(selector2);
      if (isXpath === false) {
        return Array.from(xmlDoc.querySelectorAll(selector2));
      }
      const xpr = xmlDoc.evaluate(
        selector2.slice(6, -1),
        xmlDoc,
        null,
        XPathResult.UNORDERED_NODE_SNAPSHOT_TYPE,
        null
      );
      const out = [];
      for (let i = 0; i < xpr.snapshotLength; i++) {
        const node = xpr.snapshotItem(i);
        out.push(node);
      }
      return out;
    };
    const pruneFromDoc = (xmlDoc) => {
      try {
        if (selectorCheck !== "" && xmlDoc.querySelector(selectorCheck) === null) {
          return xmlDoc;
        }
        if (extraArgs.logdoc) {
          const serializer = new XMLSerializer();
          safe.uboLog(logPrefix, `Document is
	${serializer.serializeToString(xmlDoc)}`);
        }
        const items = queryAll(xmlDoc, selector);
        if (items.length === 0) {
          return xmlDoc;
        }
        safe.uboLog(logPrefix, `Removing ${items.length} items`);
        for (const item of items) {
          if (item.nodeType === 1) {
            item.remove();
          } else if (item.nodeType === 2) {
            item.ownerElement.removeAttribute(item.nodeName);
          }
          safe.uboLog(logPrefix, `${item.constructor.name}.${item.nodeName} removed`);
        }
      } catch (ex) {
        safe.uboErr(logPrefix, `Error: ${ex}`);
      }
      return xmlDoc;
    };
    const pruneFromText = (text) => {
      if ((/^\s*</.test(text) && />\s*$/.test(text)) === false) {
        return text;
      }
      try {
        const xmlParser = new DOMParser();
        const xmlDoc = xmlParser.parseFromString(text, "text/xml");
        pruneFromDoc(xmlDoc);
        const serializer = new XMLSerializer();
        text = serializer.serializeToString(xmlDoc);
      } catch (e) {
        console.warn("[uBR] scriptlets: json-prune XML parse failed", e);
      }
      return text;
    };
    const urlFromArg = (arg) => {
      if (typeof arg === "string") {
        return arg;
      }
      if (arg instanceof Request) {
        return arg.url;
      }
      return String(arg);
    };
    self.fetch = new Proxy(self.fetch, {
      apply: function(target, thisArg, args) {
        const fetchPromise = Reflect.apply(target, thisArg, args);
        if (reUrl.test(urlFromArg(args[0])) === false) {
          return fetchPromise;
        }
        return fetchPromise.then((responseBefore) => {
          const response = responseBefore.clone();
          return response.text().then((text) => {
            const responseAfter = new Response(pruneFromText(text), {
              status: responseBefore.status,
              statusText: responseBefore.statusText,
              headers: responseBefore.headers
            });
            Object.defineProperties(responseAfter, {
              ok: { value: responseBefore.ok },
              redirected: { value: responseBefore.redirected },
              type: { value: responseBefore.type },
              url: { value: responseBefore.url }
            });
            return responseAfter;
          }).catch((e) => {
            console.warn("[uBR] scriptlets: json-prune response text failed", e);
            return responseBefore;
          });
        });
      }
    });
    self.XMLHttpRequest.prototype.open = new Proxy(self.XMLHttpRequest.prototype.open, {
      apply: async (target, thisArg, args) => {
        if (reUrl.test(urlFromArg(args[1])) === false) {
          return Reflect.apply(target, thisArg, args);
        }
        thisArg.addEventListener("readystatechange", () => {
          if (thisArg.readyState !== 4) {
            return;
          }
          const type = thisArg.responseType;
          if (type === "document" || type === "" && thisArg.responseXML instanceof XMLDocument) {
            pruneFromDoc(thisArg.responseXML);
            const serializer = new XMLSerializer();
            const textout = serializer.serializeToString(thisArg.responseXML);
            Object.defineProperty(thisArg, "responseText", { value: textout });
            if (typeof thisArg.response === "string") {
              Object.defineProperty(thisArg, "response", { value: textout });
            }
            return;
          }
          if (type === "text" || type === "" && typeof thisArg.responseText === "string") {
            const textin = thisArg.responseText;
            const textout = pruneFromText(textin);
            if (textout === textin) {
              return;
            }
            Object.defineProperty(thisArg, "response", { value: textout });
            Object.defineProperty(thisArg, "responseText", { value: textout });
            return;
          }
        });
        return Reflect.apply(target, thisArg, args);
      }
    });
  }
  builtinScriptlets.push({
    name: "m3u-prune.js",
    fn: m3uPrune,
    dependencies: [
      "proxy-apply.fn",
      "safe-self.fn"
    ]
  });
  function m3uPrune(m3uPattern = "", urlPattern = "") {
    if (typeof m3uPattern !== "string") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("m3u-prune", m3uPattern, urlPattern);
    const toLog = [];
    const regexFromArg = (arg) => {
      if (arg === "") {
        return /^/;
      }
      const match = /^\/(.+)\/([gms]*)$/.exec(arg);
      if (match !== null) {
        let flags = match[2] || "";
        if (flags.includes("m")) {
          flags += "s";
        }
        return new RegExp(match[1], flags);
      }
      return new RegExp(
        arg.replace(/[.+?^${}()|[\]\\]/g, "\\$&").replace(/\*+/g, ".*?")
      );
    };
    const reM3u = regexFromArg(m3uPattern);
    const reUrl = regexFromArg(urlPattern);
    const pruneSpliceoutBlock = (lines, i) => {
      if (lines[i].startsWith('#EXT-X-CUE:TYPE="SpliceOut"') === false) {
        return false;
      }
      toLog.push(`	${lines[i]}`);
      lines[i] = void 0;
      i += 1;
      if (lines[i].startsWith("#EXT-X-ASSET:CAID")) {
        toLog.push(`	${lines[i]}`);
        lines[i] = void 0;
        i += 1;
      }
      if (lines[i].startsWith("#EXT-X-SCTE35:")) {
        toLog.push(`	${lines[i]}`);
        lines[i] = void 0;
        i += 1;
      }
      if (lines[i].startsWith("#EXT-X-CUE-IN")) {
        toLog.push(`	${lines[i]}`);
        lines[i] = void 0;
        i += 1;
      }
      if (lines[i].startsWith("#EXT-X-SCTE35:")) {
        toLog.push(`	${lines[i]}`);
        lines[i] = void 0;
        i += 1;
      }
      return true;
    };
    const pruneInfBlock = (lines, i) => {
      if (lines[i].startsWith("#EXTINF") === false) {
        return false;
      }
      if (reM3u.test(lines[i + 1]) === false) {
        return false;
      }
      toLog.push("Discarding", `	${lines[i]}, 	${lines[i + 1]}`);
      lines[i] = lines[i + 1] = void 0;
      i += 2;
      if (lines[i].startsWith("#EXT-X-DISCONTINUITY")) {
        toLog.push(`	${lines[i]}`);
        lines[i] = void 0;
        i += 1;
      }
      return true;
    };
    const pruner = (text) => {
      if (/^\s*#EXTM3U/.test(text) === false) {
        return text;
      }
      if (m3uPattern === "") {
        safe.uboLog(` Content:
${text}`);
        return text;
      }
      if (reM3u.multiline) {
        reM3u.lastIndex = 0;
        for (; ; ) {
          const match = reM3u.exec(text);
          if (match === null) {
            break;
          }
          let discard = match[0];
          let before = text.slice(0, match.index);
          if (/^[\n\r]+/.test(discard) === false && /[\n\r]+$/.test(before) === false) {
            const startOfLine = /[^\n\r]+$/.exec(before);
            if (startOfLine !== null) {
              before = before.slice(0, startOfLine.index);
              discard = startOfLine[0] + discard;
            }
          }
          let after = text.slice(match.index + match[0].length);
          if (/[\n\r]+$/.test(discard) === false && /^[\n\r]+/.test(after) === false) {
            const endOfLine = /^[^\n\r]+/.exec(after);
            if (endOfLine !== null) {
              after = after.slice(endOfLine.index);
              discard += discard + endOfLine[0];
            }
          }
          text = `${before.trim()}
${after.trim()}`;
          reM3u.lastIndex = before.length + 1;
          toLog.push("Discarding", ...safe.String_split.call(discard, /\n+/).map((s) => `	${s}`));
          if (reM3u.global === false) {
            break;
          }
        }
        return text;
      }
      const lines = safe.String_split.call(text, /\n\r|\n|\r/);
      for (let i = 0; i < lines.length; i++) {
        if (lines[i] === void 0) {
          continue;
        }
        if (pruneSpliceoutBlock(lines, i)) {
          continue;
        }
        if (pruneInfBlock(lines, i)) {
          continue;
        }
      }
      return lines.filter((l) => l !== void 0).join("\n");
    };
    const urlFromArg = (arg) => {
      if (typeof arg === "string") {
        return arg;
      }
      if (arg instanceof Request) {
        return arg.url;
      }
      return String(arg);
    };
    proxyApplyFn("fetch", async (context) => {
      const args = context.callArgs;
      const fetchPromise = context.reflect();
      if (reUrl.test(urlFromArg(args[0])) === false) {
        return fetchPromise;
      }
      const responseBefore = await fetchPromise;
      const responseClone = responseBefore.clone();
      const textBefore = await responseClone.text();
      const textAfter = pruner(textBefore);
      if (textAfter === textBefore) {
        return responseBefore;
      }
      const responseAfter = new Response(textAfter, {
        status: responseBefore.status,
        statusText: responseBefore.statusText,
        headers: responseBefore.headers
      });
      Object.defineProperties(responseAfter, {
        url: { value: responseBefore.url },
        type: { value: responseBefore.type }
      });
      if (toLog.length !== 0) {
        toLog.unshift(logPrefix);
        safe.uboLog(toLog.join("\n"));
      }
      return responseAfter;
    });
    self.XMLHttpRequest.prototype.open = new Proxy(self.XMLHttpRequest.prototype.open, {
      apply: async (target, thisArg, args) => {
        if (reUrl.test(urlFromArg(args[1])) === false) {
          return Reflect.apply(target, thisArg, args);
        }
        thisArg.addEventListener("readystatechange", () => {
          if (thisArg.readyState !== 4) {
            return;
          }
          const type = thisArg.responseType;
          if (type !== "" && type !== "text") {
            return;
          }
          const textin = thisArg.responseText;
          const textout = pruner(textin);
          if (textout === textin) {
            return;
          }
          Object.defineProperty(thisArg, "response", { value: textout });
          Object.defineProperty(thisArg, "responseText", { value: textout });
          if (toLog.length !== 0) {
            toLog.unshift(logPrefix);
            safe.uboLog(toLog.join("\n"));
          }
        });
        return Reflect.apply(target, thisArg, args);
      }
    });
  }
  builtinScriptlets.push({
    name: "call-nothrow.js",
    fn: callNothrow,
    dependencies: [
      "safe-self.fn"
    ]
  });
  function callNothrow(chain = "") {
    if (typeof chain !== "string") {
      return;
    }
    if (chain === "") {
      return;
    }
    const safe = safeSelf();
    const parts = safe.String_split.call(chain, ".");
    let owner = window, prop;
    for (; ; ) {
      prop = parts.shift();
      if (parts.length === 0) {
        break;
      }
      owner = owner[prop];
      if (owner instanceof Object === false) {
        return;
      }
    }
    if (prop === "") {
      return;
    }
    const fn2 = owner[prop];
    if (typeof fn2 !== "function") {
      return;
    }
    owner[prop] = new Proxy(fn2, {
      apply: function(...args) {
        let r;
        try {
          r = Reflect.apply(...args);
        } catch (e) {
          console.warn("[uBR] scriptlets: proxy-apply fn failed", e);
        }
        return r;
      }
    });
  }
  builtinScriptlets.push({
    name: "remove-node-text.js",
    aliases: [
      "rmnt.js"
    ],
    fn: removeNodeText,
    world: "ISOLATED",
    dependencies: [
      "replace-node-text.fn"
    ]
  });
  function removeNodeText(nodeName, includes, ...extraArgs) {
    replaceNodeTextFn(nodeName, "", "", "includes", includes || "", ...extraArgs);
  }
  builtinScriptlets.push({
    name: "prevent-canvas.js",
    fn: preventCanvas,
    dependencies: [
      "safe-self.fn"
    ]
  });
  function preventCanvas(contextType = "") {
    const safe = safeSelf();
    const pattern = safe.initPattern(contextType, { canNegate: true });
    const proto = globalThis.HTMLCanvasElement.prototype;
    proto.getContext = new Proxy(proto.getContext, {
      apply(target, thisArg, args) {
        if (safe.testPattern(pattern, args[0])) {
          return null;
        }
        return Reflect.apply(target, thisArg, args);
      }
    });
  }
  builtinScriptlets.push({
    name: "multiup.js",
    fn: multiup,
    world: "ISOLATED"
  });
  function multiup() {
    const handler = (ev) => {
      const target = ev.target;
      if (target.matches("button[link]") === false) {
        return;
      }
      const ancestor = target.closest("form");
      if (ancestor === null) {
        return;
      }
      if (ancestor !== target.parentElement) {
        return;
      }
      const link = (target.getAttribute("link") || "").trim();
      if (link === "") {
        return;
      }
      ev.preventDefault();
      ev.stopPropagation();
      document.location.href = link;
    };
    document.addEventListener("click", handler, { capture: true });
  }
  builtinScriptlets.push({
    name: "trusted-replace-node-text.js",
    requiresTrust: true,
    aliases: [
      "trusted-rpnt.js",
      "replace-node-text.js",
      "rpnt.js"
    ],
    fn: replaceNodeText,
    world: "ISOLATED",
    dependencies: [
      "replace-node-text.fn"
    ]
  });
  function replaceNodeText(nodeName, pattern, replacement, ...extraArgs) {
    replaceNodeTextFn(nodeName, pattern, replacement, ...extraArgs);
  }
  builtinScriptlets.push({
    name: "trusted-replace-fetch-response.js",
    requiresTrust: true,
    aliases: [
      "trusted-rpfr.js"
    ],
    fn: trustedReplaceFetchResponse,
    dependencies: [
      "replace-fetch-response.fn"
    ]
  });
  function trustedReplaceFetchResponse(...args) {
    replaceFetchResponseFn(true, ...args);
  }
  builtinScriptlets.push({
    name: "trusted-replace-xhr-response.js",
    requiresTrust: true,
    fn: trustedReplaceXhrResponse,
    dependencies: [
      "match-object-properties.fn",
      "parse-properties-to-match.fn",
      "safe-self.fn"
    ]
  });
  function trustedReplaceXhrResponse(pattern = "", replacement = "", propsToMatch = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("trusted-replace-xhr-response", pattern, replacement, propsToMatch);
    const xhrInstances = /* @__PURE__ */ new WeakMap();
    if (pattern === "*") {
      pattern = ".*";
    }
    const rePattern = safe.patternToRegex(pattern);
    const propNeedles = parsePropertiesToMatchFn(propsToMatch, "url");
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 3);
    const reIncludes = extraArgs.includes ? safe.patternToRegex(extraArgs.includes) : null;
    self.XMLHttpRequest = class extends self.XMLHttpRequest {
      open(method, url, ...args) {
        const outerXhr = this;
        const xhrDetails = { method, url };
        let outcome = "match";
        if (propNeedles.size !== 0) {
          if (matchObjectPropertiesFn(propNeedles, xhrDetails) === void 0) {
            outcome = "nomatch";
          }
        }
        if (outcome === "match") {
          if (safe.logLevel > 1) {
            safe.uboLog(logPrefix, `Matched "propsToMatch"`);
          }
          xhrInstances.set(outerXhr, xhrDetails);
        }
        return super.open(method, url, ...args);
      }
      get response() {
        const innerResponse = super.response;
        const xhrDetails = xhrInstances.get(this);
        if (xhrDetails === void 0) {
          return innerResponse;
        }
        const responseLength = typeof innerResponse === "string" ? innerResponse.length : void 0;
        if (xhrDetails.lastResponseLength !== responseLength) {
          xhrDetails.response = void 0;
          xhrDetails.lastResponseLength = responseLength;
        }
        if (xhrDetails.response !== void 0) {
          return xhrDetails.response;
        }
        if (typeof innerResponse !== "string") {
          return xhrDetails.response = innerResponse;
        }
        if (reIncludes && reIncludes.test(innerResponse) === false) {
          return xhrDetails.response = innerResponse;
        }
        const textBefore = innerResponse;
        const textAfter = textBefore.replace(rePattern, replacement);
        if (textAfter !== textBefore) {
          safe.uboLog(logPrefix, "Match");
        }
        return xhrDetails.response = textAfter;
      }
      get responseText() {
        const response = this.response;
        if (typeof response !== "string") {
          return super.responseText;
        }
        return response;
      }
    };
  }
  builtinScriptlets.push({
    name: "trusted-click-element.js",
    requiresTrust: true,
    fn: trustedClickElement,
    world: "ISOLATED",
    dependencies: [
      "get-all-cookies.fn",
      "get-all-local-storage.fn",
      "run-at-html-element.fn",
      "safe-self.fn"
    ]
  });
  function trustedClickElement(selectors = "", extraMatch = "", delay3 = "") {
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("trusted-click-element", selectors, extraMatch, delay3);
    if (extraMatch !== "") {
      const assertions = safe.String_split.call(extraMatch, ",").map((s) => {
        const pos1 = s.indexOf(":");
        const s1 = pos1 !== -1 ? s.slice(0, pos1) : s;
        const not = s1.startsWith("!");
        const type = not ? s1.slice(1) : s1;
        const s2 = pos1 !== -1 ? s.slice(pos1 + 1).trim() : "";
        if (s2 === "") {
          return;
        }
        const out = { not, type };
        const match = /^\/(.+)\/(i?)$/.exec(s2);
        if (match !== null) {
          out.re = new RegExp(match[1], match[2] || void 0);
          return out;
        }
        const pos2 = s2.indexOf("=");
        const key = pos2 !== -1 ? s2.slice(0, pos2).trim() : s2;
        const value = pos2 !== -1 ? s2.slice(pos2 + 1).trim() : "";
        out.re = new RegExp(`^${safe.escapeRegexChars(key)}=${safe.escapeRegexChars(value)}`);
        return out;
      }).filter((details) => details !== void 0);
      const allCookies = assertions.some((o) => o.type === "cookie") ? getAllCookiesFn() : [];
      const allStorageItems = assertions.some((o) => o.type === "localStorage") ? getAllLocalStorageFn() : [];
      const hasNeedle = (haystack, needle) => {
        for (const { key, value } of haystack) {
          if (needle.test(`${key}=${value}`)) {
            return true;
          }
        }
        return false;
      };
      for (const { not, type, re: re2 } of assertions) {
        switch (type) {
          case "cookie":
            if (hasNeedle(allCookies, re2) === not) {
              return;
            }
            break;
          case "localStorage":
            if (hasNeedle(allStorageItems, re2) === not) {
              return;
            }
            break;
        }
      }
    }
    const getShadowRoot = (elem) => {
      if (elem.openOrClosedShadowRoot) {
        return elem.openOrClosedShadowRoot;
      }
      if (typeof chrome === "object") {
        if (chrome.dom && chrome.dom.openOrClosedShadowRoot) {
          return chrome.dom.openOrClosedShadowRoot(elem);
        }
      }
      return elem.shadowRoot;
    };
    const querySelectorEx = (selector, context = document) => {
      const pos = selector.indexOf(" >>> ");
      if (pos === -1) {
        return context.querySelector(selector);
      }
      const outside = selector.slice(0, pos).trim();
      const inside = selector.slice(pos + 5).trim();
      const elem = context.querySelector(outside);
      if (elem === null) {
        return null;
      }
      const shadowRoot = getShadowRoot(elem);
      return shadowRoot && querySelectorEx(inside, shadowRoot);
    };
    const steps = safe.String_split.call(selectors, /\s*,\s*/).map((a) => {
      if (/^\d+$/.test(a)) {
        return parseInt(a, 10);
      }
      return a;
    });
    if (steps.length === 0) {
      return;
    }
    const clickDelay = parseInt(delay3, 10) || 1;
    for (let i = steps.length - 1; i > 0; i--) {
      if (typeof steps[i] !== "string") {
        continue;
      }
      if (typeof steps[i - 1] !== "string") {
        continue;
      }
      steps.splice(i, 0, clickDelay);
    }
    if (steps.length === 1 && delay3 !== "") {
      steps.unshift(clickDelay);
    }
    if (typeof steps.at(-1) !== "number") {
      steps.push(1e4);
    }
    const waitForTime = (ms2) => {
      return new Promise((resolve) => {
        safe.uboLog(logPrefix, `Waiting for ${ms2} ms`);
        waitForTime.timer = setTimeout(() => {
          waitForTime.timer = void 0;
          resolve();
        }, ms2);
      });
    };
    waitForTime.cancel = () => {
      const { timer } = waitForTime;
      if (timer === void 0) {
        return;
      }
      clearTimeout(timer);
      waitForTime.timer = void 0;
    };
    const waitForElement = (selector) => {
      return new Promise((resolve) => {
        const elem = querySelectorEx(selector);
        if (elem !== null) {
          elem.click();
          resolve();
          return;
        }
        safe.uboLog(logPrefix, `Waiting for ${selector}`);
        const observer = new MutationObserver(() => {
          const elem2 = querySelectorEx(selector);
          if (elem2 === null) {
            return;
          }
          waitForElement.cancel();
          elem2.click();
          resolve();
        });
        observer.observe(document, {
          attributes: true,
          childList: true,
          subtree: true
        });
        waitForElement.observer = observer;
      });
    };
    waitForElement.cancel = () => {
      const { observer } = waitForElement;
      if (observer === void 0) {
        return;
      }
      waitForElement.observer = void 0;
      observer.disconnect();
    };
    const waitForTimeout = (ms2) => {
      waitForTimeout.cancel();
      waitForTimeout.timer = setTimeout(() => {
        waitForTimeout.timer = void 0;
        terminate();
        safe.uboLog(logPrefix, `Timed out after ${ms2} ms`);
      }, ms2);
    };
    waitForTimeout.cancel = () => {
      if (waitForTimeout.timer === void 0) {
        return;
      }
      clearTimeout(waitForTimeout.timer);
      waitForTimeout.timer = void 0;
    };
    const terminate = () => {
      waitForTime.cancel();
      waitForElement.cancel();
      waitForTimeout.cancel();
    };
    const process = async () => {
      waitForTimeout(steps.pop());
      while (steps.length !== 0) {
        const step = steps.shift();
        if (step === void 0) {
          break;
        }
        if (typeof step === "number") {
          await waitForTime(step);
          if (step === 1) {
            continue;
          }
          continue;
        }
        if (step.startsWith("!")) {
          continue;
        }
        await waitForElement(step);
        safe.uboLog(logPrefix, `Clicked ${step}`);
      }
      terminate();
    };
    runAtHtmlElementFn(process);
  }
  builtinScriptlets.push({
    name: "trusted-replace-outbound-text.js",
    requiresTrust: true,
    fn: trustedReplaceOutboundText,
    dependencies: [
      "proxy-apply.fn",
      "safe-self.fn"
    ]
  });
  function trustedReplaceOutboundText(propChain = "", rawPattern = "", rawReplacement = "", ...args) {
    if (propChain === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("trusted-replace-outbound-text", propChain, rawPattern, rawReplacement, ...args);
    const rePattern = safe.patternToRegex(rawPattern);
    const replacement = rawReplacement.startsWith("json:") ? safe.JSON_parse(rawReplacement.slice(5)) : rawReplacement;
    const extraArgs = safe.getExtraArgs(args);
    const reCondition = safe.patternToRegex(extraArgs.condition || "");
    proxyApplyFn(propChain, (context) => {
      const encodedTextBefore = context.reflect();
      let textBefore = encodedTextBefore;
      if (extraArgs.encoding === "base64") {
        try {
          textBefore = self.atob(encodedTextBefore);
        } catch (e) {
          console.warn("[uBR] scriptlets: atob decode failed", e);
          return encodedTextBefore;
        }
      }
      if (rawPattern === "") {
        safe.uboLog(logPrefix, "Decoded outbound text:\n", textBefore);
        return encodedTextBefore;
      }
      reCondition.lastIndex = 0;
      if (reCondition.test(textBefore) === false) {
        return encodedTextBefore;
      }
      const textAfter = textBefore.replace(rePattern, replacement);
      if (textAfter === textBefore) {
        return encodedTextBefore;
      }
      safe.uboLog(logPrefix, "Matched and replaced");
      if (safe.logLevel > 1) {
        safe.uboLog(logPrefix, "Modified decoded outbound text:\n", textAfter);
      }
      let encodedTextAfter = textAfter;
      if (extraArgs.encoding === "base64") {
        encodedTextAfter = self.btoa(textAfter);
      }
      return encodedTextAfter;
    });
  }
  builtinScriptlets.push({
    name: "trusted-suppress-native-method.js",
    requiresTrust: true,
    fn: trustedSuppressNativeMethod,
    dependencies: [
      "matches-stack-trace.fn",
      "proxy-apply.fn",
      "safe-self.fn"
    ]
  });
  function trustedSuppressNativeMethod(methodPath = "", signature = "", how = "", stack = "") {
    if (methodPath === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("trusted-suppress-native-method", methodPath, signature, how, stack);
    const signatureArgs = safe.String_split.call(signature, /\s*\|\s*/).map((v2) => {
      if (/^".*"$/.test(v2)) {
        return { type: "pattern", re: safe.patternToRegex(v2.slice(1, -1)) };
      }
      if (/^\/.+\/$/.test(v2)) {
        return { type: "pattern", re: safe.patternToRegex(v2) };
      }
      if (v2 === "false") {
        return { type: "exact", value: false };
      }
      if (v2 === "true") {
        return { type: "exact", value: true };
      }
      if (v2 === "null") {
        return { type: "exact", value: null };
      }
      if (v2 === "undefined") {
        return { type: "exact", value: void 0 };
      }
    });
    const stackNeedle = safe.initPattern(stack, { canNegate: true });
    proxyApplyFn(methodPath, (context) => {
      const { callArgs } = context;
      if (signature === "") {
        safe.uboLog(logPrefix, `Arguments:
${callArgs.join("\n")}`);
        return context.reflect();
      }
      for (let i = 0; i < signatureArgs.length; i++) {
        const signatureArg = signatureArgs[i];
        if (signatureArg === void 0) {
          continue;
        }
        const targetArg = i < callArgs.length ? callArgs[i] : void 0;
        if (signatureArg.type === "exact") {
          if (targetArg !== signatureArg.value) {
            return context.reflect();
          }
        }
        if (signatureArg.type === "pattern") {
          if (safe.RegExp_test.call(signatureArg.re, targetArg) === false) {
            return context.reflect();
          }
        }
      }
      if (stackNeedle.matchAll !== true) {
        const logLevel = safe.logLevel > 1 ? "all" : "";
        if (matchesStackTraceFn(stackNeedle, logLevel) === false) {
          return context.reflect();
        }
      }
      if (how === "debug") {
        debugger;
        return context.reflect();
      }
      safe.uboLog(logPrefix, `Suppressed:
${callArgs.join("\n")}`);
      if (how === "abort") {
        throw new ReferenceError();
      }
    });
  }
  builtinScriptlets.push({
    name: "trusted-prevent-dom-bypass.js",
    requiresTrust: true,
    fn: trustedPreventDomBypass,
    dependencies: [
      "proxy-apply.fn",
      "safe-self.fn"
    ]
  });
  function trustedPreventDomBypass(methodPath = "", targetProp = "") {
    if (methodPath === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("trusted-prevent-dom-bypass", methodPath, targetProp);
    proxyApplyFn(methodPath, (context) => {
      const elems = new Set(context.callArgs.filter((e) => e instanceof HTMLElement));
      const r = context.reflect();
      if (elems.length === 0) {
        return r;
      }
      for (const elem of elems) {
        try {
          if (`${elem.contentWindow}` !== "[object Window]") {
            continue;
          }
          if (elem.contentWindow.location.href !== "about:blank") {
            if (elem.contentWindow.location.href !== self.location.href) {
              continue;
            }
          }
          if (targetProp !== "") {
            let me2 = self, it = elem.contentWindow;
            let chain = targetProp;
            for (; ; ) {
              const pos = chain.indexOf(".");
              if (pos === -1) {
                break;
              }
              const prop = chain.slice(0, pos);
              me2 = me2[prop];
              it = it[prop];
              chain = chain.slice(pos + 1);
            }
            it[chain] = me2[chain];
          } else {
            Object.defineProperty(elem, "contentWindow", { value: self });
          }
          safe.uboLog(logPrefix, "Bypass prevented");
        } catch (e) {
          console.warn("[uBR] scriptlets: prevent-element-src-loading bypass failed", e);
        }
      }
      return r;
    });
  }
  builtinScriptlets.push({
    name: "trusted-override-element-method.js",
    requiresTrust: true,
    fn: trustedOverrideElementMethod,
    dependencies: [
      "proxy-apply.fn",
      "safe-self.fn",
      "validate-constant.fn"
    ]
  });
  function trustedOverrideElementMethod(methodPath = "", selector = "", disposition = "") {
    if (methodPath === "") {
      return;
    }
    const safe = safeSelf();
    const logPrefix = safe.makeLogPrefix("trusted-override-element-method", methodPath, selector, disposition);
    const extraArgs = safe.getExtraArgs(Array.from(arguments), 3);
    proxyApplyFn(methodPath, (context) => {
      let override = selector === "";
      if (override === false) {
        const { thisArg } = context;
        try {
          override = thisArg.closest(selector) === thisArg;
        } catch (e) {
          console.warn("[uBR] scriptlets: override-element-method selector check failed", e);
        }
      }
      if (override === false) {
        return context.reflect();
      }
      safe.uboLog(logPrefix, "Overridden");
      if (disposition === "") {
        return;
      }
      if (disposition === "debug" && safe.logLevel !== 0) {
        debugger;
      }
      if (disposition === "throw") {
        throw new ReferenceError();
      }
      return validateConstantFn(true, disposition, extraArgs);
    });
  }

  // src/js/redirect-engine.ts
  var extToMimeMap = /* @__PURE__ */ new Map([
    ["css", "text/css"],
    ["fn", "fn/javascript"],
    // invented mime type for internal use
    ["gif", "image/gif"],
    ["html", "text/html"],
    ["js", "text/javascript"],
    ["json", "application/json"],
    ["mp3", "audio/mp3"],
    ["mp4", "video/mp4"],
    ["png", "image/png"],
    ["txt", "text/plain"],
    ["xml", "text/xml"]
  ]);
  var typeToMimeMap = /* @__PURE__ */ new Map([
    ["main_frame", "text/html"],
    ["other", "text/plain"],
    ["script", "text/javascript"],
    ["stylesheet", "text/css"],
    ["sub_frame", "text/html"],
    ["xmlhttprequest", "text/plain"]
  ]);
  var validMimes = new Set(extToMimeMap.values());
  var mimeFromName = (name2) => {
    const match = /\.([^.]+)$/.exec(name2);
    if (match === null) {
      return "";
    }
    return extToMimeMap.get(match[1]);
  };
  var removeTopCommentBlock = (text) => {
    return text.replace(/^\/\*[\S\s]+?\*\/\s*/g, "");
  };
  var warSecret = typeof vAPI === "object" && vAPI !== null ? vAPI.warSecret?.short || (() => "") : () => "";
  var RESOURCES_SELFIE_VERSION = 7;
  var RESOURCES_SELFIE_NAME = "selfie/redirectEngine/resources";
  var RedirectEntry = class _RedirectEntry {
    constructor() {
      this.mime = "";
      this.data = "";
      this.warURL = void 0;
      this.params = void 0;
      this.requiresTrust = false;
      this.world = "MAIN";
      this.dependencies = [];
    }
    // Prevent redirection to web accessible resources when the request is
    // of type 'xmlhttprequest', because XMLHttpRequest.responseURL would
    // cause leakage of extension id. See:
    // - https://stackoverflow.com/a/8056313
    // - https://bugzilla.mozilla.org/show_bug.cgi?id=998076
    // https://www.reddit.com/r/uBlockOrigin/comments/cpxm1v/
    //   User-supplied resources may already be base64 encoded.
    toURL(fctxt, asDataURI = false) {
      if (this.warURL !== void 0 && asDataURI !== true && fctxt instanceof Object && fctxt.type !== "xmlhttprequest") {
        const params = [];
        const secret = warSecret();
        if (secret !== "") {
          params.push(`secret=${secret}`);
        }
        if (this.params !== void 0) {
          for (const name2 of this.params) {
            const value = fctxt[name2];
            if (value === void 0) {
              continue;
            }
            params.push(`${name2}=${encodeURIComponent(value)}`);
          }
        }
        let url = `${this.warURL}`;
        if (params.length !== 0) {
          url += `?${params.join("&")}`;
        }
        return url;
      }
      if (this.data === void 0) {
        return;
      }
      if (this.data === "") {
        const mime = typeToMimeMap.get(fctxt.type);
        if (mime === "") {
          return;
        }
        return `data:${mime},`;
      }
      if (this.data.startsWith("data:") === false) {
        if (this.mime.indexOf(";") === -1) {
          this.data = `data:${this.mime};base64,${btoa(this.data)}`;
        } else {
          this.data = `data:${this.mime},${this.data}`;
        }
      }
      return this.data;
    }
    toContent() {
      if (this.data.startsWith("data:")) {
        const pos = this.data.indexOf(",");
        const base64 = this.data.endsWith(";base64", pos);
        this.data = this.data.slice(pos + 1);
        if (base64) {
          this.data = atob(this.data);
        }
      }
      return this.data;
    }
    static fromDetails(details) {
      const r = new _RedirectEntry();
      Object.assign(r, details);
      return r;
    }
  };
  var RedirectEngine = class {
    constructor() {
      this.aliases = /* @__PURE__ */ new Map();
      this.resources = /* @__PURE__ */ new Map();
      this.reset();
      this.modifyTime = Date.now();
    }
    reset() {
    }
    freeze() {
    }
    tokenToURL(fctxt, token, asDataURI = false) {
      const entry = this.resources.get(this.aliases.get(token) || token);
      if (entry === void 0) {
        return;
      }
      return entry.toURL(fctxt, asDataURI);
    }
    tokenToDNR(token) {
      const entry = this.resources.get(this.aliases.get(token) || token);
      if (entry === void 0) {
        return;
      }
      if (entry.warURL === void 0) {
        return;
      }
      return entry.warURL;
    }
    hasToken(token) {
      if (token === "none") {
        return true;
      }
      const asDataURI = token.charCodeAt(0) === 37;
      if (asDataURI) {
        token = token.slice(1);
      }
      return this.resources.get(this.aliases.get(token) || token) !== void 0;
    }
    tokenRequiresTrust(token) {
      const entry = this.resources.get(this.aliases.get(token) || token);
      return entry && entry.requiresTrust === true || false;
    }
    async toSelfie() {
    }
    async fromSelfie() {
      return true;
    }
    contentFromName(name2, mime = "") {
      const entry = this.resources.get(this.aliases.get(name2) || name2);
      if (entry === void 0) {
        return;
      }
      if (entry.mime.startsWith(mime) === false) {
        return;
      }
      if (entry.data === void 0) {
        return;
      }
      return {
        js: entry.toContent(),
        world: entry.world,
        dependencies: entry.dependencies.slice()
      };
    }
    // https://github.com/uBlockOrigin/uAssets/commit/deefe8755511
    //   Consider 'none' a reserved keyword, to be used to disable redirection.
    // https://github.com/uBlockOrigin/uBlock-issues/issues/1419
    //   Append newlines to raw text to ensure processing of trailing resource.
    resourcesFromString(text) {
      const lineIter = new LineIterator(
        `${removeTopCommentBlock(text)}

`
      );
      const reNonEmptyLine = /\S/;
      let fields, encoded, details;
      while (lineIter.eot() === false) {
        const line = lineIter.next();
        if (line.startsWith("#")) {
          continue;
        }
        if (line.startsWith("// ")) {
          continue;
        }
        if (fields === void 0) {
          if (line === "") {
            continue;
          }
          if (line.startsWith("/// ")) {
            const name3 = line.slice(4).trim();
            fields = [name3, mimeFromName(name3)];
            continue;
          }
          const head = line.trim().split(/\s+/);
          if (head.length !== 2) {
            continue;
          }
          if (head[0] === "none") {
            continue;
          }
          let pos = head[1].indexOf(";");
          if (pos === -1) {
            pos = head[1].length;
          }
          if (validMimes.has(head[1].slice(0, pos)) === false) {
            continue;
          }
          encoded = head[1].indexOf(";") !== -1;
          fields = head;
          continue;
        }
        if (line.startsWith("/// ")) {
          if (details === void 0) {
            details = [];
          }
          const [prop, value] = line.slice(4).trim().split(/\s+/);
          if (value !== void 0) {
            details.push({ prop, value });
          }
          continue;
        }
        if (reNonEmptyLine.test(line)) {
          fields.push(encoded ? line.trim() : line);
          continue;
        }
        const name2 = this.aliases.get(fields[0]) || fields[0];
        const mime = fields[1];
        const data = orphanizeString(
          fields.slice(2).join(encoded ? "" : "\n")
        );
        this.resources.set(name2, RedirectEntry.fromDetails({ mime, data }));
        if (Array.isArray(details)) {
          const resource = this.resources.get(name2);
          for (const { prop, value } of details) {
            switch (prop) {
              case "alias":
                this.aliases.set(value, name2);
                break;
              case "world":
                if (/^isolated$/i.test(value) === false) {
                  break;
                }
                resource.world = "ISOLATED";
                break;
              case "dependency":
                if (this.resources.has(value) === false) {
                  break;
                }
                resource.dependencies.push(value);
                break;
              default:
                break;
            }
          }
        }
        fields = void 0;
        details = void 0;
      }
      this.modifyTime = Date.now();
    }
    loadBuiltinResources(fetcher) {
      this.resources = /* @__PURE__ */ new Map();
      this.aliases = /* @__PURE__ */ new Map();
      const fetches = [
        Promise.resolve().then(() => {
          for (const scriptlet of builtinScriptlets) {
            const details = {};
            details.mime = mimeFromName(scriptlet.name);
            details.data = scriptlet.fn.toString();
            for (const [k2, v2] of Object.entries(scriptlet)) {
              if (k2 === "fn") {
                continue;
              }
              details[k2] = v2;
            }
            const entry = RedirectEntry.fromDetails(details);
            this.resources.set(details.name, entry);
            if (Array.isArray(details.aliases) === false) {
              continue;
            }
            for (const alias of details.aliases) {
              this.aliases.set(alias, details.name);
            }
          }
          this.modifyTime = Date.now();
        }).catch((reason) => {
          console.error(reason);
        })
      ];
      const store = (name2, data = void 0) => {
        const details = redirect_resources_default.get(name2);
        const entry = RedirectEntry.fromDetails({
          mime: mimeFromName(name2),
          data,
          warURL: `/web_accessible_resources/${name2}`,
          params: details.params
        });
        this.resources.set(name2, entry);
        if (details.alias === void 0) {
          return;
        }
        if (Array.isArray(details.alias)) {
          for (const alias of details.alias) {
            this.aliases.set(alias, name2);
          }
        } else {
          this.aliases.set(details.alias, name2);
        }
      };
      const processBlob = (name2, blob) => {
        return new Promise((resolve) => {
          const reader = new FileReader();
          reader.onload = () => {
            store(name2, reader.result);
            resolve();
          };
          reader.onabort = reader.onerror = () => {
            resolve();
          };
          reader.readAsDataURL(blob);
        });
      };
      const processText = (name2, text) => {
        store(name2, removeTopCommentBlock(text));
      };
      const process = (result) => {
        const match = /^\/web_accessible_resources\/([^?]+)/.exec(result.url);
        if (match === null) {
          return;
        }
        const name2 = match[1];
        return result.content instanceof Blob ? processBlob(name2, result.content) : processText(name2, result.content);
      };
      for (const [name2, details] of redirect_resources_default) {
        if (typeof details.data !== "string") {
          store(name2);
          continue;
        }
        fetches.push(
          fetcher(`/web_accessible_resources/${name2}`, {
            responseType: details.data
          }).then(
            (result) => process(result)
          )
        );
      }
      return Promise.all(fetches);
    }
    getResourceDetails() {
      const out = /* @__PURE__ */ new Map([
        ["none", { canInject: false, canRedirect: true, aliasOf: "" }]
      ]);
      for (const [name2, entry] of this.resources) {
        out.set(name2, {
          canInject: typeof entry.data === "string",
          canRedirect: entry.warURL !== void 0,
          aliasOf: "",
          extensionPath: entry.warURL
        });
      }
      for (const [alias, name2] of this.aliases) {
        const original = out.get(name2);
        if (original === void 0) {
          continue;
        }
        const aliased = Object.assign({}, original);
        aliased.aliasOf = name2;
        out.set(alias, aliased);
      }
      return Array.from(out).sort((a, b2) => {
        return a[0].localeCompare(b2[0]);
      });
    }
    getTrustedScriptletTokens() {
      const out = [];
      const isTrustedScriptlet = (entry) => {
        if (entry.requiresTrust !== true) {
          return false;
        }
        if (entry.warURL !== void 0) {
          return false;
        }
        if (typeof entry.data !== "string") {
          return false;
        }
        if (entry.name.endsWith(".js") === false) {
          return false;
        }
        return true;
      };
      for (const [name2, entry] of this.resources) {
        if (isTrustedScriptlet(entry) === false) {
          continue;
        }
        out.push(name2.slice(0, -3));
      }
      for (const [alias, name2] of this.aliases) {
        if (out.includes(name2.slice(0, -3)) === false) {
          continue;
        }
        out.push(alias.slice(0, -3));
      }
      return out;
    }
    selfieFromResources(storage2) {
      return storage2.toCache(RESOURCES_SELFIE_NAME, {
        version: RESOURCES_SELFIE_VERSION,
        aliases: this.aliases,
        resources: this.resources
      });
    }
    async resourcesFromSelfie(storage2) {
      const selfie = await storage2.fromCache(RESOURCES_SELFIE_NAME);
      if (selfie instanceof Object === false) {
        return false;
      }
      if (selfie.version !== RESOURCES_SELFIE_VERSION) {
        return false;
      }
      if (selfie.aliases instanceof Map === false) {
        return false;
      }
      if (selfie.resources instanceof Map === false) {
        return false;
      }
      this.aliases = selfie.aliases;
      this.resources = selfie.resources;
      for (const [token, entry] of this.resources) {
        this.resources.set(token, RedirectEntry.fromDetails(entry));
      }
      this.modifyTime = Date.now();
      return true;
    }
    invalidateResourcesSelfie(storage2) {
      storage2.remove(RESOURCES_SELFIE_NAME);
    }
  };
  var redirectEngine = new RedirectEngine();

  // src/js/scriptlet-filtering.ts
  init_uri_utils();

  // src/js/mrucache.ts
  var MRUCache = class {
    constructor(maxSize) {
      __publicField(this, "maxSize");
      __publicField(this, "array");
      __publicField(this, "map");
      __publicField(this, "resetTime");
      this.maxSize = maxSize;
      this.array = [];
      this.map = /* @__PURE__ */ new Map();
      this.resetTime = Date.now();
    }
    add(key, value) {
      const found = this.map.has(key);
      this.map.set(key, value);
      if (found) {
        return;
      }
      if (this.array.length === this.maxSize) {
        this.map.delete(this.array.pop());
      }
      this.array.unshift(key);
    }
    remove(key) {
      if (this.map.delete(key) === false) {
        return;
      }
      this.array.splice(this.array.indexOf(key), 1);
    }
    lookup(key) {
      const value = this.map.get(key);
      if (value === void 0) {
        return void 0;
      }
      if (this.array[0] === key) {
        return value;
      }
      const i = this.array.indexOf(key);
      this.array.copyWithin(1, 0, i);
      this.array[0] = key;
      return value;
    }
    reset() {
      this.array = [];
      this.map.clear();
      this.resetTime = Date.now();
    }
  };

  // src/js/static-ext-filtering-db.ts
  var naivePathnameFromURL = (url) => {
    if (typeof url !== "string") {
      return;
    }
    const hnPos = url.indexOf("://");
    if (hnPos === -1) {
      return;
    }
    const pathPos = url.indexOf("/", hnPos + 3);
    if (pathPos === -1) {
      return;
    }
    return url.slice(pathPos);
  };
  var extractSubTargets = (target) => {
    const isRegex = target.charCodeAt(0) === 47;
    if (isRegex === false) {
      const pathPos2 = target.indexOf("/");
      return {
        isRegex,
        hn: target.slice(0, pathPos2),
        pn: target.slice(pathPos2)
      };
    }
    const pathPos = target.indexOf("\\/");
    if (pathPos !== -1) {
      return {
        isRegex,
        hn: `${target.slice(1, pathPos)}$`,
        pn: `^${target.slice(pathPos, -1)}`
      };
    }
    return { isRegex, hn: target.slice(1, -1) };
  };
  var _hostnameToStringListMap, _matcherMap, _hostnameToMatcherListMap, _strSlots, _matcherSlots, _linkedLists, _regexMap, _strToSlotMap, _cleanupTimer, _StaticExtFilteringHostnameDB_instances, storeMatcher_fn, retrieveSpecificsByRegex_fn, matcherTest_fn, restrTest_fn, retrieveFromSlot_fn;
  var _StaticExtFilteringHostnameDB = class _StaticExtFilteringHostnameDB {
    constructor() {
      __privateAdd(this, _StaticExtFilteringHostnameDB_instances);
      __privateAdd(this, _hostnameToStringListMap, /* @__PURE__ */ new Map());
      __privateAdd(this, _matcherMap, /* @__PURE__ */ new Map());
      __privateAdd(this, _hostnameToMatcherListMap, /* @__PURE__ */ new Map());
      __privateAdd(this, _strSlots, [""]);
      // Array of strings (selectors and pseudo-selectors)
      __privateAdd(this, _matcherSlots, [null]);
      __privateAdd(this, _linkedLists, [0, 0]);
      // Array of integer pairs
      __privateAdd(this, _regexMap, /* @__PURE__ */ new Map());
      __privateAdd(this, _strToSlotMap, /* @__PURE__ */ new Map());
      __privateAdd(this, _cleanupTimer, vAPI.defer.create(() => {
        __privateGet(this, _strToSlotMap).clear();
      }));
      this.size = 0;
    }
    store(target, s) {
      this.size += 1;
      let iStr = __privateGet(this, _strToSlotMap).get(s);
      if (iStr === void 0) {
        iStr = __privateGet(this, _strSlots).length;
        __privateGet(this, _strSlots).push(s);
        __privateGet(this, _strToSlotMap).set(s, iStr);
        if (__privateGet(this, _cleanupTimer).ongoing() === false) {
          this.collectGarbage(true);
        }
      }
      if (target.includes("/")) {
        return __privateMethod(this, _StaticExtFilteringHostnameDB_instances, storeMatcher_fn).call(this, target, iStr);
      }
      const iList = __privateGet(this, _hostnameToStringListMap).get(target) ?? 0;
      __privateGet(this, _hostnameToStringListMap).set(target, __privateGet(this, _linkedLists).length);
      __privateGet(this, _linkedLists).push(iStr, iList);
    }
    clear() {
      __privateGet(this, _hostnameToStringListMap).clear();
      __privateGet(this, _matcherMap).clear();
      __privateGet(this, _hostnameToMatcherListMap).clear();
      __privateSet(this, _strSlots, [""]);
      __privateSet(this, _matcherSlots, [null]);
      __privateSet(this, _linkedLists, [0, 0]);
      __privateGet(this, _regexMap).clear();
      __privateGet(this, _strToSlotMap).clear();
      this.size = 0;
    }
    collectGarbage(later = false) {
      if (later) {
        return __privateGet(this, _cleanupTimer).onidle(5e3, { timeout: 5e3 });
      }
      __privateGet(this, _cleanupTimer).off();
      __privateGet(this, _strToSlotMap).clear();
    }
    retrieveSpecifics(out, hostname) {
      let hn2 = hostname;
      if (hn2 === "") {
        return;
      }
      for (; ; ) {
        const iList = __privateGet(this, _hostnameToStringListMap).get(hn2);
        if (iList !== void 0) {
          __privateMethod(this, _StaticExtFilteringHostnameDB_instances, retrieveFromSlot_fn).call(this, out, iList);
        }
        const pos = hn2.indexOf(".");
        if (pos === -1) {
          break;
        }
        hn2 = hn2.slice(pos + 1);
        if (hn2 === "*") {
          break;
        }
      }
    }
    retrieveGenerics(out) {
      let iList = __privateGet(this, _hostnameToStringListMap).get("");
      if (iList) {
        __privateMethod(this, _StaticExtFilteringHostnameDB_instances, retrieveFromSlot_fn).call(this, out, iList);
      }
      iList = __privateGet(this, _hostnameToStringListMap).get("*");
      if (iList) {
        __privateMethod(this, _StaticExtFilteringHostnameDB_instances, retrieveFromSlot_fn).call(this, out, iList);
      }
    }
    retrieveSpecificsByRegex(out, hostname, url) {
      let hn2 = hostname;
      if (hn2 === "") {
        return;
      }
      const pathname = naivePathnameFromURL(url) ?? "";
      for (; ; ) {
        __privateMethod(this, _StaticExtFilteringHostnameDB_instances, retrieveSpecificsByRegex_fn).call(this, hn2, out, hostname, pathname);
        const pos = hn2.indexOf(".");
        if (pos === -1) {
          break;
        }
        hn2 = hn2.slice(pos + 1);
      }
      __privateMethod(this, _StaticExtFilteringHostnameDB_instances, retrieveSpecificsByRegex_fn).call(this, "", out, hostname, pathname);
    }
    toSelfie() {
      return {
        VERSION: _StaticExtFilteringHostnameDB.VERSION,
        hostnameToStringListMap: __privateGet(this, _hostnameToStringListMap),
        matcherMap: __privateGet(this, _matcherMap),
        hostnameToMatcherListMap: __privateGet(this, _hostnameToMatcherListMap),
        strSlots: __privateGet(this, _strSlots),
        matcherSlots: __privateGet(this, _matcherSlots),
        linkedLists: __privateGet(this, _linkedLists),
        size: this.size
      };
    }
    fromSelfie(selfie) {
      if (typeof selfie !== "object" || selfie === null) {
        return;
      }
      if (selfie.VERSION !== _StaticExtFilteringHostnameDB.VERSION) {
        throw new TypeError("Bad selfie");
      }
      __privateSet(this, _hostnameToStringListMap, selfie.hostnameToStringListMap);
      __privateSet(this, _matcherMap, selfie.matcherMap);
      __privateSet(this, _hostnameToMatcherListMap, selfie.hostnameToMatcherListMap);
      __privateSet(this, _strSlots, selfie.strSlots);
      __privateSet(this, _matcherSlots, selfie.matcherSlots);
      __privateSet(this, _linkedLists, selfie.linkedLists);
      this.size = selfie.size;
    }
  };
  _hostnameToStringListMap = new WeakMap();
  _matcherMap = new WeakMap();
  _hostnameToMatcherListMap = new WeakMap();
  _strSlots = new WeakMap();
  _matcherSlots = new WeakMap();
  _linkedLists = new WeakMap();
  _regexMap = new WeakMap();
  _strToSlotMap = new WeakMap();
  _cleanupTimer = new WeakMap();
  _StaticExtFilteringHostnameDB_instances = new WeakSet();
  storeMatcher_fn = function(target, iStr) {
    const iMatcher = __privateGet(this, _matcherMap).get(target) || __privateGet(this, _matcherSlots).length;
    if (iMatcher === __privateGet(this, _matcherSlots).length) {
      const { isRegex, hn: hn2, pn: pn2 } = extractSubTargets(target);
      __privateGet(this, _matcherSlots).push({ isRegex, hn: hn2, pn: pn2, iList: 0 });
      __privateGet(this, _matcherMap).set(target, iMatcher);
      if (isRegex === false) {
        const iMatcherList = __privateGet(this, _hostnameToMatcherListMap).get(hn2) ?? 0;
        __privateGet(this, _hostnameToMatcherListMap).set(hn2, __privateGet(this, _linkedLists).length);
        __privateGet(this, _linkedLists).push(iMatcher, iMatcherList);
      } else {
        const iMatcherList = __privateGet(this, _hostnameToMatcherListMap).get("") ?? 0;
        __privateGet(this, _hostnameToMatcherListMap).set("", __privateGet(this, _linkedLists).length);
        __privateGet(this, _linkedLists).push(iMatcher, iMatcherList);
      }
    }
    const matcher = __privateGet(this, _matcherSlots)[iMatcher];
    const iList = matcher.iList;
    matcher.iList = __privateGet(this, _linkedLists).length;
    __privateGet(this, _linkedLists).push(iStr, iList);
  };
  retrieveSpecificsByRegex_fn = function(hn2, out, hostname, pathname) {
    let iMatchList = __privateGet(this, _hostnameToMatcherListMap).get(hn2) ?? 0;
    while (iMatchList !== 0) {
      const iMatchSlot = __privateGet(this, _linkedLists)[iMatchList + 0];
      const matcher = __privateGet(this, _matcherSlots)[iMatchSlot];
      if (__privateMethod(this, _StaticExtFilteringHostnameDB_instances, matcherTest_fn).call(this, matcher, hostname, pathname)) {
        __privateMethod(this, _StaticExtFilteringHostnameDB_instances, retrieveFromSlot_fn).call(this, out, matcher.iList);
      }
      iMatchList = __privateGet(this, _linkedLists)[iMatchList + 1];
    }
  };
  matcherTest_fn = function(matcher, hn2, pn2) {
    if (matcher.isRegex === false) {
      return pn2.startsWith(matcher.pn);
    }
    if (__privateMethod(this, _StaticExtFilteringHostnameDB_instances, restrTest_fn).call(this, matcher.hn, hn2) === false) {
      return false;
    }
    if (matcher.pn === void 0) {
      return true;
    }
    return __privateMethod(this, _StaticExtFilteringHostnameDB_instances, restrTest_fn).call(this, matcher.pn, pn2);
  };
  restrTest_fn = function(restr, s) {
    let re2 = __privateGet(this, _regexMap).get(restr);
    if (re2 === void 0) {
      __privateGet(this, _regexMap).set(restr, re2 = new RegExp(restr));
    }
    return re2.test(s);
  };
  retrieveFromSlot_fn = function(out, iList) {
    if (iList === void 0) {
      return;
    }
    do {
      const iStr = __privateGet(this, _linkedLists)[iList + 0];
      out.add(__privateGet(this, _strSlots)[iStr]);
      iList = __privateGet(this, _linkedLists)[iList + 1];
    } while (iList !== 0);
  };
  __publicField(_StaticExtFilteringHostnameDB, "VERSION", 1);
  var StaticExtFilteringHostnameDB = _StaticExtFilteringHostnameDB;

  // src/js/scriptlet-filtering-core.ts
  init_uri_utils();
  var codeSorter = (a, b2) => {
    if (a.startsWith("try")) {
      return 1;
    }
    if (b2.startsWith("try")) {
      return -1;
    }
    return 0;
  };
  var normalizeRawFilter = (parser, sourceIsTrusted = false) => {
    const args = parser.getScriptletArgs();
    if (args.length !== 0) {
      let token = `${args[0]}.js`;
      if (redirectEngine.aliases.has(token)) {
        token = redirectEngine.aliases.get(token);
      }
      if (parser.isException() !== true) {
        if (sourceIsTrusted !== true) {
          if (redirectEngine.tokenRequiresTrust(token)) {
            return;
          }
        }
      }
      args[0] = token.slice(0, -3);
    }
    return JSON.stringify(args);
  };
  var lookupScriptlet = (rawToken, mainMap, isolatedMap, debug = false) => {
    if (mainMap.has(rawToken) || isolatedMap.has(rawToken)) {
      return;
    }
    const args = JSON.parse(rawToken);
    const token = `${args[0]}.js`;
    const details = redirectEngine.contentFromName(token, "text/javascript");
    if (details === void 0) {
      return;
    }
    const targetWorldMap = details.world !== "ISOLATED" ? mainMap : isolatedMap;
    const match = /^function\s+([^(\s]+)\s*\(/.exec(details.js);
    const fname = match && match[1];
    const content = patchScriptlet(fname, details.js, args.slice(1));
    if (fname) {
      targetWorldMap.set(token, details.js);
    }
    const dependencies = details.dependencies || [];
    while (dependencies.length !== 0) {
      const token2 = dependencies.shift();
      if (targetWorldMap.has(token2)) {
        continue;
      }
      const details2 = redirectEngine.contentFromName(token2, "fn/javascript") || redirectEngine.contentFromName(token2, "text/javascript");
      if (details2 === void 0) {
        continue;
      }
      targetWorldMap.set(token2, details2.js);
      if (Array.isArray(details2.dependencies) === false) {
        continue;
      }
      dependencies.push(...details2.dependencies);
    }
    targetWorldMap.set(rawToken, [
      "try {",
      `	${content}`,
      "} catch (e) {",
      debug ? "	console.error(e);" : "",
      "}"
    ].join("\n"));
  };
  var patchScriptlet = (fname, content, arglist) => {
    if (fname) {
      content = `${fname}({{args}});`;
    } else {
      for (let i = 0; i < arglist.length; i++) {
        content = content.replace(`{{${i + 1}}}`, arglist[i]);
      }
    }
    return content.replace(
      "{{args}}",
      JSON.stringify(arglist).slice(1, -1).replace(/\$/g, "$$$")
    );
  };
  var requote = (s) => {
    if (/^(["'`]).*\1$|,|^$/.test(s) === false) {
      return s;
    }
    if (s.includes("'") === false) {
      return `'${s}'`;
    }
    if (s.includes('"') === false) {
      return `"${s}"`;
    }
    if (s.includes("`") === false) {
      return `\`${s}\``;
    }
    return `'${s.replace(/'/g, "\\'")}'`;
  };
  var decompile = (json, isException) => {
    const prefix = isException ? "#@#" : "##";
    const args = JSON.parse(json);
    return `${prefix}+js(${args.map((s) => requote(s)).join(", ")})`;
  };
  var ScriptletFilteringEngine = class {
    constructor() {
      this.acceptedCount = 0;
      this.discardedCount = 0;
      this.scriptletDB = new StaticExtFilteringHostnameDB();
      this.duplicates = /* @__PURE__ */ new Set();
    }
    getFilterCount() {
      return this.scriptletDB.size;
    }
    reset() {
      this.scriptletDB.clear();
      this.duplicates.clear();
      this.acceptedCount = 0;
      this.discardedCount = 0;
    }
    freeze() {
      this.duplicates.clear();
      this.scriptletDB.collectGarbage();
    }
    // parser: instance of AstFilterParser from static-filtering-parser.js
    // writer: instance of CompiledListWriter from static-filtering-io.js
    compile(parser, writer) {
      writer.select("SCRIPTLET_FILTERS");
      const isException = parser.isException();
      const normalized = normalizeRawFilter(parser, writer.properties.get("trustedSource"));
      if (normalized === void 0) {
        return;
      }
      if (normalized === "[]" && isException === false) {
        return;
      }
      if (parser.hasOptions() === false) {
        if (isException) {
          writer.push([32, "", `-${normalized}`]);
        }
        return;
      }
      for (const { hn: hn2, not, bad } of parser.getExtFilterDomainIterator()) {
        if (bad) {
          continue;
        }
        const prefix = (isException ? 1 : 0) ^ (not ? 1 : 0) ? "-" : "+";
        writer.push([32, hn2, `${prefix}${normalized}`]);
      }
    }
    // writer: instance of CompiledListReader from static-filtering-io.js
    fromCompiledContent(reader) {
      reader.select("SCRIPTLET_FILTERS");
      while (reader.next()) {
        this.acceptedCount += 1;
        const fingerprint = reader.fingerprint();
        if (this.duplicates.has(fingerprint)) {
          this.discardedCount += 1;
          continue;
        }
        this.duplicates.add(fingerprint);
        const args = reader.args();
        this.scriptletDB.store(args[1], args[2]);
      }
    }
    toSelfie() {
      return this.scriptletDB.toSelfie();
    }
    fromSelfie(selfie) {
      this.scriptletDB.fromSelfie(selfie);
      return true;
    }
    retrieve(request, options = {}) {
      if (this.scriptletDB.size === 0) {
        return;
      }
      const all = /* @__PURE__ */ new Set();
      const { ancestors = [], domain, hostname } = request;
      this.scriptletDB.retrieveSpecifics(all, hostname);
      const entity = entityFromHostname(hostname, domain);
      this.scriptletDB.retrieveSpecifics(all, entity);
      this.scriptletDB.retrieveSpecificsByRegex(all, hostname, request.url);
      this.scriptletDB.retrieveGenerics(all);
      const visitedAncestors = [];
      for (const ancestor of ancestors) {
        const { domain: domain2, hostname: hostname2 } = ancestor;
        if (visitedAncestors.includes(hostname2)) {
          continue;
        }
        visitedAncestors.push(hostname2);
        this.scriptletDB.retrieveSpecifics(all, `${hostname2}>>`);
        const entity2 = entityFromHostname(hostname2, domain2);
        if (entity2 !== "") {
          this.scriptletDB.retrieveSpecifics(all, `${entity2}>>`);
        }
      }
      if (all.size === 0) {
        return;
      }
      if (all.has("-[]")) {
        return { filters: ["#@#+js()"] };
      }
      const scriptlets = /* @__PURE__ */ new Set();
      const exceptions = /* @__PURE__ */ new Set();
      for (const s of all) {
        if (s.charCodeAt(0) === 45) {
          continue;
        }
        const selector = s.slice(1);
        if (all.has(`-${selector}`)) {
          exceptions.add(selector);
        } else {
          scriptlets.add(selector);
        }
      }
      const mainWorldMap = /* @__PURE__ */ new Map();
      const isolatedWorldMap = /* @__PURE__ */ new Map();
      for (const token of scriptlets) {
        lookupScriptlet(token, mainWorldMap, isolatedWorldMap, options.debug);
      }
      if (scriptlets.size !== 0) {
        if (mainWorldMap.size === 0) {
          if (isolatedWorldMap.size === 0) {
            return;
          }
        }
      }
      const mainWorldCode = [];
      for (const js2 of mainWorldMap.values()) {
        mainWorldCode.push(js2);
      }
      mainWorldCode.sort(codeSorter);
      const isolatedWorldCode = [];
      for (const js2 of isolatedWorldMap.values()) {
        isolatedWorldCode.push(js2);
      }
      isolatedWorldCode.sort(codeSorter);
      const scriptletDetails = {
        mainWorld: mainWorldCode.join("\n\n"),
        isolatedWorld: isolatedWorldCode.join("\n\n"),
        filters: [
          ...Array.from(scriptlets).map((a) => decompile(a, false)),
          ...Array.from(exceptions).map((a) => decompile(a, true))
        ]
      };
      const scriptletGlobals2 = options.scriptletGlobals || {};
      if (options.debug) {
        scriptletGlobals2.canDebug = true;
      }
      const scriptletGlobalsJSON = JSON.stringify(scriptletGlobals2, null, 4);
      return {
        mainWorld: scriptletDetails.mainWorld === "" ? "" : [
          "(function() {",
          "// >>>> start of private namespace",
          "",
          options.debugScriptlets ? "debugger;" : ";",
          "",
          // For use by scriptlets to share local data among themselves
          `const scriptletGlobals = ${scriptletGlobalsJSON};`,
          "",
          scriptletDetails.mainWorld,
          "",
          "// <<<< end of private namespace",
          "})();"
        ].join("\n"),
        isolatedWorld: scriptletDetails.isolatedWorld === "" ? "" : [
          "function() {",
          "// >>>> start of private namespace",
          "",
          options.debugScriptlets ? "debugger;" : ";",
          "",
          // For use by scriptlets to share local data among themselves
          `const scriptletGlobals = ${scriptletGlobalsJSON};`,
          "",
          scriptletDetails.isolatedWorld,
          "",
          "// <<<< end of private namespace",
          "}"
        ].join("\n"),
        filters: scriptletDetails.filters
      };
    }
  };

  // src/js/scriptlet-filtering.ts
  var contentScriptRegisterer = {
    id: 1,
    hostnameToDetails: /* @__PURE__ */ new Map(),
    register(hostname, code) {
      if (browser.contentScripts === void 0) {
        return false;
      }
      if (hostname === "") {
        return false;
      }
      const details = this.hostnameToDetails.get(hostname);
      if (details !== void 0) {
        if (code === details.code) {
          return details.handle instanceof Promise === false;
        }
        this.unregisterHandle(details.handle);
        this.hostnameToDetails.delete(hostname);
      }
      const id = this.id++;
      const promise = browser.contentScripts.register({
        js: [{ code }],
        allFrames: true,
        matches: [`*://*.${hostname}/*`],
        matchAboutBlank: true,
        runAt: "document_start"
      }).then((handle) => {
        const details2 = this.hostnameToDetails.get(hostname);
        if (details2 === void 0) {
          return;
        }
        if (details2.id !== id) {
          return;
        }
        details2.handle = handle;
      }).catch((e) => {
        console.warn("[uBR] scriptlet-filtering: contentScripts.register failed for", hostname, e);
        this.hostnameToDetails.delete(hostname);
      });
      this.hostnameToDetails.set(hostname, { id, handle: promise, code });
      return false;
    },
    unregister(hostname) {
      if (hostname === "") {
        return;
      }
      if (this.hostnameToDetails.size === 0) {
        return;
      }
      const details = this.hostnameToDetails.get(hostname);
      if (details === void 0) {
        return;
      }
      this.hostnameToDetails.delete(hostname);
      this.unregisterHandle(details.handle);
    },
    flush(hostname) {
      if (hostname === "") {
        return;
      }
      if (hostname === "*") {
        return this.reset();
      }
      for (const hn2 of this.hostnameToDetails.keys()) {
        if (hn2.endsWith(hostname) === false) {
          continue;
        }
        const pos = hn2.length - hostname.length;
        if (pos !== 0 && hn2.charCodeAt(pos - 1) !== 46) {
          continue;
        }
        this.unregister(hn2);
      }
    },
    reset() {
      if (this.hostnameToDetails.size === 0) {
        return;
      }
      for (const details of this.hostnameToDetails.values()) {
        this.unregisterHandle(details.handle);
      }
      this.hostnameToDetails.clear();
    },
    unregisterHandle(handle) {
      if (handle instanceof Promise) {
        handle.then((handle2) => {
          if (handle2) {
            handle2.unregister();
          }
        });
      } else {
        handle.unregister();
      }
    }
  };
  var isolatedWorldInjector = (() => {
    const parts = [
      "(",
      `function(details) {
            if ( self.uBR_isolatedScriptlets === 'done' ) { return; }
            const doc = document;
            if ( doc.location === null ) { return; }
            const hostname = doc.location.hostname;
            if ( hostname !== '' && details.hostname !== hostname ) { return; }
            const isolatedScriptlets = function(){};
            isolatedScriptlets();
            self.uBR_isolatedScriptlets = 'done';
            return 0;
        }`,
      ")(",
      "json-slot",
      ");"
    ];
    const jsonSlot = parts.indexOf("json-slot");
    if (jsonSlot === -1) {
      console.error("isolatedWorldInjector: json-slot not found in parts");
      return {
        assemble(hostname, details) {
          return "";
        }
      };
    }
    return {
      assemble(hostname, details) {
        parts[jsonSlot] = JSON.stringify({ hostname });
        const code = parts.join("");
        const match = /function\(\)\{\}/.exec(code);
        if (match === null) {
          return code;
        }
        return code.slice(0, match.index) + details.isolatedWorld + code.slice(match.index + match[0].length);
      }
    };
  })();
  var onScriptletMessageInjector = (() => {
    const parts = [
      "(",
      function(name2) {
        if (self.uBR_bcSecret) {
          return;
        }
        try {
          const bcSecret = new self.BroadcastChannel(name2);
          bcSecret.onmessage = (ev) => {
            const msg = ev.data;
            switch (typeof msg) {
              case "string":
                if (msg !== "areyouready?") {
                  break;
                }
                bcSecret.postMessage("iamready!");
                break;
              case "object":
                if (self.vAPI && self.vAPI.messaging) {
                  self.vAPI.messaging.send("contentscript", msg);
                } else {
                  console.log(`[uBR][${msg.type}]${msg.text}`);
                }
                break;
            }
          };
          bcSecret.postMessage("iamready!");
          self.uBR_bcSecret = bcSecret;
        } catch (e) {
          console.warn("[uBR] scriptlet-filtering: BroadcastChannel failed", e);
        }
      }.toString(),
      ")(",
      "bcSecret-slot",
      ");"
    ];
    const bcSecretSlot = parts.indexOf("bcSecret-slot");
    return {
      assemble(details) {
        parts[bcSecretSlot] = JSON.stringify(details.bcSecret);
        return parts.join("\n");
      }
    };
  })();
  var ScriptletFilteringEngineEx = class extends ScriptletFilteringEngine {
    constructor() {
      super();
      this.warOrigin = vAPI.getURL("/web_accessible_resources");
      this.warSecret = void 0;
      this.scriptletCache = new MRUCache(32);
      this.isDevBuild = void 0;
      this.logLevel = 1;
      this.bc = onBroadcast((msg) => {
        switch (msg.what) {
          case "filteringBehaviorChanged": {
            const direction = msg.direction || 0;
            if (direction > 0) {
              return;
            }
            if (direction >= 0 && msg.hostname) {
              return contentScriptRegisterer.flush(msg.hostname);
            }
            contentScriptRegisterer.reset();
            break;
          }
          case "hiddenSettingsChanged":
            this.isDevBuild = void 0;
          /* fall through */
          case "loggerEnabled":
          case "loggerDisabled":
            this.clearCache();
            break;
          case "loggerLevelChanged":
            this.logLevel = msg.level;
            vAPI.tabs.query({
              discarded: false,
              url: ["http://*/*", "https://*/*"]
            }).then((tabs) => {
              for (const tab of tabs) {
                const { status } = tab;
                if (status !== "loading" && status !== "complete") {
                  continue;
                }
                vAPI.tabs.executeScript(tab.id, {
                  allFrames: true,
                  file: `/js/scriptlets/scriptlet-loglevel-${this.logLevel}.js`,
                  matchAboutBlank: true
                });
              }
            });
            this.clearCache();
            break;
        }
      });
    }
    reset() {
      super.reset();
      this.warSecret = vAPI.warSecret.long(this.warSecret);
      this.clearCache();
    }
    freeze() {
      super.freeze();
      this.warSecret = vAPI.warSecret.long(this.warSecret);
      this.clearCache();
    }
    clearCache() {
      this.scriptletCache.reset();
      contentScriptRegisterer.reset();
    }
    retrieve(request) {
      const { hostname } = request;
      if (background_default.userSettings.advancedUserEnabled) {
        if (sessionFirewall.evaluateCellZY(hostname, hostname, "*") === 2) {
          return;
        }
      }
      if (this.scriptletCache.resetTime < redirectEngine.modifyTime) {
        this.clearCache();
      }
      let scriptletDetails = this.scriptletCache.lookup(hostname);
      if (scriptletDetails !== void 0) {
        return scriptletDetails || void 0;
      }
      if (this.isDevBuild === void 0) {
        this.isDevBuild = vAPI.webextFlavor.soup.has("devbuild") || background_default.hiddenSettings.filterAuthorMode;
      }
      if (this.warSecret === void 0) {
        this.warSecret = vAPI.warSecret.long();
      }
      const bcSecret = vAPI.generateSecret(3);
      const options = {
        scriptletGlobals: {
          warOrigin: this.warOrigin,
          warSecret: this.warSecret
        },
        debug: this.isDevBuild,
        debugScriptlets: background_default.hiddenSettings.debugScriptlets
      };
      if (logger_default.enabled) {
        options.scriptletGlobals.bcSecret = bcSecret;
        options.scriptletGlobals.logLevel = this.logLevel;
      }
      scriptletDetails = super.retrieve(request, options);
      if (scriptletDetails === void 0) {
        if (request.nocache !== true) {
          this.scriptletCache.add(hostname, null);
        }
        return;
      }
      const contentScript = [];
      if (scriptletDetails.mainWorld) {
        contentScript.push(vAPI.scriptletsInjector(hostname, scriptletDetails));
      }
      if (scriptletDetails.isolatedWorld) {
        contentScript.push(isolatedWorldInjector.assemble(hostname, scriptletDetails));
      }
      const cachedScriptletDetails = {
        bcSecret,
        code: contentScript.join("\n\n"),
        filters: scriptletDetails.filters
      };
      if (hostname !== "" && request.nocache !== true) {
        this.scriptletCache.add(hostname, cachedScriptletDetails);
      }
      return cachedScriptletDetails;
    }
    injectNow(details) {
      if (typeof details.frameId !== "number") {
        return;
      }
      const hostname = hostnameFromURI(details.url);
      const domain = domainFromHostname(hostname);
      const scriptletDetails = this.retrieve({
        tabId: details.tabId,
        frameId: details.frameId,
        url: details.url,
        hostname,
        domain,
        ancestors: details.ancestors
      });
      if (scriptletDetails === void 0) {
        contentScriptRegisterer.unregister(hostname);
        return;
      }
      if (Boolean(scriptletDetails.code) === false) {
        return scriptletDetails;
      }
      const contentScript = [scriptletDetails.code];
      if (logger_default.enabled) {
        contentScript.unshift(
          onScriptletMessageInjector.assemble(scriptletDetails)
        );
      }
      if (background_default.hiddenSettings.debugScriptletInjector) {
        contentScript.unshift("debugger");
      }
      const code = contentScript.join("\n\n");
      const isAlreadyInjected = contentScriptRegisterer.register(hostname, code);
      if (isAlreadyInjected !== true) {
        vAPI.tabs.executeScript(details.tabId, {
          code,
          frameId: details.frameId,
          matchAboutBlank: true,
          runAt: "document_start"
        });
      }
      return scriptletDetails;
    }
    toLogger(request, details) {
      if (details === void 0) {
        return;
      }
      if (logger_default.enabled !== true) {
        return;
      }
      if (Array.isArray(details.filters) === false) {
        return;
      }
      background_default.filteringContext.duplicate().fromTabId(request.tabId).setRealm("extended").setType("scriptlet").setURL(request.url).setDocOriginFromURL(request.url).setFilter(details.filters.map((a) => ({ source: "extended", raw: a }))).toLogger();
    }
  };
  var scriptletFilteringEngine = new ScriptletFilteringEngineEx();
  var scriptlet_filtering_default = scriptletFilteringEngine;

  // src/js/reverselookup.ts
  init_static_filtering_parser();
  init_uri_utils();
  init_static_filtering_io();
  init_static_net_filtering();
  var pendingResponses = /* @__PURE__ */ new Map();
  var worker = null;
  var needLists = true;
  var messageId = 1;
  var onWorkerMessage = function(e) {
    const msg = e.data;
    const resolver = pendingResponses.get(msg.id);
    pendingResponses.delete(msg.id);
    resolver(msg.response);
  };
  var stopWorker = function() {
    workerTTLTimer.off();
    if (worker === null) {
      return;
    }
    worker.terminate();
    worker = null;
    needLists = true;
    for (const resolver of pendingResponses.values()) {
      resolver();
    }
    pendingResponses.clear();
  };
  var workerTTLTimer = vAPI.defer.create(stopWorker);
  var workerTTL = { min: 1.5 };
  var initWorker = function() {
    if (worker === null) {
      worker = new Worker("js/reverselookup-worker.js");
      worker.onmessage = onWorkerMessage;
    }
    workerTTLTimer.offon(workerTTL);
    if (needLists === false) {
      return Promise.resolve();
    }
    needLists = false;
    const entries = /* @__PURE__ */ new Map();
    const onListLoaded = function(details) {
      const entry = entries.get(details.assetKey);
      worker.postMessage({
        what: "setList",
        details: {
          assetKey: details.assetKey,
          title: entry.title || details.assetKey,
          supportURL: entry.supportURL,
          content: details.content
        }
      });
    };
    for (const listKey in background_default.availableFilterLists) {
      if (Object.hasOwn(background_default.availableFilterLists, listKey) === false) {
        continue;
      }
      const entry = background_default.availableFilterLists[listKey];
      if (entry.off === true) {
        continue;
      }
      entries.set(listKey, {
        title: listKey !== background_default.userFiltersPath ? entry.title : i18n$("1pPageName"),
        supportURL: entry.supportURL || ""
      });
    }
    if (entries.size === 0) {
      return Promise.resolve();
    }
    const promises = [];
    for (const listKey of entries.keys()) {
      promises.push(
        background_default.getCompiledFilterList(listKey).then((details) => {
          onListLoaded(details);
        })
      );
    }
    return Promise.all(promises);
  };
  var fromNetFilter = async function(rawFilter) {
    if (typeof rawFilter !== "string" || rawFilter === "") {
      return;
    }
    const writer = new CompiledListWriter();
    const parser = new AstFilterParser({
      trustedSource: true,
      maxTokenLength: static_net_filtering_default.MAX_TOKEN_LENGTH,
      nativeCssHas: vAPI.webextFlavor?.env.includes("native_css_has") ?? false
    });
    parser.parse(rawFilter);
    const compiler = static_net_filtering_default.createCompiler();
    if (compiler.compile(parser, writer) === false) {
      return;
    }
    await initWorker();
    const id = messageId++;
    worker.postMessage({
      what: "fromNetFilter",
      id,
      compiledFilter: writer.last(),
      rawFilter
    });
    return new Promise((resolve) => {
      pendingResponses.set(id, resolve);
    });
  };
  var fromExtendedFilter = async function(details) {
    if (typeof details.rawFilter !== "string" || details.rawFilter === "") {
      return;
    }
    await initWorker();
    const id = messageId++;
    const hostname = hostnameFromURI(details.url);
    const parser = new AstFilterParser({
      trustedSource: true,
      nativeCssHas: vAPI.webextFlavor?.env.includes("native_css_has") ?? false
    });
    parser.parse(details.rawFilter);
    let needle;
    if (parser.isScriptletFilter()) {
      needle = JSON.stringify(parser.getScriptletArgs());
    } else if (parser.isResponseheaderFilter()) {
      needle = parser.getResponseheaderName();
    }
    worker.postMessage({
      what: "fromExtendedFilter",
      id,
      url: details.url,
      domain: domainFromHostname(hostname),
      ignoreGeneric: static_net_filtering_default.matchRequestReverse(
        "generichide",
        details.url
      ) === 2,
      ignoreSpecific: static_net_filtering_default.matchRequestReverse(
        "specifichide",
        details.url
      ) === 2,
      rawFilter: details.rawFilter,
      needle
    });
    return new Promise((resolve) => {
      pendingResponses.set(id, resolve);
    });
  };
  var resetLists = function() {
    needLists = true;
    if (worker === null) {
      return;
    }
    worker.postMessage({ what: "resetLists" });
  };
  var staticFilteringReverseLookup = {
    fromNetFilter,
    fromExtendedFilter,
    resetLists,
    shutdown: stopWorker
  };

  // src/js/messaging.ts
  init_static_net_filtering();

  // src/js/traffic.ts
  init_filtering_context();
  init_static_filtering_parser();

  // src/js/html-filtering.ts
  init_uri_utils();
  var pselectors = /* @__PURE__ */ new Map();
  var duplicates = /* @__PURE__ */ new Set();
  var filterDB = new StaticExtFilteringHostnameDB();
  var acceptedCount = 0;
  var discardedCount = 0;
  var docRegister;
  var htmlFilteringEngine = {
    get acceptedCount() {
      return acceptedCount;
    },
    get discardedCount() {
      return discardedCount;
    },
    getFilterCount() {
      return filterDB.size;
    }
  };
  var regexFromString = (s, exact = false) => {
    if (s === "") {
      return /^/;
    }
    const match = /^\/(.+)\/([i]?)$/.exec(s);
    if (match !== null) {
      return new RegExp(match[1], match[2] || void 0);
    }
    const reStr = s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    return new RegExp(exact ? `^${reStr}$` : reStr, "i");
  };
  var PSelectorVoidTask = class {
    constructor(task) {
      console.info(`[uBR] HTML filtering: :${task[0]}() operator is not supported`);
    }
    transpose() {
    }
  };
  var PSelectorHasTextTask = class {
    constructor(task) {
      this.needle = regexFromString(task[1]);
    }
    transpose(node, output) {
      if (this.needle.test(node.textContent)) {
        output.push(node);
      }
    }
  };
  var PSelectorIfTask = class {
    constructor(task) {
      this.pselector = new PSelector(task[1]);
    }
    transpose(node, output) {
      if (this.pselector.test(node) === this.target) {
        output.push(node);
      }
    }
  };
  PSelectorIfTask.prototype.target = true;
  var PSelectorIfNotTask = class extends PSelectorIfTask {
  };
  PSelectorIfNotTask.prototype.target = false;
  var PSelectorMinTextLengthTask = class {
    constructor(task) {
      this.min = task[1];
    }
    transpose(node, output) {
      if (node.textContent.length >= this.min) {
        output.push(node);
      }
    }
  };
  var PSelectorSpathTask = class _PSelectorSpathTask {
    constructor(task) {
      this.spath = task[1];
      this.nth = /^(?:\s*[+~]|:)/.test(this.spath);
      if (this.nth) {
        return;
      }
      if (/^\s*>/.test(this.spath)) {
        this.spath = `:scope ${this.spath.trim()}`;
      }
    }
    transpose(node, output) {
      const nodes = this.nth ? _PSelectorSpathTask.qsa(node, this.spath) : node.querySelectorAll(this.spath);
      for (const node2 of nodes) {
        output.push(node2);
      }
    }
    // Helper method for other operators.
    static qsa(node, selector) {
      const parent = node.parentElement;
      if (parent === null) {
        return [];
      }
      let pos = 1;
      for (; ; ) {
        node = node.previousElementSibling;
        if (node === null) {
          break;
        }
        pos += 1;
      }
      return parent.querySelectorAll(
        `:scope > :nth-child(${pos})${selector}`
      );
    }
  };
  var PSelectorUpwardTask = class {
    constructor(task) {
      const arg = task[1];
      if (typeof arg === "number") {
        this.i = arg;
      } else {
        this.s = arg;
      }
    }
    transpose(node, output) {
      if (this.s !== "") {
        const parent = node.parentElement;
        if (parent === null) {
          return;
        }
        node = parent.closest(this.s);
        if (node === null) {
          return;
        }
      } else {
        let nth = this.i;
        for (; ; ) {
          node = node.parentElement;
          if (node === null) {
            return;
          }
          nth -= 1;
          if (nth === 0) {
            break;
          }
        }
      }
      output.push(node);
    }
  };
  PSelectorUpwardTask.prototype.i = 0;
  PSelectorUpwardTask.prototype.s = "";
  var PSelectorXpathTask = class {
    constructor(task) {
      this.xpe = task[1];
    }
    transpose(node, output) {
      const xpr = docRegister.evaluate(
        this.xpe,
        node,
        null,
        XPathResult.UNORDERED_NODE_SNAPSHOT_TYPE,
        null
      );
      let j2 = xpr.snapshotLength;
      while (j2--) {
        const node2 = xpr.snapshotItem(j2);
        if (node2.nodeType === 1) {
          output.push(node2);
        }
      }
    }
  };
  var PSelector = class {
    constructor(o) {
      this.raw = o.raw;
      this.selector = o.selector;
      this.tasks = [];
      if (!o.tasks) {
        return;
      }
      for (const task of o.tasks) {
        const ctor = this.operatorToTaskMap.get(task[0]) || PSelectorVoidTask;
        const pselector = new ctor(task);
        this.tasks.push(pselector);
      }
    }
    prime(input) {
      const root = input || docRegister;
      if (this.selector === "") {
        return [root];
      }
      if (input !== docRegister && /^ ?[>+~]/.test(this.selector)) {
        return Array.from(PSelectorSpathTask.qsa(input, this.selector));
      }
      return Array.from(root.querySelectorAll(this.selector));
    }
    exec(input) {
      let nodes = this.prime(input);
      for (const task of this.tasks) {
        if (nodes.length === 0) {
          break;
        }
        const transposed = [];
        for (const node of nodes) {
          task.transpose(node, transposed);
        }
        nodes = transposed;
      }
      return nodes;
    }
    test(input) {
      const nodes = this.prime(input);
      for (const node of nodes) {
        let output = [node];
        for (const task of this.tasks) {
          const transposed = [];
          for (const node2 of output) {
            task.transpose(node2, transposed);
          }
          output = transposed;
          if (output.length === 0) {
            break;
          }
        }
        if (output.length !== 0) {
          return true;
        }
      }
      return false;
    }
  };
  PSelector.prototype.operatorToTaskMap = /* @__PURE__ */ new Map([
    ["has", PSelectorIfTask],
    ["has-text", PSelectorHasTextTask],
    ["if", PSelectorIfTask],
    ["if-not", PSelectorIfNotTask],
    ["min-text-length", PSelectorMinTextLengthTask],
    ["not", PSelectorIfNotTask],
    ["nth-ancestor", PSelectorUpwardTask],
    ["spath", PSelectorSpathTask],
    ["upward", PSelectorUpwardTask],
    ["xpath", PSelectorXpathTask]
  ]);
  function logOne(details, exception, selector) {
    background_default.filteringContext.duplicate().fromTabId(details.tabId).setRealm("extended").setType("dom").setURL(details.url).setDocOriginFromURL(details.url).setFilter({
      source: "extended",
      raw: `${exception === 0 ? "##" : "#@#"}^${selector}`
    }).toLogger();
  }
  function applyProceduralSelector(details, selector) {
    let pselector = pselectors.get(selector);
    if (pselector === void 0) {
      pselector = new PSelector(JSON.parse(selector));
      pselectors.set(selector, pselector);
    }
    const nodes = pselector.exec();
    let modified = false;
    for (const node of nodes) {
      node.remove();
      modified = true;
    }
    if (modified && logger_default.enabled) {
      logOne(details, 0, pselector.raw);
    }
    return modified;
  }
  function applyCSSSelector(details, selector) {
    const nodes = docRegister.querySelectorAll(selector);
    let modified = false;
    for (const node of nodes) {
      node.remove();
      modified = true;
    }
    if (modified && logger_default.enabled) {
      logOne(details, 0, selector);
    }
    return modified;
  }
  function logError(writer, msg) {
    logger_default.writeOne({
      realm: "message",
      type: "error",
      text: msg.replace("{who}", writer.properties.get("name") || "?")
    });
  }
  htmlFilteringEngine.reset = function() {
    filterDB.clear();
    pselectors.clear();
    duplicates.clear();
    acceptedCount = 0;
    discardedCount = 0;
  };
  htmlFilteringEngine.freeze = function() {
    duplicates.clear();
    filterDB.collectGarbage();
  };
  htmlFilteringEngine.compile = function(parser, writer) {
    const isException = parser.isException();
    const { raw, compiled } = parser.result;
    if (compiled === void 0) {
      return logError(writer, `Invalid HTML filter in {who}: ##${raw}`);
    }
    writer.select("HTML_FILTERS");
    if (parser.hasOptions() === false) {
      if (isException) {
        writer.push([64, "", 1, compiled]);
      }
      return;
    }
    const compiledFilters = [];
    let hasOnlyNegated = true;
    for (const { hn: hn2, not, bad } of parser.getExtFilterDomainIterator()) {
      if (bad) {
        continue;
      }
      const prefix = (isException ? 1 : 0) ^ (not ? 1 : 0) ? "-" : "+";
      if (not === false) {
        hasOnlyNegated = false;
      }
      compiledFilters.push([64, hn2, `${prefix}${compiled}`]);
    }
    if (isException === false && hasOnlyNegated) {
      return logError(writer, `Invalid HTML filter in {who}: ##${raw}`);
    }
    writer.pushMany(compiledFilters);
  };
  htmlFilteringEngine.fromCompiledContent = function(reader) {
    if (background_default.canFilterResponseData === false) {
      return;
    }
    reader.select("HTML_FILTERS");
    while (reader.next()) {
      acceptedCount += 1;
      const fingerprint = reader.fingerprint();
      if (duplicates.has(fingerprint)) {
        discardedCount += 1;
        continue;
      }
      duplicates.add(fingerprint);
      const args = reader.args();
      filterDB.store(args[1], args[2]);
    }
  };
  htmlFilteringEngine.retrieve = function(fctxt) {
    const all = /* @__PURE__ */ new Set();
    const hostname = fctxt.getHostname();
    filterDB.retrieveSpecifics(all, hostname);
    const entity = entityFromHostname(hostname, fctxt.getDomain());
    filterDB.retrieveSpecifics(all, entity);
    filterDB.retrieveSpecificsByRegex(all, hostname, fctxt.url);
    filterDB.retrieveGenerics(all);
    if (all.size === 0) {
      return;
    }
    if (background_default.userSettings.advancedUserEnabled) {
      if (sessionFirewall.evaluateCellZY(hostname, hostname, "*") === 2) {
        return;
      }
    }
    const plains = /* @__PURE__ */ new Set();
    const procedurals = /* @__PURE__ */ new Set();
    for (const s of all) {
      if (s.charCodeAt(0) === 45) {
        continue;
      }
      const selector = s.slice(1);
      const isProcedural = selector.startsWith("{");
      if (all.has(`-${selector}`)) {
        logOne(fctxt, 1, isProcedural ? JSON.parse(selector).raw : selector);
      } else if (isProcedural) {
        procedurals.add(selector);
      } else {
        plains.add(selector);
      }
    }
    if (plains.size === 0 && procedurals.size === 0) {
      return;
    }
    return { plains, procedurals };
  };
  htmlFilteringEngine.apply = function(doc, details, selectors) {
    docRegister = doc;
    let modified = false;
    for (const selector of selectors.plains) {
      if (applyCSSSelector(details, selector)) {
        modified = true;
      }
    }
    for (const selector of selectors.procedurals) {
      if (applyProceduralSelector(details, selector)) {
        modified = true;
      }
    }
    docRegister = void 0;
    return modified;
  };
  htmlFilteringEngine.toSelfie = function() {
    return filterDB.toSelfie();
  };
  htmlFilteringEngine.fromSelfie = function(selfie) {
    filterDB.fromSelfie(selfie);
    pselectors.clear();
  };
  var html_filtering_default = htmlFilteringEngine;

  // src/js/httpheader-filtering.ts
  init_uri_utils();
  var duplicates2 = /* @__PURE__ */ new Set();
  var filterDB2 = new StaticExtFilteringHostnameDB();
  var acceptedCount2 = 0;
  var discardedCount2 = 0;
  var headerIndexFromName = function(name2, headers, start = 0) {
    for (let i = start; i < headers.length; i++) {
      if (headers[i].name.toLowerCase() !== name2) {
        continue;
      }
      return i;
    }
    return -1;
  };
  var logOne2 = function(isException, token, fctxt) {
    fctxt.duplicate().setRealm("extended").setType("header").setFilter({
      modifier: true,
      result: isException ? 2 : 1,
      source: "extended",
      raw: `${isException ? "#@#" : "##"}^responseheader(${token})`
    }).toLogger();
  };
  var httpheaderFilteringEngine = {
    get acceptedCount() {
      return acceptedCount2;
    },
    get discardedCount() {
      return discardedCount2;
    }
  };
  httpheaderFilteringEngine.reset = function() {
    filterDB2.clear();
    duplicates2.clear();
    acceptedCount2 = 0;
    discardedCount2 = 0;
  };
  httpheaderFilteringEngine.freeze = function() {
    duplicates2.clear();
    filterDB2.collectGarbage();
  };
  httpheaderFilteringEngine.compile = function(parser, writer) {
    writer.select("HTTPHEADER_FILTERS");
    const isException = parser.isException();
    const headerName = parser.getResponseheaderName();
    if (headerName === "" && isException === false) {
      return;
    }
    if (parser.hasOptions() === false) {
      if (isException) {
        writer.push([64, "", 1, headerName]);
      }
      return;
    }
    for (const { hn: hn2, not, bad } of parser.getExtFilterDomainIterator()) {
      if (bad) {
        continue;
      }
      const prefix = (isException ? 1 : 0) ^ (not ? 1 : 0) ? "-" : "+";
      writer.push([64, hn2, `${prefix}${headerName}`]);
    }
  };
  httpheaderFilteringEngine.fromCompiledContent = function(reader) {
    reader.select("HTTPHEADER_FILTERS");
    while (reader.next()) {
      acceptedCount2 += 1;
      const fingerprint = reader.fingerprint();
      if (duplicates2.has(fingerprint)) {
        discardedCount2 += 1;
        continue;
      }
      duplicates2.add(fingerprint);
      const args = reader.args();
      filterDB2.store(args[1], args[2]);
    }
  };
  httpheaderFilteringEngine.apply = function(fctxt, headers) {
    if (filterDB2.size === 0) {
      return;
    }
    const hostname = fctxt.getHostname();
    if (hostname === "") {
      return;
    }
    const all = /* @__PURE__ */ new Set();
    filterDB2.retrieveSpecifics(all, hostname);
    const entity = entityFromHostname(hostname, fctxt.getDomain());
    filterDB2.retrieveSpecifics(all, entity);
    filterDB2.retrieveSpecificsByRegex(all, hostname, fctxt.url);
    filterDB2.retrieveGenerics(all);
    if (all.size === 0) {
      return;
    }
    if (background_default.userSettings.advancedUserEnabled) {
      if (sessionFirewall.evaluateCellZY(hostname, hostname, "*") === 2) {
        return;
      }
    }
    const filters = /* @__PURE__ */ new Set();
    const exceptions = /* @__PURE__ */ new Set();
    for (const s of all) {
      const selector = s.slice(1);
      if (s.charCodeAt(0) === 45) {
        exceptions.add(selector);
      } else {
        filters.add(selector);
      }
    }
    const hasGlobalException = exceptions.has("");
    let modified = false;
    let i = 0;
    for (const name2 of filters) {
      const isExcepted = hasGlobalException || exceptions.has(name2);
      if (isExcepted) {
        if (logger_default.enabled) {
          logOne2(true, hasGlobalException ? "" : name2, fctxt);
        }
        continue;
      }
      i = 0;
      for (; ; ) {
        i = headerIndexFromName(name2, headers, i);
        if (i === -1) {
          break;
        }
        headers.splice(i, 1);
        if (logger_default.enabled) {
          logOne2(false, name2, fctxt);
        }
        modified = true;
      }
    }
    return modified;
  };
  httpheaderFilteringEngine.toSelfie = function() {
    return filterDB2.toSelfie();
  };
  httpheaderFilteringEngine.fromSelfie = function(selfie) {
    filterDB2.fromSelfie(selfie);
  };
  var httpheader_filtering_default = httpheaderFilteringEngine;

  // src/js/traffic.ts
  init_uri_utils();
  init_static_net_filtering();

  // src/js/text-encode.ts
  var textEncode = (() => {
    if (background_default.canFilterResponseData !== true) {
      return;
    }
    const normalizedCharset = /* @__PURE__ */ new Map([
      ["utf8", "utf-8"],
      ["unicode-1-1-utf-8", "utf-8"],
      ["utf-8", "utf-8"],
      ["windows-1250", "windows-1250"],
      ["cp1250", "windows-1250"],
      ["x-cp1250", "windows-1250"],
      ["windows-1251", "windows-1251"],
      ["cp1251", "windows-1251"],
      ["x-cp1251", "windows-1251"],
      ["windows-1252", "windows-1252"],
      ["ansi_x3.4-1968", "windows-1252"],
      ["ascii", "windows-1252"],
      ["cp1252", "windows-1252"],
      ["cp819", "windows-1252"],
      ["csisolatin1", "windows-1252"],
      ["ibm819", "windows-1252"],
      ["iso-8859-1", "windows-1252"],
      ["iso-ir-100", "windows-1252"],
      ["iso8859-1", "windows-1252"],
      ["iso88591", "windows-1252"],
      ["iso_8859-1", "windows-1252"],
      ["iso_8859-1:1987", "windows-1252"],
      ["l1", "windows-1252"],
      ["latin1", "windows-1252"],
      ["us-ascii", "windows-1252"],
      ["x-cp1252", "windows-1252"]
    ]);
    const cp1250_range0 = new Uint8Array([
      /* 0x0100 */
      0,
      0,
      195,
      227,
      165,
      185,
      198,
      230,
      /* 0x0108 */
      0,
      0,
      0,
      0,
      200,
      232,
      207,
      239,
      /* 0x0110 */
      208,
      240,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0118 */
      202,
      234,
      204,
      236,
      0,
      0,
      0,
      0,
      /* 0x0120 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0128 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0130 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0138 */
      0,
      197,
      229,
      0,
      0,
      188,
      190,
      0,
      /* 0x0140 */
      0,
      163,
      179,
      209,
      241,
      0,
      0,
      210,
      /* 0x0148 */
      242,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0150 */
      213,
      245,
      0,
      0,
      192,
      224,
      0,
      0,
      /* 0x0158 */
      216,
      248,
      140,
      156,
      0,
      0,
      170,
      186,
      /* 0x0160 */
      138,
      154,
      222,
      254,
      141,
      157,
      0,
      0,
      /* 0x0168 */
      0,
      0,
      0,
      0,
      0,
      0,
      217,
      249,
      /* 0x0170 */
      219,
      251,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0178 */
      0,
      143,
      159,
      175,
      191,
      142,
      158,
      0
    ]);
    const cp1251_range0 = new Uint8Array([
      /* 0x0400 */
      0,
      168,
      128,
      129,
      170,
      189,
      178,
      175,
      /* 0x0408 */
      163,
      138,
      140,
      142,
      141,
      0,
      161,
      143,
      /* 0x0410 */
      192,
      193,
      194,
      195,
      196,
      197,
      198,
      199,
      /* 0x0418 */
      200,
      201,
      202,
      203,
      204,
      205,
      206,
      207,
      /* 0x0420 */
      208,
      209,
      210,
      211,
      212,
      213,
      214,
      215,
      /* 0x0428 */
      216,
      217,
      218,
      219,
      220,
      221,
      222,
      223,
      /* 0x0430 */
      224,
      225,
      226,
      227,
      228,
      229,
      230,
      231,
      /* 0x0438 */
      232,
      233,
      234,
      235,
      236,
      237,
      238,
      239,
      /* 0x0440 */
      240,
      241,
      242,
      243,
      244,
      245,
      246,
      247,
      /* 0x0448 */
      248,
      249,
      250,
      251,
      252,
      253,
      254,
      255,
      /* 0x0450 */
      0,
      184,
      144,
      131,
      186,
      190,
      179,
      191,
      /* 0x0458 */
      188,
      154,
      156,
      158,
      157,
      0,
      162,
      159,
      /* 0x0460 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0468 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0470 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0478 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0480 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0488 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0490 */
      165,
      180,
      0,
      0,
      0,
      0,
      0,
      0
    ]);
    const cp1252_range0 = new Uint8Array([
      /* 0x0150 */
      0,
      0,
      140,
      156,
      0,
      0,
      0,
      0,
      /* 0x0158 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0160 */
      138,
      154,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0168 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0170 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x0178 */
      159,
      0,
      0,
      0,
      0,
      142,
      158,
      0
    ]);
    const cp125x_range0 = new Uint8Array([
      /* 0x2010 */
      0,
      0,
      0,
      150,
      151,
      0,
      0,
      0,
      /* 0x2018 */
      145,
      146,
      130,
      0,
      147,
      148,
      132,
      0,
      /* 0x2020 */
      134,
      135,
      149,
      0,
      0,
      0,
      133,
      0,
      /* 0x2028 */
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x2030 */
      137,
      0,
      0,
      0,
      0,
      0,
      0,
      0,
      /* 0x2038 */
      0,
      139,
      155,
      0,
      0,
      0,
      0,
      0
    ]);
    const encoders = {
      "windows-1250": function(buf) {
        let i = 0, n = buf.byteLength, o = 0, c;
        while (i < n) {
          c = buf[i++];
          if (c < 128) {
            buf[o++] = c;
          } else {
            if ((c & 224) === 192) {
              c = (c & 31) << 6;
              c |= buf[i++] & 63;
            } else if ((c & 240) === 224) {
              c = (c & 15) << 12;
              c |= (buf[i++] & 63) << 6;
              c |= buf[i++] & 63;
            } else if ((c & 248) === 240) {
              c = (c & 7) << 18;
              c |= (buf[i++] & 63) << 12;
              c |= (buf[i++] & 63) << 6;
              c |= buf[i++] & 63;
            }
            if (c < 256) {
              buf[o++] = c;
            } else if (c < 384) {
              buf[o++] = cp1250_range0[c - 256];
            } else if (c >= 8208 && c < 8256) {
              buf[o++] = cp125x_range0[c - 8208];
            } else if (c === 711) {
              buf[o++] = 161;
            } else if (c === 728) {
              buf[o++] = 162;
            } else if (c === 729) {
              buf[o++] = 255;
            } else if (c === 731) {
              buf[o++] = 178;
            } else if (c === 733) {
              buf[o++] = 189;
            } else if (c === 8364) {
              buf[o++] = 136;
            } else if (c === 8482) {
              buf[o++] = 153;
            }
          }
        }
        return buf.slice(0, o);
      },
      "windows-1251": function(buf) {
        let i = 0, n = buf.byteLength, o = 0, c;
        while (i < n) {
          c = buf[i++];
          if (c < 128) {
            buf[o++] = c;
          } else {
            if ((c & 224) === 192) {
              c = (c & 31) << 6;
              c |= buf[i++] & 63;
            } else if ((c & 240) === 224) {
              c = (c & 15) << 12;
              c |= (buf[i++] & 63) << 6;
              c |= buf[i++] & 63;
            } else if ((c & 248) === 240) {
              c = (c & 7) << 18;
              c |= (buf[i++] & 63) << 12;
              c |= (buf[i++] & 63) << 6;
              c |= buf[i++] & 63;
            }
            if (c < 256) {
              buf[o++] = c;
            } else if (c >= 1024 && c < 1184) {
              buf[o++] = cp1251_range0[c - 1024];
            } else if (c >= 8208 && c < 8256) {
              buf[o++] = cp125x_range0[c - 8208];
            } else if (c === 8364) {
              buf[o++] = 136;
            } else if (c === 8470) {
              buf[o++] = 185;
            } else if (c === 8482) {
              buf[o++] = 153;
            }
          }
        }
        return buf.slice(0, o);
      },
      "windows-1252": function(buf) {
        let i = 0, n = buf.byteLength, o = 0, c;
        while (i < n) {
          c = buf[i++];
          if (c < 128) {
            buf[o++] = c;
          } else {
            if ((c & 224) === 192) {
              c = (c & 31) << 6;
              c |= buf[i++] & 63;
            } else if ((c & 240) === 224) {
              c = (c & 15) << 12;
              c |= (buf[i++] & 63) << 6;
              c |= buf[i++] & 63;
            } else if ((c & 248) === 240) {
              c = (c & 7) << 18;
              c |= (buf[i++] & 63) << 12;
              c |= (buf[i++] & 63) << 6;
              c |= buf[i++] & 63;
            }
            if (c < 256) {
              buf[o++] = c;
            } else if (c >= 336 && c < 384) {
              buf[o++] = cp1252_range0[c - 336];
            } else if (c >= 8208 && c < 8256) {
              buf[o++] = cp125x_range0[c - 8208];
            } else if (c === 402) {
              buf[o++] = 131;
            } else if (c === 710) {
              buf[o++] = 136;
            } else if (c === 732) {
              buf[o++] = 152;
            } else if (c === 8364) {
              buf[o++] = 128;
            } else if (c === 8482) {
              buf[o++] = 153;
            }
          }
        }
        return buf.slice(0, o);
      }
    };
    return {
      encode: function(charset, buf) {
        return Object.hasOwn(encoders, charset) ? encoders[charset](buf) : buf;
      },
      normalizeCharset: function(charset) {
        if (charset === void 0) {
          return "utf-8";
        }
        return normalizedCharset.get(charset.toLowerCase());
      }
    };
  })();
  var text_encode_default = textEncode;

  // src/js/traffic.ts
  var isGecko = vAPI.webextFlavor.isGecko;
  var patchLocalRedirectURL = (url) => url.charCodeAt(0) === 47 ? vAPI.getURL(url) : url;
  function onBeforeRequest(details) {
    const fctxt = background_default.filteringContext.fromWebrequestDetails(details);
    if (fctxt.itype === fctxt.MAIN_FRAME) {
      return onBeforeRootFrameRequest(fctxt);
    }
    const tabId = details.tabId;
    if (tabId < 0) {
      return onBeforeBehindTheSceneRequest(fctxt);
    }
    let pageStore = background_default.pageStoreFromTabId(tabId);
    if (pageStore === null) {
      const tabContext = background_default.tabContextManager.mustLookup(tabId);
      if (tabContext.tabId < 0) {
        return onBeforeBehindTheSceneRequest(fctxt);
      }
      vAPI.tabs.onNavigation({ tabId, frameId: 0, url: tabContext.rawURL });
      pageStore = background_default.pageStoreFromTabId(tabId);
    }
    const result = pageStore.filterRequest(fctxt);
    pageStore.journalAddRequest(fctxt, result);
    if (logger_default.enabled) {
      fctxt.setRealm("network").toLogger();
    }
    if (fctxt.redirectURL !== void 0) {
      return { redirectUrl: patchLocalRedirectURL(fctxt.redirectURL) };
    }
    if (result === 1) {
      return { cancel: true };
    }
    if (fctxt.itype === fctxt.SUB_FRAME && details.parentFrameId !== -1 && details.aliasURL === void 0) {
      pageStore.setFrameURL(details);
    }
    if (result === 2) {
      return { cancel: false };
    }
  }
  function onBeforeRootFrameRequest(fctxt) {
    const requestURL = fctxt.url;
    const requestHostname = fctxt.getHostname();
    let result = 0;
    let logData;
    const trusted = background_default.getNetFilteringSwitch(requestURL) === false;
    if (trusted) {
      result = 2;
      if (logger_default.enabled) {
        logData = { engine: "u", result: 2, raw: "whitelisted" };
      }
    }
    if (result === 0 && sessionSwitches.evaluateZ("no-strict-blocking", requestHostname)) {
      result = 2;
      if (logger_default.enabled) {
        logData = {
          engine: "u",
          result: 2,
          raw: `no-strict-blocking: ${sessionSwitches.z} true`
        };
      }
    }
    if (result === 0 && strictBlockBypasser.isBypassed(requestHostname)) {
      result = 2;
      if (logger_default.enabled) {
        logData = {
          engine: "u",
          result: 2,
          raw: "no-strict-blocking: true (temporary)"
        };
      }
    }
    if (result === 0) {
      const shouldStrictBlockResult = shouldStrictBlock(fctxt, logger_default.enabled);
      result = shouldStrictBlockResult.result;
      logData = shouldStrictBlockResult.logData;
    }
    const pageStore = background_default.bindTabToPageStore(fctxt.tabId, "beforeRequest");
    if (pageStore !== null) {
      pageStore.journalAddRootFrame("uncommitted", requestURL);
      pageStore.journalAddRequest(fctxt, result);
    }
    if (logger_default.enabled) {
      fctxt.setRealm("network").setFilter(logData);
    }
    if (trusted === false && pageStore !== null) {
      if (result !== 1) {
        pageStore.redirectNonBlockedRequest(fctxt);
      } else {
        pageStore.skipMainDocument(fctxt, true);
      }
    }
    if (logger_default.enabled) {
      fctxt.toLogger();
    }
    if (fctxt.redirectURL !== void 0) {
      return { redirectUrl: patchLocalRedirectURL(fctxt.redirectURL) };
    }
    if (result !== 1) {
      return;
    }
    if (logData === void 0) {
      return;
    }
    let reason = logData.reason;
    pageStore.skipMainDocument(fctxt, false);
    if (reason === void 0 && Array.isArray(fctxt.filter)) {
      const filter = fctxt.filter.find((a) => a.reason !== void 0);
      reason = filter?.reason;
    }
    const query = {
      url: requestURL,
      dn: fctxt.getDomain() || requestHostname,
      fs: logData.raw,
      hn: requestHostname,
      to: fctxt.redirectURL || ""
    };
    if (reason) {
      query.reason = reason;
    }
    vAPI.tabs.replace(
      fctxt.tabId,
      `${vAPI.getURL("document-blocked.html?details=")}${encodeURIComponent(JSON.stringify(query))}`
    );
    return { cancel: true };
  }
  function shouldStrictBlock(fctxt, loggerEnabled) {
    const snfe = static_net_filtering_default;
    const rs2 = snfe.matchRequest(fctxt, 3);
    const is2 = rs2 === 1 && snfe.isBlockImportant();
    let lds;
    if (rs2 !== 0 || loggerEnabled) {
      lds = snfe.toLogData();
    }
    if (rs2 === 1 && is2) {
      return { result: rs2, logData: lds };
    }
    fctxt.type = "no_type";
    let rg = snfe.matchRequest(fctxt, 3);
    fctxt.type = "main_frame";
    const ig = rg === 1 && snfe.isBlockImportant();
    let ldg;
    if (rg !== 0 || loggerEnabled) {
      ldg = snfe.toLogData();
      if (rg === 1 && validateStrictBlock(fctxt, ldg) === false) {
        rg = 0;
        ldg = void 0;
      }
    }
    if (rs2 === 0 || rg === 1 && ig || rg === 2 && rs2 !== 2) {
      return { result: rg, logData: ldg };
    }
    return { result: rs2, logData: lds };
  }
  function validateStrictBlock(fctxt, logData) {
    if (typeof logData?.regex !== "string") {
      return false;
    }
    if (typeof logData?.raw === "string" && /\w/.test(logData.raw) === false) {
      return false;
    }
    const url = fctxt.url;
    const re2 = new RegExp(logData.regex, "i");
    const match = re2.exec(url.toLowerCase());
    if (match === null) {
      return false;
    }
    const hostname = fctxt.getHostname();
    const hnpos = url.indexOf(hostname);
    const hnlen = hostname.length;
    const end = match.index + match[0].length - hnpos - hnlen;
    return end === 0 || end === 1 || end === 2 && url.charCodeAt(hnpos + hnlen) === 46;
  }
  function onBeforeBehindTheSceneRequest(fctxt) {
    const pageStore = background_default.pageStoreFromTabId(fctxt.tabId);
    if (pageStore === null) {
      return;
    }
    let result = 0;
    if (fctxt.tabOrigin.endsWith("-scheme") === false && isNetworkURI(fctxt.tabOrigin) || background_default.userSettings.advancedUserEnabled || fctxt.itype === fctxt.CSP_REPORT) {
      result = pageStore.filterRequest(fctxt);
      if (result === 1 && background_default.getNetFilteringSwitch(fctxt.tabOrigin) === false) {
        result = 2;
        fctxt.redirectURL = void 0;
        fctxt.filter = { engine: "u", result: 2, raw: "whitelisted" };
      }
    }
    onBeforeBehindTheSceneRequest.journalAddRequest(fctxt, result);
    if (logger_default.enabled) {
      fctxt.setRealm("network").toLogger();
    }
    if (fctxt.redirectURL !== void 0) {
      return { redirectUrl: patchLocalRedirectURL(fctxt.redirectURL) };
    }
    if (result === 1) {
      return { cancel: true };
    }
  }
  {
    const pageStores = /* @__PURE__ */ new Set();
    let hostname = "";
    let pageStoresToken = 0;
    const reset = function() {
      hostname = "";
      pageStores.clear();
      pageStoresToken = 0;
    };
    const gc2 = () => {
      if (pageStoresToken !== background_default.pageStoresToken) {
        return reset();
      }
      gcTimer.on(30011);
    };
    const gcTimer = vAPI.defer.create(gc2);
    onBeforeBehindTheSceneRequest.journalAddRequest = (fctxt, result) => {
      const docHostname = fctxt.getDocHostname();
      if (docHostname !== hostname || pageStoresToken !== background_default.pageStoresToken) {
        hostname = docHostname;
        pageStores.clear();
        for (const pageStore of background_default.pageStores.values()) {
          if (pageStore.tabHostname !== docHostname) {
            continue;
          }
          pageStores.add(pageStore);
        }
        pageStoresToken = background_default.pageStoresToken;
        gcTimer.offon(30011);
      }
      for (const pageStore of pageStores) {
        pageStore.journalAddRequest(fctxt, result);
      }
    };
  }
  function onHeadersReceived(details) {
    const fctxt = background_default.filteringContext.fromWebrequestDetails(details);
    const isRootDoc = fctxt.itype === fctxt.MAIN_FRAME;
    let pageStore = background_default.pageStoreFromTabId(fctxt.tabId);
    if (pageStore === null) {
      if (isRootDoc === false) {
        return;
      }
      pageStore = background_default.bindTabToPageStore(fctxt.tabId, "beforeRequest");
    }
    if (pageStore.getNetFilteringSwitch(fctxt) === false) {
      return;
    }
    if (isRootDoc && fctxt.ipaddress) {
      const r = onBeforeRootFrameRequest(fctxt);
      if (r) {
        return r;
      }
    }
    if ((fctxt.itype & foilLargeMediaElement.TYPE_BITS) !== 0) {
      const result = foilLargeMediaElement(details, fctxt, pageStore);
      if (result !== void 0) {
        return result;
      }
    }
    const { responseHeaders } = details;
    if (Array.isArray(responseHeaders) === false) {
      return;
    }
    if (isRootDoc === false) {
      const result = pageStore.filterOnHeaders(
        fctxt,
        responseHeaders,
        requestHeadersManager.lookup(details)
      );
      if (result !== 0) {
        if (logger_default.enabled) {
          fctxt.setRealm("network").toLogger();
        }
        if (result === 1) {
          pageStore.journalAddRequest(fctxt, 1);
          return { cancel: true };
        }
      }
    }
    const mime = mimeFromHeaders(responseHeaders);
    if (isRootDoc) {
      if (reMediaContentTypes.test(mime)) {
        pageStore.allowLargeMediaElementsUntil = 0;
      }
    }
    if (bodyFilterer.canFilter(fctxt, details)) {
      const jobs = [];
      const replaceDirectives = static_net_filtering_default.matchAndFetchModifiers(fctxt, "replace");
      if (replaceDirectives) {
        jobs.push({
          fn: textResponseFilterer,
          args: [replaceDirectives]
        });
      }
      if (mime === "text/html" || mime === "application/xhtml+xml") {
        const selectors = html_filtering_default.retrieve(fctxt);
        if (selectors) {
          jobs.push({
            fn: htmlResponseFilterer,
            args: [selectors]
          });
        }
      }
      if (jobs.length !== 0) {
        bodyFilterer.doFilter(details.requestId, fctxt, jobs);
      }
    }
    let modifiedHeaders = false;
    if (httpheader_filtering_default.apply(fctxt, responseHeaders) === true) {
      modifiedHeaders = true;
    }
    if (fctxt.isDocument()) {
      if (injectCSP(fctxt, pageStore, responseHeaders) === true) {
        modifiedHeaders = true;
      }
      if (injectPP(fctxt, pageStore, responseHeaders) === true) {
        modifiedHeaders = true;
      }
    }
    if (modifiedHeaders && isGecko) {
      const cacheControl = background_default.hiddenSettings.cacheControlForFirefox1376932;
      if (cacheControl !== "unset") {
        const i = headerIndexFromName2("cache-control", responseHeaders);
        if (i !== -1) {
          responseHeaders[i].value = cacheControl;
        } else {
          responseHeaders.push({ name: "Cache-Control", value: cacheControl });
        }
        modifiedHeaders = true;
      }
    }
    if (modifiedHeaders) {
      return { responseHeaders };
    }
  }
  var reMediaContentTypes = /^(?:audio|image|video)\/|(?:\/ogg)$/;
  var mimeFromHeaders = (headers) => {
    if (Array.isArray(headers) === false) {
      return "";
    }
    return mimeFromContentType(headerValueFromName("content-type", headers));
  };
  var mimeFromContentType = (contentType) => {
    const match = reContentTypeMime.exec(contentType);
    if (match === null) {
      return "";
    }
    return match[0].toLowerCase();
  };
  var reContentTypeMime = /^[^;]+/i;
  function textResponseFilterer(session, directives) {
    const applied = [];
    for (const directive of directives) {
      if (directive.refs instanceof Object === false) {
        continue;
      }
      if (directive.result !== 1) {
        applied.push(directive);
        continue;
      }
      const { refs } = directive;
      if (refs.$cache !== null) {
        const { jsonp } = refs.$cache;
        if (jsonp && jsonp.apply === void 0) {
          refs.$cache = null;
        }
      }
      if (refs.$cache === null) {
        refs.$cache = parseReplaceValue(refs.value);
      }
      const cache = refs.$cache;
      if (cache === void 0) {
        continue;
      }
      switch (cache.type) {
        case "json": {
          const json = session.getString();
          let obj;
          try {
            obj = JSON.parse(json);
          } catch (e) {
            console.warn("[uBR] traffic: json JSON.parse failed", e);
            break;
          }
          const objAfter = cache.jsonp.apply(obj);
          if (objAfter === void 0) {
            break;
          }
          session.setString(cache.jsonp.toJSON(objAfter));
          applied.push(directive);
          break;
        }
        case "jsonl": {
          const linesBefore = session.getString().split(/\n+/);
          const linesAfter = [];
          for (const lineBefore of linesBefore) {
            let obj;
            try {
              obj = JSON.parse(lineBefore);
            } catch (e) {
              console.warn("[uBR] traffic: jsonl JSON.parse failed", e);
            }
            if (typeof obj !== "object" || obj === null) {
              linesAfter.push(lineBefore);
              continue;
            }
            const objAfter = cache.jsonp.apply(obj);
            if (objAfter === void 0) {
              linesAfter.push(lineBefore);
              continue;
            }
            linesAfter.push(cache.jsonp.toJSON(objAfter));
          }
          session.setString(linesAfter.join("\n"));
          break;
        }
        case "text": {
          cache.re.lastIndex = 0;
          if (cache.re.test(session.getString()) !== true) {
            break;
          }
          cache.re.lastIndex = 0;
          session.setString(session.getString().replace(
            cache.re,
            cache.replacement
          ));
          applied.push(directive);
          break;
        }
        default:
          break;
      }
    }
    if (applied.length === 0) {
      return;
    }
    if (logger_default.enabled !== true) {
      return;
    }
    session.setRealm("network").pushFilters(applied.map((a) => a.logData())).toLogger();
  }
  function htmlResponseFilterer(session, selectors) {
    if (htmlResponseFilterer.domParser === null) {
      htmlResponseFilterer.domParser = new DOMParser();
      htmlResponseFilterer.xmlSerializer = new XMLSerializer();
    }
    const doc = htmlResponseFilterer.domParser.parseFromString(
      session.getString(),
      session.mime
    );
    if (selectors === void 0) {
      return;
    }
    if (html_filtering_default.apply(doc, session, selectors) !== true) {
      return;
    }
    const doctypeStr = [
      doc.doctype instanceof Object ? `${htmlResponseFilterer.xmlSerializer.serializeToString(doc.doctype)}
` : "",
      doc.documentElement.outerHTML
    ].join("\n");
    session.setString(doctypeStr);
  }
  htmlResponseFilterer.domParser = null;
  htmlResponseFilterer.xmlSerializer = null;
  var bodyFilterer = (() => {
    const sessions = /* @__PURE__ */ new Map();
    const reContentTypeCharset = /charset=['"]?([^'" ]+)/i;
    const otherValidMimes = /* @__PURE__ */ new Set([
      "application/dash+xml",
      "application/javascript",
      "application/json",
      "application/mpegurl",
      "application/vnd.api+json",
      "application/vnd.apple.mpegurl",
      "application/vnd.apple.mpegurl.audio",
      "application/x-javascript",
      "application/x-mpegurl",
      "application/xhtml+xml",
      "application/xml",
      "audio/mpegurl",
      "audio/x-mpegurl"
    ]);
    const BINARY_TYPES = FONT | IMAGE | MEDIA | WEBSOCKET;
    const MAX_RESPONSE_BUFFER_LENGTH = 10 * 1024 * 1024;
    let textDecoder2;
    let textEncoder2;
    let mime = "";
    let charset = "";
    const contentTypeFromDetails = (details) => {
      switch (details.type) {
        case "script":
          return "text/javascript; charset=utf-8";
        case "stylesheet":
          return "text/css";
        default:
          break;
      }
      return "";
    };
    const charsetFromContentType = (contentType) => {
      const match = reContentTypeCharset.exec(contentType);
      if (match === null) {
        return;
      }
      return match[1].toLowerCase();
    };
    const charsetFromMime = (mimeVal) => {
      switch (mimeVal) {
        case "application/xml":
        case "application/xhtml+xml":
        case "text/html":
        case "text/css":
          return;
        default:
          break;
      }
      return "utf-8";
    };
    const charsetFromStream = (bytes) => {
      if (bytes.length < 3) {
        return;
      }
      if (bytes[0] === 239 && bytes[1] === 187 && bytes[2] === 191) {
        return "utf-8";
      }
      let i = -1;
      while (i < 65536) {
        i += 1;
        if (bytes[i + 0] !== 99) {
          continue;
        }
        if (bytes[i + 1] !== 104) {
          continue;
        }
        if (bytes[i + 2] !== 97) {
          continue;
        }
        if (bytes[i + 3] !== 114) {
          continue;
        }
        if (bytes[i + 4] !== 115) {
          continue;
        }
        if (bytes[i + 5] !== 101) {
          continue;
        }
        if (bytes[i + 6] !== 116) {
          continue;
        }
        break;
      }
      if (i + 40 >= 65536) {
        return;
      }
      i += 8;
      let j2 = -1;
      while (j2 < 8) {
        j2 += 1;
        const c = bytes[i + j2];
        if (c >= 65 && c <= 90) {
          break;
        }
        if (c >= 97 && c <= 122) {
          break;
        }
      }
      if (j2 === 8) {
        return;
      }
      i += j2;
      const chars = [];
      j2 = 0;
      while (j2 < 24) {
        const c = bytes[i + j2];
        if (c < 45) {
          break;
        }
        if (c > 45 && c < 48) {
          break;
        }
        if (c > 57 && c < 65) {
          break;
        }
        if (c > 90 && c < 97) {
          break;
        }
        if (c > 122) {
          break;
        }
        chars.push(c);
        j2 += 1;
      }
      if (j2 === 20) {
        return;
      }
      return String.fromCharCode(...chars).toLowerCase();
    };
    const streamClose = (session, buffer2) => {
      if (buffer2 !== void 0) {
        session.stream.write(buffer2);
      } else if (session.buffer !== void 0) {
        session.stream.write(session.buffer);
      }
      session.stream.close();
    };
    const onStreamData = function(ev) {
      const session = sessions.get(this);
      if (session === void 0) {
        this.write(ev.data);
        this.disconnect();
        return;
      }
      if (this.status !== "transferringdata") {
        if (this.status !== "finishedtransferringdata") {
          sessions.delete(this);
          this.disconnect();
          return;
        }
      }
      if (session.buffer === null) {
        session.buffer = new Uint8Array(ev.data);
        return;
      }
      const buffer2 = new Uint8Array(
        session.buffer.byteLength + ev.data.byteLength
      );
      buffer2.set(session.buffer);
      buffer2.set(new Uint8Array(ev.data), session.buffer.byteLength);
      session.buffer = buffer2;
      if (session.buffer.length >= MAX_RESPONSE_BUFFER_LENGTH) {
        sessions.delete(this);
        this.write(session.buffer);
        this.disconnect();
      }
    };
    const onStreamStop = function() {
      const session = sessions.get(this);
      sessions.delete(this);
      if (session === void 0 || session.buffer === null) {
        this.close();
        return;
      }
      if (this.status !== "finishedtransferringdata") {
        return;
      }
      if (session.charset === void 0) {
        const charsetFound = charsetFromStream(session.buffer);
        if (charsetFound !== void 0) {
          const charsetUsed = text_encode_default.normalizeCharset(charsetFound);
          if (charsetUsed === void 0) {
            return streamClose(session);
          }
          session.charset = charsetUsed;
        } else {
          session.charset = "utf-8";
        }
      }
      while (session.jobs.length !== 0) {
        const job = session.jobs.shift();
        job.fn(session, ...job.args);
      }
      if (session.modified !== true) {
        return streamClose(session);
      }
      if (textEncoder2 === void 0) {
        textEncoder2 = new TextEncoder();
      }
      let encodedStream = textEncoder2.encode(session.str);
      if (session.charset !== "utf-8") {
        encodedStream = text_encode_default.encode(session.charset, encodedStream);
      }
      streamClose(session, encodedStream);
    };
    const onStreamError = function() {
      sessions.delete(this);
    };
    return class Session extends background_default.FilteringContext {
      constructor(fctxt, mimeVal, charsetVal, jobs) {
        super(fctxt);
        __publicField(this, "stream");
        __publicField(this, "buffer");
        __publicField(this, "mime");
        __publicField(this, "charset");
        __publicField(this, "str");
        __publicField(this, "modified");
        __publicField(this, "jobs");
        this.stream = null;
        this.buffer = null;
        this.mime = mimeVal;
        this.charset = charsetVal;
        this.str = null;
        this.modified = false;
        this.jobs = jobs;
      }
      getString() {
        if (this.str !== null) {
          return this.str;
        }
        if (textDecoder2 !== void 0) {
          if (textDecoder2.encoding !== this.charset) {
            textDecoder2 = void 0;
          }
        }
        if (textDecoder2 === void 0) {
          textDecoder2 = new TextDecoder(this.charset);
        }
        this.str = textDecoder2.decode(this.buffer);
        return this.str;
      }
      setString(s) {
        this.str = s;
        this.modified = true;
      }
      static doFilter(requestId, fctxt, jobs) {
        if (jobs.length === 0) {
          return;
        }
        const session = new Session(fctxt, mime, charset, jobs);
        session.stream = browser.webRequest.filterResponseData(requestId);
        session.stream.ondata = onStreamData;
        session.stream.onstop = onStreamStop;
        session.stream.onerror = onStreamError;
        sessions.set(session.stream, session);
      }
      static canFilter(fctxt, details) {
        if (background_default.canFilterResponseData !== true) {
          return;
        }
        if ((fctxt.itype & BINARY_TYPES) !== 0) {
          return;
        }
        if (fctxt.method !== METHOD_GET) {
          if (fctxt.method !== METHOD_POST) {
            return;
          }
        }
        const statusCode = details.statusCode || 0;
        if (statusCode === 0) {
          return;
        }
        const hostname = fctxt.getHostname();
        if (hostname === "") {
          return;
        }
        const headers = details.responseHeaders;
        const disposition = headerValueFromName("content-disposition", headers);
        if (disposition !== "") {
          if (disposition.startsWith("inline") === false) {
            return;
          }
        }
        mime = "text/plain";
        charset = "utf-8";
        const contentType = headerValueFromName("content-type", headers) || contentTypeFromDetails(details);
        if (contentType !== "") {
          mime = mimeFromContentType(contentType);
          if (mime === void 0) {
            return;
          }
          if (mime.startsWith("text/") === false) {
            if (otherValidMimes.has(mime) === false) {
              return;
            }
          }
          charset = charsetFromContentType(contentType);
          if (charset !== void 0) {
            charset = text_encode_default.normalizeCharset(charset);
            if (charset === void 0) {
              return;
            }
          } else {
            charset = charsetFromMime(mime);
          }
        }
        return true;
      }
    };
  })();
  function injectCSP(fctxt, pageStore, responseHeaders) {
    const cspSubsets = [];
    const requestType = fctxt.type;
    const builtinDirectives = [];
    if (pageStore.filterScripting(fctxt, true) === 1) {
      builtinDirectives.push(background_default.hiddenSettings.noScriptingCSP);
      if (logger_default.enabled) {
        fctxt.setRealm("network").setType("scripting").toLogger();
      }
    } else {
      const fctxt2 = fctxt.duplicate();
      fctxt2.type = "inline-script";
      fctxt2.setDocOriginFromURL(fctxt.url);
      const result = pageStore.filterRequest(fctxt2);
      if (result === 1) {
        builtinDirectives.push(background_default.cspNoInlineScript);
      }
      if (result === 2 && logger_default.enabled) {
        fctxt2.setRealm("network").toLogger();
      }
    }
    fctxt.type = "inline-font";
    if (pageStore.filterRequest(fctxt) === 1) {
      builtinDirectives.push(background_default.cspNoInlineFont);
      if (logger_default.enabled) {
        fctxt.setRealm("network").toLogger();
      }
    }
    if (builtinDirectives.length !== 0) {
      cspSubsets[0] = builtinDirectives.join(", ");
    }
    fctxt.type = requestType;
    const staticDirectives = static_net_filtering_default.matchAndFetchModifiers(fctxt, "csp");
    if (staticDirectives !== void 0) {
      for (const directive of staticDirectives) {
        if (directive.result !== 1) {
          continue;
        }
        cspSubsets.push(directive.value);
      }
    }
    if (cspSubsets.length !== 0 && sessionURLFiltering.evaluateZ(
      fctxt.getTabHostname(),
      fctxt.url,
      "csp"
    ) === 2) {
      if (logger_default.enabled) {
        fctxt.setRealm("network").setType("csp").setFilter(sessionURLFiltering.toLogData()).toLogger();
      }
      return;
    }
    if (cspSubsets.length !== 0 && background_default.userSettings.advancedUserEnabled && sessionFirewall.evaluateCellZY(
      fctxt.getTabHostname(),
      fctxt.getTabHostname(),
      "*"
    ) === 2) {
      if (logger_default.enabled) {
        fctxt.setRealm("network").setType("csp").setFilter(sessionFirewall.toLogData()).toLogger();
      }
      return;
    }
    if (logger_default.enabled && staticDirectives !== void 0) {
      fctxt.setRealm("network").pushFilters(staticDirectives.map((a) => a.logData())).toLogger();
    }
    if (cspSubsets.length === 0) {
      return;
    }
    background_default.updateToolbarIcon(fctxt.tabId, 2);
    responseHeaders.push({
      name: "Content-Security-Policy",
      value: cspSubsets.join(", ")
    });
    return true;
  }
  function injectPP(fctxt, pageStore, responseHeaders) {
    const permissions = [];
    const directives = static_net_filtering_default.matchAndFetchModifiers(fctxt, "permissions");
    if (directives !== void 0) {
      for (const directive of directives) {
        if (directive.result !== 1) {
          continue;
        }
        permissions.push(directive.value.replace("|", ", "));
      }
    }
    if (logger_default.enabled && directives !== void 0) {
      fctxt.setRealm("network").pushFilters(directives.map((a) => a.logData())).toLogger();
    }
    if (permissions.length === 0) {
      return;
    }
    background_default.updateToolbarIcon(fctxt.tabId, 2);
    responseHeaders.push({
      name: "permissions-policy",
      value: permissions.join(", ")
    });
    return true;
  }
  function foilLargeMediaElement(details, fctxt, pageStore) {
    if (details.fromCache === true) {
      return;
    }
    onDemandHeaders.setHeaders(details.responseHeaders || []);
    const result = pageStore.filterLargeMediaElement(fctxt, onDemandHeaders);
    onDemandHeaders.reset();
    if (result === 0) {
      return;
    }
    if (logger_default.enabled) {
      fctxt.setRealm("network").toLogger();
    }
    return { cancel: true };
  }
  foilLargeMediaElement.TYPE_BITS = IMAGE | MEDIA | XMLHTTPREQUEST;
  var headerIndexFromName2 = function(headerName, headers) {
    for (let i = 0, n = headers.length; i < n; i++) {
      if (headers[i].name.toLowerCase() === headerName) {
        return i;
      }
    }
    return -1;
  };
  var headerValueFromName = function(headerName, headers) {
    const i = headerIndexFromName2(headerName, headers);
    return i !== -1 ? headers[i].value : "";
  };
  var onDemandHeaders = {
    headers: [],
    get contentLength() {
      const contentLength = headerValueFromName("content-length", this.headers);
      if (contentLength === "") {
        return Number.NaN;
      }
      return parseInt(contentLength, 10) || 0;
    },
    get contentType() {
      return headerValueFromName("content-type", this.headers);
    },
    setHeaders(headers) {
      this.headers = headers;
    },
    reset() {
      this.headers = [];
    }
  };
  var strictBlockBypasser = {
    hostnameToDeadlineMap: /* @__PURE__ */ new Map(),
    cleanupTimer: vAPI.defer.create(() => {
      strictBlockBypasser.cleanup();
    }),
    cleanup: function() {
      for (const [hostname, deadline] of this.hostnameToDeadlineMap) {
        if (deadline <= Date.now()) {
          this.hostnameToDeadlineMap.delete(hostname);
        }
      }
    },
    revokeTime: function() {
      return Date.now() + background_default.hiddenSettings.strictBlockingBypassDuration * 1e3;
    },
    bypass: function(hostname) {
      if (typeof hostname !== "string" || hostname === "") {
        return;
      }
      this.hostnameToDeadlineMap.set(hostname, this.revokeTime());
    },
    isBypassed: function(hostname) {
      if (this.hostnameToDeadlineMap.size === 0) {
        return false;
      }
      this.cleanupTimer.on({ sec: background_default.hiddenSettings.strictBlockingBypassDuration + 10 });
      for (; ; ) {
        const deadline = this.hostnameToDeadlineMap.get(hostname);
        if (deadline !== void 0) {
          if (deadline > Date.now()) {
            this.hostnameToDeadlineMap.set(hostname, this.revokeTime());
            return true;
          }
          this.hostnameToDeadlineMap.delete(hostname);
        }
        const pos = hostname.indexOf(".");
        if (pos === -1) {
          break;
        }
        hostname = hostname.slice(pos + 1);
      }
      return false;
    }
  };
  function onResponseStarted(details) {
    if (details.tabId === -1) {
      return;
    }
    const pageStore = background_default.pageStoreFromTabId(details.tabId);
    if (pageStore === null) {
      return;
    }
    if (pageStore.getNetFilteringSwitch() === false) {
      return;
    }
    if (isGecko === false && details.type === "main_frame") {
      const fctxt = background_default.filteringContext.fromWebrequestDetails(details);
      const r = onBeforeRootFrameRequest(fctxt);
      if (r?.cancel) {
        return;
      }
    }
    details.ancestors = pageStore.getFrameAncestorDetails(details.frameId);
    scriptlet_filtering_default.injectNow(details);
  }
  onResponseStarted.start = function() {
    browser.webRequest.onResponseStarted.addListener(onResponseStarted, {
      types: ["main_frame", "sub_frame"],
      urls: ["http://*/*", "https://*/*"]
    });
  };
  var requestHeadersManager = {
    requests: /* @__PURE__ */ new Map(),
    start() {
      const extraInfoSpec = ["requestHeaders"];
      if (isGecko !== true) {
        extraInfoSpec.push("extraHeaders");
      }
      browser.webRequest.onSendHeaders.addListener((details) => {
        this.requests.set(details.requestId, details.requestHeaders || []);
      }, {
        urls: ["http://*/*", "https://*/*"]
      }, extraInfoSpec);
      browser.webRequest.onBeforeRedirect.addListener((details) => {
        this.requests.delete(details.requestId);
      }, {
        urls: ["http://*/*", "https://*/*"]
      });
      browser.webRequest.onCompleted.addListener((details) => {
        this.requests.delete(details.requestId);
      }, {
        urls: ["http://*/*", "https://*/*"]
      });
      browser.webRequest.onErrorOccurred.addListener((details) => {
        this.requests.delete(details.requestId);
      }, {
        urls: ["http://*/*", "https://*/*"]
      });
    },
    lookup(details) {
      return this.requests.get(details.requestId) || [];
    }
  };
  var webRequest = {
    onBeforeRequest,
    start: (() => {
      if (typeof vAPI.Net !== "function") {
        return () => {
        };
      }
      vAPI.net = new vAPI.Net();
      if (vAPI.Net?.canSuspend?.()) {
        vAPI.net.suspend();
      }
      return () => {
        vAPI.net.setSuspendableListener(onBeforeRequest);
        vAPI.net.addListener("onHeadersReceived", onHeadersReceived, {
          urls: ["http://*/*", "https://*/*"]
        }, ["blocking", "responseHeaders"]);
        onResponseStarted.start();
        requestHeadersManager.start();
        vAPI.defer.once({ sec: background_default.hiddenSettings.toolbarWarningTimeout }).then(() => {
          if (vAPI.net.hasUnprocessedRequest() === false) {
            return;
          }
          vAPI.net.removeUnprocessedRequest();
          return vAPI.tabs.getCurrent();
        }).then((tab) => {
          if (tab instanceof Object === false) {
            return;
          }
          background_default.updateToolbarIcon(tab.id, 6);
        });
        vAPI.net.unsuspend({ all: true });
      };
    })(),
    strictBlockBypass: (hostname) => {
      strictBlockBypasser.bypass(hostname);
    }
  };
  var traffic_default = webRequest;

  // src/js/mv3/scripting-manager.ts
  init_static_net_filtering();

  // src/js/storage.ts
  init_static_filtering_parser();
  init_static_filtering_io();
  init_text_utils();
  init_publicsuffixlist();
  init_punycode();

  // src/js/static-ext-filtering.ts
  var staticExtFilteringEngine = {
    get acceptedCount() {
      return scriptlet_filtering_default.acceptedCount + httpheader_filtering_default.acceptedCount + html_filtering_default.acceptedCount;
    },
    get discardedCount() {
      return scriptlet_filtering_default.discardedCount + httpheader_filtering_default.discardedCount + html_filtering_default.discardedCount;
    }
  };
  staticExtFilteringEngine.reset = function() {
    scriptlet_filtering_default.reset();
    httpheader_filtering_default.reset();
    html_filtering_default.reset();
  };
  staticExtFilteringEngine.freeze = function() {
    scriptlet_filtering_default.freeze();
    httpheader_filtering_default.freeze();
    html_filtering_default.freeze();
  };
  staticExtFilteringEngine.compile = function(parser, writer) {
    if (parser.isExtendedFilter() === false) {
      return false;
    }
    if (parser.hasError()) {
      logger_default.writeOne({
        realm: "message",
        type: "error",
        text: `Invalid extended filter in ${writer.properties.get("name") || "?"}: ${parser.raw}`
      });
      return true;
    }
    if (parser.isScriptletFilter()) {
      scriptlet_filtering_default.compile(parser, writer);
      return true;
    }
    if (parser.isResponseheaderFilter()) {
      httpheader_filtering_default.compile(parser, writer);
      return true;
    }
    if (parser.isHtmlFilter()) {
      html_filtering_default.compile(parser, writer);
      return true;
    }
    logger_default.writeOne({
      realm: "message",
      type: "error",
      text: `Unknown extended filter in ${writer.properties.get("name") || "?"}: ${parser.raw}`
    });
    return true;
  };
  staticExtFilteringEngine.fromCompiledContent = function(reader, options) {
    scriptlet_filtering_default.fromCompiledContent(reader, options);
    httpheader_filtering_default.fromCompiledContent(reader, options);
    html_filtering_default.fromCompiledContent(reader, options);
  };
  staticExtFilteringEngine.toSelfie = function() {
    return {
      scriptlets: scriptlet_filtering_default.toSelfie(),
      httpHeaders: httpheader_filtering_default.toSelfie(),
      html: html_filtering_default.toSelfie()
    };
  };
  staticExtFilteringEngine.fromSelfie = async function(selfie) {
    if (typeof selfie !== "object" || selfie === null) {
      return false;
    }
    httpheader_filtering_default.fromSelfie(selfie.httpHeaders);
    html_filtering_default.fromSelfie(selfie.html);
    if (scriptlet_filtering_default.fromSelfie(selfie.scriptlets) === false) {
      return false;
    }
    return true;
  };
  var static_ext_filtering_default = staticExtFilteringEngine;

  // src/js/storage.ts
  init_static_net_filtering();
  background_default.getBytesInUse = async function() {
    const promises = [];
    let bytesInUse;
    promises.push(
      vAPI.storage.getBytesInUse instanceof Function ? vAPI.storage.getBytesInUse(null) : void 0
    );
    if (navigator.storage instanceof Object && navigator.storage.estimate instanceof Function) {
      promises.push(navigator.storage.estimate());
    }
    const results = await Promise.all(promises);
    const processCount = (count) => {
      if (typeof count !== "number") {
        return;
      }
      if (bytesInUse === void 0) {
        bytesInUse = 0;
      }
      bytesInUse += count;
      return bytesInUse;
    };
    processCount(results[0]);
    if (results.length > 1 && results[1] instanceof Object) {
      processCount(results[1].usage);
    }
    background_default.storageUsed = bytesInUse;
    return bytesInUse;
  };
  {
    const requestStats = background_default.requestStats;
    let requestStatsDisabled = false;
    background_default.loadLocalSettings = async () => {
      requestStatsDisabled = background_default.hiddenSettings.requestStatsDisabled;
      if (requestStatsDisabled) {
        return;
      }
      return Promise.all([
        vAPI.sessionStorage.get("requestStats"),
        vAPI.storage.get("requestStats"),
        vAPI.storage.get(["blockedRequestCount", "allowedRequestCount"])
      ]).then(([a, b2, c]) => {
        if (a instanceof Object && a.requestStats) {
          return a.requestStats;
        }
        if (b2 instanceof Object && b2.requestStats) {
          return b2.requestStats;
        }
        if (c instanceof Object && Object.keys(c).length === 2) {
          return {
            blockedCount: c.blockedRequestCount,
            allowedCount: c.allowedRequestCount
          };
        }
        return { blockedCount: 0, allowedCount: 0 };
      }).then(({ blockedCount, allowedCount }) => {
        requestStats.blockedCount += blockedCount;
        requestStats.allowedCount += allowedCount;
      });
    };
    const SAVE_DELAY_IN_MINUTES = 3.6;
    const QUICK_SAVE_DELAY_IN_SECONDS = 23;
    const stopTimers = () => {
      vAPI.alarms.clear("saveLocalSettings");
      quickSaveTimer.off();
      saveTimer.off();
    };
    const saveTimer = vAPI.defer.create(() => {
      background_default.saveLocalSettings();
    });
    const quickSaveTimer = vAPI.defer.create(() => {
      if (vAPI.sessionStorage.unavailable !== true) {
        vAPI.sessionStorage.set({ requestStats });
      }
      if (requestStatsDisabled) {
        return;
      }
      saveTimer.on({ min: SAVE_DELAY_IN_MINUTES });
      vAPI.alarms.createIfNotPresent("saveLocalSettings", {
        delayInMinutes: SAVE_DELAY_IN_MINUTES + 0.5
      });
    });
    background_default.incrementRequestStats = (blocked, allowed) => {
      requestStats.blockedCount += blocked;
      requestStats.allowedCount += allowed;
      quickSaveTimer.on({ sec: QUICK_SAVE_DELAY_IN_SECONDS });
    };
    background_default.saveLocalSettings = () => {
      stopTimers();
      if (requestStatsDisabled) {
        return;
      }
      return vAPI.storage.set({ requestStats: background_default.requestStats });
    };
    onBroadcast((msg) => {
      if (msg.what !== "hiddenSettingsChanged") {
        return;
      }
      const newState = background_default.hiddenSettings.requestStatsDisabled;
      if (requestStatsDisabled === newState) {
        return;
      }
      requestStatsDisabled = newState;
      if (newState) {
        stopTimers();
        background_default.requestStats.blockedCount = background_default.requestStats.allowedCount = 0;
      } else {
        background_default.loadLocalSettings();
      }
    });
  }
  background_default.loadUserSettings = async function() {
    const usDefault = this.userSettingsDefault;
    const results = await Promise.all([
      vAPI.storage.get(Object.assign(usDefault)),
      vAPI.adminStorage.get("userSettings")
    ]);
    const usUser = results[0] instanceof Object && results[0] || Object.assign(usDefault);
    if (Array.isArray(results[1])) {
      const adminSettings = results[1];
      for (const entry of adminSettings) {
        if (entry.length < 1) {
          continue;
        }
        const name2 = entry[0];
        if (Object.hasOwn(usDefault, name2) === false) {
          continue;
        }
        const value = entry.length < 2 ? usDefault[name2] : this.settingValueFromString(usDefault, name2, entry[1]);
        if (value === void 0) {
          continue;
        }
        usUser[name2] = usDefault[name2] = value;
      }
    }
    return usUser;
  };
  background_default.saveUserSettings = function() {
    this.userSettings.externalLists = this.userSettings.importedLists.join("\n");
    const toSave = this.getModifiedSettings(
      this.userSettings,
      this.userSettingsDefault
    );
    const toRemove = [];
    for (const key in this.userSettings) {
      if (Object.hasOwn(this.userSettings, key) === false) {
        continue;
      }
      if (Object.hasOwn(toSave, key)) {
        continue;
      }
      toRemove.push(key);
    }
    if (toRemove.length !== 0) {
      vAPI.storage.remove(toRemove);
    }
    vAPI.storage.set(toSave);
  };
  background_default.loadHiddenSettings = async function() {
    const hsDefault = this.hiddenSettingsDefault;
    const hsAdmin = this.hiddenSettingsAdmin;
    const hsUser = this.hiddenSettings;
    const results = await Promise.all([
      vAPI.adminStorage.get([
        "advancedSettings",
        "disableDashboard",
        "disabledPopupPanelParts"
      ]),
      vAPI.storage.get("hiddenSettings")
    ]);
    if (results[0] instanceof Object) {
      const {
        advancedSettings,
        disableDashboard,
        disabledPopupPanelParts
      } = results[0];
      if (Array.isArray(advancedSettings)) {
        for (const entry of advancedSettings) {
          if (entry.length < 1) {
            continue;
          }
          const name2 = entry[0];
          if (Object.hasOwn(hsDefault, name2) === false) {
            continue;
          }
          const value = entry.length < 2 ? hsDefault[name2] : this.hiddenSettingValueFromString(name2, entry[1]);
          if (value === void 0) {
            continue;
          }
          hsDefault[name2] = hsAdmin[name2] = hsUser[name2] = value;
        }
      }
      background_default.noDashboard = disableDashboard === true;
      if (Array.isArray(disabledPopupPanelParts)) {
        const partNameToBit = /* @__PURE__ */ new Map([
          ["globalStats", 2],
          ["basicTools", 4],
          ["extraTools", 8],
          ["overviewPane", 16]
        ]);
        let bits = hsDefault.popupPanelDisabledSections;
        for (const part of disabledPopupPanelParts) {
          const bit = partNameToBit.get(part);
          if (bit === void 0) {
            continue;
          }
          bits |= bit;
        }
        hsDefault.popupPanelDisabledSections = hsAdmin.popupPanelDisabledSections = hsUser.popupPanelDisabledSections = bits;
      }
    }
    const hs2 = results[1] instanceof Object && results[1].hiddenSettings || {};
    if (Object.keys(hsAdmin).length === 0 && Object.keys(hs2).length === 0) {
      return;
    }
    for (const key in hsDefault) {
      if (Object.hasOwn(hsDefault, key) === false) {
        continue;
      }
      if (Object.hasOwn(hsAdmin, name)) {
        continue;
      }
      if (typeof hs2[key] !== typeof hsDefault[key]) {
        continue;
      }
      this.hiddenSettings[key] = hs2[key];
    }
    broadcast({ what: "hiddenSettingsChanged" });
  };
  background_default.saveHiddenSettings = function() {
    vAPI.storage.set({
      hiddenSettings: this.getModifiedSettings(
        this.hiddenSettings,
        this.hiddenSettingsDefault
      )
    });
  };
  onBroadcast((msg) => {
    if (msg.what !== "hiddenSettingsChanged") {
      return;
    }
    const \u00B5bhs = background_default.hiddenSettings;
    ubologSet(\u00B5bhs.consoleLogLevel === "info");
    vAPI.net.setOptions({
      cnameIgnoreList: \u00B5bhs.cnameIgnoreList,
      cnameIgnore1stParty: \u00B5bhs.cnameIgnore1stParty,
      cnameIgnoreExceptions: \u00B5bhs.cnameIgnoreExceptions,
      cnameIgnoreRootDocument: \u00B5bhs.cnameIgnoreRootDocument,
      cnameMaxTTL: \u00B5bhs.cnameMaxTTL,
      cnameReplayFullURL: \u00B5bhs.cnameReplayFullURL,
      dnsCacheTTL: \u00B5bhs.dnsCacheTTL,
      dnsResolveEnabled: \u00B5bhs.dnsResolveEnabled
    });
  });
  background_default.hiddenSettingsFromString = function(raw) {
    const out = Object.assign({}, this.hiddenSettingsDefault);
    const lineIter = new LineIterator(raw);
    while (lineIter.eot() === false) {
      const line = lineIter.next();
      const matches = /^\s*(\S+)\s+(.+)$/.exec(line);
      if (matches === null || matches.length !== 3) {
        continue;
      }
      const name2 = matches[1];
      if (Object.hasOwn(out, name2) === false) {
        continue;
      }
      if (Object.hasOwn(this.hiddenSettingsAdmin, name2)) {
        continue;
      }
      const value = this.hiddenSettingValueFromString(name2, matches[2]);
      if (value !== void 0) {
        out[name2] = value;
      }
    }
    return out;
  };
  background_default.hiddenSettingValueFromString = function(name2, value) {
    if (typeof name2 !== "string" || typeof value !== "string") {
      return;
    }
    const hsDefault = this.hiddenSettingsDefault;
    if (Object.hasOwn(hsDefault, name2) === false) {
      return;
    }
    let r;
    switch (typeof hsDefault[name2]) {
      case "boolean":
        if (value === "true") {
          r = true;
        } else if (value === "false") {
          r = false;
        }
        break;
      case "string":
        r = value.trim();
        break;
      case "number":
        if (value.startsWith("0b")) {
          r = parseInt(value.slice(2), 2);
        } else if (value.startsWith("0x")) {
          r = parseInt(value.slice(2), 16);
        } else {
          r = parseInt(value, 10);
        }
        if (isNaN(r)) {
          r = void 0;
        }
        break;
      default:
        break;
    }
    return r;
  };
  background_default.stringFromHiddenSettings = function() {
    const out = [];
    for (const key of Object.keys(this.hiddenSettings).sort()) {
      out.push(`${key} ${this.hiddenSettings[key]}`);
    }
    return out.join("\n");
  };
  background_default.savePermanentFirewallRules = function() {
    vAPI.storage.set({
      dynamicFilteringString: permanentFirewall.toString()
    });
  };
  background_default.savePermanentURLFilteringRules = function() {
    vAPI.storage.set({
      urlFilteringString: permanentURLFiltering.toString()
    });
  };
  background_default.saveHostnameSwitches = function() {
    vAPI.storage.set({
      hostnameSwitchesString: permanentSwitches.toString()
    });
  };
  background_default.saveWhitelist = function() {
    vAPI.storage.set({
      netWhitelist: this.arrayFromWhitelist(this.netWhitelist)
    });
    this.netWhitelistModifyTime = Date.now();
  };
  background_default.isTrustedList = function(assetKey) {
    if (assetKey === this.userFiltersPath) {
      if (this.userSettings.userFiltersTrusted) {
        return true;
      }
    }
    if (this.parsedTrustedListPrefixes.length === 0) {
      this.parsedTrustedListPrefixes = background_default.hiddenSettings.trustedListPrefixes.split(/ +/).map((prefix) => {
        if (prefix === "") {
          return;
        }
        if (prefix.startsWith("http://")) {
          return;
        }
        if (prefix.startsWith("file:///")) {
          return prefix;
        }
        if (prefix.startsWith("https://") === false) {
          return prefix.includes("://") ? void 0 : prefix;
        }
        try {
          const url = new URL(prefix);
          if (url.hostname.length > 0) {
            return url.href;
          }
        } catch (e) {
          console.warn("[uBR] storage: isTrustedList URL parse failed for prefix", prefix, e);
        }
      }).filter((prefix) => prefix !== void 0);
    }
    const match = /^[a-z-]+:\/\/[^/]+\//.exec(assetKey);
    const assetOrigin = match && match[0];
    for (const prefix of this.parsedTrustedListPrefixes) {
      if (assetOrigin && prefix.length < assetOrigin.length) {
        continue;
      }
      if (assetKey.startsWith(prefix)) {
        return true;
      }
    }
    return false;
  };
  onBroadcast((msg) => {
    if (msg.what !== "hiddenSettingsChanged") {
      return;
    }
    background_default.parsedTrustedListPrefixes = [];
  });
  background_default.loadSelectedFilterLists = async function() {
    const bin = await vAPI.storage.get("selectedFilterLists");
    if (bin instanceof Object && Array.isArray(bin.selectedFilterLists)) {
      this.selectedFilterLists = bin.selectedFilterLists;
      return;
    }
    const lists = await assets_default.metadata();
    this.saveSelectedFilterLists(this.autoSelectRegionalFilterLists(lists));
  };
  background_default.saveSelectedFilterLists = function(newKeys, append = false) {
    const oldKeys = this.selectedFilterLists.slice();
    if (append) {
      newKeys = newKeys.concat(oldKeys);
    }
    const newSet = new Set(newKeys);
    for (const oldKey of oldKeys) {
      if (newSet.has(oldKey) === false) {
        this.removeFilterList(oldKey);
      }
    }
    newKeys = Array.from(newSet);
    this.selectedFilterLists = newKeys;
    return vAPI.storage.set({ selectedFilterLists: newKeys });
  };
  background_default.applyFilterListSelection = function(details) {
    let selectedListKeySet = new Set(this.selectedFilterLists);
    let importedLists = this.userSettings.importedLists.slice();
    if (Array.isArray(details.toSelect)) {
      if (details.merge) {
        for (let i = 0, n = details.toSelect.length; i < n; i++) {
          selectedListKeySet.add(details.toSelect[i]);
        }
      } else {
        selectedListKeySet = new Set(details.toSelect);
      }
    }
    if (Array.isArray(details.toRemove)) {
      for (let i = 0, n = details.toRemove.length; i < n; i++) {
        const assetKey = details.toRemove[i];
        selectedListKeySet.delete(assetKey);
        const pos = importedLists.indexOf(assetKey);
        if (pos !== -1) {
          importedLists.splice(pos, 1);
        }
        this.removeFilterList(assetKey);
      }
    }
    if (typeof details.toImport === "string") {
      const assetKeyFromURL = (url) => {
        const needle = url.replace(/^https?:/, "");
        const assets2 = this.availableFilterLists;
        for (const assetKey in assets2) {
          const asset = assets2[assetKey];
          if (asset.content !== "filters") {
            continue;
          }
          if (typeof asset.contentURL === "string") {
            if (asset.contentURL.endsWith(needle)) {
              return assetKey;
            }
            continue;
          }
          if (Array.isArray(asset.contentURL) === false) {
            continue;
          }
          for (let i = 0, n = asset.contentURL.length; i < n; i++) {
            if (asset.contentURL[i].endsWith(needle)) {
              return assetKey;
            }
          }
        }
        return url;
      };
      const importedSet = new Set(this.listKeysFromCustomFilterLists(importedLists));
      const toImportSet = new Set(this.listKeysFromCustomFilterLists(details.toImport));
      for (const urlKey of toImportSet) {
        if (importedSet.has(urlKey)) {
          selectedListKeySet.add(urlKey);
          continue;
        }
        const assetKey = assetKeyFromURL(urlKey);
        if (assetKey === urlKey) {
          importedSet.add(urlKey);
        }
        selectedListKeySet.add(assetKey);
      }
      importedLists = Array.from(importedSet).sort();
    }
    const result = Array.from(selectedListKeySet);
    if (importedLists.join() !== this.userSettings.importedLists.join()) {
      this.userSettings.importedLists = importedLists;
      this.saveUserSettings();
    }
    this.saveSelectedFilterLists(result);
  };
  background_default.listKeysFromCustomFilterLists = function(raw) {
    const urls = typeof raw === "string" ? raw.trim().split(/[\n\r]+/) : raw;
    const out = /* @__PURE__ */ new Set();
    const reIgnore = /^[!#]/;
    const reValid = /^[a-z-]+:\/\/\S+/;
    for (const url of urls) {
      if (reIgnore.test(url) || !reValid.test(url)) {
        continue;
      }
      if (this.badLists.get(url) === true) {
        continue;
      }
      out.add(url);
    }
    return Array.from(out);
  };
  background_default.saveUserFilters = function(content) {
    content = content.trim();
    this.removeCompiledFilterList(this.userFiltersPath);
    return assets_default.put(this.userFiltersPath, content);
  };
  background_default.loadUserFilters = function() {
    return assets_default.get(this.userFiltersPath);
  };
  background_default.appendUserFilters = async function(filters, options) {
    filters = filters.trim();
    if (filters.length === 0) {
      return;
    }
    let comment = "";
    if (options instanceof Object && options.autoComment === true && this.hiddenSettings.autoCommentFilterTemplate.indexOf("{{") !== -1) {
      const d2 = /* @__PURE__ */ new Date();
      const url = new URL(options.docURL);
      comment = `! ${this.hiddenSettings.autoCommentFilterTemplate.replace("{{isodate}}", d2.toISOString().split("T")[0]).replace("{{date}}", d2.toLocaleDateString(void 0, { dateStyle: "medium" })).replace("{{time}}", d2.toLocaleTimeString()).replace("{{hostname}}", url.hostname).replace("{{origin}}", url.origin).replace("{{url}}", url.href)}`;
    }
    const details = await this.loadUserFilters();
    if (details.error) {
      return;
    }
    if (comment !== "") {
      const beg = details.content.lastIndexOf(comment);
      const end = beg === -1 ? -1 : beg + comment.length;
      if (end === -1 || details.content.startsWith("\n", end) === false || details.content.includes("\n!", end)) {
        filters = `
${comment}
${filters}`;
      }
    }
    await this.saveUserFilters(`${details.content.trim()}
${filters}`);
    const compiledFilters = this.compileFilters(filters, {
      assetKey: this.userFiltersPath,
      trustedSource: true
    });
    const snfe = static_net_filtering_default;
    const acceptedCount3 = snfe.acceptedCount;
    const discardedCount3 = snfe.discardedCount;
    this.applyCompiledFilters(compiledFilters, true);
    const entry = this.availableFilterLists[this.userFiltersPath];
    const deltaEntryCount = snfe.acceptedCount - acceptedCount3;
    const deltaEntryUsedCount = deltaEntryCount - (snfe.discardedCount - discardedCount3);
    entry.entryCount += deltaEntryCount;
    entry.entryUsedCount += deltaEntryUsedCount;
    vAPI.storage.set({ "availableFilterLists": this.availableFilterLists });
    static_net_filtering_default.freeze();
    redirectEngine.freeze();
    static_ext_filtering_default.freeze();
    this.selfieManager.destroy();
    filteringBehaviorChanged();
    broadcast({ what: "userFiltersUpdated" });
  };
  background_default.createUserFilters = async function(details) {
    await this.appendUserFilters(details.filters, details);
    await this.removeCompiledFilterList(this.userFiltersPath);
    background_default.selfieManager.destroy({ janitor: true });
    await this.loadFilterLists();
    background_default.selfieManager.create();
    staticFilteringReverseLookup.resetLists();
  };
  background_default.userFiltersAreEnabled = function() {
    return this.selectedFilterLists.includes(this.userFiltersPath);
  };
  background_default.autoSelectRegionalFilterLists = function(lists) {
    const selectedListKeys = [this.userFiltersPath];
    for (const key in lists) {
      if (Object.hasOwn(lists, key) === false) {
        continue;
      }
      const list = lists[key];
      if (list.content !== "filters") {
        continue;
      }
      if (list.off !== true) {
        selectedListKeys.push(key);
        continue;
      }
      if (this.listMatchesEnvironment(list)) {
        selectedListKeys.push(key);
        list.off = false;
      }
    }
    return selectedListKeys;
  };
  background_default.hasInMemoryFilter = function(raw) {
    return this.inMemoryFilters.includes(raw);
  };
  background_default.addInMemoryFilter = async function(raw) {
    if (this.inMemoryFilters.includes(raw)) {
      return true;
    }
    this.inMemoryFilters.push(raw);
    this.inMemoryFiltersCompiled = "";
    await this.loadFilterLists();
    return true;
  };
  background_default.removeInMemoryFilter = async function(raw) {
    const pos = this.inMemoryFilters.indexOf(raw);
    if (pos === -1) {
      return false;
    }
    this.inMemoryFilters.splice(pos, 1);
    this.inMemoryFiltersCompiled = "";
    await this.loadFilterLists();
    return false;
  };
  background_default.clearInMemoryFilters = async function() {
    if (this.inMemoryFilters.length === 0) {
      return;
    }
    this.inMemoryFilters = [];
    this.inMemoryFiltersCompiled = "";
    await this.loadFilterLists();
  };
  background_default.getAvailableLists = async function() {
    const newAvailableLists = {};
    newAvailableLists[this.userFiltersPath] = {
      content: "filters",
      group: "user",
      title: i18n$("1pPageName")
    };
    const importedListKeys = new Set(
      this.listKeysFromCustomFilterLists(this.userSettings.importedLists)
    );
    for (const listKey of importedListKeys) {
      const asset = {
        content: "filters",
        contentURL: listKey,
        external: true,
        group: "custom",
        submitter: "user",
        title: ""
      };
      newAvailableLists[listKey] = asset;
      assets_default.registerAssetSource(listKey, asset);
    }
    const [bin, registeredAssets, badlists] = await Promise.all([
      Object.keys(this.availableFilterLists).length !== 0 ? { availableFilterLists: this.availableFilterLists } : vAPI.storage.get("availableFilterLists"),
      assets_default.metadata(),
      this.badLists.size === 0 ? assets_default.get("ublock-badlists") : false
    ]);
    if (badlists instanceof Object) {
      for (const line of badlists.content.split(/\s*[\n\r]+\s*/)) {
        if (line === "" || line.startsWith("#")) {
          continue;
        }
        const fields = line.split(/\s+/);
        const remove = fields.length === 2;
        this.badLists.set(fields[0], remove);
      }
    }
    const oldAvailableLists = bin && bin.availableFilterLists || {};
    for (const [assetKey, asset] of Object.entries(registeredAssets)) {
      if (asset.content !== "filters") {
        continue;
      }
      newAvailableLists[assetKey] = Object.assign({}, asset);
    }
    const selectedListset = new Set(this.selectedFilterLists);
    for (const [stockAssetKey, stockEntry] of Object.entries(newAvailableLists)) {
      if (stockEntry.content !== "filters") {
        continue;
      }
      if (stockEntry.group === "user") {
        continue;
      }
      if (stockEntry.submitter === "user") {
        continue;
      }
      if (stockAssetKey.includes("://")) {
        continue;
      }
      const contentURLs = Array.isArray(stockEntry.contentURL) ? stockEntry.contentURL : [stockEntry.contentURL];
      for (const importedAssetKey of contentURLs) {
        const importedEntry = newAvailableLists[importedAssetKey];
        if (importedEntry === void 0) {
          continue;
        }
        delete newAvailableLists[importedAssetKey];
        assets_default.unregisterAssetSource(importedAssetKey);
        this.removeFilterList(importedAssetKey);
        if (selectedListset.has(importedAssetKey)) {
          selectedListset.add(stockAssetKey);
          selectedListset.delete(importedAssetKey);
        }
        importedListKeys.delete(importedAssetKey);
        break;
      }
    }
    for (const [assetKey, oldEntry] of Object.entries(oldAvailableLists)) {
      if (newAvailableLists[assetKey] !== void 0) {
        continue;
      }
      const on2 = selectedListset.delete(assetKey);
      this.removeFilterList(assetKey);
      assets_default.unregisterAssetSource(assetKey);
      if (assetKey.includes("://")) {
        continue;
      }
      if (on2 === false) {
        continue;
      }
      const listURL = Array.isArray(oldEntry.contentURL) ? oldEntry.contentURL[0] : oldEntry.contentURL;
      if (this.badLists.has(listURL)) {
        continue;
      }
      const newEntry = {
        content: "filters",
        contentURL: listURL,
        external: true,
        group: "custom",
        submitter: "user",
        title: oldEntry.title || ""
      };
      newAvailableLists[listURL] = newEntry;
      assets_default.registerAssetSource(listURL, newEntry);
      importedListKeys.add(listURL);
      selectedListset.add(listURL);
    }
    for (const [assetKey, asset] of Object.entries(newAvailableLists)) {
      if (asset.submitter !== "user") {
        continue;
      }
      if (importedListKeys.has(assetKey)) {
        continue;
      }
      selectedListset.delete(assetKey);
      delete newAvailableLists[assetKey];
      this.removeFilterList(assetKey);
      assets_default.unregisterAssetSource(assetKey);
    }
    for (const [assetKey, asset] of Object.entries(newAvailableLists)) {
      asset.off = selectedListset.has(assetKey) === false;
    }
    for (const [assetKey, oldEntry] of Object.entries(oldAvailableLists)) {
      const newEntry = newAvailableLists[assetKey];
      if (newEntry === void 0) {
        continue;
      }
      if (oldEntry.entryCount !== void 0) {
        newEntry.entryCount = oldEntry.entryCount;
      }
      if (oldEntry.entryUsedCount !== void 0) {
        newEntry.entryUsedCount = oldEntry.entryUsedCount;
      }
      if (newEntry.title === "" && typeof oldEntry.title === "string" && oldEntry.title !== "") {
        newEntry.title = oldEntry.title;
      }
    }
    if (Array.from(importedListKeys).join("\n") !== this.userSettings.importedLists.join("\n")) {
      this.userSettings.importedLists = Array.from(importedListKeys);
      this.saveUserSettings();
    }
    if (Array.from(selectedListset).join() !== this.selectedFilterLists.join()) {
      this.saveSelectedFilterLists(Array.from(selectedListset));
    }
    return newAvailableLists;
  };
  {
    const loadedListKeys = [];
    let loadingPromise;
    let t0 = 0;
    const elapsed = () => `${Date.now() - t0} ms`;
    const onDone = () => {
      ubolog(`loadFilterLists() All filters in memory at ${elapsed()}`);
      static_net_filtering_default.freeze();
      static_ext_filtering_default.freeze();
      redirectEngine.freeze();
      vAPI.net.unsuspend();
      filteringBehaviorChanged();
      ubolog(`loadFilterLists() All filters ready at ${elapsed()}`);
      logger_default.writeOne({
        realm: "message",
        type: "info",
        text: `Reloading all filter lists: done, took ${elapsed()}`
      });
      vAPI.storage.set({ "availableFilterLists": background_default.availableFilterLists });
      broadcast({
        what: "staticFilteringDataChanged",
        ignoreGenericCosmeticFilters: background_default.userSettings.ignoreGenericCosmeticFilters,
        listKeys: loadedListKeys
      });
      background_default.selfieManager.destroy();
      background_default.compiledFormatChanged = false;
      loadingPromise = void 0;
    };
    const applyCompiledFilters = (assetKey, compiled) => {
      ubolog(`loadFilterLists() Loading filters from ${assetKey} at ${elapsed()}`);
      const isUserFilters = assetKey === background_default.userFiltersPath;
      console.log("[MV3-STORAGE] applyCompiledFilters called - assetKey:", assetKey, "isUserFilters:", isUserFilters);
      if (isUserFilters && compiled) {
        console.log("[MV3-STORAGE] User filters compiled content length:", compiled.length);
        console.log("[MV3-STORAGE] Compiled content preview:", compiled.substring(0, 500));
      }
      const snfe = static_net_filtering_default;
      const sxfe = static_ext_filtering_default;
      const acceptedCount3 = snfe.acceptedCount + sxfe.acceptedCount;
      const discardedCount3 = snfe.discardedCount + sxfe.discardedCount;
      background_default.applyCompiledFilters(compiled, isUserFilters);
      if (Object.hasOwn(background_default.availableFilterLists, assetKey)) {
        const entry = background_default.availableFilterLists[assetKey];
        entry.entryCount = snfe.acceptedCount + sxfe.acceptedCount - acceptedCount3;
        entry.entryUsedCount = entry.entryCount - (snfe.discardedCount + sxfe.discardedCount - discardedCount3);
      }
      loadedListKeys.push(assetKey);
    };
    const onFilterListsReady = (lists) => {
      logger_default.writeOne({
        realm: "message",
        type: "info",
        text: "Reloading all filter lists: start"
      });
      background_default.availableFilterLists = lists;
      if (vAPI.Net?.canSuspend?.()) {
        vAPI.net.suspend();
      }
      redirectEngine.reset();
      static_ext_filtering_default.reset();
      static_net_filtering_default.reset();
      background_default.selfieManager.destroy();
      staticFilteringReverseLookup.resetLists();
      ubolog(`loadFilterLists() All filters removed at ${elapsed()}`);
      const toLoad = [];
      for (const assetKey in lists) {
        if (Object.hasOwn(lists, assetKey) === false) {
          continue;
        }
        if (lists[assetKey].off) {
          continue;
        }
        toLoad.push(
          background_default.getCompiledFilterList(assetKey).then((details) => {
            applyCompiledFilters(details.assetKey, details.content);
          })
        );
      }
      if (background_default.inMemoryFilters.length !== 0) {
        if (background_default.inMemoryFiltersCompiled === "") {
          background_default.inMemoryFiltersCompiled = background_default.compileFilters(background_default.inMemoryFilters.join("\n"), {
            assetKey: "in-memory",
            trustedSource: true
          });
        }
        if (background_default.inMemoryFiltersCompiled !== "") {
          toLoad.push(
            background_default.applyCompiledFilters(background_default.inMemoryFiltersCompiled, true)
          );
        }
      }
      return Promise.all(toLoad);
    };
    background_default.loadFilterLists = function() {
      if (loadingPromise instanceof Promise) {
        return loadingPromise;
      }
      ubolog("loadFilterLists() Start");
      t0 = Date.now();
      loadedListKeys.length = 0;
      loadingPromise = this.loadRedirectResources().then(() => {
        ubolog(`loadFilterLists() Redirects/scriptlets ready at ${elapsed()}`);
        return this.getAvailableLists();
      }).then((lists) => {
        return onFilterListsReady(lists);
      }).then(() => {
        onDone();
      });
      return loadingPromise;
    };
  }
  background_default.getCompiledFilterList = async function(assetKey) {
    const compiledPath = `compiled/${assetKey}`;
    console.log("[MV3-STORAGE] getCompiledFilterList called for:", assetKey);
    if (this.compiledFormatChanged === false && this.badLists.has(assetKey) === false) {
      const content = await assets_default.fromCache(compiledPath);
      const compilerVersion = `${this.systemSettings.compiledMagic}
`;
      if (content && content.startsWith(compilerVersion)) {
        console.log("[MV3-STORAGE] Using cached compiled version for:", assetKey, "length:", content.length);
        return { assetKey, content };
      }
    }
    if (this.badLists.get(assetKey)) {
      console.log("[MV3-STORAGE] Skipping bad list:", assetKey);
      return { assetKey, content: "" };
    }
    const rawDetails = await assets_default.get(assetKey, {
      favorLocal: this.readyToFilter !== true,
      silent: true
    });
    console.log("[MV3-STORAGE] Raw content loaded for:", assetKey, "length:", rawDetails.content ? rawDetails.content.length : 0);
    if (rawDetails.content === "") {
      rawDetails.assetKey = assetKey;
      return rawDetails;
    }
    this.extractFilterListMetadata(assetKey, rawDetails.content);
    if (this.badLists.has(assetKey)) {
      return { assetKey, content: "" };
    }
    const compiledContent = this.compileFilters(rawDetails.content, {
      assetKey,
      trustedSource: this.isTrustedList(assetKey)
    });
    console.log("[MV3-STORAGE] Compiled content for:", assetKey, "length:", compiledContent ? compiledContent.length : 0);
    if (compiledContent && compiledContent.includes("COSMETIC_FILTERS:SPECIFIC")) {
      console.log("[MV3-STORAGE] Compiled content contains COSMETIC_FILTERS:SPECIFIC section");
    }
    assets_default.toCache(compiledPath, compiledContent);
    return { assetKey, content: compiledContent };
  };
  background_default.extractFilterListMetadata = function(assetKey, raw) {
    const listEntry = this.availableFilterLists[assetKey];
    if (listEntry === void 0) {
      return;
    }
    if (listEntry.group !== "custom") {
      return;
    }
    const data = assets_default.extractMetadataFromList(raw, ["Title", "Homepage"]);
    const props = {};
    if (data.title && data.title !== listEntry.title) {
      props.title = listEntry.title = orphanizeString(data.title);
    }
    if (data.homepage && /^https?:\/\/\S+/.test(data.homepage)) {
      if (data.homepage !== listEntry.supportURL) {
        props.supportURL = listEntry.supportURL = orphanizeString(data.homepage);
      }
    }
    assets_default.registerAssetSource(assetKey, props);
  };
  background_default.removeCompiledFilterList = function(assetKey) {
    assets_default.remove(`compiled/${assetKey}`);
  };
  background_default.removeFilterList = function(assetKey) {
    this.removeCompiledFilterList(assetKey);
    assets_default.remove(assetKey);
  };
  background_default.compileFilters = function(rawText, details = {}) {
    const writer = new CompiledListWriter();
    const trustedSource = details.trustedSource === true;
    if (details.assetKey) {
      writer.properties.set("name", details.assetKey);
      writer.properties.set("trustedSource", trustedSource);
    }
    const assetName = details.assetKey ? details.assetKey : "?";
    console.log("[MV3-STORAGE] compileFilters called for:", assetName, "trustedSource:", trustedSource);
    const parser = new AstFilterParser({
      trustedSource,
      maxTokenLength: static_net_filtering_default.MAX_TOKEN_LENGTH,
      nativeCssHas: vAPI.webextFlavor.env.includes("native_css_has")
    });
    const compiler = static_net_filtering_default.createCompiler(parser);
    const lineIter = new LineIterator(
      utils.preparser.prune(rawText, vAPI.webextFlavor.env)
    );
    compiler.start(writer);
    let extendedFilterCount = 0;
    while (lineIter.eot() === false) {
      let line = lineIter.next();
      while (line.endsWith(" \\")) {
        if (lineIter.peek(4) !== "    ") {
          break;
        }
        line = line.slice(0, -2).trim() + lineIter.next().trim();
      }
      parser.parse(line);
      if (parser.isFilter() === false) {
        continue;
      }
      if (parser.hasError()) {
        logger_default.writeOne({
          realm: "message",
          type: "error",
          text: `Invalid filter (${assetName}): ${parser.raw}`
        });
        continue;
      }
      if (parser.isExtendedFilter()) {
        extendedFilterCount++;
        static_ext_filtering_default.compile(parser, writer);
        continue;
      }
      if (parser.isNetworkFilter() === false) {
        continue;
      }
      if (compiler.compile(parser, writer)) {
        continue;
      }
      if (compiler.error !== void 0) {
        logger_default.writeOne({
          realm: "message",
          type: "error",
          text: compiler.error
        });
      }
    }
    compiler.finish(writer);
    parser.finish();
    const compiledContent = `${this.systemSettings.compiledMagic}
${writer.toString()}`;
    return compiledContent;
  };
  background_default.applyCompiledFilters = function(rawText, firstparty) {
    if (rawText === "") {
      return;
    }
    console.log("[MV3-STORAGE] applyCompiledFilters called, rawText length:", rawText.length, "firstparty:", firstparty);
    const reader = new CompiledListReader(rawText);
    static_net_filtering_default.fromCompiled(reader);
    static_ext_filtering_default.fromCompiledContent(reader, {
      skipGenericCosmetic: this.userSettings.ignoreGenericCosmeticFilters
    });
  };
  background_default.loadRedirectResources = async function() {
    try {
      const success = await redirectEngine.resourcesFromSelfie(assets_default);
      if (success === true) {
        ubolog("Loaded redirect/scriptlets resources from selfie");
        return true;
      }
      const fetcher = (path, options = void 0) => {
        if (path.startsWith("/web_accessible_resources/")) {
          const secret = typeof vAPI.warSecret?.short === "function" ? vAPI.warSecret.short() : "";
          path += `?secret=${secret}`;
          return assets_default.fetch(path, options);
        }
        return assets_default.fetchText(path);
      };
      const fetchPromises = [
        redirectEngine.loadBuiltinResources(fetcher)
      ];
      const userResourcesLocation = this.hiddenSettings.userResourcesLocation;
      if (userResourcesLocation !== "unset") {
        for (const url of userResourcesLocation.split(/\s+/)) {
          fetchPromises.push(assets_default.fetchText(url));
        }
      }
      const results = await Promise.all(fetchPromises);
      if (Array.isArray(results) === false) {
        return results;
      }
      const content = [];
      for (let i = 1; i < results.length; i++) {
        const result = results[i];
        if (result instanceof Object === false) {
          continue;
        }
        if (typeof result.content !== "string") {
          continue;
        }
        if (result.content === "") {
          continue;
        }
        content.push(result.content);
      }
      if (content.length !== 0) {
        redirectEngine.resourcesFromString(content.join("\n\n"));
      }
      redirectEngine.selfieFromResources(assets_default);
    } catch (ex) {
      console.warn("[uBR] storage: loadRedirectResources failed", ex);
      return false;
    }
    return true;
  };
  background_default.loadPublicSuffixList = async function() {
    const psl = publicsuffixlist_default;
    if (vAPI.canWASM && this.hiddenSettings.disableWebAssembly !== true) {
      const wasmModuleFetcher = function(path) {
        return fetch(`${path}.wasm`, {
          mode: "same-origin"
        }).then(
          WebAssembly.compileStreaming
        ).catch((reason) => {
          console.warn("[uBR] storage: WASM module fetch failed", reason);
        });
      };
      let result2 = false;
      try {
        result2 = await psl.enableWASM(
          wasmModuleFetcher,
          "./lib/publicsuffixlist/wasm/"
        );
      } catch (reason) {
        console.warn("[uBR] storage: PSL WASM enable failed", reason);
      }
      if (result2) {
        ubolog(`WASM PSL ready ${Date.now() - vAPI.T0} ms after launch`);
      }
    }
    try {
      const selfie = await assets_default.fromCache(`selfie/${this.pslAssetKey}`);
      if (psl.fromSelfie(selfie)) {
        ubolog("Loaded PSL from selfie");
        return;
      }
    } catch (reason) {
      console.warn("[uBR] storage: PSL selfie load failed", reason);
    }
    const result = await assets_default.get(this.pslAssetKey);
    if (result.content !== "") {
      this.compilePublicSuffixList(result.content);
    }
  };
  background_default.compilePublicSuffixList = function(content) {
    const psl = publicsuffixlist_default;
    psl.parse(content, punycode_default.toASCII);
    ubolog(`Loaded PSL from ${this.pslAssetKey}`);
    const selfie = psl.toSelfie();
    chrome.storage.local.set({ pslSelfie: { magic: selfie.magic, buf32: Array.from(selfie.buf32) } }).catch((e) => {
      console.warn("[uBR] storage: compilePublicSuffixList chrome.storage.local.set failed", e);
    });
    return assets_default.toCache(`selfie/${this.pslAssetKey}`, selfie);
  };
  {
    const create = async function() {
      vAPI.alarms.clear("createSelfie");
      createTimer.off();
      if (background_default.inMemoryFilters.length !== 0) {
        return;
      }
      if (Object.keys(background_default.availableFilterLists).length === 0) {
        return;
      }
      await Promise.all([
        assets_default.toCache("selfie/staticMain", {
          magic: background_default.systemSettings.selfieMagic,
          availableFilterLists: background_default.availableFilterLists
        }),
        assets_default.toCache(
          "selfie/staticExtFilteringEngine",
          static_ext_filtering_default.toSelfie()
        ),
        assets_default.toCache(
          "selfie/staticNetFilteringEngine",
          static_net_filtering_default.toSelfie()
        )
      ]);
      background_default.selfieIsInvalid = false;
      ubolog("Filtering engine selfie created");
    };
    const loadMain = async function() {
      const selfie = await assets_default.fromCache("selfie/staticMain");
      if (selfie instanceof Object === false) {
        return false;
      }
      if (selfie.magic !== background_default.systemSettings.selfieMagic) {
        return false;
      }
      if (selfie.availableFilterLists instanceof Object === false) {
        return false;
      }
      if (Object.keys(selfie.availableFilterLists).length === 0) {
        return false;
      }
      background_default.availableFilterLists = selfie.availableFilterLists;
      return true;
    };
    const load = async function() {
      if (background_default.selfieIsInvalid) {
        return false;
      }
      try {
        const results = await Promise.all([
          loadMain(),
          assets_default.fromCache("selfie/staticExtFilteringEngine").then((selfie) => {
            console.log("[MV3-STORAGE] Loading staticExtFilteringEngine from selfie, selfie exists:", !!selfie);
            const result = static_ext_filtering_default.fromSelfie(selfie);
            return result;
          }),
          assets_default.fromCache("selfie/staticNetFilteringEngine").then(
            (selfie) => static_net_filtering_default.fromSelfie(selfie)
          )
        ]);
        if (results.every((v2) => v2)) {
          return background_default.loadRedirectResources();
        }
      } catch (reason) {
        console.warn("[uBR] storage: filtering engine selfie load failed", reason);
      }
      ubolog("Filtering engine selfie not available");
      destroy2();
      return false;
    };
    const destroy2 = function(options = {}) {
      if (background_default.selfieIsInvalid === false) {
        assets_default.remove(/^selfie\/static/, options);
        background_default.selfieIsInvalid = true;
        ubolog("Filtering engine selfie marked for invalidation");
      }
      vAPI.alarms.create("createSelfie", {
        delayInMinutes: (background_default.hiddenSettings.selfieDelayInSeconds + 17) / 60
      });
      createTimer.offon({ sec: background_default.hiddenSettings.selfieDelayInSeconds });
    };
    const createTimer = vAPI.defer.create(create);
    background_default.selfieManager = { load, create, destroy: destroy2 };
  }
  background_default.restoreAdminSettings = async function() {
    let toOverwrite = {};
    let data;
    try {
      const store = await vAPI.adminStorage.get([
        "adminSettings",
        "toOverwrite"
      ]) || {};
      if (store.toOverwrite instanceof Object) {
        toOverwrite = store.toOverwrite;
      }
      const json = store.adminSettings;
      if (typeof json === "string" && json !== "") {
        data = JSON.parse(json);
      } else if (json instanceof Object) {
        data = json;
      }
    } catch (ex) {
      console.error(ex);
    }
    if (data instanceof Object === false) {
      data = {};
    }
    const bin = {};
    let binNotEmpty = false;
    if (typeof data.assetsBootstrapLocation === "string" && data.assetsBootstrapLocation !== "") {
      background_default.assetsBootstrapLocation = data.assetsBootstrapLocation;
    }
    if (typeof data.userSettings === "object") {
      const \u00B5bus = this.userSettings;
      const adminus = data.userSettings;
      for (const name2 in \u00B5bus) {
        if (Object.hasOwn(\u00B5bus, name2) === false) {
          continue;
        }
        if (Object.hasOwn(adminus, name2) === false) {
          continue;
        }
        bin[name2] = adminus[name2];
        binNotEmpty = true;
      }
    }
    if (Array.isArray(toOverwrite.filterLists) && toOverwrite.filterLists.length !== 0) {
      const importedLists = [];
      for (const list of toOverwrite.filterLists) {
        if (/^[a-z-]+:\/\//.test(list) === false) {
          continue;
        }
        importedLists.push(list);
      }
      if (importedLists.length !== 0) {
        bin.importedLists = importedLists;
        bin.externalLists = importedLists.join("\n");
      }
      bin.selectedFilterLists = toOverwrite.filterLists;
      binNotEmpty = true;
    } else if (Array.isArray(data.selectedFilterLists)) {
      bin.selectedFilterLists = data.selectedFilterLists;
      binNotEmpty = true;
    }
    if (Array.isArray(toOverwrite.trustedSiteDirectives) && toOverwrite.trustedSiteDirectives.length !== 0) {
      background_default.netWhitelistDefault = toOverwrite.trustedSiteDirectives.slice();
      bin.netWhitelist = toOverwrite.trustedSiteDirectives.slice();
      binNotEmpty = true;
    } else if (Array.isArray(data.whitelist)) {
      bin.netWhitelist = data.whitelist;
      binNotEmpty = true;
    } else if (typeof data.netWhitelist === "string") {
      bin.netWhitelist = data.netWhitelist.split("\n");
      binNotEmpty = true;
    }
    if (typeof data.dynamicFilteringString === "string") {
      bin.dynamicFilteringString = data.dynamicFilteringString;
      binNotEmpty = true;
    }
    if (typeof data.urlFilteringString === "string") {
      bin.urlFilteringString = data.urlFilteringString;
      binNotEmpty = true;
    }
    if (typeof data.hostnameSwitchesString === "string") {
      bin.hostnameSwitchesString = data.hostnameSwitchesString;
      binNotEmpty = true;
    }
    if (binNotEmpty) {
      vAPI.storage.set(bin);
    }
    let userFiltersAfter;
    if (Array.isArray(toOverwrite.filters)) {
      userFiltersAfter = toOverwrite.filters.join("\n").trim();
    } else if (typeof data.userFilters === "string") {
      userFiltersAfter = data.userFilters.trim();
    }
    if (typeof userFiltersAfter === "string") {
      const bin2 = await vAPI.storage.get(this.userFiltersPath);
      const userFiltersBefore = bin2 && bin2[this.userFiltersPath] || "";
      if (userFiltersAfter !== userFiltersBefore) {
        await Promise.all([
          this.saveUserFilters(userFiltersAfter),
          this.selfieManager.destroy()
        ]);
      }
    }
  };
  background_default.listMatchesEnvironment = function(details) {
    if (typeof details.lang === "string") {
      let re2 = this.listMatchesEnvironment.reLang;
      if (re2 === void 0) {
        const match = /^[a-z]+/.exec(i18n.getUILanguage());
        if (match !== null) {
          re2 = new RegExp(`\\b${match[0]}\\b`);
          this.listMatchesEnvironment.reLang = re2;
        }
      }
      if (re2 !== void 0 && re2.test(details.lang)) {
        return true;
      }
    }
    if (typeof details.ua === "string") {
      const re2 = new RegExp(`\\b${this.escapeRegex(details.ua)}\\b`, "i");
      if (re2.test(self.navigator.userAgent)) {
        return true;
      }
    }
    return false;
  };
  {
    background_default.scheduleAssetUpdater = async function(_details = {}) {
      assets_default.updateStop();
    };
  }
  background_default.assetObserver = function(topic, details) {
    if (topic === "before-asset-updated") {
      if (details.type === "filters") {
        if (Object.hasOwn(this.availableFilterLists, details.assetKey) === false || this.selectedFilterLists.indexOf(details.assetKey) === -1 || this.badLists.get(details.assetKey)) {
          return;
        }
      }
      return true;
    }
    if (topic === "after-asset-updated") {
      if (details.assetKey.startsWith("selfie/")) {
        return;
      }
      const cached = typeof details.content === "string" && details.content !== "";
      if (Object.hasOwn(this.availableFilterLists, details.assetKey)) {
        if (cached) {
          if (this.selectedFilterLists.indexOf(details.assetKey) !== -1) {
            this.extractFilterListMetadata(
              details.assetKey,
              details.content
            );
            if (this.badLists.has(details.assetKey) === false) {
              assets_default.toCache(
                `compiled/${details.assetKey}`,
                this.compileFilters(details.content, {
                  assetKey: details.assetKey,
                  trustedSource: this.isTrustedList(details.assetKey)
                })
              );
            }
          }
        } else {
          this.removeCompiledFilterList(details.assetKey);
        }
      } else if (details.assetKey === this.pslAssetKey) {
        if (cached) {
          this.compilePublicSuffixList(details.content);
        }
      } else if (details.assetKey === "ublock-badlists") {
        this.badLists = /* @__PURE__ */ new Map();
      }
      broadcast({
        what: "assetUpdated",
        key: details.assetKey,
        cached
      });
      this.selfieManager.destroy();
      return;
    }
    if (topic === "asset-update-failed") {
      broadcast({
        what: "assetUpdated",
        key: details.assetKey,
        failed: true
      });
      return;
    }
    if (topic === "after-assets-updated") {
      if (details.assetKeys.length !== 0) {
        if (this.hiddenSettings.userResourcesLocation !== "unset" || vAPI.webextFlavor.soup.has("devbuild")) {
          redirectEngine.invalidateResourcesSelfie(assets_default);
        }
        this.loadFilterLists();
      }
      this.scheduleAssetUpdater();
      broadcast({
        what: "assetsUpdated",
        assetKeys: details.assetKeys
      });
      return;
    }
    if (topic === "builtin-asset-source-added") {
      if (details.entry.content === "filters") {
        if (details.entry.off === true && this.listMatchesEnvironment(details.entry)) {
          this.saveSelectedFilterLists([details.assetKey], true);
        }
      }
      return;
    }
    if (topic === "assets.json-updated") {
      const { newDict, oldDict } = details;
      if (newDict["assets.json"] === void 0) {
        return;
      }
      if (oldDict["assets.json"] === void 0) {
        return;
      }
      const newDefaultListset = new Set(newDict["assets.json"].defaultListset || []);
      const oldDefaultListset = new Set(oldDict["assets.json"].defaultListset || []);
      if (newDefaultListset.size === 0) {
        return;
      }
      if (oldDefaultListset.size === 0) {
        Array.from(Object.entries(oldDict)).filter(
          (a) => a[1].content === "filters" && a[1].off === void 0 && /^https?:\/\//.test(a[0]) === false
        ).map((a) => a[0]).forEach((a) => oldDefaultListset.add(a));
        if (oldDefaultListset.size === 0) {
          return;
        }
      }
      const selectedListset = new Set(this.selectedFilterLists);
      let selectedListModified = false;
      for (const assetKey of oldDefaultListset) {
        if (newDefaultListset.has(assetKey)) {
          continue;
        }
        selectedListset.delete(assetKey);
        selectedListModified = true;
      }
      for (const assetKey of newDefaultListset) {
        if (oldDefaultListset.has(assetKey)) {
          continue;
        }
        selectedListset.add(assetKey);
        selectedListModified = true;
      }
      if (selectedListModified) {
        this.saveSelectedFilterLists(Array.from(selectedListset));
      }
      return;
    }
  };

  // src/js/mv3/scripting-manager.ts
  var webextAPI = chrome;
  var SCRIPT_ID_PREFIX = "ubo-";
  var SCRIPTLET_URLS = [];
  if (typeof redirectEngine !== "undefined" && redirectEngine.getResourceDetails) {
    const details = redirectEngine.getResourceDetails();
    for (const [token] of details) {
      SCRIPTLET_URLS.push(`/js/scriptlets/${token}.js`);
    }
  }
  var ScriptingManager = class {
    constructor() {
      __publicField(this, "registeredScripts", /* @__PURE__ */ new Map());
      __publicField(this, "filteringModeCache", /* @__PURE__ */ new Map());
    }
    async initialize() {
      console.log("[ScriptingManager] Initializing...");
      await this.registerInjectables();
    }
    async registerInjectables() {
      if (typeof webextAPI.scripting === "undefined") {
        console.log("[ScriptingManager] scripting API not available");
        return;
      }
      try {
        const directives = await this.buildContentScriptDirectives();
        if (directives.length === 0) {
          console.log("[ScriptingManager] No directives to register");
          return;
        }
        await webextAPI.scripting.registerContentScripts(directives);
        console.log("[ScriptingManager] Registered", directives.length, "content scripts");
      } catch (e) {
        console.error("[ScriptingManager] Failed to register content scripts:", e);
      }
    }
    async buildContentScriptDirectives() {
      const directives = [];
      const scriptletsDirective = this.buildScriptletsDirective();
      if (scriptletsDirective) {
        directives.push(scriptletsDirective);
      }
      const cosmeticDirectives = await this.buildCosmeticDirectives();
      directives.push(...cosmeticDirectives);
      return directives;
    }
    buildScriptletsDirective() {
      if (SCRIPTLET_URLS.length === 0) {
        return null;
      }
      return {
        id: `${SCRIPT_ID_PREFIX}scriptlets`,
        matches: ["<all_urls>"],
        js: SCRIPTLET_URLS,
        runAt: "document_start"
      };
    }
    async buildCosmeticDirectives() {
      const directives = [];
      directives.push({
        id: `${SCRIPT_ID_PREFIX}cosmetic-specific`,
        matches: ["<all_urls>"],
        js: ["/js/contentscript/04-dom-filterer.js"],
        runAt: "document_start"
      });
      directives.push({
        id: `${SCRIPT_ID_PREFIX}cosmetic-generic`,
        matches: ["<all_urls>"],
        js: ["/js/contentscript/05-dom-collapser.js"],
        runAt: "document_idle"
      });
      return directives;
    }
    async getRegisteredContentScripts() {
      if (typeof webextAPI.scripting?.getRegisteredContentScripts !== "function") {
        return [];
      }
      try {
        const scripts = await webextAPI.scripting.getRegisteredContentScripts();
        return scripts.map((s) => s.id);
      } catch (e) {
        console.error("[ScriptingManager] Failed to get registered scripts:", e);
        return [];
      }
    }
    async unregisterAllContentScripts() {
      if (typeof webextAPI.scripting?.unregisterContentScripts !== "function") {
        return;
      }
      try {
        await webextAPI.scripting.unregisterContentScripts({ ids: [] });
        console.log("[ScriptingManager] Unregistered all content scripts");
      } catch (e) {
        console.error("[ScriptingManager] Failed to unregister scripts:", e);
      }
    }
    async reRegisterContentScripts() {
      await this.unregisterAllContentScripts();
      await this.registerInjectables();
    }
  };
  var scriptingManager = new ScriptingManager();

  // src/js/mv3/storage.ts
  var webextAPI2 = chrome;
  var storage = {
    async readUserFilters() {
      const data = await webextAPI2.storage.local.get("user-filters");
      const value = data["user-filters"];
      if (typeof value === "string") {
        return { content: value };
      }
      return { content: "" };
    },
    async readFilteringMode() {
      const data = await webextAPI2.storage.local.get("filtering-modes");
      return data["filtering-modes"] || {};
    },
    async writeFilteringMode(hostname, mode) {
      const data = await webextAPI2.storage.local.get("filtering-modes");
      const modes = data["filtering-modes"] || {};
      modes[hostname] = mode;
      await webextAPI2.storage.local.set({ "filtering-modes": modes });
    },
    async deleteFilteringMode(hostname) {
      const data = await webextAPI2.storage.local.get("filtering-modes");
      const modes = data["filtering-modes"] || {};
      delete modes[hostname];
      await webextAPI2.storage.local.set({ "filtering-modes": modes });
    }
  };

  // src/js/dnr-integration.ts
  init_static_dnr_filtering();
  init_static_net_filtering();
  var isMV3 = () => {
    return typeof browser !== "undefined" && browser.runtime?.getManifest?.()?.manifest_version === 3;
  };
  var vAPI2 = globalThis.vAPI || {};
  var isGecko2 = vAPI2.webextFlavor?.isGecko === true;
  var DNRIntegration = class {
    constructor() {
      this.enabled = false;
      this.dnrApi = null;
      this.ruleIdCounter = 1;
    }
    async initialize() {
      if (isGecko2 && !isMV3()) {
        console.log("[DNR] Running in Firefox MV2 mode - using webRequest");
        return;
      }
      this.dnrApi = browser?.declarativeNetRequest;
      if (!this.dnrApi) {
        console.log("[DNR] DNR API not available");
        return;
      }
      console.log("[DNR] Initializing MV3 mode with DNR");
      this.enabled = true;
      onBroadcast((msg) => {
        if (msg.what === "filteringBehaviorChanged") {
          this.updateRules();
        }
        if (msg.what === "userFiltersUpdated") {
          this.updateRules();
        }
      });
      try {
        await this.compileAndInstallRules();
      } catch (e) {
        console.error("[DNR] Failed to initialize:", e);
      }
    }
    async compileAndInstallRules() {
      if (!this.enabled) return;
      console.log("[DNR] Compiling filter rules...");
      try {
        this.ruleIdCounter = 1;
        const firewallRules = this.compileUserRules();
        const whitelistRules = this.compileWhitelist();
        const userFilterRules = await this.compileUserFiltersFromStorage();
        const staticFilterRules = await this.compileStaticFiltersFromLists();
        const allRules = [
          ...staticFilterRules,
          ...firewallRules,
          ...whitelistRules,
          ...userFilterRules
        ];
        const existingRules = await this.dnrApi.getDynamicRules();
        const removeRuleIds = existingRules.map((r) => r.id);
        if (allRules.length === 0 && removeRuleIds.length === 0) {
          console.log("[DNR] No rules to update");
          return;
        }
        if (removeRuleIds.length > 0) {
          await this.dnrApi.updateDynamicRules({
            removeRuleIds
          });
        }
        const chunkSize = 100;
        for (let i = 0; i < allRules.length; i += chunkSize) {
          const chunk = allRules.slice(i, i + chunkSize);
          await this.dnrApi.updateDynamicRules({
            addRules: chunk
          });
        }
        console.log(`[DNR] Installed ${allRules.length} rules (static: ${staticFilterRules.length}, firewall: ${firewallRules.length}, whitelist: ${whitelistRules.length}, userFilters: ${userFilterRules.length})`);
      } catch (e) {
        console.error("[DNR] Failed to compile/install rules:", e);
      }
    }
    /**
     * Compile static filter lists to DNR rules
     * First tries to use compiled filter data from the engine (from selfie),
     * falls back to reading raw list content if needed
     */
    async compileStaticFiltersFromLists() {
      const rules = [];
      try {
        console.log("[DNR] Starting static filter compilation...");
        const selectedLists = background_default.selectedFilterLists;
        if (!selectedLists || selectedLists.length === 0) {
          console.log("[DNR] No filter lists selected");
          return rules;
        }
        console.log("[DNR] Selected filter lists:", selectedLists);
        const engineRules = this.compileFromEngine();
        if (engineRules.length > 0) {
          console.log("[DNR] Compiled", engineRules.length, "rules from engine (selfie mode)");
          return engineRules;
        }
        console.log("[DNR] Engine empty, falling back to reading raw list content...");
        const listPromises = [];
        const listNames = [];
        for (const assetKey of selectedLists) {
          if (assetKey === background_default.userFiltersPath) {
            continue;
          }
          const promise = assets_default.get(assetKey, { dontCache: true }).then((details) => {
            listNames.push(assetKey);
            return {
              name: assetKey,
              text: details.content || "",
              trustedSource: assetKey.startsWith("ublock-")
            };
          }).catch((err) => {
            console.warn("[uBR] dnr-integration: Failed to load list:", assetKey, err);
            return null;
          });
          listPromises.push(promise);
        }
        const lists = await Promise.all(listPromises);
        const validLists = lists.filter((l) => l && l.text);
        console.log("[DNR] Loaded", validLists.length, "filter lists");
        if (validLists.length === 0) {
          return rules;
        }
        const extensionPaths = [];
        if (typeof background_default.redirectEngine !== "undefined" && background_default.redirectEngine.getResourceDetails) {
          const details = background_default.redirectEngine.getResourceDetails();
          for (const [token, detail] of details) {
            if (typeof detail.extensionPath === "string" && detail.extensionPath !== "") {
              extensionPaths.push([token, detail.extensionPath]);
            }
          }
        }
        const options = {
          extensionPaths,
          env: vAPI2.webextFlavor?.env || []
        };
        console.log("[DNR] Converting", validLists.length, "lists to DNR rules...");
        const dnrData = await dnrRulesetFromRawLists(validLists, options);
        if (dnrData && dnrData.network && dnrData.network.ruleset) {
          const staticRules = dnrData.network.ruleset;
          let ruleId = 100;
          for (const rule of staticRules) {
            if (rule.id === 0) {
              continue;
            }
            rules.push({
              ...rule,
              id: ruleId++
            });
          }
          console.log("[DNR] Compiled", rules.length, "static filter rules");
        } else {
          console.log("[DNR] No static rules from dnrRulesetFromRawLists");
        }
      } catch (e) {
        console.error("[DNR] Failed to compile static filters:", e);
      }
      return rules;
    }
    /**
     * Compile filters from the already-loaded engine (works with selfie)
     * This uses the compiled filter data that was loaded from selfie
     */
    compileFromEngine() {
      const rules = [];
      try {
        console.log("[DNR] Compiling from engine...");
        const filterCount = static_net_filtering_default.getFilterCount();
        console.log("[DNR] Engine has", filterCount, "compiled filters");
        if (filterCount === 0) {
          return rules;
        }
        const extensionPaths = [];
        if (typeof background_default.redirectEngine !== "undefined" && background_default.redirectEngine.getResourceDetails) {
          const details = background_default.redirectEngine.getResourceDetails();
          for (const [token, detail] of details) {
            if (typeof detail.extensionPath === "string" && detail.extensionPath !== "") {
              extensionPaths.push([token, detail.extensionPath]);
            }
          }
        }
        const context = {
          bad: /* @__PURE__ */ new Set(),
          good: /* @__PURE__ */ new Set(),
          invalid: /* @__PURE__ */ new Set(),
          filterCount: 0,
          acceptedFilterCount: 0,
          rejectedFilterCount: 0,
          extensionPaths: new Map(extensionPaths),
          env: vAPI2.webextFlavor?.env || [],
          responseHeaderRules: []
        };
        static_net_filtering_default.dnrFromCompiled("begin", context);
        const result = static_net_filtering_default.dnrFromCompiled("end", context);
        if (result && result.ruleset) {
          const staticRules = result.ruleset;
          let ruleId = 100;
          for (const rule of staticRules) {
            if (rule.id === 0) {
              continue;
            }
            rules.push({
              ...rule,
              id: ruleId++
            });
          }
          console.log("[DNR] Extracted", rules.length, "rules from compiled engine");
        }
      } catch (e) {
        console.error("[DNR] Failed to compile from engine:", e);
      }
      return rules;
    }
    compileUserRules() {
      const rules = [];
      const permRules = this.compileFirewallRules(permanentFirewall, 9e6);
      const sessRules = this.compileFirewallRules(sessionFirewall, 9001e3);
      rules.push(...permRules, ...sessRules);
      console.log(`[DNR] Compiled ${rules.length} firewall rules`);
      return rules;
    }
    /**
     * Compile a DynamicHostRuleFiltering instance to DNR rules
     * Uses firewall.toArray() to get rules as strings
     */
    compileFirewallRules(firewall, baseId) {
      const rules = [];
      if (typeof firewall?.toArray !== "function") {
        return rules;
      }
      const ruleStrings = firewall.toArray();
      let ruleId = 0;
      for (const ruleStr of ruleStrings) {
        const parts = ruleStr.split(" ");
        if (parts.length < 4) {
          continue;
        }
        const [src, des, type, action] = parts;
        if (action === "noop") {
          continue;
        }
        if (des === "*") {
          continue;
        }
        const resourceTypes = this.getDNRResourceTypes(type);
        const urlFilter = this.buildURLFilter(des);
        const dnrAction = action === "allow" ? "allow" : "block";
        for (const rt2 of resourceTypes) {
          rules.push({
            id: baseId + ruleId,
            priority: baseId + ruleId,
            // Use rule ID as priority (within valid range 1-2147483647)
            action: { type: dnrAction },
            condition: {
              initiatorDomains: src !== "*" ? [src] : void 0,
              urlFilter,
              resourceTypes: [rt2]
            }
          });
          ruleId++;
        }
      }
      return rules;
    }
    /**
     * Map firewall types to DNR resource types
     * Note: Some firewall types require multiple DNR rules
     */
    getDNRResourceTypes(firewallType) {
      const map = {
        "*": ["main_frame", "sub_frame", "script", "image", "other"],
        "image": ["image"],
        "3p-script": ["script"],
        "3p-frame": ["sub_frame"],
        "1p-script": ["script"],
        "inline-script": ["script"],
        "3p": ["script", "image", "sub_frame"]
      };
      return map[firewallType] || ["script"];
    }
    /**
     * Convert firewall destination to URL filter
     */
    buildURLFilter(destination) {
      if (destination === "*") {
        return ".*";
      }
      return destination.replace(/[.+?^${}()|[\]\\]/g, "\\$&");
    }
    async compileUserFiltersFromStorage() {
      const rules = [];
      try {
        const userFiltersData = await storage.readUserFilters();
        const content = userFiltersData.content || "";
        const filterLines = content.split("\n");
        for (const line of filterLines) {
          const filter = line.trim();
          if (!filter || filter.startsWith("!") || filter.startsWith("[")) continue;
          const rule = this.parseFilterToDNRRule(filter);
          if (rule) {
            rules.push(rule);
          }
        }
        console.log(`[DNR] Compiled ${rules.length} rules from user filters`);
      } catch (e) {
        console.error("[DNR] Failed to compile user filters:", e);
      }
      return rules;
    }
    parseFilterToDNRRule(filter) {
      const id = this.ruleIdCounter++;
      if (filter.startsWith("@@")) {
        return this.parseAllowRule(filter.slice(2), id);
      } else if (filter.startsWith("||")) {
        return this.parseBlockRule(filter.slice(2), id);
      } else if (filter.startsWith("|")) {
        return this.parseBlockRule(filter.slice(1), id);
      }
      return null;
    }
    parseBlockRule(pattern, id) {
      let urlFilter = pattern;
      let domains = null;
      const domainOptionMatch = urlFilter.match(/\$domain=([^,]+)/i);
      if (domainOptionMatch) {
        const domainStr = domainOptionMatch[1];
        domains = domainStr.split("|").map((d2) => d2.trim());
        urlFilter = urlFilter.replace(/\$domain=[^,]+/i, "");
      }
      const thirdPartyMatch = urlFilter.match(/\$third-party/i);
      const firstPartyMatch = urlFilter.match(/\$first-party/i);
      urlFilter = urlFilter.replace(/\$[^,]+/g, "");
      if (!urlFilter || urlFilter === "^") {
        urlFilter = ".*";
      } else {
        urlFilter = urlFilter.replace(/\^/g, "[/:?#&]").replace(/\*/g, ".*").replace(/\./g, "\\.");
      }
      if (!urlFilter.startsWith("^") && !urlFilter.startsWith(".*")) {
        urlFilter = `^${urlFilter}`;
      }
      return {
        id,
        priority: 1,
        action: { type: "block" },
        condition: {
          urlFilter,
          ...domains && { initiatorDomains: domains },
          ...thirdPartyMatch && { resourceTypes: ["image", "script", "stylesheet", "font", "websocket", "media", "other"] }
        }
      };
    }
    parseAllowRule(pattern, id) {
      let urlFilter = pattern;
      let domains = null;
      const domainOptionMatch = urlFilter.match(/\$domain=([^,]+)/i);
      if (domainOptionMatch) {
        const domainStr = domainOptionMatch[1];
        domains = domainStr.split("|").map((d2) => d2.trim());
        urlFilter = urlFilter.replace(/\$domain=[^,]+/i, "");
      }
      urlFilter = urlFilter.replace(/\$[^,]+/g, "");
      if (!urlFilter || urlFilter === "^") {
        urlFilter = ".*";
      } else {
        urlFilter = urlFilter.replace(/\^/g, "[/:?#&]").replace(/\*/g, ".*").replace(/\./g, "\\.");
      }
      return {
        id,
        priority: 2,
        action: { type: "allow" },
        condition: {
          urlFilter,
          ...domains && { initiatorDomains: domains }
        }
      };
    }
    compileWhitelist() {
      const rules = [];
      const whitelist = background_default.arrayFromWhitelist(background_default.netWhitelist) || [];
      for (const pattern of whitelist) {
        if (typeof pattern !== "string" || pattern.length === 0) continue;
        if (pattern.startsWith("#")) continue;
        rules.push({
          id: this.ruleIdCounter++,
          priority: 3,
          action: { type: "allow" },
          condition: {
            urlFilter: this.patternToRegex(pattern)
          }
        });
      }
      return rules;
    }
    patternToRegex(pattern) {
      if (!pattern || pattern === "*") return ".*";
      let regex = pattern;
      if (regex.startsWith("||")) {
        regex = `^https?://([^/]+\\.)?${regex.slice(2)}`;
      } else if (regex.startsWith("|")) {
        regex = `^${regex.slice(1)}`;
      } else if (regex.endsWith("|")) {
        regex = `${regex.slice(0, -1)}$`;
      }
      regex = regex.replace(/[.+?^${}()|[\]\\]/g, "\\$&");
      regex = regex.replace(/\\\*/g, ".*");
      return regex || ".*";
    }
    async updateRules() {
      if (!this.enabled) return;
      await this.compileAndInstallRules();
    }
    async clear() {
      if (!this.enabled || !this.dnrApi) return;
      try {
        const rules = await this.dnrApi.getDynamicRules();
        const ids = rules.map((r) => r.id);
        if (ids.length > 0) {
          await this.dnrApi.updateDynamicRules({ removeRuleIds: ids });
        }
        console.log("[DNR] Cleared all dynamic rules");
      } catch (e) {
        console.error("[DNR] Failed to clear rules:", e);
      }
    }
    getStats() {
      if (!this.enabled) return null;
      return {
        enabled: true,
        mode: isMV3() ? "MV3" : "MV2-webRequest",
        platform: isGecko2 ? "Firefox" : "Chrome/Chromium"
      };
    }
    async getDynamicRules() {
      if (!this.enabled || !this.dnrApi) return [];
      try {
        const rules = await this.dnrApi.getDynamicRules();
        return rules;
      } catch (e) {
        console.error("[DNR] Failed to get dynamic rules:", e);
        return [];
      }
    }
    async getSessionRules() {
      if (!this.enabled || !this.dnrApi) return [];
      try {
        const rules = await this.dnrApi.getSessionRules();
        return rules;
      } catch (e) {
        console.error("[DNR] Failed to get session rules:", e);
        return [];
      }
    }
    async getUserRules() {
      const userRules = [];
      try {
        const whitelist = background_default.arrayFromWhitelist(background_default.netWhitelist) || [];
        for (const pattern of whitelist) {
          if (typeof pattern !== "string" || pattern.length === 0) continue;
          if (pattern.startsWith("#")) continue;
          userRules.push({
            pattern,
            type: "whitelist"
          });
        }
        const userFiltersData = await storage.readUserFilters();
        const content = userFiltersData.content || "";
        const filterLines = content.split("\n");
        for (const line of filterLines) {
          const filter = line.trim();
          if (!filter || filter.startsWith("!") || filter.startsWith("[")) continue;
          userRules.push({
            filter,
            type: "user-filter"
          });
        }
      } catch (e) {
        console.error("[DNR] Failed to get user rules:", e);
      }
      return userRules;
    }
    async getRulesetDetails() {
      const details = [];
      try {
        const selectedLists = background_default.selectedFilterLists || [];
        for (const assetKey of selectedLists) {
          const list = background_default.availableFilterLists?.[assetKey];
          details.push({
            assetKey,
            title: list?.title || assetKey,
            enabled: true,
            url: list?.supportURL || ""
          });
        }
      } catch (e) {
        console.error("[DNR] Failed to get ruleset details:", e);
      }
      return details;
    }
    async getEnabledRulesetsDetails() {
      return this.getRulesetDetails();
    }
  };
  var dnrIntegration = new DNRIntegration();

  // src/js/messaging.ts
  var registerMessagingListener = (details) => {
    if (typeof vAPI.messaging?.listen !== "function") {
      return;
    }
    vAPI.messaging.listen(details);
  };
  var getWarSecretShort = () => typeof vAPI === "object" && vAPI !== null && typeof vAPI.warSecret?.short === "function" ? vAPI.warSecret.short() : "";
  {
    const clickToLoad = function(request, sender) {
      const { tabId, frameId } = sender;
      if (tabId === void 0 || frameId === void 0) {
        return false;
      }
      const pageStore = background_default.pageStoreFromTabId(tabId);
      if (pageStore === null) {
        return false;
      }
      pageStore.clickToLoad(frameId, request.frameURL);
      return true;
    };
    const getDomainNames = function(targets) {
      return targets.map((target) => {
        if (typeof target !== "string") {
          return "";
        }
        return target.indexOf("/") !== -1 ? domainFromURI(target) || "" : domainFromHostname(target) || target;
      });
    };
    const onMessage = function(request, sender, callback) {
      const webextAPI3 = typeof self.browser !== "undefined" ? self.browser : chrome;
      switch (request.what) {
        case "getAssetContent":
          assets_default.get(request.url, {
            dontCache: true,
            needSourceURL: true
          }).then((result) => {
            result.trustedSource = background_default.isTrustedList(result.assetKey);
            callback(result);
          });
          return;
        case "listsFromNetFilter":
          staticFilteringReverseLookup.fromNetFilter(
            request.rawFilter
          ).then((response2) => {
            callback(response2);
          });
          return;
        case "listsFromCosmeticFilter":
          staticFilteringReverseLookup.fromExtendedFilter(
            request
          ).then((response2) => {
            callback(response2);
          });
          return;
        case "reloadAllFilters":
          console.log("[MV3-DEBUG] reloadAllFilters called");
          background_default.loadFilterLists().then(() => {
            console.log("[MV3-DEBUG] loadFilterLists completed");
            callback();
          });
          return;
        case "createUserFilter":
          console.log("[MSG] createUserFilter received - filters:", request.filters, "docURL:", request.docURL);
          const allLines = (request.filters || "").split("\n").map((l) => l.trim()).filter(Boolean);
          const smartLinesFromFilter = [];
          const networkLinesFromFilter = [];
          for (const line of allLines) {
            if (line.startsWith("hide|") || line.startsWith("unhide|")) {
              smartLinesFromFilter.push(line);
            } else if (/##/.test(line)) {
              const m = line.match(/^(.*?)(#@?#)(.+)$/);
              if (m) {
                const domain = m[1] || "*";
                const isException = m[2] === "#@#";
                const selector = m[3];
                smartLinesFromFilter.push(`${isException ? "unhide" : "hide"}|${domain}|${selector}`);
              } else {
                networkLinesFromFilter.push(line);
              }
            } else {
              networkLinesFromFilter.push(line);
            }
          }
          if (smartLinesFromFilter.length > 0) {
            (async () => {
              try {
                const { smartRuleStore: smartRuleStore2 } = await Promise.resolve().then(() => (init_smart_rule_store(), smart_rule_store_exports));
                await smartRuleStore2.load();
                for (const smartLine of smartLinesFromFilter) {
                  const parts = smartLine.split("|");
                  const isUnhide = parts[0] === "unhide";
                  const selector = parts[parts.length - 1];
                  const domain = parts.length > 2 ? parts.slice(1, -1).join("|") : "*";
                  if (!selector) continue;
                  const targets = domain === "*" ? [{ form: "host", value: "*" }] : domain.split(",").filter(Boolean).map((d2) => ({ form: "host", value: d2 }));
                  const rule = {
                    type: isUnhide ? "show-exact" : "hide-exact",
                    id: `ubr:smart:user-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
                    syntaxVersion: 1,
                    state: "active",
                    targets,
                    selector,
                    action: { action: isUnhide ? "show" : "hide" },
                    metadata: { createdAt: (/* @__PURE__ */ new Date()).toISOString(), source: "user-filter" },
                    collectionId: "user-filters"
                  };
                  await smartRuleStore2.addRule(rule);
                }
              } catch (e) {
                console.error("[MSG] Failed to create smart rule:", e);
              }
            })();
          }
          return background_default.createUserFilters({ ...request, filters: networkLinesFromFilter.join("\n") || void 0 }).then(() => {
            console.log("[MSG] createUserFilters completed");
            callback();
          });
        case "scriptlet":
          vAPI.tabs.executeScript(request.tabId, {
            file: `/js/scriptlets/${request.scriptlet}.js`
          }).then((result) => {
            callback(result);
          });
          return;
        // GAP #1: Dynamic Content Script Registration
        case "getRegisteredContentScripts":
          scriptingManager.getRegisteredContentScripts().then((ids) => {
            callback(ids);
          });
          return;
        case "reRegisterContentScripts":
          scriptingManager.reRegisterContentScripts().then(() => {
            callback();
          });
          return;
        // GAP #2: Effective DNR Rules Visibility
        case "getEffectiveDynamicRules":
          dnrIntegration.getDynamicRules().then((rules) => {
            callback(rules);
          });
          return;
        case "getEffectiveSessionRules":
          dnrIntegration.getSessionRules().then((rules) => {
            callback(rules);
          });
          return;
        case "getEffectiveUserRules":
          dnrIntegration.getUserRules().then((rules) => {
            callback(rules);
          });
          return;
        case "getRulesetDetails":
          dnrIntegration.getRulesetDetails().then((details) => {
            callback(details);
          });
          return;
        case "getEnabledRulesetsDetails":
          dnrIntegration.getEnabledRulesetsDetails().then((details) => {
            callback(details);
          });
          return;
        case "showMatchedRules":
          vAPI.tabs.create({ url: "/matched-rules.html" });
          callback();
          return;
        default:
          break;
      }
      let response;
      switch (request.what) {
        case "applyFilterListSelection":
          response = background_default.applyFilterListSelection(request);
          break;
        case "clickToLoad":
          response = clickToLoad(request, sender);
          break;
        case "getAppData":
          response = {
            name: webextAPI3.runtime.getManifest().name,
            version: vAPI.app.version,
            canBenchmark: background_default.hiddenSettings.benchmarkDatasetURL !== "unset"
          };
          break;
        case "getDomainNames":
          response = getDomainNames(request.targets);
          break;
        case "getTrustedScriptletTokens":
          response = redirectEngine.getTrustedScriptletTokens();
          break;
        case "getWhitelist":
          response = {
            whitelist: background_default.arrayFromWhitelist(background_default.netWhitelist),
            whitelistDefault: background_default.netWhitelistDefault,
            reBadHostname: background_default.reWhitelistBadHostname.source,
            reHostnameExtractor: background_default.reWhitelistHostnameExtractor.source
          };
          break;
        case "launchElementPicker":
          background_default.epickerArgs.mouse = false;
          background_default.elementPickerExec(request.tabId, 0, request.targetURL, request.zap);
          break;
        case "loggerDisabled":
          background_default.clearInMemoryFilters();
          break;
        case "gotoURL":
          background_default.openNewTab(request.details);
          break;
        case "readyToFilter":
          response = background_default.readyToFilter;
          break;
        // https://github.com/uBlockOrigin/uBlock-issues/issues/1954
        //   In case of document-blocked page, navigate to blocked URL instead
        //   of forcing a reload.
        case "reloadTab": {
          if (vAPI.isBehindTheSceneTabId(request.tabId)) {
            break;
          }
          const { tabId, bypassCache, url, select } = request;
          vAPI.tabs.get(tabId).then((tab) => {
            if (url && tab && url !== tab.url) {
              vAPI.tabs.replace(tabId, url);
            } else {
              vAPI.tabs.reload(tabId, bypassCache === true);
            }
          }).catch(() => {
          });
          if (select && vAPI.tabs.select) {
            vAPI.tabs.select(tabId);
          }
          break;
        }
        case "setWhitelist":
          background_default.netWhitelist = background_default.whitelistFromString(request.whitelist);
          background_default.saveWhitelist();
          filteringBehaviorChanged();
          break;
        case "toggleHostnameSwitch":
          background_default.toggleHostnameSwitch(request);
          break;
        case "uiAccentStylesheet":
          background_default.uiAccentStylesheet = request.stylesheet;
          break;
        case "uiStyles":
          response = {
            uiAccentCustom: background_default.userSettings.uiAccentCustom,
            uiAccentCustom0: background_default.userSettings.uiAccentCustom0,
            uiAccentStylesheet: background_default.uiAccentStylesheet,
            uiStyles: background_default.hiddenSettings.uiStyles,
            uiTheme: background_default.userSettings.uiTheme
          };
          break;
        case "userSettings":
          response = background_default.changeUserSettings(request.name, request.value);
          if (response instanceof Object) {
            if (vAPI.net.canUncloakCnames !== true) {
              response.cnameUncloakEnabled = void 0;
            }
            response.canLeakLocalIPAddresses = vAPI.browserSettings.canLeakLocalIPAddresses === true;
          }
          break;
        default:
          return vAPI.messaging.UNHANDLED;
      }
      callback(response);
    };
    if (typeof vAPI.messaging?.setup === "function") {
      vAPI.messaging.setup(onMessage);
    }
  }
  {
    const createCounts = () => {
      return {
        blocked: { any: 0, frame: 0, script: 0 },
        allowed: { any: 0, frame: 0, script: 0 }
      };
    };
    const getHostnameDict = function(hostnameDetailsMap, out) {
      const hnDict = /* @__PURE__ */ Object.create(null);
      const cnMap = [];
      const createDictEntry = (domain, hostname, details) => {
        if (details.cname) {
          cnMap.push([details.cname, hostname]);
        }
        hnDict[hostname] = { domain, counts: details.counts };
      };
      for (const hnDetails of hostnameDetailsMap.values()) {
        const hostname = hnDetails.hostname;
        if (hnDict[hostname] !== void 0) {
          continue;
        }
        const domain = domainFromHostname(hostname) || hostname;
        const dnDetails = hostnameDetailsMap.get(domain) || { counts: createCounts() };
        if (hnDict[domain] === void 0) {
          createDictEntry(domain, domain, dnDetails);
        }
        if (hostname === domain) {
          continue;
        }
        createDictEntry(domain, hostname, hnDetails);
      }
      out.hostnameDict = hnDict;
      out.cnameMap = cnMap;
    };
    const firewallRuleTypes = [
      "*",
      "image",
      "3p",
      "inline-script",
      "1p-script",
      "3p-script",
      "3p-frame"
    ];
    const getFirewallRules = function(src, out) {
      const ruleset = out.firewallRules = {};
      const df = sessionFirewall;
      for (const type of firewallRuleTypes) {
        const r = df.lookupRuleData("*", "*", type);
        if (r === void 0) {
          continue;
        }
        ruleset[`/ * ${type}`] = r;
      }
      if (typeof src !== "string") {
        return;
      }
      for (const type of firewallRuleTypes) {
        const r = df.lookupRuleData(src, "*", type);
        if (r === void 0) {
          continue;
        }
        ruleset[`. * ${type}`] = r;
      }
      const { hostnameDict } = out;
      for (const des in hostnameDict) {
        let r = df.lookupRuleData("*", des, "*");
        if (r !== void 0) {
          ruleset[`/ ${des} *`] = r;
        }
        r = df.lookupRuleData(src, des, "*");
        if (r !== void 0) {
          ruleset[`. ${des} *`] = r;
        }
      }
    };
    const popupDataFromTabId = function(tabId, tabTitle) {
      const tabContext = background_default.tabContextManager.mustLookup(tabId);
      const rootHostname = tabContext.rootHostname;
      const \u00B5bus = background_default.userSettings;
      const \u00B5bhs = background_default.hiddenSettings;
      const r = {
        advancedUserEnabled: \u00B5bus.advancedUserEnabled,
        appName: vAPI.app.name,
        appVersion: vAPI.app.version,
        colorBlindFriendly: \u00B5bus.colorBlindFriendly,
        cosmeticFilteringSwitch: false,
        firewallPaneMinimized: \u00B5bus.firewallPaneMinimized,
        globalAllowedRequestCount: background_default.requestStats.allowedCount,
        globalBlockedRequestCount: background_default.requestStats.blockedCount,
        fontSize: \u00B5bhs.popupFontSize,
        godMode: \u00B5bhs.filterAuthorMode,
        netFilteringSwitch: false,
        userFiltersAreEnabled: background_default.userFiltersAreEnabled(),
        rawURL: tabContext.rawURL,
        pageURL: tabContext.normalURL,
        pageHostname: rootHostname,
        pageDomain: tabContext.rootDomain,
        popupBlockedCount: 0,
        popupPanelSections: \u00B5bus.popupPanelSections,
        popupPanelDisabledSections: \u00B5bhs.popupPanelDisabledSections,
        popupPanelLockedSections: \u00B5bhs.popupPanelLockedSections,
        popupPanelHeightMode: \u00B5bhs.popupPanelHeightMode,
        popupPanelOrientation: \u00B5bhs.popupPanelOrientation,
        tabId,
        tabTitle,
        tooltipsDisabled: \u00B5bus.tooltipsDisabled,
        hasUnprocessedRequest: vAPI.net && vAPI.net.hasUnprocessedRequest(tabId)
      };
      if (\u00B5bhs.uiPopupConfig !== "unset") {
        r.uiPopupConfig = \u00B5bhs.uiPopupConfig;
      }
      const pageStore = background_default.pageStoreFromTabId(tabId);
      if (pageStore) {
        r.pageCounts = pageStore.counts;
        r.netFilteringSwitch = pageStore.getNetFilteringSwitch();
        getHostnameDict(pageStore.getAllHostnameDetails(), r);
        r.contentLastModified = pageStore.contentLastModified;
        getFirewallRules(rootHostname, r);
        r.canElementPicker = isNetworkURI(r.rawURL) && /^https?:\/\/(chrome\.google\.com|chromewebstore\.google\.com)\//.test(r.rawURL) === false;
        r.noPopups = sessionSwitches.evaluateZ(
          "no-popups",
          rootHostname
        );
        r.popupBlockedCount = pageStore.popupBlockedCount;
        r.noCosmeticFiltering = sessionSwitches.evaluateZ(
          "no-cosmetic-filtering",
          rootHostname
        );
        r.noLargeMedia = sessionSwitches.evaluateZ(
          "no-large-media",
          rootHostname
        );
        r.largeMediaCount = pageStore.largeMediaCount;
        r.noRemoteFonts = sessionSwitches.evaluateZ(
          "no-remote-fonts",
          rootHostname
        );
        r.remoteFontCount = pageStore.remoteFontCount;
        r.noScripting = sessionSwitches.evaluateZ(
          "no-scripting",
          rootHostname
        );
      } else {
        r.hostnameDict = {};
        getFirewallRules(void 0, r);
      }
      r.matrixIsDirty = sessionFirewall.hasSameRules(
        permanentFirewall,
        rootHostname,
        r.hostnameDict
      ) === false;
      if (r.matrixIsDirty === false) {
        r.matrixIsDirty = sessionSwitches.hasSameRules(
          permanentSwitches,
          rootHostname
        ) === false;
      }
      return r;
    };
    const popupDataFromRequest = async function(request) {
      if (request.tabId) {
        return popupDataFromTabId(request.tabId, "");
      }
      const tab = await vAPI.tabs.getCurrent();
      let tabId = "";
      let tabTitle = "";
      if (tab instanceof Object) {
        tabId = tab.id;
        tabTitle = tab.title || "";
      }
      return popupDataFromTabId(tabId, tabTitle);
    };
    const getElementCount = async function(tabId, what) {
      const results = await vAPI.tabs.executeScript(tabId, {
        allFrames: true,
        file: `/js/scriptlets/dom-survey-${what}.js`,
        runAt: "document_end"
      });
      let total = 0;
      for (const count of results) {
        if (typeof count !== "number") {
          continue;
        }
        if (count === -1) {
          return -1;
        }
        total += count;
      }
      return total;
    };
    const onMessage = function(request, sender, callback) {
      switch (request.what) {
        case "getHiddenElementCount":
          getElementCount(request.tabId, "elements").then((count) => {
            callback(count);
          });
          return;
        case "getScriptCount":
          getElementCount(request.tabId, "scripts").then((count) => {
            callback(count);
          });
          return;
        case "getPopupData":
          popupDataFromRequest(request).then((popupData) => {
            callback(popupData);
          });
          return;
        default:
          break;
      }
      let response;
      switch (request.what) {
        case "dismissUnprocessedRequest":
          vAPI.net.removeUnprocessedRequest(request.tabId);
          background_default.updateToolbarIcon(request.tabId, 6);
          break;
        case "hasPopupContentChanged": {
          const pageStore = background_default.pageStoreFromTabId(request.tabId);
          const lastModified = pageStore ? pageStore.contentLastModified : 0;
          response = lastModified !== request.contentLastModified;
          break;
        }
        case "revertFirewallRules":
          sessionFirewall.copyRules(
            permanentFirewall,
            request.srcHostname,
            Object.assign(/* @__PURE__ */ Object.create(null), request.desHostnames)
          );
          sessionSwitches.copyRules(
            permanentSwitches,
            request.srcHostname
          );
          background_default.updateToolbarIcon(request.tabId, 4);
          response = popupDataFromTabId(request.tabId);
          break;
        case "saveFirewallRules":
          if (permanentFirewall.copyRules(
            sessionFirewall,
            request.srcHostname,
            Object.assign(/* @__PURE__ */ Object.create(null), request.desHostnames)
          )) {
            background_default.savePermanentFirewallRules();
          }
          if (permanentSwitches.copyRules(
            sessionSwitches,
            request.srcHostname
          )) {
            background_default.saveHostnameSwitches();
          }
          break;
        case "toggleHostnameSwitch":
          background_default.toggleHostnameSwitch(request);
          response = popupDataFromTabId(request.tabId);
          break;
        case "toggleFirewallRule":
          background_default.toggleFirewallRule(request);
          response = popupDataFromTabId(request.tabId);
          break;
        case "toggleNetFiltering": {
          const pageStore = background_default.pageStoreFromTabId(request.tabId);
          if (pageStore) {
            pageStore.toggleNetFilteringSwitch(
              request.url,
              request.scope,
              request.state
            );
            background_default.updateToolbarIcon(request.tabId, 7);
          }
          break;
        }
        default:
          return vAPI.messaging.UNHANDLED;
      }
      callback(response);
    };
    registerMessagingListener({
      name: "popupPanel",
      listener: onMessage,
      privileged: true
    });
  }
  {
    const retrieveContentScriptParameters = async function(sender, request) {
      if (background_default.readyToFilter !== true) {
        return;
      }
      const { tabId, frameId } = sender;
      if (tabId === void 0 || frameId === void 0) {
        return;
      }
      const pageStore = background_default.pageStoreFromTabId(tabId);
      if (pageStore === null || pageStore.getNetFilteringSwitch() === false) {
        return;
      }
      if (frameId !== 0 && request.url.startsWith("about:")) {
        request.url = pageStore.getEffectiveFrameURL(sender);
      }
      const noSpecificCosmeticFiltering = pageStore.shouldApplySpecificCosmeticFilters(frameId) === false;
      const noGenericCosmeticFiltering = pageStore.shouldApplyGenericCosmeticFilters(frameId) === false;
      const response = {
        collapseBlocked: background_default.userSettings.collapseBlocked,
        noGenericCosmeticFiltering,
        noSpecificCosmeticFiltering
      };
      request.tabId = tabId;
      request.frameId = frameId;
      request.hostname = hostnameFromURI(request.url);
      request.domain = domainFromHostname(request.hostname);
      request.ancestors = pageStore.getFrameAncestorDetails(frameId);
      console.log("[MV3-MSg] retrieveContentScriptParameters - tabId:", tabId, "frameId:", frameId, "hostname:", request.hostname);
      if (scf.proceduralFilters.length !== 0 || logger_default.enabled && (scf.convertedProceduralFilters.length !== 0 || scf.exceptedFilters.length !== 0)) {
        await vAPI.tabs.executeScript(tabId, {
          allFrames: false,
          file: "/js/contentscript-extra.js",
          frameId,
          matchAboutBlank: true,
          runAt: "document_start"
        });
      }
      if (logger_default.enabled) {
        const scriptletDetails = scriptlet_filtering_default.retrieve(request);
        if (scriptletDetails !== void 0) {
          scriptlet_filtering_default.toLogger(request, scriptletDetails);
        }
      }
      if (request.needScriptlets) {
        scriptlet_filtering_default.injectNow(request);
      }
      if (logger_default.enabled) {
        if (noSpecificCosmeticFiltering === false || noGenericCosmeticFiltering === false) {
          vAPI.tabs.executeScript(tabId, {
            allFrames: false,
            file: "/js/scriptlets/cosmetic-logger.js",
            frameId,
            matchAboutBlank: true,
            runAt: "document_start"
          });
        }
      }
      return response;
    };
    const onMessage = function(request, sender, callback) {
      switch (request.what) {
        case "retrieveContentScriptParameters":
          return retrieveContentScriptParameters(
            sender,
            request
          ).then((response2) => {
            callback(response2);
          });
        default:
          break;
      }
      const pageStore = background_default.pageStoreFromTabId(sender.tabId);
      let response;
      switch (request.what) {
        case "maybeGoodPopup":
          background_default.maybeGoodPopup.tabId = sender.tabId;
          background_default.maybeGoodPopup.url = request.url;
          break;
        case "messageToLogger":
          if (logger_default.enabled !== true) {
            break;
          }
          logger_default.writeOne({
            tabId: sender.tabId,
            realm: "message",
            type: request.type || "info",
            keywords: ["scriptlet"],
            text: request.text
          });
          break;
        case "shouldRenderNoscriptTags": {
          if (pageStore === null) {
            break;
          }
          if (background_default.hiddenSettings.noScriptingCSP !== background_default.hiddenSettingsDefault.noScriptingCSP) {
            break;
          }
          const fctxt = background_default.filteringContext.fromTabId(sender.tabId);
          if (pageStore.filterScripting(fctxt, void 0)) {
            vAPI.tabs.executeScript(sender.tabId, {
              file: "/js/scriptlets/noscript-spoof.js",
              frameId: sender.frameId,
              runAt: "document_end"
            });
          }
          break;
        }
        default:
          return vAPI.messaging.UNHANDLED;
      }
      callback(response);
    };
    registerMessagingListener({
      name: "contentscript",
      listener: onMessage
    });
  }
  {
    let epickerDebugLogs = [];
    const onMessage = function(request, sender, callback) {
      switch (request.what) {
        // Save epicker debug logs
        case "epickerDebugLog":
          epickerDebugLogs.push(request.log);
          if (epickerDebugLogs.length > 500) {
            epickerDebugLogs = epickerDebugLogs.slice(-500);
          }
          console.log("[EPICKER-LOG]", request.log);
          return;
        // Get epicker debug logs
        case "getEpickerLogs":
          callback(epickerDebugLogs);
          return;
        // Clear epicker debug logs
        case "clearEpickerLogs":
          epickerDebugLogs = [];
          callback();
          return;
        // The procedural filterer must be present in case the user wants to
        // type-in custom filters.
        case "elementPickerArguments":
          return vAPI.tabs.executeScript(sender.tabId, {
            allFrames: false,
            file: "/js/contentscript-extra.js",
            frameId: sender.frameId,
            matchAboutBlank: true,
            runAt: "document_start"
          }).then(() => {
            callback({
              target: background_default.epickerArgs.target,
              mouse: background_default.epickerArgs.mouse,
              zap: background_default.epickerArgs.zap,
              eprom: background_default.epickerArgs.eprom,
              pickerURL: vAPI.getURL(
                `/web_accessible_resources/epicker-ui.html?secret=${getWarSecretShort()}`
              )
            });
            background_default.epickerArgs.target = "";
          });
        case "createUserFilter": {
          const filterLines = (request.filters || request.filter || "").split("\n").map((l) => l.trim()).filter(Boolean);
          const smartLinesFromFilter = [];
          const networkLinesFromFilter = [];
          for (const line of filterLines) {
            if (line.startsWith("hide|") || line.startsWith("unhide|")) {
              smartLinesFromFilter.push(line);
            } else if (/##/.test(line)) {
              const m = line.match(/^(.*?)(#@?#)(.+)$/);
              if (m) {
                const domain = m[1] || "*";
                const isException = m[2] === "#@#";
                const selector = m[3];
                smartLinesFromFilter.push(`${isException ? "unhide" : "hide"}|${domain}|${selector}`);
              } else {
                networkLinesFromFilter.push(line);
              }
            } else {
              networkLinesFromFilter.push(line);
            }
          }
          if (smartLinesFromFilter.length > 0) {
            (async () => {
              try {
                const { smartRuleStore: smartRuleStore2 } = await Promise.resolve().then(() => (init_smart_rule_store(), smart_rule_store_exports));
                await smartRuleStore2.load();
                for (const smartLine of smartLinesFromFilter) {
                  const parts = smartLine.split("|");
                  const isUnhide = parts[0] === "unhide";
                  const selector = parts[parts.length - 1];
                  const domain = parts.length > 2 ? parts.slice(1, -1).join("|") : "*";
                  if (!selector) continue;
                  const targets = domain === "*" ? [{ form: "host", value: "*" }] : domain.split(",").filter(Boolean).map((d2) => ({ form: "host", value: d2 }));
                  const rule = {
                    type: isUnhide ? "show-exact" : "hide-exact",
                    id: `ubr:smart:user-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
                    syntaxVersion: 1,
                    state: "active",
                    targets,
                    selector,
                    action: { action: isUnhide ? "show" : "hide" },
                    metadata: { createdAt: (/* @__PURE__ */ new Date()).toISOString(), source: "user-filter" },
                    collectionId: "user-filters"
                  };
                  await smartRuleStore2.addRule(rule);
                }
              } catch (e) {
                console.error("[MSG] Failed to create smart rule:", e);
              }
            })();
          }
          if (networkLinesFromFilter.length > 0) {
            return background_default.createUserFilters({ ...request, filters: networkLinesFromFilter.join("\n") }).then(() => {
              callback();
            });
          }
          callback();
          return;
        }
        default:
          break;
      }
      let response;
      switch (request.what) {
        case "elementPickerEprom":
          background_default.epickerArgs.eprom = request;
          break;
        default:
          return vAPI.messaging.UNHANDLED;
      }
      callback(response);
    };
    registerMessagingListener({
      name: "elementPicker",
      listener: onMessage
    });
  }
  {
    const fromBase64 = function(encoded) {
      if (typeof encoded !== "string") {
        return Promise.resolve(encoded);
      }
      let u8array;
      try {
        u8array = denseBase64.decode(encoded);
      } catch (e) {
        console.warn("[uBR] messaging: denseBase64.decode failed", e);
      }
      return Promise.resolve(u8array !== void 0 ? u8array : encoded);
    };
    const onMessage = function(request, sender, callback) {
      if (background_default.cloudStorageSupported !== true) {
        callback();
        return;
      }
      switch (request.what) {
        case "cloudGetOptions":
          vAPI.cloud.getOptions((options) => {
            options.enabled = background_default.userSettings.cloudStorageEnabled === true;
            callback(options);
          });
          return;
        case "cloudSetOptions":
          vAPI.cloud.setOptions(request.options, callback);
          return;
        case "cloudPull":
          request.decode = (encoded) => {
            if (isSerialized(encoded)) {
              return deserializeAsync(encoded, { thread: true });
            }
            return lz4_default.decode(encoded, fromBase64);
          };
          return vAPI.cloud.pull(request).then((result) => {
            callback(result);
          });
        case "cloudPush":
          request.encode = (data) => {
            const options = {
              compress: background_default.hiddenSettings.cloudStorageCompression,
              thread: true
            };
            return serializeAsync(data, options);
          };
          return vAPI.cloud.push(request).then((result) => {
            callback(result);
          });
        case "cloudUsed":
          return vAPI.cloud.used(request.datakey).then((result) => {
            callback(result);
          });
        default:
          break;
      }
      let response;
      switch (request.what) {
        // For when cloud storage is disabled.
        case "cloudPull":
        // fallthrough
        case "cloudPush":
          break;
        default:
          return vAPI.messaging.UNHANDLED;
      }
      callback(response);
    };
    registerMessagingListener({
      name: "cloudWidget",
      listener: onMessage,
      privileged: true
    });
  }
  {
    const getLocalData = async function() {
      const data = Object.assign({}, background_default.restoreBackupSettings);
      data.storageUsed = await background_default.getBytesInUse();
      data.cloudStorageSupported = background_default.cloudStorageSupported;
      data.privacySettingsSupported = background_default.privacySettingsSupported;
      return data;
    };
    const backupUserData = async function() {
      const userFilters = await background_default.loadUserFilters();
      const userData = {
        timeStamp: Date.now(),
        version: vAPI.app.version,
        userSettings: background_default.getModifiedSettings(background_default.userSettings, background_default.userSettingsDefault),
        selectedFilterLists: background_default.selectedFilterLists,
        hiddenSettings: background_default.getModifiedSettings(background_default.hiddenSettings, background_default.hiddenSettingsDefault),
        whitelist: background_default.arrayFromWhitelist(background_default.netWhitelist),
        dynamicFilteringString: permanentFirewall.toString(),
        urlFilteringString: permanentURLFiltering.toString(),
        hostnameSwitchesString: permanentSwitches.toString(),
        userFilters: userFilters.content
      };
      const filename = i18n$("aboutBackupFilename").replace("{{datetime}}", background_default.dateNowToSensibleString()).replace(/ +/g, "_");
      background_default.restoreBackupSettings.lastBackupFile = filename;
      background_default.restoreBackupSettings.lastBackupTime = Date.now();
      vAPI.storage.set(background_default.restoreBackupSettings);
      const localData = await getLocalData();
      return { localData, userData };
    };
    const restoreUserData = async function(request) {
      const userData = request.userData;
      if (vAPI.webextFlavor.soup.has("firefox") && vAPI.app.intFromVersion(userData.version) <= 1031003011) {
        userData.hostnameSwitchesString += "\nno-csp-reports: * true";
      }
      if (Array.isArray(userData.externalLists)) {
        userData.externalLists = userData.externalLists.join("\n");
      }
      assets_default.rmrf();
      await Promise.all([
        cachestorage_default.clear(),
        vAPI.storage.clear()
      ]);
      background_default.saveLocalSettings();
      vAPI.storage.set(userData.userSettings);
      let hiddenSettings = userData.hiddenSettings;
      if (hiddenSettings instanceof Object === false) {
        hiddenSettings = background_default.hiddenSettingsFromString(
          userData.hiddenSettingsString || ""
        );
      }
      for (const key in hiddenSettings) {
        if (Object.hasOwn(background_default.hiddenSettingsDefault, key) === false || hiddenSettings[key] === background_default.hiddenSettingsDefault[key]) {
          delete hiddenSettings[key];
        }
      }
      let whitelist = userData.whitelist;
      if (Array.isArray(whitelist) === false && typeof userData.netWhitelist === "string" && userData.netWhitelist !== "") {
        whitelist = userData.netWhitelist.split("\n");
      }
      vAPI.storage.set({
        hiddenSettings,
        netWhitelist: whitelist || [],
        dynamicFilteringString: userData.dynamicFilteringString || "",
        urlFilteringString: userData.urlFilteringString || "",
        hostnameSwitchesString: userData.hostnameSwitchesString || "",
        lastRestoreFile: request.file || "",
        lastRestoreTime: Date.now(),
        lastBackupFile: "",
        lastBackupTime: 0
      });
      background_default.saveUserFilters(userData.userFilters);
      if (Array.isArray(userData.selectedFilterLists)) {
        await background_default.saveSelectedFilterLists(userData.selectedFilterLists);
      }
      vAPI.app.restart();
    };
    const resetUserData = async function() {
      await Promise.all([
        cachestorage_default.clear(),
        vAPI.storage.clear()
      ]);
      await background_default.saveLocalSettings();
      vAPI.app.restart();
    };
    const prepListEntries = function(entries) {
      for (const k2 in entries) {
        if (Object.hasOwn(entries, k2) === false) {
          continue;
        }
        const entry = entries[k2];
        if (typeof entry.supportURL === "string" && entry.supportURL !== "") {
          entry.supportName = hostnameFromURI(entry.supportURL);
        } else if (typeof entry.homeURL === "string" && entry.homeURL !== "") {
          const hn2 = hostnameFromURI(entry.homeURL);
          entry.supportURL = `http://${hn2}/`;
          entry.supportName = domainFromHostname(hn2);
        }
      }
    };
    const getLists = async function(callback) {
      const r = {
        autoUpdate: background_default.userSettings.autoUpdate,
        available: null,
        cache: null,
        current: background_default.availableFilterLists,
        ignoreGenericCosmeticFilters: background_default.userSettings.ignoreGenericCosmeticFilters,
        isUpdating: assets_default.isUpdating(),
        netFilterCount: static_net_filtering_default.getFilterCount(),
        suspendUntilListsAreLoaded: background_default.userSettings.suspendUntilListsAreLoaded,
        userFiltersPath: background_default.userFiltersPath
      };
      const [lists, metadata] = await Promise.all([
        background_default.getAvailableLists(),
        assets_default.metadata()
      ]);
      r.available = lists;
      prepListEntries(r.available);
      r.cache = metadata;
      prepListEntries(r.cache);
      callback(r);
    };
    const getOriginHints = function() {
      const out = /* @__PURE__ */ new Set();
      for (const tabId of background_default.pageStores.keys()) {
        if (tabId === -1) {
          continue;
        }
        const tabContext = background_default.tabContextManager.lookup(tabId);
        if (tabContext === null) {
          continue;
        }
        const { rootDomain, rootHostname } = tabContext;
        if (rootDomain.endsWith("-scheme")) {
          continue;
        }
        const isPunycode = rootHostname.includes("xn--");
        out.add(isPunycode ? punycode_default.toUnicode(rootDomain) : rootDomain);
        if (rootHostname === rootDomain) {
          continue;
        }
        out.add(isPunycode ? punycode_default.toUnicode(rootHostname) : rootHostname);
      }
      return Array.from(out);
    };
    const getRules = function() {
      return {
        permanentRules: permanentFirewall.toArray().concat(
          permanentSwitches.toArray(),
          permanentURLFiltering.toArray()
        ),
        sessionRules: sessionFirewall.toArray().concat(
          sessionSwitches.toArray(),
          sessionURLFiltering.toArray()
        ),
        pslSelfie: publicsuffixlist_default.toSelfie()
      };
    };
    const modifyRuleset = function(details) {
      let swRuleset, hnRuleset, urlRuleset;
      if (details.permanent) {
        swRuleset = permanentSwitches;
        hnRuleset = permanentFirewall;
        urlRuleset = permanentURLFiltering;
      } else {
        swRuleset = sessionSwitches;
        hnRuleset = sessionFirewall;
        urlRuleset = sessionURLFiltering;
      }
      const toRemove = new Set(details.toRemove.trim().split(/\s*[\n\r]+\s*/));
      for (const rule of toRemove) {
        if (rule === "") {
          continue;
        }
        const parts = rule.split(/\s+/);
        if (hnRuleset.removeFromRuleParts(parts) === false) {
          if (swRuleset.removeFromRuleParts(parts) === false) {
            urlRuleset.removeFromRuleParts(parts);
          }
        }
      }
      const toAdd = new Set(details.toAdd.trim().split(/\s*[\n\r]+\s*/));
      for (const rule of toAdd) {
        if (rule === "") {
          continue;
        }
        const parts = rule.split(/\s+/);
        if (hnRuleset.addFromRuleParts(parts) === false) {
          if (swRuleset.addFromRuleParts(parts) === false) {
            urlRuleset.addFromRuleParts(parts);
          }
        }
      }
      if (details.permanent) {
        if (swRuleset.changed) {
          background_default.saveHostnameSwitches();
          swRuleset.changed = false;
        }
        if (hnRuleset.changed) {
          background_default.savePermanentFirewallRules();
          hnRuleset.changed = false;
        }
        if (urlRuleset.changed) {
          background_default.savePermanentURLFilteringRules();
          urlRuleset.changed = false;
        }
      }
    };
    const onMessage = function(request, sender, callback) {
      switch (request.what) {
        case "getSmartRules":
          return (async () => {
            const { smartRuleStore: smartRuleStore2 } = await Promise.resolve().then(() => (init_smart_rule_store(), smart_rule_store_exports));
            await smartRuleStore2.load();
            const rules = smartRuleStore2.getAllRules();
            const collections = smartRuleStore2.getAllCollections();
            callback({ rules, collections });
          })();
        case "addSmartRule":
          return (async () => {
            const { smartRuleStore: smartRuleStore2 } = await Promise.resolve().then(() => (init_smart_rule_store(), smart_rule_store_exports));
            await smartRuleStore2.load();
            const rule = request.rule;
            rule.id = rule.id || `ubr:smart:${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
            rule.metadata = rule.metadata || { createdAt: (/* @__PURE__ */ new Date()).toISOString() };
            const result = await smartRuleStore2.addRule(rule);
            callback(result);
          })();
        case "updateSmartRule":
          return (async () => {
            const { smartRuleStore: smartRuleStore2 } = await Promise.resolve().then(() => (init_smart_rule_store(), smart_rule_store_exports));
            await smartRuleStore2.load();
            const result = await smartRuleStore2.updateRule(request.rule.id, request.rule);
            callback(result);
          })();
        case "removeSmartRule":
          return (async () => {
            const { smartRuleStore: smartRuleStore2 } = await Promise.resolve().then(() => (init_smart_rule_store(), smart_rule_store_exports));
            await smartRuleStore2.load();
            const ok = await smartRuleStore2.removeRule(request.id);
            callback({ ok });
          })();
        case "setSmartRuleState":
          return (async () => {
            const { smartRuleStore: smartRuleStore2 } = await Promise.resolve().then(() => (init_smart_rule_store(), smart_rule_store_exports));
            await smartRuleStore2.load();
            const ok = await smartRuleStore2.setRuleState(request.id, request.state);
            callback({ ok });
          })();
        case "subscribeSmartCollection":
          return (async () => {
            const { smartEngine: smartEngine2 } = await Promise.resolve().then(() => (init_engine(), engine_exports));
            await smartEngine2.init();
            const collectionId = `col-${Date.now()}`;
            const ok = await smartEngine2.subscribeToCollection(request.url, collectionId);
            callback({ ok, collectionId });
          })();
        case "testSmartRules":
          return (async () => {
            const { smartEngine: smartEngine2 } = await Promise.resolve().then(() => (init_engine(), engine_exports));
            await smartEngine2.init();
            const selectors = smartEngine2.applyRulesToTab(0, request.url).selectors;
            callback({ selectors });
          })();
        case "getCosmeticPlanForDocument":
          return (async () => {
            const { smartEngine: smartEngine2 } = await Promise.resolve().then(() => (init_engine(), engine_exports));
            await smartEngine2.init();
            const tabId = request.tabId || sender.tabId || 0;
            const result = smartEngine2.getCosmeticPlanForTab(tabId, request.url);
            callback(result);
          })();
        case "getSmartTabId":
          callback({ tabId: sender.tabId || 0 });
        case "previewSmartCosmeticRule":
          return (async () => {
            const { smartRuleStore: smartRuleStore2 } = await Promise.resolve().then(() => (init_smart_rule_store(), smart_rule_store_exports));
            const { evaluateWhereExcept: evaluateWhereExcept2 } = await Promise.resolve().then(() => (init_logic_evaluator(), logic_evaluator_exports));
            await smartRuleStore2.load();
            callback({ ok: true });
          })();
        case "confirmSmartCosmeticRulePreview":
          return (async () => {
            const { smartRuleStore: smartRuleStore2 } = await Promise.resolve().then(() => (init_smart_rule_store(), smart_rule_store_exports));
            await smartRuleStore2.load();
            const rule = smartRuleStore2.getRule(request.id);
            if (rule) {
              const updated = {
                ...rule,
                preview: {
                  status: "confirmed",
                  confirmationHash: request.hash || rule.preview?.confirmationHash,
                  confirmedAt: (/* @__PURE__ */ new Date()).toISOString(),
                  lastPreviewedAt: (/* @__PURE__ */ new Date()).toISOString()
                }
              };
              await smartRuleStore2.updateRule(request.id, updated);
              callback({ ok: true });
            } else {
              callback({ ok: false, error: "Rule not found" });
            }
          })();
        case "exportSmartRules":
          return (async () => {
            const { exportAllRules: exportAllRules2 } = await Promise.resolve().then(() => (init_export(), export_exports));
            const result = await exportAllRules2();
            callback(result);
          })();
        case "exportSmartRulesToClassic":
          return (async () => {
            const { exportAllRules: exportAllRules2 } = await Promise.resolve().then(() => (init_export(), export_exports));
            const result = await exportAllRules2();
            callback({ classicLines: result.classicLines, lossMetadata: result.lossMetadata });
          })();
        case "importSmartRules":
          return (async () => {
            const { parseSmartRules: parseSmartRules2 } = await Promise.resolve().then(() => (init_smart_rule_parser(), smart_rule_parser_exports));
            const { smartRuleStore: smartRuleStore2 } = await Promise.resolve().then(() => (init_smart_rule_store(), smart_rule_store_exports));
            await smartRuleStore2.load();
            const parsed = parseSmartRules2(request.yaml);
            const imported = [];
            const errors = [...parsed.errors];
            for (const rule of parsed.rules) {
              const meta = rule.metadata || { createdAt: (/* @__PURE__ */ new Date()).toISOString() };
              meta.importedAt = (/* @__PURE__ */ new Date()).toISOString();
              rule.metadata = meta;
              const result = await smartRuleStore2.addRule(rule);
              if (result.ok) {
                imported.push(rule);
              } else {
                errors.push(`Validation failed for rule ${rule.id}: ${JSON.stringify(result.validation?.diagnostics)}`);
              }
            }
            callback({ ok: imported.length > 0, count: imported.length, errors });
          })();
        case "backupUserData":
          return backupUserData().then((data) => {
            callback(data);
          });
        case "getLists":
          return background_default.isReadyPromise.then(() => {
            getLists(callback);
          });
        case "getLocalData":
          return getLocalData().then((localData) => {
            callback(localData);
          });
        case "readUserFilters":
          return background_default.loadUserFilters().then((result) => {
            result.enabled = background_default.selectedFilterLists.includes(background_default.userFiltersPath);
            result.trusted = background_default.isTrustedList(background_default.userFiltersPath);
            callback(result);
          });
        case "writeUserFilters":
          console.log("[MV3-DEBUG] writeUserFilters called - content length:", request.content ? request.content.length : 0);
          console.log("[MV3-DEBUG] Enabled:", request.enabled, "Trusted:", request.trusted);
          if (request.content && request.content.includes("##")) {
            const lines = request.content.split("\n");
            const smartLines = [];
            const cleaned = [];
            for (const line of lines) {
              if (line.includes("##")) {
                smartLines.push(line);
              } else {
                cleaned.push(line);
              }
            }
            if (smartLines.length > 0) {
              (async () => {
                try {
                  const { smartRuleStore: smartRuleStore2 } = await Promise.resolve().then(() => (init_smart_rule_store(), smart_rule_store_exports));
                  await smartRuleStore2.load();
                  for (const line of smartLines) {
                    const hashIdx = line.indexOf("##");
                    const domainPart = hashIdx > 0 ? line.slice(0, hashIdx) : "*";
                    const selector = line.slice(hashIdx + 2);
                    if (!selector) continue;
                    const targets = domainPart === "*" ? [{ form: "host", value: "*" }] : domainPart.split(",").filter(Boolean).map((d2) => ({ form: "host", value: d2 }));
                    const rule = {
                      type: "hide-exact",
                      id: `migrated:wf-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
                      syntaxVersion: 1,
                      state: "active",
                      targets,
                      selector,
                      action: { action: "hide" },
                      metadata: { createdAt: (/* @__PURE__ */ new Date()).toISOString(), source: "migration" },
                      collectionId: "migrated-cosmetic"
                    };
                    await smartRuleStore2.addRule(rule);
                  }
                } catch (e) {
                  console.error("[writeUserFilters] Failed to migrate ## filters:", e);
                }
              })();
            }
            request.content = cleaned.join("\n");
          }
          return Promise.resolve().then(async () => {
            const selected = new Set(background_default.selectedFilterLists);
            if (request.enabled) {
              selected.add(background_default.userFiltersPath);
            } else {
              selected.delete(background_default.userFiltersPath);
            }
            await background_default.saveSelectedFilterLists(Array.from(selected));
            background_default.changeUserSettings("userFiltersTrusted", request.trusted || false);
            const result = await background_default.saveUserFilters(request.content || "");
            broadcast({ what: "userFiltersUpdated" });
            callback(result);
          }).catch((reason) => {
            console.warn("[uBR] writeUserFilters: applyUserFilters failed", reason);
            callback({
              error: reason instanceof Error ? reason.message : String(reason)
            });
          });
        case "getAllCustomFilters":
          return getAllCustomFilters().then((result) => {
            callback(result);
          });
        case "addCustomFilters":
          return addCustomFilters(request.hostname, request.selectors).then((result) => {
            callback(result);
          });
        case "removeCustomFilters":
          return removeCustomFilters(request.hostname, request.selectors).then((result) => {
            callback(result);
          });
        case "removeAllCustomFilters":
          return removeAllCustomFilters(request.hostname).then((result) => {
            callback(result);
          });
        case "customFiltersFromHostname":
          return customFiltersFromHostname(request.hostname).then((result) => {
            callback(result);
          });
        default:
          break;
      }
      let response;
      switch (request.what) {
        case "dashboardConfig":
          response = {
            noDashboard: background_default.noDashboard
          };
          break;
        case "getAutoCompleteDetails":
          response = {};
          if ((request.hintUpdateToken || 0) === 0) {
            response.redirectResources = redirectEngine.getResourceDetails();
            response.preparseDirectiveEnv = vAPI.webextFlavor.env.slice();
            response.preparseDirectiveHints = utils.preparser.getHints();
          }
          if (request.hintUpdateToken !== background_default.pageStoresToken) {
            response.originHints = getOriginHints();
            response.hintUpdateToken = background_default.pageStoresToken;
          }
          break;
        case "getRules":
          response = getRules();
          break;
        case "modifyRuleset":
          modifyRuleset(request);
          response = getRules();
          break;
        case "listsUpdateNow": {
          const { assetKeys, preferOrigin = false } = request;
          if (assetKeys.length === 0) {
            return;
          }
          for (const assetKey of assetKeys) {
            assets_default.purge(assetKey);
          }
          background_default.scheduleAssetUpdater({ now: true, fetchDelay: 100, auto: preferOrigin !== true });
          break;
        }
        case "readHiddenSettings":
          response = {
            "default": background_default.hiddenSettingsDefault,
            "admin": background_default.hiddenSettingsAdmin,
            "current": background_default.hiddenSettings
          };
          break;
        case "restoreUserData":
          restoreUserData(request);
          break;
        case "resetUserData":
          resetUserData();
          break;
        case "updateNow":
          background_default.scheduleAssetUpdater({ now: true, fetchDelay: 100, auto: true });
          break;
        case "writeHiddenSettings":
          background_default.changeHiddenSettings(background_default.hiddenSettingsFromString(request.content));
          break;
        default:
          return vAPI.messaging.UNHANDLED;
      }
      callback(response);
    };
    registerMessagingListener({
      name: "dashboard",
      listener: onMessage,
      privileged: true
    });
  }
  {
    const extensionOriginURL = vAPI.getURL("");
    const documentBlockedURL = vAPI.getURL("document-blocked.html");
    const getLoggerData = async function(details, activeTabId, callback) {
      const response = {
        activeTabId,
        colorBlind: background_default.userSettings.colorBlindFriendly,
        entries: logger_default.readAll(details.ownerId),
        tabIdsToken: background_default.pageStoresToken,
        tooltips: background_default.userSettings.tooltipsDisabled === false
      };
      if (background_default.pageStoresToken !== details.tabIdsToken) {
        response.tabIds = [];
        for (const [tabId, pageStore] of background_default.pageStores) {
          const { rawURL, title } = pageStore;
          if (rawURL.startsWith(extensionOriginURL)) {
            if (rawURL.startsWith(documentBlockedURL) === false) {
              continue;
            }
          }
          response.tabIds.push([tabId, title]);
        }
      }
      if (activeTabId) {
        const pageStore = background_default.pageStoreFromTabId(activeTabId);
        const rawURL = pageStore && pageStore.rawURL;
        if (rawURL === null || rawURL.startsWith(extensionOriginURL) && rawURL.startsWith(documentBlockedURL) === false) {
          response.activeTabId = void 0;
        }
      }
      if (details.popupLoggerBoxChanged && vAPI.windows instanceof Object) {
        const tabs = await vAPI.tabs.query({
          url: vAPI.getURL("/logger-ui.html?popup=1")
        });
        if (tabs.length !== 0) {
          const win = await vAPI.windows.get(tabs[0].windowId);
          if (win === null) {
            return;
          }
          vAPI.localStorage.setItem("popupLoggerBox", JSON.stringify({
            left: win.left,
            top: win.top,
            width: win.width,
            height: win.height
          }));
        }
      }
      callback(response);
    };
    const getURLFilteringData = function(details) {
      const colors = {};
      const response = {
        dirty: false,
        colors
      };
      const suf = sessionURLFiltering;
      const puf = permanentURLFiltering;
      const urls = details.urls;
      const context = details.context;
      const type = details.type;
      for (const url of urls) {
        const colorEntry = colors[url] = { r: 0, own: false };
        if (suf.evaluateZ(context, url, type).r !== 0) {
          colorEntry.r = suf.r;
          colorEntry.own = suf.r !== 0 && suf.context === context && suf.url === url && suf.type === type;
        }
        if (response.dirty) {
          continue;
        }
        puf.evaluateZ(context, url, type);
        const pown = puf.r !== 0 && puf.context === context && puf.url === url && puf.type === type;
        response.dirty = colorEntry.own !== pown || colorEntry.r !== puf.r;
      }
      return response;
    };
    const onMessage = function(request, sender, callback) {
      switch (request.what) {
        case "readAll":
          if (logger_default.ownerId !== void 0 && logger_default.ownerId !== request.ownerId) {
            return callback({ unavailable: true });
          }
          vAPI.tabs.getCurrent().then((tab) => {
            getLoggerData(request, tab && tab.id, callback);
          });
          return;
        case "toggleInMemoryFilter": {
          const promise = background_default.hasInMemoryFilter(request.filter) ? background_default.removeInMemoryFilter(request.filter) : background_default.addInMemoryFilter(request.filter);
          promise.then((status) => {
            callback(status);
          });
          return;
        }
        default:
          break;
      }
      let response;
      switch (request.what) {
        case "hasInMemoryFilter":
          response = background_default.hasInMemoryFilter(request.filter);
          break;
        case "releaseView":
          if (request.ownerId !== logger_default.ownerId) {
            break;
          }
          logger_default.ownerId = void 0;
          background_default.clearInMemoryFilters();
          break;
        case "saveURLFilteringRules":
          response = permanentURLFiltering.copyRules(
            sessionURLFiltering,
            request.context,
            request.urls,
            request.type
          );
          if (response) {
            background_default.savePermanentURLFilteringRules();
          }
          break;
        case "setURLFilteringRule":
          background_default.toggleURLFilteringRule(request);
          break;
        case "getURLFilteringData":
          response = getURLFilteringData(request);
          break;
        default:
          return vAPI.messaging.UNHANDLED;
      }
      callback(response);
    };
    registerMessagingListener({
      name: "loggerUI",
      listener: onMessage,
      privileged: true
    });
  }
  {
    const onMessage = (request, sender, callback) => {
      switch (request.what) {
        default:
          break;
      }
      let response;
      switch (request.what) {
        case "getInspectorArgs": {
          const bc2 = new globalThis.BroadcastChannel("contentInspectorChannel");
          bc2.postMessage({
            what: "contentInspectorChannel",
            tabId: sender.tabId || 0,
            frameId: sender.frameId || 0
          });
          response = {
            inspectorURL: vAPI.getURL(
              `/web_accessible_resources/dom-inspector.html?secret=${getWarSecretShort()}`
            )
          };
          break;
        }
        default:
          return vAPI.messaging.UNHANDLED;
      }
      callback(response);
    };
    registerMessagingListener({
      name: "domInspectorContent",
      listener: onMessage,
      privileged: false
    });
  }
  {
    const onMessage = function(request, sender, callback) {
      const tabId = sender.tabId || 0;
      switch (request.what) {
        default:
          break;
      }
      let response;
      switch (request.what) {
        case "closeThisTab":
          vAPI.tabs.remove(tabId);
          break;
        case "temporarilyWhitelistDocument":
          traffic_default.strictBlockBypass(request.hostname);
          break;
        default:
          return vAPI.messaging.UNHANDLED;
      }
      callback(response);
    };
    registerMessagingListener({
      name: "documentBlocked",
      listener: onMessage,
      privileged: true
    });
  }
  {
    const onMessage = function(request, sender, callback) {
      switch (request.what) {
        case "purgeAllCaches":
          background_default.getBytesInUse().then(
            (bytesInUseBefore) => assets_default.remove(/./).then(
              () => background_default.getBytesInUse().then((bytesInUseAfter) => {
                callback([
                  `Storage used before: ${background_default.formatCount(bytesInUseBefore)}B`,
                  `Storage used after: ${background_default.formatCount(bytesInUseAfter)}B`
                ].join("\n"));
              })
            )
          );
          return;
        case "snfeBenchmark":
          Promise.all([
            import("/js/benchmarks.js").catch((e) => {
              console.warn("[uBR] messaging: benchmarks.js import failed", e);
              return { benchmarkStaticNetFiltering: async () => ({ error: "Benchmarks disabled" }) };
            })
          ]).then(([module]) => {
            if (module && typeof module.benchmarkStaticNetFiltering === "function") {
              module.benchmarkStaticNetFiltering({ redirectEngine }).then((result) => {
                callback(result);
              });
            } else {
              callback({ error: "Benchmarks not available" });
            }
          });
          return;
        case "cfeBenchmark":
          Promise.all([
            import("/js/benchmarks.js").catch((e) => {
              console.warn("[uBR] messaging: benchmarks.js import failed", e);
              return { benchmarkCosmeticFiltering: async () => ({ error: "Benchmarks disabled" }) };
            })
          ]).then(([module]) => {
            if (module && typeof module.benchmarkCosmeticFiltering === "function") {
              module.benchmarkCosmeticFiltering().then((result) => {
                callback(result);
              });
            } else {
              callback({ error: "Benchmarks not available" });
            }
          });
          return;
        case "sfeBenchmark":
          Promise.all([
            import("/js/benchmarks.js").catch((e) => {
              console.warn("[uBR] messaging: benchmarks.js import failed", e);
              return { benchmarkScriptletFiltering: async () => ({ error: "Benchmarks disabled" }) };
            })
          ]).then(([module]) => {
            if (module && typeof module.benchmarkScriptletFiltering === "function") {
              module.benchmarkScriptletFiltering().then((result) => {
                callback(result);
              });
            } else {
              callback({ error: "Benchmarks not available" });
            }
          });
          return;
        case "snfeToDNR": {
          const listPromises = [];
          const listNames = [];
          for (const assetKey of background_default.selectedFilterLists) {
            listPromises.push(
              assets_default.get(assetKey, { dontCache: true }).then((details) => {
                listNames.push(assetKey);
                return {
                  name: assetKey,
                  text: details.content,
                  trustedSource: assetKey.startsWith("ublock-") || assetKey === background_default.userFiltersPath && background_default.userSettings.userFiltersTrusted
                };
              })
            );
          }
          const options = {
            extensionPaths: redirectEngine.getResourceDetails().filter(
              (e) => typeof e[1].extensionPath === "string" && e[1].extensionPath !== ""
            ).map(
              (e) => [e[0], e[1].extensionPath]
            ),
            env: vAPI.webextFlavor.env
          };
          Promise.resolve().then(() => (init_static_dnr_filtering(), static_dnr_filtering_exports)).then((module) => {
            const t0 = Date.now();
            module.dnrRulesetFromRawLists(listPromises, options).then((dnrdata) => {
              dnrdata.listNames = listNames;
              dnrdata.runtime = Date.now() - t0;
              callback(serialize(dnrdata));
            });
          }).catch((reason) => {
            console.warn("[uBR] dnrModule: dnrRulesetFromRawLists call failed", reason);
            callback(reason);
          });
          return;
        }
        default:
          break;
      }
      let response;
      switch (request.what) {
        case "snfeDump":
          response = static_net_filtering_default.dump();
          break;
        case "snfeQuery":
          response = static_net_filtering_default.test(
            Object.assign({ redirectEngine }, request.query)
          );
          break;
        default:
          return vAPI.messaging.UNHANDLED;
      }
      callback(response);
    };
    registerMessagingListener({
      name: "devTools",
      listener: onMessage,
      privileged: true
    });
  }
  {
    const logCosmeticFilters = function(tabId, details) {
      if (logger_default.enabled === false) {
        return;
      }
      const filter = { source: "cosmetic", raw: "" };
      const fctxt = background_default.filteringContext.duplicate();
      fctxt.fromTabId(tabId).setRealm("cosmetic").setType("dom").setURL(details.frameURL).setDocOriginFromURL(details.frameURL).setFilter(filter);
      for (const selector of details.matchedSelectors.sort()) {
        filter.raw = selector;
        fctxt.toLogger();
      }
    };
    const logCSPViolations = function(pageStore, request) {
      if (logger_default.enabled === false || pageStore === null) {
        return false;
      }
      if (request.violations.length === 0) {
        return true;
      }
      const fctxt = background_default.filteringContext.duplicate();
      fctxt.fromTabId(pageStore.tabId).setRealm("network").setDocOriginFromURL(request.docURL).setURL(request.docURL);
      let cspData = pageStore.extraData.get("cspData");
      if (cspData === void 0) {
        cspData = /* @__PURE__ */ new Map();
        const staticDirectives = static_net_filtering_default.matchAndFetchModifiers(fctxt, "csp");
        if (staticDirectives !== void 0) {
          for (const directive of staticDirectives) {
            if (directive.result !== 1) {
              continue;
            }
            cspData.set(directive.value, directive.logData());
          }
        }
        fctxt.type = "inline-script";
        fctxt.filter = void 0;
        if (pageStore.filterRequest(fctxt) === 1) {
          cspData.set(background_default.cspNoInlineScript, fctxt.filter);
        }
        fctxt.type = "script";
        fctxt.filter = void 0;
        if (pageStore.filterScripting(fctxt, true) === 1) {
          cspData.set(background_default.hiddenSettings.noScriptingCSP, fctxt.filter);
        }
        fctxt.type = "inline-font";
        fctxt.filter = void 0;
        if (pageStore.filterRequest(fctxt) === 1) {
          cspData.set(background_default.cspNoInlineFont, fctxt.filter);
        }
        if (cspData.size === 0) {
          return false;
        }
        pageStore.extraData.set("cspData", cspData);
      }
      const typeMap = logCSPViolations.policyDirectiveToTypeMap;
      for (const json of request.violations) {
        const violation = JSON.parse(json);
        let type = typeMap.get(violation.directive);
        if (type === void 0) {
          continue;
        }
        const logData = cspData.get(violation.policy);
        if (logData === void 0) {
          continue;
        }
        if (/^[\w.+-]+:\/\//.test(violation.url) === false) {
          violation.url = request.docURL;
          if (type === "script") {
            type = "inline-script";
          } else if (type === "font") {
            type = "inline-font";
          }
        }
        logData.modifier = void 0;
        fctxt.setURL(violation.url).setType(type).setFilter(logData).toLogger();
      }
      return true;
    };
    logCSPViolations.policyDirectiveToTypeMap = /* @__PURE__ */ new Map([
      ["img-src", "image"],
      ["connect-src", "xmlhttprequest"],
      ["font-src", "font"],
      ["frame-src", "sub_frame"],
      ["media-src", "media"],
      ["object-src", "object"],
      ["script-src", "script"],
      ["script-src-attr", "script"],
      ["script-src-elem", "script"],
      ["style-src", "stylesheet"],
      ["style-src-attr", "stylesheet"],
      ["style-src-elem", "stylesheet"]
    ]);
    const onMessage = function(request, sender, callback) {
      const tabId = sender.tabId || 0;
      const pageStore = background_default.pageStoreFromTabId(tabId);
      switch (request.what) {
        default:
          break;
      }
      let response;
      switch (request.what) {
        case "inlinescriptFound":
          if (logger_default.enabled && pageStore !== null) {
            const fctxt = background_default.filteringContext.duplicate();
            fctxt.fromTabId(tabId).setType("inline-script").setURL(request.docURL).setDocOriginFromURL(request.docURL);
            if (pageStore.filterRequest(fctxt) === 0) {
              fctxt.setRealm("network").toLogger();
            }
          }
          break;
        case "logCosmeticFilteringData":
          logCosmeticFilters(tabId, request);
          break;
        case "securityPolicyViolation":
          response = logCSPViolations(pageStore, request);
          break;
        case "temporarilyAllowLargeMediaElement":
          if (pageStore !== null) {
            pageStore.allowLargeMediaElementsUntil = Date.now() + 5e3;
          }
          break;
        case "subscribeTo": {
          if (/^(file|https?):\/\//.test(request.location) === false) {
            break;
          }
          const url = encodeURIComponent(request.location);
          const title = encodeURIComponent(request.title);
          const hash = background_default.selectedFilterLists.indexOf(request.location) !== -1 ? "#subscribed" : "";
          vAPI.tabs.open({
            url: `/asset-viewer.html?url=${url}&title=${title}&subscribe=1${hash}`,
            select: true
          });
          break;
        }
        case "updateLists": {
          const listkeys = request.listkeys.split(",").filter((s) => s !== "");
          if (listkeys.length === 0) {
            return;
          }
          if (listkeys.includes("all")) {
            assets_default.purge(/./, "public_suffix_list.dat");
          } else {
            for (const listkey of listkeys) {
              assets_default.purge(listkey);
            }
          }
          background_default.openNewTab({
            url: "dashboard.html#3p-filters.html",
            select: true
          });
          background_default.scheduleAssetUpdater({ now: true, fetchDelay: 100, auto: request.auto });
          break;
        }
        default:
          return vAPI.messaging.UNHANDLED;
      }
      callback(response);
    };
    registerMessagingListener({
      name: "scriptlets",
      listener: onMessage
    });
  }
})();
/*! Home: https://github.com/gorhill/publicsuffixlist.js -- GPLv3 APLv2 */
/*! https://mths.be/punycode v1.3.2 by @mathias */
